# dependencies
node_modules/
.pnpm-store/

# next.js
.next/
out/
next-env.d.ts

# production / vercel
.vercel/

# python (vision-service)
__pycache__/
*.pyc
.venv/
.env

# vision-service ML models (downloaded on-demand by probe scripts)
vision-service/models/*.task
vision-service/scripts/output/
apps/web/scripts/output/
# Phase 07.1.6 D-06: commit only the golden fixture; outros outputs continuam ignored.
# Note: parent dir é ignored, mas re-incluímos com pattern de file específico — git
# precisa do parent NÃO-ignored para a negação funcionar, então usamos /* no parent
# e negate apenas o golden. Crops/ dirs continuam ignored via /* wildcard.
!apps/web/scripts/output/
apps/web/scripts/output/*
!apps/web/scripts/output/landmarks-probe-sonnet-4-6.json

# environment
.env.local
.env*.local
.env

# macOS / IDE
.DS_Store
.idea/
.vscode/
*.log

# supabase
supabase/.branches
supabase/.temp
