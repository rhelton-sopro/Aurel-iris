# Core deps from SPEC §4.2 (Modal vision pipeline) — Phase 5 finalized in 05-10.
# `supabase` removed per RESEARCH Anti-Pattern + D-T6: Modal container must NOT
# carry a service-role key. Pipeline uses signed URLs only.
modal>=1.4.2
opencv-python-headless==4.13.0.92
mediapipe==0.10.35
numpy>=1.26.0
scikit-image>=0.24.0
torch==2.11.0
torchvision
Pillow>=10.4.0
pydantic==2.13.3
httpx>=0.27

# Testing
pytest>=8.0

# Phase 6 — RAG ingestion
voyageai==0.3.7
PyMuPDF==1.27.2.3
pdfplumber==0.11.9
docx2txt==0.8
tiktoken>=0.7,<0.10
supabase>=2.5,<3.0
psycopg2-binary>=2.9
anthropic>=0.40
