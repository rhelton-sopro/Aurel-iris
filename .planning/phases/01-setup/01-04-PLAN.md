---
phase: 01-setup
plan: 04
type: execute
wave: 3
depends_on:
  - "01-03"
files_modified:
  - apps/web/types/database.ts
  - apps/web/package.json
autonomous: true
requirements:
  - SETUP-03
must_haves:
  truths:
    - "Schema da migration 0001 está aplicado no projeto Supabase remoto sa-east-1 (replica do estado local validado em plan 1.3)."
    - "apps/web/types/database.ts é gerado automaticamente via supabase gen types typescript --linked e contém os types das 6 tabelas (D-09)."
    - "pnpm gen:types em apps/web/ regenera o arquivo a partir do remoto (script real, não mais o stub do plan 1.1)."
    - "tsc --noEmit em apps/web/ continua passando após a importação dos types gerados."
  artifacts:
    - path: "apps/web/types/database.ts"
      provides: "Tipos TS gerados a partir do schema remoto Supabase (Database type, Tables, Row, Insert, Update)"
      contains: "export type Database"
      min_lines: 100
    - path: "apps/web/package.json"
      provides: "Script gen:types com comando real supabase gen types"
      contains: "supabase gen types"
  key_links:
    - from: "apps/web/types/database.ts"
      to: "remote Supabase project (sa-east-1)"
      via: "supabase gen types typescript --linked"
      pattern: "supabase gen types"
---

<objective>
**[BLOQUEANTE — schema_push_requirement]** Aplicar a migration 0001 ao projeto Supabase remoto via `supabase db push` e gerar `apps/web/types/database.ts` (D-09) a partir do schema remoto.

Propósito: Esta é a única plan que escreve no DB de produção do MVP. Sem este push, plan 1.5 (RLS test) e plan 1.6 (Vercel deploy + dogfooding em apps/web/) não têm DB real. **Build e type-check do plan 1.1 passam com `database.ts` ausente** (placeholder em `types/.gitkeep`); essa é exatamente a falha de verificação que `schema_push_requirement` no kickoff alertou. Por isso este plan é não-pulável.

Output: Schema do SPEC §3 vivo no DB remoto sa-east-1, `database.ts` gerado e committed, script `pnpm gen:types` repetível para futuras migrations.

Pré-requisito: plan 1.3 deve ter linkado o projeto remoto via `supabase link`. Se `SUPABASE_ACCESS_TOKEN` está setado (env do executor — fundador deve setar antes de rodar este plan), `supabase db push --linked` corre não-interativo. Caso contrário, a CLI vai pedir interação para confirmar diff e password.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-setup/01-CONTEXT.md
@.planning/phases/01-setup/01-03-PLAN.md
@apps/web/package.json
@supabase/migrations/0001_initial_schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] supabase db push --linked + gerar database.ts</name>
  <files>
    apps/web/types/database.ts,
    apps/web/package.json
  </files>
  <read_first>
    - D:/GDrive/iridologista/.planning/phases/01-setup/01-03-PLAN.md (confirma que plan 1.3 já linkou o projeto)
    - D:/GDrive/iridologista/.planning/phases/01-setup/01-03-SUMMARY.md (project-ref do remoto)
    - D:/GDrive/iridologista/supabase/migrations/0001_initial_schema.sql (o que vai para o remoto)
    - D:/GDrive/iridologista/apps/web/package.json (atual; vamos substituir o script `gen:types` pelo comando real)
  </read_first>
  <action>
    Working directory: `D:/GDrive/iridologista/`.

    **Pré-requisitos do executor:**
    - Plan 1.3 finalizado (projeto linkado).
    - `SUPABASE_ACCESS_TOKEN` setado no env do shell (de https://supabase.com/dashboard/account/tokens) **OU** o executor está pronto para responder o prompt da CLI interativamente.

    **1) Aplicar migration ao remoto (BLOCKING):**
    ```bash
    cd D:/GDrive/iridologista

    # Verifica que está linkado
    supabase projects list
    # Deve mostrar aurel-iris LINKED em sa-east-1.

    # Sanity check: dry-run do diff antes de push
    supabase db diff --linked
    # Saída esperada: lista as 6 tabelas/índices/policies que vão ser criadas.
    # Se aparecer DROP de algo, abortar — algo está errado.

    # Push real (BLOCKING)
    supabase db push --linked
    # Vai pedir confirmação (interativa) ou usa SUPABASE_ACCESS_TOKEN se setado.
    # Aplica 0001_initial_schema.sql no DB remoto e registra em supabase_migrations.schema_migrations.
    ```

    **Se `supabase db push --linked` falhar:**
    - Erro `pgvector extension not available` -> ir ao Dashboard do Supabase, Database -> Extensions -> habilitar `vector`. Re-rodar push.
    - Erro de password -> `supabase link --project-ref <ref>` de novo, fornecendo password do passo plan 1.3.
    - Qualquer outro erro -> não inventar fix; abortar e logar a saída completa para o fundador resolver.

    **2) Sanity check do remoto:**
    ```bash
    # Lista migrations aplicadas no remoto
    supabase migration list --linked
    # Deve mostrar 0001_initial_schema marcado como APPLIED no remoto.

    # Pode-se verificar via psql contra a connection string remota também (Dashboard -> Settings -> Database -> Connection string),
    # mas plan 1.5 vai cobrir isso com mais profundidade (RLS test).
    ```

    **3) Gerar `apps/web/types/database.ts` (D-09):**
    ```bash
    cd D:/GDrive/iridologista
    rm -f apps/web/types/.gitkeep   # remove o placeholder do plan 1.1

    # Gera types do schema remoto e salva no path canônico
    supabase gen types typescript --linked > apps/web/types/database.ts

    # Sanity check do conteúdo
    head -5 apps/web/types/database.ts
    # Esperado: começa com algo como "export type Json = ..." ou "export interface Database {"

    grep -c "profiles\|clients\|readings\|reading_images\|knowledge_chunks\|subscriptions" apps/web/types/database.ts
    # Esperado: ≥ 6 (cada tabela aparece ao menos uma vez)
    ```

    **4) Atualizar `apps/web/package.json`** — substituir o stub do plan 1.1 pelo comando real.

    No `apps/web/package.json`, substituir a linha do script `gen:types`:
    ```json
    "gen:types": "echo 'use plan 1.4 supabase gen types command' && exit 1"
    ```
    por:
    ```json
    "gen:types": "supabase gen types typescript --linked > types/database.ts"
    ```

    Nota: este script roda relativo a `apps/web/` (cwd). O `--linked` resolve a partir do `.supabase/` no root, que está acima do cwd — supabase CLI sobe na árvore para encontrar o link state. Se isso não funcionar do `apps/web/`, ajustar para rodar do root via `cd .. && supabase gen types ... > apps/web/types/database.ts`.

    **5) Validar build após inserir os types:**
    ```bash
    cd D:/GDrive/iridologista/apps/web
    pnpm exec tsc --noEmit
    # Deve passar sem erro. Os types em database.ts ainda não são importados em lugar nenhum
    # (UI consumirá em Fase 2), mas devem ser sintaticamente válidos.
    ```

    **6) Auditoria final:** confirmar que `apps/web/types/database.ts` é não-trivial.
    ```bash
    wc -l apps/web/types/database.ts
    # Esperado: ≥ 100 linhas (6 tabelas * Row/Insert/Update + helpers)
    ```
  </action>
  <verify>
    <automated>cd D:/GDrive/iridologista && supabase migration list --linked > /tmp/mlist.out 2>&1 && grep -q "0001_initial_schema" /tmp/mlist.out && test -f apps/web/types/database.ts && [ "$(wc -l < apps/web/types/database.ts)" -ge 100 ] && grep -q "profiles" apps/web/types/database.ts && grep -q "clients" apps/web/types/database.ts && grep -q "readings" apps/web/types/database.ts && grep -q "reading_images" apps/web/types/database.ts && grep -q "knowledge_chunks" apps/web/types/database.ts && grep -q "subscriptions" apps/web/types/database.ts && grep -q "supabase gen types" apps/web/package.json && ! grep -q "use plan 1.4 supabase gen types command" apps/web/package.json && cd apps/web && pnpm exec tsc --noEmit && echo "TYPECHECK_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase migration list --linked` (exit code 0) cuja saída capturada (stdout+stderr em /tmp/mlist.out) contém `0001_initial_schema`. A separação `&&` + grep no arquivo capturado garante que (a) falhas do supabase CLI não são mascaradas, (b) o grep só vê o output real, não mensagens de erro que casualmente mencionem `0001_initial_schema`.
    - Arquivo `apps/web/types/database.ts` existe e tem ≥ 100 linhas.
    - `apps/web/types/database.ts` contém menções literais a `profiles`, `clients`, `readings`, `reading_images`, `knowledge_chunks`, `subscriptions` (cada nome de tabela aparece pelo menos uma vez).
    - `apps/web/package.json` script `gen:types` agora contém literalmente `supabase gen types`.
    - `apps/web/package.json` script `gen:types` NÃO contém mais o stub `use plan 1.4 supabase gen types command`.
    - `pnpm exec tsc --noEmit` em `apps/web/` retorna 0 após a geração do `database.ts`.
    - Arquivo `apps/web/types/.gitkeep` removido (substituído por `database.ts`).
    - Vocabulário compliant: zero ocorrências de `diagnóstico|tratamento|cura` (case-insensitive) em `database.ts` (deve ser óbvio — é tipo gerado, mas defensive check).
  </acceptance_criteria>
  <done>
    Schema do SPEC §3 está vivo em sa-east-1, types TS estão sincronizados via D-09, e `pnpm gen:types` é repetível para todas as próximas migrations. Plan 1.5 (RLS test) e plan 1.6 (Vercel deploy) podem agora consumir o DB real.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → remote Supabase DB | Push de DDL via `supabase db push --linked`. Autenticação via `supabase login` (browser OAuth do fundador) ou `SUPABASE_ACCESS_TOKEN`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11 | T (Tampering) | DDL applied to remote | mitigate | `supabase db diff --linked` corrido antes do push para confirmar que o diff aplicado é exatamente o esperado (apenas CREATEs do 0001, sem DROPs). |
| T-01-12 | I (Info Disclosure) | database.ts em git | accept | Tipos do schema são públicos por construção (tabela names + column types). Não contém credenciais nem dados de produção. Risco zero. |
| T-01-13 | D (Denial of Service) | push contra remoto | accept | Operação é DDL transacional — falha cancela tudo. Sem usuários reais ainda, downtime é irrelevante. |
</threat_model>

<verification>
1. `supabase migration list --linked` mostra `0001_initial_schema` APPLIED.
2. Dashboard Supabase (https://supabase.com/dashboard/project/<ref>/database/tables) mostra as 6 tabelas em `public`.
3. `apps/web/types/database.ts` foi gerado e tem types para as 6 tabelas (Row/Insert/Update).
4. `pnpm exec tsc --noEmit` em apps/web/ passa.
5. `pnpm gen:types` é executável e regenera o arquivo (idempotente).
</verification>

<success_criteria>
- DB remoto sa-east-1 contém schema completo do SPEC §3.
- Types TypeScript sincronizados (D-09).
- Pipeline `migration -> push -> gen:types` é repetível e versionado.
- Plan 1.5 e 1.6 podem agora rodar contra o DB real.
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-setup/01-04-SUMMARY.md` documentando:
- Output completo de `supabase db push --linked` (para auditoria — primeiro DDL do projeto).
- Tamanho final em linhas de `apps/web/types/database.ts`.
- Confirmação de que `pnpm gen:types` regenera idempotente.
- Próximo plan: 1.5 (RLS test em remoto, Wave 4) e 1.6 (Vercel deploy, Wave 4) podem rodar paralelo.
</output>
