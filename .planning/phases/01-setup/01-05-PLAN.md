---
phase: 01-setup
plan: 05
type: execute
wave: 4
depends_on:
  - "01-04"
files_modified:
  - supabase/tests/cross_therapist_rls.sql
  - supabase/tests/.gitkeep
autonomous: true
requirements:
  - SETUP-04
must_haves:
  truths:
    - "Existe um teste SQL versionado em supabase/tests/cross_therapist_rls.sql que cria 2 perfis dummy, 1 cliente e 1 reading para cada, e tenta cross-leitura usando dois auth.uid() distintos via set_config canônico do Supabase."
    - "Teste executa contra o DB Supabase REMOTO linkado (sa-east-1) — não local — porque o phase goal de Fase 1 é sobre o estado do remoto após o push do plan 1.4."
    - "Teste tem control block (BYPASSRLS via postgres role) provando que a fixture realmente inseriu 2 clients antes de RLS ser aplicada — sem isso, um falso PASS é possível se inserts falharem silenciosamente."
    - "Teste tem own-data assertion (terapeuta lê próprio cliente = 1 row) E cross-therapist assertion (terapeuta lê cliente alheio = 0 rows) — sem own-data, RLS pode estar bloqueando tudo (false PASS)."
    - "Teste falha verboso (RAISE EXCEPTION) se qualquer assertion divergir; psql exit != 0 sinaliza falha."
    - "Cobertura: clients (SPEC §3 verbatim), readings (SPEC §3 verbatim), reading_images (D-12 adicional), profiles (D-12 adicional)."
  artifacts:
    - path: "supabase/tests/cross_therapist_rls.sql"
      provides: "Teste SQL versionado de RLS cross-terapeuta com control + own-data + cross-therapist assertions, executado contra remoto (D-13)"
      contains: "set_config('request.jwt.claims'"
      min_lines: 100
  key_links:
    - from: "supabase/tests/cross_therapist_rls.sql"
      to: "supabase/migrations/0001_initial_schema.sql (aplicada em remoto via plan 1.4)"
      via: "Testa as policies criadas pela migration, executando contra o DB remoto"
      pattern: "auth.uid"
---

<objective>
Implementar o teste de verificação de RLS cross-terapeuta exigido por D-13 do CONTEXT.md, em formato SQL (escolha do planner per Claude's Discretion), executando contra o **DB remoto** (sa-east-1) — não local.

Propósito: SETUP-04 não está fechado se a RLS estiver "configurada mas não verificada". O ROADMAP §1 success criterion #4 fala literalmente "tentativa de leitura cross-terapeuta (com dois `auth.uid()` distintos) é bloqueada por RLS." O phase goal explicitamente diz "banco Supabase com schema completo (...) aplicado" — a garantia é sobre o **remoto** após push (plan 1.4). Validação local já acontece transitivamente em plan 1.3 (`supabase db reset` aplica a mesma migration localmente). Esta plan produz a evidência empírica do remoto.

Output: Arquivo `supabase/tests/cross_therapist_rls.sql` que: (a) cria 2 usuários auth dummy + 2 profiles + 2 clients + 2 readings + 2 reading_images, (b) **control block** (BYPASSRLS, role postgres): assert que `select count(*) from clients` = 2 — prova que fixture inseriu corretamente antes da impersonação, (c) impersona cada `auth.uid()` via `set_config('request.jwt.claims', json_build_object(...)::text, true)` (forma canônica do Supabase), (d) **own-data assertion**: terapeuta A lê seu próprio cliente = 1 row (RLS não está bloqueando indiscriminadamente), (e) **cross-therapist assertion**: terapeuta A lê cliente de B = 0 rows (RLS está bloqueando o que precisa), (f) usa `assert` SQL (RAISE EXCEPTION) se qualquer assertion divergir.

**Por que SQL (não TS):** D-13 dá liberdade de escolha; SQL é (1) mais próximo da fonte da verdade (RLS é Postgres feature), (2) não requer Vitest/Jest setup adicional na Fase 1 (Deferred Idea: test runner formal), (3) roda direto via `psql -f` contra a connection string remota, (4) versionado junto com migrations no mesmo `supabase/`.

**Esta plan depende de plan 1.4** (push remoto): Wave 4. O migration tem que estar aplicada no remoto antes desta plan rodar contra ele. Pode rodar paralelo a plan 1.6 em Wave 4 (1.6 deploya Vercel; não tem conflito de arquivo nem ordem).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-setup/01-CONTEXT.md
@.planning/phases/01-setup/01-03-PLAN.md
@.planning/phases/01-setup/01-04-PLAN.md
@supabase/migrations/0001_initial_schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escrever cross_therapist_rls.sql + executar contra DB remoto linkado</name>
  <files>
    supabase/tests/cross_therapist_rls.sql,
    supabase/tests/.gitkeep
  </files>
  <read_first>
    - D:/Projetos/Iridologista/supabase/migrations/0001_initial_schema.sql (todas as 6 policies — o teste deve cobrir cada uma)
    - D:/Projetos/Iridologista/.planning/phases/01-setup/01-CONTEXT.md (D-13 — formato esperado)
    - D:/Projetos/Iridologista/.planning/ROADMAP.md (Fase 1 success criterion #4)
    - D:/Projetos/Iridologista/.planning/phases/01-setup/01-04-SUMMARY.md (se existir; confirma que push remoto rodou)
  </read_first>
  <action>
    Working directory: `D:/Projetos/Iridologista/`.

    **Pré-requisito:** Plan 1.4 finalizada (migration 0001 aplicada no remoto; `supabase migration list --linked` mostra `0001_initial_schema` APPLIED). Repo linkado (plan 1.3). `SUPABASE_DB_URL` disponível para conexão remota via `psql`.

    **1) Obter a connection string do DB remoto.** Há duas formas equivalentes — escolher a que estiver disponível no env do executor:

    **(a) Via env var pré-setada pelo fundador:**
    ```bash
    # O fundador deve setar SUPABASE_DB_URL no shell antes de rodar este plan.
    # Forma do dump em Dashboard -> Settings -> Database -> Connection string -> URI
    # postgresql://postgres.<project-ref>:<password>@aws-0-sa-east-1.pooler.supabase.com:6543/postgres
    echo "${SUPABASE_DB_URL:?SUPABASE_DB_URL nao setado}"
    ```

    **(b) Via supabase CLI (se a versão local tiver `supabase db dump --db-url`):**
    ```bash
    SUPABASE_DB_URL=$(supabase db dump --db-url --linked 2>/dev/null) || true
    if [ -z "$SUPABASE_DB_URL" ]; then
      echo "Forneça manualmente: export SUPABASE_DB_URL=postgresql://postgres.<ref>:<password>@aws-0-sa-east-1.pooler.supabase.com:6543/postgres"
      exit 1
    fi
    ```

    Se nem (a) nem (b) funcionar, o executor deve abortar e pedir ao fundador para colar a connection string como env var antes de re-executar.

    **2) Criar `supabase/tests/cross_therapist_rls.sql`:**

    Este teste roda como um único bloco transacional. Usa o mecanismo canônico do Supabase para impersonar JWT claims (`set_config('request.jwt.claims', json_build_object('sub', '<uuid>', 'role', 'authenticated')::text, true)`), que é exatamente como a RLS é avaliada em produção (auth.uid() lê de `request.jwt.claims`->>'sub'; auth.role() lê de `request.jwt.claims`->>'role').

    Conteúdo a gravar literalmente:

    ```sql
    -- supabase/tests/cross_therapist_rls.sql
    -- D-13: prova empiricamente que RLS bloqueia leitura cross-terapeuta no DB REMOTO (sa-east-1).
    -- Roda com: psql "$SUPABASE_DB_URL" -v ON_ERROR_STOP=1 -f supabase/tests/cross_therapist_rls.sql
    --
    -- Estratégia (3 níveis de assertion):
    --   1) FIXTURE: Inserir 2 usuários auth dummy + profiles + 1 client/reading/image cada (via service-role/postgres bypass).
    --   2) CONTROL (BYPASSRLS, role postgres): assert que select count(*) from clients = 2.
    --      Sem isso, um falso PASS é possível se inserts falharem silenciosamente em RLS-on tables.
    --   3) Para cada terapeuta, impersonar via set_config('request.jwt.claims', json_build_object('sub', uuid, 'role', 'authenticated')::text, true) + role=authenticated.
    --   4) OWN-DATA: assert que terapeuta A lê seu próprio cliente = 1 row.
    --      Sem isso, "RLS bloqueia tudo" passa (false PASS); este check distingue "RLS funciona" de "RLS está aterrorizada".
    --   5) CROSS-THERAPIST: assert que terapeuta A lê cliente de B = 0 rows. Espelho para B->A.
    --   6) Cobertura: clients, readings, reading_images, profiles.
    --   7) Toda inserção/teste num BEGIN; ... ROLLBACK; — DB volta ao estado anterior (idempotente, seguro pra remoto).

    begin;

    -- IDs determinísticos (UUIDs fixos para reprodutibilidade)
    -- Therapist A: 11111111-1111-1111-1111-111111111111
    -- Therapist B: 22222222-2222-2222-2222-222222222222

    -- Limpa estado de runs anteriores (idempotente). Roda como postgres (BYPASSRLS aqui).
    delete from reading_images where reading_id in (
      select id from readings where therapist_id in (
        '11111111-1111-1111-1111-111111111111',
        '22222222-2222-2222-2222-222222222222'
      )
    );
    delete from readings where therapist_id in (
      '11111111-1111-1111-1111-111111111111',
      '22222222-2222-2222-2222-222222222222'
    );
    delete from clients where therapist_id in (
      '11111111-1111-1111-1111-111111111111',
      '22222222-2222-2222-2222-222222222222'
    );
    delete from profiles where id in (
      '11111111-1111-1111-1111-111111111111',
      '22222222-2222-2222-2222-222222222222'
    );
    delete from auth.users where id in (
      '11111111-1111-1111-1111-111111111111',
      '22222222-2222-2222-2222-222222222222'
    );

    -- 1) FIXTURE: 2 auth.users dummy (mínimo para FK profiles.id -> auth.users.id).
    -- Em remoto, postgres role tem privilégio para inserir em auth.users (operação service-side).
    insert into auth.users (id, email, instance_id, aud, role, created_at, updated_at)
    values
      ('11111111-1111-1111-1111-111111111111', 'therapist-a@aurel-iris-test.local', '00000000-0000-0000-0000-000000000000', 'authenticated', 'authenticated', now(), now()),
      ('22222222-2222-2222-2222-222222222222', 'therapist-b@aurel-iris-test.local', '00000000-0000-0000-0000-000000000000', 'authenticated', 'authenticated', now(), now());

    -- 2) Profiles
    insert into profiles (id, full_name) values
      ('11111111-1111-1111-1111-111111111111', 'Terapeuta A'),
      ('22222222-2222-2222-2222-222222222222', 'Terapeuta B');

    -- 3) Cada terapeuta tem 1 cliente
    insert into clients (id, therapist_id, full_name) values
      ('aaaaaaaa-1111-1111-1111-111111111111', '11111111-1111-1111-1111-111111111111', 'Cliente do Terapeuta A'),
      ('bbbbbbbb-2222-2222-2222-222222222222', '22222222-2222-2222-2222-222222222222', 'Cliente do Terapeuta B');

    -- 4) Cada terapeuta tem 1 reading
    insert into readings (id, client_id, therapist_id, status) values
      ('cccccccc-1111-1111-1111-111111111111', 'aaaaaaaa-1111-1111-1111-111111111111', '11111111-1111-1111-1111-111111111111', 'pending'),
      ('dddddddd-2222-2222-2222-222222222222', 'bbbbbbbb-2222-2222-2222-222222222222', '22222222-2222-2222-2222-222222222222', 'pending');

    -- 5) Cada reading tem 1 imagem
    insert into reading_images (reading_id, eye, angle, storage_path) values
      ('cccccccc-1111-1111-1111-111111111111', 'right', 'frontal', 'therapist-a/reading-c/right-frontal.jpg'),
      ('dddddddd-2222-2222-2222-222222222222', 'right', 'frontal', 'therapist-b/reading-d/right-frontal.jpg');

    -- ===========================================================
    -- CONTROL: roda como postgres (BYPASSRLS) e prova que fixture
    -- realmente inseriu 2 clients. Se RLS já estivesse engajada para postgres,
    -- isso falharia e sinalizaria que o ambiente de teste está quebrado
    -- (ex.: postgres role em remoto sem BYPASSRLS por algum motivo).
    -- ===========================================================

    do $$
    declare
      cnt_clients_total integer;
      cnt_readings_total integer;
    begin
      select count(*) into cnt_clients_total from clients
        where therapist_id in (
          '11111111-1111-1111-1111-111111111111',
          '22222222-2222-2222-2222-222222222222'
        );
      if cnt_clients_total <> 2 then
        raise exception 'CONTROL FAIL: fixture nao inseriu 2 clients (postgres BYPASSRLS view). Esperado 2, viu %. Ambiente de teste quebrado.', cnt_clients_total;
      end if;

      select count(*) into cnt_readings_total from readings
        where therapist_id in (
          '11111111-1111-1111-1111-111111111111',
          '22222222-2222-2222-2222-222222222222'
        );
      if cnt_readings_total <> 2 then
        raise exception 'CONTROL FAIL: fixture nao inseriu 2 readings (postgres BYPASSRLS view). Esperado 2, viu %.', cnt_readings_total;
      end if;

      raise notice 'CONTROL PASS: fixture inseriu 2 clients e 2 readings (verificado com BYPASSRLS).';
    end $$;

    -- ===========================================================
    -- IMPERSONA TERAPEUTA A E TENTA LER DADOS DE B
    -- (set_config canônico Supabase: request.jwt.claims = JSON com sub + role)
    -- ===========================================================

    select set_config(
      'request.jwt.claims',
      json_build_object(
        'sub', '11111111-1111-1111-1111-111111111111',
        'role', 'authenticated'
      )::text,
      true
    );
    set local role authenticated;

    do $$
    declare
      cnt_clients_a_self integer;
      cnt_clients_b integer;
      cnt_readings_a_self integer;
      cnt_readings_b integer;
      cnt_images_b integer;
      cnt_profiles_b integer;
    begin
      -- OWN-DATA: terapeuta A deve ler o próprio cliente (sanity positivo).
      -- Sem isso, "RLS bloqueia tudo indiscriminadamente" passa silenciosamente.
      select count(*) into cnt_clients_a_self from clients
        where therapist_id = '11111111-1111-1111-1111-111111111111';
      if cnt_clients_a_self <> 1 then
        raise exception 'OWN-DATA FAIL: Terapeuta A nao consegue ler proprio cliente. Esperado 1, viu %. RLS bloqueando demais.', cnt_clients_a_self;
      end if;

      select count(*) into cnt_readings_a_self from readings
        where therapist_id = '11111111-1111-1111-1111-111111111111';
      if cnt_readings_a_self <> 1 then
        raise exception 'OWN-DATA FAIL: Terapeuta A nao consegue ler proprio reading. Esperado 1, viu %.', cnt_readings_a_self;
      end if;

      -- CROSS-THERAPIST: terapeuta A NAO deve ler nada de B.
      select count(*) into cnt_clients_b from clients
        where therapist_id = '22222222-2222-2222-2222-222222222222';
      if cnt_clients_b <> 0 then
        raise exception 'RLS FAIL: Terapeuta A leu % cliente(s) de B em clients', cnt_clients_b;
      end if;

      select count(*) into cnt_readings_b from readings
        where therapist_id = '22222222-2222-2222-2222-222222222222';
      if cnt_readings_b <> 0 then
        raise exception 'RLS FAIL: Terapeuta A leu % reading(s) de B em readings', cnt_readings_b;
      end if;

      select count(*) into cnt_images_b from reading_images
        where reading_id = 'dddddddd-2222-2222-2222-222222222222';
      if cnt_images_b <> 0 then
        raise exception 'RLS FAIL: Terapeuta A leu % imagem(ns) do reading de B em reading_images', cnt_images_b;
      end if;

      select count(*) into cnt_profiles_b from profiles
        where id = '22222222-2222-2222-2222-222222222222';
      if cnt_profiles_b <> 0 then
        raise exception 'RLS FAIL: Terapeuta A leu o profile de B (count = %)', cnt_profiles_b;
      end if;

      raise notice 'PASS: Terapeuta A le proprio cliente/reading (1+1) e e bloqueado em todas as 4 tabelas vs B (0+0+0+0).';
    end $$;

    -- ===========================================================
    -- IMPERSONA TERAPEUTA B E TENTA LER DADOS DE A (espelho)
    -- ===========================================================

    select set_config(
      'request.jwt.claims',
      json_build_object(
        'sub', '22222222-2222-2222-2222-222222222222',
        'role', 'authenticated'
      )::text,
      true
    );

    do $$
    declare
      cnt_clients_b_self integer;
      cnt_clients_a integer;
      cnt_readings_b_self integer;
      cnt_readings_a integer;
      cnt_images_a integer;
      cnt_profiles_a integer;
    begin
      -- OWN-DATA: terapeuta B deve ler o próprio cliente.
      select count(*) into cnt_clients_b_self from clients
        where therapist_id = '22222222-2222-2222-2222-222222222222';
      if cnt_clients_b_self <> 1 then
        raise exception 'OWN-DATA FAIL: Terapeuta B nao consegue ler proprio cliente. Esperado 1, viu %.', cnt_clients_b_self;
      end if;

      select count(*) into cnt_readings_b_self from readings
        where therapist_id = '22222222-2222-2222-2222-222222222222';
      if cnt_readings_b_self <> 1 then
        raise exception 'OWN-DATA FAIL: Terapeuta B nao consegue ler proprio reading. Esperado 1, viu %.', cnt_readings_b_self;
      end if;

      -- CROSS-THERAPIST: terapeuta B NAO deve ler nada de A.
      select count(*) into cnt_clients_a from clients
        where therapist_id = '11111111-1111-1111-1111-111111111111';
      if cnt_clients_a <> 0 then
        raise exception 'RLS FAIL: Terapeuta B leu % cliente(s) de A em clients', cnt_clients_a;
      end if;

      select count(*) into cnt_readings_a from readings
        where therapist_id = '11111111-1111-1111-1111-111111111111';
      if cnt_readings_a <> 0 then
        raise exception 'RLS FAIL: Terapeuta B leu % reading(s) de A em readings', cnt_readings_a;
      end if;

      select count(*) into cnt_images_a from reading_images
        where reading_id = 'cccccccc-1111-1111-1111-111111111111';
      if cnt_images_a <> 0 then
        raise exception 'RLS FAIL: Terapeuta B leu % imagem(ns) do reading de A em reading_images', cnt_images_a;
      end if;

      select count(*) into cnt_profiles_a from profiles
        where id = '11111111-1111-1111-1111-111111111111';
      if cnt_profiles_a <> 0 then
        raise exception 'RLS FAIL: Terapeuta B leu o profile de A (count = %)', cnt_profiles_a;
      end if;

      raise notice 'PASS: Terapeuta B le proprio cliente/reading (1+1) e e bloqueado em todas as 4 tabelas vs A (0+0+0+0).';
    end $$;

    -- Cleanup
    reset role;
    rollback;
    -- ROLLBACK garante que o teste é destrutivo apenas dentro de sua própria transação:
    -- o DB remoto volta ao estado anterior. Idempotente: pode rodar 100x sem efeito acumulado.
    ```

    **3) Criar `supabase/tests/.gitkeep` (vazio)** — convenção (pode ser opcional já que cross_therapist_rls.sql está committed):
    ```bash
    touch supabase/tests/.gitkeep
    ```

    **4) Executar o teste contra DB REMOTO linkado:**
    ```bash
    cd D:/Projetos/Iridologista
    psql "$SUPABASE_DB_URL" -v ON_ERROR_STOP=1 -f supabase/tests/cross_therapist_rls.sql 2>&1 | tee /tmp/rls-test.out
    ```

    **Saída esperada (resumida):**
    ```
    BEGIN
    DELETE 0  (várias vezes, idempotência; primeira run pode ter 0)
    INSERT 0 2  (auth.users)
    INSERT 0 2  (profiles)
    INSERT 0 2  (clients)
    INSERT 0 2  (readings)
    INSERT 0 2  (reading_images)
    NOTICE: CONTROL PASS: fixture inseriu 2 clients e 2 readings (verificado com BYPASSRLS).
    DO
    SET (set_config result row)
    SET
    NOTICE: PASS: Terapeuta A le proprio cliente/reading (1+1) e e bloqueado em todas as 4 tabelas vs B (0+0+0+0).
    DO
    SET (set_config result row)
    NOTICE: PASS: Terapeuta B le proprio cliente/reading (1+1) e e bloqueado em todas as 4 tabelas vs A (0+0+0+0).
    DO
    RESET
    ROLLBACK
    ```

    Saída contendo "CONTROL FAIL", "OWN-DATA FAIL", "RLS FAIL" ou erro psql -> RLS está com furo OU fixture quebrada. Não tentar ajustar policies aqui — abortar e voltar para plan 1.3 corrigir a migration, depois rodar plan 1.4 push novamente, depois re-rodar este teste.

    **5) Auditar vocabulário no test SQL:**
    ```bash
    grep -iE 'diagn[óo]stico|tratamento|\bcura\b' supabase/tests/cross_therapist_rls.sql && echo "FAIL" || echo "OK"
    ```
  </action>
  <verify>
    <automated>cd D:/Projetos/Iridologista && test -f supabase/tests/cross_therapist_rls.sql && [ "$(wc -l < supabase/tests/cross_therapist_rls.sql)" -ge 100 ] && grep -q "set_config('request.jwt.claims'" supabase/tests/cross_therapist_rls.sql && grep -q "json_build_object" supabase/tests/cross_therapist_rls.sql && grep -q "raise exception" supabase/tests/cross_therapist_rls.sql && grep -q "CONTROL PASS" supabase/tests/cross_therapist_rls.sql && grep -q "OWN-DATA FAIL" supabase/tests/cross_therapist_rls.sql && grep -q "RLS FAIL" supabase/tests/cross_therapist_rls.sql && ! grep -iE 'diagn[óo]stico|tratamento|\bcura\b' supabase/tests/cross_therapist_rls.sql && [ -n "$SUPABASE_DB_URL" ] && psql "$SUPABASE_DB_URL" -v ON_ERROR_STOP=1 -f supabase/tests/cross_therapist_rls.sql > /tmp/rls-test.out 2>&1 && grep -q "CONTROL PASS: fixture inseriu 2 clients" /tmp/rls-test.out && grep -q "PASS: Terapeuta A le proprio cliente" /tmp/rls-test.out && grep -q "PASS: Terapeuta B le proprio cliente" /tmp/rls-test.out && ! grep -qE "CONTROL FAIL|OWN-DATA FAIL|RLS FAIL" /tmp/rls-test.out && echo "RLS_REMOTE_OK"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `supabase/tests/cross_therapist_rls.sql` existe e tem ≥ 100 linhas.
    - Arquivo contém literalmente: `set_config('request.jwt.claims'` (impersonação JWT canônica Supabase), `json_build_object` (forma canônica de montar o claim), `raise exception`, `CONTROL PASS`, `OWN-DATA FAIL`, `RLS FAIL`.
    - Arquivo cobre cross-leitura em **4 tabelas**: clients, readings, reading_images, profiles (cada uma tem ao menos um `select count(*)` cross-terapeuta).
    - Arquivo tem **3 níveis de assertion**: (1) CONTROL block (BYPASSRLS via postgres, count = 2 prova fixture inseriu); (2) OWN-DATA (cada terapeuta lê próprio cliente/reading = 1 row); (3) CROSS-THERAPIST (cada terapeuta lê dados do outro = 0 rows).
    - Auditoria de vocabulário: zero ocorrências de `diagnóstico|tratamento|cura` (case-insensitive).
    - Variável `$SUPABASE_DB_URL` está setada no env do executor.
    - Execução via `psql "$SUPABASE_DB_URL" -v ON_ERROR_STOP=1 -f supabase/tests/cross_therapist_rls.sql` exit code 0 — teste roda contra **remoto linkado**, não local.
    - Output do psql contém literalmente: `CONTROL PASS: fixture inseriu 2 clients`, `PASS: Terapeuta A le proprio cliente`, `PASS: Terapeuta B le proprio cliente`.
    - Output do psql NÃO contém: `CONTROL FAIL`, `OWN-DATA FAIL`, `RLS FAIL`, `ERROR:`.
    - Final do arquivo é `rollback;` (test não polui o DB remoto — idempotente).
  </acceptance_criteria>
  <done>
    SETUP-04 verificado empiricamente no remoto. ROADMAP §1 success criterion #4 satisfeito (cross-leitura bloqueada por RLS no DB remoto sa-east-1). Em qualquer fase futura que mexer em policies, este teste pode ser re-executado contra o remoto linkado.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon JWT → RLS-protected tables (REMOTO sa-east-1) | Esta plan é o teste empírico desta fronteira no DB de produção. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-14 | I (Information Disclosure) | RLS policies (remoto) | mitigate | Esta plan **é** a mitigation: prova que policies bloqueiam cross-leitura no remoto. Sem ele, T-01-06 (do plan 1.3) fica não-validado em produção. |
| T-01-15 | T (Tampering — test pollution remoto) | DB remoto após test | mitigate | `begin; ... rollback;` envolve toda a inserção/teste; rollback final restaura estado. Idempotente: pode rodar 100x sem efeito acumulado. Crítico no remoto onde "limpar manualmente" não é trivial. |
| T-01-16 | E (Privilege escalation via postgres role) | postgres role em remoto | accept | A connection string do Dashboard usa `postgres.<project-ref>` que tem BYPASSRLS — necessário para inserir em `auth.users` e para o CONTROL block validar fixture. Em produção real, esses inserts viriam via Supabase Auth API; aceitamos o privilégio porque (a) acontece dentro de `begin;...rollback;`, (b) password é segredo do fundador no .env. |
| T-01-17 | R (Repudiation — false PASS) | RLS test methodology | mitigate | 3-níveis de assertion (CONTROL + OWN-DATA + CROSS-THERAPIST) elimina os 3 modos clássicos de false PASS: (1) fixture quebrada, (2) RLS bloqueia tudo indiscriminadamente, (3) RLS aberta (regression). |
</threat_model>

<verification>
1. Arquivo `supabase/tests/cross_therapist_rls.sql` existe e é versionado em git.
2. Execução via `psql "$SUPABASE_DB_URL" -v ON_ERROR_STOP=1` retorna 0 e output contém os 3 NOTICES (CONTROL PASS + 2x PASS de impersonação).
3. Cobertura: 4 tabelas testadas (clients, readings, reading_images, profiles).
4. 3 níveis de assertion (CONTROL + OWN-DATA + CROSS-THERAPIST) — distingue fixture-broken (no rows) de RLS-too-restrictive (rejects own data) de RLS-too-permissive (leaks cross-therapist).
5. Teste roda contra **DB remoto linkado** (sa-east-1), não local. Validação local já é feita transitivamente em plan 1.3 (`supabase db reset` aplica a mesma migration).
</verification>

<success_criteria>
- D-13 satisfeito: existe teste versionado de RLS cross-terapeuta executado contra remoto.
- ROADMAP §1 success criterion #4 satisfeito no remoto: `auth.uid()` distintos retornam 0 rows na cross-leitura no DB sa-east-1.
- SETUP-04 fechado.
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-setup/01-05-SUMMARY.md` documentando:
- Output completo da execução do test SQL (contra remoto).
- Tabelas cobertas (4 — clients, readings, reading_images, profiles).
- Confirmação dos 3 níveis de assertion (CONTROL PASS, OWN-DATA não falhou, CROSS-THERAPIST = 0).
- Confirmação de que rollback final mantém o DB remoto limpo.
- Próximo plan: 1.6 (env vars + Vercel deploy, pode rodar paralelo a esta em Wave 4).
</output>
