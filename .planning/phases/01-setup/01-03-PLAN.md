---
phase: 01-setup
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - supabase/config.toml
  - supabase/.gitignore
  - supabase/migrations/0001_initial_schema.sql
  - supabase/seed.sql
autonomous: false
requirements:
  - SETUP-03
  - SETUP-04
must_haves:
  truths:
    - "Diretório supabase/ existe via supabase init e está versionado em git (D-07)."
    - "Migration 0001_initial_schema.sql contém o schema completo do SPEC §3 (verbatim do SPEC §3, com adição mínima documentada — RLS enable em knowledge_chunks por necessidade lógica das policies SPEC; ver D-12): extensão pgvector, 6 tabelas, 5 índices simples, 1 índice HNSW, RLS habilitada em 6 tabelas (5 do SPEC §3 + knowledge_chunks por necessidade lógica das policies SPEC; ver D-12), 4 policies-base do SPEC + 1 policy adicional para profiles e 1 para reading_images (D-12)."
    - "supabase db reset (local) aplica a migration sem erro, refletindo o estado que será replicado para o remoto em plan 1.4."
    - "Projeto Supabase remoto está criado (sa-east-1, D-05) e linked via supabase link — esta parte requer ação manual do fundador."
  artifacts:
    - path: "supabase/config.toml"
      provides: "Configuração Supabase CLI (project_id, regiões, ports locais)"
      contains: "project_id"
    - path: "supabase/migrations/0001_initial_schema.sql"
      provides: "Schema completo do SPEC §3 (extensão, tabelas, índices, RLS, policies), verbatim do SPEC §3 com adição mínima documentada (RLS enable em knowledge_chunks por necessidade lógica)"
      contains: "create extension if not exists vector"
    - path: "supabase/.gitignore"
      provides: "Ignora .branches/, .temp/, runtime do Supabase local"
  key_links:
    - from: "supabase/config.toml"
      to: "remote project (sa-east-1)"
      via: "supabase link --project-ref <ref>"
      pattern: "project_id"
    - from: "supabase/migrations/0001_initial_schema.sql"
      to: "auth.users (Supabase managed schema)"
      via: "profiles.id references auth.users(id) on delete cascade"
      pattern: "references auth.users"
---

<objective>
Inicializar o projeto Supabase localmente via Supabase CLI (D-07), criar a migration `0001_initial_schema.sql` com o schema completo do SPEC §3 (verbatim do SPEC §3, com adição mínima documentada — RLS enable em `knowledge_chunks` por necessidade lógica das policies SPEC; incluindo `pgvector`, índice HNSW e RLS — D-12), validar localmente com `supabase db reset`, e linkar o repo a um projeto Supabase remoto criado em **sa-east-1** (D-05).

Propósito: Sem o schema versionado em git e sem o link com o projeto remoto, plan 1.4 (push BLOQUEANTE) não tem o que aplicar e não tem onde aplicar. SPEC §3 é a única fonte autoritativa do schema; esta migration replica-o verbatim, com uma única adição estrita (`alter table knowledge_chunks enable row level security`) sem a qual a policy de SELECT que o SPEC §3 já define em `knowledge_chunks` ficaria inerte.

Output: Diretório `supabase/` com config.toml + migrations/0001_initial_schema.sql + .gitignore. Schema validado em DB local (Postgres em Docker via `supabase start`). Projeto remoto Supabase criado (manual) e linkado (manual).

**Esta plan tem checkpoint de ação humana** (D-CONTEXT marca tarefas que dependem do fundador como autonomous: false). Os passos manuais — criar conta/projeto Supabase em sa-east-1 e fornecer o `project-ref` — não podem ser automatizados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-setup/01-CONTEXT.md
@.planning/intel/constraints.md
@SPEC.md
@.planning/phases/01-setup/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: supabase init + migration 0001 com schema verbatim do SPEC §3 (mais adição mínima documentada)</name>
  <files>
    supabase/config.toml,
    supabase/.gitignore,
    supabase/migrations/0001_initial_schema.sql,
    supabase/seed.sql
  </files>
  <read_first>
    - D:/Projetos/Iridologista/SPEC.md §3 (linhas 124-243 — schema SQL canônico, **fonte da verdade**)
    - D:/Projetos/Iridologista/.planning/intel/constraints.md ("Database Schema (canonical tables)" — invariantes adicionais não óbvios do SPEC: enums-by-convention, defaults, índices mandatórios)
    - D:/Projetos/Iridologista/.planning/phases/01-setup/01-CONTEXT.md (D-05, D-07, D-08, D-12)
    - D:/Projetos/Iridologista/.gitignore (criado em plan 1.1 — confirma que `supabase/.branches`, `supabase/.temp` já estão ignorados globalmente)
  </read_first>
  <action>
    Working directory: `D:/Projetos/Iridologista/`.

    **Pré-requisito do executor:** ter `supabase` CLI instalado (https://supabase.com/docs/guides/cli) e Docker Desktop rodando para `supabase start` local. Se faltar, abortar e pedir ao fundador instalar antes.

    **1) Inicializar projeto Supabase no repo:**
    ```bash
    supabase init
    ```
    Isso cria `supabase/config.toml` e `supabase/.gitignore` com defaults.

    **2) Ajustar `supabase/config.toml`** — apenas garantir que o `project_id` é `aurel-iris` e que campos default ficam (planner não toca portas locais nem auth.email config aqui — defaults são adequados para Fase 1; Resend SMTP custom é Deferred Idea para Fase 2/8).

    Conferir/ajustar a linha:
    ```toml
    project_id = "aurel-iris"
    ```

    **3) Criar `supabase/migrations/0001_initial_schema.sql`** — schema **verbatim do SPEC §3, com adição mínima documentada (RLS enable em knowledge_chunks por necessidade lógica)** (linhas 127-243), preservando:
    - Comentários em português (sem alterar para inglês — eles documentam a tabela).
    - Ordem original das declarações (extensão -> profiles -> clients -> indexes -> readings -> indexes -> reading_images -> index -> knowledge_chunks -> hnsw -> subscriptions -> RLS enables -> policies).
    - Adicionar policies adicionais que o SPEC §3 menciona como "exemplo" e D-12 manda formalizar para `profiles`, `reading_images` e `subscriptions`.
    - Adicionar `alter table knowledge_chunks enable row level security` (não está literalmente no SPEC §3 — adição lógica documentada inline; sem ela, a policy de SELECT que o SPEC §3 já define para `knowledge_chunks` fica inerte; ver D-12).

    Conteúdo completo (ler do SPEC §3 e copiar; abaixo é o conteúdo exato a gravar):

    ```sql
    -- supabase/migrations/0001_initial_schema.sql
    -- Aurel Iris — schema inicial completo (SPEC §3, com adição mínima documentada).
    -- Cobre: pgvector, 6 tabelas (profiles, clients, readings, reading_images,
    -- knowledge_chunks, subscriptions), índices mandatórios (incluindo HNSW),
    -- RLS habilitada em 6 tabelas (5 do SPEC §3 verbatim + knowledge_chunks por
    -- necessidade lógica das policies SPEC; ver D-12) e policies-base por D-12 do CONTEXT.

    -- Habilita pgvector
    create extension if not exists vector;

    -- Perfis de terapeuta (estende auth.users)
    create table profiles (
      id uuid references auth.users on delete cascade primary key,
      full_name text not null,
      professional_id text,                  -- registro profissional opcional
      bio text,
      phone text,
      city text,
      state text,
      subscription_status text default 'trial', -- trial | active | cancelled | past_due
      trial_ends_at timestamptz default (now() + interval '14 days'),
      stripe_customer_id text,
      created_at timestamptz default now()
    );

    -- Clientes do terapeuta (pacientes não têm conta)
    create table clients (
      id uuid primary key default gen_random_uuid(),
      therapist_id uuid references profiles(id) on delete cascade not null,
      full_name text not null,
      birth_date date,
      gender text,
      notes text,
      consent_signed_at timestamptz,         -- consentimento LGPD assinado
      consent_document_url text,
      created_at timestamptz default now()
    );

    create index on clients(therapist_id);

    -- Leituras (cada sessão de análise)
    create table readings (
      id uuid primary key default gen_random_uuid(),
      client_id uuid references clients(id) on delete cascade not null,
      therapist_id uuid references profiles(id) on delete cascade not null,
      status text default 'pending',         -- pending | processing | ready | failed | edited
      capture_method text,                   -- mobile_camera | desktop_upload
      iris_map text default 'jensen',        -- jensen | jausas | hidalgo
      vision_features jsonb,                 -- output do Modal
      ai_report_raw text,                    -- resposta crua do Claude
      ai_report_edited text,                 -- versão editada pelo terapeuta
      therapist_notes text,                  -- anotações privadas
      is_delivered boolean default false,    -- entregou pro cliente?
      created_at timestamptz default now(),
      processed_at timestamptz,
      delivered_at timestamptz
    );

    create index on readings(therapist_id);
    create index on readings(client_id);
    create index on readings(status);

    -- Imagens de cada leitura (3 por olho × 2 olhos = até 6)
    create table reading_images (
      id uuid primary key default gen_random_uuid(),
      reading_id uuid references readings(id) on delete cascade not null,
      eye text not null,                     -- left | right
      angle text not null,                   -- frontal | lateral | backlight
      storage_path text not null,            -- path no bucket privado
      quality_score float,                   -- 0-1 do validador on-device
      width int,
      height int,
      created_at timestamptz default now()
    );

    create index on reading_images(reading_id);

    -- Base de conhecimento iridológica (RAG)
    create table knowledge_chunks (
      id uuid primary key default gen_random_uuid(),
      source_book text not null,             -- "Jensen - Iridology Vol 1"
      source_chapter text,
      source_page int,
      content text not null,
      embedding vector(1024),                -- voyage-3 = 1024 dim
      metadata jsonb,                        -- {tema, tradicao, escola, idioma}
      created_at timestamptz default now()
    );

    create index on knowledge_chunks
      using hnsw (embedding vector_cosine_ops);

    -- Assinaturas Stripe
    create table subscriptions (
      id uuid primary key default gen_random_uuid(),
      therapist_id uuid references profiles(id) on delete cascade not null,
      stripe_subscription_id text unique not null,
      status text not null,
      plan text not null,                    -- starter | professional | school
      current_period_end timestamptz,
      created_at timestamptz default now()
    );

    -- RLS (Row Level Security): cada terapeuta só vê os próprios dados
    alter table profiles enable row level security;
    alter table clients enable row level security;
    alter table readings enable row level security;
    alter table reading_images enable row level security;
    alter table subscriptions enable row level security;
    -- NOTA: Esta linha não está no SPEC §3 verbatim. Adicionada por necessidade lógica:
    -- a policy "Knowledge chunks são públicos pra usuários autenticados" do SPEC §3
    -- fica inerte se RLS não estiver enabled na tabela. Ver CONTEXT.md D-12.
    alter table knowledge_chunks enable row level security;

    -- Policies SPEC §3 verbatim
    create policy "Terapeutas só veem seus próprios clientes"
      on clients for all
      using (auth.uid() = therapist_id);

    create policy "Terapeutas só veem suas próprias leituras"
      on readings for all
      using (auth.uid() = therapist_id);

    -- Knowledge chunks são lidos por todos os terapeutas autenticados
    create policy "Knowledge chunks são públicos pra usuários autenticados"
      on knowledge_chunks for select
      using (auth.role() = 'authenticated');

    -- Policies adicionais por D-12 (CONTEXT.md):
    -- profiles, reading_images, subscriptions também precisam policies.

    create policy "Terapeutas só veem o próprio profile"
      on profiles for all
      using (auth.uid() = id);

    create policy "Terapeutas só veem imagens de suas próprias leituras"
      on reading_images for all
      using (
        exists (
          select 1 from readings
          where readings.id = reading_images.reading_id
            and readings.therapist_id = auth.uid()
        )
      );

    create policy "Terapeutas só veem as próprias assinaturas"
      on subscriptions for all
      using (auth.uid() = therapist_id);
    ```

    **4) Criar `supabase/seed.sql`** — vazio com header explicativo (Phase 1 não tem seed; futuras fases podem usar para dados de demonstração):
    ```sql
    -- supabase/seed.sql
    -- Dados de seed para desenvolvimento local (supabase db reset roda este arquivo).
    -- Phase 1: vazio. Phase 2+ pode adicionar perfis/clientes de demo.
    ```

    **5) Garantir `supabase/.gitignore`** — gerado pelo `supabase init`, mas conferir que contém:
    ```
    .branches
    .temp
    ```

    **6) Validar a migration localmente com `supabase db reset`:**
    ```bash
    # Levantar containers Postgres locais (Postgres + GoTrue + PostgREST + Storage local).
    supabase start

    # Aplica todas as migrations + seed.sql num DB limpo (local é o default; sem flag).
    supabase db reset

    # Sanity checks via psql na connection local
    PGPASSWORD=postgres psql -h 127.0.0.1 -p 54322 -U postgres -d postgres -c "
      select count(*) as table_count from pg_tables
      where schemaname='public' and tablename in
        ('profiles','clients','readings','reading_images','knowledge_chunks','subscriptions');
    "
    # Esperado: 6

    PGPASSWORD=postgres psql -h 127.0.0.1 -p 54322 -U postgres -d postgres -c "
      select count(*) as policy_count from pg_policies where schemaname='public';
    "
    # Esperado: 6 (3 SPEC verbatim + 3 D-12 adicionais)

    PGPASSWORD=postgres psql -h 127.0.0.1 -p 54322 -U postgres -d postgres -c "
      select indexname from pg_indexes where schemaname='public' and indexname like '%hnsw%' or indexdef like '%hnsw%';
    "
    # Esperado: ao menos 1 índice HNSW em knowledge_chunks(embedding)

    # Insert dummy de vector(1024) para validar pgvector
    PGPASSWORD=postgres psql -h 127.0.0.1 -p 54322 -U postgres -d postgres -c "
      insert into knowledge_chunks (source_book, content, embedding)
      values ('test', 'sanity', array_fill(0.0, ARRAY[1024])::vector);
      select id, source_book from knowledge_chunks;
      delete from knowledge_chunks where source_book='test';
    "
    ```

    Se qualquer um desses retornar erro ou o count divergir, a migration está errada — corrigir e rodar `supabase db reset` de novo.

    **7) Auditoria de vocabulário** (PROJECT.md restrição não-negociável):
    ```bash
    grep -iE 'diagn[óo]stico|tratamento|\bcura\b' supabase/migrations/0001_initial_schema.sql && echo "FAIL" || echo "OK"
    ```
    O comentário `consent_signed_at -- consentimento LGPD assinado` é o único lugar próximo do tema; "consentimento" não está na lista proibida. OK esperado.
  </action>
  <verify>
    <automated>cd D:/Projetos/Iridologista && test -f supabase/config.toml && test -f supabase/migrations/0001_initial_schema.sql && test -f supabase/.gitignore && grep -q 'project_id = "aurel-iris"' supabase/config.toml && grep -q "create extension if not exists vector" supabase/migrations/0001_initial_schema.sql && grep -q "create table profiles" supabase/migrations/0001_initial_schema.sql && grep -q "create table clients" supabase/migrations/0001_initial_schema.sql && grep -q "create table readings" supabase/migrations/0001_initial_schema.sql && grep -q "create table reading_images" supabase/migrations/0001_initial_schema.sql && grep -q "create table knowledge_chunks" supabase/migrations/0001_initial_schema.sql && grep -q "create table subscriptions" supabase/migrations/0001_initial_schema.sql && grep -q "vector(1024)" supabase/migrations/0001_initial_schema.sql && grep -q "using hnsw (embedding vector_cosine_ops)" supabase/migrations/0001_initial_schema.sql && grep -q "alter table profiles enable row level security" supabase/migrations/0001_initial_schema.sql && grep -q "alter table knowledge_chunks enable row level security" supabase/migrations/0001_initial_schema.sql && [ "$(grep -c '^create policy' supabase/migrations/0001_initial_schema.sql)" -eq 6 ] && ! grep -iE 'diagn[óo]stico|tratamento|\bcura\b' supabase/migrations/0001_initial_schema.sql && supabase db reset && echo "MIGRATION_OK"</automated>
  </verify>
  <acceptance_criteria>
    - Diretório `supabase/` existe na raiz com `config.toml`, `.gitignore`, `migrations/0001_initial_schema.sql`, `seed.sql`.
    - `supabase/config.toml` contém literalmente `project_id = "aurel-iris"`.
    - `supabase/migrations/0001_initial_schema.sql` contém literalmente as substrings: `create extension if not exists vector`, `create table profiles`, `create table clients`, `create table readings`, `create table reading_images`, `create table knowledge_chunks`, `create table subscriptions`, `vector(1024)`, `using hnsw (embedding vector_cosine_ops)`.
    - `supabase/migrations/0001_initial_schema.sql` contém **exatamente 6 ocorrências** de `^create policy` (3 SPEC §3 verbatim + 3 D-12 adicionais para profiles/reading_images/subscriptions).
    - `supabase/migrations/0001_initial_schema.sql` contém `alter table ... enable row level security` para as 6 tabelas (5 do SPEC §3 + knowledge_chunks por necessidade lógica documentada inline; sem RLS enabled em knowledge_chunks, a policy de SELECT do SPEC §3 fica inerte).
    - Auditoria de vocabulário: zero ocorrências de `diagnóstico|tratamento|cura` (case-insensitive) na migration.
    - `supabase db reset` (sem flag — local é default) executa sem erro contra DB local.
    - Após `supabase db reset`, `pg_tables` em schema `public` tem exatamente 6 entradas: profiles, clients, readings, reading_images, knowledge_chunks, subscriptions.
    - `pg_policies` em schema `public` retorna exatamente 6 linhas.
    - `pg_indexes` em schema `public` inclui ao menos um índice HNSW sobre `knowledge_chunks(embedding)`.
    - Insert dummy `array_fill(0.0, ARRAY[1024])::vector` em `knowledge_chunks.embedding` é aceito.
  </acceptance_criteria>
  <done>
    Schema do SPEC §3 está versionado em git e validado em Postgres local. Plan 1.4 pode pegar este migration e fazer `supabase db push` contra o remoto.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [HUMAN] Criar projeto Supabase remoto em sa-east-1 + linkar repo</name>
  <what-built>
    Migration 0001 e estrutura `supabase/` validados localmente em Task 1. Falta apenas o lado remoto: criar o projeto Supabase real em sa-east-1 (D-05) e linkar este repo a ele.
  </what-built>
  <how-to-verify>
    Estes passos exigem ação do fundador (não-automatizáveis — criação de conta/projeto e fornecimento do `project-ref`):

    **1) Criar projeto no Supabase Dashboard**
    - Acesse https://supabase.com/dashboard
    - Faça login (ou crie conta) com o e-mail rhelton@gmail.com.
    - Click "New project".
    - Nome: `aurel-iris`.
    - Database password: gerar password forte e guardar em password manager.
    - **Region: South America (São Paulo) — sa-east-1** (D-05 — não-negociável: dados biométricos residem em território nacional, ver PROJECT.md restrições LGPD).
    - Plan: começar em Free; pode upgrar para Pro depois quando tiver dogfooding consistente (a meta de US$25/mo do PROJECT.md cobre isso).
    - Aguardar provisionamento (~2 minutos).

    **2) Capturar credenciais (vão para Vercel/.env em plan 1.6)**
    No dashboard do projeto:
    - **Settings → API → Project URL** (`NEXT_PUBLIC_SUPABASE_URL`).
    - **Settings → API → anon public** (`NEXT_PUBLIC_SUPABASE_ANON_KEY`).
    - **Settings → API → service_role** (`SUPABASE_SERVICE_ROLE_KEY`) — secret, nunca commitar.
    - **Settings → General → Reference ID** (o `project-ref`, formato `xxxxxxxxxxxxxxxxxxxx`).

    Salvar tudo num gerenciador de senhas. **Não escreva em arquivo do repo nesta task** (plan 1.6 popula `.env.example` e Vercel envs).

    **3) Autenticar a CLI Supabase localmente** (uma vez por máquina):
    ```bash
    supabase login
    ```
    (Abre browser; OAuth com a conta Supabase do passo 1.)

    **4) Linkar este repo ao projeto remoto:**
    ```bash
    cd D:/Projetos/Iridologista
    supabase link --project-ref <PROJECT_REF_DO_PASSO_2>
    # Vai pedir o database password do passo 1.
    ```

    Após linkar, `supabase/config.toml` terá `project_id` atualizado para o ref real e o estado linked é gravado em `.supabase/` (já no .gitignore).

    **5) Sanity check do link:**
    ```bash
    supabase projects list
    # Deve mostrar aurel-iris com region sa-east-1 e LINKED.
    ```

    **Antes de digitar `approved`:**
    - Confirme: "Projeto Supabase em sa-east-1 criado, eu (fundador) tenho a senha do DB salva, anon/service_role keys salvos, e `supabase projects list` mostra aurel-iris linked."
    - **Se a região não for sa-east-1**, derrube o projeto e crie de novo. Migrar dados depois é mais doloroso que recriar agora (D-05 é não-negociável por LGPD).
  </how-to-verify>
  <resume-signal>
    Type `approved` (e cole o `project-ref` na resposta para o executor pegar para o plan 1.4) ou descreva problemas (ex.: "não consigo criar projeto na região sa-east-1, ela não aparece").
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| auth.users → public.profiles | FK com cascade delete; deleção de auth.users (via Supabase admin) cascateia até reading_images via policy chain. Validado por estrutura, exercitado em plan 1.5. |
| anon JWT → RLS-protected tables | Toda query autenticada via anon key passa por RLS; verificação efetiva fica em plan 1.5 (cross-therapist test). |
| service_role JWT → all tables | Bypass de RLS deliberado para server-side admin (Vision webhook escrevendo `vision_features`, ingest-knowledge.ts escrevendo `knowledge_chunks`). Risco mitigado mantendo `SUPABASE_SERVICE_ROLE_KEY` server-only e nunca em código client. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | I (Information Disclosure) | RLS policies | mitigate | Migration habilita RLS em todas as 6 tabelas e adiciona policies para profiles/reading_images/subscriptions além das 3 do SPEC verbatim (D-12). Plan 1.5 verifica empiricamente com cross-therapist test. |
| T-01-07 | T (Tampering) | knowledge_chunks RLS | mitigate | Policy é `for select` apenas (read-only para autenticados). Inserts/updates só via service_role (script de ingest em Fase 6). |
| T-01-08 | R (Repudiation) | migrations versioning | mitigate | D-07: todas alterações de schema entram como nova migration timestamped (0002, 0003...). Sem SQL ad-hoc no dashboard exceto debug temporário. Histórico fica em git + `supabase_migrations.schema_migrations`. |
| T-01-09 | I (Info Disclosure) | sa-east-1 residency | mitigate | D-05 (PROJECT.md LGPD não-negociável): projeto criado em sa-east-1; chamadas server-to-server pra Modal/Anthropic (pares de proxy de dados) ainda enviam imagens, mas storage de imagens fica em sa-east-1 (Fase 3). |
| T-01-10 | E (Privilege escalation) | service_role key | mitigate | Plan 1.6 cadastra `SUPABASE_SERVICE_ROLE_KEY` apenas em Vercel server-side env (sem `NEXT_PUBLIC_` prefix); `.gitignore` (plan 1.1) já bloqueia commit acidental de `.env*.local`. |
</threat_model>

<verification>
1. Task 1: `supabase db reset` (local — flag default) aplica migration sem erro; counts batem (6 tabelas, 6 policies, ≥1 HNSW index).
2. Task 1: insert dummy de `vector(1024)` em `knowledge_chunks` é aceito (valida que pgvector está habilitado e a coluna está com a dimensão certa para Voyage `voyage-3`).
3. Task 2 (humano): `supabase projects list` mostra `aurel-iris` em sa-east-1 e linked status.
4. `git status`: `supabase/config.toml`, `supabase/migrations/0001_initial_schema.sql`, `supabase/seed.sql`, `supabase/.gitignore` aparecem como new files. `.supabase/` (estado de link) não aparece (gitignored).
</verification>

<success_criteria>
- SPEC §3 está versionado em `supabase/migrations/0001_initial_schema.sql` verbatim, com adições D-12 (policies para profiles, reading_images, subscriptions) e adição mínima documentada (RLS enable em knowledge_chunks por necessidade lógica das policies SPEC).
- Migration aplicada e validada em DB local.
- Projeto remoto Supabase existe em sa-east-1.
- Repo está linked ao projeto remoto via `supabase link`.
- Plan 1.4 pode rodar `supabase db push` sem precisar criar nada novo.
</success_criteria>

<output>
Após conclusão das duas tasks, criar `.planning/phases/01-setup/01-03-SUMMARY.md` documentando:
- O `project-ref` do projeto remoto (formato `xxxxxxxxxxxxxxxxxxxx`) — útil para plan 1.4 e 1.6.
- Confirmação de que migration aplica em local sem erro (output do `supabase db reset`).
- Quaisquer ajustes feitos ao schema do SPEC §3 e justificativa (única adição: `alter table knowledge_chunks enable row level security` — necessidade lógica das policies SPEC §3, ver D-12).
- Próximo plan: 1.4 (BLOCKING push remoto + types) em Wave 3, depois 1.5 (RLS test em remoto) e 1.6 (Vercel deploy) em Wave 4.
</output>
