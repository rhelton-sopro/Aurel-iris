---
phase: 01-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .nvmrc
  - README.md
  - package.json
  - pnpm-workspace.yaml
  - .vercelignore
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/next.config.ts
  - apps/web/tailwind.config.ts
  - apps/web/postcss.config.mjs
  - apps/web/eslint.config.mjs
  - apps/web/components.json
  - apps/web/app/layout.tsx
  - apps/web/app/page.tsx
  - apps/web/app/globals.css
  - apps/web/lib/utils.ts
  - apps/web/types/.gitkeep
autonomous: true
requirements:
  - SETUP-01
must_haves:
  truths:
    - "pnpm dev levanta o app Next.js localmente em http://localhost:3000 e responde 200 em /."
    - "Repo segue layout monorepo D-01: apps/web/ contém o Next.js, vision-service/ é subdir paralelo."
    - "shadcn/ui está instalado e configurado em apps/web/ com defaults (D-01 + Deferred: tema fica para Fase 2)."
    - "Node version travada (D-04: planner escolhe .nvmrc) em LTS 20+."
    - "pnpm é o package manager (D-03), com pnpm-lock.yaml versionado em apps/web/."
  artifacts:
    - path: "apps/web/package.json"
      provides: "Next.js 15 App Router + TypeScript + Tailwind + shadcn/ui dependencies"
      contains: '"next":'
    - path: "apps/web/app/page.tsx"
      provides: "Default Next.js home page"
    - path: "apps/web/components.json"
      provides: "shadcn/ui config"
      contains: '"style":'
    - path: ".nvmrc"
      provides: "Node LTS pin (D-04)"
    - path: "package.json"
      provides: "Root pnpm scaffolding with delegating scripts"
    - path: ".gitignore"
      provides: "Ignores node_modules, .env.local, .next, .vercel, .DS_Store"
      contains: ".env.local"
    - path: ".vercelignore"
      provides: "Excludes vision-service/ from Vercel deploys (D-01)"
      contains: "vision-service"
  key_links:
    - from: "package.json (root)"
      to: "apps/web/package.json"
      via: "pnpm script delegation: pnpm dev -> pnpm --filter web dev"
      pattern: "pnpm --filter"
    - from: "apps/web/components.json"
      to: "apps/web/lib/utils.ts"
      via: "shadcn/ui generates cn() helper"
      pattern: "export function cn"
---

<objective>
Inicializar a estrutura do monorepo (D-01) com o Next.js 15 funcional em `apps/web/`, gerenciado por pnpm (D-03), com Tailwind + shadcn/ui defaults e Node LTS travado (D-04).

Propósito: Esta é a base sobre a qual todas as fases seguintes constroem. Sem `pnpm dev` rodando localmente e sem o layout `apps/web/` + `vision-service/` correto, nada mais da Fase 1 faz sentido.

Output: Repositório com `apps/web/` rodando Next.js 15 App Router, root scaffolding pnpm, `.gitignore`, `.nvmrc`, `.vercelignore`, README, `pnpm-workspace.yaml` opcional, e shadcn/ui inicializado em apps/web/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-setup/01-CONTEXT.md
@SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inicializar root scaffolding monorepo (pnpm + git ignores + Node pin)</name>
  <files>
    .gitignore,
    .nvmrc,
    .vercelignore,
    README.md,
    package.json,
    pnpm-workspace.yaml
  </files>
  <read_first>
    - D:/GDrive/iridologista/.planning/phases/01-setup/01-CONTEXT.md (D-01..D-04)
    - D:/GDrive/iridologista/SPEC.md §2 (folder layout — apps/web vs vision-service)
    - D:/GDrive/iridologista/.gitignore (verifica se já existe; provavelmente não)
  </read_first>
  <action>
    Criar 6 arquivos no root do repositório (D:/GDrive/iridologista/):

    **`.nvmrc`** (D-04 — escolha do planner: usar `.nvmrc` em vez de `engines` por ser mais explícito e respeitado por nvm/fnm/volta sem ambiguidade):
    ```
    20
    ```

    **`.gitignore`** (deve ignorar tudo o que Next.js, pnpm e Vercel produzem; herda do .gitignore default do create-next-app + adições):
    ```gitignore
    # dependencies
    node_modules/
    .pnpm-store/

    # next.js
    .next/
    out/
    next-env.d.ts

    # production / vercel
    .vercel/

    # python (vision-service)
    __pycache__/
    *.pyc
    .venv/
    .env

    # environment
    .env*.local
    .env

    # macOS / IDE
    .DS_Store
    .idea/
    .vscode/
    *.log

    # supabase
    supabase/.branches
    supabase/.temp
    ```

    **`.vercelignore`** (D-01 — Vercel ignora vision-service/ porque ele só é deployado via Modal):
    ```
    vision-service/
    supabase/
    ```

    **`pnpm-workspace.yaml`** (Deferred Idea: workspaces formais. Mas D-03 + D-01 + futuro pacote shared apontam que vamos precisar logo. Planner decide criar agora — é trivial e evita refator depois. Justificativa explícita no commit.):
    ```yaml
    packages:
      - "apps/*"
    ```

    **`package.json`** (root — apenas delegação; vision-service não é um pacote pnpm):
    ```json
    {
      "name": "aurel-iris",
      "version": "0.0.0",
      "private": true,
      "packageManager": "pnpm@9.12.0",
      "engines": {
        "node": ">=20.0.0"
      },
      "scripts": {
        "dev": "pnpm --filter web dev",
        "build": "pnpm --filter web build",
        "start": "pnpm --filter web start",
        "lint": "pnpm --filter web lint",
        "type-check": "pnpm --filter web exec tsc --noEmit"
      }
    }
    ```

    **`README.md`** (mínimo, em pt-BR; conteúdo expandido virá em fases posteriores):
    ```markdown
    # Aurel Iris

    SaaS de leitura iridológica assistida por IA para terapeutas integrativos.

    > Ferramenta de apoio à anamnese terapêutica integrativa, não substitui avaliação médica.

    ## Estrutura do monorepo

    - `apps/web/` — Next.js 15 (App Router, TypeScript, Tailwind, shadcn/ui).
    - `vision-service/` — Pipeline de visão computacional Python (Modal serverless GPU).
    - `supabase/` — Migrations versionadas e tests SQL do schema Postgres + pgvector.

    ## Setup local

    Pré-requisitos: Node 20+ (ver `.nvmrc`), `pnpm@9+`, `supabase` CLI.

    ```bash
    pnpm install
    pnpm dev
    ```

    Detalhes em cada subdiretório.

    ## Stack

    Ver `.planning/PROJECT.md` (Restrições — Stack) e `SPEC.md` §1.
    ```
  </action>
  <verify>
    <automated>cd D:/GDrive/iridologista && test -f .nvmrc && test -f .gitignore && test -f .vercelignore && test -f package.json && test -f README.md && test -f pnpm-workspace.yaml && grep -q '"packageManager"' package.json && grep -q '"name": "aurel-iris"' package.json && grep -q "vision-service" .vercelignore && grep -q ".env.local" .gitignore && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `.nvmrc` existe e contém apenas `20` (sem prefixo `v`).
    - Arquivo `.gitignore` contém literalmente as substrings `node_modules/`, `.next/`, `.env.local`, `.vercel/`, `__pycache__/`, `supabase/.temp`.
    - Arquivo `.vercelignore` contém literalmente as linhas `vision-service/` e `supabase/`.
    - Arquivo `package.json` contém `"name": "aurel-iris"`, `"private": true`, `"packageManager": "pnpm@9.12.0"`, e tem chave `"engines"` com `"node": ">=20.0.0"`.
    - Arquivo `package.json` define os scripts `dev`, `build`, `start`, `lint`, `type-check` — todos delegando via `pnpm --filter web`.
    - Arquivo `pnpm-workspace.yaml` contém `packages:` com `- "apps/*"`.
    - Arquivo `README.md` menciona "ferramenta de apoio à anamnese" (vocabulário obrigatório PROJECT.md).
    - Arquivo `README.md` NÃO contém as palavras `diagnóstico`, `tratamento`, `cura` (PROJECT.md restrição não-negociável).
  </acceptance_criteria>
  <done>
    Root do repositório tem `.nvmrc`, `.gitignore`, `.vercelignore`, `package.json`, `pnpm-workspace.yaml` e `README.md` consistentes com D-01..D-04. Repositório pronto para receber `apps/web/` no próximo task.
  </done>
</task>

<task type="auto">
  <name>Task 2: Scaffold Next.js 15 + Tailwind + shadcn/ui em apps/web/</name>
  <files>
    apps/web/package.json,
    apps/web/tsconfig.json,
    apps/web/next.config.ts,
    apps/web/tailwind.config.ts,
    apps/web/postcss.config.mjs,
    apps/web/eslint.config.mjs,
    apps/web/components.json,
    apps/web/app/layout.tsx,
    apps/web/app/page.tsx,
    apps/web/app/globals.css,
    apps/web/lib/utils.ts,
    apps/web/types/.gitkeep,
    apps/web/.gitignore
  </files>
  <read_first>
    - D:/GDrive/iridologista/.planning/phases/01-setup/01-CONTEXT.md (D-01, D-03; Deferred: tema shadcn fica para Fase 2)
    - D:/GDrive/iridologista/SPEC.md §2 (estrutura de pastas — note que SPEC mostra `app/`, `components/`, `lib/` no root; D-01 sobrepõe colocando tudo dentro de `apps/web/`)
    - D:/GDrive/iridologista/package.json (recém-criado; valida estrutura do monorepo)
  </read_first>
  <action>
    Rodar `create-next-app` em modo não-interativo apontando para `apps/web/`, depois inicializar shadcn/ui e ajustar para o monorepo.

    Working directory: `D:/GDrive/iridologista/`.

    ```bash
    # 1) Scaffold Next.js 15 (não-interativo). D-01 + D-03 + decisões locked: Next 15, App Router, TS, Tailwind, ESLint, sem src/, alias @/*.
    pnpm create next-app@latest apps/web \
      --typescript \
      --tailwind \
      --eslint \
      --app \
      --no-src-dir \
      --import-alias "@/*" \
      --use-pnpm \
      --skip-install

    # 2) Instalar dependências do apps/web isoladamente (evita conflito com workspace root antes do install root).
    cd apps/web && pnpm install && cd ../..

    # 3) Inicializar shadcn/ui dentro do apps/web/ com defaults (Deferred: tema/base color fica para Fase 2).
    cd apps/web && pnpm dlx shadcn@latest init --yes --base-color slate --css-variables && cd ../..
    ```

    **Pós-create-next-app — validações + ajustes manuais:**

    1. **Verificar/criar `apps/web/types/.gitkeep`** (placeholder para `database.ts` que será gerado em plan 1.4):
       ```bash
       mkdir -p apps/web/types && touch apps/web/types/.gitkeep
       ```

    2. **Atualizar `apps/web/app/page.tsx`** — substituir o boilerplate do create-next-app por uma home minimalista em pt-BR (sem vocabulário proibido):
       ```tsx
       export default function Home() {
         return (
           <main className="flex min-h-screen flex-col items-center justify-center p-24">
             <div className="z-10 max-w-2xl w-full text-center space-y-4">
               <h1 className="text-4xl font-bold">Aurel Iris</h1>
               <p className="text-lg text-muted-foreground">
                 Ferramenta de apoio à anamnese terapêutica integrativa.
               </p>
               <p className="text-sm text-muted-foreground">
                 Não substitui avaliação médica.
               </p>
             </div>
           </main>
         );
       }
       ```

    3. **Verificar `apps/web/components.json`** — gerado pelo `shadcn init`. Ele deve apontar para `app/globals.css`, alias `@/components`, `@/lib/utils`. Se shadcn pediu para criar `lib/utils.ts`, ele já criou; valide que `apps/web/lib/utils.ts` exporta `cn`.

    4. **Adicionar a `apps/web/package.json`** o script de geração de tipos do Supabase (placeholder — comando real em plan 1.4):
       ```json
       "scripts": {
         "...": "...",
         "gen:types": "echo 'use plan 1.4 supabase gen types command' && exit 1"
       }
       ```
       (Deixe-o falhando intencionalmente até plan 1.4 substituir pelo comando real, conforme D-09.)

    5. **Não modificar** `tailwind.config.ts`, `next.config.ts`, `tsconfig.json`, `eslint.config.mjs` além do que `create-next-app` + `shadcn init` produziram. Defaults são suficientes para Fase 1 (D-CONTEXT: planner discricionariedade — keep defaults).

    **Auditoria de vocabulário** (PROJECT.md restrição não-negociável):
    ```bash
    grep -riE 'diagn[óo]stico|tratamento|\bcura\b' apps/web/app apps/web/components apps/web/lib 2>/dev/null && echo "FAIL: vocabulary violation" || echo "OK: no forbidden vocabulary"
    ```
  </action>
  <verify>
    <automated>cd D:/GDrive/iridologista && test -d apps/web/app && test -f apps/web/package.json && test -f apps/web/tsconfig.json && test -f apps/web/components.json && test -f apps/web/lib/utils.ts && test -f apps/web/app/page.tsx && grep -q '"next":' apps/web/package.json && grep -q '"tailwindcss"' apps/web/package.json && grep -q 'export function cn' apps/web/lib/utils.ts && grep -q 'Aurel Iris' apps/web/app/page.tsx && grep -q 'apoio à anamnese' apps/web/app/page.tsx && ! grep -riE 'diagn[óo]stico|tratamento|\bcura\b' apps/web/app apps/web/components apps/web/lib && cd apps/web && pnpm exec tsc --noEmit && pnpm exec next build && echo "BUILD_OK"</automated>
  </verify>
  <acceptance_criteria>
    - Diretório `apps/web/app/` existe (App Router — não `apps/web/src/app/`).
    - Arquivo `apps/web/package.json` contém `"next":` em dependencies (versão 15.x) e `"tailwindcss"` em devDependencies.
    - Arquivo `apps/web/tsconfig.json` contém `"@/*"` no `paths`.
    - Arquivo `apps/web/components.json` existe e contém `"style":` (gerado por shadcn init).
    - Arquivo `apps/web/lib/utils.ts` exporta `function cn` (helper shadcn).
    - Arquivo `apps/web/app/page.tsx` contém literalmente as strings `Aurel Iris` e `apoio à anamnese`.
    - Comando `pnpm exec tsc --noEmit` em `apps/web/` retorna 0.
    - Comando `pnpm exec next build` em `apps/web/` retorna 0 (build de produção passa).
    - Auditoria de vocabulário: `grep -riE 'diagn[óo]stico|tratamento|\bcura\b' apps/web/app apps/web/components apps/web/lib` retorna 0 matches (PROJECT.md restrição não-negociável).
    - Diretório `apps/web/types/` existe (com `.gitkeep` ou `database.ts` futuro).
  </acceptance_criteria>
  <done>
    `apps/web/` é um app Next.js 15 funcional com TypeScript, Tailwind, ESLint e shadcn/ui inicializados. `pnpm build` passa, `pnpm dev` levantaria localmente. Vocabulário do produto compliant com PROJECT.md desde o primeiro commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev → repo | Conteúdo committado é a única origem de tudo (sem usuários ainda); auditoria de vocabulário (PROJECT.md restrição não-negociável) já vale aqui. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | I (Information Disclosure) | `.gitignore` / commits | mitigate | `.gitignore` ignora `.env*.local` e `.env` desde o commit inicial; `.vercelignore` exclui `vision-service/` e `supabase/` do bundle Vercel para evitar leak acidental de migrations/credentials no edge. |
| T-01-02 | T (Tampering / Compliance) | UI strings (page.tsx, README.md) | mitigate | Auditoria de vocabulário em `apps/web/app`, `apps/web/components`, `apps/web/lib` rejeita "diagnóstico", "tratamento", "cura" desde o primeiro commit (PROJECT.md restrição jurídica não-negociável). |
| T-01-03 | E (Elevation via supply chain) | npm packages | accept | Risco mínimo nesta fase (apenas Next.js + shadcn defaults, todos pacotes com auditoria pública). Reavaliar quando integrar Modal/Stripe/Anthropic em fases posteriores. |
</threat_model>

<verification>
Após execução das duas tasks:
1. `pnpm dev` em D:/GDrive/iridologista (delegação root) levanta o app Next.js em http://localhost:3000.
2. Curl `http://localhost:3000/` retorna 200 e o HTML contém "Aurel Iris".
3. `pnpm build` (root) passa sem erro.
4. `git status` mostra `apps/web/`, `package.json` (root), `.gitignore`, `.vercelignore`, `.nvmrc`, `README.md`, `pnpm-workspace.yaml` como novos files.
5. Layout em disco corresponde a D-01: monorepo com `apps/web/` (Next.js) preparado para `vision-service/` paralelo (criado em plan 1.2).
</verification>

<success_criteria>
- `pnpm dev` (root) e `pnpm dev` em apps/web/ ambos levantam Next.js 15 sem erro.
- `pnpm build` (root) passa.
- shadcn/ui está inicializado e `cn()` está disponível em `@/lib/utils`.
- Vocabulário do produto auditado: zero ocorrências das palavras proibidas em qualquer arquivo committado em `apps/web/`.
- Estrutura monorepo (D-01) pronta para receber `vision-service/` (plan 1.2 paralelo) e `supabase/` (plan 1.3).
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-setup/01-01-SUMMARY.md` documentando:
- Comandos `create-next-app` e `shadcn init` exatos usados (versões pinned).
- Quaisquer decisões discricionárias do executor (ex.: optou por `pnpm-workspace.yaml` agora vs. depois).
- Confirmação da auditoria de vocabulário.
- Próximo plan: 1.2 (vision-service skeleton, Wave 1 paralelo) e 1.3 (Supabase init, Wave 2).
</output>
