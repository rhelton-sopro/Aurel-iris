---
phase: 01-setup
plan: 06
type: execute
wave: 4
depends_on:
  - "01-04"
files_modified:
  - .env.example
  - apps/web/.env.local.example
  - vercel.json
autonomous: false
requirements:
  - SETUP-02
must_haves:
  truths:
    - ".env.example no root commitado contém literalmente as 11 chaves de D-11 do CONTEXT (todas vazias) — NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, ANTHROPIC_API_KEY, VOYAGE_API_KEY, MODAL_TOKEN_ID, MODAL_TOKEN_SECRET, MODAL_WEBHOOK_SECRET, STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, RESEND_API_KEY (D-10)."
    - "vercel.json declara regions: ['gru1'] (D-06) — funções server casam com Supabase sa-east-1."
    - "Projeto Vercel está criado, linkado ao git e às envs de produção/preview cadastradas (manual — autonomous: false)."
    - "Deploy preview da Vercel responde 200 em / e a página renderiza 'Aurel Iris' + 'apoio à anamnese'."
    - ".env.local local do fundador tem ao menos as 3 envs Supabase reais preenchidas (NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY) — outras envs ficam vazias até a fase que as usa."
  artifacts:
    - path: ".env.example"
      provides: "Template de envs com 11 chaves do D-11 (vazias)"
      contains: "NEXT_PUBLIC_SUPABASE_URL"
    - path: "vercel.json"
      provides: "Configuração Vercel (region gru1)"
      contains: "gru1"
    - path: "apps/web/.env.local.example"
      provides: "Subset de envs que apps/web/ usa (espelho do root .env.example, mais ergonômico para local dev)"
      contains: "NEXT_PUBLIC_SUPABASE_URL"
  key_links:
    - from: "vercel.json"
      to: "Vercel deploy region"
      via: '"regions": ["gru1"]'
      pattern: '"regions"'
    - from: ".env.example (root)"
      to: ".env.local (fundador, gitignored)"
      via: "fundador copia template e preenche"
      pattern: "cp .env.example .env.local"
---

<objective>
Fechar SETUP-02: cadastrar a estrutura completa de variáveis de ambiente (D-10, D-11) e fazer o primeiro deploy preview da Vercel em região `gru1` (D-06), confirmando que o pipeline `git push -> Vercel build -> /` retorna 200.

Propósito: SETUP-02 exige que envs estejam "presentes em `.env.local` e em Vercel → Environment Variables" mesmo que vazias para serviços ainda não usados. Sem este plan, dogfooding da Fase 1 (Estágio 1 do PROJECT.md) não tem como acontecer porque não há URL pública para o fundador testar. Também é o gate de fim da Fase 1 — depois deste plan, Fase 2 (Auth + Dashboard) pode começar.

Output: `.env.example` no root com 11 chaves (D-11), `vercel.json` com `regions: ["gru1"]`, projeto Vercel criado e linkado, envs Supabase reais cadastradas em Vercel produção/preview, e deploy preview confirmado funcionando.

**Esta plan tem checkpoints humanos** (autonomous: false): criação de conta/projeto Vercel, `vercel link`, e cadastro de envs em Vercel dashboard requerem ação do fundador.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-setup/01-CONTEXT.md
@.planning/intel/constraints.md
@.planning/phases/01-setup/01-03-PLAN.md
@.planning/phases/01-setup/01-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar .env.example (root + apps/web/) e vercel.json com região gru1</name>
  <files>
    .env.example,
    apps/web/.env.local.example,
    vercel.json
  </files>
  <read_first>
    - D:/GDrive/iridologista/.planning/phases/01-setup/01-CONTEXT.md (D-06, D-10, D-11 — lista exata das envs)
    - D:/GDrive/iridologista/.planning/intel/constraints.md ("Stack — *" — para entender em qual fase cada env vai realmente ser usada)
    - D:/GDrive/iridologista/.gitignore (criado em plan 1.1; confirma que .env.local é ignorado)
  </read_first>
  <action>
    Working directory: `D:/GDrive/iridologista/`.

    **1) Criar `.env.example` no root** com **exatamente** as 11 chaves do D-11 (mais comentários por bloco indicando em qual fase cada uma é consumida):

    ```bash
    # .env.example
    # Template de variáveis de ambiente para Aurel Iris.
    # Copie para .env.local (gitignored) e preencha. Em Vercel, cadastre via Dashboard
    # -> Settings -> Environment Variables, separadas por ambiente (Production / Preview / Development).
    # Vide PROJECT.md "Restrições" e CONTEXT.md D-10/D-11.

    # ============================================================
    # SUPABASE (Phase 1 Setup — usado já)
    # ============================================================
    # Project URL: Dashboard -> Settings -> API -> Project URL
    NEXT_PUBLIC_SUPABASE_URL=

    # Public anon key: Dashboard -> Settings -> API -> anon public
    NEXT_PUBLIC_SUPABASE_ANON_KEY=

    # Service role key: Dashboard -> Settings -> API -> service_role
    # NUNCA expor no client. Usar apenas em Route Handlers / scripts server-side.
    SUPABASE_SERVICE_ROLE_KEY=

    # ============================================================
    # ANTHROPIC (Phase 7 — Análise LLM)
    # ============================================================
    # Console: https://console.anthropic.com/settings/keys
    ANTHROPIC_API_KEY=

    # ============================================================
    # VOYAGE AI (Phase 6 — RAG Embeddings)
    # ============================================================
    # Dashboard: https://dash.voyageai.com/api-keys
    VOYAGE_API_KEY=

    # ============================================================
    # MODAL (Phase 5 — Pipeline de Visão)
    # ============================================================
    # Settings: https://modal.com/settings/tokens
    MODAL_TOKEN_ID=
    MODAL_TOKEN_SECRET=

    # HMAC secret usado pelo webhook do Modal -> /api/vision/webhook
    # Gerar com: openssl rand -hex 32
    MODAL_WEBHOOK_SECRET=

    # ============================================================
    # STRIPE (Phase 8 — Pagamento + LGPD)
    # ============================================================
    # Dashboard Brazil: https://dashboard.stripe.com/apikeys
    STRIPE_SECRET_KEY=

    # Webhook signing secret (Stripe Dashboard -> Webhooks -> endpoint -> Signing secret)
    STRIPE_WEBHOOK_SECRET=

    # ============================================================
    # RESEND (Phase 9 — E-mail transacional)
    # ============================================================
    # Dashboard: https://resend.com/api-keys
    RESEND_API_KEY=
    ```

    **Notas de auditoria:**
    - **Exatamente 11 chaves** (contagem D-11), todas vazias.
    - Cada bloco diz em qual fase a env é consumida (apoio ao planning futuro).
    - Comentários **em pt-BR** (consistente com idioma de planejamento do PROJECT.md).
    - Nenhum valor de produção commitado.

    **2) Criar `apps/web/.env.local.example`** — espelho mínimo focado no que apps/web/ usa em runtime (mesmo conteúdo do root mas explícito ao Next.js dev:
    ```bash
    # apps/web/.env.local.example
    # Copie para apps/web/.env.local para desenvolvimento local do Next.js.
    # Next.js carrega .env.local automaticamente (já em .gitignore).
    # Para produção, cadastre em Vercel Dashboard -> Settings -> Environment Variables.

    # Supabase (Phase 1+)
    NEXT_PUBLIC_SUPABASE_URL=
    NEXT_PUBLIC_SUPABASE_ANON_KEY=
    SUPABASE_SERVICE_ROLE_KEY=

    # Anthropic (Phase 7+)
    ANTHROPIC_API_KEY=

    # Voyage AI (Phase 6+)
    VOYAGE_API_KEY=

    # Modal (Phase 5+)
    MODAL_TOKEN_ID=
    MODAL_TOKEN_SECRET=
    MODAL_WEBHOOK_SECRET=

    # Stripe (Phase 8+)
    STRIPE_SECRET_KEY=
    STRIPE_WEBHOOK_SECRET=

    # Resend (Phase 9+)
    RESEND_API_KEY=
    ```

    **3) Criar `vercel.json` no root** — configura região gru1 (D-06) e diz à Vercel que o app vive em `apps/web/`:

    ```json
    {
      "$schema": "https://openapi.vercel.sh/vercel.json",
      "regions": ["gru1"],
      "buildCommand": "pnpm --filter web build",
      "devCommand": "pnpm --filter web dev",
      "installCommand": "pnpm install --frozen-lockfile",
      "framework": "nextjs",
      "outputDirectory": "apps/web/.next",
      "ignoreCommand": "git diff --quiet HEAD^ HEAD ./apps/web ./vercel.json ./package.json ./pnpm-lock.yaml ./pnpm-workspace.yaml"
    }
    ```

    **Notas:**
    - `regions: ["gru1"]` — D-06 (São Paulo, casa com Supabase sa-east-1).
    - `buildCommand`/`devCommand` — pnpm workspace filter (D-03 + D-01).
    - `outputDirectory` — `.next` está dentro de `apps/web/` por causa do monorepo.
    - `ignoreCommand` — Vercel só re-deploya se mudou apps/web/, vercel.json, package.json root, pnpm-lock.yaml, ou pnpm-workspace.yaml. Mudanças em vision-service/ ou supabase/ não disparam build (já cobertas por `.vercelignore` mas reforçado aqui).

    **4) Auditoria final:**
    ```bash
    # Confirma 11 chaves no .env.example
    grep -cE '^[A-Z][A-Z0-9_]+=' .env.example
    # Esperado: 11

    # Confirma que nenhum valor está preenchido
    grep -E '^[A-Z][A-Z0-9_]+=.+' .env.example && echo "FAIL: env values committed" || echo "OK: empty template"

    # Confirma vercel.json
    grep -q '"gru1"' vercel.json && echo "OK"
    ```
  </action>
  <verify>
    <automated>cd D:/GDrive/iridologista && test -f .env.example && test -f apps/web/.env.local.example && test -f vercel.json && [ "$(grep -cE '^[A-Z][A-Z0-9_]+=' .env.example)" -eq 11 ] && grep -q "^NEXT_PUBLIC_SUPABASE_URL=$" .env.example && grep -q "^NEXT_PUBLIC_SUPABASE_ANON_KEY=$" .env.example && grep -q "^SUPABASE_SERVICE_ROLE_KEY=$" .env.example && grep -q "^ANTHROPIC_API_KEY=$" .env.example && grep -q "^VOYAGE_API_KEY=$" .env.example && grep -q "^MODAL_TOKEN_ID=$" .env.example && grep -q "^MODAL_TOKEN_SECRET=$" .env.example && grep -q "^MODAL_WEBHOOK_SECRET=$" .env.example && grep -q "^STRIPE_SECRET_KEY=$" .env.example && grep -q "^STRIPE_WEBHOOK_SECRET=$" .env.example && grep -q "^RESEND_API_KEY=$" .env.example && ! grep -E '^[A-Z][A-Z0-9_]+=.+' .env.example && grep -q '"regions"' vercel.json && grep -q '"gru1"' vercel.json && grep -q '"framework": "nextjs"' vercel.json && grep -q '"buildCommand"' vercel.json && python -c "import json; json.load(open('vercel.json'))" && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `.env.example` no root contém **exatamente 11 linhas** que casam com regex `^[A-Z][A-Z0-9_]+=` (uma por env de D-11).
    - As 11 chaves são literalmente: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `ANTHROPIC_API_KEY`, `VOYAGE_API_KEY`, `MODAL_TOKEN_ID`, `MODAL_TOKEN_SECRET`, `MODAL_WEBHOOK_SECRET`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `RESEND_API_KEY`.
    - Nenhum valor preenchido em `.env.example` (todas as linhas terminam em `=` sem texto após).
    - Arquivo `apps/web/.env.local.example` existe e contém as mesmas 11 chaves.
    - Arquivo `vercel.json` existe e é JSON válido (`json.load` em Python passa).
    - `vercel.json` contém literalmente `"regions"` e `"gru1"`.
    - `vercel.json` contém `"framework": "nextjs"`, `"buildCommand"`, `"outputDirectory"`.
    - Auditoria de vocabulário: zero ocorrências de `diagnóstico|tratamento|cura` (case-insensitive) em `.env.example` ou `vercel.json`.
  </acceptance_criteria>
  <done>
    Estrutura de envs versionada conforme D-10/D-11. Vercel config conforme D-06. Pronto para checkpoint humano (Task 2).
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [HUMAN] Criar projeto Vercel + linkar repo + cadastrar envs Supabase em Production/Preview</name>
  <what-built>
    `.env.example` (template) e `vercel.json` (config) já estão no repo. Falta a parte que só o fundador pode fazer:
    (a) criar o projeto Vercel,
    (b) `vercel link` neste repo,
    (c) cadastrar as 3 envs Supabase reais (URL + anon + service_role) em Production e Preview no Vercel Dashboard,
    (d) confirmar que o deploy preview retorna 200 em `/`.

    Demais envs (Anthropic, Voyage, Modal, Stripe, Resend) ficam **vazias** ou **não cadastradas** por enquanto — são populadas nas fases que de fato as consomem (Phase 5+).
  </what-built>
  <how-to-verify>
    **Passos manuais (rhelton@gmail.com):**

    **1) Criar conta/projeto Vercel:**
    - Acesse https://vercel.com/signup (ou login se já tem).
    - Conecte com GitHub se o repo `iridologista` já estiver lá; se ainda for local apenas, criar repo no GitHub primeiro (`gh repo create rhelton/aurel-iris --private --source=. --remote=origin --push`).
    - No Vercel Dashboard, "Add New Project" -> selecionar o repo.
    - Framework Preset: **Next.js** (deve auto-detectar via `vercel.json`).
    - Root Directory: deixe em `.` (a `vercel.json` no root cuida do monorepo via `buildCommand`).
    - **NÃO** clique em "Deploy" ainda — vamos cadastrar envs primeiro.

    **2) Cadastrar envs Supabase em Production e Preview:**

    No projeto Vercel: Settings -> Environment Variables. Adicione 3 envs, cada uma marcada para os 3 ambientes (Production, Preview, Development):

    | Name | Value | Environments |
    |---|---|---|
    | `NEXT_PUBLIC_SUPABASE_URL` | (do plan 1.3 — Project URL do Supabase) | Production, Preview, Development |
    | `NEXT_PUBLIC_SUPABASE_ANON_KEY` | (do plan 1.3 — anon public key) | Production, Preview, Development |
    | `SUPABASE_SERVICE_ROLE_KEY` | (do plan 1.3 — service_role key) | Production, Preview ⚠ **NUNCA marque "Development" se compartilhar a env com colegas via dashboard** — ok aqui pois é solo dev |

    **NÃO cadastre as outras 8 envs ainda** (Anthropic, Voyage, Modal, Stripe, Resend) — adicione apenas quando a respectiva fase iniciar e exigir.

    **3) Linkar localmente (opcional mas útil):**
    ```bash
    cd D:/GDrive/iridologista
    pnpm dlx vercel@latest link
    # Escolha o projeto criado no passo 1.
    # Cria .vercel/ (já no .gitignore).
    ```

    **4) Disparar primeiro deploy:**
    - No dashboard Vercel, click "Deploy" (se ainda não fez).
    - **OU** faça `git push origin main` (Vercel detecta e builda automaticamente).

    **5) Verificar o deploy:**
    - Aguarde build concluir (~2 min).
    - Vercel dá uma URL tipo `https://aurel-iris-<hash>-rhelton.vercel.app` ou similar.
    - Teste:
      ```bash
      curl -I https://aurel-iris-<seu-deploy>.vercel.app/
      # Esperado: HTTP/2 200
      ```
    - Abra a URL no browser. Você deve ver:
      - Título "Aurel Iris" (h1)
      - Texto "Ferramenta de apoio à anamnese terapêutica integrativa."
      - Texto "Não substitui avaliação médica."

    **6) Verificar dev local também:**
    Antes de aprovar, garante que `pnpm dev` ainda funciona localmente com as envs reais:
    ```bash
    cd D:/GDrive/iridologista
    cp .env.example .env.local
    # Edite .env.local e cole as 3 envs Supabase reais.
    pnpm dev
    # Abra http://localhost:3000 — mesma página renderiza.
    # Crtl+C para parar.
    ```

    **Antes de digitar `approved`, confirme:**
    - [ ] Projeto Vercel criado, linkado ao repo.
    - [ ] As 3 envs Supabase cadastradas em Production + Preview + Development.
    - [ ] Deploy preview retorna 200 em `/` e mostra "Aurel Iris" + "apoio à anamnese".
    - [ ] `apps/web/.env.local` (gitignored) preenchido com as 3 envs Supabase reais; `pnpm dev` mostra a mesma home local.
    - [ ] **Vocabulário compliant:** Página renderizada NÃO contém "diagnóstico", "tratamento" ou "cura" (PROJECT.md restrição não-negociável).
  </how-to-verify>
  <resume-signal>
    Type `approved` (e cole a URL do deploy preview na resposta para o SUMMARY) ou descreva problemas (ex.: "Vercel não encontrou o framework Next.js — ajustar buildCommand", "deploy retornou 500 — checar logs").
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo (git) → Vercel build | Vercel lê `vercel.json` e o repo todo; secrets vêm separadamente via Vercel envs (não do repo). |
| browser (público) → Vercel deploy | Primeiro endpoint público do produto. Página `/` é estática (sem dados sensíveis), mas ainda é vetor para auditoria de vocabulário. |
| Vercel envs → Next.js runtime | `NEXT_PUBLIC_*` envs vão para o client; `SUPABASE_SERVICE_ROLE_KEY` (sem prefix) fica server-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-17 | I (Information Disclosure) | service_role key | mitigate | `SUPABASE_SERVICE_ROLE_KEY` cadastrada em Vercel sem prefix `NEXT_PUBLIC_` -> server-only por convenção Next.js. `.gitignore` (plan 1.1) bloqueia `.env*.local` no commit. |
| T-01-18 | T (Tampering — vocabulário público) | rendered page.tsx | mitigate | Auditoria de vocabulário em apps/web/ feita em plan 1.1; visual check no deploy preview reforça em produção. |
| T-01-19 | I (Information Disclosure) | vercel.json regions | accept | `gru1` em vercel.json é meta-config, não secret. Reside públicamente no repo. |
| T-01-20 | E (Elevation via Vercel preview) | preview URLs | accept | Preview deploys pra branches são por padrão protegidos pelo Vercel SSO em projetos privados. Solo dev hoje, baixo risco; reavaliar quando Phase 9 expor URLs públicas. |
</threat_model>

<verification>
1. Task 1: `.env.example` tem 11 chaves D-11, todas vazias; `vercel.json` é JSON válido com `gru1`.
2. Task 2: deploy preview Vercel responde 200 em `/`; página renderiza "Aurel Iris" e "apoio à anamnese".
3. `pnpm dev` (root, delegando para apps/web) também funciona com `.env.local` populado.
4. Settings -> Environment Variables no Vercel mostra as 3 envs Supabase cadastradas.
</verification>

<success_criteria>
- SETUP-02 satisfeito: envs cadastradas em `.env.example` (template, 11 chaves) e em Vercel (3 reais Supabase + 8 vazias deferidas para fases futuras).
- ROADMAP §1 success criterion #1 satisfeito: `pnpm dev` levanta local; deploy Vercel retorna 200 em `/`.
- D-06 satisfeito: `regions: ["gru1"]` em `vercel.json`.
- D-10/D-11 satisfeitos: `.env.example` versionado, sem valores; valores reais entram em `.env.local` (gitignored) + Vercel Dashboard.
- Fase 1 está pronta para fechar.
</success_criteria>

<output>
Após conclusão das duas tasks, criar `.planning/phases/01-setup/01-06-SUMMARY.md` documentando:
- URL do primeiro deploy preview Vercel.
- Confirmação de que envs Supabase estão cadastradas em Production + Preview + Development.
- Lista das envs ainda **não cadastradas** (Anthropic, Voyage, Modal, Stripe, Resend) e em qual fase entram.
- **Esta é a última plan da Fase 1** — após este SUMMARY, próxima ação é fechar a fase no ROADMAP e iniciar `/gsd-plan-phase 2`.
</output>
