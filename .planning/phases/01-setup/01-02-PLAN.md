---
phase: 01-setup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - vision-service/.python-version
  - vision-service/requirements.txt
  - vision-service/modal_app.py
  - vision-service/pipeline/__init__.py
  - vision-service/pipeline/detect.py
  - vision-service/pipeline/segment.py
  - vision-service/pipeline/compose.py
  - vision-service/pipeline/normalize.py
  - vision-service/pipeline/enhance.py
  - vision-service/pipeline/features.py
  - vision-service/models/.gitkeep
  - vision-service/README.md
autonomous: true
requirements:
  - SETUP-01
must_haves:
  truths:
    - "Diretório vision-service/ existe paralelo a apps/web/ no monorepo (D-01)."
    - "requirements.txt lista as dependências do SPEC §4.2: opencv-python-headless, numpy, scikit-image, mediapipe, torch, torchvision, Pillow, supabase."
    - "modal_app.py declara um Modal app com placeholder analyze_iris que importa o pacote pipeline (esqueleto, não funcional ainda)."
    - "Estrutura pipeline/ tem 6 stubs (detect, segment, compose, normalize, enhance, features) refletindo SPEC §4.2 ordering."
    - "Repo é deployable em fase futura (Fase 5) sem refator estrutural."
  artifacts:
    - path: "vision-service/requirements.txt"
      provides: "Dependências Python pinned para Modal"
      contains: "opencv-python-headless"
    - path: "vision-service/modal_app.py"
      provides: "Modal app definition + analyze_iris stub"
      contains: "modal.App"
    - path: "vision-service/pipeline/__init__.py"
      provides: "Pacote Python pipeline (importável)"
    - path: "vision-service/pipeline/detect.py"
      provides: "Stub para etapa 1: detecção de íris (MediaPipe)"
    - path: "vision-service/pipeline/segment.py"
      provides: "Stub para etapa 2: segmentação Hough (OpenCV)"
    - path: "vision-service/pipeline/compose.py"
      provides: "Stub para etapa 3: photometric stereo composition"
    - path: "vision-service/pipeline/normalize.py"
      provides: "Stub para etapa 4: Daugman polar normalize"
    - path: "vision-service/pipeline/enhance.py"
      provides: "Stub para etapa 5: CLAHE enhancement"
    - path: "vision-service/pipeline/features.py"
      provides: "Stub para etapa 6: feature extraction"
  key_links:
    - from: "vision-service/modal_app.py"
      to: "vision-service/pipeline/*"
      via: "from pipeline import detect, segment, compose, normalize, enhance, features"
      pattern: "from pipeline import"
---

<objective>
Criar o esqueleto do `vision-service/` Python paralelo ao `apps/web/` (D-01 monorepo), com todas as dependências do SPEC §4.2 declaradas e a estrutura de subpacotes `pipeline/` que a Fase 5 vai implementar.

Propósito: A Fase 5 (Pipeline de visão / Modal) será massiva (5-7 dias). Ter o skeleton importável e o `requirements.txt` resolvido agora é um custo trivial que: (a) valida D-01 (monorepo, não dois repos); (b) garante que `.vercelignore` (plan 1.1) realmente exclui esta área; (c) permite à Fase 5 começar direto na lógica do pipeline, não no scaffolding.

Output: `vision-service/` com 6 stubs Python que importam-se mutuamente sem erro, `requirements.txt` com as 8 dependências do SPEC §4.2, e `modal_app.py` declarando um Modal App com a função `analyze_iris` como stub que lança `NotImplementedError`.

Esta fase **NÃO** instala o Modal CLI nem deploya nada — só cria estrutura e código. Modal deploy é Fase 5.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-setup/01-CONTEXT.md
@SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar estrutura vision-service/ com requirements.txt e Modal app stub</name>
  <files>
    vision-service/.python-version,
    vision-service/requirements.txt,
    vision-service/modal_app.py,
    vision-service/README.md,
    vision-service/models/.gitkeep
  </files>
  <read_first>
    - D:/Projetos/Iridologista/SPEC.md §2 (folder layout para vision-service/)
    - D:/Projetos/Iridologista/SPEC.md §4.2 (Modal app — image, deps, função analyze_iris signature)
    - D:/Projetos/Iridologista/.planning/phases/01-setup/01-CONTEXT.md (D-01 monorepo)
    - D:/Projetos/Iridologista/.planning/intel/constraints.md ("Stack — Vision Pipeline" — T4 GPU, 120s timeout, HMAC webhook, input contract)
  </read_first>
  <action>
    Working directory: `D:/Projetos/Iridologista/vision-service/`.

    **1) `.python-version`** (pin Python 3.11 — versão estável e suportada pela Modal image debian_slim default; evita surpresa em Fase 5):
    ```
    3.11
    ```

    **2) `requirements.txt`** (verbatim conforme SPEC §4.2 `pip_install` — 8 deps; pin de versões mínimas para evitar drift, mas sem upper bound rígido por enquanto):
    ```
    # Core deps from SPEC §4.2 (Modal vision pipeline)
    opencv-python-headless>=4.10.0
    numpy>=1.26.0
    scikit-image>=0.24.0
    mediapipe>=0.10.14
    torch>=2.4.0
    torchvision>=0.19.0
    Pillow>=10.4.0
    supabase>=2.7.0

    # Modal SDK (deploy/test). Phase 5 will pin the version.
    modal>=0.64.0
    ```

    **3) `modal_app.py`** (stub que respeita o contrato do SPEC §4.2: app name, image, gpu, timeout, signature; lança NotImplementedError para deixar claro que é skeleton):
    ```python
    """
    Modal app for the Aurel Iris vision pipeline.

    Status: SKELETON ONLY — Phase 1 (Setup).
    Real pipeline implementation lands in Phase 5 (Pipeline de visão / Modal).

    Contract per SPEC §4.2:
      analyze_iris(reading_id: str, image_urls: list[dict]) -> dict
        image_urls: [{"eye": "right" | "left", "angle": "frontal" | "lateral" | "backlight", "url": str}]

    Pipeline stages (SPEC §4.2): detect -> segment -> compose -> normalize -> enhance -> features.
    """

    import modal

    app = modal.App("aurel-iris-vision")

    image = modal.Image.debian_slim().pip_install(
        "opencv-python-headless",
        "numpy",
        "scikit-image",
        "mediapipe",
        "torch",
        "torchvision",
        "Pillow",
        "supabase",
    )


    @app.function(image=image, gpu="T4", timeout=120)
    def analyze_iris(reading_id: str, image_urls: list[dict]) -> dict:
        """
        Analyze iris images and return the canonical features JSON (SPEC §4.3).

        Args:
            reading_id: UUID of the reading row in Supabase (readings.id).
            image_urls: List of {"eye", "angle", "url"} dicts, up to 6 entries.

        Returns:
            Canonical features JSON per SPEC §4.3 with right_eye, left_eye,
            asymmetry_notes, processing_metadata.

        Raises:
            NotImplementedError: This is a Phase 1 skeleton. Real implementation
                                 lands in Phase 5.
        """
        # Import side-effect: validates that the pipeline package is structured
        # correctly. Phase 5 will replace this with real orchestration.
        from pipeline import detect, segment, compose, normalize, enhance, features  # noqa: F401

        raise NotImplementedError(
            "analyze_iris is a Phase 1 skeleton. "
            "Real pipeline implementation will be added in Phase 5 "
            "(see .planning/ROADMAP.md Fase 5: Pipeline de visão / Modal)."
        )


    if __name__ == "__main__":
        # Local sanity check (does NOT run Modal; just validates imports).
        print("Aurel Iris vision-service skeleton loaded.")
        print(f"App name: {app.name}")
    ```

    **4) `vision-service/README.md`** (orienta planner/exec da Fase 5):
    ```markdown
    # vision-service

    Pipeline de visão computacional do Aurel Iris (Modal serverless GPU).

    > **Status:** Esqueleto — Fase 1 (Setup). Implementação real em Fase 5.

    Contrato e arquitetura: ver `SPEC.md` §4 e `.planning/ROADMAP.md` Fase 5.

    ## Setup local (futuro — Fase 5)

    ```bash
    python -m venv .venv
    source .venv/bin/activate  # Linux/Mac
    # .venv\Scripts\activate   # Windows
    pip install -r requirements.txt
    modal token new             # autentica com Modal Cloud
    modal run modal_app.py::analyze_iris   # após implementação
    ```

    ## Estrutura

    - `modal_app.py` — Modal App com função `analyze_iris(reading_id, image_urls)` (SPEC §4.2).
    - `pipeline/` — Etapas do pipeline:
      - `detect.py` — Detecção de íris (MediaPipe Face Mesh, indices 468-477 / 473-477).
      - `segment.py` — Segmentação (Hough circular OpenCV; U-Net pré-treinada CASIA-Iris em v1.1).
      - `compose.py` — Composição photometric stereo (3 ângulos -> 1 imagem rica).
      - `normalize.py` — Normalização polar Daugman (rubber sheet).
      - `enhance.py` — CLAHE.
      - `features.py` — Extração das features finais (SPEC §4.3 schema).
    - `models/` — Pesos pré-treinados (vazio na Fase 1).
    ```

    **5) `vision-service/models/.gitkeep`** — placeholder para `models/` (vazio na Fase 1):
    ```bash
    mkdir -p vision-service/models && touch vision-service/models/.gitkeep
    ```
  </action>
  <verify>
    <automated>cd D:/Projetos/Iridologista && test -f vision-service/.python-version && test -f vision-service/requirements.txt && test -f vision-service/modal_app.py && test -f vision-service/README.md && test -f vision-service/models/.gitkeep && grep -q "opencv-python-headless" vision-service/requirements.txt && grep -q "mediapipe" vision-service/requirements.txt && grep -q "supabase" vision-service/requirements.txt && grep -q "torch" vision-service/requirements.txt && grep -q 'modal.App("aurel-iris-vision")' vision-service/modal_app.py && grep -q 'gpu="T4"' vision-service/modal_app.py && grep -q "timeout=120" vision-service/modal_app.py && grep -q "def analyze_iris" vision-service/modal_app.py && grep -q "from pipeline import" vision-service/modal_app.py && python -c "import ast; ast.parse(open('vision-service/modal_app.py').read())" && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `vision-service/.python-version` contém apenas `3.11`.
    - `vision-service/requirements.txt` contém literalmente as 8 strings: `opencv-python-headless`, `numpy`, `scikit-image`, `mediapipe`, `torch`, `torchvision`, `Pillow`, `supabase` (cada uma em sua própria linha, com versão mínima opcional).
    - `vision-service/requirements.txt` também contém `modal` (SDK).
    - `vision-service/modal_app.py` contém literalmente `modal.App("aurel-iris-vision")`, `gpu="T4"`, `timeout=120`, `def analyze_iris(reading_id: str, image_urls: list[dict])`, e `from pipeline import detect, segment, compose, normalize, enhance, features`.
    - `vision-service/modal_app.py` é um arquivo Python sintaticamente válido (`python -c "import ast; ast.parse(...)"` retorna 0).
    - `vision-service/README.md` referencia explicitamente "Fase 5" e "SPEC.md §4".
    - `vision-service/models/` existe (com `.gitkeep`).
  </acceptance_criteria>
  <done>
    `vision-service/` é deployable em Fase 5 sem refator. Contratos do SPEC §4.2 (app name, GPU, timeout, signature) estão fixados. Subpackage `pipeline/` será criado no Task 2 deste plan.
  </done>
</task>

<task type="auto">
  <name>Task 2: Criar pipeline/ subpackage com 6 stubs (detect → segment → compose → normalize → enhance → features)</name>
  <files>
    vision-service/pipeline/__init__.py,
    vision-service/pipeline/detect.py,
    vision-service/pipeline/segment.py,
    vision-service/pipeline/compose.py,
    vision-service/pipeline/normalize.py,
    vision-service/pipeline/enhance.py,
    vision-service/pipeline/features.py
  </files>
  <read_first>
    - D:/Projetos/Iridologista/SPEC.md §4.2 (ordem das 6 etapas e bibliotecas usadas em cada uma)
    - D:/Projetos/Iridologista/SPEC.md §4.3 (estrutura do JSON de saída — referência para o stub features.py)
    - D:/Projetos/Iridologista/SPEC.md §4.4 (modelos e bibliotecas do MVP — MediaPipe, Hough, OpenCV heuristics, HSV clustering)
    - D:/Projetos/Iridologista/vision-service/modal_app.py (recém-criado; a importação `from pipeline import detect, segment, compose, normalize, enhance, features` deve funcionar)
  </read_first>
  <action>
    Working directory: `D:/Projetos/Iridologista/vision-service/pipeline/`.

    **1) `pipeline/__init__.py`** (marca o diretório como pacote Python; reexporta os 6 módulos):
    ```python
    """
    Aurel Iris vision pipeline stages (SPEC §4.2).

    Order: detect -> segment -> compose -> normalize -> enhance -> features.

    Status: SKELETON ONLY — Phase 1 (Setup). Real implementations land in Phase 5.
    """

    from . import compose, detect, enhance, features, normalize, segment

    __all__ = ["detect", "segment", "compose", "normalize", "enhance", "features"]
    ```

    **2) `pipeline/detect.py`** — etapa 1: detecção de íris via MediaPipe Face Mesh (SPEC §4.4):
    ```python
    """
    Stage 1/6: Iris detection via MediaPipe Face Mesh.

    Per SPEC §4.4 + §4.1: MediaPipe FaceLandmarker with iris landmark indices
    468-477 (right eye) and 473-477 (left eye).

    Status: skeleton.
    """


    def find_iris(image):
        """
        Locate iris in a single eye image and return landmark coordinates.

        Args:
            image: numpy array (H, W, 3), BGR or RGB.

        Returns:
            dict with iris center (x, y), radius, and landmarks.

        Raises:
            NotImplementedError: Phase 5 implements this.
        """
        raise NotImplementedError("pipeline.detect.find_iris — implement in Phase 5")
    ```

    **3) `pipeline/segment.py`** — etapa 2: segmentação Hough circular (OpenCV baseline):
    ```python
    """
    Stage 2/6: Iris segmentation.

    MVP baseline (SPEC §4.4): Hough Transform circular via OpenCV.
    v1.1 upgrade: U-Net pre-trained on CASIA-Iris.

    Status: skeleton.
    """


    def iris_mask(image, detection):
        """
        Compute a binary mask isolating the iris region.

        Args:
            image: numpy array (H, W, 3).
            detection: dict from pipeline.detect.find_iris.

        Returns:
            dict with binary_mask (H, W, bool) and segmented_image.

        Raises:
            NotImplementedError: Phase 5 implements this.
        """
        raise NotImplementedError("pipeline.segment.iris_mask — implement in Phase 5")
    ```

    **4) `pipeline/compose.py`** — etapa 3: photometric stereo combine (3 ângulos -> 1):
    ```python
    """
    Stage 3/6: Photometric composition.

    Combines 3 angles per eye (frontal, lateral, backlight) into a single
    information-rich representation.

    Status: skeleton.
    """


    def photometric_combine(segmented_images):
        """
        Combine multiple segmented eye images into a composite.

        Args:
            segmented_images: list of dicts from pipeline.segment.iris_mask
                              (one per angle: frontal, lateral, backlight).

        Returns:
            Composite image enhanced for downstream feature extraction.

        Raises:
            NotImplementedError: Phase 5 implements this.
        """
        raise NotImplementedError(
            "pipeline.compose.photometric_combine — implement in Phase 5"
        )
    ```

    **5) `pipeline/normalize.py`** — etapa 4: Daugman polar (rubber sheet):
    ```python
    """
    Stage 4/6: Polar normalization (Daugman rubber sheet).

    Converts the iris (donut shape) into a rectangular polar representation
    standard in iridology and biometric pipelines.

    Status: skeleton.
    """


    def daugman_polar(composite_image):
        """
        Apply Daugman's rubber-sheet model to map the iris ring to a rectangle.

        Args:
            composite_image: output of pipeline.compose.photometric_combine.

        Returns:
            Rectangular polar image of the iris.

        Raises:
            NotImplementedError: Phase 5 implements this.
        """
        raise NotImplementedError(
            "pipeline.normalize.daugman_polar — implement in Phase 5"
        )
    ```

    **6) `pipeline/enhance.py`** — etapa 5: CLAHE (contrast):
    ```python
    """
    Stage 5/6: Contrast enhancement (CLAHE).

    Status: skeleton.
    """


    def clahe(normalized_image):
        """
        Apply Contrast Limited Adaptive Histogram Equalization.

        Args:
            normalized_image: output of pipeline.normalize.daugman_polar.

        Returns:
            Enhanced image ready for feature extraction.

        Raises:
            NotImplementedError: Phase 5 implements this.
        """
        raise NotImplementedError("pipeline.enhance.clahe — implement in Phase 5")
    ```

    **7) `pipeline/features.py`** — etapa 6: extração final das features (SPEC §4.3 schema):
    ```python
    """
    Stage 6/6: Feature extraction.

    Produces the per-eye block of the canonical features JSON (SPEC §4.3):
    constitution, iris_color, fiber_density, collarette, pupil, sectors[],
    rings, global_signs, image_quality.

    MVP techniques (SPEC §4.4):
      - HSV clustering for iris_color.
      - OpenCV heuristics (adaptive threshold + morphology) for lacunas/criptas.
      - Constitutional palette comparison for constitution.

    Status: skeleton.
    """


    def extract_all(enhanced_image, composite_image):
        """
        Extract the full per-eye features block.

        Args:
            enhanced_image: output of pipeline.enhance.clahe.
            composite_image: output of pipeline.compose.photometric_combine
                             (kept for color analysis pre-CLAHE).

        Returns:
            Dict matching SPEC §4.3 per-eye shape:
              constitution, iris_color, fiber_density, collarette, pupil,
              sectors, rings, global_signs, image_quality.

        Raises:
            NotImplementedError: Phase 5 implements this.
        """
        raise NotImplementedError(
            "pipeline.features.extract_all — implement in Phase 5"
        )
    ```

    **Sanity check** após criar os arquivos:
    ```bash
    cd D:/Projetos/Iridologista/vision-service && python -c "import ast, os; [ast.parse(open(os.path.join('pipeline', f)).read()) for f in os.listdir('pipeline') if f.endswith('.py')]" && echo "ALL_PARSE_OK"
    ```
  </action>
  <verify>
    <automated>cd D:/Projetos/Iridologista/vision-service && test -f pipeline/__init__.py && test -f pipeline/detect.py && test -f pipeline/segment.py && test -f pipeline/compose.py && test -f pipeline/normalize.py && test -f pipeline/enhance.py && test -f pipeline/features.py && grep -q "from . import compose, detect, enhance, features, normalize, segment" pipeline/__init__.py && grep -q "def find_iris" pipeline/detect.py && grep -q "def iris_mask" pipeline/segment.py && grep -q "def photometric_combine" pipeline/compose.py && grep -q "def daugman_polar" pipeline/normalize.py && grep -q "def clahe" pipeline/enhance.py && grep -q "def extract_all" pipeline/features.py && python -c "import ast, os; [ast.parse(open(os.path.join('pipeline', f)).read()) for f in os.listdir('pipeline') if f.endswith('.py')]" && python -c "import sys; sys.path.insert(0, '.'); from pipeline import detect, segment, compose, normalize, enhance, features; print('IMPORT_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - 7 arquivos existem em `vision-service/pipeline/`: `__init__.py`, `detect.py`, `segment.py`, `compose.py`, `normalize.py`, `enhance.py`, `features.py`.
    - `pipeline/__init__.py` reexporta os 6 módulos via `from . import ...`.
    - Cada módulo tem exatamente 1 função pública conforme SPEC §4.2 (`find_iris`, `iris_mask`, `photometric_combine`, `daugman_polar`, `clahe`, `extract_all`) que lança `NotImplementedError` com mensagem mencionando "Phase 5".
    - Cada módulo é Python sintaticamente válido (`ast.parse` retorna sem erro).
    - Import `from pipeline import detect, segment, compose, normalize, enhance, features` executa sem erro a partir de `vision-service/`.
    - Cada módulo tem docstring referenciando a SPEC (§4.2, §4.3 ou §4.4).
  </acceptance_criteria>
  <done>
    Subpackage `pipeline/` importável a partir de `vision-service/modal_app.py`. As 6 etapas do SPEC §4.2 estão fixadas em arquivo. Fase 5 começa direto preenchendo cada `NotImplementedError` em vez de criar a estrutura.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none yet) | vision-service não recebe input nesta fase — só skeleton. Trust boundaries reais entram em Fase 5 (HMAC webhook Modal -> Next.js, Supabase signed URLs). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | E (Elevation via supply chain) | requirements.txt | accept | Pacotes pinados com versão mínima conhecida (opencv-python-headless 4.10+, mediapipe 0.10.14+, torch 2.4+); Fase 5 fará pin completo (==) e auditoria de hashes via `pip-audit`. Risco baixo na Fase 1 porque nenhum código real é executado. |
| T-01-05 | T (Tampering) | modal_app.py | accept | App name `aurel-iris-vision` é namespace público no Modal Cloud — sem deploy nesta fase, sem superfície ainda. Fase 5 adiciona HMAC webhook (D-CONTEXT D-11 `MODAL_WEBHOOK_SECRET`). |
</threat_model>

<verification>
1. `python -c "import ast, os; [ast.parse(open(os.path.join('pipeline', f)).read()) for f in os.listdir('pipeline') if f.endswith('.py')]"` em vision-service/ retorna 0.
2. `python -c "from pipeline import detect, segment, compose, normalize, enhance, features"` em vision-service/ retorna 0.
3. `python -c "import ast; ast.parse(open('modal_app.py').read())"` em vision-service/ retorna 0.
4. `git status` mostra `vision-service/` como novo diretório com 11 arquivos novos.
5. `.vercelignore` (criado em plan 1.1) cobre `vision-service/` — Vercel deploy futuro (plan 1.6) não vai tentar bundlar nada disso.
</verification>

<success_criteria>
- `vision-service/` está estruturado conforme SPEC §2 + §4.2.
- `requirements.txt` lista todas as 8 deps do SPEC §4.2 + `modal`.
- `modal_app.py` declara o Modal App com signature exata do SPEC §4.2 (`@app.function(image=image, gpu="T4", timeout=120)`).
- Subpackage `pipeline/` é importável e cobre as 6 etapas em ordem.
- Fase 5 (Pipeline de visão) pode começar sem refator estrutural.
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-setup/01-02-SUMMARY.md` documentando:
- Versões pinadas em `requirements.txt` (snapshot do que foi escolhido).
- Confirmação de que `from pipeline import ...` funciona.
- Próximo plan: 1.3 (Supabase init — Wave 2, depende de 1.1).
</output>
