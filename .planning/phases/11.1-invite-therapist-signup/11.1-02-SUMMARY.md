---
phase: 11.1-invite-therapist-signup
plan: 02
subsystem: auth
tags: [vitest, tdd, server-action, therapist-invites, supabase, postgres, unique-constraint]

requires:
  - phase: 11.1-invite-therapist-signup
    plan: 01
    provides: "therapist_invites table + clients_therapist_email_unique constraint + types/database.ts regen"

provides:
  - "inviteTherapistAction reescrita: D-DUPE check via listUsers + INSERT em therapist_invites + URL /convite-terapeuta/[token]"
  - "createClientAction trata UNIQUE violation 23505 com mensagem amigável"
  - "7 testes vitest GREEN cobrindo todos os paths do invite action"

affects:
  - "11.1-03: /convite-terapeuta/[token] pode ser construída — actionLink agora aponta para essa rota"
  - "apps/web/app/admin/terapeutas/InviteTherapistForm.tsx: sem mudança necessária (shape idêntico)"
  - "/clientes (novo cliente): UNIQUE violation agora devolve mensagem amigável ao invés de erro cru"

tech-stack:
  added: []
  patterns:
    - "TDD RED/GREEN: test commit + feat commit separados por task"
    - "vi.hoisted() para factory de mocks que precisam ser hoisted antes dos vi.mock() calls"
    - "D-DUPE pattern: listUsers perPage:1000 + filter client-side (lowercase) antes de INSERT"
    - "PostgreSQL error code 23505 check em server actions para UNIQUE violations"

key-files:
  created:
    - apps/web/app/actions/admin-therapists.test.ts
  modified:
    - apps/web/app/actions/admin-therapists.ts
    - apps/web/app/actions/clients.ts

key-decisions:
  - "D-DUPE via listUsers+filter ao invés de getUserByEmail (SDK v2 não tem filter oficial por email)"
  - "userStatus simplificado para 'new_invited' apenas — sem fallback existing_magiclink (removido)"
  - "mockSelect removida do destructure vi.hoisted (lint no-unused-vars) — chain funciona sem referência direta"

patterns-established:
  - "Error code 23505 pattern: if (error.code === '23505' && error.message.includes('<constraint_name>')) — aplicar em todos INSERT futuros"
  - "vi.hoisted factory: exportar apenas mocks referenciados diretamente em assertions"

requirements-completed: [ONBOARD-01]

duration: 4min
completed: 2026-05-26
---

# Phase 11.1 Plan 02: inviteTherapistAction rewrite + clients UNIQUE handling Summary

**inviteTherapistAction reescrita sem generateLink — D-DUPE check (listUsers + filter) + INSERT em therapist_invites retorna URL /convite-terapeuta/{token}; createClientAction trata 23505 amigavelmente; 7 testes vitest GREEN.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-05-26T17:19:24Z
- **Completed:** 2026-05-26T17:23:20Z
- **Tasks:** 3 (TDD: Task 1 RED + Task 2 GREEN + Task 3)
- **Files modified:** 3

## Accomplishments

- Remove completo de `service.auth.admin.generateLink` e fallback `existing_magiclink` — bug PKCE eliminado na raiz
- D-DUPE implementado: `service.auth.admin.listUsers({ perPage: 1000 })` + filter client-side lowercase — bloqueia "E-mail já cadastrado como terapeuta neste sistema."
- INSERT em `therapist_invites` (token uuid PK gerado pelo DB) + `actionLink = ${siteUrl}/convite-terapeuta/${token}`
- 7 testes vitest escritos RED-first, todos GREEN após rewrite
- `createClientAction` trata `23505 + clients_therapist_email_unique` com "Cliente com este e-mail já cadastrado em sua lista."

## Task Commits

Cada task commitada atomicamente:

1. **Task 1: Tests RED para inviteTherapistAction** - `dc07f0e` (test)
2. **Task 2: Rewrite inviteTherapistAction (GREEN)** - `91f7e3f` (feat)
3. **Task 3: createClientAction handle UNIQUE violation** - `ee4cd34` (feat)
4. **Lint fix: mockSelect no-unused-vars** - `6fbaff1` (style, deviation Rule 1)

_TDD: RED commit (dc07f0e) → GREEN commit (91f7e3f) gate validado._

## Files Created/Modified

- `apps/web/app/actions/admin-therapists.test.ts` — 7 casos vitest: founder gate, email inválido, self-invite, D-DUPE block, happy path new_invited, INSERT fail, case-insensitive match
- `apps/web/app/actions/admin-therapists.ts` — inviteTherapistAction reescrita; deleteTherapistAction intocada
- `apps/web/app/actions/clients.ts` — createClientAction: tratamento amigável de 23505

## Decisions Made

- **D-DUPE via listUsers+filter:** SDK Supabase v2 não tem `getUserByEmail` direto no admin API; `listUsers` com `perPage:1000` + filter client-side é o padrão correto para beta (paginar em V1.1 se prod > 1000 users)
- **userStatus simplificado:** Apenas `'new_invited'` — sem `'existing_magiclink'`. D-DUPE bloqueia em vez de fallback, mantendo fluxo mais limpo e auditável
- **Sem mock para `mockSelect` no destructure:** `vi.hoisted` retorna a função mas ela não é referenciada diretamente em assertions (chain: mockInsert → mockSelect → mockSingle). ESLint reportaria warning; removida do destructure sem afetar comportamento

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Lint warning: mockSelect destruturada mas não usada em assertions**
- **Found during:** Task 1 (lint verification pós-commit de Task 3)
- **Issue:** ESLint `no-unused-vars` warning em `admin-therapists.test.ts` linha 10 — `mockSelect` era destruturada do `vi.hoisted()` mas não referenciada diretamente em assertions (usada via chain interno `mockInsert→mockSelect→mockSingle`)
- **Fix:** Removida do destructure externo; factory ainda retorna e encadeia corretamente
- **Files modified:** `apps/web/app/actions/admin-therapists.test.ts`
- **Verification:** `pnpm lint` limpo nos arquivos modificados; 7 testes ainda GREEN
- **Committed in:** `6fbaff1` (style commit separado)

---

**Total deviations:** 1 auto-fixed (Rule 1 - lint/quality)
**Impact on plan:** Fix mínimo, sem mudança de comportamento. Lint baseline preservado nos arquivos do plano.

## Issues Encountered

Nenhum bloqueador. tsc reportou 22 erros pre-existing nos 4 arquivos de teste conhecidos (debt de Phase 3, documentado em MEMORY `feedback_quality_scoring_test_gate`) — zero erros novos introduzidos.

## TDD Gate Compliance

- [x] RED gate: `test(11.1-02)` commit `dc07f0e` — 4/7 testes falhando conforme esperado (Tests 4-7 pois mock `generateLink` não existia)
- [x] GREEN gate: `feat(11.1-02)` commit `91f7e3f` — 7/7 testes passando

## Known Stubs

Nenhum stub introduzido — apenas rewrite de server action existente + tratamento de erro.

## Threat Flags

| Flag | File | Description |
|------|------|-------------|
| threat_flag: info_disclosure | apps/web/app/actions/admin-therapists.ts | listUsers retorna lista completa de 1000 users em memória; filtrado client-side — exposição mínima (service role, nunca client-side) |

Nota: T-11.1-02-02 (listUsers leak) documentado no `<threat_model>` do plano com disposição `mitigate` — service role + isFounderEmail gate combinados; resposta descartada após boolean check. Sem informação vazada para o caller.

## Next Phase Readiness

- **11.1-03 desbloqueado:** `actionLink` aponta para `/convite-terapeuta/${token}` — rota pode ser construída na Wave 3
- **InviteTherapistForm.tsx:** Sem mudança necessária — consome `{ actionLink, userStatus, email }` com shape idêntico (apenas `userStatus` perdeu `'existing_magiclink'` como valor possível)
- **createClientAction:** Constraint `clients_therapist_email_unique` (migration 0033) agora tratada graciosamente

## Self-Check: PASSED

- [x] `apps/web/app/actions/admin-therapists.test.ts` FOUND
- [x] `apps/web/app/actions/admin-therapists.ts` FOUND (modified)
- [x] `apps/web/app/actions/clients.ts` FOUND (modified)
- [x] Commit `dc07f0e` FOUND (test RED)
- [x] Commit `91f7e3f` FOUND (feat GREEN)
- [x] Commit `ee4cd34` FOUND (feat clients.ts)
- [x] Commit `6fbaff1` FOUND (style lint fix)
- [x] 7 testes vitest GREEN confirmado (saída: "7 passed (7)")
- [x] tsc: zero erros em clients.ts e admin-therapists.ts
- [x] lint: zero erros/warnings nos arquivos modificados

---
*Phase: 11.1-invite-therapist-signup*
*Completed: 2026-05-26*
