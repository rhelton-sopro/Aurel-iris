---
phase: 11.1-invite-therapist-signup
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - apps/web/app/actions/admin-therapists.ts
  - apps/web/app/actions/admin-therapists.test.ts
  - apps/web/app/actions/clients.ts
autonomous: true
requirements:
  - ONBOARD-01

must_haves:
  truths:
    - "inviteTherapistAction chamado com email já em auth.users retorna { ok:false, error:'E-mail já cadastrado como terapeuta neste sistema.' } sem INSERT em therapist_invites"
    - "inviteTherapistAction chamado com email novo INSERTa row em therapist_invites + retorna { ok:true, actionLink:'${siteUrl}/convite-terapeuta/${token}', userStatus:'new_invited', email }"
    - "Lookup de email em auth.users usa service.auth.admin.listUsers + filter por email (case-insensitive via citext na nossa col, mas auth.users.email é text e Supabase faz lowercase)"
    - "createClientAction (ou wherever clients são criados) trata UNIQUE violation com mensagem amigável 'Cliente com este e-mail já cadastrado em sua lista'"
    - "Sem fallback magic-link — userStatus simplifica pra 'new_invited' ou error"
    - "InviteTherapistForm.tsx NÃO precisa mudança (consome { actionLink, userStatus, email } como antes; apenas valores possíveis mudam)"
  artifacts:
    - path: "apps/web/app/actions/admin-therapists.ts"
      provides: "inviteTherapistAction rewrite — auth.users check + INSERT therapist_invites + retorna URL /convite-terapeuta/[token]"
      contains: "therapist_invites"
    - path: "apps/web/app/actions/admin-therapists.test.ts"
      provides: "vitest cobrindo: founder gate, email inválido, email duplicado (auth.users existing), email novo happy path, INSERT fail"
      contains: "inviteTherapistAction"
    - path: "apps/web/app/actions/clients.ts"
      provides: "createClientAction (ou equivalent) com try/catch em UNIQUE violation → mensagem amigável"
      contains: "23505"
  key_links:
    - from: "apps/web/app/actions/admin-therapists.ts"
      to: "therapist_invites table"
      via: "service.from('therapist_invites').insert({ email, invited_by: user.id })"
      pattern: "therapist_invites"
    - from: "apps/web/app/actions/admin-therapists.ts"
      to: "/convite-terapeuta/[token]"
      via: "actionLink string template"
      pattern: "/convite-terapeuta/"
---

<objective>
Reescrever `inviteTherapistAction` para usar therapist_invites table em vez de `service.auth.admin.generateLink({type:'invite'})`. Adicionar check D-DUPE (auth.users email lookup → bloqueia se existe). Atualizar createClientAction para tratamento amigável de UNIQUE violation. Tests vitest cobrindo todos os paths.

Purpose: D-DUPE + D-ROUTE + D-CLIENT em código — sem isso o fix do invite não funciona end-to-end.

Output: 2 arquivos modificados + 1 arquivo de teste + 1 commit atômico.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11.1-invite-therapist-signup/11.1-CONTEXT.md
@.planning/phases/11.1-invite-therapist-signup/11.1-01-SUMMARY.md
@apps/web/app/actions/admin-therapists.ts
@apps/web/app/actions/clients.ts
@apps/web/app/admin/terapeutas/InviteTherapistForm.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tests RED para inviteTherapistAction rewrite</name>
  <files>apps/web/app/actions/admin-therapists.test.ts</files>
  <read_first>
    - apps/web/app/actions/admin-therapists.ts (signature atual + InviteTherapistResult interface)
    - apps/web/app/actions/admin-therapists.test.ts (se já existir — pode não haver; usar pattern de onboarding.test.ts da Fase 9)
  </read_first>
  <behavior>
    - Test 1: Não-founder chama inviteTherapistAction → { ok:false, error:'Não autorizado.' } (gate inalterado)
    - Test 2: Email inválido → { ok:false, error:'E-mail inválido.' }
    - Test 3: Founder próprio email → { ok:false, error:'Não dá pra convidar o founder a si mesmo.' }
    - Test 4: Email já em auth.users → { ok:false, error:'E-mail já cadastrado como terapeuta neste sistema.' } + NÃO chamou .insert() em therapist_invites
    - Test 5: Email novo + INSERT OK → { ok:true, actionLink:`${siteUrl}/convite-terapeuta/${token}`, userStatus:'new_invited', email }
    - Test 6: Email novo + INSERT fail → { ok:false, error:'Falha ao criar convite: ${msg}' }
    - Test 7: Service.auth.admin.listUsers (lookup duplicate) faz lowercase + trim no email antes de matching (idempotência case)
  </behavior>
  <action>
    Criar/atualizar `apps/web/app/actions/admin-therapists.test.ts` com 7 cases vitest:
    - Mock `@/lib/supabase/server` createClient + `@/lib/supabase/service` createServiceClient
    - Mock `@/lib/auth/founder` isFounderEmail
    - Stub do service: from('therapist_invites').insert().select().single() retorna { token: 'fake-uuid-token' }
    - Stub do service.auth.admin.listUsers retorna users array configurável

    Usar vi.hoisted() para factories. RED first — tests devem falhar com signature atual.
  </action>
  <acceptance_criteria>
    - `grep -c "describe.*inviteTherapistAction" apps/web/app/actions/admin-therapists.test.ts` retorna ≥1.
    - 7 test cases definidos.
    - `pnpm --filter web vitest run app/actions/admin-therapists.test.ts` falha (RED) por causa do behavior antigo (generateLink path) — esperado.
  </acceptance_criteria>
  <verify>
    <automated>echo "RED phase — test failures expected"</automated>
  </verify>
  <done>
    7 tests vitest definidos. RED commit prepared.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite inviteTherapistAction (GREEN)</name>
  <files>apps/web/app/actions/admin-therapists.ts</files>
  <read_first>
    - apps/web/app/actions/admin-therapists.ts (estrutura atual: founder gate + email normalize + service.auth.admin.generateLink + fallback magiclink)
    - .planning/phases/11.1-invite-therapist-signup/11.1-CONTEXT.md (D-DUPE/D-ROUTE)
  </read_first>
  <action>
    Reescrever `inviteTherapistAction` em `apps/web/app/actions/admin-therapists.ts`:

    ```typescript
    export interface InviteTherapistResult {
      ok: boolean
      error?: string
      /** URL pra copiar e enviar via WhatsApp. Presente quando ok=true. */
      actionLink?: string
      /** new_invited = token gerado em therapist_invites. */
      userStatus?: 'new_invited'
      /** E-mail normalizado (lowercase + trim) pra confirmar pro founder. */
      email?: string
    }

    /**
     * Gera link de cadastro pro founder copiar e enviar via WhatsApp.
     * Hand-held protocol da Fase 11 + signup fix da Fase 11.1 (2026-05-26).
     *
     * Fluxo:
     * 1. Founder gate via isFounderEmail.
     * 2. Email normalize + validate.
     * 3. D-DUPE: check auth.users — bloqueia se já existe.
     * 4. INSERT em therapist_invites (token gerado pelo DB, expires_at default 7d).
     * 5. Retorna actionLink = ${siteUrl}/convite-terapeuta/${token}.
     *
     * Diferente da Fase 11 11-01: NÃO usa supabase.auth.admin.generateLink (que
     * causava bug PKCE com /dashboard como redirectTo). O novo flow vai pra
     * /convite-terapeuta/[token] que renderiza signup form com email
     * pré-preenchido + OTP no submit.
     */
    export async function inviteTherapistAction(
      emailRaw: string,
    ): Promise<InviteTherapistResult> {
      const supabase = await createClient()
      const {
        data: { user },
      } = await supabase.auth.getUser()
      if (!user || !isFounderEmail(user.email)) {
        return { ok: false, error: 'Não autorizado.' }
      }

      const email = emailRaw.trim().toLowerCase()
      if (!email || !/.+@.+\..+/.test(email)) {
        return { ok: false, error: 'E-mail inválido.' }
      }
      if (isFounderEmail(email)) {
        return { ok: false, error: 'Não dá pra convidar o founder a si mesmo.' }
      }

      const service = createServiceClient()

      // D-DUPE: bloqueia se email já em auth.users.
      // service.auth.admin.listUsers retorna paginado; usamos perPage:1000 e
      // filtramos client-side (não há filter param oficial por email no SDK v2).
      // Se prod crescer pra 1000+ users, paginar adequadamente.
      const { data: usersList, error: listErr } =
        await service.auth.admin.listUsers({ perPage: 1000 })
      if (listErr) {
        return {
          ok: false,
          error: `Falha ao validar e-mail: ${listErr.message}`,
        }
      }
      const exists = (usersList?.users ?? []).some(
        (u) => (u.email ?? '').toLowerCase() === email,
      )
      if (exists) {
        return {
          ok: false,
          error: 'E-mail já cadastrado como terapeuta neste sistema.',
        }
      }

      // INSERT em therapist_invites — token gerado pelo DB.
      const { data: invite, error: insertErr } = await service
        .from('therapist_invites')
        .insert({
          email,
          invited_by: user.id,
        })
        .select('token')
        .single()

      if (insertErr || !invite) {
        return {
          ok: false,
          error: `Falha ao criar convite: ${insertErr?.message ?? 'sem token'}`,
        }
      }

      const siteUrl =
        process.env.NEXT_PUBLIC_SITE_URL?.replace(/\/$/, '') ??
        'https://iriscodex.com'
      const actionLink = `${siteUrl}/convite-terapeuta/${invite.token}`

      console.log('[admin-therapists] INVITE_TOKEN_GENERATED', {
        targetEmail: email,
        token: invite.token,
        userStatus: 'new_invited',
        by: user.email,
        at: new Date().toISOString(),
      })

      return {
        ok: true,
        actionLink,
        userStatus: 'new_invited',
        email,
      }
    }
    ```

    Remove o fallback magiclink path (não é mais necessário — D-DUPE bloqueia ao invés de fallback).

    NÃO mudar deleteTherapistAction (Task fora do escopo).

    Tests Task 1 devem ficar GREEN após este edit.
  </action>
  <acceptance_criteria>
    - `grep -c "therapist_invites" apps/web/app/actions/admin-therapists.ts` retorna ≥2 (insert table name + 1 outro)
    - `grep -c "service.auth.admin.generateLink" apps/web/app/actions/admin-therapists.ts` retorna 0 (removido)
    - `grep -c "convite-terapeuta" apps/web/app/actions/admin-therapists.ts` retorna ≥1
    - `grep -c "E-mail já cadastrado" apps/web/app/actions/admin-therapists.ts` retorna 1
    - `grep -c "userStatus.*new_invited" apps/web/app/actions/admin-therapists.ts` retorna ≥1
    - `grep -c "existing_magiclink" apps/web/app/actions/admin-therapists.ts` retorna 0 (removido)
    - 7 testes vitest GREEN
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && pnpm vitest run app/actions/admin-therapists.test.ts</automated>
  </verify>
  <done>
    inviteTherapistAction reescrita usando therapist_invites table + D-DUPE check. 7 testes GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 3: createClientAction handle UNIQUE violation</name>
  <files>apps/web/app/actions/clients.ts</files>
  <read_first>
    - apps/web/app/actions/clients.ts (createClientAction ou equivalent — verify name + signature)
    - .planning/phases/11.1-invite-therapist-signup/11.1-CONTEXT.md (D-CLIENT)
  </read_first>
  <action>
    Localizar a função que faz INSERT em `clients` (provavelmente `createClientAction` ou `createClient`). Adicionar try/catch ou error code check pra UNIQUE violation (PostgreSQL error code `23505`).

    Padrão:
    ```typescript
    const { data, error } = await supabase.from('clients').insert({...}).select().single()
    if (error) {
      if (error.code === '23505' && error.message.includes('clients_therapist_email_unique')) {
        return { ok: false, error: 'Cliente com este e-mail já cadastrado em sua lista.' }
      }
      return { ok: false, error: `Falha ao criar cliente: ${error.message}` }
    }
    ```

    Se o action atual já tem dedup logic ad-hoc (verificar com grep antes), preferir o DB constraint catch sobre o pre-check (atomicidade).

    Se não houver createClientAction (clientes são criados via outro caminho), revisar grep "from('clients').insert" e aplicar o handle em cada call site.

    Sem adicionar testes novos — comportamento é dirigido por DB constraint que só passa a existir após migration 0033 aplicada (não tem como mockar fielmente sem cobertura E2E).
  </action>
  <acceptance_criteria>
    - `grep -rc "clients_therapist_email_unique\\|23505" apps/web/app/actions/clients.ts` retorna ≥1
    - `grep -c "Cliente com este e-mail" apps/web/app/actions/clients.ts` retorna ≥1
    - pnpm tsc baseline preservado
    - pnpm lint baseline preservado
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit 2>&1 | grep "app/actions/clients\\.ts" | grep -v "^$" && exit 1 || echo "TSC OK"</automated>
  </verify>
  <done>
    createClientAction (ou call site equivalent) trata 23505 com mensagem amigável quando constraint violar. tsc + lint preservados.
  </done>
</task>

</tasks>

<threat_model>
| ID | Cat | Component | Disposition | Mitigation |
|----|-----|-----------|-------------|------------|
| T-11.1-02-01 | Spoofing | Non-founder calls action | mitigate | isFounderEmail gate (defense-in-depth com middleware) |
| T-11.1-02-02 | Info disclosure | listUsers leak | mitigate | service role; resposta filtrada client-side (só boolean) |
| T-11.1-02-03 | DoS | listUsers 1000+ pagination | accept | V1 beta — paginate em V1.1 |
| T-11.1-02-04 | Tampering | INSERT token sem auth | mitigate | service role + isFounderEmail gate combine |
| T-11.1-02-05 | LGPD vocab | Mensagens contém "diagnóstico"? | accept | "cliente", "convite", "e-mail" — sem vocab proibido |
</threat_model>

<verification>
- inviteTherapistAction rewrite faz INSERT em therapist_invites + retorna /convite-terapeuta URL
- D-DUPE bloqueia email já em auth.users
- createClientAction handle UNIQUE violation amigavelmente
- 7 testes vitest GREEN
- tsc + lint baseline preservados
</verification>

<success_criteria>
- inviteTherapistAction reescrito (sem generateLink) + D-DUPE check
- createClientAction trata 23505
- 7 tests GREEN
- Commit atômico: `feat(11.1-02): inviteTherapistAction via therapist_invites table + clients UNIQUE handle`
</success_criteria>

<output>
After completion, create `.planning/phases/11.1-invite-therapist-signup/11.1-02-SUMMARY.md`:
- Arquivos modificados + commit hashes
- 7 testes vitest counts
- Confirmação que 11.1-03 está unblocked
</output>
