---
phase: 07.1.5-detect-segment-close-up-fix
plan: 01
status: complete
verdict: B_INFEASIBLE
date: 2026-05-11
subsystem: vision-pipeline
tags: [opencv, hsv, hough-circles, color-segmentation, mediapipe, iris-detection]

# Dependency graph
requires:
  - phase: 07.1.5
    provides: detect_diagnostics field (per_eye_diag accumulator) emitted by Modal — empirical baseline from commit 8494a4a
provides:
  - pipeline.masks.color_iris_mask (HSV color pre-segmentation, single source of truth)
  - _detector="hough_color_masked" breadcrumb for downstream observability
  - _color_mask_ratio diagnostic key on detection dict (open-dict per D-13)
  - probe_pigment_deltas.py --dump-detect-table + --dump-json flags (BLOCKER-2 input for P2 SC-2 gate)
  - synthetic_close_up_eye conftest fixture (single source of truth, WARN-6)
  - 9 new tests (6 unit + 3 integration including BLOCKER-3 MediaPipe non-regression guard)
  - BINARY verdict B_INFEASIBLE per D-07: P2 must morph into Approach C (U-Net) research+implementation
affects: [07.1.5-02 P2 (must escalate to C), 07.2 corpus expansion]

# Tech tracking
tech-stack:
  added: [pipeline.masks module, argparse in probe script]
  patterns:
    - HSV inRange + morphology CLOSE/OPEN for iris pre-segmentation
    - Shared CV helper module (pipeline.masks) imported by detect.py + segment.py — single source of truth (WARN-2 fix)
    - Mocked-landmarker regression test pattern for MediaPipe non-regression (BLOCKER-3)
    - Per-test @pytest.mark.skipif decomposition (replaces module-level pytestmark)
    - Builder-callable conftest fixture (synthetic_close_up_eye) consumed across multiple test files (WARN-6)

key-files:
  created:
    - vision-service/pipeline/masks.py
    - vision-service/tests/test_color_iris_mask.py
  modified:
    - vision-service/pipeline/detect.py
    - vision-service/pipeline/segment.py
    - vision-service/tests/test_detect.py
    - vision-service/tests/conftest.py
    - vision-service/scripts/probe_pigment_deltas.py

key-decisions:
  - "B_INFEASIBLE verdict per D-07: monotonic improvement observed (Nailli RIGHT 974 -> 496 -> 279 px) but 50 px gate not reached after 3 cycles"
  - "Cycle 3 final constants: S_MIN=80, V_MIN=50, CLOSE (5,5)x2, OPEN (7,7)x2"
  - "Starting CLOSE kernel widened from (3,3) to (5,5) so morphology test fixture (6x6 specular hole) closes; documented as Wave 2 starting-constant correction (Rule 1 inline)"
  - "Pre-existing test bug fixed: test_no_face_raises_value_error expected 'mediapipe_no_face_detected' but detect.py raises 'iris_not_detected' (Rule 1 inline; bug was hidden by module-level skipif)"
  - "D-05 fixtures relaxed from status='ready' to status='pending' for audit-only because only Nailli has status='ready' in the DB (documented; verdict gate is Nailli-only per WARN-3)"
  - "P2 must morph into Approach C (U-Net CASIA-Iris) research+implementation per CONTEXT D-02; ESCALATE_NEW_PHASE option (BLOCKER-4) also available"

patterns-established:
  - "pipeline.masks.color_iris_mask is the canonical entry point for any future shared CV mask logic"
  - "Probe --dump-json flag emits per-eye per-angle JSON snapshot consumed by downstream gate scripts"
  - "BLOCKER-3 mocked-landmarker pattern: monkeypatch _landmarker=None + get_landmarker -> _FakeLandmarker; spy color_iris_mask via shared masks module reference"

requirements-completed: []  # phase frontmatter has no `requirements:` field

# Metrics
duration: ~30min
completed: 2026-05-11
---

# Phase 07.1.5 Plan 01: Approach B (HSV Color Pre-Segmentation) Summary

**HSV color pre-segmentation implemented and integrated end-to-end (detect+segment via shared `pipeline.masks` module); verdict B_INFEASIBLE — Nailli failed convergence gate after 3 threshold-tuning cycles despite monotonic trajectory.**

## Performance

- **Duration:** ~30 min (Wave 0 + Wave 1 + 3 Wave 2 cycles)
- **Started:** 2026-05-11T (Wave 0 RED scaffolding)
- **Completed:** 2026-05-11T (final Cycle 3 + SUMMARY)
- **Tasks:** 3 (Wave 0 RED, Wave 1 GREEN, Wave 2 empirical)
- **Files modified:** 5 modified + 2 created = 7 total

## Verdict

**B_INFEASIBLE** — Approach B alone does not converge Nailli to the SC-2 gate (segment center spread <=50 px AND radius CV <=15% per eye across 3 angles).

**P2 MUST morph into Approach C (U-Net CASIA-Iris) research-phase + implementation per CONTEXT D-02.** Recommend spawning `gsd-phase-researcher` to verify LightIrisNet / segmentation-models-pytorch.Unet weight availability per RESEARCH (RESOLVED) Open Question #4 BEFORE committing to a specific U-Net checkpoint. Note: P2 Task 0 also presents an `ESCALATE_NEW_PHASE` option (BLOCKER-4) to split C into a fresh phase if the founder prefers a per-phase cost ceiling.

Why B did not pass:
- Monotonic improvement was observed on Nailli RIGHT (max_center_spread 973.9 -> 495.5 -> 279.0 px across Cycles 1-3) — the color mask IS removing some search-space noise.
- However, after 3 cycles RIGHT remains 5.6x the 50 px target and LEFT remains 16x off.
- Hough still locks onto eyebrow/sclera arcs at radii 411-680 px even with mask ratios reduced from ~0.94 (Cycle 1) to ~0.45-0.77 (Cycle 3). The residual problem is **Hough parameter sensitivity at full-frame scales**, not color-mask permissiveness — Approach A (Hough param tuning standalone) is still deferred per D-03, but the empirical evidence here suggests A or C is the binding bottleneck once B is in place.

## Threshold trajectory

| Cycle | S_MIN | V_MIN | CLOSE       | OPEN        | Mask ratio range | Nailli RIGHT max spread | Nailli LEFT max spread | Verdict |
|-------|-------|-------|-------------|-------------|------------------|------------------------:|-----------------------:|---------|
| 1     | 30    | 40    | (5,5) x 2   | (5,5) x 1   | 0.94 - 0.99      | 973.9 px FAIL           | 125.6 px (2 angles only — backlight detect failed) | FAIL |
| 2     | 60    | 50    | (5,5) x 2   | (5,5) x 1   | 0.77 - 0.95      | 495.5 px FAIL           | 891.4 px (3 angles)    | FAIL |
| 3     | 80    | 50    | (5,5) x 2   | (7,7) x 2   | 0.45 - 0.77      | 279.0 px FAIL (CV PASS 0.098) | 795.9 px FAIL    | FAIL |

Verdict gate (per WARN-3 + D-07): BINARY on Nailli only. D-05 columns (below) are informational/audit.

Cycle-3 ending constants (committed in `7bfb5b6`):
- `_COLOR_MASK_S_MIN = 80`
- `_COLOR_MASK_V_MIN = 50`
- `_COLOR_MASK_CLOSE_KERNEL = (5, 5)`, `_COLOR_MASK_CLOSE_ITER = 2`
- `_COLOR_MASK_OPEN_KERNEL = (7, 7)`, `_COLOR_MASK_OPEN_ITER = 2`

Cycle 3 vs Cycle 2 delta on Nailli (D-07 monotonicity check):
- RIGHT: 279.0 < 495.5 -> monotonic improvement (delta -216 px)
- LEFT:  795.9 < 891.4 -> monotonic improvement (delta -95 px)

Both eyes showed monotonic improvement; verdict is B_INFEASIBLE because the gate (50 px) was not reached, not because the trajectory stalled.

## Per-fixture final-cycle convergence tables

### Nailli (`71a7bf1d-747f-4de8-9129-13b69197c6a4`) — primary fixture, gate

```
[probe] === RIGHT segment convergence (Cycle 3) ===
[probe]   backlight  center=( 1418.1, 1187.4) radius= 546.1
[probe]   frontal    center=( 1418.1, 1250.7) radius= 680.9
[probe]   lateral    center=( 1685.0, 1169.3) radius= 676.0
[probe]   max center spread:    279.0 px  (gate: <=50 px)  FAIL
[probe]   radius CV:            0.098      (gate: <=0.15) PASS

[probe] === LEFT segment convergence (Cycle 3) ===
[probe]   backlight  center=( 1567.4, 1191.9) radius= 411.3
[probe]   frontal    center=(  839.1, 1513.1) radius= 661.0
[probe]   lateral    center=( 1368.3, 1101.5) radius= 378.3
[probe]   max center spread:    795.9 px  (gate: <=50 px)  FAIL
[probe]   radius CV:            0.261      (gate: <=0.15) FAIL
```

JSON snapshot for P2 SC-2 gate (BLOCKER-2 input):
- `/tmp/nailli_postfix_detect_diagnostics.json` (Windows-resolved: `C:\Users\rhelt\AppData\Local\Temp\nailli_postfix_detect_diagnostics.json`)
- Contents: `{"left": {"backlight": {"segment_center": [1567.37, 1191.93], "segment_radius": 411.33}, "frontal": {...}, "lateral": {...}}, "right": {...}}` (per-eye per-angle Cycle 3 final).

### D-05 informational fixtures (audit only — do NOT gate verdict per WARN-3)

**`93489977-c9f0-4246-bd8e-c6878f893623`** (status='pending', client `d82b6d9d-9497-4c97-9912-1fa7c76efa84`):
```
[probe] === RIGHT segment convergence ===
[probe]   frontal    center=(  800.0,  814.1) radius= 162.7
[probe]   lateral    center=(  738.3,  925.4) radius= 155.1
[probe]   backlight  center=(  758.5,  823.0) radius= 213.0
[probe]   max center spread:    127.3 px  (gate: <=50 px)  FAIL
[probe]   radius CV:            0.145      (gate: <=0.15) PASS
[probe] === LEFT segment convergence ===
[probe]   frontal    center=(  446.1,  891.3) radius= 144.9
[probe]   lateral    center=(  383.1,  832.9) radius= 198.1
[probe]   backlight  center=(  401.7,  871.4) radius= 299.2
[probe]   max center spread:     85.9 px  (gate: <=50 px)  FAIL
[probe]   radius CV:            0.299      (gate: <=0.15) FAIL
```

**`ff0b1fbe-92ef-44c1-96cd-f3a15ad3daaa`** (status='pending', client `d82b6d9d`):
```
[probe] === RIGHT segment convergence ===
[probe]   frontal    center=(   92.2,  116.2) radius=  44.2
[probe]   lateral    center=(   95.2,  132.8) radius=  55.2
[probe]   backlight  center=(   78.8,  174.8) radius=  76.9
[probe]   max center spread:     60.0 px  (gate: <=50 px)  FAIL (10 px short)
[probe]   radius CV:            0.231      (gate: <=0.15) FAIL
```

D-05 fixtures show better convergence trajectories than Nailli (one only 10 px shy of the gate), suggesting Approach B + slight tuning may handle some real-world fixtures — but Nailli remains the primary gate per D-07.

## D-05 fixture selection rationale (informational)

**Constraint relaxation noted:** D-05 originally specified `status='ready' AND client_id != Nailli's AND has 6 images`. Strict query returns 0 candidates — only Nailli has `status='ready'` in the database (28 readings total: 26 pending, 1 failed, 1 ready). To produce informational audit data per the plan's `<behavior>` section, we relaxed the status filter to allow `pending` readings while keeping the other two constraints (different client, 6 images = 3 angles x 2 eyes). 8 candidates surfaced, all from client `d82b6d9d-9497-4c97-9912-1fa7c76efa84`.

**Selected (first 2 by id ordering, audit-only):**
- `93489977-c9f0-4246-bd8e-c6878f893623` — pending status, full 6-image set, both eyes detected
- `ff0b1fbe-92ef-44c1-96cd-f3a15ad3daaa` — pending status, full 6-image set, RIGHT eye only (LEFT detect failed all angles in Cycle 3)

**Recommendation for founder:** Capture and process to `status='ready'` 2-3 additional client readings to satisfy strict D-05 in future verification cycles (e.g., when P2 Task 0 evaluates whether to escalate to C). The `pending` status here means images exist but full pipeline run hasn't been triggered/persisted; the probe still fetches the raw images via signed URL — semantically equivalent for color-mask tuning.

Query used (relaxed):
```sql
-- All readings (any status) where client != Nailli's and reading has 6 images
SELECT r.id FROM readings r
JOIN reading_images i ON i.reading_id = r.id
WHERE r.client_id != '2eba81e9-fb54-4b26-9e41-e50a8625b245'  -- Nailli's client
GROUP BY r.id
HAVING COUNT(*) = 6;
```

## Visual gate evidence (SC-1 / Invariant 5)

- **Pre-fix baseline (commit `8494a4a`, Modal call `fc-01KRBPC40B4D15XGQJPSNMNX6V`):** `vision-service/scripts/output/71a7bf1d-747f-4de8-9129-13b69197c6a4_{right,left}_enhanced_polar.png` (committed-mtime ~14:50)
- **Post-fix regenerated (Cycle 3 final):** same paths, mtime 2026-05-11 14:55 (newer than baseline; output/ is gitignored — neither is committed)

**Founder qualitative assessment placeholder:** open both PNGs side-by-side and check whether radial striated iris texture fills >=80% of polar width. Given the verdict is B_INFEASIBLE, the post-fix polar likely still shows substantial non-iris content (sclera/eyelid bleeding into the disk because the segment radius is locking onto the wrong contour). This visual evidence is consistent with the numeric verdict.

## Test gate evidence (SC-4 / Invariant 1 / Invariant 6 / WARN-4)

| Metric | Value | Gate |
|---|---|---|
| Pre-Wave-1 pytest-collected count | 270 | (baseline) |
| Post-Wave-2 pytest-collected count | **279** | **>=266 per WARN-4 PASS** |
| Test failures | **0** | **PASS** |
| Test pass count | 275 (4 skipped: 3 detect tests need face_landmarker.task; 1 segment test needs JPEG fixtures) | |
| New tests added | **9** (6 in `test_color_iris_mask.py` + 3 in `test_detect.py` incl. BLOCKER-3 MediaPipe non-regression guard) | |
| BLOCKER-3 guard status | `test_mediapipe_path_does_not_invoke_color_mask` -> **PASS** (verified both with and without face_landmarker.task locally; mocked landmarker pattern survives refactors) | **PASS** |
| Pre-existing test fix | `test_no_face_raises_value_error` corrected from `'mediapipe_no_face_detected'` to `'iris_not_detected'` (matches actual sentinel raised by `find_iris` when both MediaPipe and Hough fail) | (Rule 1 inline) |

## Handoff to P2

**Verdict B_INFEASIBLE -> P2 must morph into Approach C (U-Net CASIA-Iris) research-phase + implementation per CONTEXT D-02.**

Recommended P2 path:
1. P2 Task 0 (gate selection): present 2 options to founder:
   - **Option A — In-phase escalation to C** (default): require typed `ESCALATE_C` token (per existing 07.1.5-02 P2 hardening from commit `0602fda`). Spawn `gsd-phase-researcher` to verify LightIrisNet / `segmentation-models-pytorch` U-Net weight availability per RESEARCH RESOLVED Open Question #4 (CASIA-Iris pretrained weights, model size <=200MB, T4 inference <=200ms).
   - **Option B — `ESCALATE_NEW_PHASE`** (BLOCKER-4): split C into a fresh phase 07.1.6 with its own scope/cost ceiling.
2. If C is built: re-run probe convergence on Nailli + the same 2 informational fixtures used here. Compare against this SUMMARY's Cycle 3 numbers as the new baseline-to-beat.
3. P2's existing SC-2 programmatic gate (BLOCKER-2) consumes `/tmp/nailli_postfix_detect_diagnostics.json` produced by THIS plan — the file exists and is the Cycle-3 snapshot. P2 must overwrite it (with C's results) before re-running the gate, or the gate will pass on B's failing numbers spuriously. **Recommend P2 Task 0 add a step: delete or rotate this file before any C run.**

**JSON snapshot for P2 BLOCKER-2 SC-2 gate:** `/tmp/nailli_postfix_detect_diagnostics.json` exists with Cycle 3 final per-eye per-angle segment circles. Path resolves to `C:\Users\rhelt\AppData\Local\Temp\nailli_postfix_detect_diagnostics.json` on Windows.

## Empirical evidence appendix (D-08 PR evidence)

### Nailli before/after segment center+radius table

**Pre-fix baseline** (commit `8494a4a`, Modal call `fc-01KRBPC40B4D15XGQJPSNMNX6V` per CONTEXT specifics table):

| Olho | Ângulo | Detector | Segment center | Segment radius |
|---|---|---|---|---|
| right | backlight | hough | (744, 952) | 131 |
| right | frontal | hough | (1180, 541) | 112 |
| right | lateral | hough | (755, 1108) | 191 |
| left | backlight | **failed** | — | — |
| left | frontal | hough | (1272, 972) | 97 |
| left | lateral | hough | (176, 1993) | 108 |

Pre-fix gates:
- right max center spread: ~700 px (gate: <=50)
- left max center spread: ~1497 px (gate: <=50)

**Post-fix Cycle 3 (final B trajectory)**:

| Olho | Ângulo | Detector | Segment center | Segment radius |
|---|---|---|---|---|
| right | backlight | hough_color_masked | (1418.1, 1187.4) | 546.1 |
| right | frontal  | hough_color_masked | (1418.1, 1250.7) | 680.9 |
| right | lateral  | hough_color_masked | (1685.0, 1169.3) | 676.0 |
| left  | backlight | hough_color_masked | (1567.4, 1191.9) | 411.3 |
| left  | frontal  | hough_color_masked | (839.1, 1513.1)  | 661.0 |
| left  | lateral  | hough_color_masked | (1368.3, 1101.5) | 378.3 |

Post-fix gates:
- right max center spread: 279.0 px (gate: <=50, **5.6x off**, but improvement from 700)
- left max center spread: 795.9 px (gate: <=50, **16x off**, but improvement from 1497; backlight no longer fails detect — soft-degradation eliminated)

**Notable side effects of Approach B:**
1. **`left/backlight` no longer fails detect.** Pre-fix: detect failed (no Hough circle found). Post-fix: detect succeeds with `hough_color_masked` and produces a segment circle. Color mask reduces the noise floor enough for Hough to find SOMETHING — though that something is not the iris.
2. **Segment radii are 3-6x larger post-fix** (post-fix 411-681 vs pre-fix 97-191). Because the color mask leaves the eyeball/sclera region as "iris-candidate" connected component, Hough's strongest accumulator response is now at the eyeball's outer arc, not the iris's inner arc. This is the single biggest qualitative change and the smoking-gun for B's infeasibility: **B doesn't fix the Hough param sensitivity, it just shifts where Hough gets fooled**.
3. **Detector breadcrumb correctly propagates:** all 6 angles report `_detector="hough_color_masked"` (verified via `_color_mask_ratio` in stdout); modal_app.py per_eye_diag accumulator reads via `det.get("_detector", "unknown")` so this flows through to `processing_metadata.detect_diagnostics` automatically (WARN-5 confirmed).

## Task Commits

Each task was committed atomically (sequential executor on `main`, no worktree):

1. **Task 1: Wave 0 RED scaffolding** — `a67eb1f` (test) — 4 files: `test_color_iris_mask.py` NEW + `test_detect.py` extension (incl. BLOCKER-3) + `conftest.py` `synthetic_close_up_eye` fixture + `probe_pigment_deltas.py` argparse + `--dump-detect-table` + `--dump-json` flags.
2. **Task 2: Wave 1 GREEN implementation** — `173c6ea` (feat) — `pipeline/masks.py` NEW + `detect.py` integration + `segment.py` additive defense.
3. **Task 3 (intermediate): Wave 2 Cycle 2/3 constants tune** — `7bfb5b6` (tune) — `pipeline/masks.py` constants update from starting values to S=80/V=50/OPEN(7,7)x2 after empirical iteration on Nailli.

**Plan metadata commit (this SUMMARY):** to be created after writing this file via plain `git commit` — see `final_commit` step in execute-plan workflow.

## Files Created/Modified

Created:
- `vision-service/pipeline/masks.py` — shared HSV color iris mask helper (single source of truth, WARN-2)
- `vision-service/tests/test_color_iris_mask.py` — 6 unit tests covering shape/dtype/binary invariants + recall/specificity + morphology

Modified:
- `vision-service/pipeline/detect.py` — added `from pipeline.masks import color_iris_mask`; integrated mask + bitwise_and into `_hough_circle_fallback`; updated return dict (`_detector="hough_color_masked"` + `_color_mask_ratio`)
- `vision-service/pipeline/segment.py` — added `from pipeline.masks import color_iris_mask`; integrated additive-defense bitwise_and pre-Hough
- `vision-service/tests/test_detect.py` — decomposed module-level `pytestmark = skipif` into per-test `_REQUIRES_MODEL` decorators; fixed `test_no_face_raises_value_error` sentinel (Rule 1); added 3 new tests including BLOCKER-3 MediaPipe non-regression guard (mocked landmarker)
- `vision-service/tests/conftest.py` — added `synthetic_close_up_eye` session-scoped builder fixture (WARN-6 single source of truth)
- `vision-service/scripts/probe_pigment_deltas.py` — added argparse + `--dump-detect-table` (per-eye convergence table) + `--dump-json` (BLOCKER-2 P2 SC-2 gate input)

## Decisions Made

- **B_INFEASIBLE verdict per D-07/WARN-3:** monotonic improvement observed but Nailli still misses 50 px gate after 3 cycles -> binary verdict is B_INFEASIBLE; P2 escalates to C.
- **CLOSE kernel widened from (3,3) to (5,5) at starting constants:** the morphology specular-hole test fixture (6x6 hole) doesn't close with (3,3) iter=2 (effective filling radius ~4 px). Documented as Rule 1 inline starting-constant correction; consistent with Wave 2 starting-constant discretion window.
- **D-05 fixture status filter relaxed from `ready` to allow `pending`:** strict query returns 0 candidates (only Nailli is `ready` in DB). Audit-only fixtures used to produce informational convergence tables; verdict gate is Nailli-only per WARN-3 so the relaxation does not affect the binary verdict.
- **Pre-existing test fix (`test_no_face_raises_value_error`):** corrected expected sentinel from `mediapipe_no_face_detected` to `iris_not_detected`. The bug was hidden by module-level skipif; surfaced once the skipif was decomposed per Task 1 acceptance criteria. Treated as Rule 1 inline fix because the test would otherwise FAIL once the model file is available locally and break the SC-4 gate.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Pre-existing `test_no_face_raises_value_error` had wrong expected sentinel string**
- **Found during:** Task 1 (Wave 0 — decomposing module-level pytestmark to per-test decorators)
- **Issue:** Test expected `pytest.raises(ValueError, match="mediapipe_no_face_detected")`. Actual `find_iris` raises `ValueError("iris_not_detected")` when both MediaPipe and Hough fail (detect.py line 233). Bug was hidden by module-level skipif because no local dev had the model file in default path.
- **Fix:** Changed `match="mediapipe_no_face_detected"` -> `match="iris_not_detected"` in `tests/test_detect.py`.
- **Files modified:** `vision-service/tests/test_detect.py`
- **Verification:** Test now passes when `MEDIAPIPE_FACE_LANDMARKER_PATH` is set (verified locally).
- **Committed in:** `a67eb1f` (Task 1 commit)

**2. [Rule 1 - Bug] Starting CLOSE kernel `(3,3) iter=2` does not fill 6x6 specular hole**
- **Found during:** Task 2 (Wave 1 — running `test_color_mask_morphology_fills_specular_hole` after creating pipeline/masks.py with starting constants per plan)
- **Issue:** Plan-specified starting constants `_COLOR_MASK_CLOSE_KERNEL=(3,3), _COLOR_MASK_CLOSE_ITER=2` create an effective closing radius of ~4 px, which does NOT close the 6x6 specular hole asserted by the test fixture. Plan's RESEARCH Pitfall 4 envelope claimed `<=6 px diameter` closure but the math is borderline.
- **Fix:** Widened starting `_COLOR_MASK_CLOSE_KERNEL` from `(3,3)` to `(5,5)` (still iter=2). Effective closing radius ~6-7 px now reliably closes the test fixture and is within the Wave 2 starting-constant discretion window per `<behavior>`.
- **Files modified:** `vision-service/pipeline/masks.py`
- **Verification:** All 6 unit tests in `test_color_iris_mask.py` pass.
- **Committed in:** `173c6ea` (Task 2 commit)

**3. [Rule 3 - Blocking] D-05 strict status filter returns 0 fixtures**
- **Found during:** Task 3 (Wave 2 — querying Supabase for fixtures matching D-05 constraint)
- **Issue:** Query `SELECT * FROM readings WHERE status='ready' AND client_id != Nailli's AND has 6 images` returns 0 rows. Only Nailli has status='ready'. Plan's stop-loss criterion still applies (Nailli is primary, D-05 is informational), but the SUMMARY structure asks for 2+ D-05 convergence tables.
- **Fix:** Relaxed status filter from `'ready'` to allow `'pending'` for audit-only fixtures (selected 2 from client `d82b6d9d`). Documented relaxation explicitly in SUMMARY's "D-05 fixture selection rationale" section. Verdict gate is Nailli-only per WARN-3 so this does not affect the binary outcome.
- **Files modified:** none (operational decision, documented in SUMMARY).
- **Verification:** 2 informational convergence tables produced; both fail gate but show better trajectories than Nailli (one only 10 px shy).
- **Committed in:** N/A (operational; recorded in this SUMMARY).

---

**Total deviations:** 3 auto-fixed (2 Rule 1 bugs, 1 Rule 3 blocking).
**Impact on plan:** All deviations were necessary to make the planned tests/probe runnable. No scope creep — verdict is determined per D-07 stop-loss criterion exactly as planned.

## Issues Encountered

- **Approach B + current Hough params is geometrically insufficient for Nailli.** Color mask narrows the search space but Hough still locks onto the eyeball's outer arc (radii 411-681 post-fix vs 97-191 pre-fix). Documented in "Empirical evidence appendix > Notable side effects of Approach B" as the smoking-gun observation. Resolved by declaring B_INFEASIBLE and recommending C escalation per D-02.

## User Setup Required

None - no external service configuration required by P1. Probe consumes existing Supabase signed URLs via `apps/web/.env.local` credentials (already configured).

## Next Phase Readiness

P2 (07.1.5-02) can begin immediately. P2 Task 0 must:
1. Read this SUMMARY's Verdict + Handoff sections.
2. Present founder with the 2-option gate (in-phase ESCALATE_C vs ESCALATE_NEW_PHASE per BLOCKER-4).
3. **Delete or rotate `/tmp/nailli_postfix_detect_diagnostics.json` before any C run** — otherwise BLOCKER-2 SC-2 gate will read B's stale (failing) numbers.
4. If C path is selected, spawn `gsd-phase-researcher` for U-Net checkpoint feasibility per RESEARCH RESOLVED Open Question #4.

Vision pipeline structural integrity preserved:
- 275 tests pass, 4 skipped (model + JPEG fixtures unavailable locally — environmental, not regressions)
- 0 failures across full vision-service suite
- BLOCKER-3 MediaPipe non-regression guard PASS (verified both with and without local model file)
- modal_app.py NOT modified (WARN-5: per_eye_diag automatically propagates the new `_detector` value via `.get()`)
- find_iris MediaPipe success path NOT modified (lines 199-225 in detect.py unchanged; structurally + dynamically protected)

## Self-Check: PASSED

Verified post-write:
- Created files exist: `vision-service/pipeline/masks.py`, `vision-service/tests/test_color_iris_mask.py`, `.planning/phases/07.1.5-detect-segment-close-up-fix/07.1.5-01-SUMMARY.md` (this file).
- Modified files exist: `detect.py`, `segment.py`, `test_detect.py`, `conftest.py`, `probe_pigment_deltas.py`.
- Commits exist: `a67eb1f` (Wave 0 RED), `173c6ea` (Wave 1 GREEN), `7bfb5b6` (Cycle 2/3 tune).
- BLOCKER-2 JSON snapshot exists: `C:\Users\rhelt\AppData\Local\Temp\nailli_postfix_detect_diagnostics.json` (Cycle 3 final).

---
*Phase: 07.1.5-detect-segment-close-up-fix*
*Completed: 2026-05-11*
