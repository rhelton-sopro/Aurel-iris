---
phase: 2
plan: 3
type: execute
wave: 3
depends_on: [1]
files_modified:
  - apps/web/app/(dashboard)/layout.tsx
  - apps/web/app/(dashboard)/dashboard/page.tsx
  - apps/web/app/(dashboard)/leituras/page.tsx
  - apps/web/components/dashboard/app-sidebar.tsx
  - apps/web/components/dashboard/dashboard-header.tsx
  - apps/web/components/dashboard/summary-cards.tsx
autonomous: true
requirements:
  - CLIENT-03
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "Usuário autenticado acessa /dashboard e vê sidebar + header + 3 cards de resumo"
    - "Header exibe nome do terapeuta (profiles.full_name) + avatar com inicial + badge de trial"
    - "Sidebar tem links para Dashboard, Clientes e Leituras com ícones corretos"
    - "Mobile: sidebar colapsa em Sheet drawer via hamburger"
    - "Copy LGPD obrigatória aparece no layout do dashboard"
    - "/leituras mostra página placeholder 'Em breve'"
  artifacts:
    - path: "apps/web/app/(dashboard)/layout.tsx"
      provides: "DashboardLayout com SidebarProvider + AppSidebar + DashboardHeader + footer LGPD"
      contains: "Ferramenta de apoio à anamnese terapêutica integrativa"
    - path: "apps/web/components/dashboard/app-sidebar.tsx"
      provides: "Sidebar com nav links: Dashboard / Clientes / Leituras + ícones lucide"
      exports: ["AppSidebar"]
    - path: "apps/web/components/dashboard/dashboard-header.tsx"
      provides: "Header com SidebarTrigger + badge trial + avatar + dropdown Sair"
      exports: ["DashboardHeader"]
    - path: "apps/web/components/dashboard/summary-cards.tsx"
      provides: "3 cards: total clientes, leituras esta semana (0), status assinatura"
      exports: ["SummaryCards"]
    - path: "apps/web/app/(dashboard)/dashboard/page.tsx"
      provides: "Página /dashboard com SummaryCards"
      contains: "Dashboard"
    - path: "apps/web/app/(dashboard)/leituras/page.tsx"
      provides: "Placeholder 'Em breve'"
      contains: "Em breve"
  key_links:
    - from: "apps/web/app/(dashboard)/layout.tsx"
      to: "apps/web/lib/supabase/server.ts"
      via: "createClient() para buscar profiles.full_name e clients count"
      pattern: "createClient"
    - from: "apps/web/components/dashboard/dashboard-header.tsx"
      to: "apps/web/lib/supabase/client.ts"
      via: "supabase.auth.signOut() no logout"
      pattern: "signOut"
    - from: "apps/web/app/(dashboard)/layout.tsx"
      to: "apps/web/components/dashboard/app-sidebar.tsx"
      via: "import AppSidebar"
      pattern: "AppSidebar"
---

<objective>
Construir o layout do dashboard com sidebar lateral, header com perfil do terapeuta, e as páginas principais: /dashboard (cards de resumo) e /leituras (placeholder).

Purpose: O layout do dashboard é a casca visual que envolverá todas as páginas de trabalho (Clientes, Leituras, etc.). Depende apenas do Wave 1 (infra Supabase) — pode rodar em paralelo com o Wave 2 (páginas de auth).

Output: route group (dashboard) com layout completo, 6 arquivos de componentes/páginas, shadcn components instalados (sidebar, avatar, badge, dropdown-menu, separator, card).
</objective>

<execution_context>
@D:/Projetos/Iridologista/apps/web/../../.claude/get-shit-done/workflows/execute-plan.md
@D:/Projetos/Iridologista/apps/web/../../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Projetos/Iridologista/.planning/ROADMAP.md
@D:/Projetos/Iridologista/.planning/phases/02-auth-dashboard-basico/02-CONTEXT.md
@D:/Projetos/Iridologista/.planning/phases/02-auth-dashboard-basico/02-RESEARCH.md
@D:/Projetos/Iridologista/.planning/phases/02-auth-dashboard-basico/02-UI-SPEC.md

<interfaces>
<!-- Contratos do Plano 01 que este plano consome -->

```typescript
// apps/web/lib/supabase/server.ts
export const createClient: () => Promise<SupabaseClient<Database>>
// Uso: const supabase = await createClient()
// Depois: const { data: { user } } = await supabase.auth.getUser()
// Depois: const { data: profile } = await supabase.from('profiles').select('*').eq('id', user.id).single()
// Depois: const { count } = await supabase.from('clients').select('*', { count: 'exact', head: true })
```

```typescript
// apps/web/types/database.ts (profiles relevante)
// profiles.Row: { id, full_name, subscription_status, trial_ends_at, ... }
// clients.Row: { id, therapist_id, full_name, ... }
// RLS: therapist_id = auth.uid() — cada terapeuta só vê seus próprios dados
```

<!-- Decisões de UI da UI-SPEC para esta wave -->
// D-09: Sidebar fixa w-64 desktop, Sheet drawer mobile via shadcn Sidebar
// D-10: Header h-14, badge trial "Trial: X dias" (outline > 3 dias, destructive <= 3 dias)
// D-11: /dashboard: 3 cards — Clientes (count), Leituras esta semana (0), Assinatura (trial)
// Icons: LayoutDashboard (Dashboard), Users (Clientes), Eye (Leituras)
// Logout: supabase.auth.signOut() client-side + router.push('/login')

// date-fns para calcular "X dias restantes" de trial_ends_at:
// import { differenceInDays } from 'date-fns'
// const daysLeft = differenceInDays(new Date(trial_ends_at), new Date())
</interfaces>
</context>

<tasks>

<task type="auto" id="02-03-T1">
  <name>Task 1: Instalar shadcn components de dashboard e criar componentes (sidebar, header, cards)</name>
  <read_first>
    - apps/web/app/globals.css (confirmar que sidebar CSS variables existem — comentário na RESEARCH.md diz que já estão presentes)
    - apps/web/components.json (confirmar aliases @/components)
    - apps/web/package.json (confirmar se date-fns já está instalado)
    - .planning/phases/02-auth-dashboard-basico/02-UI-SPEC.md (Component Contracts: AppSidebar, DashboardHeader, SummaryCards)
    - .planning/phases/02-auth-dashboard-basico/02-RESEARCH.md (seção Standard Stack — shadcn, lista de componentes)
  </read_first>
  <files>
    apps/web/components/dashboard/app-sidebar.tsx,
    apps/web/components/dashboard/dashboard-header.tsx,
    apps/web/components/dashboard/summary-cards.tsx
  </files>
  <action>
Instalar shadcn components de dashboard:
```bash
cd apps/web && pnpm dlx shadcn add sidebar avatar badge dropdown-menu separator
```

Se solicitado confirmação para sobrescrever arquivos, aceitar (Y).

Instalar date-fns (para calcular dias de trial):
```bash
cd apps/web && pnpm add date-fns@4.1.0
```

Criar diretório `apps/web/components/dashboard/`.

**apps/web/components/dashboard/app-sidebar.tsx** — sidebar fixa com nav links (D-09):

```tsx
'use client'

import Link from 'next/link'
import { usePathname } from 'next/navigation'
import { LayoutDashboard, Users, Eye } from 'lucide-react'
import {
  Sidebar,
  SidebarContent,
  SidebarHeader,
  SidebarMenu,
  SidebarMenuButton,
  SidebarMenuItem,
} from '@/components/ui/sidebar'

const navItems = [
  { href: '/dashboard', label: 'Dashboard', icon: LayoutDashboard },
  { href: '/clientes', label: 'Clientes', icon: Users },
  { href: '/leituras', label: 'Leituras', icon: Eye },
]

export function AppSidebar() {
  const pathname = usePathname()

  return (
    <Sidebar>
      <SidebarHeader className="px-4 py-3 border-b">
        <span className="font-semibold text-lg">Aurel Iris</span>
      </SidebarHeader>
      <SidebarContent>
        <SidebarMenu>
          {navItems.map((item) => {
            const isActive = pathname === item.href || pathname.startsWith(item.href + '/')
            const Icon = item.icon
            return (
              <SidebarMenuItem key={item.href}>
                <SidebarMenuButton asChild isActive={isActive}>
                  <Link href={item.href}>
                    <Icon className="h-4 w-4" />
                    <span>{item.label}</span>
                  </Link>
                </SidebarMenuButton>
              </SidebarMenuItem>
            )
          })}
        </SidebarMenu>
      </SidebarContent>
    </Sidebar>
  )
}
```

**apps/web/components/dashboard/dashboard-header.tsx** — header com trial badge + avatar + dropdown (D-10):

```tsx
'use client'

import { useRouter } from 'next/navigation'
import { differenceInDays } from 'date-fns'
import { LogOut } from 'lucide-react'
import { createClient } from '@/lib/supabase/client'
import { SidebarTrigger } from '@/components/ui/sidebar'
import { Badge } from '@/components/ui/badge'
import { Avatar, AvatarFallback } from '@/components/ui/avatar'
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuTrigger,
} from '@/components/ui/dropdown-menu'

interface DashboardHeaderProps {
  fullName: string
  trialEndsAt: string | null
  subscriptionStatus: string | null
}

export function DashboardHeader({ fullName, trialEndsAt, subscriptionStatus }: DashboardHeaderProps) {
  const router = useRouter()
  const initial = fullName ? fullName.charAt(0).toUpperCase() : 'T'

  const daysLeft = trialEndsAt
    ? differenceInDays(new Date(trialEndsAt), new Date())
    : null

  const isTrial = subscriptionStatus === 'trial'
  const isTrialUrgent = daysLeft !== null && daysLeft <= 3

  async function handleLogout() {
    const supabase = createClient()
    await supabase.auth.signOut()
    router.push('/login')
  }

  return (
    <header className="h-14 border-b flex items-center justify-between px-4">
      <div className="flex items-center gap-2">
        <SidebarTrigger />
      </div>
      <div className="flex items-center gap-3">
        {isTrial && daysLeft !== null && (
          <Badge variant={isTrialUrgent ? 'destructive' : 'outline'}>
            Trial: {daysLeft} dias
          </Badge>
        )}
        <DropdownMenu>
          <DropdownMenuTrigger asChild>
            <Avatar
              className="h-8 w-8 cursor-pointer"
              aria-label={`${fullName} — menu da conta`}
            >
              <AvatarFallback className="bg-primary text-primary-foreground text-sm font-medium">
                {initial}
              </AvatarFallback>
            </Avatar>
          </DropdownMenuTrigger>
          <DropdownMenuContent align="end">
            <DropdownMenuItem onClick={handleLogout} className="cursor-pointer">
              <LogOut className="mr-2 h-4 w-4" />
              Sair
            </DropdownMenuItem>
          </DropdownMenuContent>
        </DropdownMenu>
      </div>
    </header>
  )
}
```

**apps/web/components/dashboard/summary-cards.tsx** — 3 cards de resumo (D-11):

```tsx
import { Users, FileText, CreditCard } from 'lucide-react'
import { differenceInDays } from 'date-fns'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'

interface SummaryCardsProps {
  clientsCount: number
  trialEndsAt: string | null
  subscriptionStatus: string | null
}

export function SummaryCards({ clientsCount, trialEndsAt, subscriptionStatus }: SummaryCardsProps) {
  const daysLeft = trialEndsAt
    ? differenceInDays(new Date(trialEndsAt), new Date())
    : null

  const subscriptionLabel =
    subscriptionStatus === 'trial' && daysLeft !== null
      ? daysLeft > 0
        ? `Trial ativo — ${daysLeft} dias restantes`
        : 'Trial encerrado — assine para continuar'
      : subscriptionStatus ?? 'Sem assinatura'

  return (
    <div className="grid grid-cols-1 sm:grid-cols-3 gap-6">
      <Card>
        <CardHeader className="flex flex-row items-center justify-between pb-2">
          <CardTitle className="text-sm font-medium">Clientes</CardTitle>
          <Users className="h-4 w-4 text-muted-foreground" />
        </CardHeader>
        <CardContent>
          <div className="text-2xl font-semibold">{clientsCount}</div>
        </CardContent>
      </Card>

      <Card>
        <CardHeader className="flex flex-row items-center justify-between pb-2">
          <CardTitle className="text-sm font-medium">Leituras esta semana</CardTitle>
          <FileText className="h-4 w-4 text-muted-foreground" />
        </CardHeader>
        <CardContent>
          <div className="text-2xl font-semibold text-muted-foreground">0</div>
        </CardContent>
      </Card>

      <Card>
        <CardHeader className="flex flex-row items-center justify-between pb-2">
          <CardTitle className="text-sm font-medium">Assinatura</CardTitle>
          <CreditCard className="h-4 w-4 text-muted-foreground" />
        </CardHeader>
        <CardContent>
          <div className="text-sm font-medium">{subscriptionLabel}</div>
        </CardContent>
      </Card>
    </div>
  )
}
```
  </action>
  <verify>
    <automated>cd D:/Projetos/Iridologista/apps/web && pnpm exec tsc --noEmit --skipLibCheck 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/components/dashboard/app-sidebar.tsx` exporta `AppSidebar` e importa de `@/components/ui/sidebar`
    - `apps/web/components/dashboard/dashboard-header.tsx` exporta `DashboardHeader` e usa `supabase.auth.signOut()`
    - `apps/web/components/dashboard/summary-cards.tsx` exporta `SummaryCards` com 3 cards
    - `apps/web/components/ui/sidebar.tsx` existe (instalado pelo shadcn add)
    - `apps/web/components/ui/avatar.tsx` existe
    - `apps/web/components/ui/badge.tsx` existe
    - `grep -r "diagnóstico\|tratamento\|cura" apps/web/components/dashboard/` retorna 0
    - TypeScript typecheck limpo
  </acceptance_criteria>
  <done>Componentes de dashboard (sidebar, header, cards) criados e shadcn de dashboard instalados.</done>
</task>

<task type="auto" id="02-03-T2">
  <name>Task 2: Criar (dashboard)/layout.tsx, /dashboard/page.tsx e /leituras/page.tsx</name>
  <read_first>
    - apps/web/components/dashboard/app-sidebar.tsx (AppSidebar recém-criado — assinatura)
    - apps/web/components/dashboard/dashboard-header.tsx (DashboardHeader — props: fullName, trialEndsAt, subscriptionStatus)
    - apps/web/components/dashboard/summary-cards.tsx (SummaryCards — props: clientsCount, trialEndsAt, subscriptionStatus)
    - apps/web/lib/supabase/server.ts (createClient — async server-side)
    - apps/web/types/database.ts (profiles.Row e clients.Row)
    - .planning/phases/02-auth-dashboard-basico/02-UI-SPEC.md (Layout Architecture, Leituras Placeholder, Copywriting)
  </read_first>
  <files>
    apps/web/app/(dashboard)/layout.tsx,
    apps/web/app/(dashboard)/dashboard/page.tsx,
    apps/web/app/(dashboard)/leituras/page.tsx
  </files>
  <action>
Criar diretório `apps/web/app/(dashboard)/dashboard/` e `apps/web/app/(dashboard)/leituras/`.

**apps/web/app/(dashboard)/layout.tsx** — Server Component, busca perfil e passa para componentes:

```tsx
import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'
import { AppSidebar } from '@/components/dashboard/app-sidebar'
import { DashboardHeader } from '@/components/dashboard/dashboard-header'
import { SidebarProvider, SidebarInset } from '@/components/ui/sidebar'

export default async function DashboardLayout({
  children,
}: {
  children: React.ReactNode
}) {
  const supabase = await createClient()

  // getUser() valida JWT server-side (T-02-01 — nunca usar getSession no servidor)
  const { data: { user }, error: authError } = await supabase.auth.getUser()

  if (!user || authError) {
    redirect('/login')
  }

  // Buscar perfil do terapeuta (RLS garante que só vê o próprio)
  const { data: profile } = await supabase
    .from('profiles')
    .select('full_name, subscription_status, trial_ends_at')
    .eq('id', user.id)
    .single()

  const fullName = profile?.full_name ?? 'Terapeuta'
  const trialEndsAt = profile?.trial_ends_at ?? null
  const subscriptionStatus = profile?.subscription_status ?? null

  return (
    <SidebarProvider>
      <AppSidebar />
      <SidebarInset>
        <DashboardHeader
          fullName={fullName}
          trialEndsAt={trialEndsAt}
          subscriptionStatus={subscriptionStatus}
        />
        <main className="flex-1 p-6">
          {children}
        </main>
        <footer className="px-6 pb-4">
          <p className="text-xs text-muted-foreground">
            Ferramenta de apoio à anamnese terapêutica integrativa, não substitui avaliação médica.
          </p>
        </footer>
      </SidebarInset>
    </SidebarProvider>
  )
}
```

**apps/web/app/(dashboard)/dashboard/page.tsx** — Server Component, conta clientes:

```tsx
import { createClient } from '@/lib/supabase/server'
import { SummaryCards } from '@/components/dashboard/summary-cards'

export default async function DashboardPage() {
  const supabase = await createClient()

  const { data: { user } } = await supabase.auth.getUser()

  // Contar clientes do terapeuta (RLS filtra por therapist_id = auth.uid())
  const { count: clientsCount } = await supabase
    .from('clients')
    .select('*', { count: 'exact', head: true })

  // Buscar trial info do perfil
  const { data: profile } = await supabase
    .from('profiles')
    .select('subscription_status, trial_ends_at')
    .eq('id', user?.id ?? '')
    .single()

  return (
    <div className="space-y-6">
      <h1 className="text-2xl font-semibold">Dashboard</h1>
      <SummaryCards
        clientsCount={clientsCount ?? 0}
        trialEndsAt={profile?.trial_ends_at ?? null}
        subscriptionStatus={profile?.subscription_status ?? null}
      />
    </div>
  )
}
```

**apps/web/app/(dashboard)/leituras/page.tsx** — placeholder (UI-SPEC: Leituras Placeholder):

```tsx
import { Eye } from 'lucide-react'

export default function LeiturasPage() {
  return (
    <div className="flex flex-col items-center justify-center min-h-[400px] gap-4 text-center">
      <Eye className="h-16 w-16 text-muted-foreground" />
      <h1 className="text-xl font-semibold">Em breve</h1>
      <p className="text-sm text-muted-foreground max-w-sm">
        A captura e análise de íris estará disponível em breve.
      </p>
    </div>
  )
}
```
  </action>
  <verify>
    <automated>cd D:/Projetos/Iridologista/apps/web && pnpm exec tsc --noEmit --skipLibCheck 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/(dashboard)/layout.tsx` existe como Server Component (sem 'use client') e chama `supabase.auth.getUser()` (não getSession)
    - layout.tsx contém `redirect('/login')` se não houver user (defense in depth per T-02-06)
    - layout.tsx contém `Ferramenta de apoio à anamnese terapêutica integrativa, não substitui avaliação médica.` no footer
    - `apps/web/app/(dashboard)/dashboard/page.tsx` importa e renderiza `SummaryCards`
    - `apps/web/app/(dashboard)/leituras/page.tsx` contém "Em breve" e "captura e análise de íris estará disponível em breve"
    - `grep -r "diagnóstico\|tratamento\|cura" apps/web/app/(dashboard)/` retorna 0
    - `grep "getSession" apps/web/app/(dashboard)/layout.tsx` retorna 0 (T-02-01)
    - Build TypeScript limpo
  </acceptance_criteria>
  <done>Layout do dashboard completo com sidebar, header, cards de resumo, e páginas /dashboard e /leituras.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → Supabase DB | Server Component busca profiles via createClient(); RLS impõe isolamento por therapist_id |
| browser → supabase.auth.signOut() | Client-side logout; sem dados sensíveis transmitidos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | app/(dashboard)/layout.tsx | mitigate | `supabase.auth.getUser()` valida JWT server-side; redirect('/login') se não autenticado |
| T-02-06 | IDOR | app/(dashboard)/layout.tsx, dashboard/page.tsx | mitigate | RLS filtra clients por `therapist_id = auth.uid()`; layout também verifica getUser() como defesa em profundidade |
| T-02-07 | Information Disclosure | Todos componentes desta wave | mitigate | Vocabulário proibido ausente; copy LGPD obrigatória no layout footer |
</threat_model>

<verification>
Após completar todas as tasks deste plano:

1. `pnpm --filter @aurel-iris/web build` deve compilar sem erros TypeScript
2. Navegando para `http://localhost:3000/dashboard` sem sessão → redirect para `/login`
3. Logado: `/dashboard` mostra sidebar com 3 links + header com badge trial + 3 cards
4. `/leituras` mostra ícone Eye + "Em breve"
5. `grep "getSession" apps/web/app/(dashboard)/layout.tsx` retorna 0
6. `grep -r "diagnóstico\|tratamento\|cura" apps/web/app/(dashboard)/` retorna 0
</verification>

<success_criteria>
- Layout do dashboard com sidebar fixa (desktop) e Sheet drawer (mobile via shadcn Sidebar)
- Header com nome do terapeuta, badge trial e dropdown de logout
- /dashboard com 3 cards de resumo (clientes, leituras, assinatura)
- /leituras com placeholder "Em breve"
- Copy LGPD no footer do dashboard layout
- Shadcn components instalados: sidebar, avatar, badge, dropdown-menu, separator
- date-fns instalado para cálculo de dias de trial
- Build TypeScript limpo
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-auth-dashboard-basico/02-03-SUMMARY.md` seguindo o template em `$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
