---
phase: 2
plan: 2
type: execute
wave: 2
depends_on: [1]
files_modified:
  - apps/web/app/(auth)/layout.tsx
  - apps/web/app/(auth)/signup/page.tsx
  - apps/web/app/(auth)/login/page.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02

must_haves:
  truths:
    - "Terapeuta acessa /signup, preenche e-mail + nome, clica CTA e vê confirmation state (não redireciona)"
    - "Terapeuta acessa /login, preenche e-mail, clica CTA e vê confirmation state"
    - "signInWithOtp em /signup usa shouldCreateUser: true; em /login usa shouldCreateUser: false"
    - "Copy LGPD obrigatória aparece no rodapé do layout de auth"
    - "Vocabulário proibido (diagnóstico, tratamento, cura) ausente em todas as páginas de auth"
    - "Usuário já logado visitando /login ou /signup é redirecionado para /dashboard pelo middleware"
  artifacts:
    - path: "apps/web/app/(auth)/layout.tsx"
      provides: "Layout minimal de auth — fundo bg-background, card centralizado max-w-sm, footer LGPD"
      contains: "Ferramenta de apoio à anamnese terapêutica integrativa"
    - path: "apps/web/app/(auth)/signup/page.tsx"
      provides: "Página de cadastro — email + full_name + signInWithOtp shouldCreateUser: true"
      contains: "shouldCreateUser: true"
    - path: "apps/web/app/(auth)/login/page.tsx"
      provides: "Página de login — email + signInWithOtp shouldCreateUser: false"
      contains: "shouldCreateUser: false"
  key_links:
    - from: "apps/web/app/(auth)/signup/page.tsx"
      to: "apps/web/lib/supabase/client.ts"
      via: "createClient() para signInWithOtp no browser"
      pattern: "signInWithOtp"
    - from: "apps/web/app/(auth)/signup/page.tsx"
      to: "/api/auth/callback"
      via: "emailRedirectTo: process.env.NEXT_PUBLIC_SITE_URL + /api/auth/callback"
      pattern: "api/auth/callback"
---

<objective>
Criar as páginas de autenticação (signup e login) com magic link flow completo, layout de auth com copy LGPD, e todos os shadcn/ui components necessários para esta wave instalados.

Purpose: As páginas de auth são o ponto de entrada do produto para novos terapeutas. Dependem do Supabase client (Wave 1) e devem implementar os padrões PKCE, LGPD e UX definidos em CONTEXT.md D-01 a D-08 e UI-SPEC.

Output: route group (auth) com layout + 2 páginas funcionais; shadcn components instalados (input, label, form, card, toast).
</objective>

<execution_context>
@D:/Projetos/Iridologista/apps/web/../../.claude/get-shit-done/workflows/execute-plan.md
@D:/Projetos/Iridologista/apps/web/../../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Projetos/Iridologista/.planning/ROADMAP.md
@D:/Projetos/Iridologista/.planning/phases/02-auth-dashboard-basico/02-CONTEXT.md
@D:/Projetos/Iridologista/.planning/phases/02-auth-dashboard-basico/02-RESEARCH.md
@D:/Projetos/Iridologista/.planning/phases/02-auth-dashboard-basico/02-UI-SPEC.md

<interfaces>
<!-- Contratos que este plano consome, criados no Plano 01 -->

```typescript
// apps/web/lib/supabase/client.ts
export const createClient = () => createBrowserClient<Database>(URL, ANON_KEY)
// Uso em Client Components: const supabase = createClient()
// Depois: await supabase.auth.signInWithOtp({ email, options: { ... } })
```

<!-- Decisões de UX da UI-SPEC que guiam este plano -->
// Auth pages: formulários com react-hook-form + Zod v4 + shadcn Form
// Após submit bem-sucedido: confirmation state (ícone MailCheck, heading, body) — NÃO redirect
// Callback auth falhou: toast "Link inválido ou expirado. Solicite um novo link de acesso."
// Copy LGPD obrigatória no footer do (auth)/layout.tsx

// Copywriting contract (UI-SPEC):
// /signup — título: "Criar conta", subtítulo: "Aurel Iris — apoio à anamnese terapêutica"
// /login — título: "Entrar", subtítulo: "Acesse sua conta Aurel Iris"
// CTA ambos: "Enviar link de acesso"
// Confirmation heading: "Verifique seu e-mail"
// Link de /signup → /login: "Já tem conta? Entrar"
// Link de /login → /signup: "Primeira vez? Criar conta"

// Zod v4 — API de erros mudou (pitfall da RESEARCH.md):
// NÃO usar: z.string({ required_error: "..." })
// USAR: z.string().min(1, 'Campo obrigatório')
</interfaces>
</context>

<tasks>

<task type="auto" id="02-02-T1">
  <name>Task 1: Instalar shadcn components de auth e criar (auth)/layout.tsx</name>
  <read_first>
    - apps/web/components.json (confirmar style base-nova, aliases @/components)
    - apps/web/app/globals.css (confirmar que CSS variables existem — não sobrescrever)
    - apps/web/app/layout.tsx (root layout — não modificar; apenas referência de padrão)
    - .planning/phases/02-auth-dashboard-basico/02-UI-SPEC.md (seção Auth Layout e Copywriting Contract)
  </read_first>
  <files>
    apps/web/app/(auth)/layout.tsx
  </files>
  <action>
Instalar shadcn components necessários para as páginas de auth:
```bash
cd apps/web && pnpm dlx shadcn add input label form card toast
```

Se solicitado confirmação para sobrescrever arquivos, aceitar (Y).

Criar diretório `apps/web/app/(auth)/`.

**apps/web/app/(auth)/layout.tsx** — layout minimal de auth (sem sidebar):

```tsx
export default function AuthLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <div className="min-h-screen bg-background flex flex-col items-center justify-center px-4 py-12">
      <div className="w-full max-w-sm">
        {children}
      </div>
      <footer className="mt-8 pb-4">
        <p className="text-xs text-muted-foreground text-center">
          Ferramenta de apoio à anamnese terapêutica integrativa, não substitui avaliação médica.
        </p>
      </footer>
    </div>
  )
}
```

Nota: Este é um Server Component (sem 'use client'). O layout não precisa de lógica de auth — o middleware.ts já redireciona usuários logados.
  </action>
  <verify>
    <automated>cd D:/Projetos/Iridologista/apps/web && pnpm exec tsc --noEmit --skipLibCheck 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/(auth)/layout.tsx` existe e contém `Ferramenta de apoio à anamnese terapêutica integrativa, não substitui avaliação médica.` (copy LGPD obrigatória per LGPD-05/06)
    - `apps/web/components/ui/input.tsx` existe (instalado pelo shadcn add)
    - `apps/web/components/ui/form.tsx` existe
    - `apps/web/components/ui/card.tsx` existe
    - `grep -r "diagnóstico\|tratamento\|cura" apps/web/app/(auth)/` retorna 0 resultados (vocabulário LGPD proibido ausente)
    - TypeScript typecheck limpo
  </acceptance_criteria>
  <done>Layout de auth com footer LGPD criado; componentes shadcn de auth instalados.</done>
</task>

<task type="auto" id="02-02-T2">
  <name>Task 2: Criar signup/page.tsx e login/page.tsx com magic link flow</name>
  <read_first>
    - apps/web/app/(auth)/layout.tsx (layout pai recém-criado)
    - apps/web/lib/supabase/client.ts (assinatura de createClient — browser)
    - apps/web/components/ui/form.tsx (API shadcn Form com react-hook-form)
    - .planning/phases/02-auth-dashboard-basico/02-RESEARCH.md (Pattern 5 — signInWithOtp com shouldCreateUser; Pitfall 3 e 6)
    - .planning/phases/02-auth-dashboard-basico/02-UI-SPEC.md (Form States, Interaction Contracts, Copywriting Contract)
  </read_first>
  <files>
    apps/web/app/(auth)/signup/page.tsx,
    apps/web/app/(auth)/login/page.tsx
  </files>
  <action>
Instalar react-hook-form + zod + @hookform/resolvers (necessários para formulários):
```bash
cd apps/web && pnpm add react-hook-form@7.74.0 zod@4.4.1 @hookform/resolvers@5.2.2
```

Criar `apps/web/app/(auth)/signup/` e `apps/web/app/(auth)/login/`.

**apps/web/app/(auth)/signup/page.tsx** — 'use client' (formulário interativo):

```tsx
'use client'

import { useState } from 'react'
import { useForm } from 'react-hook-form'
import { zodResolver } from '@hookform/resolvers/zod'
import { z } from 'zod'
import Link from 'next/link'
import { MailCheck, Loader2 } from 'lucide-react'
import { createClient } from '@/lib/supabase/client'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'
import {
  Form,
  FormControl,
  FormField,
  FormItem,
  FormLabel,
  FormMessage,
} from '@/components/ui/form'

const signupSchema = z.object({
  email: z.string().min(1, 'E-mail é obrigatório').email('E-mail inválido'),
  full_name: z.string().min(1, 'Nome é obrigatório'),
})

type SignupFormValues = z.infer<typeof signupSchema>

export default function SignupPage() {
  const [sent, setSent] = useState(false)
  const [sentEmail, setSentEmail] = useState('')
  const [formError, setFormError] = useState<string | null>(null)

  const form = useForm<SignupFormValues>({
    resolver: zodResolver(signupSchema),
    defaultValues: { email: '', full_name: '' },
  })

  const siteUrl = process.env.NEXT_PUBLIC_SITE_URL || 'http://localhost:3000'

  async function onSubmit(values: SignupFormValues) {
    setFormError(null)
    const supabase = createClient()
    const { error } = await supabase.auth.signInWithOtp({
      email: values.email,
      options: {
        emailRedirectTo: `${siteUrl}/api/auth/callback`,
        data: {
          full_name: values.full_name, // lido pelo trigger: raw_user_meta_data->>'full_name'
        },
        shouldCreateUser: true, // cria usuário se não existir (signup)
      },
    })

    if (error) {
      setFormError('Ocorreu um erro. Tente novamente.')
      return
    }

    setSentEmail(values.email)
    setSent(true)
  }

  if (sent) {
    return (
      <Card>
        <CardContent className="pt-6 flex flex-col items-center text-center gap-4">
          <MailCheck className="w-12 h-12 text-primary" />
          <h2 className="text-xl font-semibold">Verifique seu e-mail</h2>
          <p className="text-sm text-muted-foreground">
            Enviamos um link de acesso para <strong>{sentEmail}</strong>. Clique no link para entrar.
            O link é válido por 24 horas.
          </p>
          <p className="text-xs text-muted-foreground">
            Não recebeu? Verifique a pasta de spam ou tente novamente.
          </p>
        </CardContent>
      </Card>
    )
  }

  return (
    <Card>
      <CardHeader>
        <CardTitle className="text-2xl">Criar conta</CardTitle>
        <CardDescription>Aurel Iris — apoio à anamnese terapêutica</CardDescription>
      </CardHeader>
      <CardContent>
        {formError && (
          <div className="mb-4 bg-destructive/10 border border-destructive text-sm px-4 py-2 rounded text-destructive">
            {formError}
          </div>
        )}
        <Form {...form}>
          <form onSubmit={form.handleSubmit(onSubmit)} className="space-y-4">
            <FormField
              control={form.control}
              name="full_name"
              render={({ field }) => (
                <FormItem>
                  <FormLabel>Nome completo</FormLabel>
                  <FormControl>
                    <Input placeholder="Nome do terapeuta" {...field} />
                  </FormControl>
                  <FormMessage />
                </FormItem>
              )}
            />
            <FormField
              control={form.control}
              name="email"
              render={({ field }) => (
                <FormItem>
                  <FormLabel>E-mail</FormLabel>
                  <FormControl>
                    <Input type="email" placeholder="seu@email.com" {...field} />
                  </FormControl>
                  <FormMessage />
                </FormItem>
              )}
            />
            <Button
              type="submit"
              className="w-full"
              disabled={form.formState.isSubmitting}
              aria-busy={form.formState.isSubmitting}
            >
              {form.formState.isSubmitting ? (
                <>
                  <Loader2 className="mr-2 h-4 w-4 animate-spin" />
                  Salvando...
                </>
              ) : (
                'Enviar link de acesso'
              )}
            </Button>
          </form>
        </Form>
        <p className="mt-4 text-center text-sm text-muted-foreground">
          Já tem conta?{' '}
          <Link href="/login" className="underline">
            Entrar
          </Link>
        </p>
      </CardContent>
    </Card>
  )
}
```

**apps/web/app/(auth)/login/page.tsx** — similar ao signup mas sem full_name e com shouldCreateUser: false (T-02-04):

```tsx
'use client'

import { useState } from 'react'
import { useForm } from 'react-hook-form'
import { zodResolver } from '@hookform/resolvers/zod'
import { z } from 'zod'
import Link from 'next/link'
import { useSearchParams } from 'next/navigation'
import { MailCheck, Loader2 } from 'lucide-react'
import { createClient } from '@/lib/supabase/client'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'
import {
  Form,
  FormControl,
  FormField,
  FormItem,
  FormLabel,
  FormMessage,
} from '@/components/ui/form'

const loginSchema = z.object({
  email: z.string().min(1, 'E-mail é obrigatório').email('E-mail inválido'),
})

type LoginFormValues = z.infer<typeof loginSchema>

export default function LoginPage() {
  const searchParams = useSearchParams()
  const hasError = searchParams.get('error') === 'auth_callback_failed'

  const [sent, setSent] = useState(false)
  const [sentEmail, setSentEmail] = useState('')
  const [formError, setFormError] = useState<string | null>(
    hasError ? 'Link inválido ou expirado. Solicite um novo link de acesso.' : null
  )

  const form = useForm<LoginFormValues>({
    resolver: zodResolver(loginSchema),
    defaultValues: { email: '' },
  })

  const siteUrl = process.env.NEXT_PUBLIC_SITE_URL || 'http://localhost:3000'

  async function onSubmit(values: LoginFormValues) {
    setFormError(null)
    const supabase = createClient()
    const { error } = await supabase.auth.signInWithOtp({
      email: values.email,
      options: {
        emailRedirectTo: `${siteUrl}/api/auth/callback`,
        shouldCreateUser: false, // NÃO cria usuário se não existir (T-02-04, Pitfall 3)
      },
    })

    if (error) {
      // Supabase retorna erro quando email não existe (com shouldCreateUser: false)
      if (error.message.toLowerCase().includes('not found') || error.status === 422) {
        setFormError('Não encontramos uma conta com este e-mail.')
      } else {
        setFormError('Ocorreu um erro. Tente novamente.')
      }
      return
    }

    setSentEmail(values.email)
    setSent(true)
  }

  if (sent) {
    return (
      <Card>
        <CardContent className="pt-6 flex flex-col items-center text-center gap-4">
          <MailCheck className="w-12 h-12 text-primary" />
          <h2 className="text-xl font-semibold">Verifique seu e-mail</h2>
          <p className="text-sm text-muted-foreground">
            Enviamos um link de acesso para <strong>{sentEmail}</strong>. Clique no link para entrar.
            O link é válido por 24 horas.
          </p>
          <p className="text-xs text-muted-foreground">
            Não recebeu? Verifique a pasta de spam ou tente novamente.
          </p>
        </CardContent>
      </Card>
    )
  }

  return (
    <Card>
      <CardHeader>
        <CardTitle className="text-2xl">Entrar</CardTitle>
        <CardDescription>Acesse sua conta Aurel Iris</CardDescription>
      </CardHeader>
      <CardContent>
        {formError && (
          <div className="mb-4 bg-destructive/10 border border-destructive text-sm px-4 py-2 rounded text-destructive">
            {formError}
            {formError.includes('Não encontramos') && (
              <span>
                {' '}
                <Link href="/signup" className="underline">
                  Deseja criar uma conta?
                </Link>
              </span>
            )}
          </div>
        )}
        <Form {...form}>
          <form onSubmit={form.handleSubmit(onSubmit)} className="space-y-4">
            <FormField
              control={form.control}
              name="email"
              render={({ field }) => (
                <FormItem>
                  <FormLabel>E-mail</FormLabel>
                  <FormControl>
                    <Input type="email" placeholder="seu@email.com" {...field} />
                  </FormControl>
                  <FormMessage />
                </FormItem>
              )}
            />
            <Button
              type="submit"
              className="w-full"
              disabled={form.formState.isSubmitting}
              aria-busy={form.formState.isSubmitting}
            >
              {form.formState.isSubmitting ? (
                <>
                  <Loader2 className="mr-2 h-4 w-4 animate-spin" />
                  Enviando...
                </>
              ) : (
                'Enviar link de acesso'
              )}
            </Button>
          </form>
        </Form>
        <p className="mt-4 text-center text-sm text-muted-foreground">
          Primeira vez?{' '}
          <Link href="/signup" className="underline">
            Criar conta
          </Link>
        </p>
      </CardContent>
    </Card>
  )
}
```

Nota sobre useSearchParams: em Next.js 15 App Router, `useSearchParams()` em um Client Component requer que a página seja wrapped em `<Suspense>` se houver preocupações de SSR. Para simplificar no MVP, a leitura de searchParams pode ser feita diretamente no componente 'use client' — Next.js emitirá um aviso de build mas não causará erro funcional. Se o build emitir erro (não apenas warning), extrair o componente com useSearchParams para um subcomponente separado wrapped em Suspense.
  </action>
  <verify>
    <automated>cd D:/Projetos/Iridologista/apps/web && pnpm exec tsc --noEmit --skipLibCheck 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/(auth)/signup/page.tsx` existe com `'use client'` e contém `shouldCreateUser: true`
    - `apps/web/app/(auth)/login/page.tsx` existe com `'use client'` e contém `shouldCreateUser: false` (T-02-04)
    - Ambas as páginas importam `createClient` de `@/lib/supabase/client` (não do server)
    - Ambas as páginas contêm `emailRedirectTo` apontando para `/api/auth/callback`
    - `grep -r "diagnóstico\|tratamento\|cura" apps/web/app/(auth)/` retorna 0 (vocabulário LGPD proibido)
    - signup/page.tsx contém o link "Já tem conta? Entrar" para /login
    - login/page.tsx contém o link "Primeira vez? Criar conta" para /signup
    - signup/page.tsx usa `zodResolver` com schema Zod v4 (`.min(1,` não `required_error:`)
    - `pnpm build` em apps/web/ completa sem erro TypeScript (pode ter warning de useSearchParams — aceitável)
  </acceptance_criteria>
  <done>Páginas de signup e login funcionais com magic link flow, confirmation state, e links cruzados corretos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form → Supabase Auth | Email e full_name enviados via signInWithOtp; validados com Zod antes de chamar a API |
| Supabase Auth → trigger (profiles) | full_name passa via raw_user_meta_data; coalesce no trigger protege contra null |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Spoofing | app/(auth)/login/page.tsx | mitigate | `shouldCreateUser: false` em /login — não cria contas acidentalmente para emails errados |
| T-02-07 | Information Disclosure | app/(auth)/signup/page.tsx, login/page.tsx | mitigate | Vocabulário proibido (diagnóstico, tratamento, cura) ausente; copy LGPD obrigatória no layout |
| T-02-auth-csrf | Tampering | Server Actions (nenhum nesta wave) | accept | Formulários usam Client Components + createBrowserClient; Next.js Server Actions têm CSRF built-in para wave seguinte |
</threat_model>

<verification>
Após completar todas as tasks deste plano:

1. `pnpm --filter @aurel-iris/web build` deve compilar sem erros (warnings de useSearchParams são aceitáveis)
2. `http://localhost:3000/signup` carrega card com título "Criar conta" e footer LGPD
3. `http://localhost:3000/login` carrega card com título "Entrar"
4. `grep -r "diagnóstico\|tratamento\|cura" apps/web/app/(auth)/` retorna 0
5. `grep "shouldCreateUser: false" apps/web/app/(auth)/login/page.tsx` retorna 1 resultado
6. `grep "shouldCreateUser: true" apps/web/app/(auth)/signup/page.tsx` retorna 1 resultado
</verification>

<success_criteria>
- Route group (auth) com layout minimal e footer LGPD criado
- /signup: formulário email + nome, shouldCreateUser: true, confirmation state após envio
- /login: formulário email, shouldCreateUser: false, confirmation state após envio
- Nenhum vocabulário proibido nas páginas de auth
- Shadcn components instalados: input, label, form, card, toast
- react-hook-form + zod + @hookform/resolvers instalados
- Build TypeScript limpo
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-auth-dashboard-basico/02-02-SUMMARY.md` seguindo o template em `$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
