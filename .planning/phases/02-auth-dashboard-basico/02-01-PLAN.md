---
phase: 2
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/lib/supabase/client.ts
  - apps/web/lib/supabase/server.ts
  - apps/web/lib/supabase/middleware.ts
  - apps/web/middleware.ts
  - apps/web/app/api/auth/callback/route.ts
  - supabase/migrations/0003_profiles_trigger.sql
  - apps/web/.env.example
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "Supabase client (browser + server) é instanciável sem erro de runtime"
    - "middleware.ts protege /dashboard, /clientes, /leituras — redireciona para /login sem sessão"
    - "GET /api/auth/callback?code=... troca o code e redireciona para /dashboard"
    - "trigger on_auth_user_created existe em auth.users e cria row em public.profiles com trial"
    - "NEXT_PUBLIC_SITE_URL está em .env.example e em Vercel"
  artifacts:
    - path: "apps/web/lib/supabase/client.ts"
      provides: "createClient() browser — createBrowserClient<Database>"
      exports: ["createClient"]
    - path: "apps/web/lib/supabase/server.ts"
      provides: "createClient() server — createServerClient<Database> com await cookies()"
      exports: ["createClient"]
    - path: "apps/web/lib/supabase/middleware.ts"
      provides: "updateSession() helper — refresh token + retorna { supabase, supabaseResponse, user }"
      exports: ["updateSession"]
    - path: "apps/web/middleware.ts"
      provides: "Auth guard Next.js — redireciona não-autenticados"
      contains: "export async function middleware"
    - path: "apps/web/app/api/auth/callback/route.ts"
      provides: "exchangeCodeForSession — PKCE callback server-only"
      exports: ["GET"]
    - path: "supabase/migrations/0003_profiles_trigger.sql"
      provides: "Trigger SQL on_auth_user_created → profiles INSERT com trial"
      contains: "on_auth_user_created"
  key_links:
    - from: "apps/web/middleware.ts"
      to: "apps/web/lib/supabase/middleware.ts"
      via: "import { updateSession }"
      pattern: "updateSession"
    - from: "apps/web/app/api/auth/callback/route.ts"
      to: "apps/web/lib/supabase/server.ts"
      via: "import { createClient }"
      pattern: "exchangeCodeForSession"
    - from: "supabase/migrations/0003_profiles_trigger.sql"
      to: "public.profiles"
      via: "INSERT ... ON CONFLICT DO NOTHING"
      pattern: "on_auth_user_created"

user_setup:
  - service: resend
    why: "SMTP custom para envio de magic link em produção"
    env_vars:
      - name: RESEND_API_KEY
        source: "resend.com/api-keys — já deve existir no .env.local e na Vercel desde Fase 1"
    dashboard_config:
      - task: "Verificar domínio mail.soprodaorigem.com no Resend"
        location: "resend.com/domains → Add Domain → inserir 'mail.soprodaorigem.com' → copiar registros SPF TXT e DKIM TXT"
      - task: "Adicionar registros DNS no painel Hostinger"
        location: "Hostinger → DNS Zone Editor → subdomínio mail.soprodaorigem.com → adicionar os registros SPF e DKIM fornecidos pelo Resend → aguardar propagação (5–30 min) → clicar Verify no Resend"
      - task: "Configurar SMTP custom no Supabase Auth"
        location: "supabase.com/dashboard/project/owgbrllpznsngrkvodyw → Authentication → Email → SMTP Settings → Enable: ON → Host: smtp.resend.com → Port: 465 → Username: resend → Password: [RESEND_API_KEY] → Sender name: Aurel Iris → Sender email: noreply@mail.soprodaorigem.com"
      - task: "Customizar template do magic link"
        location: "Supabase Dashboard → Authentication → Email Templates → Magic Link → Subject: 'Seu acesso ao Aurel Iris' → Body: HTML do template pt-BR (ver ação da Task 3 abaixo)"
      - task: "Registrar Redirect URLs no Supabase"
        location: "Supabase Dashboard → Authentication → URL Configuration → Site URL: https://aurel-iris-web.vercel.app → Redirect URLs: adicionar https://aurel-iris-web.vercel.app/api/auth/callback e http://localhost:3000/api/auth/callback"
      - task: "Adicionar NEXT_PUBLIC_SITE_URL no Vercel"
        location: "Vercel → Project aurel-iris-web → Settings → Environment Variables → adicionar NEXT_PUBLIC_SITE_URL = https://aurel-iris-web.vercel.app (Production + Preview + Development)"
---

<objective>
Instalar a fundação completa de autenticação Supabase: clientes SSR (browser + server), middleware de proteção de rotas, callback PKCE do magic link, migration com trigger de criação de profiles, e configuração do Resend SMTP.

Purpose: Toda UI de auth (Wave 2) e o dashboard (Wave 3) dependem destes artefatos de infraestrutura. Sem eles não há sessão, proteção de rotas nem criação automática de profiles.

Output: 5 arquivos TypeScript + 1 migration SQL + .env.example atualizado + banco com trigger aplicado via supabase db push --linked.
</objective>

<execution_context>
@D:/Projetos/Iridologista/apps/web/../../.claude/get-shit-done/workflows/execute-plan.md
@D:/Projetos/Iridologista/apps/web/../../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Projetos/Iridologista/.planning/ROADMAP.md
@D:/Projetos/Iridologista/.planning/phases/02-auth-dashboard-basico/02-CONTEXT.md
@D:/Projetos/Iridologista/.planning/phases/02-auth-dashboard-basico/02-RESEARCH.md

<interfaces>
<!-- Tipos chave do database.ts que os clientes Supabase precisam. Extraído de apps/web/types/database.ts -->

```typescript
// apps/web/types/database.ts (excerpt relevante para este plano)
export type Database = {
  public: {
    Tables: {
      profiles: {
        Row: {
          id: string
          full_name: string
          subscription_status: string | null
          trial_ends_at: string | null
          bio: string | null
          city: string | null
          phone: string | null
          professional_id: string | null
          state: string | null
          stripe_customer_id: string | null
          created_at: string | null
        }
        Insert: {
          id: string
          full_name: string
          subscription_status?: string | null
          trial_ends_at?: string | null
          // ... outros campos opcionais
        }
      }
      clients: {
        Row: {
          id: string
          therapist_id: string
          full_name: string
          birth_date: string | null
          gender: string | null
          notes: string | null
          consent_document_url: string | null
          consent_signed_at: string | null
          created_at: string | null
        }
      }
    }
  }
}
```

<!-- Estrutura de arquivos existente em apps/web/app/ -->
<!-- app/layout.tsx — root layout com Geist font, lang="pt-BR", metadata LGPD já correto -->
<!-- app/page.tsx — página inicial placeholder -->
<!-- app/globals.css — CSS variables base-nova neutral (sidebar vars já incluídas) -->
<!-- components/ui/button.tsx — único componente shadcn existente -->
<!-- lib/utils.ts — cn() helper -->
<!-- types/database.ts — tipos gerados do Supabase (profiles, clients, readings, etc.) -->
</interfaces>
</context>

<tasks>

<task type="auto" id="02-01-T1">
  <name>Task 1: Instalar @supabase/ssr e criar os três clientes Supabase</name>
  <read_first>
    - apps/web/package.json (versões e dependências existentes)
    - apps/web/types/database.ts (tipo Database completo)
    - apps/web/lib/utils.ts (padrão de exportação do projeto)
    - .planning/phases/02-auth-dashboard-basico/02-RESEARCH.md (Pattern 1, 2 e 3 — código exato dos clients)
  </read_first>
  <files>
    apps/web/lib/supabase/client.ts,
    apps/web/lib/supabase/server.ts,
    apps/web/lib/supabase/middleware.ts
  </files>
  <action>
Execute em apps/web/:

```bash
cd apps/web && pnpm add @supabase/ssr@0.10.2 @supabase/supabase-js@2.105.1
```

Criar o diretório: `apps/web/lib/supabase/` (se não existir).

**apps/web/lib/supabase/client.ts** — cliente browser (createBrowserClient):
```typescript
import { createBrowserClient } from '@supabase/ssr'
import type { Database } from '@/types/database'

export const createClient = () =>
  createBrowserClient<Database>(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
  )
```

**apps/web/lib/supabase/server.ts** — cliente server (createServerClient, SEMPRE await cookies()):
```typescript
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'
import type { Database } from '@/types/database'

export const createClient = async () => {
  const cookieStore = await cookies()

  return createServerClient<Database>(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() {
          return cookieStore.getAll()
        },
        setAll(cookiesToSet) {
          try {
            cookiesToSet.forEach(({ name, value, options }) =>
              cookieStore.set(name, value, options)
            )
          } catch {
            // Chamado de Server Component — cookies não podem ser escritos aqui.
            // O middleware já fez o refresh; este catch silencia o erro esperado.
          }
        },
      },
    }
  )
}
```

**apps/web/lib/supabase/middleware.ts** — helper updateSession (refresh token via getUser()):
```typescript
import { createServerClient } from '@supabase/ssr'
import { NextResponse, type NextRequest } from 'next/server'
import type { Database } from '@/types/database'

export async function updateSession(request: NextRequest) {
  let supabaseResponse = NextResponse.next({
    request,
  })

  const supabase = createServerClient<Database>(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() {
          return request.cookies.getAll()
        },
        setAll(cookiesToSet) {
          cookiesToSet.forEach(({ name, value }) =>
            request.cookies.set(name, value)
          )
          supabaseResponse = NextResponse.next({ request })
          cookiesToSet.forEach(({ name, value, options }) =>
            supabaseResponse.cookies.set(name, value, options)
          )
        },
      },
    }
  )

  // NUNCA usar getSession() no servidor — não valida assinatura JWT.
  // getUser() valida o JWT contra as chaves públicas do projeto Supabase.
  const { data: { user } } = await supabase.auth.getUser()

  return { supabase, supabaseResponse, user }
}
```
  </action>
  <verify>
    <automated>cd D:/Projetos/Iridologista/apps/web && pnpm exec tsc --noEmit --skipLibCheck 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/lib/supabase/client.ts` exporta `createClient` usando `createBrowserClient`
    - `apps/web/lib/supabase/server.ts` exporta `createClient` usando `createServerClient` com `await cookies()`
    - `apps/web/lib/supabase/middleware.ts` exporta `updateSession` e chama `supabase.auth.getUser()` (não getSession)
    - `apps/web/package.json` contém `"@supabase/ssr"` e `"@supabase/supabase-js"` em dependencies
    - `grep -r "getSession" apps/web/lib/supabase/` retorna 0 resultados (getSession proibido no servidor per T-02-01)
  </acceptance_criteria>
  <done>Três clientes Supabase criados e typecheck limpo; @supabase/ssr instalado.</done>
</task>

<task type="auto" id="02-01-T2">
  <name>Task 2: Criar middleware.ts (auth guard) e /api/auth/callback</name>
  <read_first>
    - apps/web/lib/supabase/middleware.ts (recém-criado — assinatura de updateSession)
    - apps/web/lib/supabase/server.ts (recém-criado — assinatura de createClient)
    - .planning/phases/02-auth-dashboard-basico/02-RESEARCH.md (Pattern 3 middleware.ts e Pattern 4 callback)
    - apps/web/app/layout.tsx (confirmar que root layout não tem lógica de auth)
  </read_first>
  <files>
    apps/web/middleware.ts,
    apps/web/app/api/auth/callback/route.ts
  </files>
  <action>
**apps/web/middleware.ts** — auth guard, arquivo na raiz de apps/web/ (NÃO em app/):
```typescript
import { updateSession } from '@/lib/supabase/middleware'
import { NextResponse, type NextRequest } from 'next/server'

// ATENÇÃO Next.js 15: arquivo se chama middleware.ts (não proxy.ts — isso é Next.js 16+)
// export function se chama middleware (não proxy)

const PROTECTED_PATHS = ['/dashboard', '/clientes', '/leituras', '/assinatura']

export async function middleware(request: NextRequest) {
  const { supabaseResponse, user } = await updateSession(request)
  const pathname = request.nextUrl.pathname

  const isProtected = PROTECTED_PATHS.some(p => pathname.startsWith(p))

  if (isProtected && !user) {
    const url = request.nextUrl.clone()
    url.pathname = '/login'
    return NextResponse.redirect(url)
  }

  // Usuário logado tentando acessar /login ou /signup → redireciona para /dashboard
  if (user && (pathname === '/login' || pathname === '/signup')) {
    const url = request.nextUrl.clone()
    url.pathname = '/dashboard'
    return NextResponse.redirect(url)
  }

  return supabaseResponse
}

export const config = {
  matcher: [
    '/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)',
  ],
}
```

Criar diretório `apps/web/app/api/auth/callback/`.

**apps/web/app/api/auth/callback/route.ts** — PKCE callback, server-only (per T-02-02):
```typescript
import { createClient } from '@/lib/supabase/server'
import { NextResponse, type NextRequest } from 'next/server'

export async function GET(request: NextRequest) {
  const { searchParams, origin } = new URL(request.url)
  const code = searchParams.get('code')

  if (code) {
    const supabase = await createClient()
    const { error } = await supabase.auth.exchangeCodeForSession(code)

    if (!error) {
      // Redirect para /dashboard após auth bem-sucedida (per D-04, D-05)
      return NextResponse.redirect(`${origin}/dashboard`)
    }
  }

  // Falha na troca → retorna para login com indicação de erro
  return NextResponse.redirect(`${origin}/login?error=auth_callback_failed`)
}
```
  </action>
  <verify>
    <automated>cd D:/Projetos/Iridologista/apps/web && pnpm exec tsc --noEmit --skipLibCheck 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/middleware.ts` existe com `export async function middleware` (não proxy) e `export const config`
    - `config.matcher` NÃO contém `/(dashboard)` com parênteses — usa `/dashboard` sem parênteses (pitfall 7)
    - `PROTECTED_PATHS` inclui `/dashboard`, `/clientes`, `/leituras`
    - `apps/web/app/api/auth/callback/route.ts` exporta `GET` e contém `exchangeCodeForSession`
    - `grep "getSession" apps/web/app/api/auth/callback/route.ts` retorna 0 (T-02-02: token não exposto ao browser)
    - `grep "proxy" apps/web/middleware.ts` retorna 0 (confirma que é middleware.ts / Next.js 15)
    - TypeScript typecheck limpo
  </acceptance_criteria>
  <done>middleware.ts protege rotas do dashboard e /api/auth/callback troca code por sessão server-side.</done>
</task>

<task type="auto" id="02-01-T3">
  <name>Task 3: Migration 0003 (profiles trigger) + [BLOCKING] supabase db push --linked + .env.example</name>
  <read_first>
    - supabase/migrations/0002_grant_authenticated_role.sql (padrão de migrations existentes)
    - .planning/phases/02-auth-dashboard-basico/02-RESEARCH.md (SQL da Migration 0003 e Pattern 6)
    - apps/web/types/database.ts (estrutura da tabela profiles — id, full_name, subscription_status, trial_ends_at)
    - .env.example na raiz do repo (se existir — para saber quais chaves já estão listadas)
  </read_first>
  <files>
    supabase/migrations/0003_profiles_trigger.sql,
    apps/web/.env.example
  </files>
  <action>
**supabase/migrations/0003_profiles_trigger.sql** — trigger que cria profiles automaticamente no signup:
```sql
-- Cria profiles automaticamente no signup de auth.users.
-- Design choices:
--   - security definer: executa com privilégios do owner, não do caller (T-02-05)
--   - set search_path = '': previne SQL injection via search_path
--   - ON CONFLICT DO NOTHING: idempotente; protege contra double-fire
--   - coalesce para full_name: profiles.full_name é NOT NULL

create or replace function public.handle_new_user()
returns trigger
language plpgsql
security definer set search_path = ''
as $$
begin
  insert into public.profiles (id, full_name, subscription_status, trial_ends_at)
  values (
    new.id,
    coalesce(new.raw_user_meta_data->>'full_name', 'Terapeuta'),
    'trial',
    now() + interval '14 days'
  )
  on conflict (id) do nothing;

  return new;
end;
$$;

-- Drop trigger se já existir (idempotência na migration)
drop trigger if exists on_auth_user_created on auth.users;

create trigger on_auth_user_created
  after insert on auth.users
  for each row execute procedure public.handle_new_user();
```

**[BLOCKING] Aplicar migration ao banco remoto:**
```bash
cd D:/Projetos/Iridologista && supabase db push --linked
```

Aguardar conclusão. Se houver prompt interativo pedindo confirmação, responder "y". Após push, verificar que trigger existe:
```sql
-- Executar no Supabase Dashboard → SQL Editor:
select trigger_name, event_object_schema, event_object_table, action_timing
from information_schema.triggers
where trigger_name = 'on_auth_user_created';
-- Deve retornar 1 row com event_object_table = 'users'
```

**apps/web/.env.example** — adicionar NEXT_PUBLIC_SITE_URL (verificar se .env.example existe na raiz ou em apps/web; usar o que existir da Fase 1):
Adicionar ao final do arquivo:
```
# Site URL (usado em emailRedirectTo do magic link)
# Produção: https://aurel-iris-web.vercel.app
# Local: http://localhost:3000
NEXT_PUBLIC_SITE_URL=https://aurel-iris-web.vercel.app
```

**Template do e-mail (configurar manualmente no Supabase Dashboard como user_setup):**
O template HTML completo está nos passos de user_setup desta plan. Remetente: `noreply@mail.soprodaorigem.com` (D-08). Usar `{{ .ConfirmationURL }}` como href do botão.
  </action>
  <verify>
    <automated>cd D:/Projetos/Iridologista && supabase migration list --linked 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/0003_profiles_trigger.sql` existe e contém `security definer set search_path = ''`
    - `supabase/migrations/0003_profiles_trigger.sql` contém `on conflict (id) do nothing` (idempotência)
    - `supabase migration list --linked` mostra `0003_profiles_trigger` como aplicada
    - Consulta SQL `select trigger_name from information_schema.triggers where trigger_name = 'on_auth_user_created'` retorna 1 row no Supabase remoto
    - `apps/web/.env.example` contém a chave `NEXT_PUBLIC_SITE_URL`
    - O trigger usa `coalesce(new.raw_user_meta_data->>'full_name', 'Terapeuta')` (NOT NULL safe)
  </acceptance_criteria>
  <done>Migration 0003 aplicada ao Supabase remoto; trigger on_auth_user_created ativo; .env.example com NEXT_PUBLIC_SITE_URL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/auth/callback | Código PKCE recebido via query param; server valida com Supabase Auth |
| middleware → Supabase Auth | getUser() valida JWT server-side antes de permitir acesso a rotas protegidas |
| trigger SQL → public.profiles | Executa com security definer; search_path fixo evita substituição de funções |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | lib/supabase/middleware.ts, lib/supabase/server.ts | mitigate | Usar `supabase.auth.getUser()` em todo código servidor; getSession() banido — não valida assinatura JWT |
| T-02-02 | Information Disclosure | app/api/auth/callback/route.ts | mitigate | exchangeCodeForSession executado server-side; código e token nunca expostos ao browser; erro → redirect sem token |
| T-02-03 | Tampering | Supabase Auth redirect URLs | mitigate | Registrar apenas URLs explícitas no Dashboard (produção + localhost); Supabase bloqueia qualquer outro redirect |
| T-02-05 | Elevation of Privilege | supabase/migrations/0003_profiles_trigger.sql | mitigate | `security definer set search_path = ''` previne SQL injection via search_path manipulation |
</threat_model>

<verification>
Após completar todas as tasks deste plano:

1. `pnpm --filter @aurel-iris/web build` deve compilar sem erros TypeScript
2. `supabase migration list --linked` mostra 0003 como aplicada
3. SQL no Supabase Dashboard: `select trigger_name from information_schema.triggers where trigger_name = 'on_auth_user_created'` retorna 1 row
4. `grep -r "getSession" apps/web/lib/supabase/` retorna 0 resultados
5. `grep -r "proxy" apps/web/middleware.ts` retorna 0 (confirma Next.js 15 pattern)
</verification>

<success_criteria>
- Três clientes Supabase (browser, server, middleware) criados com os padrões corretos do @supabase/ssr
- middleware.ts protege /dashboard, /clientes, /leituras usando getUser() (não getSession)
- /api/auth/callback troca code server-side e redireciona para /dashboard
- Migration 0003 aplicada — trigger cria profiles com trial automaticamente no signup
- .env.example contém NEXT_PUBLIC_SITE_URL
- Build de produção limpo sem erros TypeScript
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-auth-dashboard-basico/02-01-SUMMARY.md` seguindo o template em `$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
