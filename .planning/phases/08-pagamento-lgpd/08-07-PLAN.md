---
phase: 08-pagamento-lgpd
plan: 07
type: execute
wave: 3
depends_on: [08-05]
files_modified:
  - apps/web/app/actions/readings.ts
  - apps/web/app/actions/invites.ts
  - apps/web/app/api/readings/[id]/analyze/route.ts
  - apps/web/lib/gates/billing-gate.ts
  - apps/web/lib/gates/__tests__/billing-gate.test.ts
autonomous: true
requirements: [BILLING-02]
must_haves:
  truths:
    - "createReadingAction substitui BETA_READING_CAP por reserveCreditForReading; no_balance → erro humano com link /assinatura/comprar"
    - "createInviteTokenAction (invites) chama reserveCreditForReading ANTES de INSERT do token; no_balance bloqueia"
    - "analyze route chama convertReservationToConsume após report_generations INSERT success; idempotent"
    - "evaluateBilling (pure gate) compõe trial + credit balance + internal_use → BillingGate union"
    - "Internal_use=true → todos os 3 gates passam (não consome, não bloqueia)"
  artifacts:
    - path: "apps/web/lib/gates/billing-gate.ts"
      provides: "Pure gate composition pattern (lib/gates/profile-completeness style)"
      exports: ["evaluateBilling", "type BillingGate"]
  key_links:
    - from: "createReadingAction"
      to: "reserveCreditForReading"
      via: "replace BETA_READING_CAP enforcement"
      pattern: "reserveCreditForReading"
    - from: "createInviteTokenAction"
      to: "reserveCreditForReading"
      via: "new gate before token INSERT"
      pattern: "reserveCreditForReading"
    - from: "analyze route success path"
      to: "convertReservationToConsume"
      via: "after report_generations success"
      pattern: "convertReservationToConsume"
---

<objective>
**Wave 3 — credit gate ONLY.** Termo gate de LGPD-01 (D-19) é entregue em plano 08-15 (wave 5, depende de 08-08 termo helper). Quando os dois planos terminarem, BILLING-02 + BILLING-03 estão completos.

Wire o credit ledger nos 3 fluxos identificados em D-10:
1. **createReadingAction** (já existe) — substitui o cap beta velho por `reserveCreditForReading`
2. **createInviteTokenAction** (já existe) — NOVO gate de saldo antes de criar token de convite
3. **analyze route** (já existe) — converte reservation em consume após report_generations success

Plus: `lib/gates/billing-gate.ts` puro pra que UI (plano 08-10/08-11) reuse sem duplicar lógica.

Purpose: Sem este plano, o credit ledger é uma tabela isolada. Este plano é onde o "saldo passa a importar" pro produto.
Output: 3 arquivos existentes modificados + 1 gate novo + 1 test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-pagamento-lgpd/08-CONTEXT.md
@.planning/phases/08-pagamento-lgpd/08-RESEARCH.md
@.planning/phases/08-pagamento-lgpd/08-05-SUMMARY.md
@apps/web/app/actions/readings.ts
@apps/web/app/actions/invites.ts
@apps/web/app/api/readings/[id]/analyze/route.ts
@apps/web/lib/beta/config.ts
@apps/web/lib/gates/profile-completeness.ts
@apps/web/lib/billing/credits.ts
@apps/web/lib/billing/trial.ts
@apps/web/lib/audit/log.ts

<interfaces>
<!-- From 08-05 -->
```typescript
reserveCreditForReading(userId, readingId): Promise<ReserveResult>
convertReservationToConsume(readingId): Promise<ConsumeResult>
evaluateTrial(row, now): TrialState
getTrialState(userId): Promise<TrialState>
```

<!-- From 08-03 (audit log shared) -->
```typescript
// apps/web/lib/audit/log.ts
logAuditEvent({ event_type, actor_user_id, actor_email?, target_type, target_id, metadata? }): Promise<void>
```

<!-- Existing -->
- apps/web/app/actions/readings.ts:createReadingAction (lines 47-138)
  - Currently checks BETA_READING_CAP via count query
- apps/web/app/actions/invites.ts:createInviteTokenAction (lines 40-90)
  - Currently no credit gate
- apps/web/app/api/readings/[id]/analyze/route.ts (lines 84-100 hook point)
  - After report_generations INSERT success, before return

<!-- Pattern from lib/gates/profile-completeness.ts -->
```typescript
export type ProfileGate = { complete: true } | { complete: false; missing: string[] }
export function evaluateProfileCompleteness(profile, now): ProfileGate { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/gates/billing-gate.ts + test</name>
  <files>apps/web/lib/gates/billing-gate.ts, apps/web/lib/gates/__tests__/billing-gate.test.ts</files>
  <behavior>
    - evaluateBilling({ internal_use: true, ... }) → { status: 'ok', source: 'internal' }
    - evaluateBilling({ trial: { active, readings_remaining: 1, ... }, total_credits_remaining: 0 }) → { status: 'ok', source: 'trial' }
    - evaluateBilling({ trial: { ended }, total_credits_remaining: 5 }) → { status: 'ok', source: 'credit' }
    - evaluateBilling({ trial: { ended }, total_credits_remaining: 0 }) → { status: 'no_balance', redirect_to: '/assinatura/comprar' }
    - Pure function, sem DB access; aceita snapshot
  </behavior>
  <action>
**Arquivo principal:**

```typescript
import type { TrialState } from '@/lib/billing/trial'

export type BillingGate =
  | { status: 'ok'; source: 'internal' | 'trial' | 'credit'; trial_state?: TrialState; credits_remaining?: number }
  | { status: 'no_balance'; trial_state: TrialState; credits_remaining: number; redirect_to: '/assinatura/comprar' }

export interface BillingSnapshot {
  internal_use: boolean
  trial: TrialState
  total_credits_remaining: number  // soma de leituras_remaining em customer_credits status='active' não expirados
  total_credits_reserved: number   // pra UI display
}

/**
 * Pure gate: dado snapshot, decide se terapeuta pode usar 1 leitura.
 * Não escreve em nada — chamado por UI (CreditsBalanceWidget) E por server actions (createReadingAction).
 * 
 * Server actions confiam neste resultado mas o write atômico (reserveCreditForReading)
 * é quem garante atomicidade — esta função pode ser stale por milisegundos.
 */
export function evaluateBilling(snap: BillingSnapshot): BillingGate {
  // D-09: internal_use bypass sempre
  if (snap.internal_use) {
    return { status: 'ok', source: 'internal' }
  }

  // D-06: trial active wins (cliente em trial gera de graça)
  if (snap.trial.status === 'active') {
    return { status: 'ok', source: 'trial', trial_state: snap.trial }
  }

  // D-04: créditos comprados
  if (snap.total_credits_remaining > 0) {
    return { status: 'ok', source: 'credit', credits_remaining: snap.total_credits_remaining }
  }

  // D-07: trial ended + 0 créditos = bloqueia
  return {
    status: 'no_balance',
    trial_state: snap.trial,
    credits_remaining: snap.total_credits_remaining,
    redirect_to: '/assinatura/comprar',
  }
}
```

**Test file:**

```typescript
import { describe, expect, it } from 'vitest'
import { evaluateBilling } from '../billing-gate'

describe('evaluateBilling', () => {
  const trialActive = { status: 'active' as const, readings_remaining: 2, days_remaining: 30 }
  const trialEnded = { status: 'ended' as const, reason: 'readings_exhausted' as const }
  const noTrial = { status: 'no_trial' as const }

  it('internal_use always passes regardless of trial/credits', () => {
    expect(evaluateBilling({ internal_use: true, trial: trialEnded, total_credits_remaining: 0, total_credits_reserved: 0 }))
      .toEqual({ status: 'ok', source: 'internal' })
  })

  it('trial active is source even with 0 credits', () => {
    const r = evaluateBilling({ internal_use: false, trial: trialActive, total_credits_remaining: 0, total_credits_reserved: 0 })
    expect(r.status).toBe('ok')
    if (r.status === 'ok') expect(r.source).toBe('trial')
  })

  it('credit balance is source after trial ended', () => {
    const r = evaluateBilling({ internal_use: false, trial: trialEnded, total_credits_remaining: 5, total_credits_reserved: 0 })
    expect(r.status).toBe('ok')
    if (r.status === 'ok') expect(r.source).toBe('credit')
  })

  it('no_balance when trial ended + 0 credits', () => {
    const r = evaluateBilling({ internal_use: false, trial: trialEnded, total_credits_remaining: 0, total_credits_reserved: 0 })
    expect(r).toEqual({
      status: 'no_balance',
      trial_state: trialEnded,
      credits_remaining: 0,
      redirect_to: '/assinatura/comprar',
    })
  })

  it('no_balance when no_trial + 0 credits (edge case — usuario antigo sem trial row)', () => {
    const r = evaluateBilling({ internal_use: false, trial: noTrial, total_credits_remaining: 0, total_credits_reserved: 0 })
    expect(r.status).toBe('no_balance')
  })
})
```
  </action>
  <verify>
    <automated>cd D:/Projetos/Iridologista/apps/web && pnpm vitest run lib/gates/__tests__/billing-gate.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>5 testes green; gate é pure (zero imports DB).</done>
</task>

<task type="auto">
  <name>Task 2: createReadingAction — replace BETA_READING_CAP por reserveCreditForReading</name>
  <files>apps/web/app/actions/readings.ts</files>
  <action>
**Read apps/web/app/actions/readings.ts:47-138 createReadingAction** primeiro pra ver o shape atual.

Modificar:
1. Remover (ou comentar com `// DEPRECATED Fase 8`) o block que checa `BETA_READING_CAP` via count query.
2. Após criação da `reading` row (mas ANTES de retornar 'ok'), chamar `reserveCreditForReading(user.id, reading.id)`.
3. Se retorno `{ ok: false, reason: 'no_balance' }` → DELETE da reading row criada (rollback) + retornar erro humano com link.

Pattern (substituir o cap check existente):

```typescript
// IMPORTS no topo
import { reserveCreditForReading } from '@/lib/billing/credits'
import { logAuditEvent } from '@/lib/audit/log'

// ... no createReadingAction, depois de inserir a reading ...

const reserve = await reserveCreditForReading(user.id, reading.id)
if (!reserve.ok) {
  // Rollback: a reading foi criada mas sem reservation; remover pra não poluir
  await service.from('readings').delete().eq('id', reading.id).eq('therapist_id', user.id)
  if (reserve.reason === 'no_balance') {
    return {
      ok: false,
      error: 'Sem saldo. Você esgotou o trial e não tem créditos ativos.',
      action: { label: 'Comprar pacote', href: '/assinatura/comprar' },
    }
  }
  return { ok: false, error: 'Erro interno ao reservar crédito.' }
}

// Log que founder/admin foi usado (D-09 audit)
if (reserve.source === 'internal') {
  await logAuditEvent({
    event_type: 'admin.internal_use_used',
    actor_user_id: user.id,
    target_type: 'reading', target_id: reading.id,
  })
}
```

**NÃO remover `lib/beta/config.ts`** (memory `feedback_delete_consumers_first`) — manter o arquivo temporariamente; novo plano futuro deletará em V1.1 quando legacy paths confirmadamente fora.

Comment block explicando substituição:
```typescript
// Fase 8 (D-10/D-11): substitui BETA_READING_CAP por credit ledger reserve.
// Histórico:
//   - 2026-05-26 (Fase 11): BETA_READING_CAP = 3 enforced via count query.
//   - 2026-05-27 (Fase 8): reserveCreditForReading + lib/billing/credits.
// Reading row é criada PRIMEIRO; reservation atrelada via reading_id.
// Rollback delete da reading se reserve falha (atomicidade soft).
```

**Verificar:** Os existing tests de createReadingAction (se houver) — talvez precise ajustar mocks pra cobrir reserveCreditForReading. Se quebrar tests existentes, adicionar mocks. Aceitar test failures pré-existentes (memory `feedback_quality_scoring_test_gate`).
  </action>
  <verify>
    <automated>cd D:/Projetos/Iridologista/apps/web && pnpm vitest run app/actions/__tests__/readings.test.ts 2>&1 | tail -15 && pnpm tsc --noEmit 2>&1 | grep "readings.ts" | head -3</automated>
  </verify>
  <done>Tests verdes (ou pre-existing failures isolados); tsc limpo; createReadingAction usa reserveCreditForReading.</done>
</task>

<task type="auto">
  <name>Task 3: createInviteTokenAction — gate saldo antes do INSERT</name>
  <files>apps/web/app/actions/invites.ts</files>
  <action>
**Read apps/web/app/actions/invites.ts:40-90 createInviteTokenAction** primeiro.

Adicionar gate ANTES do INSERT do token: este é o D-10 #1 ("verificação antes de criar link de captura remota") + D-11 reserva 7d.

Padrão:

```typescript
import { reserveCreditForReading } from '@/lib/billing/credits'
import { logAuditEvent } from '@/lib/audit/log'

// ... dentro da função, após session gate + zod ...

// Fase 8 D-10 #1: reserva 7d antes de criar link de captura remota.
// Reading row precisa existir ANTES da reservation (reservation referencia reading_id).
// Se o invite create não cria reading row, gerar UUID aqui e inserir reading 'pending_invite' OR
// trocar a ordem de criação no createInviteTokenAction.

// Estratégia mínima:
// - Se invite cria reading row: chamar reserve após INSERT reading, antes INSERT token
// - Se invite NÃO cria reading row (gera só token + reading lazily): NÃO podemos reservar aqui
//   sem trocar arquitetura. Então criar a reading pending ANTES do token, reservar nela.

// Code:
const { data: reading, error: rErr } = await service.from('readings').insert({
  therapist_id: user.id,
  client_id: parsed.data.client_id,
  status: 'pending_capture',
  source_kind: 'remote_invite',
}).select('id').single()
if (rErr || !reading) {
  return { ok: false, error: 'Erro ao criar leitura.' }
}

const reserve = await reserveCreditForReading(user.id, reading.id)
if (!reserve.ok) {
  // Rollback reading
  await service.from('readings').delete().eq('id', reading.id).eq('therapist_id', user.id)
  if (reserve.reason === 'no_balance') {
    return {
      ok: false,
      error: 'Sem saldo. Compre um pacote pra continuar criando links de convite.',
      action: { label: 'Comprar', href: '/assinatura/comprar' },
    }
  }
  return { ok: false, error: 'Erro interno.' }
}

// BLOCKER-2 fix: emite o mesmo audit event que createReadingAction quando founder/admin usa internal_use.
// Sem isso, invite-link creations do founder ficam invisíveis pra analytics exclusion.
// Mantém shape simétrico com task 2 (createReadingAction).
if (reserve.source === 'internal') {
  await logAuditEvent({
    event_type: 'admin.internal_use_used',
    actor_user_id: user.id,
    actor_email: user.email,
    target_type: 'reading',
    target_id: reading.id,
    metadata: { entry_point: 'invite_token', client_id: parsed.data.client_id },
  })
}

// Insert token referenciando reading.id (used_by_reading_id já existia per memory project_invite_resume_pre_binding)
const { data: token } = await service.from('client_invite_tokens').insert({
  client_id: parsed.data.client_id,
  therapist_id: user.id,
  used_by_reading_id: reading.id,  // pre-binding
  expires_at: ...,
}).select('token').single()
```

**Importante:** O fluxo existente do invite pode já criar reading internamente. Inspecionar o código primeiro e ADAPTAR — não duplicar. Se reading já é criada via outra ação, mover reserve pra ali.

Se a inspeção revelar que invite tokens NÃO criam reading na ação (a reading só é criada quando cliente acessa o link), então a reservação acontece quando o cliente captura — não aqui. Nesse caso este task NÃO modifica invites.ts (skip), e o gate vai aparecer no startCaptureSessionAction (D-10 #2). Documentar a decisão.

**Decisão obrigatória do implementor:** ler o código real, decidir ponto exato do gate. Documentar no SUMMARY.
  </action>
  <verify>
    <automated>cd D:/Projetos/Iridologista/apps/web && pnpm vitest run app/actions/__tests__/invites.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>Tests verdes; lógica documentada — ou gate aqui, ou gate em outro ponto explicado.</done>
</task>

<task type="auto">
  <name>Task 4: analyze route — convertReservationToConsume após success</name>
  <files>apps/web/app/api/readings/[id]/analyze/route.ts</files>
  <action>
**Read apps/web/app/api/readings/[id]/analyze/route.ts:84-100** — hook point identificado em PATTERNS.

Após report_generations INSERT success + antes do return final, chamar `convertReservationToConsume(reading_id)`:

```typescript
import { convertReservationToConsume } from '@/lib/billing/credits'

// ... após o INSERT report_generations bem-sucedido ...

// Fase 8 D-11: converte reservation 'active' → 'converted' (debita firmemente do saldo).
// Idempotent — se reservation já foi convertida (regenerate, race), no-op.
const consume = await convertReservationToConsume(readingId)
if (!consume.ok && consume.reason !== 'not_found') {
  // not_found acontece em regenerate ou em internal_use users (reservation pode não existir)
  // Outros erros: log mas não bloqueia entrega do relatório
  console.warn(`[analyze] consume failed reading=${readingId}:`, consume.reason)
}
// Audit já é emitido por convertReservationToConsume — não duplicar
```

**Hook point exato:** após o último `await ... INSERT into report_generations` e antes do `return new Response/NextResponse`.

NÃO bloquear delivery do relatório por erro de consume — o ledger é defensivo, o produto é o relatório. Memory `feedback_classify_change_impact_honestly`: falha de consume não é falha de produto pro cliente.

Verificar via grep que existing analyze route não tem outro lugar marcando "usage" que precise ser limpado.
  </action>
  <verify>
    <automated>cd D:/Projetos/Iridologista/apps/web && pnpm vitest run app/api/readings/__tests__ 2>&1 | tail -10 && grep -c "convertReservationToConsume" app/api/readings/\[id\]/analyze/route.ts</automated>
  </verify>
  <done>≥1 ocorrência de convertReservationToConsume na rota; testes existentes verdes (ou pre-existing failures isolados).</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07-01 | Tampering | terapeuta cria reading SEM gate de saldo via API direta | mitigate | Gate vive em createReadingAction (server action) + analyze route consume — ambos chamados via session-gated APIs. Direct DB INSERT (não exposto pro cliente) seria via service-role, sem caminho público |
| T-08-07-02 | Information Disclosure | erro "no_balance" expõe estrutura interna | accept | Msg é product-grade ("compre um pacote"), não técnica |
| T-08-07-03 | Repudiation | invite cria reading + reserva sem cliente terminar fluxo | mitigate | Reservation expira em 7d via cron (plano 08-13); credit volta pro saldo. Sem perda |
| T-08-07-04 | Tampering | rollback de reading falha → DB poluído | accept | Soft atomicidade — race window pequeno. Cron de cleanup pode marcar readings órfãs futuro |
</threat_model>

<verification>
- 5 testes billing-gate green
- pnpm tsc --noEmit limpo
- 3 arquivos existentes modificados sem quebrar tests pré-existentes (catch pre-existing per memory)
- grep confirma reserveCreditForReading em readings.ts + (invites.ts OR documented elsewhere)
- grep confirma convertReservationToConsume em analyze route
</verification>

<success_criteria>
- Gates aplicados nos 3 fluxos D-10
- Lib gate composition pattern estabelecido
- Logs estruturados pra debug
- Sem regressões em tests existentes
</success_criteria>

<output>
Após completion, criar `.planning/phases/08-pagamento-lgpd/08-07-SUMMARY.md`:
- Onde cada gate vive (createReadingAction, createInviteTokenAction OR startCaptureSessionAction, analyze route)
- Justificativa se invite gate ficou em outro ponto que não invites.ts
- evaluateBilling pure helper disponível pra UI
</output>
