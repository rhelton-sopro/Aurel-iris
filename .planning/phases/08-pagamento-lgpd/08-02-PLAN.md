---
phase: 08-pagamento-lgpd
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/lib/asaas/client.ts
  - apps/web/lib/asaas/webhook-auth.ts
  - apps/web/lib/asaas/types.ts
  - apps/web/lib/asaas/idempotency.ts
  - apps/web/lib/asaas/__tests__/client.test.ts
  - apps/web/lib/asaas/__tests__/webhook-auth.test.ts
autonomous: true
requirements: [BILLING-01]
user_setup:
  - service: asaas
    why: "Pagamento avulso BR (PIX/cartão/boleto) + NF automática + webhook contract"
    env_vars:
      - name: ASAAS_API_KEY
        source: "Asaas Dashboard → Integrações → API"
      - name: ASAAS_API_BASE_URL
        source: "Constante: https://api.asaas.com/v3 (prod) ou https://api-sandbox.asaas.com/v3 (sandbox)"
      - name: ASAAS_WEBHOOK_TOKEN
        source: "String aleatória 32-64 chars (gerar com openssl rand -hex 32); configurar IGUAL no Asaas Dashboard → Integrações → Webhook"
    account_setup:
      - "Criar conta sandbox em https://sandbox.asaas.com pra testes E2E (5min)"
      - "Conta production: CNPJ founder já ativo confirmado em D-01"
    dashboard_config:
      - task: "Configurar webhook endpoint com ASAAS_WEBHOOK_TOKEN matching env var"
        location: "Asaas Dashboard → Integrações → Webhook → URL: https://iriscodex.com/api/asaas/webhook"
        when: "Após plano 08-04 deploy LIVE"
must_haves:
  truths:
    - "createAsaasCustomer(input) faz POST /v3/customers e retorna { ok, data: { id, ... } }"
    - "createAsaasPayment(input) faz POST /v3/payments e retorna { ok, data: { id, invoiceUrl, ... } }"
    - "refundAsaasPayment(id, body?) faz POST /v3/payments/{id}/refund — body sem `value` = refund total; com value = parcial"
    - "verifyAsaasToken(provided, expected) usa timingSafeEqual; retorna discriminated union"
    - "Zod schema asaasWebhookEnvelopeSchema valida payload Asaas via passthrough() (não rejeita campos extras)"
    - "Testes unitários green pra webhook-auth (401 cases) e client (mock fetch responses)"
  artifacts:
    - path: "apps/web/lib/asaas/client.ts"
      provides: "REST client tipado (3 funções export)"
      exports: ["createAsaasCustomer", "createAsaasPayment", "refundAsaasPayment"]
    - path: "apps/web/lib/asaas/webhook-auth.ts"
      provides: "Timing-safe shared-secret comparison"
      exports: ["verifyAsaasToken"]
    - path: "apps/web/lib/asaas/types.ts"
      provides: "Zod schemas + TS types pro envelope webhook + payment object"
      exports: ["asaasWebhookEnvelopeSchema", "asaasPaymentSchema", "AsaasWebhookEnvelope", "AsaasPayment"]
    - path: "apps/web/lib/asaas/idempotency.ts"
      provides: "Helper pra INSERT em asaas_webhook_events com retorno 23505=duplicate"
      exports: ["recordWebhookEvent"]
  key_links:
    - from: "lib/asaas/client.ts"
      to: "process.env.ASAAS_API_KEY + ASAAS_API_BASE_URL"
      via: "fetch direto, sem SDK"
      pattern: "access_token: apiKey"
    - from: "lib/asaas/webhook-auth.ts"
      to: "node:crypto.timingSafeEqual"
      via: "Buffer.from comparison"
      pattern: "timingSafeEqual"
---

<objective>
Criar a camada de cliente Asaas + autenticação webhook + schemas Zod + idempotency helper. Esta é a única dependência externa NOVA da Fase 8 (per RESEARCH §Standard Stack — não instalar SDK comunitário).

Purpose: Isolar a integração Asaas em `lib/asaas/` pra que server actions (plano 08-06) e webhook handler (plano 08-04) consumam interface tipada + testada, sem espalhar `fetch('https://api.asaas.com...')` pelo codebase.
Output: Módulo lib/asaas/ + 2 test files green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-pagamento-lgpd/08-CONTEXT.md
@.planning/phases/08-pagamento-lgpd/08-RESEARCH.md
@.planning/phases/08-pagamento-lgpd/08-PATTERNS.md
@apps/web/lib/vision/hmac.ts
@apps/web/app/api/vision/webhook/route.ts
@apps/web/lib/notifications/notify-therapist-capture-complete.ts

<interfaces>
<!-- Extracted from existing codebase patterns. Use verbatim where indicated. -->

From apps/web/lib/vision/hmac.ts (discriminated union pattern):
```typescript
export type HmacVerifyResult =
  | { valid: true }
  | { valid: false; reason: 'missing_signature' | 'invalid_signature' | 'misconfigured' }
```
Adapt for Asaas → `reason: 'missing_token' | 'invalid_token' | 'misconfigured'`.

From apps/web/lib/notifications/notify-therapist-capture-complete.ts (fetch + structured log + non-fatal catch):
```typescript
try {
  const res = await fetch(URL, { method, headers, body })
  if (!res.ok) { console.error(`[scope] ${path} HTTP ${res.status}`); return { ok: false, ... } }
  return { ok: true, data: await res.json() }
} catch (err) {
  console.error('[scope] fetch failed:', err)
  return { ok: false, status: 0, error: 'network' }
}
```

From apps/web/app/api/vision/webhook/route.ts:64-79 (zod envelope with passthrough):
```typescript
const envelopeSchema = z.object({ ... }).passthrough() // don't reject Asaas extras
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/asaas/types.ts — Zod schemas + TS types</name>
  <files>apps/web/lib/asaas/types.ts</files>
  <behavior>
    - asaasPaymentSchema aceita payload Asaas real (RESEARCH lines 148-162) com .passthrough()
    - asaasWebhookEnvelopeSchema valida { id, event, dateCreated, payment } com event enum dos 8 PAYMENT_* events
    - Tipos AsaasPayment + AsaasWebhookEnvelope inferidos via z.infer
    - Schema rejeita event ausente, event não-enum, payment.id vazio
  </behavior>
  <action>
Criar `apps/web/lib/asaas/types.ts` com Zod schemas seguindo PATTERNS lines 372-405 verbatim:

```typescript
import { z } from 'zod'

export const asaasPaymentSchema = z.object({
  id: z.string().min(1),
  customer: z.string().min(1),
  value: z.number().positive(),
  netValue: z.number().optional(),
  billingType: z.enum(['UNDEFINED', 'PIX', 'BOLETO', 'CREDIT_CARD']),
  status: z.string().min(1),
  externalReference: z.string().nullable().optional(),
  paymentDate: z.string().nullable().optional(),
  clientPaymentDate: z.string().nullable().optional(),
  invoiceUrl: z.string().url().nullable().optional(),
  bankSlipUrl: z.string().url().nullable().optional(),
}).passthrough()

export const asaasWebhookEnvelopeSchema = z.object({
  id: z.string().min(1),  // event.id — idempotency key (RESEARCH §Idempotency)
  event: z.enum([
    'PAYMENT_CREATED',
    'PAYMENT_CONFIRMED',
    'PAYMENT_RECEIVED',
    'PAYMENT_OVERDUE',
    'PAYMENT_DELETED',
    'PAYMENT_REFUNDED',
    'PAYMENT_PARTIALLY_REFUNDED',
    'PAYMENT_CHARGEBACK_REQUESTED',
  ]),
  dateCreated: z.string().min(1),
  payment: asaasPaymentSchema,
})

export type AsaasPayment = z.infer<typeof asaasPaymentSchema>
export type AsaasWebhookEnvelope = z.infer<typeof asaasWebhookEnvelopeSchema>

// Customer create response (POST /v3/customers)
export const asaasCustomerSchema = z.object({
  id: z.string().min(1),
  name: z.string(),
  cpfCnpj: z.string(),
  email: z.string(),
  dateCreated: z.string(),
}).passthrough()
export type AsaasCustomer = z.infer<typeof asaasCustomerSchema>
```

NÃO incluir `'use server'` — este arquivo é tipos/schemas puros importáveis client+server.
  </action>
  <verify>
    <automated>cd D:/Projetos/Iridologista/apps/web && pnpm tsc --noEmit -p tsconfig.json 2>&1 | grep -E "(lib/asaas/types|error)" | head -5</automated>
  </verify>
  <done>Sem erros TS no arquivo; export types funcionam (verificado por dummy import em outro arquivo gera 0 erros).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lib/asaas/webhook-auth.ts + test</name>
  <files>apps/web/lib/asaas/webhook-auth.ts, apps/web/lib/asaas/__tests__/webhook-auth.test.ts</files>
  <behavior>
    - verifyAsaasToken(undefined, secret) → { valid: false, reason: 'missing_token' }
    - verifyAsaasToken(token, undefined) → { valid: false, reason: 'misconfigured' }
    - verifyAsaasToken('wrong', 'secret') → { valid: false, reason: 'invalid_token' }
    - verifyAsaasToken('secret', 'secret') → { valid: true }
    - Diferença de comprimento NÃO crasha (timing-safe handles via early length check + Buffer.from same-length)
    - Uses timingSafeEqual (not ===)
  </behavior>
  <action>
**Arquivo principal** — PATTERNS lines 217-235 verbatim:

```typescript
import 'server-only'
import { timingSafeEqual } from 'node:crypto'

export type AsaasWebhookAuthResult =
  | { valid: true }
  | { valid: false; reason: 'missing_token' | 'invalid_token' | 'misconfigured' }

export function verifyAsaasToken(
  providedHeader: string | null | undefined,
  expectedSecret: string | undefined,
): AsaasWebhookAuthResult {
  if (!expectedSecret) return { valid: false, reason: 'misconfigured' }
  if (!providedHeader) return { valid: false, reason: 'missing_token' }

  const a = Buffer.from(providedHeader)
  const b = Buffer.from(expectedSecret)
  // length mismatch: not timing-safe pra comprimento mas é OK (RESEARCH: Asaas exige 32-255 chars sem ambiguidade)
  if (a.length !== b.length) return { valid: false, reason: 'invalid_token' }
  if (!timingSafeEqual(a, b)) return { valid: false, reason: 'invalid_token' }
  return { valid: true }
}
```

**Por que NÃO reusar lib/vision/hmac.ts** (per RESEARCH §HMAC line 279): Asaas usa shared secret simples (não HMAC do body); contratos diferentes → arquivo separado.

**Test file:**

```typescript
import { describe, expect, it } from 'vitest'
import { verifyAsaasToken } from '../webhook-auth'

describe('verifyAsaasToken', () => {
  it('returns misconfigured when secret env missing', () => {
    expect(verifyAsaasToken('abc', undefined)).toEqual({ valid: false, reason: 'misconfigured' })
  })
  it('returns missing_token when header absent', () => {
    expect(verifyAsaasToken(null, 'secret')).toEqual({ valid: false, reason: 'missing_token' })
    expect(verifyAsaasToken(undefined, 'secret')).toEqual({ valid: false, reason: 'missing_token' })
  })
  it('returns invalid_token on mismatch', () => {
    expect(verifyAsaasToken('wrong', 'secret')).toEqual({ valid: false, reason: 'invalid_token' })
  })
  it('returns invalid_token on length mismatch', () => {
    expect(verifyAsaasToken('short', 'much-longer-secret')).toEqual({ valid: false, reason: 'invalid_token' })
  })
  it('returns valid on exact match', () => {
    expect(verifyAsaasToken('secret123', 'secret123')).toEqual({ valid: true })
  })
})
```
  </action>
  <verify>
    <automated>cd D:/Projetos/Iridologista/apps/web && pnpm vitest run lib/asaas/__tests__/webhook-auth.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>5 testes green; timingSafeEqual usado (grep confirma).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: lib/asaas/client.ts + test</name>
  <files>apps/web/lib/asaas/client.ts, apps/web/lib/asaas/__tests__/client.test.ts</files>
  <behavior>
    - createAsaasCustomer(input) chama POST /v3/customers com header access_token
    - createAsaasPayment(input) chama POST /v3/payments com body validado
    - refundAsaasPayment(id, body?) chama POST /v3/payments/{id}/refund — body opcional
    - Quando API key ausente → { ok: false, status: 500, error: 'ASAAS_API_KEY missing' }
    - Quando HTTP 4xx/5xx → { ok: false, status, error: response_text }
    - Quando network error → { ok: false, status: 0, error: 'network' }
    - Sucesso → { ok: true, data: parsed_response }
  </behavior>
  <action>
**Arquivo principal** — PATTERNS lines 327-367 + RESEARCH §Asaas API endpoints:

```typescript
import 'server-only'
import {
  asaasCustomerSchema,
  asaasPaymentSchema,
  type AsaasCustomer,
  type AsaasPayment,
} from './types'

const ASAAS_API_BASE_URL = process.env.ASAAS_API_BASE_URL ?? 'https://api.asaas.com/v3'

export type AsaasResult<T> =
  | { ok: true; data: T }
  | { ok: false; status: number; error: string }

async function asaasRequest<T>(
  path: string,
  init: RequestInit,
): Promise<AsaasResult<unknown>> {
  const apiKey = process.env.ASAAS_API_KEY
  if (!apiKey) {
    console.error('[asaas] ASAAS_API_KEY missing')
    return { ok: false, status: 500, error: 'ASAAS_API_KEY missing' }
  }
  try {
    const res = await fetch(`${ASAAS_API_BASE_URL}${path}`, {
      ...init,
      headers: {
        ...(init.headers ?? {}),
        access_token: apiKey,
        'Content-Type': 'application/json',
      },
    })
    if (!res.ok) {
      const detail = await res.text().catch(() => '')
      console.error(`[asaas] ${init.method ?? 'GET'} ${path} HTTP ${res.status} — ${detail.slice(0, 300)}`)
      return { ok: false, status: res.status, error: detail.slice(0, 300) }
    }
    const data = await res.json()
    return { ok: true, data }
  } catch (err) {
    console.error(`[asaas] ${path} fetch failed:`, err instanceof Error ? err.message : err)
    return { ok: false, status: 0, error: 'network' }
  }
}

export interface CreateCustomerInput {
  name: string
  cpfCnpj: string  // só dígitos
  email: string
  mobilePhone: string
  externalReference: string  // profiles.id UUID
}

export async function createAsaasCustomer(
  input: CreateCustomerInput,
): Promise<AsaasResult<AsaasCustomer>> {
  const r = await asaasRequest('/customers', { method: 'POST', body: JSON.stringify(input) })
  if (!r.ok) return r
  const parsed = asaasCustomerSchema.safeParse(r.data)
  if (!parsed.success) return { ok: false, status: 502, error: 'asaas response shape invalid' }
  return { ok: true, data: parsed.data }
}

export interface CreatePaymentInput {
  customer: string  // cus_xxxxx
  billingType: 'UNDEFINED' | 'PIX' | 'BOLETO' | 'CREDIT_CARD'
  value: number  // R$ decimal (não centavos)
  dueDate: string  // ISO YYYY-MM-DD
  description: string
  externalReference: string  // customer_credits.id UUID (per pitfall #5 — UNIQUE app-level)
}

export async function createAsaasPayment(
  input: CreatePaymentInput,
): Promise<AsaasResult<AsaasPayment>> {
  const r = await asaasRequest('/payments', { method: 'POST', body: JSON.stringify(input) })
  if (!r.ok) return r
  const parsed = asaasPaymentSchema.safeParse(r.data)
  if (!parsed.success) return { ok: false, status: 502, error: 'asaas response shape invalid' }
  return { ok: true, data: parsed.data }
}

export interface RefundInput {
  value?: number  // omit = refund total (D-13)
  description?: string
}

export async function refundAsaasPayment(
  paymentId: string,
  body?: RefundInput,
): Promise<AsaasResult<AsaasPayment>> {
  const r = await asaasRequest(`/payments/${paymentId}/refund`, {
    method: 'POST',
    body: JSON.stringify(body ?? {}),
  })
  if (!r.ok) return r
  const parsed = asaasPaymentSchema.safeParse(r.data)
  if (!parsed.success) return { ok: false, status: 502, error: 'asaas response shape invalid' }
  return { ok: true, data: parsed.data }
}
```

> Per RESEARCH `## Don't Hand-Roll` + memory `feedback_auto_block_expensive_branches`: NÃO instalar npm `asaas` package.

**Test file** — mock fetch via vitest:

```typescript
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest'
import { createAsaasCustomer, createAsaasPayment, refundAsaasPayment } from '../client'

const fetchMock = vi.fn()
global.fetch = fetchMock as unknown as typeof fetch

describe('asaas client', () => {
  beforeEach(() => {
    fetchMock.mockReset()
    process.env.ASAAS_API_KEY = '$aact_test_key'
    process.env.ASAAS_API_BASE_URL = 'https://api-sandbox.asaas.com/v3'
  })

  it('createAsaasCustomer success', async () => {
    fetchMock.mockResolvedValue(new Response(JSON.stringify({
      id: 'cus_123', name: 'X', cpfCnpj: '12345678901', email: 'x@y.com', dateCreated: '2026-05-27',
    }), { status: 200, headers: { 'content-type': 'application/json' } }))

    const r = await createAsaasCustomer({
      name: 'X', cpfCnpj: '12345678901', email: 'x@y.com', mobilePhone: '47999999999',
      externalReference: 'aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa',
    })
    expect(r.ok).toBe(true)
    if (r.ok) expect(r.data.id).toBe('cus_123')
    expect(fetchMock).toHaveBeenCalledWith(
      'https://api-sandbox.asaas.com/v3/customers',
      expect.objectContaining({ headers: expect.objectContaining({ access_token: '$aact_test_key' }) }),
    )
  })

  it('returns ok:false when API key missing', async () => {
    delete process.env.ASAAS_API_KEY
    const r = await createAsaasCustomer({ name: 'X', cpfCnpj: '1', email: 'x@y.com', mobilePhone: '1', externalReference: 'aaaa' })
    expect(r.ok).toBe(false)
    if (!r.ok) expect(r.error).toContain('ASAAS_API_KEY missing')
  })

  it('returns ok:false on HTTP 4xx', async () => {
    fetchMock.mockResolvedValue(new Response('Bad Request', { status: 400 }))
    const r = await createAsaasPayment({
      customer: 'cus_x', billingType: 'PIX', value: 99.70, dueDate: '2026-06-01',
      description: 'test', externalReference: 'aaaa',
    })
    expect(r.ok).toBe(false)
    if (!r.ok) expect(r.status).toBe(400)
  })

  it('returns ok:false on network error', async () => {
    fetchMock.mockRejectedValue(new Error('ECONNREFUSED'))
    const r = await createAsaasPayment({
      customer: 'cus_x', billingType: 'PIX', value: 99.70, dueDate: '2026-06-01',
      description: 'test', externalReference: 'aaaa',
    })
    expect(r.ok).toBe(false)
    if (!r.ok) { expect(r.status).toBe(0); expect(r.error).toBe('network') }
  })

  it('refundAsaasPayment without value = full refund body {}', async () => {
    fetchMock.mockResolvedValue(new Response(JSON.stringify({
      id: 'pay_x', customer: 'cus_x', value: 99.70, billingType: 'PIX', status: 'REFUNDED',
    }), { status: 200 }))
    await refundAsaasPayment('pay_x')
    const [, init] = fetchMock.mock.calls[0]
    expect(init.body).toBe('{}')
  })

  it('refundAsaasPayment with value = partial', async () => {
    fetchMock.mockResolvedValue(new Response(JSON.stringify({
      id: 'pay_x', customer: 'cus_x', value: 99.70, billingType: 'PIX', status: 'REFUNDED',
    }), { status: 200 }))
    await refundAsaasPayment('pay_x', { value: 59.70, description: 'partial' })
    const [, init] = fetchMock.mock.calls[0]
    expect(JSON.parse(init.body as string)).toEqual({ value: 59.70, description: 'partial' })
  })
})
```
  </action>
  <verify>
    <automated>cd D:/Projetos/Iridologista/apps/web && pnpm vitest run lib/asaas/__tests__/client.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>6 testes green; fetch é chamado com header `access_token` + Content-Type.</done>
</task>

<task type="auto">
  <name>Task 4: lib/asaas/idempotency.ts — recordWebhookEvent helper</name>
  <files>apps/web/lib/asaas/idempotency.ts</files>
  <action>
Helper que isola a lógica de INSERT em `asaas_webhook_events` com handling de 23505 (duplicate event_id PK).

PATTERNS line 30-31: similar a `lib/invite/tokens.ts:markTokenUsed` idiom de idempotent guard via WHERE clause/PK conflict.

```typescript
import 'server-only'
import type { AsaasWebhookEnvelope } from './types'
import { createServiceClient } from '@/lib/supabase/service'

export type RecordEventResult =
  | { ok: true; first_seen: true }
  | { ok: true; first_seen: false }  // duplicate event_id = idempotent skip
  | { ok: false; error: string }

export async function recordWebhookEvent(
  envelope: AsaasWebhookEnvelope,
): Promise<RecordEventResult> {
  const service = createServiceClient()
  const { error } = await service.from('asaas_webhook_events').insert({
    event_id: envelope.id,
    event_type: envelope.event,
    payment_id: envelope.payment.id,
    payload: envelope,
  })
  if (!error) return { ok: true, first_seen: true }
  // PG 23505 = unique_violation; event_id PK already exists = duplicate webhook
  if ((error as { code?: string }).code === '23505') {
    return { ok: true, first_seen: false }
  }
  console.error('[asaas-idempotency] insert failed:', error.message)
  return { ok: false, error: error.message }
}

export async function markEventProcessed(eventId: string): Promise<void> {
  const service = createServiceClient()
  await service.from('asaas_webhook_events')
    .update({ processed_at: new Date().toISOString(), status: 'processed' })
    .eq('event_id', eventId)
}
```

Test file omitted nesta task — integration test do webhook handler em plano 08-04 cobre o caminho real.
  </action>
  <verify>
    <automated>cd D:/Projetos/Iridologista/apps/web && pnpm tsc --noEmit 2>&1 | grep -E "(lib/asaas/idempotency|error)" | head -5</automated>
  </verify>
  <done>Sem erros TS; export types funcionam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Iris Codex → Asaas API | Outbound REST com API key no header; HTTPS only |
| Asaas → Iris Codex webhook | Inbound; shared secret no header `asaas-access-token` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Spoofing | Webhook auth | mitigate | timingSafeEqual em verifyAsaasToken (não ===); pattern hmac.ts adaptado |
| T-08-02-02 | Information Disclosure | API key leak via logs | mitigate | structured log NÃO inclui apiKey; só path + status (PATTERNS line 354) |
| T-08-02-03 | Tampering | Webhook payload mutation | accept | Asaas usa shared secret no header, NÃO HMAC do body; trade-off conhecido (RESEARCH §HMAC). Mitigação: schema Zod strict valida shape |
| T-08-02-04 | Information Disclosure | API key em git | mitigate | env var only; .env.example documenta variável sem valor real |
| T-08-02-05 | Repudiation | Webhook replay | mitigate | event_id PK em asaas_webhook_events → recordWebhookEvent retorna first_seen:false na duplicate (plano 08-04 usa pra retornar 200 no-op) |
</threat_model>

<verification>
- 5+6=11 testes vitest green em lib/asaas/__tests__/
- pnpm tsc --noEmit limpo
- Sem dependência npm nova adicionada (verificar package.json diff = 0)
- timingSafeEqual presente em webhook-auth.ts
</verification>

<success_criteria>
- 4 arquivos lib/asaas/* criados
- 2 test files green
- Zero deps npm novas
- types/database.ts NÃO precisa update (este plano não toca DB)
</success_criteria>

<output>
Após completion, criar `.planning/phases/08-pagamento-lgpd/08-02-SUMMARY.md`:
- Files criados (4 + 2 testes)
- Funções exportadas (createAsaasCustomer, createAsaasPayment, refundAsaasPayment, verifyAsaasToken, recordWebhookEvent, markEventProcessed)
- Env vars novas declaradas (ASAAS_API_KEY, ASAAS_API_BASE_URL, ASAAS_WEBHOOK_TOKEN) — founder configura no Vercel
</output>
