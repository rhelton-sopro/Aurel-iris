---
phase: 08-pagamento-lgpd
plan: 12
type: execute
wave: 5
depends_on: [08-04, 08-05]
files_modified:
  - apps/web/lib/notifications/notify-credit-purchase-confirmed.ts
  - apps/web/lib/notifications/notify-credit-expiring.ts
  - apps/web/lib/notifications/notify-refund-processed.ts
  - apps/web/lib/notifications/__tests__/notify-credit-purchase-confirmed.test.ts
  - apps/web/lib/billing/apply-payment.ts
autonomous: true
requirements: [BILLING-01]
must_haves:
  truths:
    - "notifyCreditPurchaseConfirmed(creditId) chamado pelo applyPaymentEvent após state transition success"
    - "Email tem from=noreply@iriscodex.com (per memory project_resend_domain_unverified_launch_gate)"
    - "Email é best-effort — falha NÃO bloqueia webhook return 200"
    - "notifyCreditExpiring(creditId, daysOut: 30|7|0) chamado pelo cron daily (próximo plano 13)"
    - "notifyRefundProcessed(creditId, refund_value, kind) chamado por refundPackageAction success path"
    - "Templates pt-BR; HTML + text fallback"
  artifacts:
    - path: "apps/web/lib/notifications/notify-credit-purchase-confirmed.ts"
      provides: "Email confirmação de compra"
      exports: ["notifyCreditPurchaseConfirmed"]
    - path: "apps/web/lib/notifications/notify-credit-expiring.ts"
      provides: "Email aviso de expiração (30d/7d/0d)"
      exports: ["notifyCreditExpiring"]
    - path: "apps/web/lib/notifications/notify-refund-processed.ts"
      provides: "Email confirmação de reembolso"
      exports: ["notifyRefundProcessed"]
  key_links:
    - from: "notify-* helpers"
      to: "Resend API"
      via: "fetch POST best-effort"
      pattern: "api.resend.com/emails"
    - from: "applyPaymentEvent activated branch"
      to: "notifyCreditPurchaseConfirmed"
      via: "fire-and-forget após state transition"
      pattern: "notifyCreditPurchaseConfirmed"
---

<objective>
3 emails transacionais via Resend + wire confirmação no webhook handler.

3 emails:
1. **Compra confirmada** — disparado por `applyPaymentEvent` quando state transition success
2. **Expiração próxima** — 30d/7d/no dia, disparado pelo cron daily (plano 08-13 vai chamar)
3. **Reembolso processado** — disparado por `refundPackageAction` success

Purpose: Transacional UX. Terapeuta paga → recebe confirmação email com créditos creditados. Pacote expirando → aviso. Refund → recibo.
Output: 3 source + 1 test + 1 wire em apply-payment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-pagamento-lgpd/08-CONTEXT.md
@.planning/phases/08-pagamento-lgpd/08-RESEARCH.md
@.planning/phases/08-pagamento-lgpd/08-PATTERNS.md
@.planning/phases/08-pagamento-lgpd/08-04-SUMMARY.md
@apps/web/lib/notifications/notify-therapist-capture-complete.ts

<interfaces>
<!-- Pattern from PATTERNS lines 919-934 + existing notify-therapist-capture-complete.ts -->
- RESEND_API_URL = 'https://api.resend.com/emails'
- DEFAULT_FROM = 'Iris Codex <noreply@iriscodex.com>'  // memory: resend domain verified
- API key fail → console.log + return
- HTTP fail → console.error + return
- Wrap in try/catch
- Always escape user-controlled content via escapeHtml()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: notify-credit-purchase-confirmed.ts + test</name>
  <files>apps/web/lib/notifications/notify-credit-purchase-confirmed.ts, apps/web/lib/notifications/__tests__/notify-credit-purchase-confirmed.test.ts</files>
  <behavior>
    - notifyCreditPurchaseConfirmed({ creditId, userEmail, userName, packageName, leituras, valueBrl, expiresAt }) → fetch Resend API com from + to + subject + html + text
    - Quando RESEND_API_KEY ausente → log + return (no throw)
    - Quando HTTP fail → log + return
    - Best-effort — never throws
    - Subject: "Iris Codex — sua compra foi confirmada"
  </behavior>
  <action>
**Arquivo principal:**

```typescript
import 'server-only'

const RESEND_API_URL = 'https://api.resend.com/emails'
const DEFAULT_FROM = 'Iris Codex <noreply@iriscodex.com>'

export interface NotifyPurchaseInput {
  userEmail: string
  userName?: string | null
  packageName: string
  leituras: number
  valueBrl: number
  expiresAt: string  // ISO
}

export async function notifyCreditPurchaseConfirmed(input: NotifyPurchaseInput): Promise<void> {
  const apiKey = process.env.RESEND_API_KEY
  if (!apiKey) { console.log('[notify-purchase] RESEND_API_KEY ausente — pulando'); return }

  const fromEmail = process.env.RESEND_FROM_EMAIL ?? DEFAULT_FROM
  const greeting = input.userName ? `Olá, ${escapeHtml(input.userName)}` : 'Olá'
  const expiresAtBR = new Date(input.expiresAt).toLocaleDateString('pt-BR', { timeZone: 'America/Sao_Paulo' })

  const subject = 'Iris Codex — sua compra foi confirmada'
  const text = `${greeting},

Sua compra foi confirmada com sucesso!

Pacote: ${input.packageName}
Leituras: ${input.leituras}
Valor: R$ ${input.valueBrl.toFixed(2).replace('.', ',')}
Validade: ${expiresAtBR}

Acesse seu painel em https://iriscodex.com/assinatura para começar a usar.

Obrigado,
Iris Codex`

  const html = `<!doctype html><html><body style="font-family: Georgia, serif; line-height: 1.55; color: #1a1a1a; max-width: 560px; margin: 0 auto; padding: 24px;">
<h2 style="color: #1a1a1a; margin-top: 0;">${greeting},</h2>
<p>Sua compra foi <strong>confirmada com sucesso</strong>.</p>
<table style="border-collapse: collapse; margin: 16px 0; width: 100%;">
  <tr><td style="padding: 6px 0; color: #666;">Pacote</td><td style="padding: 6px 0;"><strong>${escapeHtml(input.packageName)}</strong></td></tr>
  <tr><td style="padding: 6px 0; color: #666;">Leituras</td><td style="padding: 6px 0;"><strong>${input.leituras}</strong></td></tr>
  <tr><td style="padding: 6px 0; color: #666;">Valor</td><td style="padding: 6px 0;"><strong>R$ ${input.valueBrl.toFixed(2).replace('.', ',')}</strong></td></tr>
  <tr><td style="padding: 6px 0; color: #666;">Validade</td><td style="padding: 6px 0;"><strong>${expiresAtBR}</strong></td></tr>
</table>
<p><a href="https://iriscodex.com/assinatura" style="display: inline-block; background: #1a4744; color: white; padding: 10px 18px; border-radius: 6px; text-decoration: none;">Ir pro painel</a></p>
<p style="margin-top: 24px; font-size: 12px; color: #666;">Iris Codex é ferramenta de apoio à anamnese terapêutica integrativa. Não substitui avaliação médica.</p>
</body></html>`

  try {
    const res = await fetch(RESEND_API_URL, {
      method: 'POST',
      headers: { Authorization: `Bearer ${apiKey}`, 'Content-Type': 'application/json' },
      body: JSON.stringify({ from: fromEmail, to: input.userEmail, subject, text, html }),
    })
    if (!res.ok) {
      const detail = await res.text().catch(() => '')
      console.error(`[notify-purchase] resend HTTP ${res.status} — ${detail.slice(0, 200)}`)
    } else {
      console.info(`[notify-purchase] sent to=${input.userEmail} pkg=${input.packageName}`)
    }
  } catch (err) {
    console.error('[notify-purchase] fetch failed:', err instanceof Error ? err.message : err)
  }
}

function escapeHtml(s: string): string {
  return s.replace(/[&<>"']/g, c => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]!))
}
```

**Test file:**

```typescript
import { beforeEach, describe, expect, it, vi } from 'vitest'

const fetchMock = vi.fn()
global.fetch = fetchMock as unknown as typeof fetch

import { notifyCreditPurchaseConfirmed } from '../notify-credit-purchase-confirmed'

describe('notifyCreditPurchaseConfirmed', () => {
  beforeEach(() => {
    fetchMock.mockReset()
    process.env.RESEND_API_KEY = 're_test'
  })

  it('does not throw when API key missing', async () => {
    delete process.env.RESEND_API_KEY
    await expect(notifyCreditPurchaseConfirmed({
      userEmail: 'x@y.com', packageName: 'Médio', leituras: 15, valueBrl: 745.50,
      expiresAt: '2027-05-27T00:00:00Z',
    })).resolves.toBeUndefined()
    expect(fetchMock).not.toHaveBeenCalled()
  })

  it('posts to Resend with correct payload', async () => {
    fetchMock.mockResolvedValue(new Response('{"id":"em_123"}', { status: 200 }))
    await notifyCreditPurchaseConfirmed({
      userEmail: 'x@y.com', userName: 'Ana', packageName: 'Médio',
      leituras: 15, valueBrl: 745.50, expiresAt: '2027-05-27T00:00:00Z',
    })
    expect(fetchMock).toHaveBeenCalledWith(
      'https://api.resend.com/emails',
      expect.objectContaining({
        method: 'POST',
        headers: expect.objectContaining({ Authorization: 'Bearer re_test' }),
      })
    )
    const body = JSON.parse(fetchMock.mock.calls[0][1].body as string)
    expect(body.to).toBe('x@y.com')
    expect(body.subject).toContain('confirmada')
    expect(body.html).toContain('Ana')
    expect(body.html).toContain('745,50')
  })

  it('does not throw on HTTP 5xx', async () => {
    fetchMock.mockResolvedValue(new Response('error', { status: 500 }))
    await expect(notifyCreditPurchaseConfirmed({
      userEmail: 'x@y.com', packageName: 'Pequeno', leituras: 5, valueBrl: 298.50,
      expiresAt: '2027-05-27T00:00:00Z',
    })).resolves.toBeUndefined()
  })

  it('does not throw on fetch rejection', async () => {
    fetchMock.mockRejectedValue(new Error('ECONNREFUSED'))
    await expect(notifyCreditPurchaseConfirmed({
      userEmail: 'x@y.com', packageName: 'Avulsa', leituras: 1, valueBrl: 99.70,
      expiresAt: '2027-05-27T00:00:00Z',
    })).resolves.toBeUndefined()
  })

  it('escapes HTML in userName', async () => {
    fetchMock.mockResolvedValue(new Response('{}', { status: 200 }))
    await notifyCreditPurchaseConfirmed({
      userEmail: 'x@y.com', userName: '<script>alert(1)</script>',
      packageName: 'Avulsa', leituras: 1, valueBrl: 99.70,
      expiresAt: '2027-05-27T00:00:00Z',
    })
    const body = JSON.parse(fetchMock.mock.calls[0][1].body as string)
    expect(body.html).not.toContain('<script>')
    expect(body.html).toContain('&lt;script&gt;')
  })
})
```
  </action>
  <verify>
    <automated>cd D:/Projetos/Iridologista/apps/web && pnpm vitest run lib/notifications/__tests__/notify-credit-purchase-confirmed.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>5 testes green; pattern best-effort confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: notify-credit-expiring.ts + notify-refund-processed.ts (siblings, same pattern)</name>
  <files>apps/web/lib/notifications/notify-credit-expiring.ts, apps/web/lib/notifications/notify-refund-processed.ts</files>
  <action>
**notify-credit-expiring.ts:**

```typescript
import 'server-only'

const RESEND_API_URL = 'https://api.resend.com/emails'
const DEFAULT_FROM = 'Iris Codex <noreply@iriscodex.com>'

export type ExpiryWindow = 30 | 7 | 0  // 30 days, 7 days, today

export interface NotifyExpiringInput {
  userEmail: string
  userName?: string | null
  packageName: string
  leituras_remaining: number
  expiresAt: string  // ISO
  daysOut: ExpiryWindow
}

const SUBJECT_MAP: Record<ExpiryWindow, string> = {
  30: 'Iris Codex — seu pacote expira em 30 dias',
  7: 'Iris Codex — seu pacote expira em 7 dias',
  0: 'Iris Codex — seu pacote expira HOJE',
}

const URGENCY_MAP: Record<ExpiryWindow, string> = {
  30: 'em 30 dias',
  7: 'em apenas 7 dias',
  0: 'HOJE',
}

export async function notifyCreditExpiring(input: NotifyExpiringInput): Promise<void> {
  const apiKey = process.env.RESEND_API_KEY
  if (!apiKey) { console.log('[notify-expiring] RESEND_API_KEY ausente — pulando'); return }

  const fromEmail = process.env.RESEND_FROM_EMAIL ?? DEFAULT_FROM
  const greeting = input.userName ? `Olá, ${escapeHtml(input.userName)}` : 'Olá'
  const expiresAtBR = new Date(input.expiresAt).toLocaleDateString('pt-BR', { timeZone: 'America/Sao_Paulo' })
  const urgency = URGENCY_MAP[input.daysOut]
  const subject = SUBJECT_MAP[input.daysOut]

  const text = `${greeting},

Seu pacote "${input.packageName}" tem ${input.leituras_remaining} ${input.leituras_remaining === 1 ? 'leitura' : 'leituras'} restantes e expira ${urgency} (${expiresAtBR}).

Após o vencimento, créditos não usados não são reembolsáveis (exceto avaliação caso-a-caso via suporte).

Acesse https://iriscodex.com/assinatura ou comece uma nova leitura agora.

Iris Codex`

  const html = `<!doctype html><html><body style="font-family: Georgia, serif; line-height: 1.55; color: #1a1a1a; max-width: 560px; margin: 0 auto; padding: 24px;">
<h2>${greeting},</h2>
<p>Seu pacote <strong>${escapeHtml(input.packageName)}</strong> tem <strong>${input.leituras_remaining} ${input.leituras_remaining === 1 ? 'leitura restante' : 'leituras restantes'}</strong> e expira <strong>${urgency}</strong> (${expiresAtBR}).</p>
<p style="color: #b45309; font-weight: 500;">Após o vencimento, créditos não usados não são reembolsáveis (exceto avaliação caso-a-caso via suporte).</p>
<p><a href="https://iriscodex.com/leituras/nova" style="display: inline-block; background: #1a4744; color: white; padding: 10px 18px; border-radius: 6px; text-decoration: none;">Começar uma leitura</a></p>
<p style="margin-top: 24px; font-size: 12px; color: #666;">Iris Codex — ferramenta de apoio à anamnese terapêutica integrativa.</p>
</body></html>`

  try {
    const res = await fetch(RESEND_API_URL, {
      method: 'POST',
      headers: { Authorization: `Bearer ${apiKey}`, 'Content-Type': 'application/json' },
      body: JSON.stringify({ from: fromEmail, to: input.userEmail, subject, text, html }),
    })
    if (!res.ok) console.error(`[notify-expiring] resend HTTP ${res.status}`)
    else console.info(`[notify-expiring] sent to=${input.userEmail} days=${input.daysOut}`)
  } catch (err) {
    console.error('[notify-expiring] fetch failed:', err)
  }
}

function escapeHtml(s: string): string {
  return s.replace(/[&<>"']/g, c => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]!))
}
```

**notify-refund-processed.ts:**

```typescript
import 'server-only'

const RESEND_API_URL = 'https://api.resend.com/emails'
const DEFAULT_FROM = 'Iris Codex <noreply@iriscodex.com>'

export interface NotifyRefundInput {
  userEmail: string
  userName?: string | null
  packageName: string
  refundValueBrl: number
  kind: 'total' | 'partial'
  leiturasRefunded: number
}

export async function notifyRefundProcessed(input: NotifyRefundInput): Promise<void> {
  const apiKey = process.env.RESEND_API_KEY
  if (!apiKey) return

  const fromEmail = process.env.RESEND_FROM_EMAIL ?? DEFAULT_FROM
  const greeting = input.userName ? `Olá, ${escapeHtml(input.userName)}` : 'Olá'
  const kindLabel = input.kind === 'total' ? 'integral' : 'proporcional'
  const subject = 'Iris Codex — reembolso processado'

  const text = `${greeting},

Seu pedido de reembolso (arrependimento 7d) foi processado.

Pacote: ${input.packageName}
Tipo: Reembolso ${kindLabel}
Leituras reembolsadas: ${input.leiturasRefunded}
Valor: R$ ${input.refundValueBrl.toFixed(2).replace('.', ',')}

O valor será creditado na sua conta original em até 5 dias úteis (PIX ≤ 1 dia útil; cartão até 5 dias úteis).

Iris Codex`

  const html = `<!doctype html><html><body style="font-family: Georgia, serif; line-height: 1.55; color: #1a1a1a; max-width: 560px; margin: 0 auto; padding: 24px;">
<h2>${greeting},</h2>
<p>Seu pedido de reembolso foi <strong>processado com sucesso</strong>.</p>
<table style="border-collapse: collapse; margin: 16px 0; width: 100%;">
  <tr><td style="padding: 6px 0; color: #666;">Pacote</td><td style="padding: 6px 0;"><strong>${escapeHtml(input.packageName)}</strong></td></tr>
  <tr><td style="padding: 6px 0; color: #666;">Tipo</td><td style="padding: 6px 0;"><strong>Reembolso ${kindLabel}</strong></td></tr>
  <tr><td style="padding: 6px 0; color: #666;">Leituras</td><td style="padding: 6px 0;"><strong>${input.leiturasRefunded}</strong></td></tr>
  <tr><td style="padding: 6px 0; color: #666;">Valor</td><td style="padding: 6px 0;"><strong>R$ ${input.refundValueBrl.toFixed(2).replace('.', ',')}</strong></td></tr>
</table>
<p>O valor será creditado na sua conta original em até <strong>5 dias úteis</strong> (PIX ≤ 1 dia útil; cartão até 5 dias úteis).</p>
<p style="margin-top: 24px; font-size: 12px; color: #666;">Iris Codex — ferramenta de apoio à anamnese terapêutica integrativa.</p>
</body></html>`

  try {
    const res = await fetch(RESEND_API_URL, {
      method: 'POST',
      headers: { Authorization: `Bearer ${apiKey}`, 'Content-Type': 'application/json' },
      body: JSON.stringify({ from: fromEmail, to: input.userEmail, subject, text, html }),
    })
    if (!res.ok) console.error(`[notify-refund] resend HTTP ${res.status}`)
    else console.info(`[notify-refund] sent to=${input.userEmail} kind=${input.kind}`)
  } catch (err) {
    console.error('[notify-refund] fetch failed:', err)
  }
}

function escapeHtml(s: string): string {
  return s.replace(/[&<>"']/g, c => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]!))
}
```

Tests dessas 2 functions omitted — padrão idêntico ao notify-credit-purchase-confirmed test; smoke manual cobre. Reduz token cost. Se reviewer pedir, copiar template.
  </action>
  <verify>
    <automated>cd D:/Projetos/Iridologista/apps/web && pnpm tsc --noEmit 2>&1 | grep "notify-" | head -5</automated>
  </verify>
  <done>2 files TS limpos; pattern best-effort consistente.</done>
</task>

<task type="auto">
  <name>Task 3: Wire notifyCreditPurchaseConfirmed em apply-payment.ts</name>
  <files>apps/web/lib/billing/apply-payment.ts</files>
  <action>
**Modificar** o branch `activated` em `applyPaymentEvent` (plano 04 já criou).

Após `logAuditEvent('credit.purchase_confirmed')` + antes do `return { applied: true, action: 'activated' }`:

```typescript
// Notify (best-effort — falha não bloqueia)
import { notifyCreditPurchaseConfirmed } from '@/lib/notifications/notify-credit-purchase-confirmed'

// ... dentro do branch activated ...

// Need user email + name + package name; fetch in single query
const { data: enriched } = await service
  .from('customer_credits')
  .select('expires_at, credit_packages(name, price_brl), profiles!inner(email, full_name)')
  .eq('id', credit.id)
  .maybeSingle()
if (enriched) {
  const pkg = (enriched as { credit_packages: { name: string; price_brl: number } }).credit_packages
  const prof = (enriched as { profiles: { email: string; full_name: string | null } }).profiles
  // Fire-and-forget — não aguardar (manter < 1s SLA do webhook)
  void notifyCreditPurchaseConfirmed({
    userEmail: prof.email,
    userName: prof.full_name,
    packageName: pkg.name,
    leituras: credit.leituras_purchased,
    valueBrl: pkg.price_brl,
    expiresAt: enriched.expires_at,
  }).catch(err => console.warn('[apply-payment] notify failed:', err))
}
```

**Cuidado:** Usar `void promise.catch(...)` pra fire-and-forget — não `await`. Webhook deve retornar < 1s.

**Wire de notifyRefundProcessed em refundPackageAction (plano 06):**

Modificar `apps/web/app/actions/billing.ts` `refundPackageAction` após success:

```typescript
import { notifyRefundProcessed } from '@/lib/notifications/notify-refund-processed'

// ... após logAuditEvent('credit.refunded'), antes return ...

const { data: profile } = await service.from('profiles').select('full_name').eq('id', user.id).maybeSingle()
void notifyRefundProcessed({
  userEmail: user.email!,
  userName: profile?.full_name,
  packageName: (credit as { credit_packages: { name: string } }).credit_packages.name,
  refundValueBrl: policy.value_brl,
  kind: policy.kind,
  leiturasRefunded: policy.leituras_to_refund,
}).catch(() => {})
```

**notifyCreditExpiring** wire vem em plano 08-13 (cron daily).
  </action>
  <verify>
    <automated>cd D:/Projetos/Iridologista/apps/web && grep -c "notifyCreditPurchaseConfirmed" lib/billing/apply-payment.ts && grep -c "notifyRefundProcessed" app/actions/billing.ts && pnpm tsc --noEmit 2>&1 | grep "apply-payment\|billing.ts" | head -5</automated>
  </verify>
  <done>Both greps ≥1; TS limpo.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-12-01 | Information Disclosure | email contém valor pago em transit | accept | TLS (Resend default); content é o que terapeuta paga já vê |
| T-08-12-02 | Spoofing | atacante envia email falso fingindo Iris Codex | mitigate | SPF + DKIM via Resend domain verified (memory project_resend_domain_unverified_launch_gate RESOLVED) |
| T-08-12-03 | Tampering | XSS via userName em email HTML | mitigate | escapeHtml em todos os user-controlled fields |
| T-08-12-04 | DoS | webhook handler bloqueia em email send timeout | mitigate | void promise + .catch fire-and-forget; webhook returns < 1s |
</threat_model>

<verification>
- 5 testes verdes (notify-purchase)
- pnpm tsc + lint limpos
- apply-payment.ts + billing.ts referenciam novos notify helpers
- Smoke email manualmente: founder paga em sandbox → email chega (validação em plano 08-14)
</verification>

<success_criteria>
- 3 notify helpers prontos
- Wire feito no webhook handler + refund action
- Pattern best-effort respeitado
- Email templates pt-BR + acessíveis
</success_criteria>

<output>
Após completion, criar `.planning/phases/08-pagamento-lgpd/08-12-SUMMARY.md`:
- 3 emails: purchase / expiring / refund
- Wire: webhook (purchase), refundPackageAction (refund), cron (expiring — plano 13)
- Best-effort fire-and-forget pattern aplicado
</output>
