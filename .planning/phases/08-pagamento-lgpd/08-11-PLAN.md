---
phase: 08-pagamento-lgpd
plan: 11
type: execute
wave: 5
depends_on: [08-05, 08-06]
files_modified:
  - apps/web/app/assinatura/page.tsx
  - apps/web/components/billing/CreditsBalanceWidget.tsx
  - apps/web/components/billing/ReservationsList.tsx
  - apps/web/components/billing/RefundPackageButton.tsx
  - apps/web/app/actions/billing-extras.ts
autonomous: false
requirements: [BILLING-02, BILLING-03]
must_haves:
  truths:
    - "/assinatura mostra: trial state (se ativo), saldo total (active credits sum), reservados (sum), processos em andamento (reservations list with TTL + cancelar button)"
    - "Cada credit package ativo (não expired/refunded) mostra: name, leituras_remaining, expires_at, [arrependimento botão se dentro 7d]"
    - "RefundPackageButton mostra modal de confirmação com value computed via computeRefundValue antes do click final"
    - "cancelReservationAction wrapper chama lib/billing/reservations.cancelReservation; usado pelo ReservationsList"
    - "Mobile responsive cards (padrão Fase 11.1 memory)"
  artifacts:
    - path: "apps/web/app/assinatura/page.tsx"
      provides: "Server component que SSR widget data"
    - path: "apps/web/components/billing/CreditsBalanceWidget.tsx"
      provides: "Card grande com saldo total + breakdown"
    - path: "apps/web/components/billing/ReservationsList.tsx"
      provides: "Lista de reservations active com TTL + cancel"
    - path: "apps/web/components/billing/RefundPackageButton.tsx"
      provides: "Modal de confirmação + chamada refundPackageAction"
    - path: "apps/web/app/actions/billing-extras.ts"
      provides: "cancelReservationAction wrapper"
      exports: ["cancelReservationAction"]
---

<objective>
Página `/assinatura` (substitui placeholder existente) mostra estado completo do terapeuta:
- Saldo total + trial state + reservados
- Lista de packages comprados (com botão arrependimento se elegível)
- Lista "Processos em andamento" (reservations active com TTL + botão cancelar)

Purpose: D-11 termina com UX "dashboard mostra saldo + reservados + processos em andamento + botão cancelar". Esta página entrega isso.
Output: 1 page + 3 components + 1 server action.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-pagamento-lgpd/08-CONTEXT.md
@.planning/phases/08-pagamento-lgpd/08-05-SUMMARY.md
@.planning/phases/08-pagamento-lgpd/08-06-SUMMARY.md
@apps/web/lib/billing/credits.ts
@apps/web/lib/billing/trial.ts
@apps/web/lib/billing/reservations.ts
@apps/web/lib/billing/refund-policy.ts
@apps/web/lib/billing/config.ts
@apps/web/app/actions/billing.ts
@apps/web/components/clientes/InviteLinkDialog.tsx

<interfaces>
<!-- From 08-05 -->
```typescript
listActiveReservations(userId): Promise<ActiveReservation[]>  // session client, RLS-aware
cancelReservation(readingId, userId): Promise<CancelResult>   // service client, ownership check
getTrialState(userId): Promise<TrialState>
```

<!-- From 08-06 -->
```typescript
refundPackageAction({ credit_id, reason? }): Promise<RefundPackageResult>
computeRefundValue(credit, now): RefundPolicy
```

<!-- From 08-01 (DB) -->
- customer_credits SELECT user_id=auth.uid() via session client (RLS)
- credit_reservations SELECT user_id=auth.uid() via session client
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: app/actions/billing-extras.ts — cancelReservationAction</name>
  <files>apps/web/app/actions/billing-extras.ts</files>
  <action>
**'use server' wrapper** pra UI invocar:

```typescript
'use server'
import 'server-only'

import { revalidatePath } from 'next/cache'
import { createClient } from '@/lib/supabase/server'
import { cancelReservation } from '@/lib/billing/reservations'

export interface CancelReservationResult {
  ok: boolean
  error?: string
}

export async function cancelReservationAction(readingId: string): Promise<CancelReservationResult> {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { ok: false, error: 'Não autenticado.' }
  if (typeof readingId !== 'string' || readingId.length === 0) {
    return { ok: false, error: 'Reading ID inválido.' }
  }

  const r = await cancelReservation(readingId, user.id)
  if (!r.ok) {
    const msg = r.reason === 'unauthorized' ? 'Você não tem permissão.'
      : r.reason === 'not_found' ? 'Reserva não encontrada.'
      : 'Erro interno.'
    return { ok: false, error: msg }
  }

  revalidatePath('/assinatura')
  revalidatePath('/dashboard')
  return { ok: true }
}
```
  </action>
  <verify>
    <automated>cd D:/Projetos/Iridologista/apps/web && pnpm tsc --noEmit 2>&1 | grep "billing-extras" | head -3</automated>
  </verify>
  <done>TS limpo; segue 'use server' hygiene (só async exports).</done>
</task>

<task type="auto">
  <name>Task 2: 3 components — CreditsBalanceWidget + ReservationsList + RefundPackageButton</name>
  <files>apps/web/components/billing/CreditsBalanceWidget.tsx, apps/web/components/billing/ReservationsList.tsx, apps/web/components/billing/RefundPackageButton.tsx</files>
  <action>
**CreditsBalanceWidget.tsx (server component — accepts data props):**

```tsx
import Link from 'next/link'
import type { TrialState } from '@/lib/billing/trial'

interface Props {
  trialState: TrialState
  totalRemaining: number
  totalReserved: number
  packagesCount: number
}

export function CreditsBalanceWidget({ trialState, totalRemaining, totalReserved, packagesCount }: Props) {
  const inTrial = trialState.status === 'active'

  return (
    <section className="rounded-lg border-2 border-teal-dark/20 bg-card p-5 space-y-4">
      <h2 className="text-lg font-semibold">Seu saldo</h2>

      {inTrial ? (
        <div className="rounded-md bg-green-50 border border-green-200 p-3 space-y-1">
          <p className="text-sm font-semibold text-green-900">Trial ativo</p>
          <p className="text-xs text-green-800">
            {trialState.readings_remaining} {trialState.readings_remaining === 1 ? 'leitura' : 'leituras'} restantes ·
            expira em {trialState.days_remaining} dias
          </p>
        </div>
      ) : null}

      <div className="grid grid-cols-3 gap-3 text-center">
        <div>
          <p className="text-2xl font-bold">{totalRemaining}</p>
          <p className="text-xs text-muted-foreground">Disponível</p>
        </div>
        <div>
          <p className="text-2xl font-bold">{totalReserved}</p>
          <p className="text-xs text-muted-foreground">Reservado</p>
        </div>
        <div>
          <p className="text-2xl font-bold">{totalRemaining + totalReserved}</p>
          <p className="text-xs text-muted-foreground">Total</p>
        </div>
      </div>

      <p className="text-xs text-muted-foreground">
        {packagesCount} {packagesCount === 1 ? 'pacote ativo' : 'pacotes ativos'}
      </p>

      <Link
        href="/assinatura/comprar"
        className="inline-block w-full text-center rounded-md bg-teal-dark px-4 py-2 text-sm font-semibold text-white hover:bg-teal-dark/90"
      >
        {totalRemaining === 0 && !inTrial ? 'Comprar créditos' : 'Comprar mais'}
      </Link>
    </section>
  )
}
```

**ReservationsList.tsx (client component — cancel via action):**

```tsx
'use client'

import { useTransition } from 'react'
import { toast } from 'sonner'
import { cancelReservationAction } from '@/app/actions/billing-extras'
import type { ActiveReservation } from '@/lib/billing/reservations'

export function ReservationsList({ reservations }: { reservations: ActiveReservation[] }) {
  const [isPending, startTransition] = useTransition()

  function handleCancel(readingId: string) {
    if (!confirm('Cancelar este processo libera o crédito reservado de volta. Confirmar?')) return
    startTransition(async () => {
      const r = await cancelReservationAction(readingId)
      if (!r.ok) { toast.error(r.error ?? 'Erro.'); return }
      toast.success('Reserva cancelada. Crédito liberado.')
    })
  }

  if (reservations.length === 0) {
    return (
      <section className="rounded-lg border border-border bg-muted/20 p-5">
        <h2 className="text-base font-semibold">Processos em andamento</h2>
        <p className="mt-2 text-sm text-muted-foreground">Nenhum processo ativo no momento.</p>
      </section>
    )
  }

  return (
    <section className="rounded-lg border border-border bg-card p-5 space-y-3">
      <h2 className="text-base font-semibold">Processos em andamento</h2>
      <p className="text-xs text-muted-foreground">
        Cada processo reserva 1 crédito por até 7 dias. Sem geração nesse prazo → crédito volta automaticamente.
      </p>
      <ul className="divide-y divide-border">
        {reservations.map(r => (
          <li key={r.id} className="py-3 flex items-center justify-between gap-3">
            <div className="flex-1 min-w-0">
              <p className="text-sm font-medium truncate">
                Leitura <code className="text-xs">{r.reading_id.slice(0, 8)}…</code>
              </p>
              <p className="text-xs text-muted-foreground">
                Reservado em {formatDateBR(r.reserved_at)} · expira em {formatRelativeBR(r.expires_at)}
              </p>
              <p className="text-xs text-muted-foreground capitalize">Fonte: {r.source}</p>
            </div>
            <button
              type="button"
              onClick={() => handleCancel(r.reading_id)}
              disabled={isPending}
              className="rounded-md border border-border bg-background px-3 py-1.5 text-xs hover:bg-muted disabled:opacity-50"
            >
              Cancelar
            </button>
          </li>
        ))}
      </ul>
    </section>
  )
}

function formatDateBR(iso: string): string {
  return new Date(iso).toLocaleString('pt-BR', { dateStyle: 'short', timeStyle: 'short', timeZone: 'America/Sao_Paulo' })
}
function formatRelativeBR(iso: string): string {
  const ms = new Date(iso).getTime() - Date.now()
  if (ms <= 0) return 'expirando…'
  const days = Math.floor(ms / 86400000)
  if (days >= 1) return `${days} ${days === 1 ? 'dia' : 'dias'}`
  const hours = Math.floor(ms / 3600000)
  return `${hours} ${hours === 1 ? 'hora' : 'horas'}`
}
```

**RefundPackageButton.tsx (client component, modal):**

```tsx
'use client'

import { useState, useTransition } from 'react'
import { toast } from 'sonner'
import { refundPackageAction } from '@/app/actions/billing'
import { computeRefundValue } from '@/lib/billing/refund-policy'

interface Props {
  creditId: string
  purchaseDate: string
  priceBrl: number
  leiturasPurchased: number
  leiturasRemaining: number
  leiturasReserved: number
  status: string
}

export function RefundPackageButton(props: Props) {
  const [open, setOpen] = useState(false)
  const [isPending, startTransition] = useTransition()

  const policy = computeRefundValue({
    purchase_date: props.purchaseDate,
    price_brl: props.priceBrl,
    leituras_purchased: props.leiturasPurchased,
    leituras_remaining: props.leiturasRemaining,
    leituras_reserved: props.leiturasReserved,
    status: props.status,
  })

  if (!policy.eligible) return null  // botão só aparece se elegível

  function handleConfirm() {
    startTransition(async () => {
      const r = await refundPackageAction({ credit_id: props.creditId })
      if (!r.ok) { toast.error(r.error); return }
      toast.success(`Reembolso solicitado — R$ ${r.refunded_value_brl.toFixed(2)} (${r.kind})`)
      setOpen(false)
    })
  }

  return (
    <>
      <button
        type="button"
        onClick={() => setOpen(true)}
        className="text-xs text-amber-700 underline hover:text-amber-900"
      >
        Solicitar reembolso (arrependimento 7d)
      </button>

      {open ? (
        <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-4">
          <div className="max-w-md w-full rounded-lg bg-background p-5 space-y-4">
            <h3 className="text-lg font-semibold">Solicitar reembolso</h3>
            <p className="text-sm">
              {policy.kind === 'total' ? (
                <>Você terá um reembolso <strong>integral de R$ {policy.value_brl.toFixed(2)}</strong> via o método original (mesmo trajeto da compra).</>
              ) : (
                <>Você terá um reembolso <strong>proporcional de R$ {policy.value_brl.toFixed(2)}</strong>{' '}
                  ({policy.leituras_to_refund} leituras restantes × R$ {policy.unit_price_brl.toFixed(2)}).</>
              )}
            </p>
            <p className="text-xs text-muted-foreground">
              O crédito será zerado imediatamente. O valor cai na sua conta original em até 5 dias úteis (PIX ≤ 1d; cartão 5d).
            </p>
            <div className="flex gap-2 justify-end">
              <button onClick={() => setOpen(false)} disabled={isPending}
                className="rounded-md border border-border px-3 py-1.5 text-sm">Cancelar</button>
              <button onClick={handleConfirm} disabled={isPending}
                className="rounded-md bg-amber-600 px-3 py-1.5 text-sm text-white">
                {isPending ? 'Processando…' : 'Confirmar reembolso'}
              </button>
            </div>
          </div>
        </div>
      ) : null}
    </>
  )
}
```
  </action>
  <verify>
    <automated>cd D:/Projetos/Iridologista/apps/web && pnpm tsc --noEmit 2>&1 | grep -E "(CreditsBalanceWidget|ReservationsList|RefundPackageButton)" | head -5</automated>
  </verify>
  <done>3 components TS limpos; export defaults disponíveis.</done>
</task>

<task type="auto">
  <name>Task 3: app/assinatura/page.tsx — server component que SSRs tudo</name>
  <files>apps/web/app/assinatura/page.tsx</files>
  <action>
**Read existing page** (provavelmente placeholder ou tem layout antigo). Reescrever:

```tsx
import { redirect } from 'next/navigation'
import Link from 'next/link'
import { createClient } from '@/lib/supabase/server'
import { getTrialState } from '@/lib/billing/trial'
import { listActiveReservations } from '@/lib/billing/reservations'
import { CreditsBalanceWidget } from '@/components/billing/CreditsBalanceWidget'
import { ReservationsList } from '@/components/billing/ReservationsList'
import { RefundPackageButton } from '@/components/billing/RefundPackageButton'
import { DisclaimerCopy } from '@/components/legal/DisclaimerCopy'

export const metadata = { title: 'Sua assinatura — Iris Codex' }

interface CreditRow {
  id: string
  status: string
  purchase_date: string
  expires_at: string
  leituras_purchased: number
  leituras_remaining: number
  leituras_reserved: number
  credit_packages: { name: string; price_brl: number; sku: string }
}

export default async function AssinaturaPage() {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/login?next=/assinatura')

  const [{ data: credits }, trialState, reservations] = await Promise.all([
    supabase.from('customer_credits')
      .select('id, status, purchase_date, expires_at, leituras_purchased, leituras_remaining, leituras_reserved, credit_packages(name, price_brl, sku)')
      .eq('user_id', user.id)
      .in('status', ['active', 'pending'])
      .order('purchase_date', { ascending: true }),
    getTrialState(user.id),
    listActiveReservations(user.id),
  ])

  const activeCredits = (credits ?? []) as unknown as CreditRow[]
  const totalRemaining = activeCredits.filter(c => c.status === 'active')
    .reduce((s, c) => s + c.leituras_remaining, 0)
  const totalReserved = activeCredits.filter(c => c.status === 'active')
    .reduce((s, c) => s + c.leituras_reserved, 0)

  return (
    <div className="max-w-4xl mx-auto px-4 py-6 space-y-6">
      <header className="flex items-center justify-between">
        <h1 className="text-2xl font-bold">Sua assinatura</h1>
        <Link href="/dashboard" className="text-sm text-muted-foreground underline">← Dashboard</Link>
      </header>

      <CreditsBalanceWidget
        trialState={trialState}
        totalRemaining={totalRemaining}
        totalReserved={totalReserved}
        packagesCount={activeCredits.filter(c => c.status === 'active').length}
      />

      <ReservationsList reservations={reservations} />

      <section className="space-y-3">
        <h2 className="text-lg font-semibold">Pacotes</h2>
        {activeCredits.length === 0 ? (
          <p className="text-sm text-muted-foreground">
            Nenhum pacote ativo.{' '}
            <Link href="/assinatura/comprar" className="text-teal-dark underline">Comprar agora</Link>
          </p>
        ) : (
          <ul className="space-y-3">
            {activeCredits.map(c => (
              <li key={c.id} className="rounded-md border border-border bg-card p-4 flex items-center justify-between gap-3">
                <div className="flex-1 min-w-0">
                  <p className="text-sm font-semibold">{c.credit_packages.name}</p>
                  <p className="text-xs text-muted-foreground">
                    Status: {c.status === 'pending' ? 'Aguardando pagamento' : 'Ativo'}
                    {c.status === 'active' ? ` · ${c.leituras_remaining}/${c.leituras_purchased} disponíveis · expira em ${new Date(c.expires_at).toLocaleDateString('pt-BR')}` : ''}
                  </p>
                  {c.status === 'active' ? (
                    <div className="mt-2">
                      <RefundPackageButton
                        creditId={c.id}
                        purchaseDate={c.purchase_date}
                        priceBrl={c.credit_packages.price_brl}
                        leiturasPurchased={c.leituras_purchased}
                        leiturasRemaining={c.leituras_remaining}
                        leiturasReserved={c.leituras_reserved}
                        status={c.status}
                      />
                    </div>
                  ) : null}
                </div>
              </li>
            ))}
          </ul>
        )}
      </section>

      <DisclaimerCopy variant="footer" />
    </div>
  )
}
```
  </action>
  <verify>
    <automated>cd D:/Projetos/Iridologista/apps/web && pnpm tsc --noEmit 2>&1 | grep "/assinatura/page" | head -3 && pnpm lint 2>&1 | tail -5</automated>
  </verify>
  <done>TS + lint limpos; page acessível em /assinatura.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Founder UAT /assinatura completa</name>
  <what-built>
- Widget de saldo, lista de reservas, lista de pacotes, botão de arrependimento
  </what-built>
  <how-to-verify>
1. Login em preview Vercel.
2. Visitar /assinatura.
3. Estados a testar:
   - **Sem créditos + trial active**: widget mostra trial leftcount + "Comprar mais"
   - **Com pacote ativo**: pacote listado com remaining/expira em + "Solicitar reembolso (arrependimento 7d)" se compra <7d
   - **Reserva ativa**: lista "Processos em andamento" com TTL relativo + botão "Cancelar"
   - **Click "Cancelar"** → confirm → toast "Crédito liberado" → reserva some + saldo aumenta em 1
4. **Modal refund**: click "Solicitar reembolso" → modal mostra valor calculado + confirmar → toast + status fica 'refunded' (visível pós-refresh)
5. Mobile responsive (375px) — cards stack vertical OK.
  </how-to-verify>
  <resume-signal>"approved" ou descrever issues.</resume-signal>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11-01 | Tampering | terapeuta cancela reservation de outro user | mitigate | cancelReservation .eq('user_id') ownership check em service-role (plano 05) + cancelReservationAction wraps session check |
| T-08-11-02 | Information Disclosure | listActiveReservations expõe credit_id de outro user | mitigate | session client + RLS (plano 01/05) |
| T-08-11-03 | Tampering | client-side computeRefundValue + server divergem | mitigate | refundPackageAction RE-computa server-side (plano 06); client computation é display only |
</threat_model>

<verification>
- TS + lint limpos
- Founder UAT visual + functional
- Cancelar reserva libera saldo
- Modal refund processa via Asaas sandbox
</verification>

<success_criteria>
- /assinatura mostra estado completo
- Founder consegue cancelar reserva
- Founder consegue solicitar refund dentro de 7d (sandbox)
</success_criteria>

<output>
Após completion, criar `.planning/phases/08-pagamento-lgpd/08-11-SUMMARY.md`:
- /assinatura LIVE
- 3 widgets reusáveis
- cancelReservationAction wrapper estabelecido
- Próximo plano 12 envia email de confirmação após webhook + email expiração; plano 13 cron daily
</output>
