---
phase: 08-pagamento-lgpd
plan: 09
type: execute
wave: 4
depends_on: [08-03]
files_modified:
  - apps/web/app/privacidade/page.tsx
  - apps/web/app/termos/page.tsx
  - apps/web/components/legal/DisclaimerCopy.tsx
  - apps/web/app/(public)/__tests__/legal.test.ts
  - apps/web/scripts/audit-vocabulary.mjs
autonomous: false
requirements: [LGPD-02, LGPD-03, LGPD-05, LGPD-06]
must_haves:
  truths:
    - "/privacidade renderiza política completa pt-BR com seção 'Como solicitar exclusão dos seus dados'"
    - "/privacidade tem mailto: pré-formatado pra suporte@iriscodex.com (ou OPERATOR_EMAIL env) com subject + body padrão"
    - "/termos renderiza Termos de Uso pt-BR com pricing D-02 + validade 12m + arrependimento 7d destacados (D-14)"
    - "DisclaimerCopy renderiza 'ferramenta de apoio à anamnese terapêutica integrativa, não substitui avaliação médica' nas 3 superfícies (landing OU header, rodapé legal, rodapé relatório)"
    - "audit-vocabulary.mjs roda green em todas as superfícies novas (sem 'diagnóstico', 'tratamento', 'cura' fora de páginas legais que citam pra negar)"
  artifacts:
    - path: "apps/web/app/privacidade/page.tsx"
      provides: "Página pública de privacidade extensa LGPD-compliant"
    - path: "apps/web/app/termos/page.tsx"
      provides: "Termos de uso público com pricing locked"
    - path: "apps/web/components/legal/DisclaimerCopy.tsx"
      provides: "Copy obrigatória reusável"
      exports: ["DisclaimerCopy", "DISCLAIMER_TEXT"]
  key_links:
    - from: "/privacidade #deletar-dados"
      to: "mailto:suporte@iriscodex.com"
      via: "anchor link"
      pattern: "mailto:"
    - from: "audit-vocabulary script"
      to: "todas as páginas/components novos da Fase 8"
      via: "grep pattern excluding legal-allowlist"
      pattern: "grep.*-v"
---

<objective>
Páginas legais públicas LGPD-02 + LGPD-03 básico (link de deleção via email) + LGPD-05 (copy obrigatória nas 3 superfícies) + audit-vocab LGPD-06 mantém green.

Quatro entregas:
1. `/privacidade` — política completa com seção "como solicitar exclusão" via mailto:
2. `/termos` — Termos de Uso com pricing D-02 + arrependimento 7d em destaque (D-14)
3. `<DisclaimerCopy />` — component pra copy obrigatória aparece em 3 surfaces
4. `audit-vocabulary.mjs` rerun green pós-changes (catch regressions)

Purpose: B2B launch precisa de páginas legais públicas (gate de compliance pra ANPD/cliente final perguntar "qual sua política?"). LGPD-05 copy obrigatória sustenta o posicionamento "não substitui avaliação médica" (memory project_governing_enfoque_emocional_comportamental).
Output: 3 pages + 1 component + audit re-run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-pagamento-lgpd/08-CONTEXT.md
@.planning/phases/08-pagamento-lgpd/08-RESEARCH.md
@.planning/phases/08-pagamento-lgpd/08-PATTERNS.md
@.planning/PROJECT.md
@apps/web/app/privacidade/page.tsx
@apps/web/lib/consent/term-v1.md
@apps/web/scripts/audit-vocabulary.mjs

<interfaces>
<!-- From CONTEXT/RESEARCH -->
- Pricing D-02: Avulsa R$99,70 / Pequeno R$298,50 / Médio R$745,50 / Grande R$1.191,00
- Validade 12m (D-03)
- Arrependimento 7d (D-13) — TOS em destaque na compra E nos termos públicos (D-14)
- LGPD-05 copy: "ferramenta de apoio à anamnese terapêutica integrativa, não substitui avaliação médica"
  - Surface 1: landing OR header de páginas autenticadas
  - Surface 2: rodapé legal (footer global)
  - Surface 3: rodapé do relatório PDF
- LGPD-03 sugestão email template em RESEARCH lines 766-781
- Founder email: rhelton@gmail.com (per RLS pattern) ou criar suporte@iriscodex.com pra suporte (planner decide; RESEARCH não trava)

<!-- Existing -->
- /privacidade page existe (precisa extend, não criar do zero)
- audit-vocabulary.mjs já passa green (memory project_resend_domain_unverified_launch_gate "✅ already passes")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: components/legal/DisclaimerCopy.tsx + wire em 3 surfaces</name>
  <files>apps/web/components/legal/DisclaimerCopy.tsx</files>
  <action>
**Component canônico:**

```typescript
// Copy obrigatória LGPD-05 / posicionamento PROJECT.md.
// Verbatim — NÃO editar sem revisar memory project_iris_codex_quality_bar.

export const DISCLAIMER_TEXT =
  'Iris Codex é uma ferramenta de apoio à anamnese terapêutica integrativa. ' +
  'NÃO substitui avaliação médica, diagnóstico clínico ou prescrição de tratamento. ' +
  'As leituras geradas não constituem diagnóstico nem prescrevem cura — são leituras ' +
  'simbólicas e funcionais voltadas a apoiar o trabalho do terapeuta integrativo.'

export function DisclaimerCopy({ variant = 'footer' }: { variant?: 'footer' | 'inline' | 'compact' }) {
  if (variant === 'compact') {
    return (
      <p className="text-xs text-muted-foreground leading-relaxed">
        Iris Codex é ferramenta de apoio terapêutico integrativo. Não substitui avaliação médica.
      </p>
    )
  }
  if (variant === 'inline') {
    return (
      <p className="text-sm text-muted-foreground leading-relaxed">
        {DISCLAIMER_TEXT}
      </p>
    )
  }
  // footer
  return (
    <footer className="border-t border-border bg-muted/30 py-6 px-4 text-center">
      <p className="text-xs text-muted-foreground leading-relaxed max-w-3xl mx-auto">
        {DISCLAIMER_TEXT}
      </p>
      <p className="mt-2 text-xs text-muted-foreground">
        <a href="/privacidade" className="underline">Privacidade</a>{' · '}
        <a href="/termos" className="underline">Termos</a>{' · '}
        <a href="/privacidade#deletar-dados" className="underline">Solicitar exclusão</a>
      </p>
    </footer>
  )
}
```

**Wire em 3 surfaces EXATAS (founder caught planner punting — WARN-8):**

1. **Surface 1 — Rodapé do relatório PDF:**
   Arquivo: `apps/web/lib/pdf/report-print-document.tsx` (CONFIRMED — único PDF doc no projeto).
   Adicionar import `{ DISCLAIMER_TEXT }` e renderizar no rodapé da última página (ou capa, dependendo da estrutura existente). Se o doc tem footer já estruturado, INSERIR `DISCLAIMER_TEXT` como nova linha no final. Se existe disclaimer-like text antigo, SUBSTITUIR por DISCLAIMER_TEXT canônico (single source of truth).

2. **Surface 2 — Layout autenticado (dashboard footer):**
   Arquivo: `apps/web/app/(dashboard)/layout.tsx` (CONFIRMED — autenticated route group; NÃO `(authenticated)` que não existe).
   Adicionar `<DisclaimerCopy variant="footer" />` no final do JSX retornado, após o `{children}`. Verificar se o layout retorna fragment ou wrapper; se wrapper único, colocar dentro do wrapper. NÃO colocar no `app/layout.tsx` raiz pra evitar duplicar nos /auth flows.

3. **Surface 3 — Auth header (dashboard component):**
   Arquivo: `apps/web/components/dashboard/dashboard-header.tsx` (CONFIRMED via grep).
   Adicionar `<DisclaimerCopy variant="compact" />` discreto após o título/breadcrumb da página, ANTES das actions à direita. Compact variant tem text-xs e text-muted-foreground — visual minimal.

**Por que esses 3 e não outros:**
- Surface 1 (PDF) — produto entregue; obrigatório o disclaimer estar no artefato final (LGPD-05 + posicionamento founder).
- Surface 2 (footer layout) — todas as páginas autenticadas pegam o disclaimer "passivamente"; cobre dashboard/clientes/leituras/assinatura.
- Surface 3 (header) — disclaimer ATIVO (não rola pra ver); reforço pro terapeuta de que o produto não é diagnóstico.

Surfaces NÃO escolhidas (justificativa pra executor não improvisar):
- /privacidade + /termos: redundante — esses já são páginas legais; vão ter o text inline mas via `variant="inline"`, não o footer.
- Landing (/): deferred — Fase 9 (memory `project_b2c_registration_future_phase`).
- /login + /signup: já tem TOS checkbox que cobre legal; adicionar disclaimer extra polui UX.

**Implementor MUST:** verificar que cada um dos 3 hook points existe (grep confirma) ANTES de editar. Se algum arquivo mudou de path desde este plan (ex: layout virou `(dashboard)/layout.tsx` → outra coisa), atualizar paths + documentar no SUMMARY.
  </action>
  <verify>
    <automated>cd D:/Projetos/Iridologista/apps/web && pnpm tsc --noEmit 2>&1 | grep "DisclaimerCopy" | head -3 && grep -rn "DisclaimerCopy" --include="*.tsx" -l | head -5</automated>
  </verify>
  <done>DisclaimerCopy importado em ≥3 lugares; TS limpo.</done>
</task>

<task type="auto">
  <name>Task 2: /privacidade extend + LGPD-03 link deletion</name>
  <files>apps/web/app/privacidade/page.tsx</files>
  <action>
**Read existing page** primeiro. Extend com:

1. **Seção "Tratamento de dados pessoais"**:
   - Quais dados coletamos (terapeuta: nome, email, CPF, telefone, profissão; cliente final: nome, data nascimento, fotos de íris = dado biométrico LGPD art. 11)
   - Finalidades (gerar leituras simbólicas; cumprir contrato comercial; comunicação)
   - Base legal (consentimento via termo biométrico assinado; execução de contrato pra dados de terapeuta)
   - Compartilhamento (Asaas pra cobrança; Resend pra email; Anthropic pra geração de relatório; nenhum vende dados)
   - Retenção (12m de validade do crédito; 24m de leituras pós-última atividade; depois anonimização)
   - Subprocessadores (Vercel, Supabase, Anthropic, Asaas, Resend)

2. **Seção `#deletar-dados`** (anchor target):
```tsx
<section id="deletar-dados" className="space-y-4 mt-8">
  <h2 className="text-xl font-semibold">Como solicitar exclusão dos seus dados</h2>
  <p>De acordo com a LGPD (art. 18), você pode solicitar a qualquer momento:</p>
  <ul className="list-disc pl-6 space-y-1">
    <li>Confirmação de tratamento dos seus dados</li>
    <li>Acesso aos dados</li>
    <li>Correção de dados incompletos</li>
    <li>Eliminação dos dados desnecessários</li>
    <li>Portabilidade pra outro fornecedor</li>
  </ul>
  <p>Para solicitar exclusão, envie um e-mail para nossa equipe usando o link abaixo:</p>

  <a
    href={`mailto:${OPERATOR_EMAIL}?subject=${encodeURIComponent('[Iris Codex] Pedido de exclusão LGPD')}&body=${encodeURIComponent(BODY_TEMPLATE)}`}
    className="inline-block rounded-md bg-teal-dark px-4 py-2 text-sm font-medium text-white"
  >
    Solicitar exclusão por e-mail →
  </a>

  <p className="text-sm text-muted-foreground">
    Responderemos sua solicitação em até <strong>15 dias úteis</strong> conforme LGPD art. 19 §3º.
  </p>
</section>
```

Constantes:
```typescript
const OPERATOR_EMAIL = process.env.NEXT_PUBLIC_OPERATOR_EMAIL ?? 'rhelton@gmail.com'

const BODY_TEMPLATE = `Olá,

Solicito a exclusão dos meus dados pessoais armazenados no Iris Codex, incluindo:
- Meu cadastro (terapeuta)
- Todos os relatórios gerados
- Imagens de íris dos meus clientes
- Demais registros associados

Nome completo: ___________________
E-mail cadastrado: ________________
CPF (para confirmação de identidade): ___________

Aguardo confirmação por email em até 15 dias úteis.

Obrigado.`
```

3. **Seção LGPD-05 obrigatória** — incluir `<DisclaimerCopy variant="inline" />` em local visível.

4. **Encarregado de dados (DPO)** — Founder atua como DPO Phase 8 (sem terceirizar). Listar contato.

Source canônico pro conteúdo: usar `apps/web/lib/consent/term-v1.md` como referência pra evitar contradição. Linguagem pt-BR formal mas legível.
  </action>
  <verify>
    <automated>cd D:/Projetos/Iridologista/apps/web && pnpm tsc --noEmit 2>&1 | grep "privacidade" | head -3 && grep -c "deletar-dados" app/privacidade/page.tsx && grep -c "mailto:" app/privacidade/page.tsx</automated>
  </verify>
  <done>Ambos greps ≥1; tsc limpo; page renderiza em preview.</done>
</task>

<task type="auto">
  <name>Task 3: /termos page + pricing D-02 destacado</name>
  <files>apps/web/app/termos/page.tsx</files>
  <action>
Criar/extend `/termos` page com conteúdo:

1. **Introdução** — Iris Codex como plataforma B2B; quem pode contratar (terapeutas/profissionais de saúde integrativa); aceitar = vincular.

2. **Modelo comercial — pricing D-02 EM DESTAQUE (D-14)**:
```tsx
<section className="my-6 rounded-md border-2 border-teal-dark/30 bg-teal-50 p-4">
  <h3 className="font-semibold mb-2">Modelo de pagamento</h3>
  <ul className="list-disc pl-6 space-y-1 text-sm">
    <li><strong>Trial gratuito:</strong> 3 leituras OU 60 dias (o que ocorrer primeiro), sem cartão de crédito.</li>
    <li><strong>Pacotes pré-pagos</strong> (créditos comprados antecipadamente):
      <ul className="list-disc pl-6 mt-1">
        <li>Avulsa: R$ 99,70 (1 leitura)</li>
        <li>Pacote Pequeno: R$ 298,50 (5 leituras — R$ 59,70/un)</li>
        <li>Pacote Médio: R$ 745,50 (15 leituras — R$ 49,70/un) — <em>Mais escolhido</em></li>
        <li>Pacote Grande: R$ 1.191,00 (30 leituras — R$ 39,70/un) — <em>Melhor valor</em></li>
      </ul>
    </li>
    <li><strong>Validade dos créditos:</strong> 12 meses a partir da confirmação do pagamento.</li>
    <li><strong>Não-uso = sem reembolso</strong> após o vencimento dos 12 meses.</li>
  </ul>
</section>
```

3. **Arrependimento (CDC art. 49)**:
```tsx
<section className="my-6 rounded-md border-2 border-amber-300 bg-amber-50 p-4">
  <h3 className="font-semibold mb-2">Direito de arrependimento</h3>
  <p className="text-sm">
    Conforme o Código de Defesa do Consumidor (art. 49), você tem <strong>7 dias corridos</strong> após
    a confirmação do pagamento pra solicitar o cancelamento da compra:
  </p>
  <ul className="list-disc pl-6 mt-2 text-sm space-y-1">
    <li>Se nenhuma leitura foi consumida → <strong>reembolso integral</strong>.</li>
    <li>Se ao menos 1 leitura foi consumida → <strong>reembolso proporcional</strong> sobre o saldo restante.</li>
    <li>Após 7 dias → sem reembolso de créditos não usados (avaliação caso-a-caso via suporte).</li>
  </ul>
</section>
```

4. **Limitação de responsabilidade** + **uso correto** + **propriedade intelectual** + **resolução de conflitos (foro)** — padrão TOS BR.

5. **Footer** — `<DisclaimerCopy variant="inline" />`.

Localização do template: pode reusar idéias do `apps/web/lib/consent/tos.ts` (TOS_VERSION) ou do checkbox tos do signup. Verificar se já existe TOS markdown e expand baseado dele.
  </action>
  <verify>
    <automated>cd D:/Projetos/Iridologista/apps/web && pnpm tsc --noEmit 2>&1 | grep "/termos" | head -3 && grep -c "99,70\|99.70" app/termos/page.tsx && grep -c "7 dias\|arrependimento" app/termos/page.tsx</automated>
  </verify>
  <done>Ambos greps ≥1; page renderiza pt-BR; pricing D-02 textual exato.</done>
</task>

<task type="auto">
  <name>Task 4: __tests__/legal.test.ts + audit-vocabulary re-run</name>
  <files>apps/web/app/(public)/__tests__/legal.test.ts</files>
  <action>
**Test minimal** (RSC render + grep canônicas):

```typescript
import { describe, expect, it } from 'vitest'
import { readFileSync } from 'node:fs'
import { resolve } from 'node:path'

describe('LGPD-05 / LGPD-02 copy in legal pages', () => {
  it('/privacidade contém anchor #deletar-dados', () => {
    const content = readFileSync(resolve(__dirname, '../../privacidade/page.tsx'), 'utf8')
    expect(content).toContain('deletar-dados')
    expect(content).toContain('mailto:')
  })

  it('/termos contém pricing D-02 (99,70 / 298,50 / 745,50 / 1191)', () => {
    const content = readFileSync(resolve(__dirname, '../../termos/page.tsx'), 'utf8')
    expect(content).toMatch(/99[,.]70/)
    expect(content).toMatch(/298[,.]50/)
    expect(content).toMatch(/745[,.]50/)
    expect(content).toMatch(/1[,.]?191/)
  })

  it('/termos contém arrependimento 7d destacado (D-14)', () => {
    const content = readFileSync(resolve(__dirname, '../../termos/page.tsx'), 'utf8')
    expect(content).toMatch(/7\s+dias/)
    expect(content.toLowerCase()).toContain('arrependimento')
  })

  it('DisclaimerCopy export inclui copy LGPD-05', () => {
    const content = readFileSync(resolve(__dirname, '../../../components/legal/DisclaimerCopy.tsx'), 'utf8')
    expect(content).toContain('apoio à anamnese terapêutica integrativa')
    expect(content).toContain('NÃO substitui avaliação médica')
  })
})
```

**audit-vocabulary re-run:**

Rodar `node apps/web/scripts/audit-vocabulary.mjs` e confirmar exit 0. Memory project_resend_domain_unverified_launch_gate diz "✅ already passes". Pós este plano:

- Adicionar conteúdo novo SEM palavras proibidas FORA das páginas legais (que citam pra negar — allowlist)
- `/privacidade` + `/termos` podem mencionar "diagnóstico", "tratamento", "cura" se for pra dizer "NÃO oferecemos"
- O audit script provavelmente já tem allowlist; verificar `apps/web/scripts/audit-vocabulary.mjs` por entry pra `/privacidade` e adicionar `/termos` se necessário

**INFO-4 fix — exact mechanism:** audit-vocabulary.mjs usa marcador IN-FILE `audit-vocabulary:allowlist` (NÃO lista de paths). Arquivos com o comentário no topo são SKIPPED.

Confirmar via grep do source script (`apps/web/scripts/audit-vocabulary.mjs` linhas ~91-97 + 143):
```javascript
const ALLOWLIST_MARKER = 'audit-vocabulary:allowlist'
// ...
if (content.includes(ALLOWLIST_MARKER)) continue
```

**1-line diff requerido em cada novo arquivo legal:** adicionar comment no topo de `/privacidade` E `/termos` se citarem vocab proibido pra negar:

```typescript
// audit-vocabulary:allowlist — legal page may cite forbidden vocab (diagnóstico, tratamento, cura) to negate.
```

Verificar: `grep -l "audit-vocabulary:allowlist" apps/web/app/privacidade/page.tsx apps/web/app/termos/page.tsx` retorna ambos paths se necessário. Se conteúdo das páginas NÃO usa vocab proibido (improvável dado contexto LGPD), o marker NÃO é necessário — `node scripts/audit-vocabulary.mjs` decide via exit code.
  </action>
  <verify>
    <automated>cd D:/Projetos/Iridologista/apps/web && pnpm vitest run app/\(public\)/__tests__/legal.test.ts 2>&1 | tail -10 && node scripts/audit-vocabulary.mjs 2>&1 | tail -10</automated>
  </verify>
  <done>4 testes green; audit-vocabulary exit 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Founder verifica copy legal + decide OPERATOR_EMAIL</name>
  <what-built>
- /privacidade extended com seção deleção + mailto
- /termos com pricing D-02 destacado + arrependimento 7d
- DisclaimerCopy em 3 surfaces (footer global + report PDF + header)
- audit-vocabulary green
  </what-built>
  <how-to-verify>
1. Visitar https://iriscodex.com/privacidade (ou preview Vercel) — ler integralmente:
   - Linguagem pt-BR formal apropriada?
   - Mailto: funciona ao clicar (abre cliente de email)?
   - Texto fala em 15 dias úteis?

2. Visitar /termos — verificar:
   - Pricing exato D-02 (não R$100, não R$300)
   - Arrependimento 7d em box destacado

3. Verificar DISCLAIMER em rodapé global de páginas autenticadas.

4. Confirmar `NEXT_PUBLIC_OPERATOR_EMAIL` no Vercel — atualmente default = founder rhelton@gmail.com. Se quer separar suporte@iriscodex.com → configurar agora + apontar pra Resend forwarding rule.

5. Validar tom do termo de uso vs term-v1.md — não contradições.
  </how-to-verify>
  <resume-signal>
"approved" + escolha:
- OPERATOR_EMAIL: founder (default) OU suporte@iriscodex.com
- Tom OK? SIM/NÃO (e o quê ajustar)
</resume-signal>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09-01 | Repudiation | terapeuta nega ter aceitado TOS | mitigate | tos_accepted_at + tos_version em profiles (Fase 8 plano 06 signup expand) |
| T-08-09-02 | Information Disclosure | termo cita CPF cliente em página pública | accept | /privacidade /termos são templates, não dados reais; CPF aparece só no PDF assinado (plano 08) |
| T-08-09-03 | Repudiation | atacante alega que vimos seu CPF sem consentimento | mitigate | Copy obrigatória LGPD-05 em footer/relatório explicita finalidade limitada; consent_terms versioned |
| T-08-09-04 | Tampering | regression: copy proibida vaza em nova UI | mitigate | audit-vocabulary CI gate (LGPD-06) já LIVE + re-rodado neste plano |
</threat_model>

<verification>
- 4 legal tests green
- audit-vocabulary exit 0 pós-changes
- pnpm tsc + lint limpos
- DisclaimerCopy em ≥3 lugares
- /privacidade + /termos renderizam em preview
</verification>

<success_criteria>
- LGPD-02 + LGPD-03 + LGPD-05 satisfeitos
- LGPD-06 mantém green
- Founder aprova copy
</success_criteria>

<output>
Após completion, criar `.planning/phases/08-pagamento-lgpd/08-09-SUMMARY.md`:
- /privacidade + /termos LIVE
- DisclaimerCopy reusable
- audit-vocabulary integrado a fluxo
- OPERATOR_EMAIL escolhido
- Próximo plano (10) UI de compra também pode importar DisclaimerCopy
</output>
