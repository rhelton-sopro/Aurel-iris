---
phase: 12-publicacao-instagram
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0049_social_posts_publishing.sql
  - apps/web/lib/admin/social-posts.ts
  - apps/web/lib/instagram/__tests__/instagram-claim.test.ts
autonomous: false
requirements: [IGPUB-02, IGPUB-06]
must_haves:
  truths:
    - "Rodar o cron duas vezes NÃO republica um post já publicado (claim idempotente)"
    - "Um post em 'publicado' nunca pode ser re-reivindicado pelo claim"
    - "Após sucesso, o social_posts grava permalink/ID; em falha grava motivo e NÃO entra em 'publicado'"
    - "A migration 0049 está aplicada no banco de produção (db push)"
  artifacts:
    - path: "supabase/migrations/0049_social_posts_publishing.sql"
      provides: "Colunas de publicação + CHECK estendido + índice + RPCs de claim/reaper"
      contains: "claim_due_social_posts"
    - path: "apps/web/lib/admin/social-posts.ts"
      provides: "SocialPostStatus union com 'publicando'|'erro' + campos novos no SocialPost"
      contains: "publicando"
  key_links:
    - from: "apps/web/lib/admin/social-posts.ts"
      to: "social_posts (banco)"
      via: "SocialPostStatus union + isSocialPostStatus + STATUS_TABS + fetchStatusCounts"
      pattern: "'publicando'"
---

<objective>
Estender o schema de `social_posts` para suportar publicação no Instagram: colunas de resultado (permalink/ID/erro/tentativas), o estado intermediário de lock `publicando`, o estado terminal `erro`, um índice de varredura, e as RPCs atômicas `SECURITY DEFINER` que garantem a idempotência (D-02) e a cadência de re-tentativa (D-03). Em lockstep, bumpar o tipo TS `SocialPostStatus` e todas as suas ocorrências.

Purpose: Sem o claim atômico no banco, dois cron runs sobrepostos (ou cron + "publicar agora") republicam o mesmo post. O claim é o portão de idempotência (IGPUB-02). As colunas de resultado são o que IGPUB-06 grava.
Output: migration 0049 aplicada em prod + union TS atualizado + teste do claim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-publicacao-instagram/12-RESEARCH.md
@.planning/phases/12-publicacao-instagram/12-PATTERNS.md

<interfaces>
<!-- Estado atual de social_posts (migration 0045) — CHECK inline, nome auto-gerado social_posts_status_check -->
From supabase/migrations/0045_social_posts.sql:
```sql
create table if not exists social_posts (
  ...
  status text not null default 'pendente'
         check (status in ('pendente','aprovado','agendado','publicado','reprovado')),
  scheduled_at timestamptz,
  media jsonb not null default '{}'::jsonb,
  ...
);
```

<!-- RPC SECURITY DEFINER analog — copie a forma exata -->
From supabase/migrations/0040_phase_8_consume_atomic_and_release_internal_fix.sql:
```sql
create or replace function public.release_reservation(...)
returns boolean
language plpgsql
security definer
as $$
declare v_reservation ...;
begin
  update credit_reservations set status = 'released' where ... returning * into v_reservation;
  if not found then return false; end if;
  ...
end; $$;
```

<!-- TS union a bumpar (TODAS as 4 ocorrências em lockstep) -->
From apps/web/lib/admin/social-posts.ts:
- type SocialPostStatus (linhas 14-19)
- STATUS_TABS array (linhas 48-54)
- isSocialPostStatus guard (linhas 56-64)
- fetchStatusCounts counts init object (linhas 92-98)
- interface SocialPost (linhas 29-45) — adicionar campos de resultado
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escrever migration 0049 (colunas + CHECK estendido + índice + RPCs de claim/reaper)</name>
  <read_first>
    - supabase/migrations/0045_social_posts.sql (tabela alvo + CHECK inline atual `social_posts_status_check`)
    - supabase/migrations/0040_phase_8_consume_atomic_and_release_internal_fix.sql (analog do SECURITY DEFINER RPC — linhas 30-49)
    - supabase/migrations/0048_app_settings.sql (estilo de header idempotente)
    - 12-PATTERNS.md §"supabase/migrations/0049_social_posts_publishing.sql" (linhas 303-350) — forma do claim RPC
    - 12-RESEARCH.md §"Schema Additions" (linhas 262-294) e §"Idempotency / Locking Pattern" (linhas 224-258)
  </read_first>
  <action>
Criar `supabase/migrations/0049_social_posts_publishing.sql`. Header `-- 0049 — social_posts: publicação Instagram (colunas de resultado + lock state + claim RPCs)` explicando motivo + nota "forward-only; db push é version-tracked, não content-tracked".

Conteúdo EXATO:

```sql
alter table social_posts
  add column if not exists ig_media_id     text,
  add column if not exists ig_permalink    text,
  add column if not exists ig_container_id text,
  add column if not exists publish_error   text,
  add column if not exists publish_attempts int not null default 0,
  add column if not exists last_attempt_at  timestamptz,
  add column if not exists published_at     timestamptz;

alter table social_posts drop constraint if exists social_posts_status_check;
alter table social_posts add constraint social_posts_status_check
  check (status in ('pendente','aprovado','agendado','publicando','publicado','reprovado','erro'));

create index if not exists social_posts_due_idx
  on social_posts(status, scheduled_at)
  where status in ('agendado','publicando');
```

RPC de claim em lote (cron sweep) — atômico, com `for update skip locked` (cap D-03 `publish_attempts < 3`):

```sql
create or replace function public.claim_due_social_posts(p_limit int default 10)
returns setof social_posts
language plpgsql security definer as $$
begin
  return query
  update social_posts
     set status = 'publicando', last_attempt_at = now(),
         publish_attempts = publish_attempts + 1, updated_at = now()
   where id in (
     select id from social_posts
      where status = 'agendado' and scheduled_at <= now() and publish_attempts < 3
      order by scheduled_at
      for update skip locked
      limit p_limit)
  returning *;
end; $$;
```

RPC de claim de UM id ("publicar agora", D-08) — força mesmo sem `scheduled_at` vencido, aceita `aprovado`/`agendado`; retorna 0 linhas se não-reivindicável (já publicando/publicado):

```sql
create or replace function public.claim_one_social_post(p_id uuid)
returns setof social_posts
language plpgsql security definer as $$
begin
  return query
  update social_posts
     set status = 'publicando', last_attempt_at = now(),
         publish_attempts = publish_attempts + 1, updated_at = now()
   where id = p_id and status in ('aprovado','agendado','erro')
  returning *;
end; $$;
```

RPC reaper — linhas presas em `publicando` há >15min voltam a `agendado` (crash recovery, ainda sob o cap de tentativas):

```sql
create or replace function public.reap_stuck_publishing()
returns int
language plpgsql security definer as $$
declare v_count int;
begin
  with reaped as (
    update social_posts
       set status = 'agendado', updated_at = now()
     where status = 'publicando'
       and last_attempt_at < now() - interval '15 minutes'
    returning 1)
  select count(*) into v_count from reaped;
  return v_count;
end; $$;
```

Definir `grant execute` para `service_role` em cada função (RLS-bypass server-side).
  </action>
  <verify>
    <automated>grep -c "claim_due_social_posts\|claim_one_social_post\|reap_stuck_publishing" supabase/migrations/0049_social_posts_publishing.sql | grep -qx 3 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "check (status in ('pendente','aprovado','agendado','publicando','publicado','reprovado','erro'))" supabase/migrations/0049_social_posts_publishing.sql` retorna 1 match.
    - `grep -c "for update skip locked" supabase/migrations/0049_social_posts_publishing.sql` ≥ 1.
    - `grep -c "drop constraint if exists social_posts_status_check" supabase/migrations/0049_social_posts_publishing.sql` == 1.
    - `grep -c "create index if not exists social_posts_due_idx" supabase/migrations/0049_social_posts_publishing.sql` == 1.
    - As 3 funções (`claim_due_social_posts`, `claim_one_social_post`, `reap_stuck_publishing`) presentes com `security definer`.
  </acceptance_criteria>
  <done>migration 0049 escrita com colunas, CHECK estendido (7 estados), índice de varredura, 3 RPCs SECURITY DEFINER e grants ao service_role.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bumpar SocialPostStatus union + STATUS_TABS + guard + counts + campos do SocialPost (lockstep)</name>
  <read_first>
    - apps/web/lib/admin/social-posts.ts (arquivo alvo — TODAS as 4 ocorrências + interface)
    - 12-PATTERNS.md §"apps/web/lib/admin/social-posts.ts" (linhas 229-240) — regra "bump EVERY occurrence"
  </read_first>
  <behavior>
    - `isSocialPostStatus('publicando')` retorna true
    - `isSocialPostStatus('erro')` retorna true
    - `isSocialPostStatus('xpto')` retorna false
    - O objeto `counts` em `fetchStatusCounts` inicializa `publicando: 0` e `erro: 0`
  </behavior>
  <action>
Em `apps/web/lib/admin/social-posts.ts`, atualizar EM LOCKSTEP (regra de memória "bump EVERY occurrence" — atualização parcial é a causa #1 de bug):

1. `type SocialPostStatus` → adicionar `| 'publicando' | 'erro'`.
2. `STATUS_TABS` → adicionar `{ status: 'publicando', label: 'Publicando' }` (entre agendado e publicado) e `{ status: 'erro', label: 'Falhou' }` (ao fim).
3. `isSocialPostStatus` → adicionar `v === 'publicando' || v === 'erro'`.
4. `fetchStatusCounts` objeto `counts` → adicionar `publicando: 0,` e `erro: 0,`.
5. interface `SocialPost` → adicionar campos novos (mesmos nomes da migration 0049):
   ```typescript
   ig_media_id: string | null
   ig_permalink: string | null
   ig_container_id: string | null
   publish_error: string | null
   publish_attempts: number
   last_attempt_at: string | null
   published_at: string | null
   ```
  </action>
  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit -p tsconfig.json 2>&1 | head -5; grep -c "'publicando'" lib/admin/social-posts.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'publicando'" apps/web/lib/admin/social-posts.ts` ≥ 4 (union + STATUS_TABS + guard + counts).
    - `grep -c "'erro'" apps/web/lib/admin/social-posts.ts` ≥ 4.
    - `grep -c "ig_permalink\|publish_attempts\|published_at" apps/web/lib/admin/social-posts.ts` ≥ 3 (campos na interface).
    - `pnpm --dir apps/web tsc --noEmit` (ou build) sem novos erros de tipo em social-posts.ts.
  </acceptance_criteria>
  <done>Union, abas, guard, counts e interface todos refletem `publicando`/`erro` + campos de resultado; tsc limpo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Teste do claim idempotente (mockado)</name>
  <read_first>
    - apps/web/lib/billing/cron-jobs.ts (forma do job-fn que chama `.rpc()`)
    - 12-VALIDATION.md §"Per-Task Verification Map" + §"Wave 0 Requirements"
    - 12-RESEARCH.md §"Validation Architecture" (linhas 397-434) — IGPUB-02 idempotency
  </read_first>
  <action>
Criar `apps/web/lib/instagram/__tests__/instagram-claim.test.ts` (vitest). Como supabase-js é mockado, testar o CONTRATO do claim conforme será consumido por `publishDuePosts`:
- Mock `createServiceClient` retornando um stub cujo `.rpc('claim_due_social_posts', { p_limit })` resolve para um array de rows.
- Teste 1 (IGPUB-02): quando o claim retorna 2 rows na 1ª chamada e `[]` na 2ª (simulando concorrência: a 2ª passada não reivindica nada porque o status já mudou), o consumidor processa exatamente 2 posts e a 2ª passada processa 0.
- Teste 2 (IGPUB-02): um row com `status='publicado'` JAMAIS aparece no retorno do claim (o stub só retorna o que casa `status='agendado'`) — afirma que o consumidor nunca tenta publicar um `publicado`.

Como `publishDuePosts` ainda não existe (Plan 03), este teste pode testar uma função-helper de baixo nível `claimDue(service, limit)` que apenas chama a RPC e devolve o array — extraída em `lib/instagram/publish.ts` no Plan 03. Se o helper ainda não existe, marcar o teste como o contrato esperado e usar `expect(typeof claimDue).toBe('function')` guard que ficará verde quando Plan 03 entregar. Preferir: escrever o teste contra a assinatura `claimDue(service, p_limit)` para que Plan 03 implemente contra ele.
  </action>
  <verify>
    <automated>cd apps/web && pnpm test:run lib/instagram/__tests__/instagram-claim.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `apps/web/lib/instagram/__tests__/instagram-claim.test.ts` existe.
    - O teste mocka `createServiceClient` (sem rede real).
    - `grep -c "claim_due_social_posts\|claimDue" apps/web/lib/instagram/__tests__/instagram-claim.test.ts` ≥ 1.
    - Há ao menos 2 casos: "2 reivindicados depois 0" e "publicado nunca reivindicado".
  </acceptance_criteria>
  <done>Teste do contrato de claim escrito; verde quando rodado (mock) ou marcado como Wave 0 contract para Plan 03.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: [BLOCKING] supabase db push — aplicar migration 0049 em produção</name>
  <action>Founder aplica a migration 0049 em produção via `supabase db push --linked` (a senha do banco não está no env local → não-autônomo) e confirma o schema via information_schema. Passos em how-to-verify.</action>
  <what-built>migration 0049 (colunas + CHECK 7 estados + índice + 3 RPCs SECURITY DEFINER).</what-built>
  <how-to-verify>
    O founder aplica a migration (a senha do banco NÃO está no env local; o agente não consegue rodar o push de forma autônoma):
    1. `supabase db push --linked` (ou via dashboard) com a 0049.
    2. Confirmar via `information_schema` (regra de memória "Verify Supabase schema, not migration list") — checar que as colunas `ig_media_id, ig_permalink, ig_container_id, publish_error, publish_attempts, last_attempt_at, published_at` existem em `social_posts`, que o CHECK aceita `publicando`/`erro`, e que `claim_due_social_posts`, `claim_one_social_post`, `reap_stuck_publishing` existem em `pg_proc`.
    3. Confirmar que o nome do constraint pré-existente era de fato `social_posts_status_check` (auto-gerado do CHECK inline em 0045) — se diferente, ajustar o `drop constraint` na 0049 antes de aplicar.
  </how-to-verify>
  <resume-signal>Digite "aplicado" após confirmar as colunas/RPCs no schema de produção, ou descreva o erro.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Node server-side → Postgres (service-role) | claim/update das rows; RLS bypassado pelo service-role |
| concurrent cron runs / cron × "publicar agora" | duas execuções podem disputar a mesma row |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | double-publish via race | mitigate | claim atômico `update ... where status='agendado' ... returning *` + `for update skip locked`; um `publicado` nunca casa o WHERE → não re-reivindicável (D-02) |
| T-12-02 | Denial of Service | row presa em `publicando` após crash | mitigate | `reap_stuck_publishing()` reseta `publicando` >15min → `agendado`, sob o cap `publish_attempts < 3` |
| T-12-03 | Elevation of Privilege | RPC SECURITY DEFINER executável por roles indevidos | mitigate | `grant execute` apenas a `service_role`; tabela `social_posts` é RLS founder-only |
</threat_model>

<verification>
- `pnpm --dir apps/web test:run lib/instagram/__tests__/instagram-claim.test.ts` verde.
- `pnpm --dir apps/web tsc --noEmit` sem novos erros.
- Schema de prod confirmado via information_schema (Task 4).
</verification>

<success_criteria>
- migration 0049 aplicada em prod com 7 estados no CHECK, colunas de resultado, índice e 3 RPCs.
- `SocialPostStatus` e suas 4 ocorrências + interface bumpadas em lockstep (tsc limpo).
- Teste do claim idempotente presente e verde (mock).
</success_criteria>

<output>
Após completar, criar `.planning/phases/12-publicacao-instagram/12-01-SUMMARY.md`.
</output>
