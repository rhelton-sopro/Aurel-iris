---
phase: 12-publicacao-instagram
plan: 04
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - apps/web/lib/admin/notifications-summary.ts
  - apps/web/app/admin/page.tsx
  - apps/web/lib/admin/__tests__/notifications-summary.test.ts
autonomous: true
requirements: [IGPUB-06, IGPUB-01]
must_haves:
  truths:
    - "Falhas de publicação (posts em 'erro') aparecem na central de notificações do /admin"
    - "Uma falha de health-check/refresh do token do IG é sinalizada no /admin (D-07)"
    - "Sucesso de publicação é silencioso — não gera notificação (D-05)"
  artifacts:
    - path: "apps/web/lib/admin/notifications-summary.ts"
      provides: "AdminNotifications com publishErrors + instagramAuthError"
      contains: "publishErrors"
    - path: "apps/web/app/admin/page.tsx"
      provides: "Card 'Falhas de publicação' no portal admin"
      contains: "Falhas de publicação"
  key_links:
    - from: "apps/web/lib/admin/notifications-summary.ts"
      to: "social_posts status='erro'"
      via: "countPublishErrors (head:true count)"
      pattern: "status.*erro|'erro'"
    - from: "apps/web/app/admin/page.tsx"
      to: "notif.publishErrors"
      via: "cards[] append + Link href /admin/painel?status=erro"
      pattern: "publishErrors"
---

<objective>
Expor as falhas de publicação na central de notificações existente do `/admin` (D-04): contar posts em `erro` (`publishErrors`) e sinalizar falha de health-check/refresh do token (`instagramAuthError`, D-07). Adicionar o card "Falhas de publicação" no portal. Sucesso é silencioso — nenhuma notificação (D-05).

Purpose: IGPUB-06 (erro visível + reenfileirável) e IGPUB-01/D-07 (alerta só em falha de conexão). Reusa `getAdminNotifications` conforme D-04 manda explicitamente.
Output: `notifications-summary.ts` + card no `admin/page.tsx` + teste estendido.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-publicacao-instagram/12-PATTERNS.md
@.planning/phases/12-01-PLAN.md

<interfaces>
<!-- Analog EXATO (auto-modificação) -->
From apps/web/lib/admin/notifications-summary.ts:
```typescript
export interface AdminNotifications {
  unreadEmails: number; pendingRefunds: number; purchasesToday: number; stuckPending: number
}
export async function getAdminNotifications(): Promise<AdminNotifications> {
  const [unreadEmails, pendingRefunds, purchasesToday, stuckPending] = await Promise.all([
    getUnreadCount().catch(() => 0),
    countPendingRefunds().catch(() => 0),
    countPurchasesToday(...).catch(() => 0),
    countStuckPending(...).catch(() => 0),
  ])
  return { unreadEmails, pendingRefunds, purchasesToday, stuckPending }
}
async function countStuckPending(beforeISO: string): Promise<number> {
  const service = createServiceClient()
  const { count } = await service.from('customer_credits')
    .select('id', { count: 'exact', head: true })
    .eq('status', 'pending').lt('created_at', beforeISO)
  return count ?? 0
}
```

From apps/web/app/admin/page.tsx (cards[] array, ~linha 78-81):
```typescript
{ label: 'Compras travadas (+2h)', count: notif.stuckPending, href: '/admin/relatorios', alert: notif.stuckPending > 0 },
```

<!-- A flag de health do token mora em app_settings; key sugerida 'instagram_health'
     (value jsonb { ok: boolean, checked_at, error? }), escrita pelo cron daily (Plan 05).
     Aqui apenas LEMOS de forma best-effort (default ok=true se ausente). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estender getAdminNotifications com publishErrors + instagramAuthError</name>
  <read_first>
    - apps/web/lib/admin/notifications-summary.ts (arquivo alvo — interface + Promise.all + countStuckPending analog)
    - 12-PATTERNS.md §"apps/web/lib/admin/notifications-summary.ts" (linhas 244-276)
    - 12-01-PLAN.md (status 'erro' já no CHECK + union TS)
  </read_first>
  <behavior>
    - `countPublishErrors()` conta `social_posts` com `status='erro'` (head:true) → número
    - `getAdminNotifications()` retorna `publishErrors` e `instagramAuthError` além dos campos atuais
    - qualquer fonte que falhe vira 0/false (best-effort, nunca quebra o /admin)
    - sucesso de publicação (status 'publicado') NÃO incrementa nada (D-05 silencioso)
  </behavior>
  <action>
Em `apps/web/lib/admin/notifications-summary.ts`:

1. Estender a interface:
```typescript
export interface AdminNotifications {
  unreadEmails: number
  pendingRefunds: number
  purchasesToday: number
  stuckPending: number
  publishErrors: number       // posts em 'erro' (IGPUB-06, D-04)
  instagramAuthError: boolean // falha de health-check/refresh do token IG (D-07)
}
```

2. Adicionar ao `Promise.all` (cada um `.catch` → 0/false):
```typescript
const [..., publishErrors, instagramAuthError] = await Promise.all([
  ...,
  countPublishErrors().catch(() => 0),
  checkInstagramAuthError().catch(() => false),
])
return { unreadEmails, pendingRefunds, purchasesToday, stuckPending, publishErrors, instagramAuthError }
```

3. Helper espelhando `countStuckPending`:
```typescript
async function countPublishErrors(): Promise<number> {
  const service = createServiceClient()
  const { count } = await service
    .from('social_posts')
    .select('id', { count: 'exact', head: true })
    .eq('status', 'erro')
  return count ?? 0
}
```

4. Helper de auth-error (lê a flag de health do app_settings, best-effort; default false se ausente):
```typescript
async function checkInstagramAuthError(): Promise<boolean> {
  const service = createServiceClient()
  const { data } = await service.from('app_settings').select('value')
    .eq('key', 'instagram_health').maybeSingle()
  const v = (data as { value?: { ok?: boolean } } | null)?.value
  return v?.ok === false
}
```
(A escrita de `instagram_health` é responsabilidade do Plan 05 — aqui só leitura defensiva.)

**Nota de ordenação (WARNING 4):** `instagramAuthError` retorna `false` por default ENQUANTO a flag `instagram_health` não existir em `app_settings`. Ela só passa a existir quando o Plan 05 deploya e o `/api/cron/daily` roda pela 1ª vez. Até lá, `false` é o comportamento esperado (não é regressão); o teste do Task 3 cobre explicitamente o caso "flag ausente/`{ ok: true }` → false".
  </action>
  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit -p tsconfig.json 2>&1 | head -5; grep -c "publishErrors\|instagramAuthError\|instagram_health" lib/admin/notifications-summary.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "publishErrors" apps/web/lib/admin/notifications-summary.ts` ≥ 3 (interface + Promise.all + return).
    - `grep -c "instagramAuthError" apps/web/lib/admin/notifications-summary.ts` ≥ 3.
    - `grep -c "status.*'erro'\|'erro'" apps/web/lib/admin/notifications-summary.ts` ≥ 1 (countPublishErrors).
    - `grep -c "\.catch(() => 0)\|\.catch(() => false)" apps/web/lib/admin/notifications-summary.ts` ≥ 6 (todas as fontes best-effort).
    - `pnpm --dir apps/web tsc --noEmit` sem novos erros.
  </acceptance_criteria>
  <done>`AdminNotifications` ganha `publishErrors`+`instagramAuthError`, ambos best-effort; conta posts em 'erro' e lê a flag de health do token.</done>
</task>

<task type="auto">
  <name>Task 2: Card "Falhas de publicação" no portal admin</name>
  <read_first>
    - apps/web/app/admin/page.tsx (cards[] array ~linha 72-82)
    - 12-PATTERNS.md §"apps/web/app/admin/page.tsx" (linhas 280-286)
  </read_first>
  <action>
Em `apps/web/app/admin/page.tsx`, no array `cards`, após "Compras travadas (+2h)", adicionar:
```typescript
{ label: 'Falhas de publicação', count: notif.publishErrors, href: '/admin/painel?status=erro', alert: notif.publishErrors > 0 },
```
E (opcional, alert visual) se `notif.instagramAuthError`, adicionar um card:
```typescript
{ label: 'Conexão Instagram', count: notif.instagramAuthError ? 1 : 0, href: '/admin/painel', alert: notif.instagramAuthError },
```
Manter o grid existente (`grid-cols-2 md:grid-cols-4`) — os cards extras fluem naturalmente.
  </action>
  <verify>
    <automated>cd apps/web && grep -c "Falhas de publicação\|notif.publishErrors" app/admin/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Falhas de publicação" apps/web/app/admin/page.tsx` == 1.
    - `grep -c "notif.publishErrors" apps/web/app/admin/page.tsx` ≥ 1.
    - `grep -c "/admin/painel?status=erro" apps/web/app/admin/page.tsx` == 1.
    - `pnpm --dir apps/web tsc --noEmit` sem novos erros.
  </acceptance_criteria>
  <done>Card "Falhas de publicação" no portal linkando pra fila de erro; alerta visual quando >0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Teste de notifications-summary (publishErrors + auth flag, mockado)</name>
  <read_first>
    - apps/web/lib/admin/notifications-summary.ts (Task 1)
    - 12-VALIDATION.md §"Per-Task Verification Map" (IGPUB-01 health surface)
  </read_first>
  <action>
Criar (ou estender, se já existir) `apps/web/lib/admin/__tests__/notifications-summary.test.ts` (vitest). Mockar `createServiceClient` (stub que responde diferente por `.from('social_posts')` vs `.from('app_settings')`) e `getUnreadCount`.

Casos:
- IGPUB-06: 2 posts em `status='erro'` → `getAdminNotifications().publishErrors === 2`.
- IGPUB-01/D-07: `app_settings.instagram_health.value = { ok: false }` → `instagramAuthError === true`.
- D-07 sadio: `instagram_health` ausente/`{ ok: true }` → `instagramAuthError === false`.
- D-05 silencioso: posts em `status='publicado'` NÃO aparecem em `publishErrors` (só 'erro' conta).
- best-effort: se a query de social_posts lança, `publishErrors === 0` e o restante não quebra.
  </action>
  <verify>
    <automated>cd apps/web && pnpm test:run lib/admin/__tests__/notifications-summary.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo de teste existe; mocka `createServiceClient` (sem rede).
    - Cobre: publishErrors=2, instagramAuthError true/false, 'publicado' não conta, best-effort 0 em erro.
    - `pnpm --dir apps/web test:run lib/admin/__tests__/notifications-summary.test.ts` verde.
  </acceptance_criteria>
  <done>Teste verde cobrindo contagem de erro, flag de auth, sucesso silencioso e best-effort.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /admin (founder session) → notifications-summary (service-role) | leitura agregada de contadores |
| app_settings instagram_health → /admin | sinal de saúde do token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-13 | Denial of Service | uma fonte de notificação falha e derruba o /admin | mitigate | cada fonte tem `.catch(() => 0/false)` — best-effort, nunca quebra o portal |
| T-12-14 | Information Disclosure | card exposto a não-founder | accept | `/admin/page.tsx` já gateia founder (notFound se não-founder) + middleware/layout; sem mudança |
| T-12-15 | Repudiation | falha de token passa despercebida | mitigate | `instagramAuthError` sinalizado no portal (D-07) — não-silencioso |
</threat_model>

<verification>
- `pnpm --dir apps/web test:run lib/admin/__tests__/notifications-summary.test.ts` verde.
- `pnpm --dir apps/web tsc --noEmit` sem novos erros.
- grep confirma card + campos novos.
</verification>

<success_criteria>
- `publishErrors` conta posts em 'erro'; `instagramAuthError` reflete a flag de health.
- Card "Falhas de publicação" no portal linkando pra fila de erro.
- Sucesso de publicação não gera notificação (D-05); falha/auth-error sim.
</success_criteria>

<output>
Após completar, criar `.planning/phases/12-publicacao-instagram/12-04-SUMMARY.md`.
</output>
