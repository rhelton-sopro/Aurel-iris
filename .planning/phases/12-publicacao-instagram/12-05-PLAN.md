---
phase: 12-publicacao-instagram
plan: 05
type: execute
wave: 3
depends_on: ["12-02", "12-03"]
files_modified:
  - apps/web/app/api/cron/instagram-publish/route.ts
  - apps/web/app/api/cron/daily/route.ts
  - apps/web/lib/instagram/token.ts
  - apps/web/app/admin/painel/actions.ts
  - apps/web/app/admin/painel/PostCard.tsx
  - apps/web/vercel.json
  - apps/web/app/api/cron/__tests__/instagram-publish-auth.test.ts
  - apps/web/app/admin/painel/__tests__/reenqueue-action.test.ts
autonomous: false
user_setup:
  - service: instagram
    why: "Publicar no IG do founder via Meta Content Publishing API (token long-lived, dev mode, sem App Review)"
    env_vars:
      - name: INSTAGRAM_APP_ID
        source: "Meta App Dashboard → Settings → Basic → App ID"
      - name: INSTAGRAM_APP_SECRET
        source: "Meta App Dashboard → Settings → Basic → App Secret"
      - name: INSTAGRAM_BUSINESS_ACCOUNT_ID
        source: "GET graph.instagram.com/me?fields=id (após token long-lived) — o id retornado"
    dashboard_config:
      - task: "Converter IG para Professional (Business/Creator); criar app Meta com produto Instagram (Instagram API with Instagram Login); convidar a conta como Instagram Tester e aceitar; OAuth → token curto → ig_exchange_token (60d); semear app_settings.instagram_token"
        location: "developers.facebook.com + instagram.com Settings → Apps and Websites → Tester Invites"
requirements: [IGPUB-01, IGPUB-02, IGPUB-04, IGPUB-06]
must_haves:
  truths:
    - "Um post agendado vencido é publicado automaticamente pelo cron horário, sem ninguém clicar"
    - "O cron rejeita requisições sem o Bearer CRON_SECRET correto (401, fail-closed, timing-safe)"
    - "O botão 'publicar agora' força a publicação imediata reusando o MESMO núcleo do cron (D-08)"
    - "Um post em 'erro' pode ser reenfileirado pelo founder no painel (→ agendado, attempts zerado) (D-04)"
    - "O token do IG é refreshado pelo cron diário; falha de refresh/health sinaliza no /admin (D-07)"
  artifacts:
    - path: "apps/web/app/api/cron/instagram-publish/route.ts"
      provides: "Cron horário GET com CRON_SECRET timing-safe → publishDuePosts()"
      contains: "publishDuePosts"
    - path: "apps/web/vercel.json"
      provides: "cron /api/cron/instagram-publish schedule '0 * * * *'"
      contains: "instagram-publish"
    - path: "apps/web/app/admin/painel/actions.ts"
      provides: "publishNowAction(id) + reenqueuePostAction(id) founder-gated"
      contains: "publishNowAction"
    - path: "apps/web/app/admin/painel/PostCard.tsx"
      provides: "Botões 'Publicar agora' (aprovado/agendado/erro) e 'Reenfileirar' (erro)"
      contains: "publishNowAction"
  key_links:
    - from: "apps/web/app/api/cron/instagram-publish/route.ts"
      to: "lib/instagram/publish.publishDuePosts"
      via: "import + await"
      pattern: "publishDuePosts"
    - from: "apps/web/app/admin/painel/actions.ts"
      to: "lib/instagram/publish.publishPost"
      via: "import + await (founder-gated)"
      pattern: "publishPost"
    - from: "apps/web/app/admin/painel/PostCard.tsx"
      to: "actions.publishNowAction / actions.reenqueuePostAction"
      via: "onClick → server action (useTransition)"
      pattern: "publishNowAction|reenqueuePostAction"
    - from: "apps/web/app/api/cron/daily/route.ts"
      to: "lib/instagram/token.refreshInstagramTokenIfNeeded"
      via: "append ao batch com .catch"
      pattern: "refreshInstagramTokenIfNeeded"
---

<objective>
Ligar o motor: a rota de cron horária (`/api/cron/instagram-publish`) que varre e publica via `publishDuePosts()`, o registro do cron no `vercel.json`, o job de refresh/health do token no cron diário existente (gravando a flag `instagram_health`), a server action `publishNowAction` ("publicar agora", D-08) e a `reenqueuePostAction` (reenfileirar falhas, D-04) — ambas founder-gated reusando o núcleo de publicação — e a UI no painel (`PostCard.tsx`) que expõe os botões "Publicar agora" e "Reenfileirar". Captura também o homework do founder (caminho IG-Login, sem Página FB, tester + bootstrap do token) como pré-requisito manual documentado.

Purpose: IGPUB-02 (cron idempotente automático), IGPUB-01 (refresh/health automáticos + alerta D-07), IGPUB-06 (reenfileiramento de falha pelo founder), D-08 (publicar agora end-to-end com UI). É o que torna o motor autônomo E operável pelo founder.
Output: rota de cron + cron entry + job de token no daily + 2 actions + UI no PostCard + teste de auth do cron + teste do reenqueue + homework documentado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-publicacao-instagram/12-RESEARCH.md
@.planning/phases/12-publicacao-instagram/12-PATTERNS.md
@.planning/phases/12-02-PLAN.md
@.planning/phases/12-03-PLAN.md

<interfaces>
<!-- Cron auth gate — COPIAR EXATO -->
From apps/web/app/api/cron/photo-ttl/route.ts:
```typescript
import { timingSafeEqual } from 'node:crypto'
import { NextResponse, type NextRequest } from 'next/server'
export const runtime = 'nodejs'
export const maxDuration = 120

export async function GET(request: NextRequest): Promise<NextResponse> {
  const secret = process.env.CRON_SECRET
  if (!secret) { return NextResponse.json({ error: 'misconfigured' }, { status: 401 }) }
  const provided = request.headers.get('authorization')?.replace(/^Bearer\s+/, '') ?? ''
  const a = Buffer.from(provided); const b = Buffer.from(secret)
  if (a.length !== b.length || !timingSafeEqual(a, b)) {
    return NextResponse.json({ error: 'unauthorized' }, { status: 401 })
  }
  const result = await purgeExpiredIrisPhotos().catch((err) => ({ ... }))
  return NextResponse.json({ ok: true, ...result })
}
```

<!-- Daily cron batch — append job -->
From apps/web/app/api/cron/daily/route.ts (linhas 45-72): `const results = { reservations: await releaseExpiredReservations().catch(...), ... }`

<!-- Server action founder gate — REUSAR (já existe requireFounder + actions) -->
From apps/web/app/admin/painel/actions.ts:
```typescript
'use server'  // só exporta funções async
import type { ActionResult } from '@/lib/admin/social-posts'
async function requireFounder(): Promise<ActionResult | null> { ... }
export async function schedulePostAction(id, scheduledAtISO): Promise<ActionResult> { ... }
const PAINEL_PATH = '/admin/painel'
```

<!-- UI do painel — PostCard é 'use client' e faz o wiring das actions via run()/useTransition -->
From apps/web/app/admin/painel/PostCard.tsx:
```typescript
'use client'
import { approvePostAction, rejectPostAction, backToPendingAction, schedulePostAction, editCaptionAction, commentPostAction } from './actions'
// helper run(fn, okMsg): startTransition → toast.success/error
function run(fn: () => Promise<ActionResult>, okMsg: string) { ... }
// componente <Actions> faz branch por post.status (pendente/aprovado/agendado/publicado/reprovado)
// <Btn primary|approve|reject> é o botão padrão; <StatusBar icon> é o rótulo de status
// NOTA: após o bump do Plan 01, post.status pode ser 'publicando'|'erro' também; o branch
//       de Actions precisa cobrir esses dois novos estados.
```

<!-- Núcleo (Plan 03) + token (Plan 02) -->
From apps/web/lib/instagram/publish.ts: `export async function publishDuePosts(): Promise<SweepResult>`, `export async function publishPost(id: string): Promise<PublishResult>`
From apps/web/lib/instagram/token.ts: `export async function refreshInstagramTokenIfNeeded()`, `export async function instagramHealthCheck()`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rota de cron horária /api/cron/instagram-publish + entry no vercel.json</name>
  <read_first>
    - apps/web/app/api/cron/photo-ttl/route.ts (copiar quase verbatim — auth gate idêntico)
    - apps/web/vercel.json (crons[] array)
    - apps/web/lib/instagram/publish.ts (publishDuePosts)
    - 12-PATTERNS.md §"api/cron/instagram-publish/route.ts" (linhas 26-67) e §"vercel.json" (linhas 290-299)
    - 12-RESEARCH.md §"Vercel Cron" (linhas 321-327)
  </read_first>
  <behavior>
    - GET sem Bearer ou com Bearer errado → 401
    - CRON_SECRET ausente → 401 (misconfigured), nunca aceita "Bearer undefined"
    - GET com Bearer correto → chama publishDuePosts() e retorna { ok: true, ...result }
  </behavior>
  <action>
Criar `apps/web/app/api/cron/instagram-publish/route.ts` copiando `photo-ttl/route.ts`. Trocar:
- import → `import { publishDuePosts } from '@/lib/instagram/publish'`
- `export const maxDuration = 300` (poll do reel é bounded internamente; PRO permite até 800s).
- log tag → `[cron-instagram-publish]`.
- worker → `const result = await publishDuePosts().catch((err) => ({ published: 0, retried: 0, failed: 0, errors: 1, error: String(err) }))`.
- NUNCA logar token (o publish.ts já cuida).

Em `apps/web/vercel.json`, no array `crons`, ANEXAR (manter os 2 existentes):
```json
{ "path": "/api/cron/instagram-publish", "schedule": "0 * * * *" }
```
  </action>
  <verify>
    <automated>cd apps/web && grep -c "publishDuePosts\|timingSafeEqual\|maxDuration = 300" app/api/cron/instagram-publish/route.ts; grep -c "instagram-publish" vercel.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "timingSafeEqual" apps/web/app/api/cron/instagram-publish/route.ts` ≥ 1.
    - `grep -c "publishDuePosts" apps/web/app/api/cron/instagram-publish/route.ts` ≥ 1.
    - `grep -c "runtime = 'nodejs'" apps/web/app/api/cron/instagram-publish/route.ts` == 1.
    - `grep -c "instagram-publish" apps/web/vercel.json` == 1 e o schedule é `"0 * * * *"`.
    - vercel.json continua com os 2 crons anteriores (daily + photo-ttl) — `grep -c "/api/cron/" apps/web/vercel.json` == 3.
  </acceptance_criteria>
  <done>Rota de cron horária com auth timing-safe → publishDuePosts; registrada no vercel.json sem remover os crons existentes.</done>
</task>

<task type="auto">
  <name>Task 2: Job de refresh/health do token no cron diário + flag instagram_health</name>
  <read_first>
    - apps/web/app/api/cron/daily/route.ts (batch com .catch por job — linhas 45-72)
    - apps/web/lib/instagram/token.ts (refreshInstagramTokenIfNeeded, instagramHealthCheck, INSTAGRAM_TOKEN_KEY)
    - 12-PATTERNS.md §"api/cron/daily/route.ts" (linhas 176-188)
    - 12-RESEARCH.md §"Token Lifecycle" — "piggyback on /api/cron/daily"
  </read_first>
  <action>
1. Em `apps/web/lib/instagram/token.ts`, adicionar uma função que combina refresh + health e PERSISTE a flag de saúde em `app_settings` key `instagram_health` (lida pelo Plan 04):
```typescript
export async function refreshAndHealthcheckInstagram(): Promise<{ refreshed: boolean; healthy: boolean; error?: string }> {
  const refresh = await refreshInstagramTokenIfNeeded()
  const health = await instagramHealthCheck()
  // persistir flag pro /admin (D-07): upsert app_settings key 'instagram_health'
  const service = createServiceClient()
  await service.from('app_settings').upsert(
    { key: 'instagram_health', value: { ok: health.ok, checked_at: new Date().toISOString(), error: health.error ?? null }, updated_at: new Date().toISOString() },
    { onConflict: 'key' },
  )
  return { refreshed: refresh.refreshed, healthy: health.ok, error: refresh.error ?? health.error }
}
```
(NUNCA logar token; persiste só ok/checked_at/error.)

2. Em `apps/web/app/api/cron/daily/route.ts`, ANEXAR ao objeto `results`:
```typescript
instagram: await refreshAndHealthcheckInstagram().catch((err) => ({ refreshed: false, healthy: false, error: String(err) })),
```
e importar `refreshAndHealthcheckInstagram` de `@/lib/instagram/token`.

**Nota de ordenação (D-07):** a flag `instagram_health` só passa a existir em `app_settings` quando o cron diário roda pela 1ª vez APÓS este deploy. Até lá, o Plan 04 lê a flag ausente e `instagramAuthError` fica `false` por default (comportamento esperado — o teste do Plan 04 cobre o caso "flag ausente → false"). Não é regressão.
  </action>
  <verify>
    <automated>cd apps/web && grep -c "refreshAndHealthcheckInstagram\|instagram_health" lib/instagram/token.ts; grep -c "refreshAndHealthcheckInstagram\|instagram:" app/api/cron/daily/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "refreshAndHealthcheckInstagram" apps/web/lib/instagram/token.ts` ≥ 1.
    - `grep -c "instagram_health" apps/web/lib/instagram/token.ts` ≥ 1 (upsert da flag).
    - `grep -c "refreshAndHealthcheckInstagram" apps/web/app/api/cron/daily/route.ts` ≥ 1 e está dentro de um `.catch(...)`.
    - Os 5 jobs anteriores do daily continuam presentes (`grep -c "await " apps/web/app/api/cron/daily/route.ts` ≥ 6).
    - `pnpm --dir apps/web tsc --noEmit` sem novos erros.
  </acceptance_criteria>
  <done>Cron diário refresha o token (buffer 10d) + faz health-check e grava a flag instagram_health pro /admin; jobs existentes intactos.</done>
</task>

<task type="auto">
  <name>Task 3: Server actions publishNowAction (D-08) + reenqueuePostAction (D-04)</name>
  <read_first>
    - apps/web/app/admin/painel/actions.ts (requireFounder + schedulePostAction + 'use server' hygiene + helper patch)
    - apps/web/lib/instagram/publish.ts (publishPost)
    - 12-PATTERNS.md §"app/admin/painel/actions.ts" (linhas 192-225)
  </read_first>
  <action>
Em `apps/web/app/admin/painel/actions.ts` (arquivo `'use server'` — só exporta funções async), adicionar DUAS actions:

1. **publishNowAction** (D-08) — força publicação imediata reusando o núcleo do cron:
```typescript
import { publishPost } from '@/lib/instagram/publish'

/** "Publicar agora" (D-08): força publicação imediata reusando o núcleo do cron. */
export async function publishNowAction(id: string): Promise<ActionResult> {
  const denied = await requireFounder()
  if (denied) return denied
  if (!id || !/^[0-9a-f-]{36}$/i.test(id)) return { ok: false, error: 'Post inválido.' }
  const result = await publishPost(id)
  revalidatePath(PAINEL_PATH)
  return result.ok ? { ok: true } : { ok: false, error: result.error }
}
```
Validar id como uuid (regex acima) antes de chamar (RESEARCH §Security V5). NÃO usa CRON_SECRET — roda no contexto autenticado do founder via `requireFounder()`.

2. **reenqueuePostAction** (D-04, IGPUB-06) — reenfileira um post em `erro` (→ `agendado`, zera tentativas, limpa motivo) para que o próximo sweep do cron (ou "publicar agora") tente de novo do zero. Só age sobre `status='erro'` (defesa: o `.eq('status','erro')` evita resetar tentativas de outro estado):
```typescript
/**
 * Reenfileira um post que falhou (D-04, IGPUB-06): de 'erro' → 'agendado',
 * zerando publish_attempts e limpando publish_error. Founder-gated.
 */
export async function reenqueuePostAction(id: string): Promise<ActionResult> {
  const denied = await requireFounder()
  if (denied) return denied
  if (!id) return { ok: false, error: 'Post inválido.' }
  const service = createServiceClient()
  const { error } = await service
    .from('social_posts')
    .update({ status: 'agendado', publish_attempts: 0, publish_error: null, updated_at: new Date().toISOString() })
    .eq('id', id)
    .eq('status', 'erro')
  if (error) return { ok: false, error: error.message }
  revalidatePath(PAINEL_PATH)
  return { ok: true }
}
```
(NOTA: `reenqueuePostAction` NÃO reusa o helper `patch` porque precisa do segundo `.eq('status','erro')` como guarda; escrever o UPDATE direto. `createServiceClient` já é importado no arquivo.)
  </action>
  <verify>
    <automated>cd apps/web && grep -c "publishNowAction\|reenqueuePostAction\|publishPost\|requireFounder" app/admin/painel/actions.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function publishNowAction" apps/web/app/admin/painel/actions.ts` == 1.
    - `grep -c "export async function reenqueuePostAction" apps/web/app/admin/painel/actions.ts` == 1.
    - `grep -c "requireFounder" apps/web/app/admin/painel/actions.ts` ≥ 3 (gate reusado nas 2 novas actions + existentes).
    - `grep -c "publishPost" apps/web/app/admin/painel/actions.ts` ≥ 1.
    - `grep -c "\.eq('status', 'erro')\|\.eq(\"status\", \"erro\")" apps/web/app/admin/painel/actions.ts` ≥ 1 (guarda do reenqueue).
    - validação de uuid presente em publishNowAction (`grep -c "test(id)\|36" ...` ≥ 1).
    - `'use server'` ainda só exporta funções async — `grep -c "^export const\|^export type\|^export interface" apps/web/app/admin/painel/actions.ts` == 0.
    - `pnpm --dir apps/web tsc --noEmit` sem novos erros.
  </acceptance_criteria>
  <done>`publishNowAction` (uuid-validada) e `reenqueuePostAction` (guarda `status='erro'`) founder-gated; reusam o núcleo/serviço; revalidam o painel; higiene 'use server' preservada.</done>
</task>

<task type="auto">
  <name>Task 4: UI no painel — botões "Publicar agora" e "Reenfileirar" no PostCard</name>
  <read_first>
    - apps/web/app/admin/painel/PostCard.tsx (componente 'use client' — helper run(), <Actions> branch por status, <Btn>)
    - apps/web/app/admin/painel/actions.ts (publishNowAction, reenqueuePostAction — Task 3)
    - apps/web/lib/admin/social-posts.ts (SocialPostStatus com 'publicando'|'erro' após Plan 01)
  </read_first>
  <action>
Em `apps/web/app/admin/painel/PostCard.tsx` (já é `'use client'`), wirar as novas actions seguindo o padrão existente (`run(fn, okMsg)` + `useTransition` + `toast`):

1. Importar de `./actions`: `publishNowAction`, `reenqueuePostAction` (juntar aos imports existentes).

2. Passar dois novos handlers ao `<Actions>` (como os atuais `onApprove`/`onReject`/...):
   - `onPublishNow={() => run(() => publishNowAction(post.id), 'Publicando agora…')}`
   - `onReenqueue={() => run(() => reenqueuePostAction(post.id), 'Reenfileirado.')}`
   Adicionar as duas props na assinatura do componente `Actions` (e nos tipos).

3. No componente `Actions`, ajustar os branches por `post.status`:
   - **`aprovado`**: adicionar `<Btn primary onClick={onPublishNow}>… Publicar agora</Btn>` (além de Agendar/Editar/Voltar). Use o ícone `Send` (já importado).
   - **`agendado`**: adicionar `<Btn onClick={onPublishNow}>Publicar agora</Btn>` ao lado de "Voltar p/ pendente".
   - **NOVO branch `erro`**: `<StatusBar icon={<X .../>}>Falhou</StatusBar>` + (se houver) `post.publish_error` num texto auxiliar muted + `<Btn primary onClick={onReenqueue}>Reenfileirar</Btn>` + `<Btn onClick={onPublishNow}>Publicar agora</Btn>` + `<Btn onClick={onBack}>Voltar p/ pendente</Btn>`.
   - **NOVO branch `publicando`**: `<StatusBar icon={<Loader2 className="h-4 w-4 animate-spin" />}>Publicando…</StatusBar>` (sem botões de ação — está em andamento).
   - Manter `publicado`: se `post.ig_permalink`, virar link "Ver no Instagram" (opcional, mínimo: manter StatusBar "Publicado").

Reusar `<Btn>` e `<StatusBar>` existentes; não criar novos estilos. Os botões disparam `disabled={isPending}` como os demais.
  </action>
  <verify>
    <automated>cd apps/web && grep -c "publishNowAction\|reenqueuePostAction\|onPublishNow\|onReenqueue" app/admin/painel/PostCard.tsx; pnpm tsc --noEmit -p tsconfig.json 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "publishNowAction" apps/web/app/admin/painel/PostCard.tsx` ≥ 1 e `grep -c "reenqueuePostAction" ...` ≥ 1.
    - `grep -c "Publicar agora" apps/web/app/admin/painel/PostCard.tsx` ≥ 1 e `grep -c "Reenfileirar" ...` ≥ 1.
    - branches `erro` e `publicando` presentes no `Actions` (`grep -c "'erro'\|'publicando'" apps/web/app/admin/painel/PostCard.tsx` ≥ 2).
    - `pnpm --dir apps/web tsc --noEmit` sem novos erros (o switch de status cobre os 7 estados → sem warning de exhaustiveness).
  </acceptance_criteria>
  <done>PostCard mostra "Publicar agora" em aprovado/agendado/erro e "Reenfileirar" em erro; estados `publicando`/`erro` renderizam; wiring via run()/useTransition como as demais ações.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Teste do reenqueuePostAction (service-role mockado)</name>
  <read_first>
    - apps/web/app/admin/painel/actions.ts (reenqueuePostAction — Task 3)
    - 12-VALIDATION.md §"Per-Task Verification Map" (12-05 T4 row) + §"Wave 0 Requirements"
    - qualquer teste existente que mocke `createServiceClient` (ex.: notifications-summary.test.ts do Plan 04, ou cron-jobs)
  </read_first>
  <action>
Criar `apps/web/app/admin/painel/__tests__/reenqueue-action.test.ts` (vitest). Mockar `@/lib/supabase/server` (`createClient` → user founder via `auth.getUser`), `@/lib/auth/founder` (`isFounderEmail` → true), `@/lib/supabase/service` (`createServiceClient` → stub com `.from().update().eq().eq()` encadeável que captura o payload), e `next/cache` (`revalidatePath` noop).

Casos:
- D-04 reset: chamar `reenqueuePostAction('<uuid>')` → o UPDATE capturado contém `status: 'agendado'`, `publish_attempts: 0`, `publish_error: null`; e há DOIS `.eq` (id + status='erro').
- guarda de estado: assert que o segundo `.eq` é `('status', 'erro')` (só reenfileira falhas).
- não-founder: `isFounderEmail` → false → `{ ok: false }` e o UPDATE NÃO é chamado.
- id vazio: `reenqueuePostAction('')` → `{ ok: false }`, sem UPDATE.

(Se preferir, estender este mesmo arquivo no futuro para publishNowAction; o escopo mínimo aqui é o reset do reenqueue, que é o BLOCKER.)
  </action>
  <verify>
    <automated>cd apps/web && pnpm test:run app/admin/painel/__tests__/reenqueue-action.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe; mocka `createServiceClient`, `createClient`, `isFounderEmail` (sem rede/DB).
    - Cobre: reset (agendado/0/null), guarda status='erro', não-founder bloqueado, id vazio.
    - `grep -c "publish_attempts\|'agendado'\|'erro'" apps/web/app/admin/painel/__tests__/reenqueue-action.test.ts` ≥ 2.
    - `pnpm --dir apps/web test:run app/admin/painel/__tests__/reenqueue-action.test.ts` verde.
  </acceptance_criteria>
  <done>Teste verde: reenqueue reseta status/attempts/error só sobre 'erro', founder-gated, valida id.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Teste de auth do cron (Bearer correto/errado/ausente)</name>
  <read_first>
    - apps/web/app/api/cron/instagram-publish/route.ts (Task 1)
    - apps/web/app/api/cron/photo-ttl/route.ts (gate de referência)
    - 12-VALIDATION.md §"Per-Task Verification Map" (cron auth → 401)
  </read_first>
  <action>
Criar `apps/web/app/api/cron/__tests__/instagram-publish-auth.test.ts` (vitest). Mockar `@/lib/instagram/publish` (`publishDuePosts` → `{ published: 0, retried: 0, failed: 0, errors: 0 }`) e setar `process.env.CRON_SECRET = 'sekret'`.

Casos:
- sem header Authorization → 401.
- `Authorization: Bearer wrong` → 401.
- `CRON_SECRET` ausente (delete env) → 401 (misconfigured), `publishDuePosts` NÃO chamado.
- `Authorization: Bearer sekret` → 200, `publishDuePosts` chamado 1×.

Construir um `NextRequest` (ou um stub com `.headers.get('authorization')`) e chamar `GET(req)`.
  </action>
  <verify>
    <automated>cd apps/web && pnpm test:run app/api/cron/__tests__/instagram-publish-auth.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe; mocka `publishDuePosts` (sem rede/DB).
    - Cobre: sem-header→401, bearer-errado→401, secret-ausente→401, bearer-correto→200.
    - `pnpm --dir apps/web test:run app/api/cron/__tests__/instagram-publish-auth.test.ts` verde.
  </acceptance_criteria>
  <done>Teste de auth do cron verde: fail-closed em todos os caminhos sem o Bearer correto; 200 + worker chamado no caminho feliz.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 7: [HOMEWORK] Bootstrap da conexão Meta/Instagram (pré-requisito de runtime)</name>
  <action>Founder executa o bootstrap manual da conexão Meta/Instagram (conta Professional, app Meta, Instagram Tester, OAuth → token long-lived, env Vercel, seed do app_settings.instagram_token) e o smoke E2E. Passos exatos em how-to-verify. Não-autônomo: depende de conta/credencial do founder.</action>
  <what-built>Toda a infra de código de publicação (cron, núcleo, token, actions, UI) está pronta. Falta a conexão viva com o IG — só o founder pode fazer (credencial/conta).</what-built>
  <how-to-verify>
    Passos exatos (RESEARCH §"Token Lifecycle", caminho **Instagram API with Instagram Login** — SEM Página do Facebook, dev mode, SEM App Review):
    1. Converter o IG do founder para **Professional** (Business ou Creator).
    2. Criar app em developers.facebook.com → adicionar produto **"Instagram" (Instagram API with Instagram Login)**.
    3. App Dashboard → **App roles → Roles → Instagram Testers** → convidar o @username do founder; aceitar em instagram.com → **Settings → Apps and Websites → Tester Invites**. (Isto libera publicar na PRÓPRIA conta em Development mode SEM App Review.)
    4. OAuth uma vez: `https://www.instagram.com/oauth/authorize?client_id={APP_ID}&redirect_uri={URI}&response_type=code&scope=instagram_business_basic,instagram_business_content_publish` → trocar o code em `POST https://api.instagram.com/oauth/access_token` (token curto ~1h).
    5. Curto → longo: `GET https://graph.instagram.com/access_token?grant_type=ig_exchange_token&client_secret={SECRET}&access_token={SHORT}` (~60d).
    6. `GET https://graph.instagram.com/me?fields=id,username&access_token={LONG}` → guardar `id`.
    7. Setar env Vercel (todas Sensitive → usar `echo`→`env add`): `INSTAGRAM_APP_ID`, `INSTAGRAM_APP_SECRET`, `INSTAGRAM_BUSINESS_ACCOUNT_ID` (= o `id` do passo 6).
    8. Semear `app_settings.instagram_token` com `{ access_token, expires_at, obtained_at, last_refresh_at: null }` via service-role (script one-off ou ação admin).
    9. Confirmar os assets de mídia das seed posts respondem HTTP 200 + content-type correto nas URLs públicas `iriscodex.com/...` (RESEARCH Pitfall 1; PNG vs JPEG é Open Question — testar empiricamente no smoke).
    10. Smoke (validação canônica E2E, D-08): clicar "publicar agora" num post carrossel aprovado e num reel aprovado → conferir que aparecem no feed real do IG com a caption correta e que `social_posts` gravou `ig_permalink`/`ig_media_id` + status `publicado`. Forçar um erro (ex.: URL quebrada) e conferir que o post cai em `erro` e que "Reenfileirar" o devolve a `agendado`.
  </how-to-verify>
  <resume-signal>Digite "conectado" após o smoke (carrossel + reel) publicar de verdade no IG e o permalink ficar gravado, ou descreva o erro (ex.: PNG rejeitado → converter pra JPEG).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet → /api/cron/instagram-publish | qualquer um pode tentar POST/GET na rota pública |
| founder session → publishNowAction / reenqueuePostAction | mutação que dispara publicação real / reset de estado |
| Vercel cron → daily route | refresh do token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-16 | Elevation of Privilege / Tampering | abuso da rota de cron (publicar sem autorização) | mitigate | `CRON_SECRET` Bearer fail-closed + `timingSafeEqual` (copiado de photo-ttl); GET-only; testado no Task 6 |
| T-12-17 | Elevation of Privilege | não-founder dispara "publicar agora"/"reenfileirar" | mitigate | `requireFounder()` no início de `publishNowAction` e `reenqueuePostAction` (defense-in-depth sobre middleware/layout); testado no Task 5 |
| T-12-18 | Input Validation | id arbitrário/injeção em publishNowAction | mitigate | regex uuid antes de chamar publishPost (V5); reenqueue guarda `.eq('status','erro')` |
| T-12-19 | Information Disclosure | token vazando em log do cron/daily | mitigate | jobs logam só contadores/status; flag instagram_health persiste só ok/checked_at/error — nunca o token |
| T-12-20 | Denial of Service | refresh falho aborta o batch diário | mitigate | `.catch` por job no daily (padrão existente) — falha de IG não derruba os outros jobs; surfacia via instagram_health (D-07) |
</threat_model>

<verification>
- `pnpm --dir apps/web test:run app/api/cron/__tests__/instagram-publish-auth.test.ts` verde.
- `pnpm --dir apps/web test:run app/admin/painel/__tests__/reenqueue-action.test.ts` verde.
- `pnpm --dir apps/web test:run` (suite completa) verde + `pnpm --dir apps/web tsc --noEmit` limpo.
- `pnpm --dir apps/web` build (lint gate) passa antes de push (regra de memória: tests → lint → push; SEM auto-push).
- vercel.json com 3 crons; daily com job instagram; actions founder-gated; PostCard com botões.
- Homework do founder concluído (smoke carrossel+reel publica no IG real, permalink gravado; reenfileiramento de erro funciona).
</verification>

<success_criteria>
- Cron horário publica posts agendados vencidos automaticamente, idempotente (IGPUB-02).
- "Publicar agora" reusa o mesmo núcleo, founder-gated, com UI no painel (D-08).
- "Reenfileirar" devolve um post em `erro` para `agendado` (attempts zerado), founder-gated, com UI (D-04, IGPUB-06).
- Token refreshado + health-checado pelo cron diário; falha sinaliza no /admin (IGPUB-01, D-07).
- Smoke E2E real (founder) confirma carrossel + reel no feed com permalink gravado + reenfileiramento de erro.
</success_criteria>

<output>
Após completar, criar `.planning/phases/12-publicacao-instagram/12-05-SUMMARY.md`.
</output>
</output>
