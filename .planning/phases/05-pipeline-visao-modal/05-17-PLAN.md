---
phase: 05-pipeline-visao-modal
plan: 17
type: execute
wave: 3
depends_on: [05-10, 05-11, 05-12]
files_modified:
  - vision-service/README.md
  - vision-service/.env.example
  - apps/web/.env.example
autonomous: true
requirements: [VISION-01, VISION-04]
tags: [vision, smoke, manual-test, env, documentation]

must_haves:
  goal: "Founder has a runnable, copy-paste-friendly smoke procedure documented in `vision-service/README.md` covering: install Python deps → `modal token set` → `modal deploy modal_app.py` → set Vercel envs → trigger one fixture reading via curl → observe webhook callback in Vercel logs → validate the resulting `readings.vision_features` jsonb in Supabase studio against SPEC §4.3. Both `apps/web/.env.example` and `vision-service/.env.example` document every Modal env var (with one-line comment per var) so a fresh clone can be brought online without referring to STATE.md or external notes."
  truths:
    - "`vision-service/README.md` exists and contains a section heading `## Smoke procedure (founder)` (or equivalent — heading text is locked verbatim) with at least 7 numbered steps."
    - "Step 1 documents `cd vision-service && pip install -r requirements.txt` (CPU-only deps; matches CI install per 05-15)."
    - "Step 2 documents `modal token set --token-id <id> --token-secret <secret>` (one-time auth; tokens come from https://modal.com/settings/tokens)."
    - "Step 3 documents `modal deploy modal_app.py` and instructs founder to capture the printed endpoint URL (e.g. `https://<workspace>--aurel-iris-vision-analyze-iris-endpoint.modal.run`)."
    - "Step 4 documents setting `MODAL_ANALYZE_ENDPOINT_URL` in Vercel project envs for BOTH Production AND Preview environments via `vercel env add MODAL_ANALYZE_ENDPOINT_URL` or the Vercel dashboard."
    - "Step 5 documents how to trigger one fixture reading: `curl -X POST https://<vercel-preview>/api/readings/<reading_id>/process -H 'Cookie: <session-cookie>'` — with a sub-bullet on how to obtain the session cookie (sign in to /leituras, copy from DevTools → Application → Cookies)."
    - "Step 6 documents observing the webhook callback in Vercel logs (`vercel logs <project> --follow` or dashboard → Deployments → Functions logs) and the expected log line `[webhook] applied reading=<id> status=ready call_id=fc-...` (matches 05-12's log format)."
    - "Step 7 documents validating `readings.vision_features` jsonb in Supabase studio: SQL query template `SELECT id, status, vision_features FROM readings WHERE id = '<reading_id>';` plus a checklist mapping the result columns to SPEC §4.3 (right_eye, left_eye, asymmetry_notes, processing_metadata)."
    - "`apps/web/.env.example` Modal block contains all 4 vars in this order with one-line `#` comment above each: `MODAL_TOKEN_ID`, `MODAL_TOKEN_SECRET`, `MODAL_WEBHOOK_SECRET`, `MODAL_ANALYZE_ENDPOINT_URL` (last one was bumped by 05-11 — this plan only adds comments)."
    - "`vision-service/.env.example` exists and contains: `MODAL_WEBHOOK_SECRET=` (used by Modal worker to sign callbacks — must match Next.js side) and `WEBHOOK_BASE_URL=` (where the worker POSTs back; e.g. `https://aurel-iris.vercel.app`). Each var has a one-line `#` comment explaining its purpose."
    - "README documents the cross-cutting principle 'Modal worker uses signed URLs only — no Supabase service role inside Modal container' (D-T6 + RESEARCH Anti-Patterns) so a future implementer doesn't accidentally ship Supabase creds into the container image."
    - "Modal worker uses signed URLs only — no Supabase service role in Modal container."
    - "JSON output conforms to SPEC §4.3 schema (validated by Pydantic IrisFeatures upstream; smoke step 7 confirms it landed correctly in DB)."
  artifacts:
    - path: vision-service/README.md
      provides: "founder-runnable smoke procedure for the full Phase 5 pipeline"
      contains: "## Smoke procedure"
    - path: vision-service/.env.example
      provides: ".env template for the Modal worker side (MODAL_WEBHOOK_SECRET + WEBHOOK_BASE_URL)"
      contains: "MODAL_WEBHOOK_SECRET"
    - path: apps/web/.env.example
      provides: "documented Modal block (4 vars with comments)"
      contains: "MODAL_ANALYZE_ENDPOINT_URL"
  key_links:
    - from: vision-service/README.md
      to: vision-service/modal_app.py
      via: "modal deploy modal_app.py"
      pattern: "modal deploy"
    - from: vision-service/.env.example
      to: apps/web/.env.example
      via: "MODAL_WEBHOOK_SECRET must match between both sides"
      pattern: "MODAL_WEBHOOK_SECRET"
    - from: vision-service/README.md
      to: apps/web/app/api/readings/[id]/process/route.ts
      via: "curl -X POST /api/readings/<id>/process"
      pattern: "/api/readings/.*/process"
---

<objective>
Close Phase 5 with a self-contained founder smoke procedure that exercises the full pipeline end-to-end on Modal cloud — explicitly OUT of CI per D-X2 (Modal cloud test is manual). Without this, the founder would have to reverse-engineer the wiring from STATE.md or piece it together from individual plan SUMMARYs every time a deploy is needed.

This plan ships:

- A README section in `vision-service/README.md` with 7 numbered steps covering install → deploy → trigger → observe → validate.
- An updated `apps/web/.env.example` with one-line comments above each Modal env var (no behavior change; pure documentation polish — the variable list itself was finalized by 05-11).
- A new `vision-service/.env.example` documenting the worker-side env vars (`MODAL_WEBHOOK_SECRET` shared with Next.js side, `WEBHOOK_BASE_URL` pointing at the deployed Vercel project).
- Inline commentary in the README about the LGPD-relevant principle "no Supabase service role inside Modal container" so the architectural decision survives onboarding handoffs.

Honors D-X2 (manual smoke, not CI-automated), D-T6 (signed URLs only — documented in README), and the cross-cutting "founder can verify the entire phase end-to-end without external help" success criterion of the milestone v1.0.

Output: a fresh clone of the repo + access to Modal/Vercel/Supabase dashboards is the only prerequisite; everything else is in the README. The smoke procedure becomes the standing acceptance test for any future Modal redeploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-pipeline-visao-modal/05-CONTEXT.md
@.planning/phases/05-pipeline-visao-modal/05-RESEARCH.md
@apps/web/.env.example
@SPEC.md

<interfaces>
Existing apps/web/.env.example Modal block (post-05-11):
```
# Modal (Phase 5+)
MODAL_TOKEN_ID=
MODAL_TOKEN_SECRET=
MODAL_WEBHOOK_SECRET=
MODAL_ANALYZE_ENDPOINT_URL=
```

Target apps/web/.env.example Modal block (this plan adds per-var comments):
```
# Modal (Phase 5+)
# Modal account-level credentials. Get from https://modal.com/settings/tokens
MODAL_TOKEN_ID=
MODAL_TOKEN_SECRET=
# Shared HMAC secret. Sign on the Modal worker side, verify in /api/vision/webhook.
# Generate once with `openssl rand -hex 32` and set IDENTICALLY in apps/web AND vision-service envs.
MODAL_WEBHOOK_SECRET=
# URL printed by `modal deploy modal_app.py` (workspace-prefixed). Set after smoke step 3.
MODAL_ANALYZE_ENDPOINT_URL=
```

Target vision-service/.env.example:
```
# vision-service/.env.example
# The Modal container reads from this file when running locally (modal serve).
# Production deploys (modal deploy) read from Modal Secrets — set the same vars
# via `modal secret create aurel-iris-vision MODAL_WEBHOOK_SECRET=... WEBHOOK_BASE_URL=...`.
#
# Shared HMAC secret. MUST match apps/web/.env MODAL_WEBHOOK_SECRET exactly.
MODAL_WEBHOOK_SECRET=
# Base URL of the deployed Next.js app (no trailing slash).
# Production: https://aurel-iris.vercel.app
# Preview:    https://aurel-iris-<branch>-rhelton.vercel.app
WEBHOOK_BASE_URL=
```

Smoke procedure structure (locked sequence — README MUST mirror this):
1. Install Python deps (CPU-only)
2. `modal token set` (one-time auth with Modal dashboard tokens)
3. `modal deploy modal_app.py` → capture endpoint URL
4. Set `MODAL_ANALYZE_ENDPOINT_URL` in Vercel envs (Production + Preview)
5. Trigger one fixture reading via `curl` (with session cookie obtained from /leituras DevTools)
6. Observe webhook callback in Vercel logs (`[webhook] applied reading=<id> status=ready ...`)
7. Validate `readings.vision_features` jsonb in Supabase studio (SQL + SPEC §4.3 checklist)

Cross-cutting principle to document inline: "The Modal worker NEVER receives Supabase service-role credentials. It only consumes time-limited signed URLs (TTL=600s — D-T6) for the 6 input images and POSTs results back via HMAC-signed webhook. This is the architectural floor for LGPD compliance."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update apps/web/.env.example with per-var comments for Modal block</name>
  <read_first>
    - apps/web/.env.example (full file — current state post-05-11; preserve all existing lines and ordering)
    - .planning/phases/05-pipeline-visao-modal/05-11-PLAN.md (lines 116-122 — confirm MODAL_ANALYZE_ENDPOINT_URL was the last addition)
  </read_first>
  <files>apps/web/.env.example</files>
  <action>
    SURGICAL EDIT — preserve all non-Modal sections verbatim. Replace ONLY the Modal block (currently 4 lines: a comment + 3 vars from Phase 1, plus MODAL_ANALYZE_ENDPOINT_URL added by 05-11) with:

    ```
    # Modal (Phase 5+)
    # Modal account-level credentials. Get from https://modal.com/settings/tokens
    MODAL_TOKEN_ID=
    MODAL_TOKEN_SECRET=
    # Shared HMAC secret. Sign on the Modal worker side, verify in /api/vision/webhook.
    # Generate once with `openssl rand -hex 32` and set IDENTICALLY in apps/web AND vision-service envs.
    MODAL_WEBHOOK_SECRET=
    # URL printed by `modal deploy modal_app.py` (workspace-prefixed). Set after smoke step 3 (vision-service/README.md).
    MODAL_ANALYZE_ENDPOINT_URL=
    ```

    Do NOT touch:
    - The Supabase, Anthropic, Voyage, Stripe, Resend sections.
    - The `NEXT_PUBLIC_SITE_URL` line.
    - Any leading comments at the top of the file.

    Verify exactly the 4 Modal env var names appear (no more, no less) and each is preceded by a `#` comment line on the line directly above it.
  </action>
  <verify>
    <automated>grep -c "^MODAL_TOKEN_ID=" apps/web/.env.example && grep -c "^MODAL_TOKEN_SECRET=" apps/web/.env.example && grep -c "^MODAL_WEBHOOK_SECRET=" apps/web/.env.example && grep -c "^MODAL_ANALYZE_ENDPOINT_URL=" apps/web/.env.example && grep -c "Get from https://modal.com/settings/tokens" apps/web/.env.example</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^MODAL_TOKEN_ID=" apps/web/.env.example` returns `1`.
    - `grep -c "^MODAL_TOKEN_SECRET=" apps/web/.env.example` returns `1`.
    - `grep -c "^MODAL_WEBHOOK_SECRET=" apps/web/.env.example` returns `1`.
    - `grep -c "^MODAL_ANALYZE_ENDPOINT_URL=" apps/web/.env.example` returns `1`.
    - `grep -c "Get from https://modal.com/settings/tokens" apps/web/.env.example` returns `1` (per-var comment for token vars).
    - `grep -c "openssl rand -hex 32" apps/web/.env.example` returns `1` (HMAC secret generation hint).
    - `grep -c "modal deploy modal_app.py" apps/web/.env.example` returns `1` (cross-ref to smoke step 3).
    - `grep -c "^# Supabase\|^# Anthropic\|^# Voyage\|^# Stripe\|^# Resend" apps/web/.env.example` returns `5` (non-Modal sections preserved).
    - `cd apps/web && pnpm audit:vocabulary` exits 0.
  </acceptance_criteria>
  <done>Modal block of apps/web/.env.example documents all 4 vars with one-line comments. Other sections untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Create vision-service/.env.example</name>
  <read_first>
    - vision-service/ directory listing (confirm whether `.env.example` already exists — if it does, read it first and preserve any existing content not covered here)
    - apps/web/.env.example (Task 1 — to align MODAL_WEBHOOK_SECRET commentary)
  </read_first>
  <files>vision-service/.env.example</files>
  <action>
    Create (or extend if file exists) `vision-service/.env.example`:

    ```
    # vision-service/.env.example
    # The Modal container reads these vars when running locally (modal serve).
    # Production deploys (modal deploy) read from Modal Secrets — set the same vars
    # via:
    #   modal secret create aurel-iris-vision \
    #     MODAL_WEBHOOK_SECRET=$(openssl rand -hex 32) \
    #     WEBHOOK_BASE_URL=https://aurel-iris.vercel.app
    # and ensure modal_app.py decorates the function with `secrets=[modal.Secret.from_name("aurel-iris-vision")]`.

    # Shared HMAC secret. MUST match apps/web/.env MODAL_WEBHOOK_SECRET exactly.
    # Used by _post_webhook to sign the callback body (Stripe-style timestamp.body convention).
    MODAL_WEBHOOK_SECRET=

    # Base URL of the deployed Next.js app (no trailing slash). The Modal worker
    # POSTs back to {WEBHOOK_BASE_URL}/api/vision/webhook with the HMAC signature.
    # Production: https://aurel-iris.vercel.app
    # Preview:    https://aurel-iris-<branch>-<account>.vercel.app
    WEBHOOK_BASE_URL=
    ```

    If a previous version of `vision-service/.env.example` exists from earlier scaffolding (Phase 1), MERGE — preserve any vars not listed here (e.g. development-only flags), but ensure `MODAL_WEBHOOK_SECRET` and `WEBHOOK_BASE_URL` are present with the comments above.
  </action>
  <verify>
    <automated>test -f vision-service/.env.example && grep -c "^MODAL_WEBHOOK_SECRET=" vision-service/.env.example && grep -c "^WEBHOOK_BASE_URL=" vision-service/.env.example && grep -c "modal secret create" vision-service/.env.example</automated>
  </verify>
  <acceptance_criteria>
    - `test -f vision-service/.env.example` exits 0.
    - `grep -c "^MODAL_WEBHOOK_SECRET=" vision-service/.env.example` returns `1`.
    - `grep -c "^WEBHOOK_BASE_URL=" vision-service/.env.example` returns `1`.
    - `grep -c "modal secret create" vision-service/.env.example` returns `1` (Modal Secrets onboarding hint).
    - `grep -c "MUST match apps/web/.env MODAL_WEBHOOK_SECRET" vision-service/.env.example` returns `1` (cross-ref to apps/web side).
    - `grep -c "https://aurel-iris.vercel.app" vision-service/.env.example` returns `1` (production URL hint).
    - `cd vision-service && python scripts/audit_vocabulary.py` exits 0 (file is in scan scope per 05-16; no forbidden vocab).
  </acceptance_criteria>
  <done>vision-service/.env.example documents the 2 worker-side vars with comments + Modal Secrets onboarding example. LGPD audit clean.</done>
</task>

<task type="auto">
  <name>Task 3: Write the founder smoke procedure into vision-service/README.md</name>
  <read_first>
    - vision-service/README.md (full file if it exists — preserve any existing content; APPEND or extend the `## Smoke procedure (founder)` section. If the file does not exist, create it with a minimal lead-in.)
    - .planning/phases/05-pipeline-visao-modal/05-CONTEXT.md (D-X2, D-T6 — manual smoke, signed URLs only)
    - apps/web/app/api/vision/webhook/route.ts (built in 05-12 — confirm exact log format `[webhook] applied reading=<id> status=<n> call_id=<id>`)
    - apps/web/app/api/readings/[id]/process/route.ts (built in 05-11 — confirm 202 contract)
    - SPEC.md §4.3 (the JSON contract that step 7 validates against)
  </read_first>
  <files>vision-service/README.md</files>
  <action>
    Either create or extend `vision-service/README.md`. If the file exists, locate the appropriate insertion point (typically after a short repo overview); if a `## Smoke procedure` heading already exists, replace its body verbatim with the content below. If creating fresh, prepend a minimal H1 + one-paragraph overview, then add the smoke procedure section.

    The smoke procedure section content:

    ````markdown
    ## Smoke procedure (founder)

    Manual end-to-end verification of the Modal pipeline. Run after every
    `modal deploy` and after any non-trivial change to `modal_app.py` /
    `pipeline/*.py` / `data/*.json`. NOT automated in CI per D-X2 (Modal cloud
    runs cost real money; CI is CPU-only pytest — see `.github/workflows/vision-service-tests.yml`).

    Pre-requisites:
    - You have access to the Modal dashboard at https://modal.com/.
    - You have access to the Supabase project (studio + dashboard).
    - You have access to the Vercel project (dashboard or `vercel` CLI).
    - You have a fixture reading already created in the database via the captura
      / upload UI (Phase 3 / Phase 4) with `status='pending'` and 6/6 reading_images
      rows. (Easiest: use the desktop upload at `/leituras/nova/upload`.)

    ### 1. Install Python deps (CPU-only, local machine)

    ```bash
    cd vision-service
    pip install -r requirements.txt
    ```

    Verify with `python -m pytest tests/ -v` — same suite the GH Actions workflow
    runs in CI. Must exit 0 before deploying.

    ### 2. Authenticate the Modal CLI

    One-time per workstation. Get tokens from https://modal.com/settings/tokens.

    ```bash
    modal token set --token-id <token-id> --token-secret <token-secret>
    ```

    Confirm with `modal token current` — must print your workspace name.

    ### 3. Deploy the Modal app

    ```bash
    cd vision-service
    modal deploy modal_app.py
    ```

    Capture the printed endpoint URL — looks like:

    ```
    https://<workspace>--aurel-iris-vision-analyze-iris-endpoint.modal.run
    ```

    Save it; you'll need it in step 4.

    ### 4. Set MODAL_ANALYZE_ENDPOINT_URL in Vercel

    Both Production and Preview environments. Via dashboard:
    - Vercel → Project → Settings → Environment Variables → Add.
    - Name: `MODAL_ANALYZE_ENDPOINT_URL`
    - Value: the URL from step 3.
    - Environments: Production + Preview (NOT Development — local dev uses
      `apps/web/.env.local`).

    Or via CLI:

    ```bash
    vercel env add MODAL_ANALYZE_ENDPOINT_URL production
    vercel env add MODAL_ANALYZE_ENDPOINT_URL preview
    ```

    Re-deploy any pre-existing preview that needs the new URL (envs are baked in
    at deploy time).

    Also confirm `MODAL_WEBHOOK_SECRET` is set in BOTH:
    - Vercel envs (consumed by `/api/vision/webhook`)
    - Modal Secrets (consumed by `_post_webhook` in modal_app.py)

    These two values MUST be IDENTICAL, or HMAC verification will reject the
    callback with 401 (visible in Vercel logs as `[webhook] HMAC rejected: mismatch`).

    ### 5. Trigger one fixture reading

    Get a session cookie:
    1. Open the deployed Vercel preview URL in a browser.
    2. Sign in via magic link.
    3. Open DevTools → Application → Cookies → copy the `sb-...-auth-token` cookie value (and any related Supabase auth cookies — copy the whole header from a request in the Network tab for safety).

    POST to the trigger route with the reading_id from your fixture:

    ```bash
    curl -X POST \
      "https://<vercel-preview>/api/readings/<reading_id>/process" \
      -H "Cookie: <paste-the-cookies-here>" \
      -i
    ```

    Expected response: HTTP 202 with empty body.

    If you get 401 → cookie copy-paste was incomplete; retry.
    If you get 404 → reading is not owned by the signed-in user OR not in
    `pending`/`failed` status; pick a different fixture.
    If you get 502 → Modal trigger failed; see the Vercel logs for the
    `[process] error: ...` line and check that `MODAL_ANALYZE_ENDPOINT_URL`
    matches the URL from step 3.

    ### 6. Observe the webhook callback

    Stream Vercel logs (in another terminal):

    ```bash
    vercel logs <project-name> --follow
    ```

    Or in the dashboard: Project → Deployments → (latest) → Functions → `/api/vision/webhook`.

    Within ~30-90s of the trigger (cold-start dependent — D-F3) you should see:

    ```
    [process] reading=<id> status=processing call_id=fc-...
    [webhook] applied reading=<id> status=ready call_id=fc-...
    ```

    If you see `[webhook] applied ... status=failed` instead → the pipeline ran
    but produced an error. Check `processing_metadata.error_summary` in step 7
    against the D-E1 catalog (`vision-service/data/error_summary.json`).

    ### 7. Validate the result in Supabase

    Open Supabase studio → SQL editor:

    ```sql
    SELECT
      id,
      status,
      processed_at,
      vision_features
    FROM readings
    WHERE id = '<reading_id>';
    ```

    Verify the result against SPEC §4.3:

    - [ ] `status = 'ready'` (or `'failed'` if the pipeline classified the
          fixture as failed — also a valid path, but check error_summary then).
    - [ ] `processed_at` is set to a recent timestamp (within the last few minutes).
    - [ ] `vision_features.right_eye` is an object (or null if D-F1 unilateral
          degradation occurred — check `asymmetry_notes`).
    - [ ] `vision_features.left_eye` similarly.
    - [ ] `vision_features.right_eye.constitution.primary` is one of
          `linfatica | hematogenica | mista | indeterminada` (per SPEC §4.3 enum).
    - [ ] `vision_features.right_eye.iris_color.primary` is a pt-BR string.
    - [ ] `vision_features.right_eye.sectors` is an array of 12 entries
          (hours 1-12).
    - [ ] `vision_features.right_eye.sectors[i].zones` strings come from
          `vision-service/data/jensen-map.json` (no ad-hoc strings).
    - [ ] `vision_features.processing_metadata.model_version` matches the
          version baked into the deployed modal_app (`pipeline_<X.Y.Z>`).
    - [ ] `vision_features.processing_metadata.modal_call_id` is the same
          `fc-...` from the Vercel log line.
    - [ ] `vision_features.processing_metadata.stages_timing_ms` has 6 keys
          (one per stage: detect/segment/compose/normalize/enhance/features).

    If all checkboxes pass → smoke green. Document the run date + reading_id in
    your dogfooding journal.

    ### Architectural floor (LGPD)

    The Modal worker container does NOT receive Supabase service-role
    credentials. It receives only time-limited signed URLs (TTL=600s per D-T6)
    for the 6 input images and POSTs results back via HMAC-signed webhook. If
    a future change adds Supabase credentials to the Modal image / Modal
    Secrets, that's a privilege-escalation regression and must be reverted.
    See `.planning/phases/05-pipeline-visao-modal/05-CONTEXT.md` D-T6 and
    `05-RESEARCH.md` Anti-Patterns table.

    ### Re-running the smoke after every redeploy

    Steps 5-7 are the standing acceptance test. Steps 1-4 only repeat when:
    - You change `requirements.txt` (re-install).
    - You change `modal_app.py` or `pipeline/*.py` (re-deploy → new endpoint URL → repeat step 4).
    - You add/rotate the HMAC secret (re-set in BOTH Vercel + Modal Secrets).
    ````
  </action>
  <verify>
    <automated>test -f vision-service/README.md && grep -c "## Smoke procedure" vision-service/README.md && grep -c "modal deploy modal_app.py" vision-service/README.md && grep -c "/api/readings/" vision-service/README.md && grep -c "vision_features" vision-service/README.md && cd vision-service && python scripts/audit_vocabulary.py 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `test -f vision-service/README.md` exits 0.
    - `grep -c "## Smoke procedure" vision-service/README.md` returns at least `1`.
    - `grep -c "modal token set" vision-service/README.md` returns at least `1` (step 2).
    - `grep -c "modal deploy modal_app.py" vision-service/README.md` returns at least `1` (step 3).
    - `grep -c "MODAL_ANALYZE_ENDPOINT_URL" vision-service/README.md` returns at least `1` (step 4).
    - `grep -c "/api/readings/" vision-service/README.md` returns at least `1` (step 5 curl).
    - `grep -c "\\[webhook\\] applied" vision-service/README.md` returns at least `1` (step 6 expected log line).
    - `grep -c "vision_features" vision-service/README.md` returns at least `2` (step 7 SQL + checklist).
    - `grep -c "no Supabase service-role" vision-service/README.md` returns `1` (LGPD architectural floor documented).
    - `grep -c "TTL=600s" vision-service/README.md` returns `1` (D-T6 referenced).
    - `cd vision-service && python scripts/audit_vocabulary.py` exits 0 (README is in scope per 05-16; no forbidden vocab).
    - The 7 numbered steps appear in order: regex `^### [1-7]\\.` matches at least 7 lines (verify with `grep -cE "^### [1-7]\\." vision-service/README.md` returns at least `7`).
  </acceptance_criteria>
  <done>vision-service/README.md contains the 7-step smoke procedure with all expected log lines, SPEC §4.3 validation checklist, and the LGPD architectural floor note. Audit clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Founder workstation → Modal CLI | `modal token set` — credentials at rest in `~/.modal.toml` (Modal CLI default). |
| Modal Secrets → Modal worker | `MODAL_WEBHOOK_SECRET` injected at runtime; never logged in `print` calls within `modal_app.py`. |
| README → repo | Plain documentation; no execution risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-17-01 | Information disclosure | README copy-paste leaks real Modal token in PR | mitigate | All commands use `<token-id>` / `<token-secret>` placeholders. `.env.example` contains EMPTY values. `.gitignore` (existing) excludes `.env` files. |
| T-05-17-02 | Tampering | Modal Secrets and Vercel envs drift apart for MODAL_WEBHOOK_SECRET | mitigate | README step 4 explicitly calls out "MUST be IDENTICAL"; symptom (`[webhook] HMAC rejected: mismatch`) and remediation documented. |
| T-05-17-03 | Privilege escalation | Future PR adds Supabase service-role to Modal image | mitigate | "Architectural floor (LGPD)" subsection documents the prohibition; cross-references D-T6 + RESEARCH Anti-Patterns. PR review + audit_vocabulary.py (which scans `requirements.txt` indirectly via the dir scan) provide secondary gates. |
| T-05-17-04 | Spoofing | Founder copies a stale endpoint URL into Vercel envs | mitigate | README step 3 instructs founder to re-capture URL on every redeploy; step 4 explicitly says "Re-deploy any pre-existing preview that needs the new URL." |
| T-05-17-05 | Repudiation | Cannot tell when smoke last passed | accept | Founder is responsible for journaling in their own dogfooding notes; product is solo-developer single-tenant; no audit trail required at this stage. Phase 8 LGPD review revisits. |
</threat_model>

<verification>
- `test -f vision-service/README.md && test -f vision-service/.env.example && test -f apps/web/.env.example` exits 0 (all three files present).
- `grep -c "MODAL_ANALYZE_ENDPOINT_URL\|MODAL_TOKEN_ID\|MODAL_TOKEN_SECRET\|MODAL_WEBHOOK_SECRET" apps/web/.env.example` returns `4` (all 4 vars documented).
- `grep -c "MODAL_WEBHOOK_SECRET\|WEBHOOK_BASE_URL" vision-service/.env.example` returns `2`.
- `grep -cE "^### [1-7]\\." vision-service/README.md` returns at least `7` (7 steps in order).
- `cd vision-service && python scripts/audit_vocabulary.py` exits 0 (README + .env.example + jensen-map + error_summary all clean).
- `cd apps/web && pnpm audit:vocabulary` exits 0.
</verification>

<success_criteria>
- A founder cloning the repo on a fresh machine can, using only the README, deploy the Modal app, wire it to Vercel, and verify a fixture reading lands as `vision_features` jsonb conforming to SPEC §4.3 — without needing to read STATE.md or any planning docs.
- Every Modal env var has a one-line `#` comment documenting its purpose in either `apps/web/.env.example` or `vision-service/.env.example`.
- The README documents the LGPD architectural floor (no Supabase service role in Modal container) so future contributors don't regress on D-T6.
- The smoke procedure becomes the standing acceptance test for any future Modal redeploy throughout milestones v1.x.
</success_criteria>

<output>
After completion, create `.planning/phases/05-pipeline-visao-modal/05-17-SUMMARY.md` documenting: the 7-step smoke procedure (with each step's purpose), the .env.example deltas (apps/web added comments; vision-service created), and the LGPD architectural floor reminder. This SUMMARY becomes the entry point in the Phase 5 closing report when the phase ships.
</output>
