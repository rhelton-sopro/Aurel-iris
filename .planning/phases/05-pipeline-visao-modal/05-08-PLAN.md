---
phase: 05-pipeline-visao-modal
plan: 08
type: execute
wave: 1
depends_on: [05-01]
files_modified:
  - vision-service/pipeline/enhance.py
  - vision-service/tests/test_enhance.py
autonomous: true
requirements: [VISION-02]
tags: [vision, pipeline, clahe, enhance]

must_haves:
  goal: "Stage 5 `enhance.clahe(normalized_image)` applies CLAHE to the L-channel of LAB color space (NOT to RGB directly), preserving hue while boosting fiber-texture contrast."
  truths:
    - "`from pipeline.enhance import clahe, CLAHE_CLIP_LIMIT, CLAHE_TILE_GRID_SIZE` succeeds."
    - "CLAHE_CLIP_LIMIT = 2.0 and CLAHE_TILE_GRID_SIZE = (4, 8) (RESEARCH Pattern 7 + assumption A3)."
    - "Output shape and dtype match input (np.uint8 H×W×3)."
    - "Conversion path is RGB → LAB → split L|a|b → CLAHE on L → merge → LAB → RGB. The a/b channels are NOT modified."
    - "Anti-pattern guard: applying CLAHE to RGB directly is REJECTED (RESEARCH Anti-Patterns)."
  artifacts:
    - path: vision-service/pipeline/enhance.py
      provides: "CLAHE on LAB L-channel"
      exports: ["clahe", "CLAHE_CLIP_LIMIT", "CLAHE_TILE_GRID_SIZE"]
    - path: vision-service/tests/test_enhance.py
      provides: "structural shape/dtype tests + hue-preservation test"
  key_links:
    - from: vision-service/pipeline/enhance.py
      to: cv2.createCLAHE
      via: "createCLAHE(clipLimit=2.0, tileGridSize=(4, 8)).apply(l_chan)"
      pattern: "createCLAHE"
---

<objective>
Stage 5 enhances the polar-normalized iris with CLAHE (Contrast Limited Adaptive Histogram Equalization) applied to the L-channel of LAB color space — RESEARCH Pattern 7 verbatim, with the explicit anti-pattern guard against applying CLAHE to RGB.

Purpose: produce the enhanced array consumed by 05-09 (features — fiber density, lacuna detection benefit from local contrast boost). Honors RESEARCH Pattern 7 + assumption A3 (clipLimit=2.0, tileGridSize=(4, 8) — calibratable per D-X3).

Output: `pipeline/enhance.py` implemented; structural + hue-preservation tests in `test_enhance.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-pipeline-visao-modal/05-RESEARCH.md
@.planning/phases/05-pipeline-visao-modal/05-PATTERNS.md
@vision-service/pipeline/enhance.py
@vision-service/pipeline/normalize.py

<interfaces>
Stub signature MUST be preserved:

```python
def clahe(normalized_image):
    """
    Args:
        normalized_image: output of pipeline.normalize.daugman_polar.
    Returns:
        Enhanced image ready for feature extraction.
    """
```

Input: `np.ndarray` shape `(H, W, 3)` uint8 RGB (from 05-07; will be 64×512 in production).
Output: same shape, same dtype, hue-preserved.

The function name is `clahe` (lowercase) to keep the stub's contract. Do NOT rename to `apply_clahe` — call sites use `enhance.clahe`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement LAB-space CLAHE</name>
  <read_first>
    - vision-service/pipeline/enhance.py (preserve docstring + signature)
    - .planning/phases/05-pipeline-visao-modal/05-RESEARCH.md (Pattern 7 lines 750-771; Anti-Patterns line 904; assumption A3)
    - .planning/phases/05-pipeline-visao-modal/05-PATTERNS.md (section "vision-service/pipeline/enhance.py")
  </read_first>
  <files>vision-service/pipeline/enhance.py</files>
  <action>
    Replace `vision-service/pipeline/enhance.py` content. The implementation MUST:

    1. Header: `from __future__ import annotations` + `import cv2` + `import numpy as np`.
    2. Module docstring referencing RESEARCH Pattern 7 AND the anti-pattern: "applying CLAHE to RGB shifts hue/saturation; we work on LAB L-channel only".
    3. Module constants `CLAHE_CLIP_LIMIT = 2.0` and `CLAHE_TILE_GRID_SIZE = (4, 8)` (matching RESEARCH A3 exactly).
    4. Function `clahe(normalized_image: np.ndarray) -> np.ndarray`:
       - Validate input: `if normalized_image.ndim != 3 or normalized_image.shape[2] != 3: raise ValueError("enhance_invalid_input_shape")`.
       - Validate dtype: `if normalized_image.dtype != np.uint8: raise ValueError("enhance_invalid_input_dtype")`.
       - Convert: `lab = cv2.cvtColor(normalized_image, cv2.COLOR_RGB2LAB)`.
       - Split: `l_chan, a_chan, b_chan = cv2.split(lab)`.
       - Build CLAHE: `clahe_obj = cv2.createCLAHE(clipLimit=CLAHE_CLIP_LIMIT, tileGridSize=CLAHE_TILE_GRID_SIZE)`.
       - Apply to L: `l_enhanced = clahe_obj.apply(l_chan)`.
       - Merge: `enhanced_lab = cv2.merge([l_enhanced, a_chan, b_chan])`.
       - Convert back: `return cv2.cvtColor(enhanced_lab, cv2.COLOR_LAB2RGB)`.
    5. Remove the existing `raise NotImplementedError(...)` line.

    The function name `clahe` (matching the stub) shadows the OpenCV builder. That is acceptable — call sites import as `from pipeline.enhance import clahe` and `cv2.createCLAHE` is referenced as fully-qualified inside the function. Do NOT add `from cv2 import createCLAHE`.
  </action>
  <verify>
    <automated>cd vision-service && python -c "from pipeline.enhance import clahe, CLAHE_CLIP_LIMIT, CLAHE_TILE_GRID_SIZE; assert CLAHE_CLIP_LIMIT == 2.0; assert CLAHE_TILE_GRID_SIZE == (4, 8); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `cd vision-service && python -c "from pipeline.enhance import clahe"` exits 0.
    - `grep -c "CLAHE_CLIP_LIMIT = 2.0" vision-service/pipeline/enhance.py` returns `1`.
    - `grep -c "CLAHE_TILE_GRID_SIZE = (4, 8)" vision-service/pipeline/enhance.py` returns `1`.
    - `grep -c "cv2.COLOR_RGB2LAB" vision-service/pipeline/enhance.py` returns `1`.
    - `grep -c "cv2.COLOR_LAB2RGB" vision-service/pipeline/enhance.py` returns `1`.
    - `grep -c "cv2.createCLAHE" vision-service/pipeline/enhance.py` returns `1`.
    - `grep -c "raise NotImplementedError" vision-service/pipeline/enhance.py` returns `0`.
  </acceptance_criteria>
  <done>enhance.py applies CLAHE to LAB L-channel; constants exposed for D-X3 calibration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Structural + hue-preservation tests for enhance</name>
  <read_first>
    - vision-service/pipeline/enhance.py (just created in Task 1)
    - .planning/phases/05-pipeline-visao-modal/05-RESEARCH.md (Pattern 7; Anti-Patterns)
  </read_first>
  <behavior>
    - Output preserves shape and dtype
    - 2D input or non-uint8 input raises ValueError
    - For a uniform input image, output equals input (CLAHE is a no-op on flat color)
    - For an image with synthetic horizontal gradient, output has higher local contrast (max - min in a tile is greater after CLAHE)
    - Hue preservation: dominant hue (HSV H channel) is approximately preserved between input and output (within tolerance)
  </behavior>
  <files>vision-service/tests/test_enhance.py</files>
  <action>
    Create `vision-service/tests/test_enhance.py` with these tests:

    1. `test_invalid_shape_raises`: 2D array `np.zeros((64, 512), dtype=np.uint8)` → `pytest.raises(ValueError, match="enhance_invalid_input_shape")`.

    2. `test_invalid_dtype_raises`: float32 input shape (64, 512, 3) → `pytest.raises(ValueError, match="enhance_invalid_input_dtype")`.

    3. `test_output_shape_and_dtype`: input shape (64, 512, 3) uint8 random; output assertions: `result.shape == (64, 512, 3)` and `result.dtype == np.uint8`.

    4. `test_uniform_image_unchanged`: uniform RGB (128, 128, 128) shape (64, 512, 3); after CLAHE the result should be very close to input (flat L channel → CLAHE no-op). Allow 2-unit tolerance per channel: `assert np.abs(result.astype(int) - input_img.astype(int)).max() <= 2`.

    5. `test_local_contrast_increases_for_gradient`: build a horizontal gradient `np.tile(np.linspace(40, 200, 512, dtype=np.uint8).reshape(1, 512, 1), (64, 1, 3))`. Compute input contrast as the max-min L value in the central 16×16 tile; do the same for output. Assert output contrast ≥ input contrast (CLAHE never reduces a tile's range when applied with these params on a smooth gradient — at minimum equal). If the OpenCV implementation produces equal-or-greater, accept >= input * 0.95 to allow micro-tolerance.

    6. `test_hue_approximately_preserved`: build an image with a single dominant hue (e.g. uniform reddish brown `(120, 80, 40)` in RGB → distinct H in HSV). Apply `clahe`. Convert input and output to HSV. Compute the median H value of input vs output. Assert abs(diff) < 5 (HSV hue is in [0, 180] in OpenCV; 5 units is < 3% drift). Skip with a clear message if the test framework's OpenCV version produces values that would make this brittle (catch numerical edge cases gracefully).

    7. `test_clahe_constants_exposed`: `from pipeline.enhance import CLAHE_CLIP_LIMIT, CLAHE_TILE_GRID_SIZE; assert CLAHE_CLIP_LIMIT == 2.0; assert CLAHE_TILE_GRID_SIZE == (4, 8)`.

    Imports: `import cv2, numpy as np, pytest; from pipeline.enhance import clahe`.
  </action>
  <verify>
    <automated>cd vision-service && python -m pytest tests/test_enhance.py -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cd vision-service && python -m pytest tests/test_enhance.py -v` exits 0 with at least 7 tests passing.
    - `grep -c "enhance_invalid_input" vision-service/tests/test_enhance.py` returns at least `2`.
    - `cd vision-service && python -m pytest tests/ -x -q` exits 0 (no regression).
  </acceptance_criteria>
  <done>CLAHE on LAB L-channel covered: shape/dtype invariants, no-op on flat input, contrast increase on gradient, hue preservation, constants exposed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Normalize output → enhance | Internal pipeline; pure transform. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08-01 | Tampering | CLAHE applied to RGB directly causing hue shift (Anti-Pattern) | mitigate | Implementation explicitly converts to LAB, applies only to L; test 6 verifies hue preservation. |
| T-05-08-02 | Repudiation | Calibration constants tweaked without traceability | mitigate | Constants exported as module-level; PR diff highlights changes. |
</threat_model>

<verification>
- `cd vision-service && python -m pytest tests/test_enhance.py -v` exits 0.
- CLAHE applied to L-channel only (verifiable by grep for `cv2.split(lab)` and `cv2.merge([l_enhanced, ...])`).
</verification>

<success_criteria>
- 05-09 (features) consumes the enhanced 64×512×3 uint8 array; sector iteration over angular axis is well-defined.
- A3 (RESEARCH assumption) committed as code; calibration via constants only.
</success_criteria>

<output>
After completion, create `.planning/phases/05-pipeline-visao-modal/05-08-SUMMARY.md` documenting CLAHE constants and the LAB-channel choice. Note that the anti-pattern (CLAHE on RGB) is now actively prevented by the implementation.
</output>
