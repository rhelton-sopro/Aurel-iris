---
phase: 05-pipeline-visao-modal
plan: 05
type: execute
wave: 1
depends_on: [05-01]
files_modified:
  - vision-service/pipeline/segment.py
  - vision-service/tests/test_segment.py
autonomous: true
requirements: [VISION-02]
tags: [vision, pipeline, opencv, hough, segment]

must_haves:
  goal: "Stage 2 `segment.iris_mask(image, detection)` returns a binary mask + iris_circle using OpenCV HoughCircles, with MediaPipe-estimate fallback when Hough returns nothing (D-F1)."
  truths:
    - "`from pipeline.segment import iris_mask, HOUGH_DEFAULTS` succeeds."
    - "`iris_mask(rgb_image, detection)` returns a dict with keys `binary_mask` (H×W bool), `iris_circle` (cx, cy, r), `segmented_image` (RGB H×W×3)."
    - "When `cv2.HoughCircles` returns `None`, the function falls back to `detection['center']`/`detection['radius']` and appends `'hough_segment_failed_fallback_mediapipe'` to a `warnings` list passed via the optional `warnings` keyword argument."
    - "When multiple Hough candidates returned, the closest to `detection['center']` wins (Pitfall 7)."
    - "HOUGH_DEFAULTS = {dp: 1.0, minDist: 100, param1: 100, param2: 40, minRadius: 80, maxRadius: 200} (RESEARCH Pattern 5 verbatim)."
  artifacts:
    - path: vision-service/pipeline/segment.py
      provides: "iris segmentation via Hough + soft-degradation fallback"
      exports: ["iris_mask", "HOUGH_DEFAULTS"]
    - path: vision-service/tests/test_segment.py
      provides: "structural tests (shape, dtype) + selection test (closest-to-detect) + fallback test"
  key_links:
    - from: vision-service/pipeline/segment.py
      to: cv2.HoughCircles
      via: "cv2.HoughCircles(gray_blur, cv2.HOUGH_GRADIENT, **HOUGH_DEFAULTS)"
      pattern: "HoughCircles"
---

<objective>
Stage 2 segments the iris ring from the source image. Replace `NotImplementedError` in `segment.py` with the OpenCV HoughCircles pattern (RESEARCH Pattern 5) that uses the upstream detect dict as both seed and fallback. Honors D-F1 (fall back to MediaPipe estimate when Hough fails — pipeline continues), Pitfall 7 (sort candidates by proximity to detect estimate, not just `circles[0][0]`).

Purpose: produce the `(binary_mask, iris_circle, segmented_image)` triple consumed by 05-06 (compose stage averages segmented_images). Output stays calibratable — HOUGH_DEFAULTS exported as a module constant so the founder can later tune them against fixtures.

Output: `pipeline/segment.py` implemented; structural + soft-degradation + selection tests in `test_segment.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-pipeline-visao-modal/05-CONTEXT.md
@.planning/phases/05-pipeline-visao-modal/05-RESEARCH.md
@.planning/phases/05-pipeline-visao-modal/05-PATTERNS.md
@vision-service/pipeline/segment.py
@vision-service/pipeline/detect.py

<interfaces>
Stub signature MUST be preserved verbatim from existing segment.py:

```python
def iris_mask(image, detection):
    """
    Args:
        image: numpy array (H, W, 3).
        detection: dict from pipeline.detect.find_iris.
    Returns:
        dict with binary_mask (H, W, bool) and segmented_image.
    """
```

Extended signature with optional warnings sink (orchestrator-controlled):

```python
def iris_mask(
    image: np.ndarray,
    detection: dict,
    *,
    warnings: list[str] | None = None,
) -> dict:
```

When `warnings` is None, function still works but soft-degradation events are silently dropped. The orchestrator (05-10) passes a list it owns so degradation is captured into `processing_metadata.warnings`.

Output dict consumed by 05-06 (compose):

```python
{
    "binary_mask": np.ndarray,    # (H, W) bool
    "iris_circle": (cx, cy, r),   # tuple of float
    "segmented_image": np.ndarray, # (H, W, 3) uint8 RGB, masked
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement HoughCircles iris segmentation with MediaPipe fallback</name>
  <read_first>
    - vision-service/pipeline/segment.py (preserve docstring + signature lines 1-25)
    - .planning/phases/05-pipeline-visao-modal/05-RESEARCH.md (Pattern 5; Pitfall 7 — picking closest to MediaPipe estimate)
    - .planning/phases/05-pipeline-visao-modal/05-PATTERNS.md (section "vision-service/pipeline/segment.py")
    - .planning/phases/05-pipeline-visao-modal/05-CONTEXT.md (D-F1 soft degradation)
  </read_first>
  <files>vision-service/pipeline/segment.py</files>
  <action>
    Replace the entire content of `vision-service/pipeline/segment.py` with the implementation below. The implementation MUST:

    1. Import `cv2`, `numpy as np`, `from typing import Optional`, and use `from __future__ import annotations`.
    2. Define `HOUGH_DEFAULTS` as a module-level dict with EXACTLY these keys/values: `dp=1.0, minDist=100, param1=100, param2=40, minRadius=80, maxRadius=200`. The values must be unquoted numerics matching RESEARCH Pattern 5 verbatim.
    3. Function signature: `def iris_mask(image, detection, *, warnings=None) -> dict:` with full type hints (`np.ndarray`, `dict`, `Optional[list[str]]`).
    4. First-line guard: `if "center" not in detection or "radius" not in detection: raise ValueError("segment_invalid_detection")` — orchestrator (05-10) catches.
    5. Convert RGB → gray via `cv2.cvtColor(image, cv2.COLOR_RGB2GRAY)`, then `cv2.medianBlur(gray, 5)` to reduce specular highlights.
    6. Call `cv2.HoughCircles(gray_blur, cv2.HOUGH_GRADIENT, **HOUGH_DEFAULTS)`.
    7. If circles is None: assign `(cx, cy, r) = (cx_seed, cy_seed, r_seed)` from `detection`; if `warnings is not None: warnings.append("hough_segment_failed_fallback_mediapipe")`.
    8. If circles is not None: extract `circles[0]` (shape (N, 3)); compute `dists = sqrt((cands[:,0]-cx_seed)**2 + (cands[:,1]-cy_seed)**2)`; pick `argmin(dists)`. NEVER use `circles[0][0]` directly (Pitfall 7).
    9. Build mask: `mask_u8 = np.zeros((h, w), dtype=np.uint8)`, then `cv2.circle(mask_u8, (int(cx), int(cy)), int(r), 1, thickness=-1)`. Return `binary_mask = mask_u8.astype(bool)`.
    10. Compute `segmented = cv2.bitwise_and(image, image, mask=mask_u8)`.
    11. Return `{"binary_mask": binary_mask, "iris_circle": (cx, cy, r), "segmented_image": segmented}` — all floats in iris_circle must be cast via `float()`.
    12. Module docstring must reference RESEARCH Pattern 5, CONTEXT D-F1, and Pitfall 7 by name so the executor in 05-10 sees the contract.

    The full module content matches RESEARCH.md Pattern 5 lines 631-691 with the additions described above. Remove the existing `raise NotImplementedError(...)` line entirely.
  </action>
  <verify>
    <automated>cd vision-service && python -c "from pipeline.segment import iris_mask, HOUGH_DEFAULTS; assert HOUGH_DEFAULTS['dp'] == 1.0; assert HOUGH_DEFAULTS['minDist'] == 100; assert HOUGH_DEFAULTS['param2'] == 40; assert HOUGH_DEFAULTS['minRadius'] == 80; assert HOUGH_DEFAULTS['maxRadius'] == 200; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `cd vision-service && python -c "from pipeline.segment import iris_mask, HOUGH_DEFAULTS"` exits 0.
    - `grep -c '"dp": 1.0' vision-service/pipeline/segment.py` returns `1`.
    - `grep -c '"param2": 40' vision-service/pipeline/segment.py` returns `1`.
    - `grep -c '"minRadius": 80' vision-service/pipeline/segment.py` returns `1`.
    - `grep -c 'hough_segment_failed_fallback_mediapipe' vision-service/pipeline/segment.py` returns `1`.
    - `grep -c 'np.argmin(dists)' vision-service/pipeline/segment.py` returns `1` (Pitfall 7 closest-to-seed selection).
    - `grep -c 'raise NotImplementedError' vision-service/pipeline/segment.py` returns `0`.
    - `grep -c 'raise ValueError("segment_invalid_detection")' vision-service/pipeline/segment.py` returns `1`.
    - `grep -c 'cv2.HoughCircles' vision-service/pipeline/segment.py` returns `1`.
  </acceptance_criteria>
  <done>segment.py imports cleanly; HOUGH_DEFAULTS exported; closest-to-seed selection guards Pitfall 7; fallback path appends to warnings sink.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Structural + selection + fallback tests for segment</name>
  <read_first>
    - vision-service/pipeline/segment.py (just created in Task 1)
    - .planning/phases/05-pipeline-visao-modal/05-CONTEXT.md (D-X3, D-F1)
    - vision-service/tests/conftest.py (existing fixtures from 05-01)
  </read_first>
  <behavior>
    - structural: returned dict has binary_mask (bool, shape H×W), iris_circle (3-tuple of floats), segmented_image (uint8 H×W×3)
    - structural: invalid detection → ValueError("segment_invalid_detection")
    - fallback: when Hough finds nothing (synthetic uniform gray image), function returns and appends "hough_segment_failed_fallback_mediapipe" to provided warnings list
    - fallback: when warnings is None, no exception, just silent fallback
    - selection: when synthetic image has multiple concentric circles, the one closest to detection["center"] wins (Pitfall 7)
    - metric (gated): with founder fixture + bbox annotation, mask covers ≥60% of expected iris bbox area
  </behavior>
  <files>vision-service/tests/test_segment.py</files>
  <action>
    Create `vision-service/tests/test_segment.py` with these test cases:

    1. `test_invalid_detection_raises_value_error` — pass `{}` as detection → expect `pytest.raises(ValueError, match="segment_invalid_detection")`.

    2. `test_returns_required_keys_with_uniform_image` — synthetic 256×256 uniform gray image (`np.full((256, 256, 3), 127, dtype=np.uint8)`); detection `{"center": (128.0, 128.0), "radius": 50.0}`; warnings = []; assert result keys = `{"binary_mask", "iris_circle", "segmented_image"}`; assert `result["binary_mask"].dtype == bool`; assert `result["binary_mask"].shape == (256, 256)`; assert `result["segmented_image"].shape == (256, 256, 3)`; assert `result["segmented_image"].dtype == np.uint8`; assert `len(result["iris_circle"]) == 3` and all are floats.

    3. `test_fallback_to_mediapipe_when_hough_fails` — same uniform image (Hough cannot find circles in solid gray); detection `{"center": (50.0, 60.0), "radius": 30.0}`; warnings = []; call `iris_mask(img, detection, warnings=warnings)`; assert `result["iris_circle"] == (50.0, 60.0, 30.0)`; assert `"hough_segment_failed_fallback_mediapipe" in warnings`.

    4. `test_fallback_silent_when_warnings_none` — same setup as #3 but call `iris_mask(img, detection, warnings=None)`; assert no exception raised; result still has all keys.

    5. `test_picks_closest_to_seed_when_multiple_candidates` — build a synthetic image with two distinct dark circles drawn via `cv2.circle` (e.g. center (100, 100) radius 80 AND center (220, 220) radius 80, both filled black on white background, 320×320). Detection `{"center": (105.0, 105.0), "radius": 80.0}` (close to first circle). Run `iris_mask`. Assert `abs(result["iris_circle"][0] - 100) < 20` and `abs(result["iris_circle"][1] - 100) < 20` (winner is first circle, not second). If Hough doesn't find both circles in a particular OpenCV build, fall back to asserting that result is plausibly close to the seed (within 30px), never beyond 100px from seed.

    6. `test_binary_mask_is_inside_iris_circle` — uniform image; check that every True pixel in `binary_mask` lies within `(iris_circle.cx, iris_circle.cy)` plus radius. Compute `dist = sqrt((y - cy)**2 + (x - cx)**2)`; assert max dist where mask is True ≤ `r + 1` (allow 1px aliasing slack).

    7. `test_segmented_image_zero_outside_mask` — uniform image (value 127); after segmentation, pixels outside `binary_mask` must be 0. Assert `(result["segmented_image"][~result["binary_mask"]] == 0).all()`.

    8. `test_metric_iris_coverage_against_fixture` (gated): if `iris_images` is empty OR `expected.get("iris_bbox", {})` is empty, `pytest.skip("no founder fixtures yet")`. Else for each fixture with a bbox, call `find_iris` (skip if model unavailable — wrap in try/except `RuntimeError` and skip), then `iris_mask`; compute mask area inside expected bbox / expected bbox area ≥ 0.6.

    Use `numpy as np` and `cv2` for synthetic image construction. Import as `from pipeline.segment import iris_mask, HOUGH_DEFAULTS`. The metric test must not fail when fixtures are absent — it must skip cleanly.
  </action>
  <verify>
    <automated>cd vision-service && python -m pytest tests/test_segment.py -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cd vision-service && python -m pytest tests/test_segment.py -v` exits 0; tests 1-7 (synthetic) pass unconditionally; test 8 (metric) is either passed or skipped (never failed).
    - `grep -c "hough_segment_failed_fallback_mediapipe" vision-service/tests/test_segment.py` returns at least `1`.
    - `grep -c "segment_invalid_detection" vision-service/tests/test_segment.py` returns at least `1`.
    - `grep -c "pytest.skip" vision-service/tests/test_segment.py` returns at least `1` (gating the metric test).
    - `cd vision-service && python -m pytest tests/ -x -q` exits 0 (smoke + audit + schema + detect + segment, no regressions).
  </acceptance_criteria>
  <done>Synthetic tests cover structure, fallback, Pitfall 7 selection, mask geometry; metric test gated by fixture availability.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Image bytes → OpenCV | Image decoded by Modal worker from a Supabase signed URL; trusted-after-fetch. |
| Detect output → segment | Internal pipeline trust; `ValueError` is the only failure channel. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering | Hough picks specular highlight as iris (Pitfall 7) | mitigate | Closest-to-seed selection via `np.argmin(dists)` guarantees the chosen circle is anchored to the MediaPipe estimate. |
| T-05-05-02 | Denial of service | Adversarial image causing OpenCV to allocate huge accumulator | accept | `HOUGH_DEFAULTS.minRadius=80, maxRadius=200` bound the search; Modal `timeout=120` caps blast radius. |
| T-05-05-03 | Information disclosure | Mask contents leaking via debug logging | accept | No logging in segment.py; orchestrator owns logging strategy. |
| T-05-05-04 | Repudiation | HOUGH_DEFAULTS tuned without traceability | mitigate | Defaults exported as module constant; calibration changes show up in git diff and trigger PR review per D-X3. |
</threat_model>

<verification>
- `cd vision-service && python -m pytest tests/test_segment.py -v` exits 0.
- HOUGH_DEFAULTS values match RESEARCH Pattern 5 verbatim (dp=1.0, minDist=100, param1=100, param2=40, minRadius=80, maxRadius=200).
- segment.py contains no `raise NotImplementedError`.
- `cd vision-service && python -m scripts.audit_vocabulary` still exits 0.
</verification>

<success_criteria>
- 05-06 (compose) can iterate over a list of `iris_mask` outputs and average `segmented_image` weighted by angle.
- 05-10 (orchestrator) can pass a per-eye `warnings` list to capture soft-degradation events into `processing_metadata.warnings`.
</success_criteria>

<output>
After completion, create `.planning/phases/05-pipeline-visao-modal/05-05-SUMMARY.md` documenting HOUGH_DEFAULTS values, the closest-to-seed selection logic, and any fixture-driven calibration changes. Note that `HOUGH_DEFAULTS` is the calibration knob for D-X3 fixture-driven tuning.
</output>
