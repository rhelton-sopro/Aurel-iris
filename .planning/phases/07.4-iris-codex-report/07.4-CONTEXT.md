# Phase 7.4: Iris Codex — Relatório funcional adaptativo + add-on placeholder - Context

> **⚠ DIRECTION CORRECTION — 2026-05-13 (post-Plan-09)** — Founder tested the v2 pipeline on a fresh reading and rejected the 8-block adaptive structure as too compressed for the depth required. Phase 7.4 is being **redirected** (not abandoned, not forked into a new phase). The historical 8-block design described in the sections below is **SUPERSEDED** — see the "Direction Correction" block immediately following for the locked replacement decisions. Original content is preserved below for audit trail.

---

## Direction Correction (locked 2026-05-13)

### Locked decisions

- **DC-1 — STRUCTURE**: Replace the 8-block JSON schema with **14 dedicated markdown sections**, each `## §N — Title`. The sections are:
  1. Constituição e Temperamento
  2. Mapa Orgânico (sistemas em ordem de prioridade visual)
  3. Linha do Tempo Emocional (mínimo 5 marcos com idade estimada)
  4. Padrões Emocionais Ativos
  5. Eixo Psicossomático
  6. Heranças Transgeracionais Sugeridas
  7. Carências Funcionais (educacional)
  8. Estado Mental e Nervoso
  9. Recursos e Forças
  10. Dimensão Arquetípica / Espiritual
  11. Sugestões Integrativas (caixa de ferramentas por categoria: Nutrição, Fitoterapia tradicional, Práticas corporais, Práticas contemplativas, Florais genéricos sem marca)
  12. Roteiro de Anamnese
  13. Síntese Integrativa
  14. Mensagem para o Cliente

- **DC-2 — FORMAT**: Markdown, not JSON. Sonnet emits long-form prose freely; no zod-validated structured output; no per-field escape constraints. Streaming parser detects section boundaries via `## §N — Title` heading regex (reusing the legacy Phase 7 `findAllBoundaries` from `lib/anthropic/parser.ts`).

- **DC-3 — PARSER**: Reuse the v1 `findAllBoundaries` regex from `lib/anthropic/parser.ts`, extended from 13 to 14 sections. No new parser code. Resolves the D-DEF-09-05 cross-version contamination at the same time — `analyze.ts` and `analyze-v2.ts` consolidate into ONE `analyze.ts` since both paths now emit markdown.

- **DC-4 — LANGUAGE RULES (mandatory in the prompt)**:
  - No author citations (Jensen, Lo Rito, Moraga Gajardo, etc.) in primary surface
  - No school references on primary surface
  - No `[ancorado em features.x]` inline anchor markers
  - No pipeline meta-language ("vision_features detected...", "Modal pipeline output...")
  - No `lacuna grau N` raw iridological vocabulary — translate to clinical meaning
  - Direct clinical-functional language for integrative therapists
  - Three priority axes always present in coverage: organs (§2) + emotions (§4) + traumas (§3 emotional timeline)

- **DC-5 — §11 BEHAVIOR**: Afirmações section is an **optional toggle per reading**. Default off. When on, Sonnet adds §11 to the output; when off, §11 is omitted entirely. UI exposes a per-reading toggle that lives alongside the analysis trigger.

- **DC-6 — §14 BEHAVIOR**: Mensagem para o Cliente is **always-on**. First-person voice. Warm, brand-aligned tone (not clinically distant). Founder will refine the tone iteratively after seeing real outputs. Prompt instructs Sonnet on voice rules but leaves room for tone calibration in follow-up plans.

- **DC-7 — DUAL-LAYER (technical tab)**: Stays as **V1.1 placeholder** — display-only `AdvancedAnalysisCTA` button + "Análise Iridológica Aprofundada — 1 crédito" modal (already shipped in Plan 07). NO second LLM call, NO citations-bearing technical layer, NO `reading_addons` row writes in V1. Defers actual technical layer build to V1.1. This explicitly REVERSES the 7.3 dual-layer abandonment to the extent that the placeholder stays, but DOES NOT implement the technical layer in this phase.

- **DC-8 — PHASE SCOPE**: Redirection inside Phase 7.4, NOT a new phase. Same DB column (`report_v2 jsonb`), same `report_version='2.0'` flag for new readings, same phase artifacts directory. No production commitment to the 8-block API → safe to throw away. Cost of redirection: 4 new plans (10/11/12/13) overwriting most of Plans 02–08 surface work. Migrations (01), rebrand (09), and reading_addons table stay intact.

- **DC-9 — DISCARDED CODE (clean delete, no `_deprecated/` archive)**:
  - `apps/web/lib/anthropic/{analyze-v2,stream-parser-v2,audit-v2,diff-v2,types-v2}.ts` (Plan 03)
  - `apps/web/lib/anthropic/{report-schema,report-schema-shared}.ts` (Plan 00 + Plan 09 split)
  - `apps/web/lib/tendency-engine/*` (Plan 04 — tendency_grade has no place in the new structure)
  - 8-block view components (Plan 06): `ReportAdaptiveView`, `SystemTendencyCard`, `GradeBar`, `GradeBadge`, `KeyTraitChipGroup`, `IntegrativeAxisItem`, `BilateralFindingsCard`, `PriorityFocusList`, `ClinicalNote`
  - 8-block editor components (Plans 07 + 07b): `ReportAdaptiveEditor`, `BlockEditPane`, `SystemTendencyCardEditor`, `IntegrativeAxesEditor`, `BilateralFindingsEditor`
  - `AdaptiveAnalysisStream` (Plan 07 — rebuilt for 14 sections, not deleted)
  - All `__tests__/` files for the above
  - Wave 0 RED stubs that referenced 8-block keys (some can be repurposed; cleanest is delete + re-stub for 14 sections)

- **DC-10 — RETAINED CODE**:
  - All 3 migrations (Plan 01): `0013_iris_codex_schema.sql`, `0014_iris_codex_drop_legacy.sql`, `0015_reading_addons_table.sql` — DB shape stays
  - `report_v2 jsonb` column stays, but content shape becomes `{section_1_constituicao_temperamento: "markdown", section_2_mapa_organico: "markdown", ...}` instead of the JSON object. No new migration needed (jsonb is structure-agnostic).
  - Rebrand work (Plan 09): manifest, layout, sidebar, login/signup, env, PROJECT.md, README.md, tagline "A íris como mapa do ser." — all kept
  - `forbidden-terms.json` + `audit-vocabulary.mjs` (Plan 02 CI half) — kept, runtime audit-v2 deleted but CI gate remains
  - `AdvancedAnalysisCTA` + `VocabularyAuditBanner` (Plan 07) — kept (display-only placeholder + runtime audit display surface)
  - `analyze.ts` legacy markdown pipeline — extended (not replaced) to handle 14 sections + new prompt
  - `EditorAccordion` legacy renderer — extended or rebuilt for 14 sections (deciding during Plan 12)
  - Page routing infrastructure (Plan 08) — simplifies (drop v2 branching, single path)

### Why the redirection (founder rationale)

- The 8-block adaptive structure compressed the report in a direction that lost essential depth — verified empirically by founder on a fresh-reading regeneration before authorizing the rewrite.
- 14 dedicated sections give each clinical theme room to breathe (especially #3 timeline, #5 psychosomatic axis, #10 archetypal/spiritual, #12 anamnesis script).
- The §3 / §4 / §5 emotional-axis triad is core IP for integrative therapists — was compressed across `constitutional_pattern` + `clinical_note` + `therapeutic_synthesis` in the 8-block, which read as a clinical-functional gloss instead of integrative depth.
- Markdown gives Sonnet prose freedom; JSON values were cramping long-form clinical text with escape sequences and length budgets.

### Audit-trail note

This is the SECOND pivot inside Phase 7.4:
1. **Original 7.3 → 7.4 pivot (2026-05-13 earlier)**: 7.3 dual-layer + §11 toggle + 13 sections ABANDONED → 7.4 adaptive 8-block JSON + §11 placeholder + Iris Codex rebrand. Locked in the historical decisions below.
2. **Direction Correction (2026-05-13 this evening)**: 7.4 8-block ABANDONED → 7.4 14-section markdown + §11 toggle back + dual-layer stays as V1.1 placeholder. Locked in DC-1..DC-10 above.

The original content of this file (below) describes the **8-block design that is now SUPERSEDED**. It is preserved verbatim for audit trail. Do not implement against it.

---

**Gathered:** 2026-05-13 via `/gsd-discuss-phase 7.3` (que pivotou pra 7.4)
**Status:** Ready for planning
**Source:** Pivot arquitetural de produto durante discuss-phase. 7.3 (dual-layer) ABANDONED, 7.4 + 7.5 criadas.
**Sucessora de:** Fase 7.3 (ABANDONED — ver ROADMAP entry pra audit trail)
**Acompanha:** Fase 7.5 (mapping engine — IP central, criada simultaneamente, executável em paralelo)

> **⚠ The content below this line describes the SUPERSEDED 8-block design — see the Direction Correction at the top of this file for the locked replacement.**

<domain>
## Phase Boundary

Sonnet 4.6 emite **um único relatório clínico-funcional adaptativo** por leitura, em formato JSON estruturado (não mais markdown freeform), consumido por um renderer adaptativo que organiza o relatório por **achados clínicos relevantes** (não por template fixo de seções anatômicas). Schema `report_v2` substitui `report_generated` da Fase 7 original. Sistemas sem achados relevantes são OMITIDOS do output (não preenchidos com "sem alterações" — relatório é adaptativo, não template).

Concretamente:

- **Schema redesign:** novo jsonb `readings.report_v2` substitui `readings.report_generated` para readings novos (post-deploy). Shape:
  ```json
  {
    "report_version": "2.0",
    "executive_summary": "string",
    "constitutional_pattern": { "description": "string", "key_traits": ["string"] },
    "systems_with_tendency": [
      {
        "system_id": "string (snake_case)",
        "system_name": "string (pt-BR display)",
        "tendency_grade": 1-5,
        "tendency_label": "string ('leve', 'moderada', 'alta', etc.)",
        "clinical_description": "string (prosa funcional, sem jargão iridológico)",
        "associated_manifestations": ["string"],
        "investigation_points": ["string"],
        "therapeutic_direction": "string (prosa curta acionável)"
      }
    ],
    "integrative_axes": [
      { "axis_name": "string", "status": "ativo|latente|inativo", "description": "string" }
    ],
    "bilateral_findings": {
      "asymmetry_present": boolean,
      "description": "string|null"
    },
    "therapeutic_synthesis": "string",
    "priority_focus": ["string", "string", "string"],
    "clinical_note": "string (disclaimer protetor + nota clínica)",
    "advanced_analysis": {
      "available": true,
      "generated": false,
      "credit_cost": 1
    }
  }
  ```
- **Prompt-mestre reescrito do zero** em `apps/web/prompts/system.md`. **D-PR1 frozen contract (Fase 7 CONTEXT) está SUSPENDED** — SPEC.md §6 ficou obsoleto pro V1 atual; founder ainda pode optar por re-sync SPEC pós-7.4 stabilization.
- **Structured output JSON mode**: Sonnet chamado via Anthropic JSON mode (`response_format: { type: "json_schema", schema: <reportV2Schema> }`). Server faz **zod validation** pós-stream contra o schema; falha → retry com prompt corretivo carregando o JSON inválido + zod error message; max 2 retries; 3ª falha → save raw + flag para founder review (não bloqueia leitura, mas marca para investigação).
- **Streaming progressivo**: parse parcial de JSON enquanto chega. Estratégia recomendada (planner decide entre): (a) `@anthropic-ai/sdk` `messages.stream()` em conjunto com `partial-json` ou similar para parse incremental do JSON; (b) Sonnet emite top-level keys em ordem fixa, parser detecta cada chave completa e renderiza section block.
- **Renderer adaptativo**: `apps/web/components/readings/ReportAdaptiveView.tsx` (novo) substitui `EditorAccordion.tsx` no rendering path do `report_v2`. EditorAccordion continua existindo como **legacy renderer para `report_version='1.0'`** (25 readings existentes).
- **Add-on placeholder UI**: botão "Análise Iridológica Aprofundada — 1 crédito" visível em `/leituras/[id]` (após report gerado), abre modal "Em breve (V1.1)". Tabela `reading_addons` criada vazia em V1; populada por V1.1.
- **Vocabulary discipline expandido**: jargão iridológico formal + Sopro da Origem vocab + diagnóstico médico continuam proibidos via `audit-vocabulary.mjs` extension.
- **Rebrand "Aurel → Iris Codex"**: PROJECT.md + UI copy + voice rules + emails atualizados pra "Iris Codex".

**Fora do escopo desta fase:**

- **Mapping engine (sinais → tendências)** — Fase 7.5. 7.4 ship com `tendency-mapping-placeholder.ts` hand-crafted heuristic (1-2 regras por sistema). 7.5 substitui depois.
- **Add-on technical analysis implementation** — V1.1. 7.4 entrega só a tabela `reading_addons` (schema vazio) + botão UI placeholder. 2ª chamada Sonnet, prompt diferente, fluxo de cobrança de crédito — tudo fica pra V1.1.
- **Add-on family** (relatório cliente final, plano terapêutico, comparação longitudinal, mapa nutricional, recomendações de exames) — V1.1+. Schema `reading_addons` é extensível pra acomodar.
- **Credit system (saldo, transações, billing)** — V1.1+. 7.4 só renderiza tooltip "Em breve" no modal do add-on. Phase 8 (Pagamento) é o lar provável.
- **Phase 10 telemetry redesign** — defer. 7.4 adiciona `report_v2_edit_diff jsonb` shape `{ system_id: { delta_type, char_delta, ... } }` como ponto de partida; Phase 10 reusa OU redesigna quando for planejada.
- **Reanalise dos 25 readings legacy** — não-fazer. Backfill `report_version='1.0'`; legacy renderiza via path legacy (EditorAccordion atual). New readings post-deploy = `report_version='2.0'` via `ReportAdaptiveView`.
- **§11 affirmations no V1 relatório padrão** — drop. Placeholder field `advanced_analysis.affirmations` no schema reserved pra future add-on "Mensagem para cliente final" (V1.1).
- **§13 mensagem final como bloco separado** — absorvido por `clinical_note` no novo schema.
- **D-PR1 SPEC.md §6 re-sync** — opcional pós-7.4. SPEC.md §6 estava locked como source-of-truth do prompt-base original; founder decide na review pós-7.4 se atualiza SPEC ou deixa SPEC.md §6 marked OBSOLETE.

</domain>

<decisions>
## Implementation Decisions

### Schema redesign — D-SCH*

- **D-SCH1 (`readings.report_v2 jsonb` novo, NÃO sobrescreve `report_generated`):** migration `0013_iris_codex_report` faz:
  ```sql
  ALTER TABLE readings
    ADD COLUMN report_v2 jsonb,
    ADD COLUMN report_v2_delivered jsonb,
    ADD COLUMN report_v2_edit_diff jsonb DEFAULT '{}',
    ADD COLUMN report_v2_generated_at timestamptz,
    ADD COLUMN report_v2_delivered_at timestamptz,
    ADD COLUMN report_version text DEFAULT '2.0' NOT NULL;

  -- Backfill: existing readings (com report_generated populated) ficam como legacy 1.0
  UPDATE readings SET report_version = '1.0' WHERE report_generated IS NOT NULL;

  -- New table for add-on family (V1.1+)
  CREATE TABLE reading_addons (
    id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
    reading_id uuid NOT NULL REFERENCES readings(id) ON DELETE CASCADE,
    addon_type text NOT NULL CHECK (addon_type IN (
      'iridological_deep', 'client_facing', 'therapeutic_plan',
      'longitudinal_comparison', 'nutritional_map', 'exam_recommendations'
    )),
    generated_content jsonb,
    generated_at timestamptz,
    credit_cost int NOT NULL DEFAULT 1,
    model_version text,
    therapist_id uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
    created_at timestamptz DEFAULT now()
  );
  CREATE INDEX idx_reading_addons_reading_id ON reading_addons(reading_id);
  CREATE INDEX idx_reading_addons_therapist_id ON reading_addons(therapist_id);

  -- RLS: therapist sees only own
  ALTER TABLE reading_addons ENABLE ROW LEVEL SECURITY;
  CREATE POLICY reading_addons_therapist_select ON reading_addons FOR SELECT USING (therapist_id = auth.uid());
  CREATE POLICY reading_addons_therapist_insert ON reading_addons FOR INSERT WITH CHECK (therapist_id = auth.uid());
  ```
  **Não dropa** `report_generated`/`report_delivered`/`audit_metadata`/`report_generated_at` — continua hospedando legacy `report_version='1.0'`. Drop é polish pós-V1.1 quando todos legacy readings forem migrados ou arquivados.

- **D-SCH2 (Drop GENERATED column `ai_report_raw` + função `jsonb_concat_sections_pt_br()`):** Estes eram artefatos de D-P1 (Fase 7) para manter contrato LLM-04 retro-compat. **LLM-04 contract revogado** (texto concatenado não faz sentido pra report adaptativo variável; PDF entrega via Fase 8 vai usar `report_v2` direto). Migration 0013 inclui:
  ```sql
  ALTER TABLE readings DROP COLUMN ai_report_raw;
  ALTER TABLE readings DROP COLUMN ai_report_edited;
  DROP FUNCTION IF EXISTS jsonb_concat_sections_pt_br(jsonb);
  ```
  **Risk check:** grep code para `ai_report_raw|ai_report_edited` references antes de migration apply; planner verifica. Esperado: zero callers em web app (UI consome `report_generated` jsonb diretamente).

- **D-SCH3 (`report_v2_edit_diff` per-system-id, não per-section-number):** shape `{ system_id: { delta_type: 'adicionado'|'removido'|'corrigido'|'reescrito', char_delta: int, levenshtein_pct: float }, executive_summary: {...}, therapeutic_synthesis: {...}, ... }`. Chaves: cada `system_id` em `systems_with_tendency[]` + top-level keys editáveis (executive_summary, therapeutic_synthesis, priority_focus, clinical_note). Phase 10 telemetry contract redesign de Phase 10 reusa ou reformata; **não é responsabilidade de 7.4 garantir Phase 10 compatibility forward**.

### Prompt rewrite — D-PR*

- **D-PR1 NEW (`apps/web/prompts/system.md` reescrito do zero):** comment header marca "Iris Codex V1 prompt — substitui Fase 7 D-PR1 SPEC.md §6 frozen contract". Conteúdo cobre:
  - Identidade: "analista clínico-funcional Iris Codex" (não mais "analista iridológico integrativo"). Brand explicit.
  - **Princípios revisados:**
    1. Você não diagnostica — usa "tendência a", "sugere", "considere investigar"
    2. Você não inventa sinais — recebe tendencies estruturadas (não vision_features direto)
    3. Você usa o conhecimento RAG fornecido (continua)
    4. Linguagem clínica funcional **OBRIGATÓRIA** — proibido jargão iridológico no relatório padrão (constituição linfática, anel nervoso, hN codes, Jensen-codes formais, "trama"). Usa equivalentes funcionais ("sobrecarga linfática" → "tendência inflamatória sistêmica + drenagem comprometida"; "anel nervoso" → "tensão nervosa autônoma elevada").
    5. **Vocabulário proibido absoluto:** "diagnóstico", "tratamento", "cura" (continua, LGPD-06)
    6. **Vocabulário proibido Iris Codex:** linguagem espiritual Sopro da Origem ("centelha divina", "atravessar", "vasto", "essência primordial", etc. — lista exata a confirmar na plan-phase). Razão: Iris Codex = brand científico-clínico separado de Sopro da Origem.
    7. **Estrutura adaptativa:** sistemas sem achados são OMITIDOS — não criar bloco "sem alterações" ruidoso.
  - **Schema do output:** prompt referencia o JSON schema (já enforced via response_format), descreve cada campo, dá exemplos few-shot.
  - **Vozes (mantém regra das duas vozes da Fase 7):** achados geométricos = factual firme; interpretações = hipotética; nunca misturar na mesma cláusula. Reescrito pra context novo (não mais "fato Jensen vs hipótese clínica" — agora "manifestação clínica documentada vs tendência inferida").

- **D-PR2 (Input contract pro prompt — não muda Sonnet's view do `vision_features`, muda input layer):** Sonnet recebe (a) `<client_context>` (nome, idade, sexo, queixa — mesmo da Fase 7), (b) `<tendencies>` (output da camada de mapping — em 7.4 vem do placeholder, em 7.5 vem da engine real), (c) `<knowledge>` (RAG chunks ancorados nos schools das tendências). **Não recebe mais vision_features direto** — engine pré-processa. Reduz hallucination + força engine a ser source-of-truth para "qual sistema com qual grade".

- **D-PR3 (Mapping placeholder em 7.4):** `apps/web/lib/tendency-engine/placeholder.ts` exporta `mapVisionFeaturesToTendencies(features: VisionFeatures): Tendency[]` com hand-crafted heuristics (1-2 regras por sistema, ~8 sistemas funcionais). Comment marker `// TODO Phase 7.5 — replace with real engine` em cada regra. Founder dogfooding suficiente com placeholder até 7.5 land.

### Structured output + validation — D-VAL*

- **D-VAL1 (Anthropic JSON mode + zod schema):** chamada Sonnet:
  ```ts
  const reportV2Schema = z.object({ /* shape from D-SCH1 */ })
  const reportV2JsonSchema = zodToJsonSchema(reportV2Schema)

  const stream = await anthropic.messages.stream({
    model: 'claude-sonnet-4-6',
    response_format: { type: 'json_schema', json_schema: { name: 'report_v2', schema: reportV2JsonSchema } },
    system: systemPrompt,
    messages: [{ role: 'user', content: userPrompt }],
    max_tokens: 4096,
  })
  ```
  Post-stream: `const parsed = reportV2Schema.safeParse(JSON.parse(rawOutput))`. Falha → retry path.

- **D-VAL2 (Retry path on validation fail):** se `parsed.success === false`:
  ```ts
  // Retry 1: feed Sonnet o JSON inválido + zod errors
  const retryUserPrompt = `O JSON anterior falhou validação. Erros: ${JSON.stringify(parsed.error.format())}.
  Output anterior: ${rawOutput}.
  Por favor produza um JSON válido conforme schema acima.`
  ```
  Max 2 retries (1ª + 2ª retry); 3ª falha → save raw output em `audit_metadata.invalid_json_output` + flag `audit_metadata.json_validation_failed=true` + render UI com "Erro de geração — founder review needed". Não bloqueia leitura.

- **D-VAL3 (Streaming + incremental render):** decisão entre 2 abordagens (planner pesquisa estado da arte 2026):
  - **(a) Partial JSON parsing**: lib `partial-json` ou `streaming-json-parser`. Parse incrementa enquanto stream flows; UI atualiza por key. Risco: lib maturity.
  - **(b) Top-level keys em ordem fixa**: prompt instrui Sonnet a emitir keys em ordem (`executive_summary` primeiro, então `constitutional_pattern`, etc.); parser detecta cada key completa via regex `"key_name":\s*"..."` ou `"key_name":\s*{...}`. Mais simples, sem nova dependência.
  - **Recommendation:** (b) primeiro (no new dep), (a) fallback se UX precisar finer-grain progress.

### Renderer adaptive — D-UI*

- **D-UI1 (`ReportAdaptiveView` novo componente):** `apps/web/components/readings/ReportAdaptiveView.tsx` renderiza `report_v2` jsonb. Estrutura visual:
  - Top: executive_summary card (prosa)
  - Constitutional pattern card (description + key_traits chips)
  - **systems_with_tendency** array → cards individuais, **ordenados por `tendency_grade` desc**, cada card mostra:
    - Header: `{system_name}` + badge `Grade {grade}/5` + label string + visual bar (CSS — 5 segments, filled per grade)
    - Body: clinical_description (markdown prose)
    - 2 columns: `associated_manifestations` (bullets) + `investigation_points` (bullets)
    - Footer: therapeutic_direction (highlighted prose)
  - Integrative axes section (list de axes com status badges)
  - Bilateral findings card (only if `asymmetry_present`)
  - Therapeutic synthesis card
  - Priority focus card (top 3 numbered)
  - Clinical note (small, gray, bottom)
  - **Add-on placeholder button at bottom**: "Análise Iridológica Aprofundada — 1 crédito" → modal "Em breve (V1.1)"

- **D-UI2 (Inline editor per-block):** cada card editável via "Editar" button → expande textarea inline (mantém prose markdown). Save → server action `saveReportV2Delivered(readingId, reportV2)`. Diff computed per-key (system_id ou top-level key) ao salvar.

- **D-UI3 (Legacy renderer fallback):** `/leituras/[id]/page.tsx` lê `report_version`:
  ```tsx
  if (reading.report_version === '2.0' || reading.report_v2 != null) {
    return <ReportAdaptiveView report={reading.report_v2} ... />
  }
  // legacy path:
  return <EditorAccordion reportGenerated={reading.report_generated} ... />
  ```
  EditorAccordion **não é tocado**; ship as-is for legacy. 25 readings existentes acessíveis sem regressão.

- **D-UI4 (Grade visual bar):** 5 segments horizontal, filled left-to-right per grade. Cores per label:
  - grade 1 ("leve") — bg-yellow-100
  - grade 2 ("leve-moderada") — bg-yellow-300
  - grade 3 ("moderada") — bg-orange-400
  - grade 4 ("alta") — bg-red-500
  - grade 5 ("muito alta") — bg-red-700
  Confirmar paleta exata com founder no plan-phase (UI-SPEC se vier).

### Vocabulary discipline expanded — D-VOC*

- **D-VOC1 (`audit-vocabulary.mjs` extension):** adiciona 3 novos pattern sets:
  - **Jargão iridológico formal (proibido no relatório padrão, allowlist em add-on iridological_deep):**
    Lista preliminar (confirmar na plan-phase): `constituição linfática`, `constituição hematogênea`, `constituição mista`, `anel nervoso`, `anel de tensão`, `linfática rosary`, `trama`, `lacuna grau \d`, `radii solaris`, `tofus`, `sodium ring`, `senile arc`, `escola alemã`, `escola Jensen`, `Jensen.*tradição`, `Lo Rito.*topografia`, `Battello`, `Deck.*Angerer`, `Lindemann`, `hN(\d+)`, `setor \d+h\d+`, `feature\.`, `\[ancorado em`, "pipeline detectou", "Modal output".
  - **Sopro da Origem vocab (proibido absoluto em Iris Codex):**
    Lista preliminar (confirmar): `centelha divina`, `atravessar`, `vasto`, `essência primordial`, `sopro`, `origem`, `princípio criador`, `mistério`, `caminho da alma`, etc.
  - **Diagnóstico médico (mantém Fase 7 D-A2):** word-boundary `\b(diagnóstico|tratamento|cura)\b/i`.

- **D-VOC2 (Allowlist comment marker):** prompt files (`system.md`) podem citar palavras proibidas pra instruir LLM a evitar. Mesmo pattern da Fase 7 D-A4 — comment marker `<!-- audit-vocabulary:allowlist -->` no topo do arquivo.

- **D-VOC3 (Audit gate runtime):** após zod validation success, server roda audit-vocabulary sobre cada string field do `report_v2` (executive_summary, clinical_description per system, therapeutic_synthesis, etc.). Hits → `audit_metadata.iridological_jargon[]`, `audit_metadata.sopro_vocab[]`, `audit_metadata.forbidden_vocab[]`. **Não bloqueia** save (mesmo pattern D-A2 Fase 7) — banner persistente no editor "Termos proibidos detectados — revise antes de entregar". Save de `report_v2_delivered` exige zero hits (mesmo pattern D-A2 hardgate).

### Rebrand "Aurel → Iris Codex" — D-BR*

- **D-BR1 (PROJECT.md update):** rename produto de "Aurel Iris" para "Iris Codex". Documenta separação de "Sopro da Origem" (linha espiritual, fora deste codebase). Update tagline + value proposition copy.

- **D-BR2 (UI copy + emails + manifest):** grep "Aurel" em `apps/web/**` + emails templates + PWA manifest.json + landing copy; replace por "Iris Codex". Planner enumerates touchpoints.

- **D-BR3 (Voice rules em system prompt):** prompt-mestre referencia explicitly "como analista clínico-funcional do Iris Codex". Founder identity check: Iris Codex é "marca científico-clínica"; Sopro da Origem é "linha espiritual" — separação explícita pra evitar cross-contamination de tom.

### Add-on placeholder — D-ADD*

- **D-ADD1 (Tabela `reading_addons` criada vazia em V1):** schema em D-SCH1. V1 NÃO popula; V1.1 implementa o flow de cobrança + 2ª chamada Sonnet + persist.

- **D-ADD2 (Botão UI placeholder):** componente `AdvancedAnalysisCTA.tsx` em `apps/web/components/readings/`. Renderiza no `ReportAdaptiveView` footer. Click → modal com:
  - Header: "Análise Iridológica Aprofundada"
  - Body: "Em breve (V1.1). Esta análise técnica usa nomenclatura iridológica formal (Jensen, Lo Rito, Deck/Angerer, Lindemann) com correlação multi-escola, dados estruturados de sinais por zona, e confiabilidade por observação. Custo previsto: 1 crédito por leitura. Persistência: análise salva por paciente, terapeuta acessa quantas vezes quiser depois sem cobrar de novo."
  - Footer: "OK" (fecha modal). Não cria row em `reading_addons` em V1.

- **D-ADD3 (Family de add-ons docs):** PROJECT.md + ROADMAP V1.1 section documenta os 5 add-ons previstos: iridological_deep, client_facing, therapeutic_plan, longitudinal_comparison, nutritional_map, exam_recommendations. Schema `reading_addons.addon_type` CHECK constraint reflete os 6 valores (5 V1.1 + iridological_deep que é o primeiro).

### Telemetry — D-TEL*

- **D-TEL1 (Phase 10 contract redesign deferred):** `report_v2_edit_diff` shape (D-SCH3) é ponto de partida; Phase 10 redesigna quando for planejada. 7.4 não bloqueia em Phase 10 compatibility forward.

- **D-TEL2 (Structured log per generation):** mesmo pattern Fase 7 D-T1:
  ```ts
  console.info({
    event: 'iris_codex_report_generate',
    reading_id, therapist_id,
    model_version: 'claude-sonnet-4-6',
    report_version: '2.0',
    n_chunks_rag, latency_ms, tokens_in, tokens_out, cost_estimate_usd,
    systems_detected: tendencies.length,
    grade_distribution: { 1: N, 2: N, ..., 5: N },
    json_validation_passed: boolean,
    retry_count: 0|1|2,
    iridological_jargon_hits, sopro_vocab_hits, forbidden_vocab_hits
  })
  ```

### Existing readings legacy path — D-LEG*

- **D-LEG1 (Backfill SQL legacy):** `UPDATE readings SET report_version='1.0' WHERE report_generated IS NOT NULL` na migration 0013.

- **D-LEG2 (Renderer routing):** D-UI3 (legacy fallback). EditorAccordion + legacy schema mantidos sem regressão.

- **D-LEG3 (Não reanalisar):** founder dogfooding pre-V1; readings legacy não precisam migrar pro novo schema. Future founder action: pode escolher "regenerar com Iris Codex" via botão (similar ao "regenerar análise" existing) que dispara fluxo 7.4 sobre vision_features existing.

### Claude's Discretion

- **Lista exata de termos proibidos** (jargão iridológico + Sopro vocab) — to be confirmed com founder na plan-phase. CONTEXT lista preliminar; planner faz pass dedicado de definição.
- **Streaming strategy** (D-VAL3) — (b) top-level keys ordem fixa recomendado primeiro, (a) partial-json fallback se UX precisar.
- **Grade visual bar paleta** (D-UI4) — sugerida; founder confirma na UI-SPEC se rodar antes de plan.
- **Schema `system_id` enum** — confirmar valores exatos com founder. Sugestão preliminar: `linfatico`, `hepatico_biliar`, `renal`, `digestivo`, `nervoso_autonomo`, `cardiovascular`, `endocrino`, `imune`, `respiratorio`, `musculoesqueletico`, `pele_tegumento`, `reprodutor`.
- **`tendency_label` strings** — devem casar com grade 1-5 (1='leve', 2='leve-moderada', 3='moderada', 4='alta', 5='muito alta')? Ou Sonnet decide texto livre? Recomendação: enum fixo no schema pra evitar drift.
- **Retry max-count** (D-VAL2) — 2 retries pre-flag. Founder pode preferir 3 (mais lenient) ou 1 (mais aggressive flagging). Recomendação: 2 (ponto médio).
- **Prompt cache strategy** — Sonnet `cache_control` no system prompt (mesmo pattern Fase 7) — economiza ~90% custo em retries dentro de 5min. Recomendação: usar.
- **Naming convention sub-componentes** de ReportAdaptiveView — Card, SystemTendencyCard, IntegrativeAxisItem, etc. Planner decide.
- **Migration 0013 split em sub-migrations** — 0013a (schema additions + report_version backfill) + 0013b (drop ai_report_raw/edited GENERATED + jsonb_concat_sections_pt_br func) + 0013c (CREATE TABLE reading_addons + RLS). Recomendação: 3 sub-migrations (atomicidade per concern).

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Phase 7.3 (ABANDONED) — audit trail
- `.planning/ROADMAP.md` linhas ~310-360 — Fase 7.3 entry ABANDONED com rationale de pivot. Substituída por 7.4 + 7.5.

### Fase 7 Original (frozen contract SUSPENDED for V1)
- `.planning/phases/07-analise-llm/07-CONTEXT.md` — decisões D-* originais. Fase 7.4 SUSPENDE D-PR1 (SPEC.md §6 frozen contract) e SUBSTITUI D-P3 (jsonb shape) com schema novo. D-A1..A4 (audit) ainda aplicável. D-U1..U3 (editor) substituído por D-UI1..4 nesta fase. D-S1..S4 (streaming + cost guards) ainda válido em essência.
- `apps/web/prompts/system.md` (current) — prompt original Fase 7, **será reescrito do zero** em 7.4.
- `apps/web/lib/anthropic/parser.ts` — section-boundary parser markdown. **Obsoleto pro report_v2** (JSON streaming). Mantido para legacy path.
- `apps/web/components/readings/EditorAccordion.tsx` — 13 sections accordion. **Mantido as legacy renderer** para `report_version='1.0'`.

### Phase 10 (Sistema de Aprendizagem Clínica)
- `.planning/phases/10-aprendizagem-clinica/10-CONTEXT.md` — telemetry contract original baseado em 13 seções. **Quebrado por 7.4**. Phase 10 redesigna quando for planejada. 7.4 adiciona `report_v2_edit_diff` per-system-id como ponto de partida.

### Vision pipeline (Fase 5 — features input)
- `apps/web/lib/rag/build-queries.ts` — `IrisFeaturesForRag` interface; Phase 7.5 mapping engine consome direto, Phase 7.4 consome via placeholder.
- `vision-service/data/jensen-map.json` — mapa setorial pt-BR; **NÃO usado no prompt do Iris Codex padrão** (jargão proibido); usado no add-on iridological_deep V1.1.

### RAG retrieval (Fase 6)
- `apps/web/lib/rag/search.ts` — `retrieveRelevantKnowledge` mesmo contrato. 7.5 mapping engine chama pra school_attribution; 7.4 prompt consome chunks via `<knowledge>` block.

### Schema atual
- `apps/web/types/database.ts` linhas 186-251 — shape atual de `readings` Row/Insert/Update. Migration 0013 muda este arquivo via `pnpm gen:types`.
- `supabase/migrations/` — diretório; próximo número livre = 0013.
- `supabase/migrations/0007_phase_7_analise_llm.sql` — referência da migration original que 7.4 atualiza.

### Existing dogfooding context
- `.planning/todos/pending/2026-05-09-dogfooding-iteration-1.md` — todo do dogfooding; item A1 (16 seções) é **descartado** pelo pivot (relatório adaptativo). Items P0c/A2/A3 (voice rules) parcialmente aplicáveis ao novo prompt (regra duas vozes mantida, declaração de base mantida); planner avalia incorporação na plan-phase.

### Standards do projeto
- `apps/web/CLAUDE.md` (se existir) — guidelines pt-BR.
- `apps/web/package.json` — `@anthropic-ai/sdk@^0.92.0` instalado; `zod` (verificar instalação — provavelmente sim); `zod-to-json-schema` (planner adiciona se não tiver); `react-markdown` + `remark-gfm` (provavelmente já instalado pro EditorAccordion).
- `apps/web/scripts/audit-vocabulary.mjs` — pattern atual; 7.4 estende com novos pattern sets.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets

- **`@anthropic-ai/sdk@^0.92.0`** já instalado. Suporta `response_format` JSON mode + streaming.
- **`retrieveRelevantKnowledge`** (Fase 6) consumida via novo input contract — chunks ancorados em school_attribution das tendencies.
- **`apps/web/lib/supabase/server.ts createClient()`** — server-side RLS. Usar para load + persistência incremental do `report_v2`.
- **Sonner toast** — feedback de generation success/error + add-on placeholder modal.
- **`apps/web/components/ui/`** — shadcn primitives (Card, Button, Badge, Dialog, Tabs). Provavelmente já instalados; planner verifica.
- **`StatusBadge.tsx`** — mantém. Estados intermediários do stream cliente-side.

### Established Patterns

- **Vocabulário proibido auditado em CI:** padrão Fase 3+ estendido com 3 novos pattern sets. `system.md` é exception via allowlist comment marker (mesmo D-A4 Fase 7).
- **Migration ALTER TABLE + função IMMUTABLE:** template `0007_phase_7_analise_llm.sql`. 7.4 split em 3 sub-migrations (0013a/b/c) recomendado.
- **Telemetria estruturada `console.info({ event: ... })`** sem PII — mesmo pattern Fase 7 D-T1.
- **`'use server'` em Server Actions, `import 'server-only'` em libs** — `lib/anthropic/analyze.ts` reescrito mantém pattern.
- **`ReadingStatus` type em `StatusBadge.tsx`** — fonte da verdade do enum no client. Sem mudança em D-P4 enum.
- **Route Handler com auth gate** — analyze route mantém pattern (user logado + reading.therapist_id === user.id + reading.status === 'ready' + reading.report_delivered IS NULL + regeneration_count < 3).

### Integration Points

- **Trigger:** botão "Gerar análise" em `/leituras/[id]` (já existe, AnalysisCTA). Para readings com `report_version='2.0'` ou ainda sem report, POST `/api/readings/[id]/analyze` chama new flow.
- **Renderer router:** `/leituras/[id]/page.tsx` lê `report_version`; routes para `ReportAdaptiveView` (new) ou `EditorAccordion` (legacy).
- **Editor router:** `/leituras/[id]/editar/page.tsx` mesmo split — `ReportAdaptiveEditor` (new) ou `EditorAccordion` (legacy).
- **Add-on button:** `AdvancedAnalysisCTA.tsx` no `ReportAdaptiveView` footer. Modal "Em breve V1.1".
- **Webhook Fase 5** continua igual (vision_features). Engine 7.5 mapping consume; 7.4 placeholder consume também.
- **Phase 10 future:** redesigna telemetry contract quando for planejada; consome `report_v2_edit_diff`.

</code_context>

<deferred>
## Deferred Ideas

- **Mapping engine (sinais → tendências) real implementation** — Fase 7.5 (criada simultaneamente, IP central, executa em paralelo). 7.4 ship com placeholder.
- **Add-on technical analysis implementation** (2ª chamada Sonnet, prompt formal iridológico, cobrança de crédito, persist em `reading_addons`) — V1.1.
- **Add-on family completa**: relatório cliente final, plano terapêutico, comparação longitudinal, mapa nutricional, recomendações de exames — V1.1+. Tabela `reading_addons` extensível.
- **Credit system** (saldo, transações, billing) — V1.1 + Phase 8 coordination. 7.4 só renderiza tooltip "Em breve" no modal.
- **Phase 10 telemetry contract redesign** — Phase 10 territory quando for planejada. 7.4 entrega `report_v2_edit_diff` per-system-id como ponto de partida.
- **Reanalisar 25 readings legacy** — não fazer. Backfill `report_version='1.0'`. Founder pode escolher regenerar individualmente.
- **§11 affirmations no V1** — drop. Placeholder field `advanced_analysis.affirmations` reservado pra future add-on "Mensagem para cliente final".
- **§13 mensagem final como bloco separado** — absorvido por `clinical_note`.
- **D-PR1 SPEC.md §6 re-sync** — opcional pós-7.4 stabilization. Founder decide se atualiza SPEC ou marca §6 OBSOLETE.
- **PDF generation do relatório** — Phase 8 (junto com termo LGPD). Consome `report_v2` direto.
- **A1 (16 seções)** do todo `2026-05-09-dogfooding-iteration-1.md` — **descartado pelo pivot**. Relatório adaptativo substitui template fixo.
- **Auto-trigger analyze** post-webhook — Phase 9 polish se UAT pedir.
- **Lista exata de termos proibidos** (jargão iridológico + Sopro vocab) — confirmar na plan-phase.
- **ML/LLM-based mapping engine** — Phase 10 se learning emergir. 7.5 é rules-based deterministic.
- **`tendency_label` enum vs free string** — recomendação enum no D-VAL1; founder pode flexibilizar.
- **Founder may opt to re-enable some iridological terms in primary report** post-UAT se feedback pedir nuance — currently locked OUT.

</deferred>

---

*Phase: 07.4-iris-codex-report*
*Context gathered: 2026-05-13 via /gsd-discuss-phase 7.3 (pivotou pra 7.4)*
*Pivot decisions locked: 7.3 ABANDONED + 7.4 nova arch + 7.5 mapping engine + Iris Codex rebrand + §11 → add-on V1.1 + 7.2-A → 7.5 + reading_addons table*
*Next: `/clear` then `/gsd-plan-phase 7.4`*
