---
phase: 07.4
plan: 12
type: execute
wave: 10
depends_on: [10, 11]
supersedes: []
files_modified:
  - apps/web/components/readings/AnalysisStream.tsx
  - apps/web/components/readings/AnalysisStream.test.tsx
  - apps/web/app/(dashboard)/leituras/[id]/analise-client.tsx
  - apps/web/components/readings/EditorAccordion.tsx
  - apps/web/components/readings/__tests__/EditorAccordion.test.tsx
autonomous: true
requirements: []
source_decisions: [DC-1, DC-2, DC-6, DC-8]
must_haves:
  truths:
    - "AnalysisStream renders 'Gerando relatório… {n}/14 seções' (was 13)"
    - "AnalysisStream Skeleton stack shows 14 placeholders (was 13)"
    - "AnalysisStream aria-live announcer says N/14"
    - "analise-client.tsx stream-buffer detector accepts §1..§14 boundaries (Plan 11 parser already accepts; client-side detector mirrors)"
    - "EditorAccordion renders all 14 sections with new titles (Plan 11 already updated keys + titles; verify visual integrity + §14 visual distinction)"
    - "§14 Mensagem para o Cliente card has a distinct visual treatment (e.g., bg-amber-50/30 background + 'Voz do terapeuta' subtitle) to signal it's the warm-voice client-delivered closer per DC-6"
    - "Tests updated to reflect 14-section counts + titles"
    - "pnpm build + tsc + lint + test all green"
  artifacts:
    - path: ".planning/phases/07.4-iris-codex-report/07.4-12-SUMMARY.md"
      provides: "Plan 12 execution record"
deferred:
  - "§11 Afirmações optional toggle (DC-5) — defers to a hypothetical Plan 14 after founder UAT verdict. Plan 11's compromise: Afirmações lives OUTSIDE the §1..§14 numbered sequence. Plan 12 ships the static 14-section UI; if founder UAT (Plan 13) wants the toggle, Plan 14 adds it (single Sonnet call + parser tolerance OR second LLM call into a separate jsonb key). Documenting this defer here so it's not lost."
---

<objective>
Make the legacy UI surface (EditorAccordion + AnalysisStream + analise-client) render the new 14-section reports cleanly. Plan 11 already updated section keys + titles in EditorAccordion. Plan 12 finishes: stream counter bumped from 13 → 14, client-side boundary detector accepts §1..§14, §14 gets a distinct warm-voice visual treatment, tests updated.

Defer §11 Afirmações toggle (DC-5) to a follow-up plan after founder UAT. Static 14-section UI first; toggle is polish.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.4-iris-codex-report/07.4-CONTEXT.md
@.planning/STATE.md
@apps/web/components/readings/AnalysisStream.tsx
@apps/web/components/readings/EditorAccordion.tsx
@apps/web/app/(dashboard)/leituras/[id]/analise-client.tsx
@apps/web/components/readings/__tests__/AnalysisStream.test.tsx
@apps/web/components/readings/__tests__/EditorAccordion.test.tsx
@apps/web/lib/anthropic/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: AnalysisStream → 14 sections</name>
  <files>
    apps/web/components/readings/AnalysisStream.tsx (MODIFY)
    apps/web/components/readings/__tests__/AnalysisStream.test.tsx (MODIFY)
  </files>
  <action>
1. In `AnalysisStream.tsx`:
   - Top docstring line 7: `0..13` → `0..14`
   - Line 11: `{N}/13 seções` → `{N}/14 seções`
   - Line 55 JSX: `{safe}/13 seções` → `{safe}/14 seções`
   - The `safe` clamp constant (likely `Math.min(Math.max(0, sectionsReceived), 13)`) → bump max to 14
   - Skeleton stack: if it's an array `.from({ length: 13 })` or hardcoded 13 Skeleton blocks → bump to 14
   - Section title array (if hardcoded for visual cues) → use the 14 new titles from `apps/web/lib/anthropic/types.ts SECTION_KEY_BY_NUMBER` value-side or hardcode the 14 short titles. Order: Constituição e Temperamento, Mapa Orgânico, Linha do Tempo Emocional, Padrões Emocionais Ativos, Eixo Psicossomático, Heranças Transgeracionais, Carências Funcionais, Estado Mental e Nervoso, Recursos e Forças, Dimensão Arquetípica, Sugestões Integrativas, Roteiro de Anamnese, Síntese Integrativa, Mensagem para o Cliente.

2. In `AnalysisStream.test.tsx`:
   - Update `Math.min` / clamp assertion if it tests 13
   - Update '{N}/13 seções' assertions to '{N}/14 seções'
   - Update Skeleton-count expectations to 14
   - If test asserts specific section titles, update to the new 14
  </action>
  <verify>
    <automated>grep -c "14 seções\|/14 seções\|, 14)" apps/web/components/readings/AnalysisStream.tsx; grep -c "13 seções\|, 13)" apps/web/components/readings/AnalysisStream.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "/14 seções" apps/web/components/readings/AnalysisStream.tsx` ≥ 1
    - `grep -c "/13 seções\|0\.\.13" apps/web/components/readings/AnalysisStream.tsx` returns 0
    - AnalysisStream test passes
  </acceptance_criteria>
  <done>Stream UI counts to 14.</done>
</task>

<task type="auto">
  <name>Task 2: analise-client.tsx stream detector → 14 sections</name>
  <files>apps/web/app/(dashboard)/leituras/[id]/analise-client.tsx (MODIFY)</files>
  <action>
Inspect the file for:
- Any constant or array like `SECTION_BOUNDARIES = [...13 elements]` → bump to 14
- Any regex for detecting `## §N — ` boundaries that hardcodes `[1-9]|1[0-3]` → change to `[1-9]|1[0-4]` (or just `\d{1,2}`)
- Any toast / progress copy referencing `13 seções` → `14 seções`
- Any `sectionsReceived` clamp at 13 → 14

If the file has the V2_KEYS_ORDERED constant or detectBlocksReceived function (left over from the now-deleted v2 path), confirm it was already removed in Plan 10. Plan 10's task 1 should have stripped that.
  </action>
  <verify>
    <automated>grep -n "13\b\|14\b\|seções\|sectionsReceived" apps/web/app/\(dashboard\)/leituras/\[id\]/analise-client.tsx | head -20</automated>
  </verify>
  <acceptance_criteria>
    - No reference to `13 seções` or boundary-detector limited to 13
    - tsc + lint clean
  </acceptance_criteria>
  <done>Client-side boundary detector aligned with parser range 1..14.</done>
</task>

<task type="auto">
  <name>Task 3: §14 Mensagem para o Cliente visual distinction</name>
  <files>
    apps/web/components/readings/EditorAccordion.tsx (MODIFY)
    apps/web/components/readings/__tests__/EditorAccordion.test.tsx (MODIFY)
  </files>
  <action>
Per DC-6, §14 is the warm-voice client-delivered closer. Give it visual distinction in EditorAccordion:

1. In EditorAccordion.tsx, find the rendering for the `14_mensagem_cliente` key (or whatever the iteration looks like). Wrap its AccordionItem (or equivalent block) with:
   - A subtle background class: `bg-amber-50/30` or `bg-orange-50/30` (warm tone — matches the brand's warmth direction)
   - A small label above the title or inline: `Voz do terapeuta · Para entrega ao cliente` (small caption, text-xs text-muted-foreground)
   - Keep the same `## §14 — Mensagem para o Cliente` accordion trigger title

2. Add a test that asserts §14 renders with the distinguishing class + the caption text. Use a 14-section fixture (you may need to update the test fixture in `__tests__/EditorAccordion.test.tsx` to include all 14 sections including `14_mensagem_cliente`).

3. If Plan 11 already added the 14-section keys to the test fixture, just add the `14_mensagem_cliente` distinguishing-render test on top of existing tests.
  </action>
  <verify>
    <automated>cd apps/web && pnpm test:run components/readings/__tests__/EditorAccordion.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - §14 has the warm-tone background class
    - §14 has the "Voz do terapeuta" caption
    - EditorAccordion test passes including the new §14 distinction test
  </acceptance_criteria>
  <done>§14 visually signals it's the client-delivered closer.</done>
</task>

<task type="auto">
  <name>Task 4: Verify end-to-end + SUMMARY + STATE</name>
  <action>
1. Run full verification:
   - `cd apps/web && pnpm build` → 0
   - `pnpm tsc --noEmit` → zero new errors
   - `pnpm lint` → 0 errors / 9 baseline warnings
   - `pnpm test:run` → expect 3 baseline failures (quality-scoring×3 only; prompts.test fixed by Plan 11)

2. Create `.planning/phases/07.4-iris-codex-report/07.4-12-SUMMARY.md`:
   - Frontmatter: phase, plan=12, subsystem=ui, tags=[14-section, accordion, stream, warm-voice]
   - provides: 14-section stream counter, 14-section EditorAccordion rendering, §14 distinct visual treatment
   - key-decisions: §11 Afirmações toggle DEFERRED to Plan 14 (post-UAT); §14 visual signal via amber-50/30 bg + caption
   - requirements-touched: addresses Direction Correction DC-1/2/6/8; DC-5 toggle deferred
   - self_check: PASSED with grep + test outputs

3. Update STATE.md:
   - status: "Phase 7.4 Plan 12 COMPLETE — 14-section UI surface. AnalysisStream counts {n}/14; analise-client detector aligned; EditorAccordion renders all 14 sections with §14 distinct warm-voice treatment. §11 Afirmações toggle (DC-5) DEFERRED to Plan 14 — Plan 13 founder UAT will determine if it's critical or polish. Phase 7.4 source-side ready for SC-10 retake."
   - stopped_at: "Plan 12 complete. Next: Plan 13 founder UAT on fresh reading with 14-section pipeline. Verify §14 brand voice, all 14 sections have depth, no jargon leakage. If UAT pass → /gsd-verify-phase 07.4 to close. If §11 toggle needed → add as Plan 14 follow-up."
   - last_updated: ISO now
   - completed_plans: 88 → 89
   - percent: 98 → 99

4. Final docs commit: `docs(07.4-12): SUMMARY + STATE — 14-section UI surface complete`
  </action>
  <acceptance_criteria>
    - All verification gates green
    - SUMMARY.md created
    - STATE.md updated
    - Final docs commit
  </acceptance_criteria>
  <done>Plan 12 complete; Plan 13 UAT ready.</done>
</task>

</tasks>

<success_criteria>
- All UI surfaces render 14 sections cleanly
- §14 visually distinguishable as the warm-voice client-delivered closer
- All tests pass
- Founder can run `pnpm dev` and walk through the full 14-section pipeline end-to-end
- §11 Afirmações toggle explicitly deferred and tracked
</success_criteria>

<output>
After completion, create `.planning/phases/07.4-iris-codex-report/07.4-12-SUMMARY.md`.
</output>
