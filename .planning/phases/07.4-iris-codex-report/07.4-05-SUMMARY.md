---
phase: 07.4-iris-codex-report
plan: 05
subsystem: api
tags: [route-handler, server-actions, streaming, zod, anthropic, lgpd, rls]

# Dependency graph
requires:
  - phase: 07.4-iris-codex-report
    provides: analyzeReadingV2 + ZodValidationFailedError (Plan 07.4-03)
  - phase: 07.4-iris-codex-report
    provides: detectCompletedKeys + REPORT_V2_TOP_LEVEL_KEYS (Plan 07.4-03)
  - phase: 07.4-iris-codex-report
    provides: runAuditV2 + classifyAllSystemsV2 (Plan 07.4-03)
  - phase: 07.4-iris-codex-report
    provides: mapVisionFeaturesToTendencies (Plan 07.4-04)
  - phase: 07.4-iris-codex-report
    provides: reportV2Schema + types (Plan 07.4-00)
  - phase: 07.4-iris-codex-report
    provides: report_v2 + report_v2_delivered + report_v2_edit_diff schema cols (Plan 07.4-01)
provides:
  - V2 branch in POST /api/readings/[id]/analyze (report_version='2.0' path)
  - saveReportV2Delivered server action (per-block + full-report save, non-blocking on audit)
  - deliverReportV2 server action (hard-gate on zero audit hits)
  - reportV2DeliveredSchema (Partial<ReportV2> for per-block submission)
  - End-to-end V2 pipeline callable from client (Plans 07.4-06+ consume)
affects: [07.4-06, 07.4-07, 07.4-07b, 07.4-08, 07.4-09, 8-pagamento, 10-aprendizagem-clinica]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "V2/V1 branching by report_version inside shared route handler"
    - "Canonical persistence ONCE after finalize (no mid-stream DB writes for V2)"
    - "LGPD client_name redaction on raw-output save (3rd-fail path)"
    - "Server actions extend existing 'use server' file with V2 counterparts (no new file)"
    - "Per-block save merge → full-schema re-validation → audit (non-blocking) → diff classify → persist"
    - "Hard-gate at deliver only; save remains forgiving (D-VOC3 split)"

key-files:
  created: []
  modified:
    - apps/web/app/api/readings/[id]/analyze/route.ts (V2 branch wired; +189/-3 lines)
    - apps/web/app/actions/analise.ts (saveReportV2Delivered + deliverReportV2; +210/-1 lines)
    - apps/web/app/actions/analise.schemas.ts (reportV2DeliveredSchema partial; +35 lines)

key-decisions:
  - "Insert V2 branch after gate (e) regen-cap and return early; legacy path remains UNTOUCHED for report_version='1.0' (zero regression for 25 existing readings)"
  - "Canonical report_v2 jsonb persists ONCE on stream completion — mid-stream disconnect = no persisted state, caller retries by re-invoking route (V1 limitation; per-key checkpoint is V1.1 polish)"
  - "detectCompletedKeys used only for client-progress signaling; not driving DB writes (decoupled from persistence)"
  - "Audit gate split: saveReportV2Delivered NON-blocking on hits (founder can keep editing); deliverReportV2 HARD-gates on totalHits===0 across all 3 vocab categories"
  - "Post-merge priority_focus length-3 invariant enforced on merged result (not on partial input), so per-block save of a different field doesn't trip"
  - "client_name redaction on 3rd-fail path: .split(clientName).join('[CLIENT_NAME_REDACTED]') before persist + 50KB cap to avoid jsonb bloat"
  - "Imported reportV2Schema in analise.schemas.ts via @/lib/anthropic/report-schema (server-only re-export was fine because schemas.ts has no 'use server' directive — only its sibling analise.ts does)"

patterns-established:
  - "Branched route handler: single auth-gate prelude → version branch → independent stream pipelines (V1 vs V2)"
  - "Server action sibling extension: legacy V1 action + V2 action coexist in same 'use server' file; only async functions exported; sync helpers stay module-private"
  - "Defense-in-depth audit: re-run runAuditV2 in deliverReportV2 even though save already audited (state could've changed)"

requirements-completed: [SC-3, SC-4, SC-5]

# Metrics
duration: 10min
completed: 2026-05-13
---

# Phase 7.4 Plan 05: Wire Wave 2 into HTTP/RPC surface — Summary

**V2 branch wired into POST /api/readings/[id]/analyze + saveReportV2Delivered/deliverReportV2 server actions extending existing analise.ts; legacy 1.0 path untouched; canonical persistence ONCE after finalize; LGPD client_name redaction on 3rd-fail.**

## Performance

- **Duration:** ~10 min (3 atomic tasks, no deviations)
- **Started:** 2026-05-13T23:47:42Z
- **Completed:** 2026-05-13T23:57:09Z
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments

- POST /api/readings/[id]/analyze branches on `reading.report_version`: V2 path invokes Iris Codex pipeline (tendency-engine → RAG → analyzeReadingV2 → stream + finalize + ZodValidationFailedError catch); legacy V1 (`'1.0'` or null) falls through to the existing Phase 7 analyzeReading flow with ZERO changes to its behavior.
- All 5 auth gates preserved verbatim (session via getUser, ownership via therapist_id === user.id, status === 'ready', not-yet-delivered with V2/V1 branch on report_v2_delivered vs report_delivered, regeneration_count < 3 with founder bypass).
- saveReportV2Delivered: validates Partial<ReportV2> → merges onto current state → re-validates merged result against full reportV2Schema → runs runAuditV2 (NON-BLOCKING per D-VOC3) → computes classifyAllSystemsV2 per-system_id diff → atomic UPDATE; returns audit so client can show VocabularyAuditBanner.
- deliverReportV2: HARD-gates on zero hits across all 3 vocab categories before flipping is_delivered=true.
- D-VAL2 3rd-fail path: ZodValidationFailedError catch saves sanitized raw output (client_name → [CLIENT_NAME_REDACTED] per RESEARCH §V8 LGPD, capped at 50KB) + flags audit_metadata.json_validation_failed=true + records zod_error_summary; stream emits `[ERRO_GERACAO: founder review needed]` sentinel + closes cleanly.

## Task Commits

Each task was committed atomically:

1. **Task 1: Extend analise.schemas.ts with reportV2DeliveredSchema** — `255759f` (feat)
2. **Task 2: Extend analise.ts with V2 server actions** — `5a99044` (feat)
3. **Task 3: V2 branch in route handler** — `f71edb5` (feat)

## Files Created/Modified

- `apps/web/app/actions/analise.schemas.ts` — Added `reportV2DeliveredSchema` (passthrough partial of reportV2Schema, skipping `.refine()` on systems_with_tendency) + `ReportV2DeliveredInput` type. Existing `readingIdSchema` + `reportDeliveredSchema` preserved.
- `apps/web/app/actions/analise.ts` — Added `saveReportV2Delivered(readingId, partial)` (D-UI2 per-block + D-VOC3 non-blocking save with merge → re-validate → audit → diff → persist) and `deliverReportV2(readingId)` (D-VOC3 hard-gate). Legacy `saveReportDelivered` + `markReadingDelivered` preserved untouched. Module-private `safeArray()` helper (NOT exported, complies with MEMORY rule about 'use server' files).
- `apps/web/app/api/readings/[id]/analyze/route.ts` — Added V2 branch after gate (e). SELECT clause extended with `report_v2`, `report_v2_delivered`, `report_version`, `therapist_notes`. Gate (d) now branches: V2 checks `report_v2_delivered`, legacy keeps `report_delivered`. V2 branch: vision_features normalization (eyeSource fallback identical to Phase 7) → mapVisionFeaturesToTendencies → retrieveRelevantKnowledge → analyzeReadingV2 → ReadableStream consumes stream + tracks completed-key count via detectCompletedKeys (progress signal only; NO mid-stream DB writes) → finalize() → ONE UPDATE on success path OR ZodValidationFailedError catch UPDATE with sanitized raw.

## Decisions Made

All decisions inherited from plan + CONTEXT.md (D-VAL1, D-VAL2, D-VAL3, D-UI2, D-UI3, D-SCH3, D-VOC3). One operational decision during execution:

- **`safeArray()` helper kept module-private in analise.ts** to comply with MEMORY rule (`'use server'` files only export async functions). Verified via grep that no top-level non-async export exists in analise.ts post-edit.

## Deviations from Plan

None — plan executed exactly as written. All 3 tasks completed in sequence with zero Rule 1/2/3 inline fixes.

The plan's literal action block proposed importing both `reportV2Schema` and `z` inside the `// Phase 7.4 additions` block of `analise.schemas.ts`; in execution I hoisted the `import { reportV2Schema }` to the top of the file alongside the existing `import { z } from 'zod'` for clean module-import grouping. This is a stylistic ordering choice within the bounds of the plan, not a functional deviation.

## Issues Encountered

None.

## Verification

- **tsc:** `pnpm tsc --noEmit` → ZERO new errors in the 3 modified files. Pre-existing Phase 3/5 baseline (22 errors in readings.test.ts, ReprocessButton.test.tsx, quality-scoring.test.ts, modal-client.test.ts) unchanged per MEMORY rule (feedback_quality_scoring_test_gate.md).
- **lint:** `pnpm lint` → 0 errors + 9 baseline warnings (all pre-existing in unrelated files); MEMORY rule honored (run lint before declaring done).
- **anthropic library tests:** `pnpm test:run lib/anthropic` → 133/139 passing; 1 pre-existing failure in `prompts.test.ts` (broken since Plan 02 system.md rewrite per STATE.md line 31, NOT introduced by Plan 05); 5 todo + 1 skipped baseline.
- **legacy route test file:** `pnpm test:run app/api/readings/[id]/analyze/__tests__/route.test.ts` → 16/16 todo stubs, no regression (file was always TDD scaffold).
- **Auth gate grep:** All 5 gates (Unauthenticated, Forbidden, not ready for analysis, already delivered, Regeneration limit) present in route.ts.
- **Update-call grep in V2 branch:** Exactly 2 `.update({` calls inside V2 branch (success persist at line 220 + zod-catch raw save at line 247). No mid-stream DB writes.
- **Acceptance grep:** `analyzeReadingV2`, `mapVisionFeaturesToTendencies`, `ZodValidationFailedError`, `detectCompletedKeys`, `CLIENT_NAME_REDACTED`, `report_v2_delivered`, `report_version === '2.0'` all present.

## Threat Surface

All threats in plan's `<threat_model>` mitigated as designed:
- T-07.4-05-01 (spoofing): getUser gate preserved.
- T-07.4-05-02 (cross-tenant tampering): `.eq('therapist_id', user.id)` filter applied on all 3 surfaces (route SELECT, saveReportV2Delivered, deliverReportV2). MEMORY rule honored (no `auth.users` queries).
- T-07.4-05-03 (malformed Partial<ReportV2>): reportV2DeliveredSchema zod validation + merged-result full reportV2Schema re-validation.
- T-07.4-05-04 (audit hit lost on save): audit_metadata persisted on EVERY save call.
- T-07.4-05-05 (LGPD): client_name redaction via split-join + 50KB cap before persist.
- T-07.4-05-06 (DoS): request.signal forwarded to analyzeReadingV2 (which forwards to Anthropic stream.controller.abort).
- T-07.4-05-07 (regen-cap bypass): gate (e) preserved.
- T-07.4-05-09 (deliver with audit hits): hard-gate `if (totalHits > 0) return error` BEFORE the is_delivered=true UPDATE.
- T-07.4-05-10 (priority_focus length deviation): post-merge length-3 check before UPDATE.
- T-07.4-05-11 (mid-stream disconnect = no persisted state): accepted V1 limitation; per-key checkpoint persistence is V1.1 polish (documented in `must_haves.truths`).

No new threat surface introduced beyond the plan's threat register.

## Next Phase Readiness

- **Plan 07.4-06** (page router + V2 detail page) now unblocked. Can consume `report_v2` from DB and call `saveReportV2Delivered` / `deliverReportV2` from client components via the standard 'use server' RPC contract.
- **Plan 07.4-07/07b** (ReportAdaptiveView + VocabularyAuditBanner) unblocked. Stream contract is plain text deltas of JSON-mode output; client can pair with detectCompletedKeys (re-exported via stream-parser-v2 barrel) for progressive render.
- **Plan 07.4-08** (AdaptiveAnalysisStream UI) unblocked. Streaming protocol confirmed: `text/plain; charset=utf-8` + `Cache-Control: no-cache, no-transform` + `X-Content-Type-Options: nosniff`.
- **Phase 7 legacy (25 readings, report_version='1.0'):** ZERO regression. Branch is purely additive; legacy path remains the exact Phase 7 code with no behavioral change.

## Self-Check: PASSED

**Files claimed (3 modified):**
- `apps/web/app/actions/analise.schemas.ts` — FOUND (35 new lines)
- `apps/web/app/actions/analise.ts` — FOUND (210 new lines)
- `apps/web/app/api/readings/[id]/analyze/route.ts` — FOUND (189 new lines)

**Commits claimed (3):**
- `255759f` Task 1 — FOUND (`feat(07.4-05): reportV2DeliveredSchema partial schema (D-UI2)`)
- `5a99044` Task 2 — FOUND (`feat(07.4-05): saveReportV2Delivered + deliverReportV2 server actions`)
- `f71edb5` Task 3 — FOUND (`feat(07.4-05): wire V2 branch into analyze route handler`)

**Gates green:**
- tsc: 0 new errors (baseline-clean)
- lint: 0 errors + 9 baseline warnings
- anthropic lib tests: 133/139 (1 baseline failure pre-dating Plan 05)
- All acceptance criteria greps PASSED

---
*Phase: 07.4-iris-codex-report*
*Completed: 2026-05-13*
