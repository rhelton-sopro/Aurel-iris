---
phase: 07.4
plan: 22
status: complete
self_check: PASSED
date: 2026-05-15
files_modified:
  - apps/web/lib/anthropic/types.ts
  - apps/web/lib/anthropic/diff.ts
  - apps/web/app/actions/analise.schemas.ts
  - apps/web/scripts/reparse-reading.mjs
  - apps/web/lib/anthropic/__tests__/parser.test.ts
  - apps/web/components/readings/ReportReadView.tsx
  - apps/web/components/readings/AnalysisStream.tsx
  - apps/web/components/readings/EditorAccordion.tsx
  - apps/web/components/readings/__tests__/ReportReadView.test.tsx
  - apps/web/components/readings/__tests__/AnalysisStream.test.tsx
  - apps/web/components/readings/__tests__/EditorAccordion.test.tsx
  - .planning/STATE.md
files_created:
  - .planning/phases/07.4-iris-codex-report/07.4-22-PLAN.md
  - .planning/phases/07.4-iris-codex-report/07.4-22-SUMMARY.md
commits:
  - 2870d6d feat(07.4-22): backend — extend types/parser/diff/schemas with §16 (16 sections, skip 15)
  - dcc0f20 feat(07.4-22): UI — § removal + §16 card-grid + counter 16 (Plan 21 throughput)
  - (this commit) docs(07.4-22): SUMMARY + STATE — Plan 22 complete
verification:
  pnpm_build: exit 0
  pnpm_tsc_noEmit: 22 baseline (zero new from Plan 22)
  pnpm_lint: 0 errors + 9 baseline warnings
  pnpm_test_run: 620 passed + 3 baseline failures (quality-scoring × 3) + 24 todo + 2 skipped (was 615 pre-22; +5 net new)
deferred_items: []
---

# Plan 07.4-22 — Code plumbing for Plan 21 prompt change (§ removal + §16 + counter 16)

## Self-Check: PASSED

- ✅ types.ts ReportSectionKey union includes '16_sintese_rapida' + NUMBERED_SECTION_HEADINGS includes '16' (skip '15')
- ✅ ReportReadView.tsx h2 format `{N}. {title}` (no §); §16 renders as 6-card grid via parseSubsections helper
- ✅ AnalysisStream.tsx counter `{n}/16` (auto-derived from array length)
- ✅ EditorAccordion.tsx label `{s.number}. {s.title}` (no §); §16 entry present
- ✅ All 5 affected test files updated; 91/91 GREEN combined
- ✅ Full suite 620 passed + 3 baseline failures (quality-scoring × 3 only); +5 net new from Plan 22
- ✅ 3 commits FOUND on main (2870d6d + dcc0f20 + this)

## What changed

### Backend (commit 2870d6d)

- **types.ts** — schema extended with §16; NUMBERED_SECTION_HEADINGS bumped from 15 → 16 entries (added '16'; sequence skips '15' per founder explicit numbering choice — visible heading "16. Síntese Rápida" not "15. Síntese Rápida"); SECTION_KEY_BY_NUMBER + SECTION_TITLE_BY_NUMBER both extend with '16' → '16_sintese_rapida' / 'Síntese Rápida'.
- **diff.ts** — ALL_REPORT_KEYS extended with '16_sintese_rapida'.
- **analise.schemas.ts** — REPORT_KEYS extended; reportDeliveredSchema zod auto-derived now includes the new optional key.
- **scripts/reparse-reading.mjs** — NUMBERED_SECTION_HEADINGS + SECTION_KEY_BY_NUMBER mirrors extended.
- **parser.ts** — NO regex change needed. Current regex already accepts `## N. Title` (Plan 21+22 canonical) AND `## §N — Title` (Plan 16 legacy) via optional `§?` + `[\p{Pd}.]` separator class. The membership check uses NUMBERED_SECTION_HEADINGS which now includes '16'.
- **parser.test.ts** — 26 → 27 tests; reframed §15 reject test to clarify "skip 15 by founder choice"; new §17 out-of-range test; extended happy paths to 16 boundaries; added Plan 22 `## N. Title` happy path; preserved Plan 17 legacy `## §N — Title` happy path for backward-compat verification.

### UI (commit dcc0f20)

- **ReportReadView.tsx** — h2 heading format `§{headingStr} — {title}` → `{headingStr}. {title}`; NEW exported `parseSubsections(body)` helper that regex-splits the body on `^### LABEL$` headings and returns `[{label, body}]` array; §16 special render: when `headingStr === '16'`, parse body and render as `grid grid-cols-1 md:grid-cols-2 gap-4` of 6 cards (rounded border + bg-muted/30 + h3 emoji-label + ReactMarkdown body); falls back to default prose if subsections cannot be parsed (defensive — Sonnet may break structure occasionally). Also `data-testid='report-read-view-sintese-grid'` and `data-testid='sintese-block-{idx}'` for test stability.
- **AnalysisStream.tsx** — SECTION_TITLES extended with 'Síntese Rápida' (15 → 16 entries); TOTAL_SECTIONS = NUMBERED_SECTION_HEADINGS.length auto-bumps from 15 → 16 driving counter, clamp, pct, iteration. Display label format `{headingNumber}. {title}` was already correct (Plan 17 used array lookup).
- **EditorAccordion.tsx** — SECTIONS array extended with `{ key: '16_sintese_rapida', number: '16', title: 'Síntese Rápida' }` as 16th entry; AccordionTrigger inner span label format `§{s.number} — {s.title}` → `{s.number}. {s.title}` (no §, no em-dash; matches Plan 22 prompt + ReportReadView).
- **EditorSectionItem.tsx + analise-client.tsx** — NO changes needed. Both regexes already accept `## N. Title` AND `## §N — Title` via optional `§?` + `[\p{Pd}.]`; `\d{1,2}` matches '16'.
- **Tests** updated: parser +1 net (26→27 with §17 reject + Plan 22 happy path); ReportReadView 9 → 12 (added § removal + Plan 22 backward-compat for legacy strip + §16 6-card grid render + §16 fallback to prose); AnalysisStream 7 tests all `/15` → `/16` + §16 entry assertion; EditorAccordion 6 tests with 16-section + new label format anchored regex.

## Judgment calls

None — Plan 22 is mechanical fan-out of Plan 21's prompt change. Strict adherence to plan as written.

## Deviations: ZERO from plan

Plan 22's must_haves were executed verbatim:
- Backend: ReportSectionKey union ✓ NUMBERED_SECTION_HEADINGS ✓ SECTION_KEY_BY_NUMBER ✓ SECTION_TITLE_BY_NUMBER ✓ diff/schemas/script mirrors ✓ parser tests ✓
- UI: ReportReadView heading format + §16 card grid + parseSubsections helper ✓ AnalysisStream counter + titles ✓ EditorAccordion SECTIONS + label format ✓
- Tests: parser §16/§17/skip-15 + ReportReadView §16 grid + fallback + AnalysisStream /16 + EditorAccordion 16-section ✓
- All verification gates PASS within baseline tolerances ✓

## What this enables

Plan 23 (next) — replace Plan 19 Print CSS PDF with @react-pdf/renderer
direct download (founder approved install in UAT-4):
- Install @react-pdf/renderer 4.5.1 (React 19 supported per peer deps)
- NEW lib/pdf/report-document.tsx (React component using @react-pdf
  primitives Document/Page/Text/View/StyleSheet); renders 16 sections in
  serif (Times-Roman built-in); brand mark + tagline in fixed page header;
  encerramento_disclaimer in fixed page footer on every page; §16
  Síntese Rápida rendered as 2-column View flexDirection row grid;
  client name + date in header
- NEW app/api/readings/[id]/pdf/route.ts (RSC route handler; createClient
  session-bound RLS; renderToStream(<ReportDocument...>); Content-Type:
  application/pdf + Content-Disposition: attachment; filename
  `Leitura-${clientName}-${date}.pdf`)
- ExportPdfButton.tsx — replace Link/href with onClick fetch + blob
  download via URL.createObjectURL + anchor.click() with download attr
- DELETE /leituras/[id]/print/page.tsx + print-trigger.tsx (no longer needed)

Plan 24 — UAT-5 founder verification of all 7 UAT-4 acceptance criteria.

## Deferred items: NONE

All Plan 22 scope landed. D-DEF-14-01 (Cronorichio corpus gap) STILL OPEN
but unchanged.
