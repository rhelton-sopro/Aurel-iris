---
phase: 07.4
plan: 14
type: execute
wave: 11
depends_on: [10, 11, 12]
supersedes: []
files_modified:
  - apps/web/components/readings/EditorAccordion.tsx
  - apps/web/components/readings/EditorSectionItem.tsx
  - apps/web/components/readings/__tests__/EditorAccordion.test.tsx
  - apps/web/prompts/system.md
  - apps/web/lib/rag/section-queries.ts
  - apps/web/lib/anthropic/analyze.ts
files_created:
  - .planning/phases/07.4-iris-codex-report/07.4-14-SUMMARY.md
autonomous: true
requirements: []
source_decisions: [DC-1, DC-2, DC-4, DC-6]
ux_feedback_source: "UAT-2 founder verdict 2026-05-14: Forer/Barnum effect in §3/§4/§6/§10/§13 (generic narratives that fit any 37yo woman); UI bug: ## markdown markers visible in rendered title; UX bug: first 3 sections open by default."

must_haves:
  truths:
    # UI fixes (Issues 1 + 2)
    - "EditorAccordion AccordionTrigger renders `§{number} — {title}` (em-dash separator, § glyph prefix) — was `{number}. {title}`"
    - "EditorSectionItem ReactMarkdown preview body has the leading `## §N — Title\\n+` heading line STRIPPED before render — heading appears only in accordion trigger, not duplicated in body"
    - "EditorAccordion defaultValue=[] — all 14 sections COLLAPSED on initial render (was: first 3 open)"
    - "Strip works for both new 14-section content AND legacy 1.0 13-section content (regex is generic over `## §\\d{1,2}` heading shape)"
    # Prompt iteration (Issue 3 — UAT-2 spec)
    - "system.md §3 LINHA DO TEMPO EMOCIONAL fully rewritten — each marker MUST cite iris sector (hour + eye + observed sign) + Cronorichio age window (Italian/Jensen/Brazilian tradition) + life area + emotional quality + EXPLICIT resolution status ('em processo de resolução' / 'ativa, com expressão atual' / 'encapsulada — não-ativa no momento') + hedge as hypothesis. Skip ages that can't be anchored (better 3 anchored than 6 generic)."
    - "system.md §4 PADRÕES EMOCIONAIS ATIVOS — REQUIRE specific sectoral/structural finding citation per pattern claim"
    - "system.md §6 HERANÇAS TRANSGERACIONAIS — REQUIRE bilateral symmetry as ground; unilateral = personal imprint, not lineage"
    - "system.md §10 ARQUETÍPICA — REQUIRE 3-5 specific findings NAMED before any symbolic interpretation; theme emerges FROM findings, not floats above"
    - "system.md §13 SÍNTESE — synthesis of THE 3 STRONGEST specific findings from §2 + §3, each thread referencing at least one anchored sector"
    - "system.md GLOBAL CALIBRATION RULE added (verbatim founder text): 'If a statement would fit any woman aged 35-40, it's wrong. If it requires THIS iris to be true, it's right. When in doubt, say less and anchor more.'"
    # RAG query update
    - "section-queries.ts has new entry/template producing biographical/chronological retrieval queries for the linha_do_tempo concern: 'cronorichio', 'mapa biográfico iridológico', 'Lo Rito cronologia', 'Jensen biographical mapping', 'biographical iris map ages' — added to a NEW 'biografia_temporal' ReportSection slug OR injected into the psicoemocional template's expansion (whichever fits the existing pattern)"
    - "ReportSection union in lib/rag/types.ts extended with 'biografia_temporal' if needed"
    - "analyze.ts retrieval call includes the new section concern so chunks reach the §3 generation context"
    # Documentation
    - "Phase 7.2-H corpus expansion gap documented in deferred-items.md (Cronorichio/biographical references may be absent from current corpus — Phase 7.2 Wave C expansion item)"
    # Verification gates
    - "pnpm build exits 0"
    - "pnpm tsc --noEmit zero new errors"
    - "pnpm lint clean"
    - "pnpm test:run: only the 3 baseline failures (quality-scoring × 3)"
  artifacts:
    - path: ".planning/phases/07.4-iris-codex-report/07.4-14-SUMMARY.md"
      provides: "Plan 14 execution record"
---

<objective>
Address UAT-2 founder feedback:
1. UI bug: section title renders `##` markdown markers literally in editor preview pane
2. UX bug: first 3 sections open by default; founder wants ALL collapsed
3. Prompt bug: §3/§4/§6/§10/§13 produce generic Forer/Barnum narratives instead of iris-anchored claims; founder wants §3 fully restructured with Cronorichio + sector + resolution status, and §4/§6/§10/§13 with mandatory visual anchoring

Output: re-UAT ready with all 3 fixes landed. Plan 15 = founder re-walks Nailli to verify Cronorichio §3 + anchored §4/§6/§10/§13 + UI fixes.

Scope discipline: NO schema changes. NO parser changes (regex already accepts `## §\d{1,2}` headings via the generic 14-key range from Plan 11). NO §11 toggle work. NO dual-layer technical tab.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.4-iris-codex-report/07.4-CONTEXT.md
@.planning/phases/07.4-iris-codex-report/deferred-items.md
@.planning/STATE.md
@apps/web/prompts/system.md
@apps/web/components/readings/EditorAccordion.tsx
@apps/web/components/readings/EditorSectionItem.tsx
@apps/web/components/readings/__tests__/EditorAccordion.test.tsx
@apps/web/lib/rag/section-queries.ts
@apps/web/lib/rag/types.ts
@apps/web/lib/anthropic/analyze.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: UI fixes — title rendering + collapse default</name>
  <files>
    apps/web/components/readings/EditorAccordion.tsx (MODIFY)
    apps/web/components/readings/EditorSectionItem.tsx (MODIFY)
    apps/web/components/readings/__tests__/EditorAccordion.test.tsx (MODIFY)
  </files>
  <action>
**EditorAccordion.tsx:**

1. Change `defaultValue={['1_constituicao_temperamento', '2_mapa_organico', '3_linha_tempo_emocional']}` → `defaultValue={[]}` (all sections collapsed initially).

2. Change AccordionTrigger inner span from `{s.number}. {s.title}` to `§{s.number} — {s.title}`:
   ```tsx
   <span className="flex items-center gap-2">
     §{s.number} — {s.title}
     {isEdited && (
       <span className="text-sm font-normal text-muted-foreground">· editado</span>
     )}
   </span>
   ```
   Note: use em-dash `—` (U+2014), NOT hyphen `-`. Matches the markdown heading format `## §N — Title`.

3. The §14 warm-voice caption ("Voz do terapeuta · Para entrega ao cliente") stays; it appears ABOVE the §14 title row.

**EditorSectionItem.tsx:**

Strip the leading `## §N — Title\n+` heading line from `deliveredValue` before passing to ReactMarkdown — the accordion trigger already displays the title, so the body shouldn't duplicate it.

Add a module-level regex constant:
```tsx
// Strip the leading `## §N — Title` (or `### N. Title` legacy form) heading
// line + trailing blank lines, so the rendered body doesn't duplicate the
// accordion-trigger title. Handles both Phase 7.4 14-section (`## §N — `) and
// legacy 13-section (`### N. `) shapes via the same regex as parser.ts BOUNDARY_RE.
const STRIP_LEADING_HEADING_RE = /^[ \t]*#{2,3}[ \t]+§?[ \t]*\d{1,2}[ \t]*[\p{Pd}.][^\n]*\n+/u
```

Change line 58 from:
```tsx
<ReactMarkdown remarkPlugins={[remarkGfm]}>{deliveredValue}</ReactMarkdown>
```
to:
```tsx
<ReactMarkdown remarkPlugins={[remarkGfm]}>
  {deliveredValue.replace(STRIP_LEADING_HEADING_RE, '')}
</ReactMarkdown>
```

Leave the Textarea content RAW (line 45 `value={deliveredValue}`) — the textarea is the editor source view; it SHOULD show the raw markdown including the heading line. Only the preview pane strips.

**EditorAccordion.test.tsx:**

1. If a test asserts the first 3 sections are open by default, invert: assert all 14 are collapsed (e.g., assert `data-state="closed"` on the AccordionItem of §1, or assert the body text of §1 is not visible).

2. Add a test: `'preview body strips the duplicate ## §N — Title heading line'` — fixture content `'## §1 — Constituição e Temperamento\n\nO organismo é...'` should render in the preview without the `## §1 — ...` line, only `O organismo é...` content. Use `screen.queryByText('## §1', { exact: false })` to assert ABSENCE in the preview region. (You may need to expand a section first via `userEvent.click(trigger)`.)

3. Update any title-assertion to expect `§1 — Constituição e Temperamento` (with `§` and em-dash) instead of `1. Constituição e Temperamento` (dot separator).
  </action>
  <verify>
    <automated>cd apps/web && pnpm test:run components/readings/__tests__/EditorAccordion.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "defaultValue={\\[\\]}" apps/web/components/readings/EditorAccordion.tsx` returns 1
    - `grep -c "§{s.number} —" apps/web/components/readings/EditorAccordion.tsx` returns 1
    - `grep -c "STRIP_LEADING_HEADING_RE" apps/web/components/readings/EditorSectionItem.tsx` returns ≥ 2 (declaration + use)
    - EditorAccordion test passes including the new strip-test
  </acceptance_criteria>
  <done>UI fixes complete.</done>
</task>

<task type="auto">
  <name>Task 2: system.md prompt iteration — §3 Cronorichio + §4/§6/§10/§13 visual anchoring + global calibration</name>
  <files>apps/web/prompts/system.md (MODIFY)</files>
  <action>
Apply the following substantive edits to `apps/web/prompts/system.md`. Find each named section and replace its instruction block per the founder's UAT-2 spec.

---

**Add GLOBAL CALIBRATION RULE near the top of the prompt** (after the "Identidade de marca" section, before the per-section instructions). Use this verbatim text:

```markdown
---

## Regra de calibração global (mandatória)

> **Se uma afirmação caberia em qualquer mulher de 35-40 anos, está errada.
> Se ela exige que ESTA íris seja como é, está certa. Em dúvida, diga menos
> e ancore mais.**

Esta regra prevalece sobre qualquer instrução posterior. Cada parágrafo do
relatório deve passar por este filtro antes de ser emitido: ele cita um
achado visual específico desta íris (setor + hora + tipo de sinal), uma
estrutura nomeada (fibra, lacuna, pigmento, anel, mancha) com posição
identificável, OU é um eixo psicossomático/temporal explicitamente ancorado
em um achado anterior do MESMO relatório? Se NÃO — reescreva ou omita.

Não compense ausência de ancoragem com prosa hipotética. Melhor 3 marcadores
ancorados em §3 que 6 genéricos. Melhor um eixo psicossomático nomeado em §5
que 3 conexões soltas. O leitor primário é terapeuta integrativo — ele/ela
quer ancoragem visual, não psicologia genérica.
```

---

**§3 LINHA DO TEMPO EMOCIONAL — REPLACE the entire section block** with:

```markdown
## §3 — Linha do Tempo Emocional

**Use Cronorichio (mapa cronológico de Lo Rito) combinado com mapas
biográficos do corpus RAG (Jensen cronologia, mapa biográfico brasileiro,
Battello se disponível).** Os trechos RAG injetados sob o concern
"biografia_temporal" trazem as faixas etárias por sector iridológico.

### Exigências mandatórias para cada marcador temporal

Cada marcador DEVE conter, em ordem:

1. **Sinal iridológico específico**: setor (hora) + olho + tipo de sinal
   observado (lacuna, fibra, pigmento, mancha, anel, espessamento, etc.).
   Sem este campo, NÃO emita o marcador.

2. **Mapeamento Cronorichio**: faixa etária aproximada citando a tradição
   (italiana/Lo Rito, americana/Jensen, brasileira). Padrão:
   "Mapeamento Cronorichio (Lo Rito): faixa aproximada de N1-N2 anos"
   ou "Mapa biográfico brasileiro: faixa N1-N2 anos". Se nenhuma das
   três tradições mapear o setor para uma faixa específica, **PULE o
   marcador** — melhor 3 ancorados que 6 inferidos.

3. **Topografia**: zona orgânica do setor (tireoide/pescoço/expressão,
   coração/peito/relação, fígado/raiva-contida, rins/medo-da-sobrevivência,
   etc.). Conecta o sector ao significado funcional.

4. **Qualidade sugerida**: leitura emocional do tipo de sinal — não
   diagnóstico, hipótese. "contenção da expressão", "marca de luto não-
   elaborado", "passagem de identidade difícil", etc.

5. **STATUS DE RESOLUÇÃO ATUAL — campo OBRIGATÓRIO**: avaliação explícita
   do estado da marca hoje, escolhendo UMA das três categorias:

   - `"Marca aparenta estar em processo de resolução"` — sinal sutil, com
     fibras circundantes íntegras, sem pigmentação adjacente; sugere
     que o organismo vem trabalhando o tema.
   - `"Marca ativa, com expressão atual no organismo"` — sinal denso,
     com pigmentação adjacente OU com correlação visível em outros
     sistemas (§2 deve reforçar com sistema correspondente sob carga).
   - `"Marca encapsulada — registro presente mas não-ativo no momento"`
     — sinal definido mas isolado, sem propagação para sistemas adjacentes;
     registro biográfico real mas sem expressão clínica atual.

6. **Hedge como hipótese**: termine com "convida a investigar com a cliente
   se há ressonância com esse período da vida" ou equivalente. NUNCA
   afirme o evento como fato — a íris ABRE uma conversa, não DECIDE
   histórias.

### Exemplo de marcador BEM ancorado

> Sinal: lacuna na hora 4 do olho esquerdo, com pigmentação amarelo-ocre
> adjacente.
> Mapeamento Cronorichio (Lo Rito): faixa aproximada de 11-14 anos —
> período de entrada na adolescência e desenvolvimento da identidade
> verbal.
> Topografia: zona da tireoide / pescoço — campo da voz, da expressão,
> do direito de dizer.
> Qualidade sugerida: contenção da expressão durante transição puberal —
> possível vivência de não-poder-dizer em contexto familiar ou escolar.
> Status atual: marca ativa — a pigmentação adjacente sugere que o tema
> permanece com expressão funcional no organismo hoje (a tireoide bilateral
> sob tendência em §2 reforça).
> Convida a investigar com a cliente se há ressonância com esse período
> da vida.

### Exemplo de marcador MAL ancorado (proibido — NÃO emita assim)

> ❌ "Por volta dos 12-16 anos, uma adolescente que engoliu mais do que
> expressou."

Sem setor, sem Cronorichio, sem status de resolução, narrativa
psicológica genérica que cabe em qualquer pessoa. Este formato é
explicitamente proibido pelo Princípio 1 + Regra de Calibração Global.

### Quantidade

**Mínimo 3 marcadores ancorados; máximo 7. Sem teto artificial.** Emita
exatamente quantos a íris ancorar — não infle nem comprima.
```

---

**§4 PADRÕES EMOCIONAIS ATIVOS — ADD this clause to its existing instruction
block** (or replace the whole block to include it):

```markdown
### Ancoragem visual (mandatória)

Cada padrão emocional citado DEVE referenciar um achado visual específico
do mapa iridológico do MESMO relatório como justificativa. Padrão de
escrita:

> "Tendência a [padrão emocional X], com base em [sinal Y observado em
> setor Z / estrutura W do olho A]. Isso conecta-se com [§2 sistema
> correspondente / §3 marcador temporal] reforçando a leitura."

**SEM ancoragem, NÃO emita o padrão.** Se a íris não mostra evidência
visual para um padrão emocional, ele não pertence ao §4. Padrões
emocionais sem ancoragem visual cabem em qualquer pessoa (Regra de
Calibração Global).
```

---

**§6 HERANÇAS TRANSGERACIONAIS — REPLACE its instruction block** with:

```markdown
## §6 — Heranças Transgeracionais Sugeridas

Padrões que a íris sugere virem de linhagem. **REGRA DE GROUND
BILATERAL OBRIGATÓRIA:**

- Uma hipótese transgeracional só é válida quando o sinal é **BILATERAL** —
  presente em ambos os olhos, no mesmo setor (ou setores equivalentes
  espelhados), com grade/intensidade similar.
- **Sinais UNILATERAIS NÃO são transgeracionais** — eles são marcas
  pessoais (biográficas) e devem ser classificados como "imprint pessoal,
  não-linhagem" se mencionados aqui.

Sinais bilaterais aceitos como ground transgeracional incluem:
- Pigmentações primárias bilaterais (tipo de constituição)
- Aneis nervosos bilaterais
- Linfáticos bilaterais (papilas, rosário linfático)
- Manchas tóxicas em setores espelhados de ambos os olhos
- Estruturas estromais bilaterais (densidade, abertura)

Para cada hipótese transgeracional:
1. Cite os achados bilaterais que servem de ground (olho esquerdo +
   olho direito, setores específicos).
2. Hedge linguagem forte: "pode haver eco de", "abre a hipótese de",
   "sugere padrão herdado de".
3. NÃO afirme constelação familiar específica. Tom: oferta de pergunta,
   não diagnóstico genealógico.

**Quantidade**: 0-3 hipóteses, dependendo do que a íris ancorar
bilateralmente. Se a íris não mostra grounds bilaterais claros, este
seção pode ter apenas 1 parágrafo explicando: "A íris desta cliente
não apresenta sinais bilaterais marcantes que ancorariam hipóteses
transgeracionais. O registro visível é predominantemente biográfico
pessoal — veja §3."
```

---

**§10 DIMENSÃO ARQUETÍPICA — ADD ancoragem clause and REPLACE quantity instruction**:

```markdown
### Ancoragem ANTES de interpretação

Antes de qualquer interpretação simbólica/arquetípica, **NOME 3-5
achados visuais específicos** que sustentam a leitura. Padrão de
escrita:

> "Olhando a íris como um todo, observa-se: [achado 1], [achado 2],
> [achado 3], [achado 4 se houver], [achado 5 se houver]. Esse
> conjunto sugere, em registro arquetípico, [tema da alma /
> tensão existencial]."

O tema arquetípico **emerge dos achados** — não flutua acima deles.
SEM a lista de achados nomeados, NÃO emita interpretação simbólica.
Linguagem cuidadosa, ressonante, sem psicologismo barato, sem
religiosidade específica.

**Limite**: 1-2 parágrafos. Não infle.
```

---

**§13 SÍNTESE INTEGRATIVA — REPLACE with**:

```markdown
## §13 — Síntese Integrativa

**3-5 fios principais conectados** — síntese dos achados MAIS FORTES
dos §2 e §3 (e §5 se relevante). NÃO tópicos isolados. NÃO resumo
genérico.

Cada fio deve:
1. Citar pelo menos um sinal iridológico específico do MESMO relatório
   (setor + olho OU estrutura nomeada de §2/§3)
2. Conectar 2-3 sistemas/seções entre si — eixo psicossomático,
   marca temporal + sistema atual, herança bilateral + manifestação
   atual, etc.

Padrão de escrita:

> "O que se vê aqui é uma constelação de [fio 1 — citando §2.linfático
> bilateral + §3.marcador-aos-12-anos], conectada a [fio 2 — citando
> §5.eixo-fígado/raiva-contida + §3.marcador-aos-18-anos], com
> [fio 3 — citando §10.tema-arquetípico nomeado]. Juntos, sugerem
> um momento de [interpretação integrativa]."

NÃO escreva síntese sem referenciar pelo menos um sinal específico
por fio. Síntese sem referências cabe em qualquer relatório (Regra
de Calibração Global).

**Quantidade**: 2-3 parágrafos. 3-5 fios concretos.
```

---

After all changes, verify:
- Line 1 still `<!-- audit-vocabulary:allowlist -->`
- `## §1 — Constituição e Temperamento` through `## §14 — Mensagem para o Cliente` headings all present
- New "Regra de calibração global (mandatória)" section present near top
- `wc -c apps/web/prompts/system.md` returns ≥ 8800 bytes (cache eligibility)
- No new occurrences of forbidden vocab (`diagnóstico`, `tratamento`, `cura`, Sopro vocab)
  </action>
  <verify>
    <automated>grep -c "Regra de calibração global" apps/web/prompts/system.md; grep -c "Cronorichio" apps/web/prompts/system.md; grep -c "Marca ativa, com expressão atual" apps/web/prompts/system.md; wc -c apps/web/prompts/system.md</automated>
  </verify>
  <acceptance_criteria>
    - Global calibration rule present (verbatim founder text)
    - §3 Cronorichio block present with 6 mandatory fields + good/bad examples
    - §4 ancoragem clause present
    - §6 bilateral ground rule present
    - §10 nomeie 3-5 achados clause present
    - §13 3-5 fios with sector citations clause present
    - File size ≥ 8800 bytes
    - All 14 section headings still present (1..14)
    - audit-vocabulary marker on line 1 preserved
  </acceptance_criteria>
  <done>Prompt iteration applied per UAT-2 spec.</done>
</task>

<task type="auto">
  <name>Task 3: RAG retrieval — biografia_temporal concern for §3</name>
  <files>
    apps/web/lib/rag/types.ts (MODIFY — add `biografia_temporal` to ReportSection union)
    apps/web/lib/rag/section-queries.ts (MODIFY — add template entry)
    apps/web/lib/anthropic/analyze.ts (MODIFY — include the new concern in the RAG section list passed to buildFamilyB)
  </files>
  <action>
1. **types.ts**: extend `ReportSection` union with `'biografia_temporal'`:
   ```ts
   export type ReportSection =
     | 'constituicao'
     | 'psicoemocional'
     | 'transgeracional'
     | 'simbolico'
     | 'mensagem_final'
     | 'mental_cognitivo'
     | 'nutricao_carencias'
     | 'biografia_temporal'  // <-- NEW: Cronorichio + biographical map retrieval for §3 (Plan 07.4-14)
   ```

2. **section-queries.ts**: add the corresponding template entry to `SECTION_QUERY_TEMPLATES`:
   ```ts
   biografia_temporal: (f) => [
     `cronorichio Lo Rito cronologia iridológica`,
     `mapa biográfico iridológico ${f.constitution.primary}`,
     `Jensen biographical mapping iris ages`,
     `mapa biográfico brasileiro setores idade`,
   ],
   ```
   The 4 queries cover: Italian Lo Rito tradition, constitution-anchored biographical, English-language Jensen biographical, and Brazilian biographical map.

3. **analyze.ts**: find the section list passed to `buildFamilyB(...)` (likely a const array of `ReportSection` slugs). Add `'biografia_temporal'` to that list so the retrieval includes Cronorichio chunks when generating §3.

   Locate the existing line (something like):
   ```ts
   const REPORT_SECTIONS: ReportSection[] = ['constituicao', 'psicoemocional', 'transgeracional', 'simbolico', 'mental_cognitivo', 'nutricao_carencias', 'mensagem_final']
   ```
   And add `'biografia_temporal'` to it.

4. **Smoke test**: there may be an existing test like `build-queries.test.ts` that asserts the full list of generated queries. Update it if needed to include the 4 new biographical queries.
  </action>
  <verify>
    <automated>grep -c "biografia_temporal" apps/web/lib/rag/types.ts apps/web/lib/rag/section-queries.ts apps/web/lib/anthropic/analyze.ts</automated>
  </verify>
  <acceptance_criteria>
    - `biografia_temporal` appears in all 3 files (types union, template entry, analyze section list)
    - tsc clean
    - existing RAG tests still pass; any new test that asserts the 4 biographical queries passes
  </acceptance_criteria>
  <done>§3 Cronorichio retrieval wired into the analyze pipeline.</done>
</task>

<task type="auto">
  <name>Task 4: Document Phase 7.2-H corpus gap + verify + SUMMARY + STATE</name>
  <action>
1. **Document Phase 7.2-H gap in `deferred-items.md`** — append a new section:

```markdown
### D-DEF-14-01 — Cronorichio + biographical map chunks may be absent from current RAG corpus

- **Discovered:** 2026-05-14 during Plan 07.4-14 implementation (§3 retrieval wiring for UAT-2 fix).
- **Concern:** Plan 14 adds 4 new RAG queries under the `biografia_temporal` concern for §3 generation:
  - `cronorichio Lo Rito cronologia iridológica`
  - `mapa biográfico iridológico {constitution.primary}`
  - `Jensen biographical mapping iris ages`
  - `mapa biográfico brasileiro setores idade`
- **Risk:** If the current Phase 6 corpus (2761 chunks across 12 books) does NOT contain Lo Rito's Cronorichio book, Jensen biographical-mapping chapters, or the Brazilian biographical iris-map literature, these retrievals will return low-relevance chunks. The §3 generation will fall back to Sonnet's pre-training knowledge of biographical iris mapping, which may be thin.
- **How to detect:** after Plan 14 lands and the founder regenerates a reading, inspect the RAG retrieval logs (or run a manual `pnpm scripts/rag-probe.mjs` if available) for the `biografia_temporal` queries. If the top-k chunks have low cosine similarity OR don't actually reference Cronorichio/biographical mapping, the corpus is the bottleneck.
- **Fix (Phase 7.2 Wave C scope — "item 7.2-H new" per founder UAT-2 spec):** ingest Lo Rito's Cronorichio (Italian-language book), the relevant Jensen biographical chapters, and the Brazilian biographical iris-map literature. Cost: each book ~$0.30 contextual ingest per Phase 6 baseline. Total ~$1 + author-permissions handling.
- **Workaround until corpus expands:** the §3 prompt instructions (Plan 14 system.md rewrite) tell Sonnet to SKIP unanchored markers ("better 3 anchored than 6 generic"). So if RAG returns thin biographical chunks, §3 will be shorter rather than fabricated — failure mode is acceptable.
- **Scope:** out of Plan 07.4-14 (prompt + UI iteration). New item for Phase 7.2 corpus expansion roadmap.
```

2. **Run full verification:**
   - `cd apps/web && pnpm build` → 0
   - `pnpm tsc --noEmit` → zero new errors
   - `pnpm lint` → 0 errors / 9 baseline warnings
   - `pnpm test:run` → expect 3 baseline failures (quality-scoring×3)

3. **Create `.planning/phases/07.4-iris-codex-report/07.4-14-SUMMARY.md`**:
   - Frontmatter: phase, plan=14, subsystem=prompt+ui+rag, tags=[uat-2, cronorichio, anchoring, calibration-rule, collapse-default]
   - provides: 3 UAT-2 fixes (UI strip + collapse default + §3 Cronorichio §4/§6/§10/§13 anchoring); GLOBAL CALIBRATION RULE
   - key-decisions: §3 markers MUST be skippable when unanchored (no inflation); resolution-status 3-category enum; bilateral-ground rule for §6; ancoragem-first rule for §10
   - requirements-touched: addresses UAT-2 feedback for DC-1/2/4/6
   - self_check: PASSED with all grep + test outputs
   - Lists D-DEF-14-01 (Phase 7.2-H corpus gap)

4. **Update STATE.md:**
   - status: "Phase 7.4 Plan 14 COMPLETE — UAT-2 fixes (UI title strip + all-sections-collapsed + §3 Cronorichio + §4/§6/§10/§13 visual anchoring + global calibration rule). RAG retrieval extended with biografia_temporal concern. Phase 7.2-H corpus expansion gap documented (D-DEF-14-01). Awaiting UAT-3 founder verdict on re-regenerated Nailli reading."
   - stopped_at: "Plan 14 complete. Next: Plan 15 founder UAT-3 walkthrough — re-regenerate Nailli, verify §3 shows 3-5 Cronorichio-anchored markers with resolution status, verify §4/§6/§10/§13 reference specific findings, verify UI strip + collapse work. If UAT-3 PASS → /gsd-verify-phase 07.4 to close. If §3 still feels generic, retrieval may need corpus expansion (D-DEF-14-01)."
   - last_updated: ISO now
   - completed_plans: 89 → 90
   - percent: 99 → 99 (still rounding to 99 with 1 plan remaining)

5. Final docs commit: `docs(07.4-14): SUMMARY + STATE + deferred-items — UAT-2 fixes complete`
  </action>
  <acceptance_criteria>
    - All verification gates green
    - deferred-items.md has D-DEF-14-01 entry
    - SUMMARY.md created
    - STATE.md updated
    - Final docs commit
  </acceptance_criteria>
  <done>Plan 14 complete; Plan 15 UAT-3 ready.</done>
</task>

</tasks>

<success_criteria>
- 3 UAT-2 issues fixed in 4 atomic commits
- Founder can regenerate Nailli and re-walk through the 14 sections with all 3 fixes visible
- §3 now produces Cronorichio-anchored markers OR is shorter (no Forer/Barnum)
- D-DEF-14-01 documents the Phase 7.2-H corpus expansion gap
- pnpm build + tsc + lint + test all green
</success_criteria>

<output>
After completion, create `.planning/phases/07.4-iris-codex-report/07.4-14-SUMMARY.md`.
</output>
