# Phase 7.4: Iris Codex — Pattern Map

**Mapped:** 2026-05-13
**Files analyzed:** 31 new + 8 modified
**Analogs found:** 36 / 39 (3 files have no close analog — flagged below)

---

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|-------------------|------|-----------|----------------|---------------|
| `supabase/migrations/0013a_iris_codex_schema.sql` | migration (DDL) | one-shot | `supabase/migrations/0007_phase_7_analise_llm.sql` | exact (same template, same patterns) |
| `supabase/migrations/0013b_drop_legacy_generated.sql` | migration (DDL) | one-shot | `supabase/migrations/0007_phase_7_analise_llm.sql` (drop section §2) | exact |
| `supabase/migrations/0013c_reading_addons.sql` | migration (DDL+RLS) | one-shot | `supabase/migrations/0007_phase_7_analise_llm.sql` (CHECK constraint §5) + `0011_calibration_rls_jwt_email.sql` | role-match (closest RLS analog) |
| `apps/web/lib/anthropic/report-schema.ts` (NEW) | zod schema | data-validation | `apps/web/app/actions/analise.schemas.ts` + `apps/web/lib/anthropic/types.ts` | role-match |
| `apps/web/lib/anthropic/analyze-v2.ts` (NEW — V2 of analyze.ts) | service | streaming + transform | `apps/web/lib/anthropic/analyze.ts` | exact |
| `apps/web/lib/anthropic/stream-parser-v2.ts` (NEW — top-level key detector) | parser | streaming (transform) | `apps/web/lib/anthropic/parser.ts` (`findAllBoundaries`) | role-match |
| `apps/web/lib/anthropic/audit-v2.ts` (extended vocab) | service | data-validation | `apps/web/lib/anthropic/audit.ts` | exact |
| `apps/web/lib/anthropic/diff-v2.ts` (keyed by system_id) | service | transform | `apps/web/lib/anthropic/diff.ts` | exact |
| `apps/web/lib/anthropic/prompts.ts` (no rewrite — loader unchanged) | utility | file-I/O | `apps/web/lib/anthropic/prompts.ts` (current) | identity (no change to interface) |
| `apps/web/prompts/system.md` (rewritten from scratch) | content (LLM prompt) | static | `apps/web/prompts/system.md` (Phase 7 version) | structural-match (file structure preserved; content replaced) |
| `apps/web/lib/tendency-engine/placeholder.ts` (NEW) | service (pure fn) | transform | `apps/web/lib/rag/build-queries.ts` | role-match (pure-function library) |
| `apps/web/app/api/readings/[id]/analyze/route.ts` (V2 path branch) | route handler | streaming + request-response | `apps/web/app/api/readings/[id]/analyze/route.ts` (current) | exact (same file, V2 branch) |
| `apps/web/app/actions/analise.ts` (extended w/ `saveReportV2Delivered`) | server action | CRUD | `apps/web/app/actions/analise.ts` (`saveReportDelivered`) | exact |
| `apps/web/app/actions/analise.schemas.ts` (extend w/ `reportV2DeliveredSchema`) | zod schema | data-validation | `apps/web/app/actions/analise.schemas.ts` (current) | exact |
| `apps/web/components/readings/ReportAdaptiveView.tsx` (NEW) | component (RSC) | request-response | `apps/web/components/readings/EditorAccordion.tsx` (legacy renderer co-exists) | role-match |
| `apps/web/components/readings/ReportAdaptiveEditor.tsx` (NEW) | component (client) | event-driven | `apps/web/app/(dashboard)/leituras/[id]/editar/editar-client.tsx` | role-match |
| `apps/web/components/readings/SystemTendencyCard.tsx` (NEW) | component (client) | request-response | `apps/web/components/readings/EditorSectionItem.tsx` | role-match |
| `apps/web/components/readings/BlockEditPane.tsx` (NEW — generic edit toggle) | component (client) | event-driven | `apps/web/components/readings/EditorSectionItem.tsx` | role-match |
| `apps/web/components/readings/GradeBadge.tsx` (NEW) | component (RSC) | static | `apps/web/components/readings/StatusBadge.tsx` | role-match |
| `apps/web/components/readings/GradeBar.tsx` (NEW — 5-segment bar) | component (RSC) | static | `apps/web/components/ui/badge.tsx` (no exact analog — CSS-only primitive) | partial-match |
| `apps/web/components/readings/IntegrativeAxisItem.tsx` (NEW) | component (RSC) | static | `apps/web/components/readings/StatusBadge.tsx` + `EditorSectionItem.tsx` | partial-match |
| `apps/web/components/readings/PriorityFocusList.tsx` (NEW) | component (RSC) | static | (no exact analog — trivial numbered-list wrapper) | none |
| `apps/web/components/readings/KeyTraitChipGroup.tsx` (NEW) | component (RSC) | static | `apps/web/components/ui/badge.tsx` | partial-match |
| `apps/web/components/readings/AdvancedAnalysisCTA.tsx` (NEW — placeholder button + modal) | component (client) | event-driven | `apps/web/components/readings/DeliverDialog.tsx` | role-match |
| `apps/web/components/readings/VocabularyAuditBanner.tsx` (NEW) | component (RSC) | request-response | `apps/web/components/readings/EditorAuditBanner.tsx` | exact (rename + extend) |
| `apps/web/components/readings/AdaptiveAnalysisStream.tsx` (NEW or extend AnalysisStream) | component (client) | streaming | `apps/web/components/readings/AnalysisStream.tsx` | exact (refactor / extend) |
| `apps/web/app/(dashboard)/leituras/[id]/page.tsx` (router by `report_version`) | page (RSC) | request-response | `apps/web/app/(dashboard)/leituras/[id]/page.tsx` (current) | exact |
| `apps/web/app/(dashboard)/leituras/[id]/editar/page.tsx` (router by `report_version`) | page (RSC) | request-response | `apps/web/app/(dashboard)/leituras/[id]/editar/page.tsx` (current) | exact |
| `apps/web/app/(dashboard)/leituras/[id]/analise-client.tsx` (extend for v2 streaming) | client orchestrator | streaming | `apps/web/app/(dashboard)/leituras/[id]/analise-client.tsx` (current) | exact |
| `apps/web/scripts/audit-vocabulary.mjs` (extended w/ 3 pattern sets) | script (CI) | file-I/O | `apps/web/scripts/audit-vocabulary.mjs` (current) | exact (extend) |
| `apps/web/app/manifest.ts` (Aurel → Iris Codex) | config | static | `apps/web/app/manifest.ts` (current) | exact (text replace) |
| `apps/web/app/layout.tsx` (Aurel → Iris Codex) | layout | static | `apps/web/app/layout.tsx` (current) | exact (text replace) |
| `apps/web/lib/anthropic/__tests__/report-schema.test.ts` | test (unit) | data-validation | `apps/web/lib/anthropic/__tests__/diff.test.ts` | role-match (unit zod test) |
| `apps/web/lib/anthropic/__tests__/analyze-v2.test.ts` | test (integration) | streaming | `apps/web/lib/anthropic/__tests__/integration.test.ts` | role-match |
| `apps/web/lib/anthropic/__tests__/stream-parser-v2.test.ts` | test (unit) | parser | `apps/web/lib/anthropic/__tests__/parser.test.ts` | exact |
| `apps/web/lib/anthropic/__tests__/audit-v2.test.ts` | test (unit) | regex | `apps/web/lib/anthropic/__tests__/audit.test.ts` | exact |
| `apps/web/lib/anthropic/__tests__/diff-v2.test.ts` | test (unit) | transform | `apps/web/lib/anthropic/__tests__/diff.test.ts` | exact |
| `apps/web/lib/tendency-engine/__tests__/placeholder.test.ts` | test (unit) | transform | `apps/web/lib/rag/build-queries.test.ts` | role-match |
| `apps/web/components/readings/__tests__/ReportAdaptiveView.test.tsx` | test (component) | render | `apps/web/components/readings/__tests__/EditorAccordion.test.tsx` | exact |
| `apps/web/components/readings/__tests__/ReportAdaptiveEditor.test.tsx` | test (component) | render+interaction | `apps/web/components/readings/__tests__/EditorAccordion.test.tsx` | exact |
| `apps/web/components/readings/__tests__/SystemTendencyCard.test.tsx` | test (component) | render | `apps/web/components/readings/__tests__/EditorSectionItem.test.tsx` | exact |
| `apps/web/components/readings/__tests__/AdvancedAnalysisCTA.test.tsx` | test (component) | interaction | `apps/web/components/readings/__tests__/AnalysisCTA.test.tsx` | exact |
| `apps/web/components/readings/__tests__/VocabularyAuditBanner.test.tsx` | test (component) | render | `apps/web/components/readings/__tests__/AnalysisStream.test.tsx` (closest existing) | role-match |
| `apps/web/components/readings/__tests__/AdaptiveAnalysisStream.test.tsx` | test (component) | render | `apps/web/components/readings/__tests__/AnalysisStream.test.tsx` | exact |
| `apps/web/components/readings/__tests__/GradeBar.test.tsx` + `GradeBadge.test.tsx` | test (component) | render | `apps/web/components/readings/StatusBadge.test.tsx` | role-match |
| `apps/web/app/__tests__/manifest.test.ts` | test (unit) | static | (no exact analog — trivial assertion test) | none |
| `apps/web/scripts/__tests__/audit-vocabulary.test.mjs` | test (script) | regex | `apps/web/scripts/audit-vocabulary-db.test.mjs` | role-match |

---

## Pattern Assignments — by Domain

### MIGRATIONS

#### `supabase/migrations/0013a_iris_codex_schema.sql` (DDL — ADD columns + backfill)

**Analog:** `supabase/migrations/0007_phase_7_analise_llm.sql`

**File header + idempotency pattern** (lines 1-25):
```sql
-- supabase/migrations/0007_phase_7_analise_llm.sql
-- Phase 7 — Análise LLM: substitui ai_report_raw/ai_report_edited (text) por
-- jsonb canônico report_generated/report_delivered, mantendo retrocompat
-- ...
-- Idempotência: DROP COLUMN IF EXISTS + ADD COLUMN IF NOT EXISTS + CREATE OR
-- REPLACE FUNCTION. CONTEXT D-P1 confirma: zero linhas existentes têm
-- ai_report_raw/edited populadas (Fase 7 nunca executou).
-- 07-PLAN-01 desta fase. Decisões: D-P1, D-P2, D-P3, D-P4.

begin;
```

**ALTER TABLE ADD COLUMN pattern** (lines 60-84):
```sql
alter table readings
  add column if not exists report_generated jsonb,
  add column if not exists report_delivered jsonb,
  add column if not exists ai_report_raw text generated always as (
    jsonb_concat_sections_pt_br(report_generated)
  ) stored,
  ...

alter table readings
  add column if not exists report_generated_at timestamptz,
  add column if not exists report_delivered_at timestamptz,
  add column if not exists edit_diff jsonb,
  ...
  add column if not exists audit_metadata jsonb default '{}'::jsonb,
  add column if not exists regeneration_count int default 0,
  add column if not exists regeneration_log jsonb default '[]'::jsonb;
```

**CHECK constraint with DO-block idempotency** (lines 86-98):
```sql
do $$
begin
  if not exists (
    select 1 from pg_constraint where conname = 'readings_regeneration_count_cap_check'
  ) then
    alter table readings
      add constraint readings_regeneration_count_cap_check
      check (regeneration_count >= 0 and regeneration_count <= 3);
  end if;
end $$;

commit;
```

**Adapt for new file:** Header references 07.4 D-SCH1 + locked decisions. Add the 5 new columns (`report_v2`, `report_v2_delivered`, `report_v2_edit_diff`, `report_v2_generated_at`, `report_v2_delivered_at`, `report_version`). Backfill `UPDATE readings SET report_version='1.0' WHERE report_generated IS NOT NULL` AFTER the ADD COLUMN block but INSIDE the same transaction.

---

#### `supabase/migrations/0013b_drop_legacy_generated.sql` (DDL — DROP GENERATED + function)

**Analog:** `supabase/migrations/0007_phase_7_analise_llm.sql` (DROP COLUMN section §2 — lines 48-55)

**DROP pattern** (lines 47-55):
```sql
-- ============================================================================
-- 2) DROP+ADD das colunas ai_report_raw / ai_report_edited
-- One-shot atômico (zero rows affected — D-P1 confirmed). Em transação para
-- evitar estado intermediário com tabela inconsistente.
-- ============================================================================
alter table readings
  drop column if exists ai_report_raw,
  drop column if exists ai_report_edited;
```

**Adapt for new file:** Wrap in `begin;…commit;`. Drop `ai_report_raw`, `ai_report_edited` (the GENERATED columns added by 0007), then `DROP FUNCTION IF EXISTS jsonb_concat_sections_pt_br(jsonb);`. Header note: "LLM-04 contract REVOKED — report_v2 jsonb is consumed directly by ReportAdaptiveView + PDF (Phase 8)."

---

#### `supabase/migrations/0013c_reading_addons.sql` (DDL — new table + RLS)

**Analog:** `supabase/migrations/0007_phase_7_analise_llm.sql` (CHECK constraint pattern, §5) + general RLS pattern from `0011_calibration_rls_jwt_email.sql`

The CONTEXT D-SCH1 already provides the exact DDL (lines 92-113 of CONTEXT.md). Pattern source for CHECK + INDEX + RLS POLICY shapes:

**CHECK constraint inside CREATE TABLE** (from CONTEXT.md D-SCH1 — copy verbatim):
```sql
CREATE TABLE reading_addons (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  reading_id uuid NOT NULL REFERENCES readings(id) ON DELETE CASCADE,
  addon_type text NOT NULL CHECK (addon_type IN (
    'iridological_deep', 'client_facing', 'therapeutic_plan',
    'longitudinal_comparison', 'nutritional_map', 'exam_recommendations'
  )),
  generated_content jsonb,
  generated_at timestamptz,
  credit_cost int NOT NULL DEFAULT 1,
  model_version text,
  therapist_id uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  created_at timestamptz DEFAULT now()
);
CREATE INDEX idx_reading_addons_reading_id ON reading_addons(reading_id);
CREATE INDEX idx_reading_addons_therapist_id ON reading_addons(therapist_id);

ALTER TABLE reading_addons ENABLE ROW LEVEL SECURITY;
CREATE POLICY reading_addons_therapist_select ON reading_addons
  FOR SELECT USING (therapist_id = auth.uid());
CREATE POLICY reading_addons_therapist_insert ON reading_addons
  FOR INSERT WITH CHECK (therapist_id = auth.uid());
```

**Adapt for new file:** Wrap in `begin;…commit;`. Header marks "V1: table created EMPTY; populated by V1.1 add-on flow." Cite D-ADD1, D-SCH1.

---

### ANTHROPIC LIB

#### `apps/web/lib/anthropic/report-schema.ts` (NEW — zod schema for report_v2)

**Analog:** `apps/web/app/actions/analise.schemas.ts` + `apps/web/lib/anthropic/types.ts`

**Imports + module header pattern** (from `analise.schemas.ts` lines 1-3, and `audit.ts` lines 36-42 for `server-only` + named-export TS pattern):
```ts
import 'server-only'
import { z } from 'zod'

const REPORT_KEYS = [
  '1_constituicao',
  ...
] as const

export const reportDeliveredSchema = z
  .object(
    Object.fromEntries(
      REPORT_KEYS.map((k) => [k, z.string().optional()]),
    ) as { [K in (typeof REPORT_KEYS)[number]]: z.ZodOptional<z.ZodString> },
  )
  .passthrough()

export type ReportDeliveredInput = z.infer<typeof reportDeliveredSchema>
```

**Adapt for new file:** RESEARCH.md §Anthropic JSON mode (lines ~158-204) already provides the full schema target. Use `'server-only'` import; export `SYSTEM_IDS`, `TENDENCY_LABELS` as `as const` tuples; export `reportV2Schema` with `z.object({...})` shape from RESEARCH.md; export `type ReportV2 = z.infer<typeof reportV2Schema>`. **CRITICAL** — RESEARCH says do NOT chain `.min/.max` constraints (Anthropic strips them); enforce length-3 on `priority_focus` POST-parse, not on the wire.

---

#### `apps/web/lib/anthropic/analyze-v2.ts` (NEW — rewritten for V2 JSON mode + zod retry)

**Analog:** `apps/web/lib/anthropic/analyze.ts` (the EXACT same file pattern, but rewritten body)

**Header + imports + types** (lines 1-65):
```ts
/**
 * `analyzeReading` — orchestrates a Phase 7 LLM analysis call.
 *
 * Composition (CONTEXT D-S1, D-S2, D-PR1, D-PR2, D-T1, RESEARCH §Pattern 1, §Pattern 2):
 *   1. Parallel: load system.md + feature-injection.md from FS (cached)
 *      + retrieveRelevantKnowledge(features, REPORT_SECTIONS) (frozen Fase 6)
 *   2. Render user content via mustache substitution
 *   3. Open Anthropic stream with cache_control: ephemeral on system block
 *   4. Return { stream: AsyncIterable<string>, finalize: () => Promise<...> }
 *
 * Auth gate is NOT here — it's the caller's (Route Handler) responsibility.
 * LGPD: telemetry log explicitly excludes client_name + therapist_notes.
 */
import 'server-only'

import {
  anthropicClient,
  MODEL,
  DEFAULT_SYSTEM_CACHE_CONTROL,
  MAX_OUTPUT_TOKENS,
  estimateCostUsd,
} from './client'
import { loadSystemPrompt, ... } from './prompts'
import { retrieveRelevantKnowledge } from '@/lib/rag/search'

export interface AnalyzeArgs { ... }
export interface AnalyzeFinalization { usage: {...}; latency_ms: number; cost_estimate_usd: number; n_chunks_rag: number }
export interface AnalyzeResult { stream: AsyncIterable<string>; finalize: () => Promise<AnalyzeFinalization> }
```

**Stream open pattern** (lines 124-156):
```ts
const llmStream = anthropicClient.messages.stream({
  model: MODEL,
  max_tokens: MAX_OUTPUT_TOKENS,
  system: [
    { type: 'text', text: systemPrompt, cache_control: DEFAULT_SYSTEM_CACHE_CONTROL },
  ],
  messages: [{ role: 'user', content: userContent }],
})

if (args.signal) {
  args.signal.addEventListener('abort', () => {
    try { llmStream.controller.abort() } catch { /* already ended */ }
  })
}

async function* toTextStream(): AsyncIterable<string> {
  for await (const event of llmStream) {
    if (event.type === 'content_block_delta' && event.delta.type === 'text_delta') {
      yield event.delta.text
    }
  }
}
```

**Finalize + telemetry pattern** (lines 158-196):
```ts
async function finalize(): Promise<AnalyzeFinalization> {
  const final = await llmStream.finalMessage()
  const usage = {
    input_tokens: final.usage.input_tokens ?? 0,
    output_tokens: final.usage.output_tokens ?? 0,
    cache_creation_input_tokens: final.usage.cache_creation_input_tokens ?? 0,
    cache_read_input_tokens: final.usage.cache_read_input_tokens ?? 0,
  }
  const latencyMs = Date.now() - startedAt
  const cost = estimateCostUsd(usage)

  // D-T1 telemetry — NO PII
  console.info({
    event: 'llm_generate',
    reading_id: args.readingId,
    therapist_id: args.therapistId,
    model_version: MODEL,
    n_chunks_rag: ragChunks.length,
    latency_ms: latencyMs,
    tokens_in: usage.input_tokens,
    tokens_out: usage.output_tokens,
    cache_creation_input_tokens: usage.cache_creation_input_tokens,
    cache_read_input_tokens: usage.cache_read_input_tokens,
    cost_estimate_usd: Number(cost.toFixed(4)),
  })

  return { usage, latency_ms: latencyMs, cost_estimate_usd: cost, n_chunks_rag: ragChunks.length }
}
```

**Adapt for new file:** Add `output_config: { format: zodOutputFormat(reportV2Schema) }` to the `messages.stream()` call (per RESEARCH.md §Anthropic JSON mode line 218). Add retry wrapper `generateWithRetry(systemPrompt, userPrompt, attempt=0, ...)` per RESEARCH.md lines 290-300 — MAX 2 retries, then save raw output + `audit_metadata.json_validation_failed=true`. After stream completes, run `reportV2Schema.safeParse(JSON.parse(buffer))`; on failure rebuild prompt with "O JSON anterior falhou validação. Erros: …" and re-invoke. Telemetry event becomes `iris_codex_report_generate` with extra fields per D-TEL2 (`systems_detected`, `grade_distribution`, `json_validation_passed`, `retry_count`, `iridological_jargon_hits`, `sopro_vocab_hits`, `forbidden_vocab_hits`).

---

#### `apps/web/lib/anthropic/stream-parser-v2.ts` (NEW — top-level JSON key completion detector)

**Analog:** `apps/web/lib/anthropic/parser.ts` (`findAllBoundaries`)

**Module pattern** (lines 1-46 + 63-80):
```ts
import 'server-only'
import { SECTION_KEY_BY_NUMBER, type NumberedSectionKey } from './types'

// Anatomy of BOUNDARY_RE:
//   ^[ \t]*        — line start with optional indent
//   #{2,3}         — 2 or 3 hashes (H2 or H3)
//   ...
const BOUNDARY_RE = /^[ \t]*#{2,3}[ \t]+§?[ \t]*(\d{1,2})[ \t]*[\p{Pd}.][ \t]*/gmu

export interface BoundaryMatch {
  number: number
  key: NumberedSectionKey
  startIdx: number
  headingEndIdx: number
}

export function findAllBoundaries(buffer: string): BoundaryMatch[] {
  BOUNDARY_RE.lastIndex = 0
  const matches: BoundaryMatch[] = []
  let m: RegExpExecArray | null
  let lastNumber = 0
  while ((m = BOUNDARY_RE.exec(buffer)) !== null) {
    const number = parseInt(m[1]!, 10)
    if (number < 1 || number > 13) continue
    if (number !== lastNumber + 1) continue
    lastNumber = number
    ...
  }
}
```

**Adapt for new file:** Replace markdown section-heading regex with JSON top-level key detection. RESEARCH.md recommends path (b): emit keys in fixed order, detect each key completion via regex. Approach:
1. Define ordered list `TOP_LEVEL_KEYS = ['executive_summary', 'constitutional_pattern', 'systems_with_tendency', 'integrative_axes', 'bilateral_findings', 'therapeutic_synthesis', 'priority_focus', 'clinical_note', 'advanced_analysis'] as const`.
2. Detect each completed key via balanced-brace tracking (count `{` and `}` at top level, looking for `"key_name":` followed by closed value).
3. Same Pitfall-2 defenses: reset state per call (no global lastIndex leak), strict monotonic ordering check (return only newly-completed keys).
4. Returns `{ key: keyof ReportV2, completedAt: number }[]` for the route handler to drive incremental persistence.

---

#### `apps/web/lib/anthropic/audit-v2.ts` (extended vocab — jargão + Sopro + diagnóstico)

**Analog:** `apps/web/lib/anthropic/audit.ts`

**Source-cleanliness allowlist marker + assembled regex pattern** (lines 1-10, 69-81):
```ts
// audit-vocabulary:allowlist — este arquivo IMPLEMENTA o detector LGPD
// (FORBIDDEN_VOCAB_RE + NEG_CONTEXT_RE) e cita os 3 termos restritos em
// COMENTÁRIOS DE DOCUMENTAÇÃO ao explicar a heurística de skip negativo
// ...
// SOURCE-CLEANLINESS CONSTRAINT (Pitfall 7 + audit-vocabulary self-match
// avoidance): the three forbidden vocabulary terms NEVER appear as literal
// substrings in this source file's CODE. They are assembled via array-join
// into the RegExp constructor.

import 'server-only'

const _F1 = ['d', 'i', 'a', 'g', 'n', 'ó', 's', 't', 'i', 'c', 'o'].join('')
const _F2 = ['t', 'r', 'a', 't', 'a', 'm', 'e', 'n', 't', 'o'].join('')
const _F3 = ['c', 'u', 'r', 'a'].join('')

export const FORBIDDEN_VOCAB_RE = new RegExp(`\\b(${_F1}|${_F2}|${_F3})\\b`, 'giu')
```

**extractForbiddenHits with neg-context lookback** (lines 114-137):
```ts
export function extractForbiddenHits(text: string, section: string): ForbiddenHit[] {
  if (!text) return []
  const regex = new RegExp(FORBIDDEN_VOCAB_RE.source, FORBIDDEN_VOCAB_RE.flags)
  const counts = new Map<string, number>()
  let m: RegExpExecArray | null
  while ((m = regex.exec(text)) !== null) {
    const lookbackStart = Math.max(0, m.index - NEG_LOOKBACK_CHARS)
    const preceding = text.slice(lookbackStart, m.index)
    if (NEG_CONTEXT_RE.test(preceding)) continue
    const term = m[0]!.toLowerCase()
    counts.set(term, (counts.get(term) ?? 0) + 1)
  }
  return Array.from(counts.entries()).map(([term, occurrences]) => ({ section, term, occurrences }))
}
```

**Adapt for new file:** Add 2 NEW pattern sets per D-VOC1 (keep existing `FORBIDDEN_VOCAB_RE` for diagnóstico/tratamento/cura unchanged):
1. **`IRIDOLOGICAL_JARGON_RE`** — assembled via similar array-join pattern (terms like `constituição linfática`, `anel nervoso`, `Jensen`, `Lo Rito`, hN codes, `setor \d+h\d+`, etc.); confirm exact list with founder during plan-phase (CONTEXT line 217).
2. **`SOPRO_VOCAB_RE`** — terms like `centelha divina`, `atravessar`, `vasto`, `essência primordial` (CONTEXT line 219).

Export new `runAuditV2(report: ReportV2): AuditMetadataV2` that returns `{ iridological_jargon: Hit[], sopro_vocab: Hit[], forbidden_vocab: Hit[], audited_at: string }`. Scan each string field in `report_v2` (executive_summary, each system's clinical_description / therapeutic_direction, integrative_axes descriptions, therapeutic_synthesis, priority_focus[], clinical_note). Reuse `NEG_CONTEXT_RE` pattern + lookback only for the LGPD set (false-positive defense).

---

#### `apps/web/lib/anthropic/diff-v2.ts` (keyed by system_id, not section_number)

**Analog:** `apps/web/lib/anthropic/diff.ts` (1:1 reuse of `classifyEdit` for the per-key part)

**classifyEdit primitive (REUSE AS-IS, do NOT duplicate)** (lines 53-92):
```ts
export function classifyEdit(generated: string, delivered: string): ClassifiedEdit {
  const trimGen = (generated ?? '').trim()
  const trimDel = (delivered ?? '').trim()

  if (trimGen === trimDel) {
    return { type: 'none', diff_summary: '', char_delta: 0, changed_pct: 0 }
  }
  if (trimGen === '' && trimDel !== '') {
    return { type: 'adicionado', diff_summary: 'novo conteúdo adicionado', char_delta: trimDel.length, changed_pct: 100 }
  }
  if (trimGen !== '' && trimDel === '') {
    return { type: 'removido', diff_summary: 'conteúdo removido', char_delta: -trimGen.length, changed_pct: 100 }
  }

  const changes: Change[] = diffWords(trimGen, trimDel)
  const totalTokens = changes.reduce((sum, c) => sum + (c.count ?? 0), 0)
  const changedTokens = changes
    .filter((c) => c.added || c.removed)
    .reduce((sum, c) => sum + (c.count ?? 0), 0)
  const changedPct = totalTokens === 0 ? 0 : Math.round((changedTokens / totalTokens) * 100)

  return {
    type: changedPct >= THRESHOLD_PCT ? 'reescrito' : 'corrigido',
    diff_summary: summarizeChanges(changes),
    char_delta: trimDel.length - trimGen.length,
    changed_pct: changedPct,
  }
}
```

**classifyAllSections — iteration over canonical key set** (lines 165-192):
```ts
const ALL_REPORT_KEYS: ReportSectionKey[] = [
  '1_constituicao', '2_estrutural_fisica', ..., 'encerramento_disclaimer',
]

export function classifyAllSections(generated: ReportJsonb, delivered: ReportJsonb): SectionDiffs {
  const keys = new Set<ReportSectionKey>()
  for (const k of ALL_REPORT_KEYS) {
    if (k in generated || k in delivered) keys.add(k)
  }

  const editDiff: Partial<Record<ReportSectionKey, ClassifiedEdit>> = {}
  const zonasEditadas: ReportSectionKey[] = []
  const tiposSet = new Set<EditTipo>()

  for (const key of keys) {
    const classified = classifyEdit(generated[key] ?? '', delivered[key] ?? '')
    editDiff[key] = classified
    if (classified.type !== 'none') {
      zonasEditadas.push(key)
      tiposSet.add(classified.type)
    }
  }

  return { edit_diff: editDiff, zonas_editadas: zonasEditadas, tipo_edicao: Array.from(tiposSet) }
}
```

**Adapt for new file:** Import `classifyEdit` from `./diff.ts` (do NOT duplicate). Write `classifyAllSystemsV2(generated: ReportV2, delivered: ReportV2): ReportV2EditDiff` where keys are:
- top-level: `executive_summary`, `constitutional_pattern.description`, `therapeutic_synthesis`, `clinical_note`, `priority_focus` (joined)
- per-system: each `system_id` in either generated.systems_with_tendency or delivered.systems_with_tendency → diff `clinical_description`, `therapeutic_direction` concatenated.

Output shape per D-SCH3: `{ [key]: { delta_type, char_delta, levenshtein_pct } }` — note CONTEXT uses `levenshtein_pct` but existing code returns `changed_pct`; either keep `changed_pct` (consistent with existing diff.ts) or compute Levenshtein with the same `diff` library — recommend keeping `changed_pct` for consistency and updating CONTEXT.md comment.

---

#### `apps/web/prompts/system.md` (rewritten from scratch — Iris Codex V1)

**Analog:** `apps/web/prompts/system.md` (current Phase 7 version) — read the existing file to preserve structure conventions, then replace all content.

**Allowlist marker pattern** — current `system.md` starts with `<!-- audit-vocabulary:allowlist -->` to allow it to mention forbidden vocab while instructing LLM to avoid. Same marker MUST be preserved at top of rewritten file (CONTEXT D-VOC2).

**Adapt for new file:** Full rewrite per D-PR1 in CONTEXT.md (lines 128-141):
- Identity: "analista clínico-funcional do Iris Codex"
- 7 revised principles (CONTEXT 128-138)
- Schema description (cross-reference `report_v2` shape but rely on `output_config` for hard enforcement)
- 2-voice rule preserved from Phase 7 (factual vs hypothetical)
- Few-shot examples for the JSON output shape
- **Must be ≥2200 tokens** (RESEARCH.md Pitfall 4 — Sonnet 4.6 cache_control threshold). Token-counted via `prompts.ts` warn at module init.

---

#### `apps/web/lib/tendency-engine/placeholder.ts` (NEW — heuristic rules per system)

**Analog:** `apps/web/lib/rag/build-queries.ts` (closest pure-function library pattern in codebase)

**Pure-function interface + types** (lines 21-60):
```ts
/**
 * Family A + B query generators (D-R2). Pure functions, no I/O.
 *
 * Family A — derived from IrisFeatures (visual findings):
 *   - 1 query per constituição primária + secundária (when present)
 *   - 1 query per sector with findings.length > 0 (templated)
 *   - 1 query per active global ring/sign
 *
 * Phase: 06-rag-ingestao | Plan: 06-10 | Decisions: D-R2A, D-R2B
 */
import type { ReportSection } from './types'
import { SECTION_QUERY_TEMPLATES } from './section-queries'

export interface IrisFeaturesForRag {
  constitution: { primary: string; secondary?: string }
  sectors: Array<{ hour: number; findings: Array<{ type: string }> }>
  rings: Record<string, { present: boolean }>
}

export function buildFamilyA(features: IrisFeaturesForRag): string[] {
  const queries: string[] = []
  if (features.constitution.primary) {
    queries.push(`constituição ${features.constitution.primary}`)
  }
  ...
  for (const sec of features.sectors) {
    if (sec.findings.length > 0) {
      const types = sec.findings.map((f) => f.type).join(', ')
      queries.push(`${types} no setor ${sec.hour}`)
    }
  }
  for (const [name, ring] of Object.entries(features.rings)) {
    if (ring.present) {
      queries.push(`${name} presente`)
    }
  }
}
```

**Adapt for new file:** Per D-PR3 (CONTEXT line 143):
- Export `interface Tendency { system_id: SystemId; system_name: string; tendency_grade: 1|2|3|4|5; signal_evidence: string[] }`.
- Export `mapVisionFeaturesToTendencies(features: IrisFeaturesForRag): Tendency[]`.
- Body: deterministic if/else rules grouped per system (~8-12 systems). Each rule MUST end with the marker comment `// TODO Phase 7.5 — replace with real engine` so 7.5 can grep them.
- Pattern: iterate `features.sectors[]`, lookup hour → system mapping, accumulate grade from finding type frequency.
- Pure function — NO I/O, NO `'server-only'` strictly required (placeholder may be exercised in unit tests), but ADD `'server-only'` to enforce server-only invocation matching the lib/anthropic pattern.

---

### ROUTE HANDLER + SERVER ACTIONS

#### `apps/web/app/api/readings/[id]/analyze/route.ts` (extend w/ V2 branch)

**Analog:** Same file (current Phase 7 implementation). All 5 auth gates + Web Streams shell + finalize pattern stay; new code branches on `reading.report_version === '2.0' || reading.report_v2 != null` to call `analyzeReadingV2` and use the V2 stream parser.

**Auth gate pattern + RLS check** (lines 47-107):
```ts
export async function POST(
  request: NextRequest,
  { params }: { params: Promise<{ id: string }> },
): Promise<Response> {
  const { id: readingId } = await params

  const supabase = await createClient()
  const { data: { user }, error: authError } = await supabase.auth.getUser()
  if (!user || authError) {
    return NextResponse.json({ error: 'Unauthenticated' }, { status: 401 })
  }

  const { data: reading, error: readingError } = await supabase
    .from('readings')
    .select('id, therapist_id, status, vision_features, ...')
    .eq('id', readingId)
    .maybeSingle()
  ...
  // Gate (b): ownership
  if (reading.therapist_id !== user.id) {
    return NextResponse.json({ error: 'Forbidden' }, { status: 403 })
  }
  // Gate (c): status
  if (reading.status !== 'ready') { ... }
  // Gate (d): not yet delivered
  if (reading.report_delivered != null) { ... }
  // Gate (e): regen cap
  const currentCount = reading.regeneration_count ?? 0
  if (currentCount >= 3 && !isFounder) { ... }
```

**Streaming + atomic persistence loop pattern** (lines 174-205):
```ts
const responseStream = new ReadableStream({
  async start(controller) {
    try {
      for await (const text of analysis.stream) {
        buffer += text
        controller.enqueue(encoder.encode(text))

        // Detect new boundaries — only trigger persistence when section CLOSES
        const boundaries = findAllBoundaries(buffer)
        if (boundaries.length > lastBoundaryCount) {
          const closed = closeSections(boundaries.slice(0, -1), buffer)
          for (const section of closed) {
            if (completedSections[section.key] !== section.content) {
              completedSections[section.key] = section.content
              await supabase
                .from('readings')
                .update({ report_generated: { ...completedSections } })
                .eq('id', readingId)
            }
          }
          lastBoundaryCount = boundaries.length
        }
      }
      ...
```

**Adapt for new file:** Branch on `report_version`. For V2 path:
- Use `analyzeReadingV2()` from `analyze-v2.ts`.
- Call `mapVisionFeaturesToTendencies()` first to get `tendencies[]`; pass to `analyzeReadingV2` as `<tendencies>` block (D-PR2).
- Use `findCompletedTopLevelKeys()` from `stream-parser-v2.ts` instead of `findAllBoundaries`.
- Atomic write per-key uses `jsonb_set` semantics — update `report_v2` column instead of `report_generated`.
- On finalize: call `runAuditV2()` (3 vocab sets); persist `audit_metadata` shape `{ iridological_jargon, sopro_vocab, forbidden_vocab, json_validation_passed, retry_count }`.
- Telemetry event `iris_codex_report_generate` per D-TEL2 (CONTEXT line 250-263).

---

#### `apps/web/app/actions/analise.ts` — extend with `saveReportV2Delivered` + `markReadingV2Delivered`

**Analog:** Same file (`saveReportDelivered` — lines 41-106; `markReadingDelivered` — lines 113-171)

**Server action shape — auth + zod + audit + diff + UPDATE pattern** (lines 41-106):
```ts
'use server'

export async function saveReportDelivered(
  readingId: string,
  reportDelivered: Record<string, string>,
): Promise<SaveResult> {
  const supabase = await createClient()
  const { data: { user }, error: authError } = await supabase.auth.getUser()
  if (!user || authError) redirect('/login')

  const idParsed = readingIdSchema.safeParse({ reading_id: readingId })
  if (!idParsed.success) return { error: 'reading_id inválido' }

  const bodyParsed = reportDeliveredSchema.safeParse(reportDelivered)
  if (!bodyParsed.success) return { error: 'Formato do relatório inválido' }

  // Defense in depth — D-A2 BLOCK save if forbidden vocab present
  const allHits = []
  for (const [key, value] of Object.entries(bodyParsed.data)) {
    if (typeof value !== 'string') continue
    allHits.push(...extractForbiddenHits(value, key))
  }
  if (allHits.length > 0) {
    const terms = Array.from(new Set(allHits.map((h) => h.term))).join(', ')
    return { error: `Não foi possível salvar: ${terms}. Corrija e tente novamente.` }
  }

  // Load + diff + audit
  const { data: reading } = await supabase
    .from('readings')
    .select('id, therapist_id, report_generated, is_delivered')
    .eq('id', readingId).eq('therapist_id', user.id).single()
  ...
  const diffs = classifyAllSections(generated, delivered)
  const audit = runAudit(delivered)

  const { error: updateError } = await supabase
    .from('readings')
    .update({
      report_delivered: delivered as never,
      edit_diff: diffs.edit_diff as never,
      ...
      status: 'edited',
      report_delivered_at: new Date().toISOString(),
      audit_metadata: audit as never,
    })
    .eq('id', readingId)

  revalidatePath(`/leituras/${readingId}`)
  revalidatePath(`/leituras/${readingId}/editar`)
  return { success: true }
}
```

**Adapt for new file:** Export `saveReportV2Delivered(readingId, reportV2: Partial<ReportV2>)`. Per-block save mode (D-UI2 + UI-SPEC FLAG-4) — server action accepts either a partial (single block) or full ReportV2 object. zod validation via new `reportV2DeliveredSchema` from `analise.schemas.ts`. Diff via `classifyAllSystemsV2` from `diff-v2.ts`. Audit via `runAuditV2` from `audit-v2.ts`. UPDATE columns: `report_v2_delivered`, `report_v2_edit_diff`, `audit_metadata` (merge with existing — don't clobber `json_validation_passed`/`retry_count` from generation), `status='edited'`. Hard-gate on `markReadingV2Delivered`: must have ZERO hits across ALL 3 vocab sets (jargão, Sopro, forbidden) before flipping `is_delivered=true`.

---

### FRONTEND — VIEW + EDITOR

#### `apps/web/components/readings/ReportAdaptiveView.tsx` (NEW)

**Analog:** `apps/web/components/readings/EditorAccordion.tsx` (legacy renderer — both coexist per D-UI3)

**Component shape — section iteration, markdown rendering** (lines 1-50):
```tsx
'use client'

import {
  Accordion, AccordionContent, AccordionItem, AccordionTrigger,
} from '@/components/ui/accordion'
import ReactMarkdown from 'react-markdown'
import remarkGfm from 'remark-gfm'

import { EditorSectionItem } from './EditorSectionItem'

export const SECTIONS: ReadonlyArray<{ key: string; number: number; title: string }> = [
  { key: '1_constituicao', number: 1, title: 'Constituição' },
  ...
]

export interface EditorAccordionProps {
  reportGenerated: Record<string, string>
  reportDelivered: Record<string, string>
  onSectionChange: (key: string, value: string) => void
  readOnly?: boolean
}

export function EditorAccordion({ reportGenerated, reportDelivered, onSectionChange, readOnly }: EditorAccordionProps) {
  return (
    <Accordion multiple defaultValue={['1_constituicao', ...]} className="w-full">
      {SECTIONS.map((s) => {
        const generated = reportGenerated[s.key] ?? ''
        const delivered = reportDelivered[s.key] ?? generated
        ...
      })}
    </Accordion>
  )
}
```

**Adapt for new file:** RSC (no `'use client'` — UI-SPEC §State Persistence Contract has RSC reading report_v2). Render adaptive stack per UI-SPEC §Layout Patterns (lines 336-417). Sequence: `executive_summary` Card → `constitutional_pattern` Card with `KeyTraitChipGroup` → "Sistemas com tendência" header + sorted `systems_with_tendency.map(s => <SystemTendencyCard />)` (sort by `tendency_grade` desc) → `IntegrativeAxisItem` list inside Eixos Card → conditional `bilateral_findings` Card (only if `asymmetry_present`) → `therapeutic_synthesis` Card → `PriorityFocusList` Card → `clinical_note` (no Card wrapper, `text-sm text-muted-foreground`) → `<AdvancedAnalysisCTA />` at footer. Use `react-markdown` + `remark-gfm` with `prose prose-sm prose-neutral max-w-none` for prose fields. Empty state per UI-SPEC line 419-434 when `systems_with_tendency.length === 0`.

---

#### `apps/web/components/readings/SystemTendencyCard.tsx` (NEW — one card per system_id)

**Analog:** `apps/web/components/readings/EditorSectionItem.tsx`

**Component header + state + markdown render** (lines 14-63):
```tsx
'use client'

import { ChangeEvent } from 'react'
import ReactMarkdown from 'react-markdown'
import remarkGfm from 'remark-gfm'

import { Textarea } from '@/components/ui/textarea'
import { Label } from '@/components/ui/label'

export interface EditorSectionItemProps {
  sectionKey: string
  number: number
  title: string
  generatedValue: string
  deliveredValue: string
  onChange: (value: string) => void
  readOnly?: boolean
}

export function EditorSectionItem({ ... }: EditorSectionItemProps) {
  const isEdited = deliveredValue !== generatedValue
  const charCount = deliveredValue.length

  return (
    <div className="space-y-3 px-2 pt-3">
      <div className="space-y-2">
        <Label htmlFor={`textarea-${sectionKey}`} className="text-sm">
          Texto da seção
        </Label>
        <Textarea
          id={`textarea-${sectionKey}`}
          value={deliveredValue}
          onChange={(e: ChangeEvent<HTMLTextAreaElement>) => onChange(e.target.value)}
          className="min-h-[200px] font-mono text-sm"
          disabled={readOnly}
        />
        <p className="text-xs text-muted-foreground">
          {charCount} caracteres
          {isEdited && <span className="ml-2">· editado</span>}
        </p>
      </div>
      <div className="space-y-2">
        <Label className="text-sm">Pré-visualização</Label>
        <div className="prose prose-sm prose-neutral max-w-none rounded-md border bg-muted/30 p-3">
          <ReactMarkdown remarkPlugins={[remarkGfm]}>{deliveredValue}</ReactMarkdown>
        </div>
      </div>
    </div>
  )
}
```

**Adapt for new file:** Props `{ system: SystemTendency; onEdit?: (system_id, partial) => void; readOnly?: boolean }`. Layout per UI-SPEC §Layout Patterns lines 354-376:
- Card with `flex justify-between items-center` header containing `<h3 className="text-xl font-semibold">{system_name}</h3>` + `<GradeBadge grade label />` + `<GradeBar grade />`
- Body: `clinical_description` rendered via ReactMarkdown
- 2-col grid (`md:grid-cols-2 gap-4`): `associated_manifestations` (bullets) + `investigation_points` (bullets)
- Highlighted footer (`bg-muted/50 rounded p-4`) with "Direção terapêutica" label + `therapeutic_direction` markdown
- Top-right `Editar` button when not readOnly → toggles `<BlockEditPane>` with form fields per UI-SPEC line 265-268.

---

#### `apps/web/components/readings/GradeBadge.tsx` + `GradeBar.tsx` (NEW)

**Analog:** `apps/web/components/readings/StatusBadge.tsx` (closest existing badge) + `apps/web/components/ui/badge.tsx` (primitive)

**Badge variant pattern** (from `badge.tsx` lines 7-28):
```tsx
const badgeVariants = cva(
  "group/badge inline-flex h-5 w-fit shrink-0 items-center justify-center gap-1 ... rounded-4xl border border-transparent px-2 py-0.5 text-xs font-medium",
  {
    variants: {
      variant: {
        default: "bg-primary text-primary-foreground ...",
        secondary: "bg-secondary text-secondary-foreground ...",
        destructive: "bg-destructive/10 text-destructive ...",
        outline: "border-border text-foreground ...",
        ghost: "hover:bg-muted hover:text-muted-foreground ...",
      },
    },
    defaultVariants: { variant: "default" },
  }
)
```

**Adapt for new files:**
- `GradeBadge.tsx`: thin wrapper around `<Badge>` rendering `Grade {n}/5 · {tendency_label}` (UI-SPEC line 217). Variant `outline` default.
- `GradeBar.tsx`: NEW pattern — no exact analog. UI-SPEC line 144-157 provides the exact structure:
  ```tsx
  <div className="flex w-24 gap-0.5">
    {[1,2,3,4,5].map(i => (
      <div key={i} className={cn(
        "h-2 flex-1 rounded-sm",
        i <= grade ? GRADE_FILL[grade] : "bg-muted",
      )} />
    ))}
  </div>
  ```
  Where `GRADE_FILL = { 1: 'bg-amber-200', 2: 'bg-amber-400', 3: 'bg-orange-500', 4: 'bg-red-500', 5: 'bg-red-700' }` per UI-SPEC §Color Grade visual bar palette.

---

#### `apps/web/components/readings/AdvancedAnalysisCTA.tsx` (NEW — placeholder + modal)

**Analog:** `apps/web/components/readings/DeliverDialog.tsx` (Dialog pattern)

**Dialog component pattern** (lines 1-58):
```tsx
'use client'

import { useRef } from 'react'

import {
  Dialog, DialogContent, DialogDescription, DialogFooter,
  DialogHeader, DialogTitle,
} from '@/components/ui/dialog'
import { Button } from '@/components/ui/button'

export interface DeliverDialogProps {
  open: boolean
  onOpenChange: (open: boolean) => void
  onConfirm: () => void
  pending: boolean
}

export function DeliverDialog({ open, onOpenChange, onConfirm, pending }: DeliverDialogProps) {
  const cancelRef = useRef<HTMLButtonElement>(null)

  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent showCloseButton={false} initialFocus={cancelRef}>
        <DialogHeader>
          <DialogTitle>Entregar ao cliente?</DialogTitle>
          <DialogDescription>
            Após entregar, esta análise fica congelada e não poderá mais ser editada. Confirme
            para prosseguir.
          </DialogDescription>
        </DialogHeader>
        <DialogFooter className="flex justify-end gap-2">
          <Button ref={cancelRef} variant="outline" onClick={() => onOpenChange(false)} disabled={pending}>
            Cancelar
          </Button>
          <Button variant="destructive" onClick={onConfirm} disabled={pending}>
            {pending ? 'Entregando…' : 'Sim, entregar'}
          </Button>
        </DialogFooter>
      </DialogContent>
    </Dialog>
  )
}
```

**Adapt for new file:** Self-contained: own `useState` for `open`. Outer trigger is the placeholder Button (`variant="outline"`, `lucide Sparkles` leading icon, label "Análise Iridológica Aprofundada — 1 crédito"). Tooltip wrap with content "Em breve na V1.1". On click, opens Dialog with locked copy (UI-SPEC Surface 1c lines 235-246): title "Análise Iridológica Aprofundada", 4 body paragraphs verbatim, single "OK" button (`variant="default"`, `initialFocus={okRef}` — informational dismissal). NO server interaction in V1 (no `reading_addons` row created).

---

#### `apps/web/components/readings/VocabularyAuditBanner.tsx` (NEW — replaces/extends EditorAuditBanner for V2)

**Analog:** `apps/web/components/readings/EditorAuditBanner.tsx`

**Banner pattern** (full file lines 1-55):
```tsx
import { AlertTriangle } from 'lucide-react'

import { Alert, AlertDescription, AlertTitle } from '@/components/ui/alert'
import type { AuditMetadata } from '@/lib/anthropic/types'

export interface EditorAuditBannerProps {
  auditMetadata: AuditMetadata | null
}

export function EditorAuditBanner({ auditMetadata }: EditorAuditBannerProps) {
  if (!auditMetadata) return null

  const showAnchor = auditMetadata.low_anchor_rate
  const showVocab = (auditMetadata.forbidden_vocab?.length ?? 0) > 0

  if (!showAnchor && !showVocab) return null

  const termList = (auditMetadata.forbidden_vocab ?? [])
    .map((h) => h.term)
    .filter((t, i, a) => a.indexOf(t) === i)
    .join(', ')

  return (
    <div className="space-y-3">
      {showAnchor && (
        <Alert variant="destructive">
          <AlertTriangle className="h-4 w-4" aria-hidden />
          <AlertTitle>Baixa ancoragem em features</AlertTitle>
          <AlertDescription>...</AlertDescription>
        </Alert>
      )}
      {showVocab && (
        <Alert variant="destructive">
          <AlertTriangle className="h-4 w-4" aria-hidden />
          <AlertTitle>Termos clinicamente afirmativos detectados</AlertTitle>
          <AlertDescription>
            Os seguintes termos foram identificados no texto e precisam ser corrigidos antes da
            entrega: {termList}.
          </AlertDescription>
        </Alert>
      )}
    </div>
  )
}
```

**Adapt for new file:** Accept new `AuditMetadataV2` shape: `{ iridological_jargon, sopro_vocab, forbidden_vocab, json_validation_failed }`. Render UP TO 4 Alert destructive banners stacked (one per non-empty hit category + one for json_validation_failed). UI-SPEC line 283 locked banner copy: "Termos proibidos detectados" + "Os seguintes termos foram identificados no relatório e precisam ser corrigidos antes da entrega: {term_list}. O Iris Codex usa linguagem clínico-funcional…". Non-dismissible — state-driven only (UI-SPEC line 175).

---

#### `apps/web/components/readings/AdaptiveAnalysisStream.tsx` (NEW or extend AnalysisStream)

**Analog:** `apps/web/components/readings/AnalysisStream.tsx`

**Streaming progress component** (lines 18-89):
```tsx
'use client'

import { Loader2, Check } from 'lucide-react'

import { Progress } from '@/components/ui/progress'
import { Skeleton } from '@/components/ui/skeleton'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'

const SECTION_TITLES = ['Constituição', ..., 'Mensagem Final']

export interface AnalysisStreamProps {
  sectionsReceived: number
  error?: string | null
}

export function AnalysisStream({ sectionsReceived, error }: AnalysisStreamProps) {
  const safe = Math.min(13, Math.max(0, sectionsReceived))
  const pct = Math.round((safe / 13) * 100)

  return (
    <Card className="max-w-3xl">
      <CardHeader>
        <CardTitle className="flex items-center gap-2 text-xl">
          <Loader2 className="h-4 w-4 animate-spin" aria-hidden />
          <span>Gerando relatório… {safe}/13 seções</span>
        </CardTitle>
        <p className="text-sm text-muted-foreground">
          Você pode atualizar a página — o progresso fica salvo.
        </p>
      </CardHeader>
      <CardContent className="space-y-4">
        <Progress value={pct} aria-label="Progresso da geração" />
        <div role="region" aria-live="polite" aria-atomic="false" className="space-y-2">
          {SECTION_TITLES.map((title, i) => {
            const received = i < safe
            return (
              <div key={i} className="flex items-center gap-3">
                {received ? <Check className="h-4 w-4 text-foreground" /> : <Skeleton className="h-4 w-4 rounded-sm" />}
                <span className={received ? 'text-sm' : 'text-sm text-muted-foreground'}>
                  {i + 1}. {title}
                </span>
              </div>
            )
          })}
        </div>
        {error && (
          <p className="text-sm text-destructive">
            A geração foi interrompida...
          </p>
        )}
      </CardContent>
    </Card>
  )
}
```

**Adapt for new file:** Constant `BLOCK_TITLES = ['Resumo executivo', 'Padrão constitucional', 'Sistemas com tendência', 'Eixos integrativos', 'Achados bilaterais', 'Síntese terapêutica', 'Foco prioritário', 'Nota clínica']` (8 blocks per UI-SPEC §Streaming Visual Cue lines 528-538). Title: `Gerando relatório… {N}/8 blocos`. Replace check-list with skeleton-per-pending-block per UI-SPEC line 229. Recommendation: extend `AnalysisStream` with a `mode: 'sections-13' | 'blocks-8'` prop OR build new `AdaptiveAnalysisStream` and route from `analise-client.tsx` based on `report_version`. UI-SPEC FLAG-2 notes planner picks.

---

### PAGES (RSC routers)

#### `apps/web/app/(dashboard)/leituras/[id]/page.tsx` (router by report_version)

**Analog:** Same file (current).

**RSC + RLS load + props pattern** (lines 24-95):
```tsx
import { createClient } from '@/lib/supabase/server'
import { AnalysisHero } from '@/components/readings/AnalysisHero'
import { AnaliseClient } from './analise-client'

export const dynamic = 'force-dynamic'

export default async function LeituraDetailPage({ params }: { params: Promise<{ id: string }> }) {
  const { id: readingId } = await params

  const supabase = await createClient()
  const { data: reading, error } = await supabase
    .from('readings')
    .select('id, status, created_at, report_generated, report_delivered, audit_metadata, regeneration_count, is_delivered, delivered_at, vision_features, client:clients(full_name, birth_date)')
    .eq('id', readingId)
    .maybeSingle()

  if (error || !reading) notFound()

  const reportGenerated = reading.report_generated as Record<string, string> | null
  const hasReport = reportGenerated != null && Object.keys(reportGenerated).length > 0
  ...

  return (
    <div className="space-y-6 px-6 py-8">
      ...
      <AnalysisHero readingId={readingId} hasReport={hasReport} ...>
        <AnaliseClient readingId={readingId} hasInitialReport={hasReport} ... />
      </AnalysisHero>
    </div>
  )
}
```

**Adapt for new file:** Extend SELECT to also pull `report_v2, report_v2_delivered, report_version, report_v2_generated_at`. After AnalysisHero, branch:
```tsx
const reportV2 = reading.report_v2 as ReportV2 | null
const isV2 = reading.report_version === '2.0' || reportV2 != null
const hasReportV2 = reportV2 != null && Object.keys(reportV2).length > 0

if (isV2 && hasReportV2) {
  return <ReportAdaptiveView report={reportV2} ... />
}
// legacy fallback
if (hasReport) {
  return <EditorAccordion reportGenerated={...} ... readOnly /> // existing legacy display path
}
```

Per UI-SPEC §Layout lines 318-334, the hero card stays unchanged; the report body is the version-routed branch BELOW the hero. Legacy `EditorAccordion` rendering remains untouched.

---

#### `apps/web/app/(dashboard)/leituras/[id]/editar/page.tsx` (same split for editor)

**Analog:** Same file (current — lines 11-64).

**Adapt for new file:** Same SELECT extension; render `<ReportAdaptiveEditor>` when `report_version='2.0'`, else continue rendering `<EditarClient>` (the current `editar-client.tsx` wrapping `EditorAccordion`).

---

### SCRIPTS / CI

#### `apps/web/scripts/audit-vocabulary.mjs` (extend with 3 pattern sets)

**Analog:** Same file (current — lines 14-19).

**Pattern + EXCLUDE_SUBPATHS structure** (lines 14-37):
```js
// Padrão de vocabulário proibido (LGPD-06 — jamais usar em superfícies do produto)
const PATTERN = /diagnóstico|tratamento|cura/i

const EXTENSIONS = new Set(['.ts', '.tsx', '.js', '.jsx', '.mjs', '.cjs'])

const DIRS = ['app', 'components', 'lib/rag', 'lib/anthropic']

const EXCLUDE_SUBPATHS = [
  '/app/admin/',
  '/components/calibration/',
  '/app/actions/calibration.test.',
]

const ALLOWLIST_MARKER = 'audit-vocabulary:allowlist'
```

**Per-file scan + match loop** (lines 74-95):
```js
for (const dir of DIRS) {
  const absDir = join(ROOT, dir)
  const files = collectFiles(absDir)
  for (const file of files) {
    const normalized = file.replace(/\\/g, '/')
    if (EXCLUDE_SUBPATHS.some(prefix => normalized.includes(prefix))) continue

    const content = readFileSync(file, 'utf8')
    if (content.includes(ALLOWLIST_MARKER)) continue
    const lines = content.split('\n')
    for (let i = 0; i < lines.length; i++) {
      if (PATTERN.test(lines[i])) {
        matches.push(`${file}:${i + 1}: ${lines[i].trim()}`)
      }
    }
  }
}
```

**Adapt for new file:** Extend `PATTERN` to 3 separate regexes per D-VOC1:
- `LGPD_PATTERN = /\b(diagnóstico|tratamento|cura)\b/iu` (existing, word-boundary tightened)
- `JARGON_PATTERN = /(constituição\s+linfática|constituição\s+hematogênea|anel\s+nervoso|trama|lacuna\s+grau\s+\d|radii\s+solaris|sodium\s+ring|escola\s+(alemã|jensen)|Jensen.*tradição|Lo\s+Rito|Battello|Deck.*Angerer|Lindemann|hN\d+|setor\s+\d+h\d+|\[ancorado em)/i`
- `SOPRO_PATTERN = /(centelha\s+divina|atravessar|vasto|essência\s+primordial|sopro\s+da\s+origem|princípio\s+criador|mistério|caminho\s+da\s+alma)/i`

Combine via OR or run all three with categorized output. Keep `EXCLUDE_SUBPATHS` + `ALLOWLIST_MARKER` pattern. Per CONTEXT.md line 217 the exact lists need founder confirmation in plan-phase.

---

### REBRAND

#### `apps/web/app/manifest.ts` (Aurel → Iris Codex)

**Analog:** Same file (current — lines 1-21).

**Current content**:
```ts
import type { MetadataRoute } from 'next'

export default function manifest(): MetadataRoute.Manifest {
  return {
    name: 'Aurel Iris',
    short_name: 'Aurel Iris',
    description: 'Ferramenta de apoio à anamnese terapêutica integrativa.',
    start_url: '/dashboard',
    ...
  }
}
```

**Adapt for new file:** Per UI-SPEC §Brand-Rename Touchpoint Inventory (lines 655-668):
- `name: 'Iris Codex'`
- `short_name: 'Iris Codex'`
- `description: 'Plataforma clínica de relatórios iridológicos funcionais adaptativos.'`

Other files needing same text-replacement (confirmed by Grep — 10 files):
- `apps/web/app/layout.tsx` (metadata title)
- `apps/web/app/page.tsx` (landing copy)
- `apps/web/app/(auth)/login/page.tsx`, `signup/page.tsx`
- `apps/web/app/(capture)/layout.tsx`
- `apps/web/app/admin/layout.tsx`
- `apps/web/components/dashboard/app-sidebar.tsx`
- `apps/web/lib/anthropic/client.ts` (comment only)
- `apps/web/prompts/system.md` (identity declaration — covered by full prompt rewrite)

Replacement rule: literal token. "Aurel Iris" → "Iris Codex"; "Aurel" → "Iris Codex".

---

### TESTS

#### `apps/web/lib/anthropic/__tests__/audit-v2.test.ts`

**Analog:** `apps/web/lib/anthropic/__tests__/audit.test.ts`

**Allowlist marker + assembled term fixtures pattern** (lines 1-30):
```ts
// audit-vocabulary:allowlist — este teste valida em runtime que `audit.ts`
// rejeita compostos pt-BR contendo termo proibido como substring...

import { describe, it, expect } from 'vitest'
import { readFileSync } from 'node:fs'
import path from 'node:path'
import { runAudit, FORBIDDEN_VOCAB_RE, extractForbiddenHits } from '../audit'
import type { ReportJsonb } from '../types'

const TERM_DIAG = ['d', 'i', 'a', 'g', 'n', 'ó', 's', 't', 'i', 'c', 'o'].join('')
const TERM_TRAT = ['t', 'r', 'a', 't', 'a', 'm', 'e', 'n', 't', 'o'].join('')
const TERM_CURA = ['c', 'u', 'r', 'a'].join('')

describe('lib/anthropic/audit — anchor rate (D-A1)', () => {
  it('low_anchor_rate=false quando overall = 100%', () => {
    const report: ReportJsonb = {
      '2_estrutural_fisica': 'Sinal A [ancorado em: features.constitution.primary]. ...',
      ...
    }
    const result = runAudit(report)
    expect(result.low_anchor_rate).toBe(false)
    expect(result.anchor_rate_pct).toBe(100)
  })
```

**Adapt for new file:** Test 3 new vocab sets. Include planted positive/negative fixtures for each:
- Iridological jargon: `"constituição linfática"` → MUST trip; `"linfático sistêmico"` → MUST NOT trip
- Sopro vocab: `"centelha divina"` → MUST trip; `"centro de gravidade"` → MUST NOT trip
- Diagnóstico (existing): reuse same neg-context lookback tests

Word-boundary parity (Pitfall 7 / W6 — audit.ts top comment): assert compound words containing forbidden term substring DO NOT match (e.g., `"naturocultura"`, `"curadoria"` must NOT match `cura`).

---

#### `apps/web/lib/anthropic/__tests__/stream-parser-v2.test.ts`

**Analog:** `apps/web/lib/anthropic/__tests__/parser.test.ts`

Same vitest unit pattern (input buffer → expected boundary matches). Test cases:
- Empty buffer → no completed keys
- Buffer with first key complete → 1 returned
- Buffer with all 8 keys → 8 returned
- Out-of-order detection (defensive — return only newly completed keys in monotonic order)
- Brace-balancing in nested objects (e.g., `systems_with_tendency` array of objects must close all braces before key is counted complete)
- Cross-call state isolation (no leaked `lastIndex`)

---

#### `apps/web/components/readings/__tests__/ReportAdaptiveView.test.tsx`

**Analog:** `apps/web/components/readings/__tests__/EditorAccordion.test.tsx`

**RTL render + assertions pattern** (lines 1-26):
```tsx
/**
 * @vitest-environment jsdom
 */
import { describe, it, expect, vi } from 'vitest'
import { render, screen } from '@testing-library/react'
import { EditorAccordion } from '../EditorAccordion'

describe('components/readings/EditorAccordion (D-U1 + UI-SPEC §Surface 2)', () => {
  it('renderiza 13 sections + 14ª encerramento read-only', () => {
    const generated = {
      '1_constituicao': 'Texto 1',
      ...
    }
    render(<EditorAccordion reportGenerated={generated} reportDelivered={generated} onSectionChange={vi.fn()} />)
    expect(screen.getByText(/1\. Constituição/)).toBeDefined()
    expect(screen.getByText(/13\. Mensagem Final/)).toBeDefined()
  })
```

**Adapt for new file:** Test cases per UI-SPEC validation needs:
- Renders all 8 top-level Card blocks for full valid `report_v2` fixture
- Omits `bilateral_findings` Card when `asymmetry_present === false`
- Omits `integrative_axes` Card entirely when array empty
- Sorts `systems_with_tendency` by `tendency_grade` descending
- Renders empty state copy when `systems_with_tendency.length === 0`
- Renders `AdvancedAnalysisCTA` at footer

Use fixtures `apps/web/lib/anthropic/__tests__/fixtures/report-v2-valid.json` + `report-v2-empty-systems.json` per RESEARCH.md line 1008.

---

### NO ANALOG FOUND

Files with no close match in the codebase (planner should improvise from primitives or RESEARCH.md examples):

| File | Role | Reason |
|------|------|--------|
| `apps/web/components/readings/GradeBar.tsx` | 5-segment visual bar | No existing visual-bar pattern in codebase; UI-SPEC §Color lines 144-157 supplies exact JSX recipe |
| `apps/web/components/readings/PriorityFocusList.tsx` | numbered 1/2/3 list | Trivial wrapper; UI-SPEC §Layout line 401-406 supplies copy |
| `apps/web/app/__tests__/manifest.test.ts` | manifest assertion test | New test directory; trivial — `import manifest from '../manifest'; expect(manifest().name).toBe('Iris Codex')` |

---

## Shared Patterns (apply across multiple new files)

### `'use server'` on Server Actions, `import 'server-only'` on libs

**Source:** `apps/web/app/actions/analise.ts` line 1, `apps/web/lib/anthropic/audit.ts` line 36

**Apply to:**
- All new server actions in `analise.ts` (`saveReportV2Delivered`, etc.) — top-of-file `'use server'`
- All new files in `apps/web/lib/anthropic/` (`analyze-v2.ts`, `report-schema.ts`, `stream-parser-v2.ts`, `audit-v2.ts`, `diff-v2.ts`) — `import 'server-only'` at top
- `apps/web/lib/tendency-engine/placeholder.ts` — `import 'server-only'` (matches lib/anthropic convention)

```ts
import 'server-only'
```

MEMORY.md note: `'use server'` files only export async functions — types/consts go in sibling files.

### Anthropic client + cache_control pattern

**Source:** `apps/web/lib/anthropic/client.ts` lines 52, `apps/web/lib/anthropic/analyze.ts` lines 124-136

**Apply to:** `analyze-v2.ts` — reuse `anthropicClient`, `MODEL`, `DEFAULT_SYSTEM_CACHE_CONTROL`, `MAX_OUTPUT_TOKENS`, `estimateCostUsd` directly. Do NOT duplicate the SDK init.

```ts
import { anthropicClient, MODEL, DEFAULT_SYSTEM_CACHE_CONTROL, MAX_OUTPUT_TOKENS, estimateCostUsd } from './client'

const stream = anthropicClient.messages.stream({
  model: MODEL,
  max_tokens: MAX_OUTPUT_TOKENS,
  system: [{ type: 'text', text: systemPrompt, cache_control: DEFAULT_SYSTEM_CACHE_CONTROL }],
  messages: [{ role: 'user', content: userPrompt }],
  output_config: { format: zodOutputFormat(reportV2Schema) }, // NEW in V2
})
```

### Auth gate (5-step pattern from route handler)

**Source:** `apps/web/app/api/readings/[id]/analyze/route.ts` lines 53-107

**Apply to:** V2 branch in same file. Gates a-e unchanged: session → ownership → status='ready' → not yet delivered → regen cap < 3.

### Server Action auth + zod + audit + revalidate triad

**Source:** `apps/web/app/actions/analise.ts` lines 45-105

**Apply to:** `saveReportV2Delivered` + `markReadingV2Delivered`. 5-step body:
1. `createClient()` + `auth.getUser()` + redirect on fail
2. zod safeParse on inputs
3. `extractForbiddenHits` defense-in-depth (or `runAuditV2` for V2)
4. Atomic `supabase.from('readings').update(...)`
5. `revalidatePath` (3 paths: `/leituras/${id}`, `/leituras/${id}/editar`, `/leituras`)

### Structured telemetry (no PII)

**Source:** `apps/web/lib/anthropic/analyze.ts` lines 170-182

**Apply to:** All new server-side logs (`analyze-v2.ts`, retry path, audit hits). Pattern:
```ts
console.info({
  event: 'iris_codex_report_generate',
  reading_id, therapist_id, model_version,
  /* numeric metrics + booleans only — NEVER clientName, NEVER report text */
})
```

D-TEL2 (CONTEXT lines 250-263) supplies the exact field list.

### Markdown rendering with prose styling

**Source:** `apps/web/components/readings/EditorAccordion.tsx` lines 94-99, `EditorSectionItem.tsx` lines 56-60

**Apply to:** All new view/edit components rendering `clinical_description`, `therapeutic_direction`, `therapeutic_synthesis`, `clinical_note`, `constitutional_pattern.description`, `bilateral_findings.description`. Pattern:
```tsx
<div className="prose prose-sm prose-neutral max-w-none rounded-md border bg-muted/30 p-3">
  <ReactMarkdown remarkPlugins={[remarkGfm]}>{value}</ReactMarkdown>
</div>
```

### shadcn Alert destructive banner (non-dismissible)

**Source:** `apps/web/components/readings/EditorAuditBanner.tsx` lines 32-51

**Apply to:** `VocabularyAuditBanner.tsx`. State-driven only; AlertTriangle icon + AlertTitle + AlertDescription. UI-SPEC line 175 forbids user-dismiss affordance.

### Dialog with `initialFocus` ref

**Source:** `apps/web/components/readings/DeliverDialog.tsx` lines 29-58

**Apply to:** `AdvancedAnalysisCTA.tsx` modal. `initialFocus={okRef}` for informational dismissal (single OK action). Note: base-ui Dialog uses `initialFocus` (RefObject), NOT Radix `onOpenAutoFocus` callback.

### Audit-vocabulary allowlist marker for prompt files + test files mentioning forbidden vocab

**Source:** Top of `apps/web/lib/anthropic/audit.ts` lines 1-10, `__tests__/audit.test.ts` lines 1-10

**Apply to:**
- Rewritten `apps/web/prompts/system.md` — must start with `<!-- audit-vocabulary:allowlist -->` so the prompt can instruct LLM to AVOID forbidden vocab without tripping CI.
- New `audit-v2.ts` — top-of-file `audit-vocabulary:allowlist` comment block + array-join assembly of forbidden terms (defense matches existing audit.ts pattern).
- New `audit-v2.test.ts` — top-of-file allowlist marker so it can include planted positive fixtures.

### vitest `__tests__/` co-location

**Source:** `apps/web/lib/anthropic/__tests__/`, `apps/web/components/readings/__tests__/`

**Apply to:** All new test files. Co-located in `__tests__/` subdirectory next to the source file. Test files named `{source}.test.{ts,tsx}`. Use `@vitest-environment jsdom` JSDoc directive on component tests (see `EditorAccordion.test.tsx` line 2).

---

## Metadata

**Analog search scope:**
- `D:\Projetos\Iridologista\apps\web\lib\anthropic\**`
- `D:\Projetos\Iridologista\apps\web\components\readings\**`
- `D:\Projetos\Iridologista\apps\web\app\(dashboard)\leituras\**`
- `D:\Projetos\Iridologista\apps\web\app\actions\**`
- `D:\Projetos\Iridologista\apps\web\app\api\readings\**`
- `D:\Projetos\Iridologista\supabase\migrations\**`
- `D:\Projetos\Iridologista\apps\web\scripts\**`

**Files scanned:** 35
**Files Read in full:** 14 (analyze.ts, audit.ts, diff.ts, prompts.ts, client.ts, parser.ts, EditorAccordion.tsx, EditorSectionItem.tsx, DeliverDialog.tsx, EditorAuditBanner.tsx, AnalysisHero.tsx, AnalysisStream.tsx, AnalysisCTA.tsx, analise.ts, route.ts, page.tsx, editar/page.tsx, editar-client.tsx, manifest.ts, badge.tsx, 0007.sql, analise.schemas.ts, build-queries.ts, audit.test.ts excerpt, AnalysisStream.test.tsx, EditorAccordion.test.tsx excerpt, diff.test.ts excerpt, analise-client.tsx)

**Pattern extraction date:** 2026-05-13
