---
phase: 07.4
plan: 23
type: execute
wave: 13
depends_on: [18, 19, 21, 22]
supersedes: [19]
files_modified:
  - apps/web/package.json + pnpm-lock.yaml (install @react-pdf/renderer 4.5.1)
  - apps/web/components/readings/ExportPdfButton.tsx (Link → Button + onClick fetch)
  - apps/web/components/readings/__tests__/ExportPdfButton.test.tsx (3→4 tests, blob download mock)
  - apps/web/components/readings/__tests__/ReadingModeActions.test.tsx (Plan 19 link assertion → Plan 23 button assertion)
files_created:
  - apps/web/lib/pdf/report-document.tsx
  - apps/web/app/api/readings/[id]/pdf/route.tsx
  - .planning/phases/07.4-iris-codex-report/07.4-23-SUMMARY.md
files_deleted:
  - apps/web/app/(dashboard)/leituras/[id]/print/page.tsx
  - apps/web/app/(dashboard)/leituras/[id]/print/print-trigger.tsx
autonomous: true
requirements: []
source_decisions: [DC-1]
ux_feedback_source: "UAT-4 founder fix #3: 'Exportar PDF triggers direct file download (not browser print dialog).' Founder explicitly approved @react-pdf/renderer install during UAT-4 dialog (vs deferred-3 deny rule on prior install attempt during Plan 19 routing)."

must_haves:
  truths:
    - "Install @react-pdf/renderer 4.5.1 (peer deps include React 19; pulls pdfkit + fontkit transitively)"
    - "NEW lib/pdf/report-document.tsx — Document → Page A4 with Times-Roman serif; first-page header (brand mark + tagline + client name + date); fixed page header on pages 2+ (small brand + client right); fixed page footer (encerramento blockquote italic muted with top border) on every page; 16 sections in NUMBERED_SECTION_HEADINGS order with `N. Title` headings; §16 Síntese Rápida special render — parseSinteseSubsections splits body into 6 cards rendered as 50/50 width grid"
    - "Lightweight markdown parser (renderMarkdownBlocks) — paragraph + bullet (- ) + numbered (N. ) + blockquote (> ); inline formatting (bold/italic/code) stripped via stripInlineFormatting"
    - "STRIP_LEADING_HEADING_RE applied per section body (mirrors EditorSectionItem + ReportReadView)"
    - "buildPdfFilename(clientName, readingDate) helper — strips accents, replaces non-word chars, joins as `Leitura-{Name}-{YYYY-MM-DD}.pdf`"
    - "NEW app/api/readings/[id]/pdf/route.tsx — GET handler, runtime nodejs, maxDuration 60s; createClient session-bound RLS via .maybeSingle()→404; status check (ready|edited && hasReport) returns 409 if not ready; reportToShow = reportDelivered ?? reportGenerated; renderToBuffer + Response with Content-Type: application/pdf + Content-Disposition: attachment; filename + Cache-Control: no-store; try/catch with structured console.error + 500 JSON on render fail"
    - "Route file is .tsx (not .ts) because it uses inline JSX <ReportDocument...>"
    - "ExportPdfButton.tsx refactored Link → Button: useTransition + setLocalPending isolated state; onClick fetch GET /api/readings/[id]/pdf; parseFilenameFromHeader from Content-Disposition; URL.createObjectURL + anchor.download click + revoke after 500ms; toast.success/error; Loader2 spinning + 'Gerando PDF…' label while pending"
    - "ExportPdfButton.test.tsx: 3 → 4 tests; mocks fetch + URL.createObjectURL; covers happy path + loading state + HTTP error path"
    - "ReadingModeActions.test.tsx: Plan 19 link assertion modernized — asserts tagName='button' + no href + 'Exportar PDF' label"
    - "DELETE /leituras/[id]/print/page.tsx + print-trigger.tsx (no other consumers; ExportPdfButton was the only link to /print)"
    - "Verification: pnpm build exit 0; pnpm tsc --noEmit 22 baseline (zero new — .next/types regenerates after build); pnpm lint clean; pnpm test:run 621 passed + 3 baseline failures (quality-scoring × 3 only)"
  artifacts:
    - path: ".planning/phases/07.4-iris-codex-report/07.4-23-SUMMARY.md"
      provides: "Plan 23 execution record"
---

<objective>
Replace Plan 19 Print CSS PDF approach with direct file download via
server-side rendered PDF using @react-pdf/renderer (founder approved
install during UAT-4). Click "Exportar PDF" → blob download → user gets
a PDF file straight to disk, no browser print dialog.

Scope discipline: NO new prompt iteration (Plans 21+22 own prompt + UI).
NO §11 toggle work. NO V1.1 technical-tab build.
</objective>

(See commits 91cf1e5 + 25cecb9 + this docs commit for the full
implementation. SUMMARY captures the same content.)
