---
phase: 07.4
plan: 12
subsystem: ui
tags: [14-section, accordion, stream, warm-voice, direction-correction]
one_liner: "14-section UI surface — AnalysisStream counter 13→14 + §14 Mensagem para o Cliente warm-voice visual distinction (amber tint + 'Voz do terapeuta' caption per DC-6)."
dependency_graph:
  requires:
    - "07.4-11 (types.ts SECTION_KEY_BY_NUMBER + parser range 1..14 + EditorAccordion 14-key SECTIONS array + system.md 14-section markdown prompt)"
  provides:
    - "AnalysisStream counts {n}/14 seções with 14 Iris Codex V1 titles"
    - "EditorAccordion §14 has bg-amber-50/30 + 'Voz do terapeuta · Para entrega ao cliente' caption + data-section-tone='warm'"
    - "analise-client docstring aligned to 14-section reality (no code change — regex was already generic \\d{1,2})"
  affects:
    - "Plan 13 founder UAT — entire 14-section pipeline now visually coherent end-to-end"
tech_stack:
  added: []
  patterns:
    - "data-section-tone='warm'|'clinical' selector hook on AccordionItem — copy-immune assertion target (same data-testid discipline as Plan 07/07b sub-editors)"
    - "Subtle styling cue per founder direction: amber-50/30 + xs caption above trigger label, not a redesign — founder iterates visually after UAT"
key_files:
  created: []
  modified:
    - "apps/web/components/readings/AnalysisStream.tsx — clamp 13→14, counter copy /13→/14, SECTION_TITLES replaced with 14 Iris Codex V1 titles, docstring + Phase header updated"
    - "apps/web/components/readings/__tests__/AnalysisStream.test.tsx — 6 /13 assertions migrated to /14 + new title fixture (Constituição e Temperamento · Mensagem para o Cliente); 7/7 GREEN"
    - "apps/web/components/readings/EditorAccordion.tsx — §14 AccordionItem now carries data-section-tone='warm' + className='bg-amber-50/30' + warm-tone caption span above title; §1..§13 carry data-section-tone='clinical' for sanity selector"
    - "apps/web/components/readings/__tests__/EditorAccordion.test.tsx — new case '§14 Mensagem para o Cliente tem distinção visual warm-voice (DC-6)' asserts caption + warm class + clinical-tone non-presence; 5/5 GREEN"
    - "apps/web/app/(dashboard)/leituras/[id]/analise-client.tsx — docstring 13→14 (Plan 11/12 lineage). BOUNDARY_RE /^### \\d{1,2}\\.\\s+/gm is already generic, no code change needed"
decisions:
  - "DC-6 §14 visual distinction shipped as subtle styling cue (amber-50/30 + 'Voz do terapeuta · Para entrega ao cliente' caption) — NOT a redesign. Founder will iterate visually after Plan 13 UAT."
  - "DC-5 §11 Afirmações optional toggle DEFERRED to a post-UAT plan (referenced as 'Plan 14' but not yet scheduled). Plan 11's compromise: Afirmações lives outside §1..§14. Plan 13 UAT will determine if toggle is critical or polish."
  - "analise-client.tsx required zero code change — BOUNDARY_RE /^### \\d{1,2}\\.\\s+/gm already accepts §1..§14 (and §15+). AnalysisStream clamps the displayed count. Server parser is the strict source of truth. Docstring updated to reflect Plan 11/12 lineage."
  - "Selector strategy: data-section-tone='warm'|'clinical' instead of probing className for amber-50/30. Copy-immune per the data-testid convention used across Plan 07/07b sub-editors. className check is kept as a secondary assertion since the styling itself is part of the DC-6 contract."
metrics:
  duration_minutes: 18
  commits: 3
  files_modified: 5
  files_created: 0
  tests_added: 1
  completed_date: "2026-05-14"
---

# Phase 07.4 Plan 12: 14-Section UI Surface Summary

## Overview

Finished the UI side of the 14-section pipeline started in Plan 07.4-11 (LLM core).
Three atomic source commits (~18 min wall-clock; sequential mode on main):

- `ba6cdd5` Task 1 — `AnalysisStream.tsx` clamp 13→14, counter copy `/13 seções`→`/14 seções`, `SECTION_TITLES` replaced with the 14 Iris Codex V1 titles (Constituição e Temperamento … Mensagem para o Cliente); test file's 6 `/13` assertions + title fixture migrated to `/14`; 7/7 GREEN.
- `22fcc04` Task 2 — `analise-client.tsx` docstring only (Plan 11/12 lineage). Boundary regex `/^### \d{1,2}\.\s+/gm` was already generic and accepts §1..§14 without modification; AnalysisStream clamps the displayed count to 14. No code change needed.
- `64833ea` Task 3 — `EditorAccordion.tsx` §14 distinction per DC-6: AccordionItem carries `data-section-tone="warm"` + `className="bg-amber-50/30"`; AccordionTrigger label gains a small `text-xs text-muted-foreground` caption "Voz do terapeuta · Para entrega ao cliente" above the existing `{number}. {title}` line. §1..§13 carry `data-section-tone="clinical"` as the sanity selector for the new test case. 5/5 EditorAccordion tests GREEN.

## What Shipped

### Truths (per plan must_haves)

- AnalysisStream renders `Gerando relatório… {n}/14 seções` (was 13).
- AnalysisStream Skeleton stack shows 14 placeholder rows (was 13).
- AnalysisStream aria-live announcer says N/14.
- `analise-client.tsx` stream-buffer detector accepts §1..§14 boundaries — verified by inspection (regex already generic).
- EditorAccordion renders all 14 sections with the new Iris Codex V1 titles (Plan 11 had already updated keys + titles; Plan 12 verified visual integrity + added §14 distinction).
- §14 Mensagem para o Cliente has a distinct visual treatment (amber-50/30 background + "Voz do terapeuta · Para entrega ao cliente" caption) signaling the warm-voice client-delivered closer per DC-6.
- Tests updated to reflect 14-section counts + titles.
- `pnpm build` + `pnpm tsc --noEmit` + `pnpm lint` + `pnpm test:run` all green within baseline.

### Test Counts

- `pnpm test:run` (full suite): **570 passed**, 3 failed, 2 skipped, 24 todo across 53 files.
- The 3 failures are the documented baseline `quality-scoring.test.ts` failures per MEMORY `feedback_quality_scoring_test_gate.md` (Phase 3 debt, not gating). Plan 11's `prompts.test` fix is preserved — previous baseline of 4 failures is now 3.
- New test added: `EditorAccordion.test.tsx > §14 Mensagem para o Cliente tem distinção visual warm-voice (DC-6)`.

## Deviations from Plan

**None — plan executed exactly as written.**

Two judgement calls worth recording (NOT deviations — within plan latitude):

1. **Task 2 was a docstring-only change.** Plan's `<action>` block read "if file has hardcoded 13 limit". The `BOUNDARY_RE = /^### \d{1,2}\.\s+/gm` regex was already generic (accepts §1..§14 and beyond), and `sectionsReceived` was already passed unclamped to `AnalysisStream` which clamps internally. The only `13` reference was a stale docstring line that referenced "currently 13, Plan 11 extends parser to 14" — updated to reflect Plan 11/12 lineage. Commit type kept as `docs(...)` since no behavior changed.

2. **Task 3 selector strategy: data-attribute + className.** The plan asked for "the warm-tone background class" and "the 'Voz do terapeuta' caption" — the test could have asserted just the caption text. I added a `data-section-tone="warm"|"clinical"` attribute as the primary selector (copy-immune) and kept the className substring check as the secondary assertion since the amber styling itself is part of the DC-6 contract. Matches the data-testid discipline used across Plan 07/07b sub-editors.

## Deferred Items

- **DC-5 §11 Afirmações optional toggle** — deferred to a post-UAT plan (Plan 14 if Plan 13 UAT says toggle is critical; else stays static). Plan 11's compromise: Afirmações lives OUTSIDE the §1..§14 numbered sequence. If Plan 13 founder UAT verdict triggers it, Plan 14 adds the toggle (single Sonnet call + parser tolerance OR second LLM call into a separate jsonb key). Documented here so it's not lost.

## Auth Gates

None. All work was local to the codebase (no external API calls, no env config changes).

## Self-Check: PASSED

**Files created/modified — verified existing on disk:**

- `apps/web/components/readings/AnalysisStream.tsx` — FOUND
- `apps/web/components/readings/__tests__/AnalysisStream.test.tsx` — FOUND
- `apps/web/components/readings/EditorAccordion.tsx` — FOUND
- `apps/web/components/readings/__tests__/EditorAccordion.test.tsx` — FOUND
- `apps/web/app/(dashboard)/leituras/[id]/analise-client.tsx` — FOUND

**Commits — verified present in git log:**

- `ba6cdd5` feat(07.4-12): AnalysisStream renders 14 sections (was 13) — FOUND
- `22fcc04` docs(07.4-12): analise-client docstring 13->14 sections — FOUND
- `64833ea` feat(07.4-12): EditorAccordion §14 warm-voice visual distinction (DC-6) — FOUND

**Verification gates:**

- `pnpm build` → exit 0
- `pnpm tsc --noEmit` → zero NEW errors (4 pre-existing D-DEF-09-03 baseline files unchanged per MEMORY)
- `pnpm lint` → 0 errors + 9 baseline warnings unchanged
- `pnpm test:run` → 570 passed + 3 baseline failures (quality-scoring×3 only, per MEMORY Phase 3 debt — gating preserved)
- `grep -c "/14 seções" apps/web/components/readings/AnalysisStream.tsx` → 2 (header + test-facing string)
- `grep -c "/13 seções\|0\.\.13\|, 13)" apps/web/components/readings/AnalysisStream.tsx` → 0

## Next

Plan 13 (founder UAT) — regenerate a real reading end-to-end via the new 14-section pipeline; verify §14 brand voice, all 14 sections have depth, no jargon leakage, no `[ancorado em features.x]` markers. If UAT pass → `/gsd-verify-phase 07.4` to close. If §11 toggle needed → add as Plan 14 follow-up.
