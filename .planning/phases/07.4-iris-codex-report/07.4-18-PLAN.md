---
phase: 07.4
plan: 18
type: execute
wave: 12
depends_on: [16, 17]
supersedes: []
files_modified:
  - apps/web/app/(dashboard)/leituras/[id]/page.tsx
files_created:
  - apps/web/components/readings/ReportReadView.tsx
  - apps/web/components/readings/ReadingModeActions.tsx
  - apps/web/components/readings/__tests__/ReportReadView.test.tsx
  - .planning/phases/07.4-iris-codex-report/07.4-18-SUMMARY.md
autonomous: true
requirements: []
source_decisions: [DC-1, DC-3, DC-6]
ux_feedback_source: "UAT-3 founder verdict 2026-05-14: accordion is for granular edits not reading; default /leituras/[id] view should be a continuous flowing professionally formatted document (serif, max-width, comfortable line height) with top action buttons (Exportar PDF — Plan 19, Editar análise → /editar, Entregar ao cliente). Accordion stays on /editar only."

must_haves:
  truths:
    # ReportReadView component
    - "NEW apps/web/components/readings/ReportReadView.tsx — RSC by default (no 'use client'); takes sections (Record<string, string>), clientName, readingDate (ISO string), optional topActionsSlot ReactNode; renders header (client name + reading date) + topActionsSlot row + 15 sections as continuous flowing prose + footer (encerramento_disclaimer)"
    - "Section iteration uses NUMBERED_SECTION_HEADINGS order (single source of truth from Plan 17); reads sections[SECTION_KEY_BY_NUMBER[headingStr]]; skips empty/missing sections defensively (safeArray-style — sections may have fewer than 15 keys mid-stream or for legacy 1.0 readings)"
    - "Section heading styled `§{N} — {Title}` as h2 element: text-2xl font-bold mt-12 mb-4; first heading mt-8 (less top space)"
    - "Body prose: font-serif (Tailwind font-serif utility — uses Georgia/serif system stack), text-base, leading-relaxed (1.7), max-w-prose (~65ch); rendered via ReactMarkdown + remarkGfm"
    - "STRIP_LEADING_HEADING_RE applied to each section body so the rendered body doesn't duplicate the title (mirrors EditorSectionItem pattern)"
    - "Footer: encerramento_disclaimer rendered as muted italic blockquote; if section absent, omit footer"

    # ReadingModeActions component
    - "NEW apps/web/components/readings/ReadingModeActions.tsx — 'use client' (manages DeliverDialog open state + regen flow); takes readingId, regenerationCount, isDelivered, hasReport"
    - "Renders right-aligned button group: Exportar PDF (placeholder — Plan 19 wires; for now button is disabled with title='Disponível em breve' OR omitted entirely — choose omit for cleanliness); Editar análise (Link to /editar); Entregar ao cliente (opens DeliverDialog); Regenerar análise (gated by regenerationCount + isDelivered, calls existing /api/readings/[id]/analyze flow)"
    - "DECISION: omit Exportar PDF button entirely in Plan 18 — Plan 19 adds it. Less misleading than a disabled button"
    - "isDelivered=true: hide Editar análise + Regenerar buttons; only show Entregar status (or nothing); show only Exportar PDF when Plan 19 lands"
    - "Reuses existing DeliverDialog component + saveReportDelivered/markReadingDelivered server actions"
    - "Regenerar reuses existing logic from AnalysisCTA — fetch POST /api/readings/[id]/analyze with router.refresh on done"

    # page.tsx refactor
    - "When status='ready'|'edited' AND hasReport: render new top-level layout (NOT inside AnalysisHero card) — header + ReadingModeActions + ReportReadView"
    - "When NOT (ready/edited && hasReport): preserve existing State A/B path (AnalysisHero with AnaliseClient children for streaming)"
    - "AnalysisHero used only for State A (empty/CTA) and State B (streaming) — no longer wraps State C content"
    - "Layout container: keep `space-y-6 px-6 py-8`; back link preserved at top"
    - "Heading: client name + reading date moved into ReportReadView header (no duplication with the page-level h1)"

    # Tests
    - "ReportReadView.test.tsx: 6+ tests covering all 15 sections render in order including §2.5, header shows client name + reading date, footer shows encerramento_disclaimer, topActionsSlot renders when provided, empty sections are skipped, sections rendered as flowing text (no accordion role)"
    - "ReadingModeActions.test.tsx (NEW or extend existing): button presence (Editar análise + Entregar ao cliente + Regenerar — NO Exportar PDF in Plan 18); isDelivered hides edit/regen; regen disabled at 3/3; DeliverDialog opens on click"

    # Verification
    - "pnpm build exits 0"
    - "pnpm tsc --noEmit zero new errors (22 baseline)"
    - "pnpm lint clean"
    - "pnpm test:run: ~600+ passed + 3 baseline failures"
    - "Manual smoke: /leituras/[some-id] with report shows continuous flowing serif text, top buttons work, /editar still renders accordion"
  artifacts:
    - path: ".planning/phases/07.4-iris-codex-report/07.4-18-SUMMARY.md"
      provides: "Plan 18 execution record"
---

<objective>
Replace the accordion-as-default reading view with a continuous flowing professional document. /leituras/[id] becomes a reading surface; accordion moves exclusively to /editar.

Scope discipline: NO PDF export (Plan 19). NO new prompt iteration. NO §2.5 backend changes (Plan 17 owns those). NO touching /editar route — it stays as-is.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.4-iris-codex-report/07.4-CONTEXT.md
@.planning/phases/07.4-iris-codex-report/07.4-17-SUMMARY.md
@apps/web/app/(dashboard)/leituras/[id]/page.tsx
@apps/web/app/(dashboard)/leituras/[id]/analise-client.tsx
@apps/web/components/readings/AnalysisHero.tsx
@apps/web/components/readings/AnalysisCTA.tsx
@apps/web/components/readings/DeliverDialog.tsx
@apps/web/components/readings/EditorAccordion.tsx
@apps/web/lib/anthropic/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReportReadView component (RSC, continuous flowing serif text)</name>
  <action>
Create apps/web/components/readings/ReportReadView.tsx (RSC). Composition:
- Header div: client name (h1 text-3xl font-serif font-bold) + reading date (LocalDateTime, text-sm text-muted-foreground mt-1)
- Optional topActionsSlot row (right-aligned, flex justify-end gap-2 my-6)
- Main body container: `max-w-prose mx-auto`. Iterate NUMBERED_SECTION_HEADINGS:
  - For each headingStr, get key = SECTION_KEY_BY_NUMBER[headingStr], content = sections[key]
  - Skip if content is empty/falsy
  - Strip leading heading line via shared regex (same as EditorSectionItem)
  - Render: <section> with h2 `§{headingStr} — {title}` (text-2xl font-bold mt-12 mb-4 font-serif; first one mt-8) + ReactMarkdown body inside `prose prose-neutral font-serif leading-relaxed`
- Footer: if sections.encerramento_disclaimer present, render as italic muted blockquote (border-l-4 border-muted pl-4 my-8 text-muted-foreground)

Use lib/anthropic/types.ts for NUMBERED_SECTION_HEADINGS + SECTION_KEY_BY_NUMBER. Need a section-titles map (15 entries — same data as AnalysisStream + EditorAccordion). DRY: extract a shared SECTION_TITLE_BY_NUMBER map to lib/anthropic/types.ts? OR keep inline (3rd duplicate). Decision: extract to types.ts as SECTION_TITLE_BY_NUMBER for true single source of truth.

Tests in apps/web/components/readings/__tests__/ReportReadView.test.tsx:
- renders header with client name + reading date
- renders 15 sections in order including §2.5 between §2 and §3
- skips empty sections (e.g., when only 5 keys present)
- strips leading `## §N — Title` heading line from rendered body
- renders encerramento_disclaimer footer when present
- omits encerramento footer when absent
- topActionsSlot renders above main body when provided
  </action>
</task>

<task type="auto">
  <name>Task 2: ReadingModeActions client component (Editar + Entregar + Regenerar)</name>
  <action>
Create apps/web/components/readings/ReadingModeActions.tsx ('use client'). Composition:
- useState for DeliverDialog open
- useTransition for regenerate flow + delivery flow
- useRouter for refresh after actions
- toast (sonner) for action feedback

Buttons (right-aligned, flex gap-2):
- Editar análise (Link to `/leituras/{readingId}/editar`, button outline variant) — hidden when isDelivered=true
- Entregar ao cliente (Button default, opens DeliverDialog) — hidden when isDelivered=true
- Regenerar análise ({n}/3) (Button outline, gated by regenerationCount<3 + !isDelivered, fetch POST /api/readings/[id]/analyze) — hidden when isDelivered=true; tooltip when 3/3
- Status indicator when isDelivered=true: small text "Entregue ao cliente em {deliveredAt}"
- NO Exportar PDF button (Plan 19 adds it)

DeliverDialog mounted at component end with open state controlled.

Reuses existing markReadingDelivered server action from app/actions/analise.ts.

Tests in apps/web/components/readings/__tests__/ReadingModeActions.test.tsx:
- renders 3 buttons when not delivered
- isDelivered=true hides edit/regen, shows status
- regen button gated at 3/3 with tooltip
- click Entregar opens DeliverDialog
- mocked fetch handles regenerate path (success → toast + router.refresh)
  </action>
</task>

<task type="auto">
  <name>Task 3: Refactor page.tsx for reading-mode default + verify + commit + SUMMARY</name>
  <action>
Refactor apps/web/app/(dashboard)/leituras/[id]/page.tsx:
- Branch on `(status === 'ready' || status === 'edited') && hasReport`:
  - TRUE: render reading-mode layout — back link + ReportReadView with topActionsSlot=<ReadingModeActions...>
  - FALSE: render existing layout — back link + h1 + StatusBadge + AnalysisHero with AnaliseClient children
- Remove the page-level h1 "Análise da leitura" + "Cliente: ... Capturada em ..." block when in reading mode (ReportReadView header replaces it); keep them for non-reading-mode (consistency with existing flow)
- Compute reportToShow = reportDelivered ?? reportGenerated; pass to ReportReadView
- Preserve all existing state machine logic for State A/B

Verification:
- pnpm tsc --noEmit zero new errors
- pnpm lint clean
- pnpm test:run all relevant tests GREEN; baseline failures only
- pnpm build exit 0

Commits (atomic):
- Commit 1: feat(07.4-18): ReportReadView + SECTION_TITLE_BY_NUMBER (continuous flowing serif text)
- Commit 2: feat(07.4-18): ReadingModeActions + reading-mode routing flip in /leituras/[id]
- Commit 3: docs(07.4-18): SUMMARY + STATE — Plan 18 complete

Write 07.4-18-SUMMARY.md following Plan 14 template.
Update STATE.md status field; total_plans 92→93.
  </action>
</task>

</tasks>
