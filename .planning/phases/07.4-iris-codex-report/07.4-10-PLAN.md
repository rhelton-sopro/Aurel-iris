---
phase: 07.4
plan: 10
type: execute
wave: 8
depends_on: [09]
supersedes: [02, 03, 04, 06, 07, 07b]
files_modified:
  - apps/web/app/api/readings/[id]/analyze/route.ts
  - apps/web/app/(dashboard)/leituras/[id]/page.tsx
  - apps/web/app/(dashboard)/leituras/[id]/editar/page.tsx
  - apps/web/app/(dashboard)/leituras/[id]/analise-client.tsx
  - apps/web/app/actions/analise.ts
  - apps/web/app/actions/analise.schemas.ts
files_deleted:
  # 8-block view components (Plan 06)
  - apps/web/components/readings/ReportAdaptiveView.tsx
  - apps/web/components/readings/SystemTendencyCard.tsx
  - apps/web/components/readings/GradeBar.tsx
  - apps/web/components/readings/GradeBadge.tsx
  - apps/web/components/readings/KeyTraitChipGroup.tsx
  - apps/web/components/readings/IntegrativeAxisItem.tsx
  - apps/web/components/readings/BilateralFindingsCard.tsx
  - apps/web/components/readings/PriorityFocusList.tsx
  - apps/web/components/readings/ClinicalNote.tsx
  - apps/web/components/readings/__tests__/ReportAdaptiveView.test.tsx
  - apps/web/components/readings/__tests__/SystemTendencyCard.test.tsx
  - apps/web/components/readings/__tests__/GradeBar.test.tsx
  - apps/web/components/readings/__tests__/GradeBadge.test.tsx
  # 8-block editor components (Plans 07 + 07b)
  - apps/web/components/readings/ReportAdaptiveEditor.tsx
  - apps/web/components/readings/BlockEditPane.tsx
  - apps/web/components/readings/SystemTendencyCardEditor.tsx
  - apps/web/components/readings/IntegrativeAxesEditor.tsx
  - apps/web/components/readings/BilateralFindingsEditor.tsx
  - apps/web/components/readings/AdaptiveAnalysisStream.tsx
  - apps/web/components/readings/__tests__/ReportAdaptiveEditor.test.tsx
  - apps/web/components/readings/__tests__/SystemTendencyCardEditor.test.tsx
  - apps/web/components/readings/__tests__/IntegrativeAxesEditor.test.tsx
  - apps/web/components/readings/__tests__/BilateralFindingsEditor.test.tsx
  - apps/web/components/readings/__tests__/AdaptiveAnalysisStream.test.tsx
  # Tendency engine (Plan 04)
  - apps/web/lib/tendency-engine/placeholder.ts
  - apps/web/lib/tendency-engine/index.ts
  - apps/web/lib/tendency-engine/types.ts
  - apps/web/lib/tendency-engine/__tests__/placeholder.test.ts
  # 8-block LLM core libs (Plan 03)
  - apps/web/lib/anthropic/analyze-v2.ts
  - apps/web/lib/anthropic/stream-parser-v2.ts
  - apps/web/lib/anthropic/audit-v2.ts
  - apps/web/lib/anthropic/diff-v2.ts
  - apps/web/lib/anthropic/types-v2.ts
  - apps/web/lib/anthropic/__tests__/analyze-v2.test.ts
  - apps/web/lib/anthropic/__tests__/stream-parser-v2.test.ts
  - apps/web/lib/anthropic/__tests__/audit-v2.test.ts
  - apps/web/lib/anthropic/__tests__/diff-v2.test.ts
  # 8-block schemas (Plans 00 + 09)
  - apps/web/lib/anthropic/report-schema.ts
  - apps/web/lib/anthropic/report-schema-shared.ts
  - apps/web/lib/anthropic/__tests__/report-schema.test.ts
  - apps/web/lib/anthropic/__tests__/fixtures/report-v2-valid.json
  - apps/web/lib/anthropic/__tests__/fixtures/report-v2-empty-systems.json
  - apps/web/lib/anthropic/__tests__/fixtures/report-v2-invalid-missing-required.json
  # Wave 0 stub (Plan 00) — manifest.test was reused for rebrand assertions (KEEP), but no other Wave 0 stubs reference 14-section keys (DELETE)
autonomous: true
requirements: []
source_decisions: [DC-1, DC-2, DC-8, DC-9, DC-10]
must_haves:
  truths:
    - "All files in files_deleted are removed from disk + git index"
    - "apps/web/components/readings/AdvancedAnalysisCTA.tsx EXISTS (retained per DC-10)"
    - "apps/web/components/readings/VocabularyAuditBanner.tsx EXISTS (retained — runtime audit display surface)"
    - "apps/web/components/readings/EditorAccordion.tsx EXISTS untouched (legacy 1.0 renderer)"
    - "apps/web/components/readings/AnalysisStream.tsx EXISTS untouched (legacy 1.0 stream UI; Plan 12 will rebuild for 14 sections OR reuse)"
    - "apps/web/lib/anthropic/analyze.ts EXISTS untouched (Plan 11 will extend it to 14 sections)"
    - "apps/web/lib/anthropic/audit.ts EXISTS untouched (Plan 11 may extend)"
    - "apps/web/lib/anthropic/diff.ts EXISTS untouched"
    - "apps/web/lib/anthropic/parser.ts EXISTS untouched (Plan 11 extends findAllBoundaries to 14 headings)"
    - "apps/web/lib/anthropic/forbidden-terms.json EXISTS untouched (audit-vocabulary still uses it)"
    - "apps/web/scripts/audit-vocabulary.mjs EXISTS untouched"
    - "supabase/migrations/0013, 0014, 0015 EXIST untouched"
    - "route.ts no longer references analyzeReadingV2, detectCompletedKeys, ZodValidationFailedError, or any v2-only symbol"
    - "page.tsx, editar/page.tsx, analise-client.tsx no longer reference ReportAdaptiveView, ReportAdaptiveEditor, AdaptiveAnalysisStream, AdvancedAnalysisCTA-as-footerSlot"
    - "page.tsx + editar/page.tsx use the legacy AnalysisHero + EditorAccordion + EditarClient path for ALL readings until Plan 12 rebuilds the v2 surface"
    - "AdvancedAnalysisCTA is still imported somewhere (kept as standalone display-only component; Plan 12 will mount it in the new 14-section view)"
    - "analise.ts no longer exports saveReportV2Delivered or deliverReportV2 (those depended on report-schema; Plan 11 redefines them or keeps signatures that work for markdown)"
    - "pnpm build exits 0"
    - "pnpm tsc --noEmit reports zero new errors (pre-existing Phase 3/5 baseline preserved per MEMORY feedback_quality_scoring_test_gate.md)"
    - "pnpm lint reports 0 errors (9 baseline warnings ok)"
    - "pnpm test:run passes with the 4 baseline pre-existing failures only (quality-scoring×3 + prompts×1 — same as before)"
  artifacts:
    - path: ".planning/phases/07.4-iris-codex-report/07.4-10-SUMMARY.md"
      provides: "Plan 10 execution record"
  key_links:
    - from: "apps/web/app/api/readings/[id]/analyze/route.ts"
      to: "apps/web/lib/anthropic/analyze.ts"
      via: "import analyzeReading from './analyze'"
      pattern: "from '@/lib/anthropic/analyze'"
---

<objective>
Delete the 8-block adaptive-report code superseded by the Direction Correction (CONTEXT.md DC-1..DC-10), and surgically update the route handler + page consumers so the codebase compiles cleanly on the legacy markdown path only.

Output: clean tree with ~30 fewer files, `pnpm build` GREEN, no dangling references. Intermediate state — the app renders legacy 1.0 readings correctly but has NO v2 surface until Plans 11+12 rebuild it for 14 sections. This is acceptable because (a) no production traffic depends on v2 yet, (b) the lone test v2 row in DB is from the founder's rejected UAT.

Critical: do NOT touch `apps/web/prompts/system.md` in this plan. The prompt is currently the 8-block JSON prompt — that's wrong for the new direction, but rewriting it is Plan 11's job. Until Plan 11 lands, regenerating a legacy 1.0 reading will hit the D-DEF-09-05 problem (Sonnet emits JSON, legacy parser sees no markdown headings, empty report). That's the same state as pre-Plan-10 and is intentional. Plan 11 fixes both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.4-iris-codex-report/07.4-CONTEXT.md
@.planning/STATE.md
@.planning/phases/07.4-iris-codex-report/deferred-items.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update consumers to drop v2-only references</name>
  <files>
    apps/web/app/api/readings/[id]/analyze/route.ts (MODIFY)
    apps/web/app/(dashboard)/leituras/[id]/page.tsx (MODIFY)
    apps/web/app/(dashboard)/leituras/[id]/editar/page.tsx (MODIFY)
    apps/web/app/(dashboard)/leituras/[id]/analise-client.tsx (MODIFY)
    apps/web/app/actions/analise.ts (MODIFY)
    apps/web/app/actions/analise.schemas.ts (MODIFY)
  </files>
  <action>
**route.ts**: drop the entire v2 branch (lines ~118-280). Specifically:
- Remove the `if (reading.report_version === '2.0') { ... return ... }` block in its entirety
- Remove imports: `analyzeReadingV2`, `ZodValidationFailedError` from `@/lib/anthropic/analyze-v2`, `detectCompletedKeys` from `@/lib/anthropic/stream-parser-v2`, `mapVisionFeaturesToTendencies` from `@/lib/tendency-engine`, `ReportV2` from anywhere
- Keep the `report_v2` / `report_v2_delivered` columns in the SELECT statement (the column still exists in DB) but stop branching on them
- Gate (d) `alreadyDelivered` check: revert to `reading.report_delivered != null` (drop the V2 branch — it was: `reading.report_version === '2.0' ? reading.report_v2_delivered != null : reading.report_delivered != null`)
- Keep the entire legacy path that follows (from `// Phase 7 path` onward) UNCHANGED — that's what runs for everything now
- Drop the `report_version === '2.0'` from the SELECT (we still need report_v2 columns to render legacy view fallback, but the version flag is no longer load-bearing for routing — Plan 12 will reintroduce it if needed)

**page.tsx** (`apps/web/app/(dashboard)/leituras/[id]/page.tsx`): drop ReportAdaptiveView + AdvancedAnalysisCTA-as-footerSlot. Keep AnalysisHero + the existing legacy render block for everything. Specifically:
- Drop imports: `ReportAdaptiveView`, `AdvancedAnalysisCTA`, `ReportV2` type
- Drop the `{isV2 && hasReportV2 && <ReportAdaptiveView ... />}` block
- Drop the `isV2` / `hasReportV2` computed variables (no longer used)
- Drop `report_v2` / `report_v2_delivered` / `report_version` from the SELECT (not needed for legacy path)
- Drop `reportVersion` prop passed to `AnaliseClient` (legacy AnaliseClient doesn't need it)

**editar/page.tsx**: drop the ReportAdaptiveEditor early-return. Specifically:
- Drop imports: `ReportAdaptiveEditor`, `ReportV2` type, audit type imports
- Drop the `{isV2 && reportV2 && <ReportAdaptiveEditor ... />}` early-return block
- Drop `isV2` / `reportV2` computed variables
- Drop `report_v2*` columns from the SELECT
- Fall through to the existing legacy `EditarClient` render path for everything

**analise-client.tsx**: drop AdaptiveAnalysisStream + V2_KEYS_ORDERED + detectBlocksReceived. Specifically:
- Drop imports: `AdaptiveAnalysisStream`, any v2 detector logic
- Drop the `reportVersion?: '1.0' | '2.0'` prop (parent stops passing it per page.tsx change above)
- Drop `V2_KEYS_ORDERED` inlined constant + `detectBlocksReceived` function
- Drop the version-branching `streaming ? (isV2 ? <AdaptiveAnalysisStream/> : <AnalysisStream/>) : <AnalysisCTA/>` — replace with `streaming ? <AnalysisStream/> : <AnalysisCTA/>` (legacy only)
- Drop the version-aware toast copy switching

**analise.ts** (server actions): drop `saveReportV2Delivered` + `deliverReportV2`. Specifically:
- Delete both exported async function declarations entirely (and any module-private helpers used only by them: `safeArray()` local helper if no other action uses it)
- Keep legacy `saveReportDelivered` + `markReadingDelivered` UNTOUCHED
- Drop imports: `reportV2DeliveredSchema`, `ReportV2DeliveredInput` from `./analise.schemas`, `runAuditV2` from `@/lib/anthropic/audit-v2`, `classifyAllSystemsV2` from `@/lib/anthropic/diff-v2`, any `ReportV2` type imports

**analise.schemas.ts**: drop `reportV2DeliveredSchema` + `ReportV2DeliveredInput` type export. Keep all legacy schemas (`saveReportDeliveredSchema`, `markReadingDeliveredSchema`).
  </action>
  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit 2>&1 | grep -E "report-schema|tendency-engine|analyze-v2|stream-parser-v2|audit-v2|diff-v2|types-v2|ReportAdaptiveView|ReportAdaptiveEditor|AdaptiveAnalysisStream|SystemTendencyCard|GradeBar|GradeBadge|KeyTraitChipGroup|IntegrativeAxis|BilateralFindings|PriorityFocusList|ClinicalNote|BlockEditPane|saveReportV2|deliverReportV2" | head -5 || echo "OK: no dangling references in modified files"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rE "ReportAdaptiveView|ReportAdaptiveEditor|AdaptiveAnalysisStream|analyzeReadingV2|detectCompletedKeys|saveReportV2Delivered|deliverReportV2|mapVisionFeaturesToTendencies" apps/web/app apps/web/app/actions` returns 0 hits (all consumer references removed)
    - `pnpm tsc --noEmit` reports zero NEW errors after this task (pre-existing baseline preserved)
  </acceptance_criteria>
  <done>Consumers no longer reference any of the 8-block code surfaces; legacy path is the only path.</done>
</task>

<task type="auto">
  <name>Task 2: Delete 8-block files</name>
  <files>(see files_deleted in frontmatter — ~30 files)</files>
  <action>
Use `git rm` on every file in `files_deleted`. Commit as one atomic deletion commit so it's easy to revert if needed.

Order:
1. UI component tests first: `git rm apps/web/components/readings/__tests__/{ReportAdaptiveView,ReportAdaptiveEditor,SystemTendencyCard,SystemTendencyCardEditor,IntegrativeAxesEditor,BilateralFindingsEditor,AdaptiveAnalysisStream,GradeBar,GradeBadge}.test.tsx`
2. UI components: `git rm apps/web/components/readings/{ReportAdaptiveView,ReportAdaptiveEditor,SystemTendencyCard,SystemTendencyCardEditor,IntegrativeAxesEditor,BilateralFindingsEditor,AdaptiveAnalysisStream,BlockEditPane,GradeBar,GradeBadge,KeyTraitChipGroup,IntegrativeAxisItem,BilateralFindingsCard,PriorityFocusList,ClinicalNote}.tsx`
3. Tendency engine: `git rm -r apps/web/lib/tendency-engine/`
4. v2 LLM core libs: `git rm apps/web/lib/anthropic/{analyze-v2,stream-parser-v2,audit-v2,diff-v2,types-v2}.ts` + their tests
5. Schema files: `git rm apps/web/lib/anthropic/{report-schema,report-schema-shared}.ts` + report-schema.test + fixtures dir if it only has report-v2 fixtures

After deletion, verify nothing else references these files:
```bash
grep -rE "from ['\"]@/lib/anthropic/(report-schema|report-schema-shared|analyze-v2|stream-parser-v2|audit-v2|diff-v2|types-v2)" apps/web
grep -rE "from ['\"]@/lib/tendency-engine" apps/web
grep -rE "ReportAdaptiveView|ReportAdaptiveEditor|SystemTendencyCard|SystemTendencyCardEditor|IntegrativeAxesEditor|BilateralFindingsEditor|AdaptiveAnalysisStream|BlockEditPane|GradeBar|GradeBadge|KeyTraitChipGroup|IntegrativeAxisItem|BilateralFindingsCard|PriorityFocusList|ClinicalNote" apps/web
```

All three greps should return 0 hits.

**Do NOT delete:**
- apps/web/components/readings/AdvancedAnalysisCTA.tsx (display-only V1.1 placeholder, retained per DC-7)
- apps/web/components/readings/VocabularyAuditBanner.tsx (runtime audit display surface, retained per DC-10)
- apps/web/components/readings/__tests__/AdvancedAnalysisCTA.test.tsx
- apps/web/components/readings/__tests__/VocabularyAuditBanner.test.tsx
- apps/web/components/readings/EditorAccordion.tsx (legacy 1.0 renderer)
- apps/web/components/readings/AnalysisStream.tsx (legacy 1.0 stream UI)
- apps/web/components/readings/ReprocessButton.tsx
- apps/web/components/readings/AnalysisHero.tsx
- apps/web/components/readings/AnalysisCTA.tsx
- apps/web/components/readings/DeliverDialog.tsx
- apps/web/components/readings/EditorSectionItem.tsx
- apps/web/components/readings/FullReportCopyBlock.tsx
- apps/web/components/ui/checkbox.tsx (just added in Plan 07b — keep, future-proof primitive)
- apps/web/lib/anthropic/{analyze,audit,diff,parser,prompts,client,types,forbidden-terms.json}.ts (legacy core — Plan 11 extends)
- supabase/migrations/* (DB schema stays — report_v2 jsonb column survives, just stores markdown sections now)
  </action>
  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - All files in files_deleted are removed (test: `for f in <list>; do [ ! -f "$f" ] || echo "STILL EXISTS: $f"; done` returns no output)
    - All retained files still exist (test: confirm AdvancedAnalysisCTA.tsx, VocabularyAuditBanner.tsx, EditorAccordion.tsx, AnalysisStream.tsx, analyze.ts, parser.ts all present)
    - `grep -rE "from ['\"]@/lib/anthropic/(report-schema|report-schema-shared|analyze-v2|stream-parser-v2|audit-v2|diff-v2|types-v2)" apps/web` returns 0 hits
    - `grep -rE "from ['\"]@/lib/tendency-engine" apps/web` returns 0 hits
    - `pnpm tsc --noEmit` reports zero new errors
  </acceptance_criteria>
  <done>~30 files deleted; tree compiles; no dangling references.</done>
</task>

<task type="auto">
  <name>Task 3: Verify clean state + write SUMMARY + update STATE</name>
  <action>
1. Run final verification:
   - `cd apps/web && pnpm build` → must exit 0
   - `cd apps/web && pnpm tsc --noEmit` → zero new errors
   - `cd apps/web && pnpm lint` → 0 errors (9 baseline warnings ok)
   - `cd apps/web && pnpm test:run` → only baseline pre-existing failures (quality-scoring×3 + prompts×1)
   - `cd D:/Projetos/Iridologista && git ls-files apps/web/components/readings | wc -l` → should be ~14 files (was 26+ before)
   - `git ls-files apps/web/lib/anthropic | wc -l` → should be ~10 files (was 17+ before)

2. Update Plan 09 SUMMARY.md to mark its UAT section as SUPERSEDED: prepend `> **SUPERSEDED by Direction Correction 2026-05-13 — see 07.4-CONTEXT.md DC-1..DC-10 + 07.4-10-PLAN.md cleanup**` to the uat_walkthrough section.

3. Mark Plans 02, 03, 04, 06, 07, 07b SUMMARY files with a top-of-file SUPERSEDED banner pointing at Plan 10 (just add a one-line YAML field `superseded_by: 07.4-10` and a markdown blockquote at the top — don't rewrite the summaries; preserve as audit trail).

4. Create `.planning/phases/07.4-iris-codex-report/07.4-10-SUMMARY.md` per template documenting:
   - Files deleted (full list with line counts)
   - Files modified (route.ts, 3 page files, 2 server-actions files)
   - Verification (pnpm build/tsc/lint/test outputs)
   - SUPERSEDES list
   - "Plan 11 next" note

5. Update STATE.md:
   - status: "2026-05-13: Phase 7.4 Plan 10 COMPLETE — 8-block code cleanup. Deleted ~30 files (UI components, 8-block editors, tendency-engine, v2 LLM libs, schemas). Modified 6 consumer files (route, 3 pages, 2 action files) to drop v2-only references. App compiles + builds; legacy 1.0 path is the only path until Plan 11 lands the 14-section pipeline. Plans 02/03/04/06/07/07b SUMMARY files marked superseded for audit trail."
   - stopped_at: "Plan 10 cleanup complete. Next: Plan 11 (rewrite system.md for 14 sections + extend analyze.ts/parser.ts to 14 headings + resolves D-DEF-09-05)."
   - last_updated: ISO 8601 now
   - completed_plans: 86 → 87
   - percent: 95 → 97

6. Final docs commit: `docs(07.4-10): SUMMARY + STATE — 8-block cleanup complete`
  </action>
  <acceptance_criteria>
    - pnpm build exits 0
    - pnpm tsc --noEmit clean
    - pnpm lint 0 errors
    - SUMMARY.md created
    - STATE.md updated
    - Plans 02/03/04/06/07/07b SUMMARY files have superseded banner
    - Final docs commit lands
  </acceptance_criteria>
  <done>Plan 10 complete; ready for Plan 11.</done>
</task>

</tasks>

<success_criteria>
- All files in `files_deleted` removed; all files in `must_haves.truths` retained
- pnpm build/tsc/lint/test all green (modulo baseline pre-existing failures)
- 3 atomic commits: Task 1 consumers + Task 2 bulk deletion + Task 3 SUMMARY/STATE
- Plan 11 unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/07.4-iris-codex-report/07.4-10-SUMMARY.md`.
</output>
