---
phase: 07.4
plan: 22
type: execute
wave: 13
depends_on: [17, 18, 21]
supersedes: []
files_modified:
  - apps/web/lib/anthropic/types.ts (extend ReportSectionKey + NUMBERED_SECTION_HEADINGS + SECTION_KEY_BY_NUMBER + SECTION_TITLE_BY_NUMBER for §16)
  - apps/web/lib/anthropic/diff.ts (extend ALL_REPORT_KEYS)
  - apps/web/app/actions/analise.schemas.ts (extend REPORT_KEYS)
  - apps/web/scripts/reparse-reading.mjs (mirror)
  - apps/web/lib/anthropic/__tests__/parser.test.ts (15→27 tests; §16 happy + §17 reject + §15 skip)
  - apps/web/components/readings/ReportReadView.tsx (heading format change + §16 card grid + parseSubsections helper)
  - apps/web/components/readings/AnalysisStream.tsx (TOTAL_SECTIONS auto-bump + SECTION_TITLES extend)
  - apps/web/components/readings/EditorAccordion.tsx (SECTIONS array extend + label format change)
  - apps/web/components/readings/__tests__/ReportReadView.test.tsx (9→12 tests)
  - apps/web/components/readings/__tests__/AnalysisStream.test.tsx (counter + §16 entry)
  - apps/web/components/readings/__tests__/EditorAccordion.test.tsx (16-section + new label)
files_created:
  - .planning/phases/07.4-iris-codex-report/07.4-22-SUMMARY.md
autonomous: true
requirements: []
source_decisions: [DC-1, DC-3]
ux_feedback_source: "UAT-4 founder feedback fix-set 1+4+6 — § removal in heading display + §16 Síntese Rápida throughput + counter 15→16. Plan 21 added §16 to the prompt; this plan plumbs it through parser/types/UI so the new section actually reaches the user."

must_haves:
  truths:
    - "types.ts: ReportSectionKey union extended with '16_sintese_rapida' between '14_mensagem_cliente' and 'encerramento_disclaimer'"
    - "NUMBERED_SECTION_HEADINGS extended to 16 entries (added '16'; sequence skips '15' per founder explicit choice)"
    - "SECTION_KEY_BY_NUMBER + SECTION_TITLE_BY_NUMBER both extended with '16'→'16_sintese_rapida'/'Síntese Rápida'"
    - "diff.ts ALL_REPORT_KEYS extended; analise.schemas.ts REPORT_KEYS extended; scripts/reparse-reading.mjs mirror"
    - "parser.ts: NO regex change needed (already accepts `## N. Title` AND `## §N — Title` via optional `§?` + `[\\p{Pd}.]`); membership check via NUMBERED_SECTION_HEADINGS auto-includes '16'"
    - "parser.test.ts new tests: §16 boundary accept + §17 reject + §15 skip + Plan 22 16-boundary happy path + Plan 17 backward-compat preserved"
    - "ReportReadView.tsx: h2 format `§{N} — {title}` → `{N}. {title}`; NEW parseSubsections helper + special §16 card-grid render (grid-cols-1 md:grid-cols-2) with 6 emoji-labeled cards + fallback to default prose if subsections absent"
    - "AnalysisStream.tsx: SECTION_TITLES extends with 'Síntese Rápida'; counter `{n}/16` derived from TOTAL_SECTIONS = NUMBERED_SECTION_HEADINGS.length"
    - "EditorAccordion.tsx: SECTIONS array extends with §16 entry; AccordionTrigger label `§{s.number} — {s.title}` → `{s.number}. {s.title}`"
    - "EditorSectionItem.tsx + analise-client.tsx: NO change needed (regexes already accept both formats)"
    - "All tests updated: parser +1, ReportReadView +3, AnalysisStream label assertion + §16 entry, EditorAccordion 16-section + new label format"
    - "Verification: pnpm build exit 0 + tsc 22 baseline + lint clean + test:run 620 passed + 3 baseline failures (quality-scoring × 3 only)"
  artifacts:
    - path: ".planning/phases/07.4-iris-codex-report/07.4-22-SUMMARY.md"
      provides: "Plan 22 execution record"
---

<objective>
Plumb Plan 21's prompt changes through the entire pipeline: § removal in
heading display + §16 Síntese Rápida added (16 sections total, skip 15)
+ counter 15→16. Without this plan, Sonnet's `## 16. Síntese Rápida`
heading would parse but the key would be unknown to the union; the
display would still show `§N — Title` even though Sonnet emits `N. Title`.

Scope discipline: NO PDF changes (Plan 23 owns those). NO new prompt
iteration (Plan 21 owns prompt). NO §11 toggle work.
</objective>

(Plan body — see commits 2870d6d + dcc0f20 + this docs commit for the
full implementation. SUMMARY.md captures the same content.)
