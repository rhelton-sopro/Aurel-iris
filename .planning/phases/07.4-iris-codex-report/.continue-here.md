# Phase 7.4 — Continue Here

**Session ended:** 2026-05-15 (founder requested `/clear` after Plan 23)
**Resume command:** Founder does UAT-5 walkthrough; then I run `/gsd-verify-phase 7.4`
**Last commit on main:** `056d9f5` docs(07.4-23): SUMMARY + STATE — Plan 23 complete (PDF direct download)
**Working tree:** clean (only untracked non-project dirs: `.claude/`, `Estratégia comercial e mkt/`, `livros/` — ignore them)

---

## Where we are

Phase 7.4 went through TWO founder UAT rounds this session, each spawning a batch of plans. ALL CODE IS LANDED. The ONLY pending item is **founder UAT-5 manual verification** — I cannot regenerate or judge LLM output myself.

### Pivot/feedback history (full context)

1. **7.3 → 7.4 (2026-05-13 morning)** — 13-section dual-layer ABANDONED → 8-block JSON. Plans 00–09.
2. **7.4 → 7.4-redirection (2026-05-13 evening)** — 8-block ABANDONED → 14 markdown sections. Plans 10–14.
3. **UAT-3 (2026-05-14 evening)** — founder rejected §3/§10/§13 still leaking author/school/sector citations + missing positive systems + accordion-only UX. Plans 16–19.
4. **UAT-4 (2026-05-15)** — founder issued 6 fixes (§ removal, §1 breathing, PDF direct download, §16 Síntese Rápida, §12 numbered, counter 16). Plans 21–23.

### Plans 16–23 — ALL COMPLETE (19 commits this session)

| Plan | What | Commits |
|---|---|---|
| 16 | system.md — 7 absolute rules + §2.5 + §3 4-field + §10 symbolic + §13 human + §1 polish | 5a65ef7, 6017d76, 447eace |
| 17 | 15-section plumbing — types/parser/diff/schemas + AnalysisStream/EditorAccordion/EditorSectionItem/analise-client (decimal §2.5 + array-index monotonicity) | fb650c9, 6439214, 4a1158d |
| 18 | ReportReadView (continuous flowing serif) + ReadingModeActions + page.tsx reading-mode routing flip; accordion now ONLY on /editar | 824ad23, 0c0a14d, b469fbf |
| 19 | PDF via Print CSS (SUPERSEDED by Plan 23) | bc451eb, 780e63d, 8f2a137 |
| 21 | system.md UAT-4 — § removal in headings + §1 short-paragraph breathing + extended polish table + §12 numbered list + NEW §16 Síntese Rápida | d71ed21 |
| 22 | Code plumbing — §16 + § removal in renderers + counter 15→16; ReportReadView §16 card-grid (parseSubsections) | 2870d6d, dcc0f20, 16ac8ce |
| 23 | @react-pdf/renderer install + lib/pdf/report-document.tsx + /api/readings/[id]/pdf route + ExportPdfButton refactor (Button+fetch+blob) + /print route DELETED | 91cf1e5, 25cecb9, 056d9f5 |

**Verification (last full run):** pnpm build exit 0 · tsc 22 baseline (zero new — D-DEF-09-03 readings.test×12 + ReprocessButton×3 + quality-scoring×2 + modal-client×5) · lint 0 errors + 9 baseline warnings · pnpm test:run 621 passed + 3 baseline failures (quality-scoring×3 ONLY per MEMORY feedback_quality_scoring_test_gate.md).

---

## Current canonical shape (post-UAT-4)

- **16 markdown sections** emitted by Sonnet as `## N. Title` (NO § symbol — Plan 21) where N ∈ {1, 2, 2.5, 3..14, 16}. **§15 is SKIPPED** (founder explicit numbering choice — §16 follows §14 directly).
- Parser `lib/anthropic/parser.ts findAllBoundaries`: regex `(\d{1,2}(?:\.\d)?)` + optional `§?` + `[\p{Pd}.]` separator → accepts BOTH new `## N. Title` AND legacy `## §N — Title`. Monotonicity via array index against `NUMBERED_SECTION_HEADINGS` (16 entries, includes '16', skips '15').
- Storage: `readings.report_generated`/`report_delivered` jsonb keyed by `ReportSectionKey` (16 numbered keys + `encerramento_disclaimer`). New key: `16_sintese_rapida`.
- **Reading mode (default)**: `/leituras/[id]` → `ReportReadView` (continuous flowing serif, max-w-prose). §16 renders as 6-card grid (`parseSubsections` splits `### 🔴 Fragilidades` etc). Top buttons: Exportar PDF + Editar análise + Entregar ao cliente + Regenerar (via `ReadingModeActions`).
- **Edit mode**: `/leituras/[id]/editar` → `EditorAccordion` (accordion ONLY here).
- **PDF**: `ExportPdfButton` → fetch `GET /api/readings/[id]/pdf` → `renderToBuffer(<ReportDocument>)` (@react-pdf/renderer 4.5.1) → blob download `Leitura-{Name}-{YYYY-MM-DD}.pdf`. `/print` route DELETED.

---

## Resume protocol — Plan 24 (UAT-5)

1. Read `.planning/STATE.md` frontmatter `status:` (most recent entry = Plan 23 record).
2. Read `MEMORY.md` index — load `project_phase_7_4_direction.md`, `iris_codex_quality_bar.md`, `feedback_iris_codex_seven_rules.md` (all updated this session).
3. Ask the founder for the UAT-5 verdict. They must FIRST re-regenerate the Nailli reading (button "Regenerar análise" on `/leituras/<nailli-id>`), then verify the **7 UAT-4 acceptance criteria**:
   1. Section titles show `1. Constituição e Temperamento` (NO §)
   2. §1 short paragraphs, NO "colarete"/"fibrilar"/"hepatobiliar"/"sinais setoriais"/"organização funcional de base"
   3. "Exportar PDF" triggers DIRECT FILE DOWNLOAD (not browser print dialog)
   4. PDF opens as clean serif document with "Iris Codex" brand mark + footer disclaimer on every page
   5. §16 Síntese Rápida shows 6 labeled blocks (🔴/🟢/💛/✨/🧭/🌱) SPECIFIC to Nailli, not generic
   6. §12 questions numbered 1. 2. 3. as proper ordered list
   7. Counter shows `{n}/16 seções` during streaming
4. Three verdict paths:
   - **`approved` / UAT-5 PASS** → run `/gsd-verify-phase 7.4` → `/gsd-secure-phase 7.4` → mark Phase 7.4 complete. THIS is the close path.
   - **`§3 still generic`** → most likely D-DEF-14-01 (Cronorichio corpus gap). Open Phase 7.2-H plan: ingest Lo Rito Cronorichio + Jensen biographical + Brazilian biographical iris-map literature (~$1 ingest). Note: §3 4-field template (Plan 16/21) hides un-anchored markers via skip-rather-than-fabricate, so generic §3 = genuine corpus gap, not prompt gap.
   - **Specific section/PDF feedback** → spawn Plan 25 with a targeted fix. Founder will name the section/surface + the issue.

---

## What NOT to do

- ❌ Don't reintroduce the `§N — Title` heading format (Plan 21 removed it; parser still accepts legacy for backward-compat with pre-Plan-21 reports, but the prompt + renderers emit `N. Title`).
- ❌ Don't reintroduce the `/print` route or PrintTrigger (Plan 23 deleted; PDF is now server-side direct download).
- ❌ Don't emit §15 — the sequence is {1, 2, 2.5, 3..14, 16}. `NUMBERED_SECTION_HEADINGS` deliberately excludes '15'.
- ❌ Don't reintroduce 8-block JSON (`tendency_grade`, `systems_with_tendency`, `zodOutputFormat`) — canonically dead per DC-9.
- ❌ Don't auto-spawn Phase 7.5 (tendency mapping engine) — DEFERRED, IP shape needs re-discussion.
- ❌ Don't implement the §11 Afirmações toggle (DC-5 — still deferred; founder hasn't requested post-UAT).
- ❌ Don't build the paid "Análise Aprofundada" V1.1 technical tab — stays as `AdvancedAnalysisCTA.tsx` placeholder.

---

## Open follow-ups (deferred-items.md + new)

- **D-DEF-09-03** — 22 pre-existing tsc errors in 4 test files (Phase 3/5 typing debt). Out of scope. Baseline — do NOT gate on these.
- **D-DEF-09-04** — ~45 pre-existing audit-vocabulary hits (Jensen RAG metadata + Cronorichio section-queries + VocabularyAuditBanner display + diagnóstico debug comments). Out of scope. system.md is allowlisted (line 1 marker) — Plan 16/21/23 changes did NOT add new hits.
- **D-DEF-14-01** — Cronorichio + biographical-map chunks may be ABSENT from current RAG corpus. If UAT-5 §3 still feels generic → Phase 7.2-H corpus expansion is the remediation (NOT another prompt iteration — the §3 4-field template already hides un-anchored markers).
- **NEW (Plan 23)** — PDF inline markdown formatting (bold/italic/code) is STRIPPED in v1 (structure-only render). If founder wants inline styling in PDF, follow-up plan adds a richer @react-pdf tokenizer.
- **NEW (Plan 18)** — AnalysisStream + EditorAccordion still inline their own SECTION_TITLES arrays; could DRY against `SECTION_TITLE_BY_NUMBER` in types.ts. Cosmetic; not blocking.

---

## Critical anti-patterns (none blocking resume)

No `blocking` anti-patterns. Standard project memory rules apply (all auto-loaded from MEMORY.md):
- Worktree cleanup on Windows uses bash `rm -rf`
- `'use server'` files only export async functions
- `safeArray()` before jsonb iteration
- Run `pnpm lint` before pushing (next build gates eslint)
- Plain `git commit` for source files (gsd-tools.cjs filters to .planning/)
- 3 quality-scoring.test.ts failures are PRE-EXISTING baseline — never gate on them
- The 7 Iris Codex absolute rules + the GLOBAL CALIBRATION RULE are LOCKED invariants for any future prompt iteration
