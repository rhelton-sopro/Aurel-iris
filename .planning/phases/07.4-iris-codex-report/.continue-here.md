# Phase 7.4 — Continue Here

## ⚡⚡ LATEST — 2026-05-16 cont.: COLUMN C (ANÁLISE DIRETA SONNET) built + dry-run validated

**Two active tracks now. Read both.** Track B (Column C) is the newest work;
Track A (SAM blind validation) is PAUSED awaiting founder data (see §"SAM
harness LIVE" block below — the decisive Nailli A/B reveal + N=5 still owed).

### Track B — Column C: 3rd calibration method (`feat(07.4-33)`)

**What:** isolates the value of Sonnet's DIRECT VISION. 6 photos → Sonnet,
**NO pipeline features, NO RAG** — same system.md (verbatim, shares the A/B
prompt-cache prefix) + a 2nd system block `VISUAL_MODE_OVERRIDE` that
re-points every "read the JSON / cite the feature" instruction to direct
visual observation WITHOUT loosening the 9 rules or the global calibration
filter. method_version `sonnet_direct_0.1.0`. Needs **NO Modal** (bypasses
segmentation) — only ANTHROPIC_API_KEY.

**Files:** `lib/anthropic/analyze-direct.ts` (generator, same {stream,finalize}
as analyzeReading) · `lib/calibration/generate-sonnet-direct.ts` (orchestrator:
canonical 800×800 crops `canonical_storage_path ?? storage_path` → sharp → 6
img blocks → parser/audit → persist `*_sonnet_direct`) · `lib/calibration/
log-generation.ts` (best-effort `report_generations` insert) · route
`POST /api/admin/calibration/sonnet-direct/[reading_id]` · UI: SamCompareShell
rewritten → **generic N-col blind shell** (Fisher-Yates order shuffle,
identity AND per-col actions hidden until "Revelar" — fixes the blind leak)
· `RunSonnetDirectButton` · `comparar/page.tsx` (3-col, defensive 0016/0017
fetch) · `database.ts` types · migration `0017`.

**Instrumentation:** all 3 methods (vigente analyze route + sam-report +
sonnet-direct) now best-effort log to `report_generations` (reading_id,
method, latency_ms, cost_usd, tokens_in/out, model_version).

**Dry-run validated TODAY** (reading `36b5abd0-296d-4666-b1f0-8535f5a1ca5a`,
Nailli, NO persist, NO migration needed): 15 sections, essence_phrase
present, audit anchor 100%, 6 images, ~$0.19/run, ~157s. Output reads
anchored + on-voice (no §-refs, no pipeline meta-language, no jargon dump).

**Verify GREEN vs baseline:** tsc **22 baseline / 0 new** · lint **0 err, 0
warnings in new code** (10 pre-existing baseline) · vitest **648 passed + 3
quality-scoring pre-existing only / 0 new**; runner auto-skips suite.

### ⛔ MIGRATION 0017 GATE (founder-applied — exact 0016 precedent)

Persisting Column C + showing the 3rd column on the page needs migration
`0017_report_generations_and_sonnet_direct.sql` (additive: `report_generations`
table + `readings.*_sonnet_direct` cols, IF NOT EXISTS, prod untouched).
Claude **cannot apply DDL** (only the PostgREST service key locally — no
Postgres conn string; `supabase db push` needs the DB password). Founder:
`supabase db push`. Page degrades gracefully ("migration 0017 pendente")
until then. AFTER it lands: founder clicks **"Gerar Coluna C"** on
`/admin/calibration/<id>/comparar`, OR run:
`cd apps/web && RUN_SONNET_DIRECT_READING=<id> RUN_SONNET_DIRECT_PERSIST=1
npx vitest run --config scripts/runner.vitest.config.ts
scripts/run-sonnet-direct.spec.ts` (dry-run = drop PERSIST; works pre-migration).

**NOT pushed by Claude** (memory: explicit `git push` is a founder decision).

### Open Column-C follow-ups (deferred — founder said NÃO FAZER this round)

- Coluna D (RAG sem features) — explicitly out of scope.
- Prompt variants — out of scope.
- PDF export for Column C — out of scope (inline compare suffices; the
  compare page now hides ALL per-column PDF buttons while blind, shows
  vigente/sam buttons only post-reveal; Column C has none — no leak since
  blind hides them all).
- GSD: `feat(07.4-33)` is committed for convention but, like the SAM
  harness, is NOT a formally registered GSD phase (no PLAN/SUMMARY).

---

## ⚡ 2026-05-16: SAM harness LIVE · first comparison done · blinded validation IN PROGRESS (Track A — PAUSED)

**Track A is paused** — founder pivoted to Track B (Column C) mid-session.
The decisive Nailli A/B reveal + the N=5 blind set are still owed by the
founder before the pre-registered SAM decision rule can run. Cross-check
fingerprint from `diag-sam-vs-vigente.mjs eb818f3c` (run 2026-05-16, DB
creds local): SAM = constitution `mista` + h3 OE **grau_4 3.835mm**;
vigente = `hematogenea`/`castanho` + saturated 12/12 OE grau_3. By the
founder's own column-content description, **B = SAM, A = vigente** —
confirm with the Revelar button (his designed ground truth), do NOT
re-scope on the content-inference + N=1.

### Status: harness built, deployed, working
- `7014abc feat(07.4-32)` = full SAM harness. **Pushed + live on Vercel**
  (`git status -sb` = in sync with origin — verified, not assumed).
- Migration `0016` **applied** (`sam_run_metadata` present in DB confirms).
- Modal **redeployed** with the SAM functions AND the 500-fix below.
- Vercel env `MODAL_ANALYZE_SAM_ENDPOINT_URL` **set** =
  `https://rhelton-sopro--aurel-iris-vision-analyze-iris-sam-endpoint.modal.run`.
- The `modal_app.py` 500-fix + 3 diag scripts are committed in THIS handoff
  commit (the only post-7014abc source change; was live on Modal via
  `modal deploy` but had been uncommitted — gap now closed).

### The 500 bug (fixed) — LESSON for any future modal_app edit
`run_pipeline_sam` built `processing_metadata` with `segment_diagnostics` +
`variant` keys. `pipeline/schemas.py ProcessingMetadata` is
`ConfigDict(extra="forbid")` → `IrisFeatures.model_validate` raised →
Modal **500** → admin route surfaced **502**. Fix: use the schema's existing
`detect_diagnostics` field, drop `variant`. **Rule: processing_metadata must
be EXACTLY the 7 ProcessingMetadata fields** (model_version,
processing_time_ms, modal_call_id, stages_timing_ms, warnings, error_summary,
detect_diagnostics). Any modal_app payload change → re-check schemas.py.

### First comparison — Nailli `eb818f3c-23ce-4817-9e26-7d1ab6ab3b71`
Founder did the blind compare. Column **B** = "CERTEIRO" on her known
jaw/cervical/bruxism/TMJ pattern, left-dominant; Column **A** = "diluído".
Technical diff (`scripts/diag-sam-vs-vigente.mjs`): VIGENTE OE = saturated
**12/12 sectors grau_3 2–4mm** (broken-Hough noise); SAM OE = differentiated,
**h3 mandíbula-esq grau_4 3.835mm standout** (vigente never emits grau_4) +
constitution moved `hematogenea/castanho` → **`mista`** (toward founder's
known `mista_biliar`). **Verdict: real signal, not Sonnet narrative
coincidence.** Caveats logged: N=1; SAM *dropped* h4 cervical (oversize-gate);
`sectoral_pigments` STILL empty both pipelines (median-relative blindness,
exactly as predicted — SAM did not fix pigment).

### ⛔ DECISIVE OPEN QUESTION — answer FIRST tomorrow
Founder's last words: *"sinceramente... gostei do A."* He prefers Column A
**as a document** (different axis from the jaw-signal finding — possibly A
"says less, anchors more" = wins on his own Iris Codex rule; or B
overclaims). **Unanswered: did the "Revelar" show A = SAM or A = vigente?**
- A = vigente → the preferred report came from the BROKEN pipeline → better
  segmentation ≠ better product → vision investment may not move product
  value (Forer-vs-real, answered *against* vision). Huge, clarifying.
- A = SAM → gut + data agree.
Do NOT guess (blind shell randomizes client-side, no server record — the
"Revelar lados" button is the only ground truth). Cross-check fingerprint:
SAM = the column whose features have h3 OE grau_4 + constitution `mista`.

### Validation IN PROGRESS (founder executing, then returns with results)
Founder will: reveal Nailli; run SAM on wife (he has the id) + 2–3 more
well-known patients; blind-compare each. Reporting format agreed (per
reading): known clinical patterns written **BEFORE** reading the reports →
blind verdict (A/B more accurate) → after-reveal SAM=which → per-pattern
hit/diluído/perdeu for vigente AND SAM → **FABRICATIONS count both sides**.
Aggregate = 3 numbers: (1) blind-pick = how often founder blind-picked the
SAM side as more accurate; (2) padrões net delta (SAM-hit & vigente-miss
minus inverse); (3) fabrications SAM ≤ vigente. **Pre-registered decision
rule:** pursue SAM only if blind-pick ≥ ~4/5 AND padrões delta positive AND
SAM fabrications ≤ vigente. Mixed → inconclusive. N=5 = directional, not
significance. Claude runs `diag-sam-vs-vigente.mjs <id>` per reading for the
feature-diff cross-check. **Design must be able to conclude "stay on
vigente" — the founder's gut already showed that outcome is live.**

### Deferred technical (answered; do NOT implement — gather data first)
- **(a) oversize-gate** `MAX_PLAUSIBLE_FINDING_MM = 4.0` in
  `vision-service/pipeline/features.py`: 1-constant knob, but DON'T tune now
  (N=1 overfit risk; SAM changes the px→mm scale so it may need
  re-derivation vs SAM geometry, not a bump). Instrument: capture
  `modal app logs aurel-iris-vision` during the 3–5 runs for the
  `[features] sector N: DROP ... size_mm=X` lines → decide with real dropped
  sizes. Better-but-bigger option: emit oversize findings flagged
  low-confidence instead of silent drop (touches Finding contract).
- **(b) pigment median-relative blindness**: small fix (~10–20 LOC in
  `detect_sectoral_pigments`, no schema change) — either add an
  absolute-yellow test, or re-base the delta against a non-superior /
  least-yellow reference (amber is superior in iridology). Honest catch:
  trivial to code, but needs 2–3 founder-confirmed amber/non-amber examples
  to set the threshold/reference — else it's a guess (false-amber on
  warm-lit photos). The multi-run incidentally yields those examples.

### GSD process note
Harness is committed `feat(07.4-32)` for convention but is NOT a formally
registered GSD phase (no PLAN/SUMMARY; not in ROADMAP/STATE). `/gsd-insert-phase`
retrofit is a deferred founder decision (raised, not yet actioned).

### Diag scripts (committed, validation tooling)
`apps/web/scripts/diag-sam-vs-vigente.mjs <readingId>` (vigente vs SAM
feature-diff, jaw-zone focus) · `diag-nailli-pigments.mjs` · `diag-nailli-imgdims.mjs`.
Run: `cd apps/web && node --env-file=.env.local scripts/<x>.mjs <readingId>`.

### Git/deploy reality (verified `git status` 2026-05-16, do not assume)
`7014abc` pushed + Vercel-live + in sync with origin. This handoff commit
adds: `modal_app.py` (500-fix), 3 diag scripts, this doc. **NOT pushed by
Claude** — founder decides `git push` (modal_app.py is git-independent for
Modal, already redeployed; pushing only updates origin/Vercel and Vercel
ignores `vision-service/`). Untracked & ignore: `.claude/`, `livros/`,
`logo/`, `Estratégia...`.

---

**Session ended:** 2026-05-15 (Plan 31 iter-6 — FIX 13+14 LANDED via Option B additive: pigment as first-class finding + objective lacuna grading + compute_asymmetry weighs pigment (Nailli OD/OE root cause). FIX 11/12 validated on real data by founder. SAM harness now supersedes the FIX 15/16 sequencing — see pivot above.)
**Last commit context:** Plan 31 iter-6 `d6648f2` (FIX 13+14). Repo does NOT auto-push (verified) — explicit `git push` required.

---

## ⛔ DEPLOY GATES (cold-start — corrected 2026-05-15 cont.)

1. **PDF gate — CLOSED.** Gotenberg live on Render, Vercel env set, PDF
   working in production (founder-confirmed). The old "still open / 503"
   text was a stale handoff snapshot — disregard. The new `?variant=sam`
   PDF reuses this same working path.
2. **Vision gate (Plan 30/31).** `cd vision-service &&
   modal deploy modal_app.py` (model_version is now `pipeline_0.2.0`).
   Then reprocess Nailli (status→`pending`/`failed` → Reprocessar on the
   `/leituras` LIST, or `POST /api/readings/<id>/process` from the browser
   console; **never DELETE the `readings` row** — `reading_images` cascades).
   `vision_features` is a JSONB column on `readings`, NOT a table. Validate:
   `model_version=="pipeline_0.2.0"`; no `size_mm>4`; Hough fallback not
   6/6; §7 OD has `type:"pigmentacao"`; `asymmetry_notes` has
   `carga_pigmentar_assimetrica_maior_*` (OD no longer "cleaner"); lacuna
   `depth` varies grau_1..4.

**Gated remaining work, in the founder's own mandated order** (do NOT start
until the Vision gate is validated clean — founder's explicit DO-NOT):
FIX 15 (density: recalibrate the saturating `mag.mean()/50` formula — NOT
"remove a sentinel"; there is none) → FIX 16 (intake quality gate: block
when composite<0.7 or Hough fails >50%) → anti-Forer iter-6 FIX 5–9
(system.md anchoring discipline) → §3 radial-chronology rewrite (separate
brief: radial-zone age inference, new "Marca" template, few-shot, 4 fixture
tests). Memory: [[vision-pipeline-reality]], [[phase-7-4-direction]],
[[iris-codex-quality-bar]], [[iris-codex-seven-rules]].

Plan history (all LANDED + pushed, green vs baseline): 26 Gotenberg engine ·
27 premium PDF v3 + 16→15 restructure · 28 iter-4 6-change (split+merge,
essence) · 29 iter-5 (§14 title, breathing, essence mandatory, page-count) ·
30 iter-6a (FIX 11 Hough scale, FIX 12 px→mm gate) · 31 iter-6 (FIX 13+14
pigment finding + grading). Baseline gates: web tsc 22 / lint 0err+9warn /
3 quality-scoring test failures are PRE-EXISTING — never gate on them.

---

## Plan 31 iter-6 (FIX 13+14 — Option B additive) — LANDED 2026-05-15

Founder approved **Option B** (additive, backward-compatible, no Pydantic
structural change, model_version MINOR) over the brief's literal breaking
restructure. Rationale: backward-compat with old readings, no
`feature-injection.md`/`SPEC.md` touch, fixes the REAL root cause.

| FIX | Change | Files |
|---|---|---|
| 13 | Pigment = first-class finding. `extract_all` merges already-computed `sectoral_pigments` INTO `sectors[].findings` as `type:"pigmentacao"` (`color`/`extension`; `Finding` already had fields → **zero schema change**). **`compute_asymmetry` now weighs `sectoral_pigments`** → `pigmento_<type>_unilateral_setor_<h>_<eye>` + `carga_pigmentar_assimetrica_maior_<eye>` — THE root cause of the Nailli OD/OE inversion. | `vision-service/pipeline/features.py`, `schemas.py` (docstring only) |
| 14 | `_grade_lacuna`: objective `grau_1..4` from interior-vs-~2mm-annulus contrast + diameter (was hardcoded `"grau_1"`). | `vision-service/pipeline/features.py` |
| — | model_version `pipeline_0.1.0` → **`pipeline_0.2.0`** (D-PM1 MINOR, both paths). system.md rule (pigment≠lacuna both ways; `findings:[]`≠clean→check `sectoral_pigments`; honor `carga_pigmentar_*`). Calibration UI surfaces `f.color`/`f.extension`. `feature-injection.md`/`SPEC.md` UNTOUCHED. | `modal_app.py`, `apps/web/prompts/system.md`, `FeaturesDisplay.tsx`, `TechnicalReportCopyButton.tsx`, tests |

Verify GREEN vs baseline: vision-service pytest **284 passed / 4 skipped
(env) / 0 failures**; web vitest **648 passed** + 3 quality-scoring baseline
only/0 new; tsc 22 baseline/0 new; lint 0err/9warn; build compiled.

**Requires Modal redeploy** to affect real readings (founder does this; NOT
done by Claude per instruction). Old readings keep validating (additive).

---

## Plan 30 iter-6a (vision pipeline integrity — BLOCKER) — FIX 11+12 LANDED 2026-05-15

The iter-6/6a brief blamed Forer content on the prompt, then on "invalid
vision JSON" with specific mechanism guesses. **Reading `vision-service/`
showed the symptoms are real but the brief's mechanisms were materially
wrong — and the pipeline is a crude MVP of placeholder heuristics.** See
memory [[vision-pipeline-reality]].

| Symptom (brief) | Brief's guessed mechanism | ACTUAL root cause (code) | Status |
|---|---|---|---|
| findings 13–16 mm > 12 mm iris | "px→mm broken in MediaPipe fallback path" | **No px→mm ever existed.** `features._detect_findings_in_sector`: `size_mm = area_px/100`. 1653-px blob → "16.53 mm". | **FIXED (FIX 12)** — real iris≈12 mm radial model (`2·√(area/π)·6/H`, iris radius cancels) + hard >4 mm DROP gate + drop-when-no-iris-scale. Threaded `iris_radius_px`+`warnings` through `extract_all`. |
| Hough failed 6/6 → MediaPipe fallback every photo | "Hough params / specular / resolution" (cause c) | `segment.iris_mask()` ran `HOUGH_DEFAULTS` (minR/maxR 80–200 px, calibrated for 1024px) on the **FULL-RES 4K** image; `detect.py` resizes to 1024 first, `segment` did not → Hough always None → D-F1 seed fallback + warning every image; boundary never refined. | **FIXED (FIX 11)** — resize ≤1024 before Hough (mirror detect.py), scale circle back, `[segment]` diagnostics. Test imgs <1024 ⇒ scale 1.0 ⇒ no behavior change. |
| density 100% "densa" both eyes | "sentinel/default, silent failure" | NOT a sentinel: `clip(sobel_mag.mean()/50,0,1)` **saturates** on CLAHE-enhanced polar images for ~every iris. Miscalibrated formula. | FIX 15 — NOT done (gated). |
| all "lacuna grau_1", zero pigments | "detector failing" | `depth:"grau_1"` is a hardcoded literal (no grading fn = FIX 14). Findings = only `gray<60→connected-components→"lacuna"`; pigments computed separately (`sectoral_pigments`) for constitution only, never enter `sectors[].findings` (= FIX 13). | FIX 13/14 — NOT done (gated; FIX 13 breaking). |
| composite 0.75 not blocking | intake gate too permissive | `_build_image_quality`: score=0.85−0.1·#warnings; orchestrator only fails when BOTH eyes None. | FIX 16 — NOT done (gated). |

Verify: full `vision-service` suite **280 passed, 4 skipped (env-gated:
3 detect = no local mediapipe model, 1 segment = empty iris fixtures), 0
new failures**. Deterministic synthetic tests (Nailli's 6 photos are NOT
in the repo — `tests/fixtures/iris/` empty, PII in Supabase).

### ⚠️ Decisions / gates before continuing (founder)

1. **Modal redeploy** — `vision-service` runs on Modal; this commit is
   source only. FIX 11/12 affect real readings only after a Modal deploy
   + a re-run of `eb818f3c`. Inspect the new JSON: expect no finding
   > 4 mm and a large drop in finding count.
2. **FIX 13 is a BREAKING contract change** (`findings: [...]` →
   `{lacunae, pigments, uncertain}`): ripples into `pipeline/schemas.py`
   (Pydantic), TS `vision_features` consumers, the analyze prompt, and
   `features.compute_asymmetry`. Needs explicit approval + a coordinated
   cross-stack plan before implementation.
3. **Sequencing (founder's own DO-NOT):** FIX 13→14→15→16, then ONLY
   AFTER clean vision JSON, the anti-Forer iter-6 (FIX 5–9) + the §3
   radial-chronology rewrite (separate message, twice). The §3 rewrite is
   well-specified (radial-zone chronology, new Marca template, few-shot,
   4 fixture tests) but per the brief must wait for clean vision input.

---

## Plan 29 iter-5 (COMPLETE — FIX 1/2/3/4) — LANDED 2026-05-15

Founder's iter-5 brief = "four fixes + investigate missing essence". The
first message truncated mid-FIX-3; the founder resent FIX 3 (full) + FIX 4
and all four are now landed + verified. (Original truncation note kept for
audit trail in commit history.)

| FIX | What | How it landed |
|---|---|---|
| 1 | §14 title → "Para {first name}" | `sectionDisplayTitle(headingStr, clientName)` in `lib/anthropic/types.ts` — §14 → `Para {clientName.split(/\s+/)[0]}`, blank → `Mensagem para o Cliente`. Render-time only; internal key + LLM `## 14.` heading UNCHANGED. Applied to PDF heading+Índice + web heading+Índice. system.md §14 note added. |
| 2 | Breathing + word-break | Body Gotenberg margins top 0.66→**0.85**, bottom 0.42→**0.7**, L/R 0→**0.7** (cover render stays all 0 = bleed). Internal CSS padding zeroed (`.content`/`.toc`/`.essence-page`) so page margins own the frame (no double inset); header/footer h-padding 16mm→0.7in to align with content column. Section gap 48→**56px** (web mt-12→mt-14). `body{word-break:normal;overflow-wrap:break-word;hyphens:auto}` + `lang="pt-BR"` already on `<html>`; web `<article lang="pt-BR">` + same CSS — kills the "racionalizadaou" mid-word break. |
| 3 | Missing "Em uma palavra" — investigate + make mandatory | **Hardening (iter-5a) + MANDATORY prompt (iter-5b).** `extractEssencePhrase` searches ONLY the pre-§1 region, marker tolerant of preamble/same-line(`:`/`—`)/ALL-CAPS/any depth. **system.md now MANDATORY**: imperative block at the TOP of "Formato de saída" ("Antes de gerar a seção 1, você DEVE emitir ... NÃO PULE"), spec+checklist soft→imperative. Tests: prompts.test asserts the mandatory wording; parser.test +realistic full-stream fixture (8 essence tests total). Render side traced & confirmed correct (renderBodyHtml includes the essence page → it's in PDF B → merged; covered by report-print test). |
| 4 | Page count excludes cover | **Correct BY CONSTRUCTION — no code change.** Split-merge: Chromium computes `.pageNumber/.totalPages` (post-iter-4 the counter is in the HEADER, `renderHeaderHtml`, not footer) during the BODY render only; cover is a separate PDF merged in front → excluded. Índice=p.1, disclaimer=p.N/N, total=body pages. Locked by a report-print regex test asserting `p. {pageNumber} / {totalPages}`. |

Verify GREEN vs baseline: test **647 passed** (parser 36 / prompts 33 /
report-print 17) + 3 quality-scoring baseline only / 0 new · tsc 22
baseline/0 new · lint 0err/9warn · `pnpm build` compiled. Still gated by
the Plan 26 Render+env action.

### Essence still missing after a fresh regen? (debug ladder)
1. Confirm Vercel deployed the iter-5 commit (deployment hash).
2. Regenerate `eb818f3c` AFTER that deploy (old readings have no `essence_phrase`).
3. Check `readings.report_raw_text` for the reading: does Sonnet emit a line containing "em uma palavra" before `## 1.`? If NOT → it's a prompt-adherence problem (strengthen system.md / move the instruction higher / add to the user message), not a parser bug.
4. If the marker IS in `report_raw_text` but `report_generated.essence_phrase` is empty → parser regex gap; the marker text from raw_text is the test fixture to add.

---

## Plan 28 iter-4 (6-change PDF/web brief) — LANDED 2026-05-15

Founder's 6-change brief. All implemented; verification GREEN vs baseline
(test 640 pass + 3 quality-scoring baseline only / 0 new · tsc 22 baseline/0
new · lint 0err/9warn · `pnpm build` compiled). Still gated by the Plan 26
Render+env action (PDF returns clean 503 until then).

| # | Change | How it landed | Deviation? |
|---|---|---|---|
| 1 | Horizontal-logo running header every internal page | **SPLIT+MERGE**: Chromium has NO per-page header skip, so the route now renders 2 PDFs — `renderCoverHtml` (white, full-bleed, zero margins, no header) + `renderBodyHtml` (Índice+essence+§1..§15+disclaimer with Gotenberg `header.html`=horizontal logo+client+`p.N/total` & slim `footer.html`) — then `/forms/pdfengines/merge`. 3 Gotenberg calls / 45s. | Page numbers count BODY pages (cover excluded → p.1 = Índice). Header right = client + p.N/total (page counters only exist in Gotenberg header/footer docs — the technically correct mechanism). |
| 2 | Cover bg pure white | `--white #FFFFFF`, `.cover{background:var(--white)}` | none |
| 3 | Breathing room | header band marginTop 0.66in reserves ~28px below the teal rule; section gap 48px; first section flush (`:first-of-type margin-top:0`) | Chromium can't do "first section ON a page" mt:0 (no per-page-first selector) — only the very first section is flush. Tune in real UAT. |
| 4 | TOC page numbers (two-pass) | **Brief-sanctioned fallback**: `generateDocumentOutline=true` → clickable PDF bookmark tree from h2 headings + dotted leaders + Ctrl+F/bookmarks footnote. Two-pass text-extraction REJECTED (fragile heading→page matching + double latency for infra founder can't see yet). | Yes — documented decision. Real navigation via the PDF bookmark panel, not inline TOC numbers. |
| 5 | "Em uma palavra" page + `essence_phrase` | New non-numbered `ReportSectionKey`. LLM emits `## Em uma palavra\n<15-25w>` before §1 (system.md spec+checklist). `parser.ts extractEssencePhrase()` (NOT a numbered boundary — monotonicity intact) pulled in `analyze/route.ts` finalize + partial-save. Synced: types.ts (union + NumberedSectionKey Exclude + ESSENCE_PHRASE_HEADING), analise.schemas REPORT_KEYS, diff.ts ALL_REPORT_KEYS. Centered page between Índice and §1 (PDF) / block (web). Absent → skipped. | none |
| 6 | Web ReportReadView ↔ PDF parity | New `lib/design/report-tokens.ts` (shared colors/fonts/§15 palette). ReportReadView restyled: horizontal logo header, teal accents, Georgia serif, **`N — Title`** (teal num+em-dash, was `N. Title` — test updated), teal rule, essence block, anchor-link Índice (`#sec-N`), §15 shared-palette tinted cards, §14 letter. White scroll, actions on top, RSC. | Heading format changed `N.`→`N —` for PDF parity (founder brief explicit) — ReportReadView.test assertions updated accordingly. |

**Files touched:** `lib/pdf/logo-assets.ts` (+`IRIS_CODEX_LOGO_HORIZONTAL_DATA_URI` 640w base64), `lib/pdf/report-print-document.tsx` (rewritten: renderCoverHtml/renderBodyHtml/renderHeaderHtml/renderFooterHtml; renderReportPrintHtml REMOVED), `app/api/readings/[id]/pdf/route.tsx` (split+merge), `lib/anthropic/parser.ts` (+extractEssencePhrase), `lib/anthropic/types.ts`, `app/actions/analise.schemas.ts`, `lib/anthropic/diff.ts`, `app/api/readings/[id]/analyze/route.ts`, `prompts/system.md`, `components/readings/ReportReadView.tsx`, `lib/design/report-tokens.ts` (new), `public/logo/iris_codex_horizontal.png` (new), + 3 test files updated (parser/report-print-document/ReportReadView).

### UAT-iter-4 verification (founder — Claude CANNOT do this)

After Render+env (Plan 26 actions still open): regenerate `eb818f3c` (mandatory — needs `essence_phrase` + 15-key shape), then check:
- **PDF**: page 1 = white cover, NO header, full bleed. Page 2 = Índice w/ dotted leaders + nav footnote. Page 3 = "Em uma palavra" (essence phrase, header present). Pages 4+ = §1..§15 each with horizontal-logo header (logo left, client + `p.N/total` right, teal rule) + ~28px breathing. PDF bookmark/outline panel navigates sections. Cover excluded from page count (p.1 = Índice).
- **Web** `/leituras/[id]`: horizontal logo header, `N — Title` teal headings + rule, "Em uma palavra" block, clickable Índice, §15 tinted cards, §14 letter — feels like the same document as the PDF.
- Verdict: **PASS** → `/gsd-verify-phase 7.4` → `/gsd-secure-phase 7.4` → close. **Logo not in PDF header** → known Chromium-header-image risk; fallback = move to a fixed-position body header or text-only (note in report-print-document.tsx). **Other** → it's plain CSS in `report-print-document.tsx PRINT_CSS` / `report-tokens.ts` — fast iteration.

---

## Plan 27 iter-3 (16→15 restructure + §2.5 merge + premium PDF v3) — 2026-05-15

Founder's 6-change brief. **Reverses the prior founder-locked "16-section / skip §15 / §2.5-as-own-section" decision** — memories updated accordingly (`project_phase_7_4_direction`, `iris_codex_seven_rules`, `iris_codex_quality_bar`, MEMORY index).

| Change | What |
|---|---|
| §2.5 → §2 merge | §2.5 "Sistemas em Bom Funcionamento" is gone as its own section. §2 now = opening ¶ + `### Sistemas que requerem atenção` + `### Sistemas em bom funcionamento` (two H3 subsections inside §2). |
| 16 → 15 renumber | Strictly sequential §1..§15. Síntese Rápida §16 → §15. **Key renames in jsonb**: `2_5_sistemas_funcionando_bem` REMOVED, `16_sintese_rapida` → `15_sintese_rapida`. NO back-compat shim → **every existing reading must be regenerated** or it renders blank for those keys. |
| Cover | Ivory (#F2EDE4) + LIGHT logo (logo-assets.ts now exports DARK + LIGHT base64), teal tagline/divider/label, dark client name, teal wordmark. |
| Heading | Single line `N — Title` (teal num + regular em-dash + bold title) + 1.5px teal rule. "SEÇÃO" eyebrow removed. |
| TOC | New "Índice" page after cover (number + title + dotted leader). **No page numbers** — Chromium/Gotenberg can't resolve section→page without Prince/2-pass (documented limitation in report-print-document.tsx header). |
| Footer | Cream plinth removed → transparent + teal hairline + `p. N / total`. Route bottom margin 0.6→0.45in. |
| §15 cards | Premium tinted dashboard (per-card accent + 4% bg tint, radius 10, shadow, small-caps label, teal `·` bullets). |

Surface touched (single source of truth = `lib/anthropic/types.ts`): types, parser, diff, analise.schemas, ReportReadView, AnalysisStream (+SECTION_TITLES now DERIVED), EditorAccordion (SECTIONS now DERIVED), EditorSectionItem, analise-client, report-print-document, route, system.md (numbering + Rule 3 + §2 spec + §15 + checklist), logo-assets, + 6 test fixture files rewritten.

Verification GREEN vs baseline: build ✓ compiled (eslint gated) · tsc 22 baseline/0 new · lint 0err/9warn · **test 631 passed** (parser/prompts/ReportReadView/EditorAccordion/AnalysisStream/report-print all green) + 3 quality-scoring baseline only.

**Plan 26 ACTION REQUIRED is STILL OPEN and still gates ALL PDF output** (see below): `pnpm remove @react-pdf/renderer`, stand up Gotenberg on Render, set `GOTENBERG_URL`+`GOTENBERG_BASIC_AUTH` in Vercel. Until then "Exportar PDF" → clean 503.

### UAT-iter-3 verification (founder)

After Render+env: regenerate `eb818f3c` (mandatory — old keys are dead). Expect: cover ivory+light logo; page 2 = Índice (15 entries); §1..§15 sequential; §2 with the two subsection subtitles; §14 letter; §15 tinted card dashboard; transparent teal-hairline footer. Web reading view also reflects 15-section model + §2 subsections.

---

## Plan 27 (premium editorial PDF redesign) — LANDED + PUSHED 2026-05-15 (`26ad14a`)

Luxury/editorial pass on the Gotenberg HTML template (Aesop/Monocle/fine-art direction). Pure CSS/HTML — the Gotenberg architecture (Plan 26) is unchanged; this is the "design evolves in one file" payoff.

- **Cover** (page 1, full bleed): black, inlined logo (`logo-assets.ts` — sharp-downscaled 460px base64; Gotenberg can't read `public/`), teal radial glow, tracked italic tagline, 30pt ivory client name, delicate date. Margins top/L/R = 0.
- **Internal**: white; section eyebrow (tracked uppercase teal) + 18pt title + full-width 1px teal rule; Georgia 11pt / lh 1.9; markdown blockquotes → teal pull quotes.
- **§14 Mensagem**: ivory letter treatment. **§16**: 2-col card grid, soft shadow, per-card brand-colour accent border, small-caps label.
- **Footer**: ivory plinth + teal hairline on every page (Gotenberg footer.html; bottom 0.6in band) — brand · client · condensed disclaimer · page numbers.
- Logos copied to `apps/web/public/logo/` (full-res, web/future use). Regenerate the base64: sharp resize 460px → base64 → `lib/pdf/logo-assets.ts` (the inline node script is in the `26ad14a` commit message context / Plan 27 notes).

Verify green vs baseline: build compiled ✓ · tsc 22/0-new · lint 0err/9warn · test 631 passed (report-print ✓) + 3 quality-scoring baseline.

**Pull-quote scope note:** blockquote STYLE only (markdown `>`), NOT auto-extracted from clinical prose — auto-pulling sentences from a health document risks distorting meaning. §14 letter + styled quotes carry the editorial voice safely. Founder can request a heuristic later if wanted.

**Claude could NOT produce a live PDF/screenshot** — needs the Render service + Vercel env + DB (`eb818f3c`), none available locally. Design is implemented + structurally unit-tested + build-verified; visual sign-off is founder UAT through the real pipeline.
**Working tree:** clean (only untracked non-project dirs: `.claude/`, `Estratégia comercial e mkt/`, `livros/` — ignore them)

---

## Plan 26 (PDF engine replacement) — LANDED 2026-05-15 (`19332be`)

@react-pdf/renderer failed 3 UAT rounds (AFM-font emoji crash + brittle bespoke markdown parser). Founder approved an architectural rebuild after research: **render the report as real HTML/CSS, convert via a Gotenberg (headless Chromium) service on Render.com.** Vercel's role = one `fetch()`; the engine runs where it actually can. WeasyPrint was researched and rejected (no CSS Grid → would break the §16 card grid; same Vercel system-lib fragility).

- `apps/web/lib/pdf/report-print-document.tsx` — server-only builder returning a full standalone HTML doc (inline CSS, react-markdown, §16 CSS grid). `react-dom/server` is **dynamically imported** (Next App Router hard-bans the static import).
- `app/api/readings/[id]/pdf/route.tsx` — renders HTML → multipart POST to `GOTENBERG_URL/forms/chromium/convert/html` (Basic auth, `footer.html` w/ page numbers + condensed disclaimer) → streams PDF back as attachment. 45s abort. `503` if `GOTENBERG_URL` unset, `502` if unreachable.
- `report-document.tsx` DELETED; `ExportPdfButton` unchanged (engine-agnostic — only GETs the route).
- `render.yaml` (repo root) — Gotenberg Blueprint + manual dashboard steps + Vercel env contract.
- `report-print-document.test.tsx` — 8 specs guard the source HTML (no live Gotenberg needed).

Verification green vs baseline + improved: build compiled (eslint gated) · tsc 22 baseline/0 new · lint 0err/9warn baseline · **test 629 passed (+8 new) + 3 quality-scoring baseline** · ReportReadView/ExportPdfButton tests ✓.

### ⚠️ ACTION REQUIRED before this works / deploys

1. **`pnpm remove @react-pdf/renderer`** — `pnpm install` was BLOCKED in Claude's env, so the dep was left in `package.json` (matches the lockfile so Vercel `--frozen-lockfile` still passes; nothing imports it — harmless but dead). One command, regenerates the lock atomically. Do this, commit the lock+package.json.
2. **Stand up Gotenberg on Render** (see `render.yaml`): New Web Service → image `docker.io/gotenberg/gotenberg:8` → start cmd `gotenberg --api-port 3000 --api-enable-basic-auth` → health `/health` → Starter ($7, always-on) → set `GOTENBERG_API_BASIC_AUTH_USERNAME`/`_PASSWORD`.
3. **Vercel env vars** (Project Settings → Environment Variables, all envs): `GOTENBERG_URL=https://<service>.onrender.com` and `GOTENBERG_BASIC_AUTH=<user>:<pass>` (same pair). Redeploy.
4. **`git push`** — Plan 26 (`19332be`) is committed locally but NOT pushed (push held: deploy will 503 until steps 1–3 are done; pushing before env is set just ships a 503-until-configured PDF button). Push after env is wired, OR push now knowing the button returns a clean 503 "PDF service not configured" until Render+env exist.

### UAT-6 resume protocol

1. After steps 1–4: confirm Vercel deployed the new commit (deployment hash) + Render service is live (`GET /health` → 200).
2. Regenerate `eb818f3c-23ce-4817-9e26-7d1ab6ab3b71`, click "Exportar PDF".
3. Expect: direct file download (no print dialog); clean serif A4; §16 as a real coloured card grid; **emoji now render** (Chromium, unlike Times-Roman); brand header; per-page footer w/ page numbers + disclaimer line; paragraph breathing.
4. Verdict:
   - **PASS** → I remove the temp `console.warn` left in… (n/a — that was Plan 25's react-pdf path, now deleted) → `/gsd-verify-phase 7.4` → `/gsd-secure-phase 7.4` → close Phase 7.4.
   - **Layout/styling tweak** → it's now plain CSS in `report-print-document.tsx` `PRINT_CSS` — fast iteration, no engine risk.
   - **Gotenberg 502/503** → check Render service up + Vercel env + basic-auth pair match; route logs the gotenberg status.

### Open follow-ups

- `pnpm remove @react-pdf/renderer` (above) — the only thing keeping the dead dep around.
- Future (founder's stated roadmap, all now trivial in `PRINT_CSS`/the HTML): therapist logo in header, per-section icons, brand colour accents, client/iris image embed. These were the reason react-pdf was abandoned.
- The Plan 25 react-pdf root-cause memories (`pdf-times-roman-winansi`) are now historical — react-pdf is gone.

---

## Plan 25 (UAT-5b root-cause fixes) — LANDED + PUSHED 2026-05-15 (`9f9e2f6`, `7707c6f`)

UAT-5b: BOTH Plan-24 fixes failed identically. Re-investigation found the **real** root causes (Plan 24 fixes were aimed at the wrong mechanism):

| Issue | Plan 24 (wrong target) | Plan 25 — REAL root cause + fix |
|---|---|---|
| PDF `unsupported number: 1.92e+21` (exact same number every render) | hyphenation + 15-digit compaction (assumed long token) | **Times-Roman is a Standard-14 AFM font → WinAnsi codepoints only. §16 emoji labels (🔴🟢💛✨🧭🌱) have no advance width → @react-pdf/textkit throws.** Fix: `sanitizeText` strips `NON_RENDERABLE_RE` (emoji/pictographs/VS/ZWJ/arrows) from all PDF text + temp `console.warn` diagnostic + `maxWidth:'100%'` guards. See MEMORY `pdf-times-roman-winansi`. |
| Web paragraph spacing "not applied" | `prose-p:mb-6` (assumed CSS tweak) | **`prose` is 100% INERT — Tailwind v4, NO @tailwindcss/typography plugin anywhere. All prose-* = zero CSS; preflight zeroes `<p>` margin.** Fix: `markdownComponents(variant)` explicit `components` on all 3 ReactMarkdown surfaces; dropped inert prose; added `data-testid="section-markdown"` + repointed tests. See MEMORY `tailwind-prose-inert`. |

Verification green vs baseline (build exit 0 + eslint gated · tsc 22 baseline/0 new · lint 0err/9warn · test 621 pass + 3 quality-scoring baseline · ReportReadView.test ✓).

**Confirmed:** Plan 24 (`050b2ca`) WAS on origin during UAT-5b — founder tested real deployed code, so the root-cause re-analysis was necessary (not a deploy artifact). Commits auto-reach origin this repo.

### UAT-5c resume protocol (founder — Claude CANNOT do this)

1. **FIRST: confirm Vercel deployed `7707c6f`** (check deployment hash in Vercel dashboard). Testing a stale/cached deployment will reproduce the old errors — this is the #1 false-negative risk.
2. Regenerate reading `eb818f3c-23ce-4817-9e26-7d1ab6ab3b71` (button "Regenerar análise"). Needs DB + server (absent in Claude's local env).
3. Verify:
   - **PDF**: "Exportar PDF" downloads with NO `unsupported number` 500. §16 labels show as text WITHOUT the emoji (Fragilidades / Forças / etc — Times-Roman can't draw emoji; this is expected & correct). Paragraphs visibly separated.
   - **Web reading view**: paragraphs have clear blank-line separation; lists have bullets/numbers + indentation (prose no longer relied upon).
   - Re-check the 7 original UAT-4 criteria + Plan-24 language (Rules 8 & 9: no §-refs, no unexplained jargon in §5/§6/§10/§13).
4. Verdict paths:
   - **PASS** → tell Claude → it removes the temp `console.warn` diagnostic from `report-document.tsx` (cleanup), commits, then `/gsd-verify-phase 7.4` → `/gsd-secure-phase 7.4` → close Phase 7.4.
   - **PDF still 500 with a NEW codepoint** → server logs now print `[pdf.sanitize] stripped ... U+XXXX @ §N`; widen `NON_RENDERABLE_RE` with the logged range.
   - **§3 still generic** → D-DEF-14-01 corpus gap → Phase 7.2-H (not prompt iteration).
   - **Other** → spawn next targeted plan.

### Open follow-ups (new this round)

- **TEMP DIAGNOSTIC** — `console.warn` in `report-document.tsx` `sanitizeText()` is intentionally shipped for UAT-5c log evidence. **Remove after UAT-5c PASS.**
- **EditorAccordion/EditorSectionItem** still use inert `prose` (therapist edit-mode preview) — same bug as the reading view, left out of UAT-5b scope. Fix with the same `markdownComponents` pattern if edit-mode spacing is ever reported.
- If the founder wants emoji visible IN the PDF (not stripped), that needs an embedded emoji/icon font via `Font.register` — Times-Roman fundamentally can't.

---

## Plan 24 (UAT-5 fix batch) — LANDED 2026-05-15 (2 commits, NOT pushed)

Founder ran UAT-5 and issued 4 fixes in 2 messages. All code landed; verification green vs baseline (build 0 · tsc 22 baseline/0 new · lint 0err/9warn · test 621 pass + 3 quality-scoring baseline).

| Fix | Where | Commit |
|---|---|---|
| PDF FIX 1 — `unsupported number: 1.92e+21` crash. Root cause: hyphenation callback `(word)=>[word]` made every token unbreakable → huge number run overflowed Yoga. Fix: callback chunks tokens >32 chars + `sanitizeText()` compacts 15+digit runs to `toExponential(2)` (founder's sanitizeValue intent), applied to all dynamic text | `lib/pdf/report-document.tsx` | `0cc7b81` |
| PDF FIX 2 — paragraph breathing. PDF para/list/blockquote marginBottom 8→12, sectionHeading marginTop 24→20. Web `ReportReadView` main prose `prose-p:my-3`→`mb-6/mt-0`, §16 cards `my-2`→`mb-4/mt-0` | `report-document.tsx` + `ReportReadView.tsx` | `0cc7b81` |
| PROMPT FIX 1 — Rule 8: never cite other sections by number/code (§3, Marcador 1) in body; name the concept directly. All 16 sections | `prompts/system.md` | `97cd813` |
| PROMPT FIX 2 — Rule 9: no unexplained clinical jargon; END CLIENT reads the report (not therapist). Replace or explain inline. Verbatim founder substitutions included | `prompts/system.md` | `97cd813` |

**system.md absolute-rules block is now 9 rules** (was 7). MEMORY `feedback_iris_codex_seven_rules.md` updated — Rules 8 & 9 + the END-CLIENT reframing override the older "leitor é terapeuta" lines (intentional; don't "fix" them).

### UAT-5b resume protocol (founder verification — Claude CANNOT do this)

1. Founder regenerates reading `eb818f3c-23ce-4817-9e26-7d1ab6ab3b71` (button "Regenerar análise") — needs DB + server (absent in Claude's local env: MODAL/DB creds empty).
2. Verify Plan-24 fixes:
   - **PDF**: "Exportar PDF" downloads WITHOUT the `unsupported number` 500; paragraphs visibly separated (generous breathing); §16 card grid intact.
   - **Language §5/§6/§10/§13** (highest §-ref + jargon surfaces): NO "§3"/"Marcador 1"/cross-section codes in body; NO unexplained jargon (vinculação primária, hipervigilância afetiva, eixo psicossomático, suprarrenal, colarete, constituição X-gênica all plain or explained inline).
   - Re-check the 7 original UAT-4 criteria (below) still hold.
3. Verdict paths:
   - **PASS** → `/gsd-verify-phase 7.4` → `/gsd-secure-phase 7.4` → close Phase 7.4. (Founder may also want `git push` — NOT done by Claude.)
   - **§3 still generic** → D-DEF-14-01 corpus gap → Phase 7.2-H (NOT another prompt iteration).
   - **Specific feedback** → spawn Plan 25 targeted fix.

**NOT pushed** — Plan 24 commits `0cc7b81`+`97cd813` are local on `main`. Pushing is a founder decision.

---

## Where we are

Phase 7.4 went through TWO founder UAT rounds this session, each spawning a batch of plans. ALL CODE IS LANDED. The ONLY pending item is **founder UAT-5 manual verification** — I cannot regenerate or judge LLM output myself.

### Pivot/feedback history (full context)

1. **7.3 → 7.4 (2026-05-13 morning)** — 13-section dual-layer ABANDONED → 8-block JSON. Plans 00–09.
2. **7.4 → 7.4-redirection (2026-05-13 evening)** — 8-block ABANDONED → 14 markdown sections. Plans 10–14.
3. **UAT-3 (2026-05-14 evening)** — founder rejected §3/§10/§13 still leaking author/school/sector citations + missing positive systems + accordion-only UX. Plans 16–19.
4. **UAT-4 (2026-05-15)** — founder issued 6 fixes (§ removal, §1 breathing, PDF direct download, §16 Síntese Rápida, §12 numbered, counter 16). Plans 21–23.

### Plans 16–23 — ALL COMPLETE (19 commits this session)

| Plan | What | Commits |
|---|---|---|
| 16 | system.md — 7 absolute rules + §2.5 + §3 4-field + §10 symbolic + §13 human + §1 polish | 5a65ef7, 6017d76, 447eace |
| 17 | 15-section plumbing — types/parser/diff/schemas + AnalysisStream/EditorAccordion/EditorSectionItem/analise-client (decimal §2.5 + array-index monotonicity) | fb650c9, 6439214, 4a1158d |
| 18 | ReportReadView (continuous flowing serif) + ReadingModeActions + page.tsx reading-mode routing flip; accordion now ONLY on /editar | 824ad23, 0c0a14d, b469fbf |
| 19 | PDF via Print CSS (SUPERSEDED by Plan 23) | bc451eb, 780e63d, 8f2a137 |
| 21 | system.md UAT-4 — § removal in headings + §1 short-paragraph breathing + extended polish table + §12 numbered list + NEW §16 Síntese Rápida | d71ed21 |
| 22 | Code plumbing — §16 + § removal in renderers + counter 15→16; ReportReadView §16 card-grid (parseSubsections) | 2870d6d, dcc0f20, 16ac8ce |
| 23 | @react-pdf/renderer install + lib/pdf/report-document.tsx + /api/readings/[id]/pdf route + ExportPdfButton refactor (Button+fetch+blob) + /print route DELETED | 91cf1e5, 25cecb9, 056d9f5 |

**Verification (last full run):** pnpm build exit 0 · tsc 22 baseline (zero new — D-DEF-09-03 readings.test×12 + ReprocessButton×3 + quality-scoring×2 + modal-client×5) · lint 0 errors + 9 baseline warnings · pnpm test:run 621 passed + 3 baseline failures (quality-scoring×3 ONLY per MEMORY feedback_quality_scoring_test_gate.md).

---

## Current canonical shape (post-UAT-4)

- **16 markdown sections** emitted by Sonnet as `## N. Title` (NO § symbol — Plan 21) where N ∈ {1, 2, 2.5, 3..14, 16}. **§15 is SKIPPED** (founder explicit numbering choice — §16 follows §14 directly).
- Parser `lib/anthropic/parser.ts findAllBoundaries`: regex `(\d{1,2}(?:\.\d)?)` + optional `§?` + `[\p{Pd}.]` separator → accepts BOTH new `## N. Title` AND legacy `## §N — Title`. Monotonicity via array index against `NUMBERED_SECTION_HEADINGS` (16 entries, includes '16', skips '15').
- Storage: `readings.report_generated`/`report_delivered` jsonb keyed by `ReportSectionKey` (16 numbered keys + `encerramento_disclaimer`). New key: `16_sintese_rapida`.
- **Reading mode (default)**: `/leituras/[id]` → `ReportReadView` (continuous flowing serif, max-w-prose). §16 renders as 6-card grid (`parseSubsections` splits `### 🔴 Fragilidades` etc). Top buttons: Exportar PDF + Editar análise + Entregar ao cliente + Regenerar (via `ReadingModeActions`).
- **Edit mode**: `/leituras/[id]/editar` → `EditorAccordion` (accordion ONLY here).
- **PDF**: `ExportPdfButton` → fetch `GET /api/readings/[id]/pdf` → `renderToBuffer(<ReportDocument>)` (@react-pdf/renderer 4.5.1) → blob download `Leitura-{Name}-{YYYY-MM-DD}.pdf`. `/print` route DELETED.

---

## Resume protocol — Plan 24 (UAT-5)

1. Read `.planning/STATE.md` frontmatter `status:` (most recent entry = Plan 23 record).
2. Read `MEMORY.md` index — load `project_phase_7_4_direction.md`, `iris_codex_quality_bar.md`, `feedback_iris_codex_seven_rules.md` (all updated this session).
3. Ask the founder for the UAT-5 verdict. They must FIRST re-regenerate the Nailli reading (button "Regenerar análise" on `/leituras/<nailli-id>`), then verify the **7 UAT-4 acceptance criteria**:
   1. Section titles show `1. Constituição e Temperamento` (NO §)
   2. §1 short paragraphs, NO "colarete"/"fibrilar"/"hepatobiliar"/"sinais setoriais"/"organização funcional de base"
   3. "Exportar PDF" triggers DIRECT FILE DOWNLOAD (not browser print dialog)
   4. PDF opens as clean serif document with "Iris Codex" brand mark + footer disclaimer on every page
   5. §16 Síntese Rápida shows 6 labeled blocks (🔴/🟢/💛/✨/🧭/🌱) SPECIFIC to Nailli, not generic
   6. §12 questions numbered 1. 2. 3. as proper ordered list
   7. Counter shows `{n}/16 seções` during streaming
4. Three verdict paths:
   - **`approved` / UAT-5 PASS** → run `/gsd-verify-phase 7.4` → `/gsd-secure-phase 7.4` → mark Phase 7.4 complete. THIS is the close path.
   - **`§3 still generic`** → most likely D-DEF-14-01 (Cronorichio corpus gap). Open Phase 7.2-H plan: ingest Lo Rito Cronorichio + Jensen biographical + Brazilian biographical iris-map literature (~$1 ingest). Note: §3 4-field template (Plan 16/21) hides un-anchored markers via skip-rather-than-fabricate, so generic §3 = genuine corpus gap, not prompt gap.
   - **Specific section/PDF feedback** → spawn Plan 25 with a targeted fix. Founder will name the section/surface + the issue.

---

## What NOT to do

- ❌ Don't reintroduce the `§N — Title` heading format (Plan 21 removed it; parser still accepts legacy for backward-compat with pre-Plan-21 reports, but the prompt + renderers emit `N. Title`).
- ❌ Don't reintroduce the `/print` route or PrintTrigger (Plan 23 deleted; PDF is now server-side direct download).
- ❌ Don't emit §15 — the sequence is {1, 2, 2.5, 3..14, 16}. `NUMBERED_SECTION_HEADINGS` deliberately excludes '15'.
- ❌ Don't reintroduce 8-block JSON (`tendency_grade`, `systems_with_tendency`, `zodOutputFormat`) — canonically dead per DC-9.
- ❌ Don't auto-spawn Phase 7.5 (tendency mapping engine) — DEFERRED, IP shape needs re-discussion.
- ❌ Don't implement the §11 Afirmações toggle (DC-5 — still deferred; founder hasn't requested post-UAT).
- ❌ Don't build the paid "Análise Aprofundada" V1.1 technical tab — stays as `AdvancedAnalysisCTA.tsx` placeholder.

---

## Open follow-ups (deferred-items.md + new)

- **D-DEF-09-03** — 22 pre-existing tsc errors in 4 test files (Phase 3/5 typing debt). Out of scope. Baseline — do NOT gate on these.
- **D-DEF-09-04** — ~45 pre-existing audit-vocabulary hits (Jensen RAG metadata + Cronorichio section-queries + VocabularyAuditBanner display + diagnóstico debug comments). Out of scope. system.md is allowlisted (line 1 marker) — Plan 16/21/23 changes did NOT add new hits.
- **D-DEF-14-01** — Cronorichio + biographical-map chunks may be ABSENT from current RAG corpus. If UAT-5 §3 still feels generic → Phase 7.2-H corpus expansion is the remediation (NOT another prompt iteration — the §3 4-field template already hides un-anchored markers).
- **NEW (Plan 23)** — PDF inline markdown formatting (bold/italic/code) is STRIPPED in v1 (structure-only render). If founder wants inline styling in PDF, follow-up plan adds a richer @react-pdf tokenizer.
- **NEW (Plan 18)** — AnalysisStream + EditorAccordion still inline their own SECTION_TITLES arrays; could DRY against `SECTION_TITLE_BY_NUMBER` in types.ts. Cosmetic; not blocking.

---

## Critical anti-patterns (none blocking resume)

No `blocking` anti-patterns. Standard project memory rules apply (all auto-loaded from MEMORY.md):
- Worktree cleanup on Windows uses bash `rm -rf`
- `'use server'` files only export async functions
- `safeArray()` before jsonb iteration
- Run `pnpm lint` before pushing (next build gates eslint)
- Plain `git commit` for source files (gsd-tools.cjs filters to .planning/)
- 3 quality-scoring.test.ts failures are PRE-EXISTING baseline — never gate on them
- The 7 Iris Codex absolute rules + the GLOBAL CALIBRATION RULE are LOCKED invariants for any future prompt iteration
