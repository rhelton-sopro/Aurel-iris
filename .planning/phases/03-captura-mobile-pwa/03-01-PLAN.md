---
plan_id: 03-01
wave: 0
depends_on: []
phase: 3
phase_slug: captura-mobile-pwa
objective: "Wave 0: vitest + scripts + migration 0004 (bucket privado iris-captures + RLS + unique constraint reading_images) + types regen + audit:vocabulary + teste RLS de Storage"
requirements: [CAPTURE-06]
files_modified:
  - apps/web/package.json
  - apps/web/vitest.config.ts
  - apps/web/tests/setup.ts
  - apps/web/tests/smoke.test.ts
  - supabase/migrations/0004_storage_bucket_iris_captures.sql
  - supabase/tests/storage_cross_therapist_rls.sql
  - apps/web/types/database.ts
must_haves:
  truths:
    - "vitest run executa green com pelo menos 1 teste smoke passando"
    - "Bucket Storage 'iris-captures' existe no remoto, é privado (public=false), file_size_limit=10485760, allowed_mime_types contém 'image/jpeg'"
    - "Storage RLS bloqueia cross-terapeuta: terapeuta A não consegue SELECT em arquivo de terapeuta B"
    - "Constraint UNIQUE (reading_id, eye, angle) existe em reading_images — segundo INSERT no mesmo slot falha"
    - "Script audit:vocabulary existe e exit-code zero indica ausência de 'diagnóstico|tratamento|cura' em apps/web/app e apps/web/components"
    - "types/database.ts contém typing para constraint reading_images_reading_eye_angle_unique (regerado após push)"
  artifacts:
    - path: "apps/web/vitest.config.ts"
      provides: "Configuração vitest com jsdom + setup file + path alias @/"
    - path: "apps/web/tests/setup.ts"
      provides: "Mocks globais (matchMedia, ResizeObserver) + import @testing-library/jest-dom"
    - path: "apps/web/tests/smoke.test.ts"
      provides: "Smoke test que verifica que vitest está vivo (1+1=2)"
    - path: "supabase/migrations/0004_storage_bucket_iris_captures.sql"
      provides: "Bucket iris-captures + 4 policies (insert/select/update/delete) + UNIQUE constraint reading_images(reading_id, eye, angle)"
      contains: "iris-captures"
    - path: "supabase/tests/storage_cross_therapist_rls.sql"
      provides: "Teste cross-terapeuta de RLS de storage.objects (paralelo a cross_therapist_rls.sql da Fase 1)"
  key_links:
    - from: "apps/web/package.json scripts"
      to: "vitest CLI"
      via: "scripts test, test:run, test:changed, audit:vocabulary"
      pattern: "\"test:run\":\\s*\"vitest run\""
    - from: "supabase/migrations/0004_storage_bucket_iris_captures.sql"
      to: "storage.objects RLS"
      via: "policy WITH CHECK (auth.uid()::text = (storage.foldername(name))[1])"
      pattern: "storage\\.foldername\\(name\\)\\)\\[1\\]"
autonomous: true
threat_model:
  - id: T-03-01-01
    threat: "Storage RLS bypass — terapeuta B baixa fotos de íris do terapeuta A (dado biométrico LGPD)"
    mitigation: "Policy folder-based em storage.objects com auth.uid()::text = (storage.foldername(name))[1] em SELECT/INSERT/UPDATE/DELETE; teste cross-terapeuta empírico em supabase/tests/storage_cross_therapist_rls.sql roda como gate da migration"
  - id: T-03-01-02
    threat: "Bucket público acidental — qualquer URL fica exposta sem auth"
    mitigation: "INSERT INTO storage.buckets com public=false explícito + comentário inline justificando + teste assert public=false"
  - id: T-03-01-03
    threat: "Slot duplicado em reading_images — tap-to-redo (D-09) duplicaria linhas sem unique constraint"
    mitigation: "ALTER TABLE reading_images ADD CONSTRAINT ... UNIQUE (reading_id, eye, angle); upsert client-side usa onConflict='reading_id,eye,angle'"
  - id: T-03-01-04
    threat: "Vazamento de vocabulário proibido em código (LGPD-06 forward) — qualquer arquivo novo poderia conter 'diagnóstico/tratamento/cura'"
    mitigation: "Script pnpm run audit:vocabulary executa grep cego em apps/web/app e apps/web/components; rodado em pre-commit hook (futuro) e em CI gate de cada plan"
created: 2026-05-01
---

<objective>
Estabelecer Wave 0 da Fase 3: instalar e configurar vitest (primeiro test runner do projeto), adicionar scripts canônicos ao package.json, criar migration `0004_storage_bucket_iris_captures.sql` que materializa o bucket privado de Storage com RLS folder-based e a unique constraint em `reading_images(reading_id, eye, angle)` que viabiliza tap-to-redo (D-09), criar teste SQL de RLS cross-terapeuta paralelo ao da Fase 1, regenerar `types/database.ts` e adicionar script `audit:vocabulary` para gate LGPD.

Purpose: prover o "chão" técnico para todos os outros 7 plans desta fase. Sem vitest, todos os testes unitários propostos viram MISSING. Sem o bucket de Storage, plan 03-07 não tem destino para upload. Sem unique constraint, retake (D-09) duplicaria linhas em vez de sobrescrever.

Output: vitest 100% verde com 1 smoke test, bucket `iris-captures` no remoto Supabase com 4 policies RLS, constraint unique aplicada, types regerados, script audit:vocabulary executável, teste cross-terapeuta de Storage idempotente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-captura-mobile-pwa/03-CONTEXT.md
@.planning/phases/03-captura-mobile-pwa/03-RESEARCH.md
@.planning/phases/03-captura-mobile-pwa/03-VALIDATION.md
@apps/web/package.json
@supabase/migrations/0001_initial_schema.sql
@supabase/migrations/0002_grant_authenticated_role.sql
@supabase/migrations/0003_profiles_trigger.sql
@supabase/tests/cross_therapist_rls.sql
@supabase/tests/run-rls-test.mjs

<interfaces>
<!-- Schema atual em reading_images (0001_initial_schema.sql linhas 64-76) — sem unique constraint hoje -->
```sql
create table reading_images (
  id uuid primary key default gen_random_uuid(),
  reading_id uuid references readings(id) on delete cascade not null,
  eye text not null,                     -- left | right
  angle text not null,                   -- frontal | lateral | backlight
  storage_path text not null,
  quality_score float,
  width int,
  height int,
  created_at timestamptz default now()
);
```

<!-- Convenção de policy nested já aplicada em reading_images (0001_initial_schema.sql linhas 136-144) -->
```sql
create policy "Terapeutas só veem imagens de suas próprias leituras"
  on reading_images for all
  using (
    exists (
      select 1 from readings
      where readings.id = reading_images.reading_id
        and readings.therapist_id = auth.uid()
    )
  );
```

<!-- Padrão de impersonação para teste RLS (cross_therapist_rls.sql linhas 100-120) -->
```sql
select set_config(
  'request.jwt.claims',
  json_build_object('sub', '11111111-1111-1111-1111-111111111111', 'role', 'authenticated')::text,
  true
);
set local role authenticated;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar vitest + jsdom, criar config e smoke test, adicionar scripts</name>
  <read_first>
    - apps/web/package.json (estado atual de deps e scripts)
    - apps/web/tsconfig.json (precisa do path alias @/ para ser respeitado pelo vitest)
    - apps/web/eslint.config.mjs (verificar se ignora *.test.ts)
    - .planning/phases/03-captura-mobile-pwa/03-VALIDATION.md (Wave 0 Requirements)
    - .planning/phases/03-captura-mobile-pwa/03-RESEARCH.md (linhas 1620-1665 — Validation Architecture / Wave 0 Gaps)
  </read_first>
  <action>
1. **Instalar deps** (em `apps/web/`):
   ```bash
   cd apps/web && pnpm add -D vitest@^2 @vitejs/plugin-react @testing-library/react@^16 @testing-library/jest-dom@^6 @testing-library/user-event@^14 jsdom@^25
   ```
   Versões alvo: vitest 2.x (estável), @testing-library/react 16.x (compatível com React 19), jsdom 25.x. Se uma versão major recente surgiu desde 2026-04-30, usar a major estável corrente (não major candidate).

2. **Criar `apps/web/vitest.config.ts`** (idêntico em estrutura — verbatim):
   ```typescript
   import { defineConfig } from 'vitest/config'
   import react from '@vitejs/plugin-react'
   import path from 'node:path'

   export default defineConfig({
     plugins: [react()],
     test: {
       environment: 'jsdom',
       globals: true,
       setupFiles: ['./tests/setup.ts'],
       include: ['**/*.{test,spec}.{ts,tsx}'],
       exclude: ['**/node_modules/**', '**/.next/**'],
     },
     resolve: {
       alias: {
         '@': path.resolve(__dirname, '.'),
       },
     },
   })
   ```

3. **Criar `apps/web/tests/setup.ts`** (mocks globais — verbatim):
   ```typescript
   import '@testing-library/jest-dom/vitest'
   import { vi } from 'vitest'

   // jsdom não implementa matchMedia (usado por hooks/use-mobile)
   Object.defineProperty(window, 'matchMedia', {
     writable: true,
     value: vi.fn().mockImplementation((query: string) => ({
       matches: false,
       media: query,
       onchange: null,
       addEventListener: vi.fn(),
       removeEventListener: vi.fn(),
       dispatchEvent: vi.fn(),
     })),
   })

   // jsdom não implementa ResizeObserver
   class ResizeObserverMock {
     observe() {}
     unobserve() {}
     disconnect() {}
   }
   globalThis.ResizeObserver = ResizeObserverMock as unknown as typeof ResizeObserver
   ```

4. **Criar `apps/web/tests/smoke.test.ts`** (smoke verbatim — não removê-lo: serve como prova-de-vida do runner):
   ```typescript
   import { describe, it, expect } from 'vitest'

   describe('vitest smoke', () => {
     it('runs', () => {
       expect(1 + 1).toBe(2)
     })
   })
   ```

5. **Adicionar scripts em `apps/web/package.json`** — inserir no objeto `scripts` (manter os existentes intactos):
   ```json
   "test": "vitest",
   "test:run": "vitest run",
   "test:changed": "vitest run --changed",
   "audit:vocabulary": "node -e \"const{execSync}=require('node:child_process');try{const out=execSync('grep -rniE \\\"diagnóstico|tratamento|cura\\\" app components',{encoding:'utf8'});if(out.trim()){console.error('VOCAB FAIL:\\n'+out);process.exit(1)}}catch(e){if(e.status===1){console.log('OK: vocabulário proibido ausente');process.exit(0)}console.error(e.stderr||e.message);process.exit(1)}\""
   ```

   Nota sobre `audit:vocabulary`: usa Node embedded para portabilidade (Windows + Unix). `grep -E` retorna exit 1 quando não encontra matches — script captura isso como sucesso. Quando encontra matches, exit 0 do grep vira exit 1 do script (failure), copiando matches para stderr.

6. **Atualizar `.gitignore`** em `apps/web/.gitignore` (criar se não existir) para incluir:
   ```
   coverage/
   public/sw.js
   public/sw.js.map
   public/swe-worker-*.js
   ```
   (`public/sw.js` será gerado pelo Serwist em plan 03-02; preempt agora.)
  </action>
  <verify>
    <automated>cd apps/web && pnpm install && pnpm run test:run && pnpm run audit:vocabulary</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/package.json` contém `"vitest"` em devDependencies (verificar com `grep -c '"vitest":' apps/web/package.json` >= 1)
    - `apps/web/vitest.config.ts` existe e contém literal `environment: 'jsdom'`
    - `apps/web/tests/setup.ts` existe e contém literal `'@testing-library/jest-dom/vitest'`
    - `apps/web/tests/smoke.test.ts` existe e contém literal `expect(1 + 1).toBe(2)`
    - `cd apps/web && pnpm run test:run` exit code 0; output contém "1 passed" ou "1 test passed"
    - `apps/web/package.json` scripts contém entradas `test`, `test:run`, `test:changed`, `audit:vocabulary` (verificar via grep `"audit:vocabulary":`)
    - `cd apps/web && pnpm run audit:vocabulary` exit code 0 com mensagem "OK: vocabulário proibido ausente"
    - `.gitignore` em apps/web contém linha `public/sw.js`
  </acceptance_criteria>
  <done>vitest verde com smoke test; scripts test/test:run/test:changed/audit:vocabulary funcionais; .gitignore preparado para Serwist.</done>
</task>

<task type="auto">
  <name>Task 2: Criar migration 0004 — bucket iris-captures + 4 RLS policies + unique constraint reading_images</name>
  <read_first>
    - supabase/migrations/0001_initial_schema.sql (linhas 64-76 — schema atual de reading_images; linhas 105-148 — padrão de RLS e policies existentes)
    - supabase/migrations/0002_grant_authenticated_role.sql (padrão de migration aditiva idempotente)
    - supabase/migrations/0003_profiles_trigger.sql (padrão de header de comentário)
    - supabase/config.toml (verificar se [storage.buckets.images] está comentado — confirmação RESEARCH §1041)
    - .planning/phases/03-captura-mobile-pwa/03-RESEARCH.md (linhas 1037-1097 — Storage Migration template + RLS folder pattern)
    - .planning/phases/03-captura-mobile-pwa/03-CONTEXT.md (D-storage path convention `{therapist_id}/{reading_id}/{eye}_{angle}.jpg`)
  </read_first>
  <action>
Criar `supabase/migrations/0004_storage_bucket_iris_captures.sql` com o conteúdo abaixo (literal, sem improvisar):

```sql
-- supabase/migrations/0004_storage_bucket_iris_captures.sql
-- Fase 3 — Captura mobile (PWA): infraestrutura de Storage e constraint para retake.
--
-- O que esta migration cobre:
--   1) Cria bucket privado "iris-captures" (não foi criado pela Fase 1 — verificado em
--      supabase/config.toml linha 115 onde [storage.buckets.images] está comentado, e em
--      0001..0003 onde nenhum INSERT INTO storage.buckets existe).
--   2) Aplica 4 policies RLS folder-based em storage.objects para INSERT/SELECT/UPDATE/DELETE
--      (per CONTEXT D-storage: path = {therapist_id}/{reading_id}/{eye}_{angle}.jpg —
--      auth.uid() = primeiro segmento da pasta).
--   3) Adiciona UNIQUE constraint (reading_id, eye, angle) em reading_images, viabilizando
--      upsert tap-to-redo (CONTEXT D-09) sem duplicar linhas.
--
-- Idempotente: pode ser re-aplicada sem erro (on conflict do nothing, drop policy if exists,
-- alter table ... if not exists no constraint via DO block).

-- ============================================================================
-- 1) Bucket privado iris-captures
-- ============================================================================
insert into storage.buckets (id, name, public, file_size_limit, allowed_mime_types)
values (
  'iris-captures',
  'iris-captures',
  false,                             -- privado: jamais public=true (LGPD: dado biométrico)
  10485760,                          -- 10MB hard limit (D-16 alvo é ~500KB; 10MB é teto de segurança)
  array['image/jpeg']                -- D-16 prescreve JPEG
)
on conflict (id) do nothing;

-- ============================================================================
-- 2) Policies de RLS em storage.objects
-- Convenção CONTEXT D-storage: path = {therapist_id}/{reading_id}/{eye}_{angle}.jpg
-- → primeiro segmento da pasta = uuid do terapeuta = auth.uid()
-- ============================================================================

-- Drop policies anteriores (idempotência em re-runs)
drop policy if exists "Terapeutas inserem fotos em sua própria pasta" on storage.objects;
drop policy if exists "Terapeutas leem suas próprias fotos de íris" on storage.objects;
drop policy if exists "Terapeutas atualizam suas próprias fotos de íris" on storage.objects;
drop policy if exists "Terapeutas removem suas próprias fotos de íris" on storage.objects;

-- INSERT
create policy "Terapeutas inserem fotos em sua própria pasta"
  on storage.objects for insert
  to authenticated
  with check (
    bucket_id = 'iris-captures'
    and auth.uid()::text = (storage.foldername(name))[1]
  );

-- SELECT
create policy "Terapeutas leem suas próprias fotos de íris"
  on storage.objects for select
  to authenticated
  using (
    bucket_id = 'iris-captures'
    and auth.uid()::text = (storage.foldername(name))[1]
  );

-- UPDATE (necessária para upsert: true no supabase-js — RESEARCH §Storage Upload + D-09 retake)
create policy "Terapeutas atualizam suas próprias fotos de íris"
  on storage.objects for update
  to authenticated
  using (
    bucket_id = 'iris-captures'
    and auth.uid()::text = (storage.foldername(name))[1]
  )
  with check (
    bucket_id = 'iris-captures'
    and auth.uid()::text = (storage.foldername(name))[1]
  );

-- DELETE (necessária para discardReadingAction — CONTEXT D-13)
create policy "Terapeutas removem suas próprias fotos de íris"
  on storage.objects for delete
  to authenticated
  using (
    bucket_id = 'iris-captures'
    and auth.uid()::text = (storage.foldername(name))[1]
  );

-- ============================================================================
-- 3) UNIQUE constraint em reading_images(reading_id, eye, angle)
-- Viabiliza upsert tap-to-redo (CONTEXT D-09) sem duplicar linhas.
-- DO block em vez de IF NOT EXISTS porque ALTER TABLE ... ADD CONSTRAINT IF NOT EXISTS
-- não é suportado em todas as versões do Postgres.
-- ============================================================================
do $$
begin
  if not exists (
    select 1
    from pg_constraint
    where conname = 'reading_images_reading_eye_angle_unique'
  ) then
    alter table reading_images
      add constraint reading_images_reading_eye_angle_unique
      unique (reading_id, eye, angle);
  end if;
end $$;
```

Após criar o arquivo, **aplicar no remoto**:

```bash
cd /d/Projetos/Iridologista && supabase db push --linked
```

Após push bem-sucedido, **regenerar `apps/web/types/database.ts`**:

```bash
cd apps/web && pnpm run gen:types
```

Verificar que o arquivo regerado é válido TypeScript (não vazio, não com erro):
```bash
head -20 apps/web/types/database.ts
```

Se `gen:types` falhar (geralmente por estar não-linkado ou env var ausente), abortar e reportar — não fazer mock manual.
  </action>
  <verify>
    <automated>cd /d/Projetos/Iridologista && supabase db push --linked --dry-run 2>&1 | grep -E "0004_storage_bucket_iris_captures" && cd apps/web && pnpm run gen:types && grep -c "reading_images" types/database.ts</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/0004_storage_bucket_iris_captures.sql` existe
    - Arquivo contém literal `'iris-captures'` (verificar via grep)
    - Arquivo contém literal `auth.uid()::text = (storage.foldername(name))[1]` em pelo menos 4 ocorrências (uma por policy)
    - Arquivo contém literal `reading_images_reading_eye_angle_unique`
    - Arquivo contém literal `public, false` ou equivalente que assegura public=false (verificar grep `public.*false`)
    - `supabase db push --linked` aplicada com sucesso (sem erro de sintaxe ou conflito)
    - `apps/web/types/database.ts` regerado e contém typing para `reading_images` (grep `reading_images` retorna >= 1)
    - Verificação no remoto via Supabase Studio (manual, mas registrar): bucket `iris-captures` aparece em Storage; tab Policies mostra 4 policies novas em `storage.objects`
  </acceptance_criteria>
  <done>Migration 0004 criada, aplicada no remoto sa-east-1, types regerados; bucket privado iris-captures com 4 policies RLS folder-based + unique constraint reading_images vivos no banco.</done>
</task>

<task type="auto">
  <name>Task 3: Criar teste SQL cross-terapeuta de Storage RLS (paralelo ao da Fase 1)</name>
  <read_first>
    - supabase/tests/cross_therapist_rls.sql (estrutura completa — 240 linhas — copiar verbatim header + impersonation pattern + cleanup)
    - supabase/tests/run-rls-test.mjs (runner Node que existe; verificar como é invocado)
    - .planning/phases/03-captura-mobile-pwa/03-RESEARCH.md (linhas 1620-1670 — Validation Architecture)
    - .planning/phases/03-captura-mobile-pwa/03-PATTERNS.md (Pattern §storage_cross_therapist_rls.sql analog)
  </read_first>
  <action>
Criar `supabase/tests/storage_cross_therapist_rls.sql` modelado **verbatim** em `supabase/tests/cross_therapist_rls.sql` (mesma estrutura begin/rollback, mesmos UUIDs determinísticos, mesma técnica de impersonation), adaptado para `storage.objects` com bucket `iris-captures`.

Conteúdo literal a criar:

```sql
-- supabase/tests/storage_cross_therapist_rls.sql
-- Fase 3 — Prova empiricamente que RLS de storage.objects bloqueia leitura cross-terapeuta
-- no DB REMOTO sa-east-1 (idempotente, em transação com ROLLBACK).
-- Roda com: supabase db query --db-url "$SUPABASE_DB_URL" -f supabase/tests/storage_cross_therapist_rls.sql
--           ou: psql "$SUPABASE_DB_URL" -v ON_ERROR_STOP=1 -f supabase/tests/storage_cross_therapist_rls.sql
--
-- Estratégia (paralela a cross_therapist_rls.sql):
--   1) FIXTURE: 2 auth.users dummy + 1 storage.objects de cada (path com folder = uuid do owner).
--   2) CONTROL (BYPASSRLS, role postgres): assert que select count(*) from storage.objects = 2.
--   3) Impersonar terapeuta A (set_config request.jwt.claims + role authenticated).
--   4) OWN-DATA: assert que A lê próprio arquivo = 1 row.
--   5) CROSS-THERAPIST: assert que A lê arquivo de B = 0 rows. Espelho B->A.
--   6) Tudo dentro de BEGIN; ... ROLLBACK; — banco volta ao estado anterior.

begin;

-- IDs determinísticos (mesmos do cross_therapist_rls.sql para coerência mental)
-- Therapist A: 11111111-1111-1111-1111-111111111111
-- Therapist B: 22222222-2222-2222-2222-222222222222

-- Limpa estado de runs anteriores (idempotente)
delete from storage.objects where bucket_id = 'iris-captures' and (
  (storage.foldername(name))[1] = '11111111-1111-1111-1111-111111111111'
  or (storage.foldername(name))[1] = '22222222-2222-2222-2222-222222222222'
);
delete from auth.users where id in (
  '11111111-1111-1111-1111-111111111111',
  '22222222-2222-2222-2222-222222222222'
);

-- 1) FIXTURE: 2 auth.users
insert into auth.users (id, email, instance_id, aud, role, created_at, updated_at)
values
  ('11111111-1111-1111-1111-111111111111', 'storage-a@aurel-iris-test.local', '00000000-0000-0000-0000-000000000000', 'authenticated', 'authenticated', now(), now()),
  ('22222222-2222-2222-2222-222222222222', 'storage-b@aurel-iris-test.local', '00000000-0000-0000-0000-000000000000', 'authenticated', 'authenticated', now(), now());

-- 2) FIXTURE: 1 storage.objects para cada terapeuta
-- Path convention CONTEXT D-storage: {therapist_id}/{reading_id}/{eye}_{angle}.jpg
insert into storage.objects (bucket_id, name, owner, metadata)
values
  ('iris-captures',
   '11111111-1111-1111-1111-111111111111/aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa/right_frontal.jpg',
   '11111111-1111-1111-1111-111111111111',
   '{"size": 512000, "mimetype": "image/jpeg"}'::jsonb),
  ('iris-captures',
   '22222222-2222-2222-2222-222222222222/bbbbbbbb-bbbb-bbbb-bbbb-bbbbbbbbbbbb/right_frontal.jpg',
   '22222222-2222-2222-2222-222222222222',
   '{"size": 512000, "mimetype": "image/jpeg"}'::jsonb);

-- 3) CONTROL: como postgres (bypassrls), deve ver os 2 objetos
do $$
declare
  cnt integer;
begin
  select count(*) into cnt from storage.objects
    where bucket_id = 'iris-captures'
      and ((storage.foldername(name))[1] = '11111111-1111-1111-1111-111111111111'
        or (storage.foldername(name))[1] = '22222222-2222-2222-2222-222222222222');
  if cnt <> 2 then
    raise exception 'CONTROL FAIL: postgres deveria ver 2 objetos, viu %', cnt;
  end if;
  raise notice 'CONTROL OK: postgres vê 2 objetos (bypassrls)';
end $$;

-- 4) Impersonar Terapeuta A
select set_config(
  'request.jwt.claims',
  json_build_object(
    'sub', '11111111-1111-1111-1111-111111111111',
    'role', 'authenticated'
  )::text,
  true
);
set local role authenticated;

-- 5) OWN-DATA + CROSS-THERAPIST checks impersonando A
do $$
declare
  cnt_own integer;
  cnt_cross integer;
begin
  select count(*) into cnt_own from storage.objects
    where bucket_id = 'iris-captures'
      and (storage.foldername(name))[1] = '11111111-1111-1111-1111-111111111111';
  if cnt_own <> 1 then
    raise exception 'OWN-DATA FAIL (A): A deveria ler 1 objeto seu, leu %', cnt_own;
  end if;

  select count(*) into cnt_cross from storage.objects
    where bucket_id = 'iris-captures'
      and (storage.foldername(name))[1] = '22222222-2222-2222-2222-222222222222';
  if cnt_cross <> 0 then
    raise exception 'CROSS-THERAPIST FAIL (A->B): A não deveria ler objetos de B, leu %', cnt_cross;
  end if;

  raise notice 'PASS A: own=1 cross=0';
end $$;

-- 6) Espelho: impersonar B
reset role;
select set_config(
  'request.jwt.claims',
  json_build_object(
    'sub', '22222222-2222-2222-2222-222222222222',
    'role', 'authenticated'
  )::text,
  true
);
set local role authenticated;

do $$
declare
  cnt_own integer;
  cnt_cross integer;
begin
  select count(*) into cnt_own from storage.objects
    where bucket_id = 'iris-captures'
      and (storage.foldername(name))[1] = '22222222-2222-2222-2222-222222222222';
  if cnt_own <> 1 then
    raise exception 'OWN-DATA FAIL (B): B deveria ler 1 objeto seu, leu %', cnt_own;
  end if;

  select count(*) into cnt_cross from storage.objects
    where bucket_id = 'iris-captures'
      and (storage.foldername(name))[1] = '11111111-1111-1111-1111-111111111111';
  if cnt_cross <> 0 then
    raise exception 'CROSS-THERAPIST FAIL (B->A): B não deveria ler objetos de A, leu %', cnt_cross;
  end if;

  raise notice 'PASS B: own=1 cross=0';
end $$;

-- 7) Cleanup
reset role;
rollback;
-- ROLLBACK garante que tudo é destrutivo apenas dentro desta transação.
```

Executar contra o remoto:

```bash
psql "$SUPABASE_DB_URL" -v ON_ERROR_STOP=1 -f supabase/tests/storage_cross_therapist_rls.sql
```

(Se `SUPABASE_DB_URL` não estiver no shell, ler de `.env.local` em apps/web ou usar `supabase db query --db-url ... -f ...`. Se a env var não estiver disponível na máquina executora, registrar comando no SUMMARY como "para rodar manualmente" e checar o arquivo via SQL local fixture-only — manual UAT registrada em VALIDATION.md já cobre isso.)
  </action>
  <verify>
    <automated>grep -c "iris-captures" supabase/tests/storage_cross_therapist_rls.sql && grep -c "OWN-DATA FAIL" supabase/tests/storage_cross_therapist_rls.sql && grep -c "CROSS-THERAPIST FAIL" supabase/tests/storage_cross_therapist_rls.sql</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/tests/storage_cross_therapist_rls.sql` existe
    - Arquivo contém `begin;` no topo e `rollback;` no fim (verificar grep `^begin;` e `^rollback;`)
    - Arquivo contém literal `bucket_id = 'iris-captures'` em pelo menos 4 ocorrências
    - Arquivo contém literal `OWN-DATA FAIL` (assertion message) em pelo menos 2 ocorrências (uma por terapeuta)
    - Arquivo contém literal `CROSS-THERAPIST FAIL` em pelo menos 2 ocorrências
    - Quando rodado contra remoto via psql ou supabase db query: exit code 0, output contém literais `PASS A: own=1 cross=0` e `PASS B: own=1 cross=0`
    - Se SUPABASE_DB_URL ausente na máquina, SUMMARY registra: "Teste criado e validado por inspeção SQL; execução manual delegada para gate de phase via Supabase Studio SQL Editor"
  </acceptance_criteria>
  <done>Teste SQL cross-terapeuta de Storage criado e (se possível) executado verde contra remoto; documenta empiricamente que RLS folder-based bloqueia cross-terapeuta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Therapist A's session → Storage objects of Therapist B | Cross-tenant data isolation crítico (LGPD: foto de íris é dado biométrico + saúde — categoria sensível) |
| Migration runtime → schema | Migration tem privilégios elevados (postgres role); pode quebrar RLS de tabelas existentes se DROP/ALTER errôneo |
| Codebase → vocabulário proibido | LGPD-06 forward: cada commit pode introduzir "diagnóstico/tratamento/cura" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Information Disclosure | storage.objects RLS | mitigate | 4 policies folder-based em storage.objects + teste cross-terapeuta empírico em supabase/tests/storage_cross_therapist_rls.sql executado contra remoto |
| T-03-01-02 | Information Disclosure | storage.buckets | mitigate | INSERT explícito com `public, false` + `allowed_mime_types = array['image/jpeg']` (rejeita não-JPEG no nível do bucket) |
| T-03-01-03 | Tampering | reading_images integridade | mitigate | UNIQUE constraint (reading_id, eye, angle) torna duplicate insert impossível; tap-to-redo (D-09) usa upsert onConflict |
| T-03-01-04 | Repudiation/Compliance | Codebase LGPD | mitigate | Script audit:vocabulary disponível para CI gate; rodado em verify do plan e em todos os plans subsequentes |
</threat_model>

<verification>
- vitest verde com smoke test (1 teste passando)
- Migration 0004 aplicada no remoto Supabase sa-east-1 sem erro
- types/database.ts regerado e contém entradas para reading_images
- Bucket iris-captures verificado no Supabase Studio (manual): public=false, allowed_mime_types contém image/jpeg
- 4 policies em storage.objects verificadas no Supabase Studio (manual)
- Teste cross-terapeuta de Storage executado contra remoto OU criado e validado por inspeção SQL para execução manual via Studio
- Script audit:vocabulary executado e exit code 0
- Plans 03-02..03-08 podem assumir bucket pronto, vitest pronto, types atualizados
</verification>

<success_criteria>
- [ ] `apps/web/vitest.config.ts`, `tests/setup.ts`, `tests/smoke.test.ts` existem e `pnpm test:run` é green
- [ ] `apps/web/package.json` contém scripts: test, test:run, test:changed, audit:vocabulary
- [ ] `pnpm run audit:vocabulary` exit 0 com mensagem "OK: vocabulário proibido ausente"
- [ ] `supabase/migrations/0004_storage_bucket_iris_captures.sql` aplicada no remoto
- [ ] `supabase/tests/storage_cross_therapist_rls.sql` existe e (se SUPABASE_DB_URL disponível) executa green
- [ ] `apps/web/types/database.ts` regerado após migration
- [ ] Bucket `iris-captures` privado no remoto com 4 policies RLS folder-based
- [ ] UNIQUE constraint reading_images_reading_eye_angle_unique aplicada (verificar via `\d+ reading_images` no psql)
</success_criteria>

<output>
After completion, create `.planning/phases/03-captura-mobile-pwa/03-01-SUMMARY.md` documenting:
- Versões exatas instaladas (vitest, @testing-library/react, jsdom)
- Confirmação da migration 0004 aplicada (timestamp do push)
- Output do `pnpm run audit:vocabulary`
- Output do teste cross-terapeuta de Storage (PASS A / PASS B) ou nota "execução manual pendente em Studio"
- Lista de arquivos criados/modificados com paths absolutos
</output>
