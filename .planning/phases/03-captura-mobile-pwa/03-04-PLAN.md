---
plan_id: 03-04
wave: 3
depends_on: [03-02, 03-03]
phase: 3
phase_slug: captura-mobile-pwa
objective: "(capture) route group + layout fullscreen + page server-component + capture-client skeleton + hook useCamera (getUserMedia) + componentes CameraView e CameraDeniedScreen (D-15)"
requirements: [CAPTURE-02]
files_modified:
  - apps/web/app/(capture)/layout.tsx
  - apps/web/app/(capture)/leituras/nova/capturar/page.tsx
  - apps/web/app/(capture)/leituras/nova/capturar/capture-client.tsx
  - apps/web/hooks/use-camera.ts
  - apps/web/hooks/use-camera.test.ts
  - apps/web/components/capture/CameraView.tsx
  - apps/web/components/capture/CameraDeniedScreen.tsx
must_haves:
  truths:
    - "Rota /leituras/nova/capturar?reading=<id> renderiza fullscreen sem sidebar do dashboard"
    - "Server component valida readingId via Supabase select (RLS garante ownership) — sem reading válido, redirect para /leituras/nova"
    - "useCamera abre stream via getUserMedia({ video: { facingMode: { ideal: 'environment' } } })"
    - "useCamera tem cleanup explícito que chama stream.getTracks().forEach(t => t.stop()) em unmount"
    - "useCamera trata erros NotAllowedError, NotFoundError, NotReadableError, OverconstrainedError, AbortError, SecurityError"
    - "CameraDeniedScreen renderiza ícone CameraOff + heading + body + 2 CTAs (Tentar novamente, Continuar via upload no computador) com link para /leituras/nova/upload"
    - "Vídeo tem playsInline + muted + autoPlay (iOS Safari requirement)"
    - "Layout (capture) usa min-h-[100dvh] bg-black, sem sidebar"
    - "iOS Safari quirk tratado: visibilitychange listener chama play() ao retornar"
  artifacts:
    - path: "apps/web/app/(capture)/layout.tsx"
      provides: "Layout fullscreen para route group capture (sem sidebar, bg preto)"
    - path: "apps/web/app/(capture)/leituras/nova/capturar/page.tsx"
      provides: "Server component que valida readingId e passa para capture-client"
    - path: "apps/web/app/(capture)/leituras/nova/capturar/capture-client.tsx"
      provides: "Client orchestrator skeleton — apenas renderiza CameraView nesta fase; state machine completa em 03-06"
    - path: "apps/web/hooks/use-camera.ts"
      provides: "Hook useCamera({ videoRef }) → { status, error, stream, start, stop }"
    - path: "apps/web/components/capture/CameraView.tsx"
      provides: "Wrapper de <video> + overlay circular + estados visuais (idle/requesting/streaming/denied)"
    - path: "apps/web/components/capture/CameraDeniedScreen.tsx"
      provides: "Tela de erro CONTEXT D-15 com instruções por SO + 2 CTAs"
  key_links:
    - from: "apps/web/hooks/use-camera.ts"
      to: "navigator.mediadevices.getUserMedia"
      via: "facingMode ideal environment"
      pattern: "facingMode.*ideal.*environment"
    - from: "apps/web/components/capture/CameraDeniedScreen.tsx"
      to: "/leituras/nova/upload"
      via: "Link com buttonVariants ghost"
      pattern: "/leituras/nova/upload"
    - from: "apps/web/app/(capture)/leituras/nova/capturar/page.tsx"
      to: "readings table com RLS"
      via: "supabase.from('readings').select.eq('id', readingId).single()"
      pattern: "from\\('readings'\\)"
autonomous: false
threat_model:
  - id: T-03-04-01
    threat: "Capture page acessa reading de outro terapeuta — IDOR via query param"
    mitigation: "Server component faz supabase.from('readings').select.eq('id', readingId).single(); RLS de readings filtra para auth.uid(); resultado null → notFound() ou redirect /leituras/nova"
  - id: T-03-04-02
    threat: "Stream da câmera continua ativo após navegação — drena bateria, indica 'spy' ao usuário, fica acesso de longa data no log do navegador"
    mitigation: "useCamera retorna cleanup que chama stream.getTracks().forEach(t => t.stop()) e zera videoRef.current.srcObject; verificado em teste vitest mockando navigator.mediaDevices"
  - id: T-03-04-03
    threat: "Vídeo tem srcObject mas iOS Safari nunca mostra frame (sem playsInline → fullscreen native player que esconde DOM)"
    mitigation: "Atributos playsInline + muted + autoPlay obrigatórios; cobertos no manual UAT em iPhone real"
  - id: T-03-04-04
    threat: "Erros de getUserMedia mostram tela genérica (UX ruim) ou expõem stack trace"
    mitigation: "useCamera mapeia error.name conhecidos (NotAllowed, NotFound, NotReadable, Overconstrained, Abort, Security) para enum errorType; CameraDeniedScreen seleciona copy por enum; nenhum error.message cru exposto"
created: 2026-05-01
---

<objective>
Materializar o esqueleto da rota de captura: route group `(capture)` separado de `(dashboard)` (UI-SPEC §Layout — sem sidebar, fullscreen mobile-first), `useCamera` hook com tratamento completo de erros, `CameraView` que renderiza `<video>` + overlay circular guia, `CameraDeniedScreen` com instruções D-15 e fallback para `/leituras/nova/upload` (placeholder do plan 03-03).

Purpose: provar que `getUserMedia` funciona em mobile real, que cleanup de stream é correto (não fica spy ativo), e que o fluxo de erro tem fallback humano. Este plan **não** integra MediaPipe ainda — é apenas o palco. MediaPipe + quality scoring entram em 03-05; sequence + interstitials em 03-06; preview + upload em 03-07; recovery + finalize em 03-08.

Output: ao terminar, terapeuta consegue chegar em `/leituras/nova/capturar?reading=<id>` (vindo do plan 03-03), ver a câmera traseira ao vivo com overlay circular, e em caso de permissão negada vê `CameraDeniedScreen` com CTA funcional para upload placeholder. Sem ainda detector de íris ou auto-captura.

**autonomous: false** — getUserMedia em mobile só funciona em device físico (Chrome DevTools mobile emulation não tem câmera real); UAT manual obrigatória.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-captura-mobile-pwa/03-CONTEXT.md
@.planning/phases/03-captura-mobile-pwa/03-RESEARCH.md
@.planning/phases/03-captura-mobile-pwa/03-PATTERNS.md
@.planning/phases/03-captura-mobile-pwa/03-UI-SPEC.md
@.planning/phases/03-captura-mobile-pwa/03-02-SUMMARY.md
@.planning/phases/03-captura-mobile-pwa/03-03-SUMMARY.md
@apps/web/app/(auth)/layout.tsx
@apps/web/app/(dashboard)/clientes/[id]/page.tsx
@apps/web/hooks/use-mobile.ts
@apps/web/hooks/use-pwa-install.ts
@apps/web/middleware.ts
@apps/web/lib/supabase/server.ts

<interfaces>
<!-- Pattern de page server component (clientes/[id]/page.tsx linhas 15-32) -->
```typescript
export default async function Page({
  searchParams,
}: {
  searchParams: Promise<{ reading?: string }>
}) {
  const { reading: readingId } = await searchParams
  if (!readingId) redirect('/leituras/nova')

  const supabase = await createClient()
  const { data: reading, error } = await supabase
    .from('readings')
    .select('*, clients(full_name), reading_images(eye, angle, quality_score)')
    .eq('id', readingId)
    .single()

  if (!reading || error) notFound()

  return <CaptureClient reading={reading} />
}
```

<!-- Pattern de layout fullscreen sem sidebar (auth/layout.tsx) -->
```typescript
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="min-h-screen flex flex-col items-center justify-center px-4 py-12">
      {children}
    </div>
  )
}
```

<!-- Pattern de hook custom (use-pwa-install.ts criado no 03-02) -->
```typescript
'use client'
import * as React from 'react'

export function useXxx() {
  const [state, setState] = React.useState(...)
  React.useEffect(() => {
    // setup
    return () => { /* cleanup */ }
  }, [])
  return { state, ... }
}
```

<!-- middleware.ts (apps/web/middleware.ts) já protege /leituras/* — validar antes de planejar guards -->
```typescript
// matcher inclui /(dashboard) e /leituras; (capture) também precisa estar protegido
// se matcher não cobrir, ADICIONAR /(capture)/ ao matcher OU duplicar guard no layout
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar (capture) route group + layout + page server component + capture-client skeleton</name>
  <read_first>
    - apps/web/middleware.ts (verificar se matcher cobre /leituras — se não cobrir o segmento (capture), ajustar)
    - apps/web/app/(auth)/layout.tsx (pattern de layout sem sidebar)
    - apps/web/app/(dashboard)/clientes/[id]/page.tsx (pattern Page async com searchParams Promise + RLS query)
    - .planning/phases/03-captura-mobile-pwa/03-UI-SPEC.md (§Layout & Route Group; §Viewport e responsividade — landscape bloqueado, max-width 480px em desktop)
    - .planning/phases/03-captura-mobile-pwa/03-PATTERNS.md (§(capture)/layout.tsx mapping; §capture-client.tsx pattern)
  </read_first>
  <action>
1. **Verificar middleware** (apps/web/middleware.ts) — abrir e confirmar que `matcher` cobre `/leituras/*` (rota `(capture)/leituras/nova/capturar` é resolvida como `/leituras/nova/capturar` para o middleware — route groups são URL-invisible). Se matcher não cobrir, **NÃO** adicionar; o (capture) layout fará seu próprio auth guard server-side.

2. **Criar `apps/web/app/(capture)/layout.tsx`** (server component, fullscreen sem sidebar):

```typescript
import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'

export default async function CaptureLayout({
  children,
}: {
  children: React.ReactNode
}) {
  const supabase = await createClient()
  // Defesa em profundidade — middleware deveria já ter bloqueado, mas a Fase 2 padronizou
  // chamar getUser() em todo layout/server action que depende de identidade.
  const { data: { user }, error: authError } = await supabase.auth.getUser()
  if (!user || authError) {
    redirect('/login')
  }

  return (
    <div className="min-h-[100dvh] bg-black flex flex-col text-white">
      {children}
    </div>
  )
}
```

3. **Criar `apps/web/app/(capture)/leituras/nova/capturar/page.tsx`** (server component):

```typescript
import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'
import { CaptureClient } from './capture-client'

export default async function CapturarPage({
  searchParams,
}: {
  searchParams: Promise<{ reading?: string; resume?: string }>
}) {
  const { reading: readingId, resume } = await searchParams

  if (!readingId) {
    redirect('/leituras/nova')
  }

  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/login')

  // RLS de readings filtra automaticamente para auth.uid() = therapist_id
  const { data: reading, error } = await supabase
    .from('readings')
    .select(`
      id,
      status,
      capture_method,
      client_id,
      created_at,
      clients(full_name),
      reading_images(id, eye, angle, quality_score, storage_path)
    `)
    .eq('id', readingId)
    .single()

  // Se não encontra (não é dono OU id inválido) → volta para /leituras/nova
  if (!reading || error) {
    redirect('/leituras/nova')
  }

  // Se reading já saiu de pending (foi para Fase 5 processing/ready/edited),
  // não faz sentido retornar para captura — manda para detalhe do reading
  // (rota implementada em fase futura — por ora, redireciona para /leituras).
  if (reading.status !== 'pending') {
    redirect('/leituras')
  }

  // Estrutura do client name vinda do select pode ser objeto ou array dependendo da forma da relação
  const clientObj = Array.isArray(reading.clients) ? reading.clients[0] : reading.clients
  const clientName = clientObj?.full_name ?? 'Cliente'

  return (
    <CaptureClient
      readingId={reading.id}
      therapistId={user.id}
      clientName={clientName}
      capturedSlots={(reading.reading_images ?? []).map(img => ({ eye: img.eye, angle: img.angle }))}
      resumeMode={resume === 'true'}
    />
  )
}
```

4. **Criar `apps/web/app/(capture)/leituras/nova/capturar/capture-client.tsx`** (client component skeleton — apenas wireframe nesta fase, IrisDetector + state machine virão em 03-05/06):

```typescript
'use client'

import { useRef } from 'react'
import Link from 'next/link'
import { X } from 'lucide-react'
import { CameraView } from '@/components/capture/CameraView'

interface CapturedSlot {
  eye: string
  angle: string
}

interface CaptureClientProps {
  readingId: string
  therapistId: string
  clientName: string
  capturedSlots: CapturedSlot[]
  resumeMode: boolean
}

export function CaptureClient({
  readingId,
  therapistId: _therapistId,
  clientName,
  capturedSlots: _capturedSlots,
  resumeMode: _resumeMode,
}: CaptureClientProps) {
  const videoRef = useRef<HTMLVideoElement>(null)

  return (
    <div className="relative flex-1 flex flex-col">
      {/* Header com botão cancelar (D-13: cancelar preserva rascunho) */}
      <header className="absolute top-0 left-0 right-0 z-20 flex items-center justify-between px-4 py-3 pt-[env(safe-area-inset-top)]">
        <span className="text-sm text-white/80">{clientName}</span>
        <Link
          href="/leituras"
          aria-label="Cancelar leitura"
          className="rounded-full bg-black/50 backdrop-blur-sm p-2 text-white"
        >
          <X className="h-5 w-5" />
        </Link>
      </header>

      {/* Camera viewfinder — em 03-05/06 receberá QualityIndicator, LiveFeedbackMessage,
          CaptureProgress, AngleOverlay etc. Por ora apenas a câmera com overlay circular. */}
      <CameraView videoRef={videoRef} />

      {/* Hint para desenvolvimento — remover quando state machine 03-06 estiver pronta */}
      <div className="absolute bottom-[calc(env(safe-area-inset-bottom)+24px)] left-1/2 -translate-x-1/2 px-4 py-2 rounded-full bg-black/60 text-xs text-white/80">
        Reading {readingId.slice(0, 8)} — captura ativa em fases seguintes
      </div>
    </div>
  )
}
```
  </action>
  <verify>
    <automated>cd apps/web && pnpm run lint && pnpm run build 2>&1 | tail -30 && pnpm run audit:vocabulary</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/(capture)/layout.tsx` existe e contém literais: `redirect('/login')`, `min-h-[100dvh]`, `bg-black`
    - `apps/web/app/(capture)/leituras/nova/capturar/page.tsx` existe e contém literais: `redirect('/leituras/nova')`, `from('readings')`, `eq('id', readingId)`, `redirect('/leituras')` (status!=pending guard)
    - `apps/web/app/(capture)/leituras/nova/capturar/capture-client.tsx` existe com `'use client'` e renderiza `<CameraView videoRef={videoRef} />`
    - Build de produção passa: `pnpm run build` exit 0
    - Lint clean
    - audit:vocabulary clean
    - Smoke manual (registrar SUMMARY): logado, ir para /leituras/nova/capturar?reading=<uuid-inválido> → redireciona para /leituras/nova; com UUID válido (criado pelo plan 03-03 anterior) → renderiza skeleton (vai mostrar a CameraView que ainda virá no Task 3 — skeleton sem CameraView é OK até Task 3)
  </acceptance_criteria>
  <done>Route group (capture) e page server component criados; capture-client renderiza skeleton com header + cancelar; CameraView usado mas será criado no Task 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar hook useCamera com error mapping completo + cleanup + iOS visibilitychange + testes vitest</name>
  <read_first>
    - apps/web/hooks/use-mobile.ts (kebab-case, import * as React, cleanup explícito)
    - apps/web/hooks/use-pwa-install.ts (criado no 03-02 — pattern de hook com tipos exportados)
    - .planning/phases/03-captura-mobile-pwa/03-RESEARCH.md (linhas 770-822 — Camera & getUserMedia constraints, error handling completo, cleanup, iOS quirks)
    - .planning/phases/03-captura-mobile-pwa/03-CONTEXT.md (D-15 fallback de câmera negada)
  </read_first>
  <behavior>
    - Hook signature: `useCamera({ videoRef, autoStart?: boolean })` retorna `{ status, error, errorType, stream, start, stop }`.
    - status: 'idle' | 'requesting' | 'streaming' | 'denied' | 'error'
    - errorType: 'NotAllowedError' | 'NotFoundError' | 'NotReadableError' | 'OverconstrainedError' | 'AbortError' | 'SecurityError' | 'UnknownError'
    - constraints fixas: `{ video: { facingMode: { ideal: 'environment' }, width: { ideal: 1920 }, height: { ideal: 1080 } }, audio: false }`
    - autoStart default true: chama start() no mount
    - cleanup em unmount: stream.getTracks().forEach(t => t.stop()) + videoRef.current.srcObject = null
    - visibilitychange listener: quando volta para foreground e está streaming, chama videoRef.current.play() (iOS quirk)
    - start() é re-entrante (chama stop() antes para evitar leak)

    Testes vitest com mock de navigator.mediaDevices:
    1. `start succeeds when getUserMedia resolves` → status='streaming', stream definido
    2. `start handles NotAllowedError` → status='denied', errorType='NotAllowedError'
    3. `start handles NotFoundError` → errorType='NotFoundError'
    4. `start handles OverconstrainedError` → errorType='OverconstrainedError'
    5. `stop kills all tracks` → todos os tracks recebem .stop()
    6. `unmount calls stop` → cleanup chamado
  </behavior>
  <action>
1. **Criar `apps/web/hooks/use-camera.ts`**:

```typescript
'use client'

import * as React from 'react'

export type CameraStatus = 'idle' | 'requesting' | 'streaming' | 'denied' | 'error'
export type CameraErrorType =
  | 'NotAllowedError'
  | 'NotFoundError'
  | 'NotReadableError'
  | 'OverconstrainedError'
  | 'AbortError'
  | 'SecurityError'
  | 'UnknownError'

export interface UseCameraOptions {
  videoRef: React.RefObject<HTMLVideoElement | null>
  /** Iniciar getUserMedia automaticamente no mount. Default true. */
  autoStart?: boolean
}

export interface UseCameraResult {
  status: CameraStatus
  errorType: CameraErrorType | null
  error: Error | null
  stream: MediaStream | null
  start: () => Promise<void>
  stop: () => void
}

const CAMERA_CONSTRAINTS: MediaStreamConstraints = {
  video: {
    // 'ideal' (não 'exact') — em desktop sem câmera traseira, 'exact' joga OverconstrainedError direto
    facingMode: { ideal: 'environment' },
    width: { ideal: 1920 },
    height: { ideal: 1080 },
  },
  audio: false,
}

function classifyError(err: unknown): CameraErrorType {
  if (typeof err === 'object' && err !== null && 'name' in err) {
    const name = (err as { name: string }).name
    if (
      name === 'NotAllowedError' ||
      name === 'NotFoundError' ||
      name === 'NotReadableError' ||
      name === 'OverconstrainedError' ||
      name === 'AbortError' ||
      name === 'SecurityError'
    ) {
      return name
    }
  }
  return 'UnknownError'
}

export function useCamera({ videoRef, autoStart = true }: UseCameraOptions): UseCameraResult {
  const [status, setStatus] = React.useState<CameraStatus>('idle')
  const [errorType, setErrorType] = React.useState<CameraErrorType | null>(null)
  const [error, setError] = React.useState<Error | null>(null)
  const [stream, setStream] = React.useState<MediaStream | null>(null)
  const streamRef = React.useRef<MediaStream | null>(null)

  const stop = React.useCallback(() => {
    const s = streamRef.current
    if (s) {
      s.getTracks().forEach(t => t.stop())
      streamRef.current = null
    }
    if (videoRef.current) {
      videoRef.current.srcObject = null
    }
    setStream(null)
    setStatus('idle')
  }, [videoRef])

  const start = React.useCallback(async () => {
    // Re-entrante: derruba stream anterior antes de pedir um novo
    if (streamRef.current) {
      streamRef.current.getTracks().forEach(t => t.stop())
      streamRef.current = null
    }
    setStatus('requesting')
    setError(null)
    setErrorType(null)
    try {
      if (typeof navigator === 'undefined' || !navigator.mediaDevices?.getUserMedia) {
        throw Object.assign(new Error('Câmera não suportada neste navegador'), { name: 'NotFoundError' })
      }
      const s = await navigator.mediaDevices.getUserMedia(CAMERA_CONSTRAINTS)
      streamRef.current = s
      setStream(s)
      if (videoRef.current) {
        videoRef.current.srcObject = s
        // iOS quirk: precisa play() explícito mesmo com autoPlay
        try { await videoRef.current.play() } catch { /* ignore — autoplay deveria cobrir */ }
      }
      setStatus('streaming')
    } catch (e) {
      const t = classifyError(e)
      setErrorType(t)
      setError(e as Error)
      setStatus(t === 'NotAllowedError' ? 'denied' : 'error')
    }
  }, [videoRef])

  // Auto-start no mount + cleanup em unmount
  React.useEffect(() => {
    if (autoStart) {
      void start()
    }
    return () => { stop() }
    // start/stop são useCallbacks estáveis (deps já cobertas)
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [])

  // iOS Safari: stream pausa em background tab — re-play() ao voltar
  React.useEffect(() => {
    if (typeof document === 'undefined') return
    const onVisibility = () => {
      if (document.visibilityState === 'visible' && streamRef.current && videoRef.current) {
        videoRef.current.play().catch(() => { /* ignore */ })
      }
    }
    document.addEventListener('visibilitychange', onVisibility)
    return () => document.removeEventListener('visibilitychange', onVisibility)
  }, [videoRef])

  return { status, errorType, error, stream, start, stop }
}
```

2. **Criar `apps/web/hooks/use-camera.test.ts`**:

```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'
import { renderHook, act, waitFor } from '@testing-library/react'
import { useCamera } from './use-camera'

function makeMockTrack() {
  return { stop: vi.fn(), kind: 'video' }
}

function makeMockStream() {
  const tracks = [makeMockTrack(), makeMockTrack()]
  return {
    getTracks: () => tracks,
    _tracks: tracks,
  } as unknown as MediaStream & { _tracks: ReturnType<typeof makeMockTrack>[] }
}

describe('useCamera', () => {
  let getUserMediaMock: ReturnType<typeof vi.fn>

  beforeEach(() => {
    getUserMediaMock = vi.fn()
    Object.defineProperty(navigator, 'mediaDevices', {
      writable: true,
      configurable: true,
      value: { getUserMedia: getUserMediaMock },
    })
  })

  afterEach(() => {
    vi.restoreAllMocks()
  })

  it('starts streaming when getUserMedia resolves', async () => {
    const stream = makeMockStream()
    getUserMediaMock.mockResolvedValue(stream)
    const videoRef = { current: { srcObject: null, play: vi.fn().mockResolvedValue(undefined) } as unknown as HTMLVideoElement }

    const { result } = renderHook(() => useCamera({ videoRef: videoRef as React.RefObject<HTMLVideoElement | null> }))

    await waitFor(() => expect(result.current.status).toBe('streaming'))
    expect(result.current.stream).toBe(stream)
    expect(result.current.errorType).toBeNull()
  })

  it('handles NotAllowedError → denied status', async () => {
    getUserMediaMock.mockRejectedValue(Object.assign(new Error('denied'), { name: 'NotAllowedError' }))
    const videoRef = { current: null }

    const { result } = renderHook(() => useCamera({ videoRef: videoRef as React.RefObject<HTMLVideoElement | null> }))

    await waitFor(() => expect(result.current.status).toBe('denied'))
    expect(result.current.errorType).toBe('NotAllowedError')
  })

  it('handles NotFoundError → error status', async () => {
    getUserMediaMock.mockRejectedValue(Object.assign(new Error('no cam'), { name: 'NotFoundError' }))
    const videoRef = { current: null }

    const { result } = renderHook(() => useCamera({ videoRef: videoRef as React.RefObject<HTMLVideoElement | null> }))

    await waitFor(() => expect(result.current.errorType).toBe('NotFoundError'))
    expect(result.current.status).toBe('error')
  })

  it('handles OverconstrainedError', async () => {
    getUserMediaMock.mockRejectedValue(Object.assign(new Error('over'), { name: 'OverconstrainedError' }))
    const videoRef = { current: null }
    const { result } = renderHook(() => useCamera({ videoRef: videoRef as React.RefObject<HTMLVideoElement | null> }))
    await waitFor(() => expect(result.current.errorType).toBe('OverconstrainedError'))
  })

  it('stop() kills all tracks', async () => {
    const stream = makeMockStream()
    getUserMediaMock.mockResolvedValue(stream)
    const videoRef = { current: { srcObject: null, play: vi.fn().mockResolvedValue(undefined) } as unknown as HTMLVideoElement }

    const { result } = renderHook(() => useCamera({ videoRef: videoRef as React.RefObject<HTMLVideoElement | null> }))
    await waitFor(() => expect(result.current.status).toBe('streaming'))

    act(() => { result.current.stop() })

    expect(stream._tracks.every(t => t.stop.mock.calls.length >= 1)).toBe(true)
    expect(result.current.status).toBe('idle')
    expect(result.current.stream).toBeNull()
  })

  it('unmount triggers cleanup (stops tracks)', async () => {
    const stream = makeMockStream()
    getUserMediaMock.mockResolvedValue(stream)
    const videoRef = { current: { srcObject: null, play: vi.fn().mockResolvedValue(undefined) } as unknown as HTMLVideoElement }

    const { unmount, result } = renderHook(() => useCamera({ videoRef: videoRef as React.RefObject<HTMLVideoElement | null> }))
    await waitFor(() => expect(result.current.status).toBe('streaming'))

    unmount()

    expect(stream._tracks.every(t => t.stop.mock.calls.length >= 1)).toBe(true)
  })

  it('autoStart=false does not call getUserMedia immediately', () => {
    const videoRef = { current: null }
    renderHook(() => useCamera({ videoRef: videoRef as React.RefObject<HTMLVideoElement | null>, autoStart: false }))
    expect(getUserMediaMock).not.toHaveBeenCalled()
  })
})
```
  </action>
  <verify>
    <automated>cd apps/web && pnpm run test:run hooks/use-camera.test.ts && pnpm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/hooks/use-camera.ts` existe e exporta literais: `useCamera`, `UseCameraResult`, `UseCameraOptions`, `CameraStatus`, `CameraErrorType`
    - Hook contém literal `facingMode: { ideal: 'environment' }` (NÃO `exact`)
    - Hook contém literal `getTracks().forEach` para cleanup (verificar grep `getTracks\\(\\).forEach.*stop`)
    - Hook contém literal `visibilitychange` listener
    - Mapeamento de erros cobre os 6 nomes específicos (NotAllowed, NotFound, NotReadable, Overconstrained, Abort, Security) — verificar grep retorna >= 6 ocorrências (cumulativo)
    - `apps/web/hooks/use-camera.test.ts` existe com >= 7 testes
    - `cd apps/web && pnpm run test:run hooks/use-camera.test.ts` exit 0; >= 7 testes verdes
    - `pnpm run lint` exit 0
    - audit:vocabulary clean
  </acceptance_criteria>
  <done>useCamera hook funcional, testado, com cleanup correto e mapeamento de erros completo.</done>
</task>

<task type="auto">
  <name>Task 3: Criar componentes CameraView (com overlay circular) e CameraDeniedScreen (D-15)</name>
  <read_first>
    - apps/web/hooks/use-camera.ts (criado no Task 2)
    - apps/web/components/clientes/clients-table.tsx (linhas 41-53 — empty-state pattern)
    - apps/web/app/(dashboard)/leituras/page.tsx (placeholder pattern fullscreen)
    - .planning/phases/03-captura-mobile-pwa/03-UI-SPEC.md (§CameraView 6 estados; §CameraDeniedScreen layout fullscreen + 2 CTAs; §Copywriting CameraDeniedScreen H1/body/CTAs)
    - .planning/phases/03-captura-mobile-pwa/03-PATTERNS.md (§CameraView analog dashboard-header; §CameraDeniedScreen analog clients-table empty-state)
    - apps/web/components/ui/button.tsx (buttonVariants para Link styled como botão)
  </read_first>
  <action>
1. **Criar `apps/web/components/capture/CameraView.tsx`** (PascalCase per RESEARCH §Canonical File List):

```typescript
'use client'

import * as React from 'react'
import { Camera } from 'lucide-react'
import { useCamera } from '@/hooks/use-camera'
import { CameraDeniedScreen } from './CameraDeniedScreen'

interface CameraViewProps {
  videoRef: React.RefObject<HTMLVideoElement | null>
}

export function CameraView({ videoRef }: CameraViewProps) {
  const { status, errorType, start } = useCamera({ videoRef })

  // Estados denied/error → tela de fallback fullscreen (D-15)
  if (status === 'denied' || status === 'error') {
    return <CameraDeniedScreen errorType={errorType} onRetry={start} />
  }

  return (
    <div className="relative flex-1 w-full bg-black overflow-hidden">
      {/* Vídeo fullscreen — playsInline + muted + autoPlay são obrigatórios para iOS Safari (RESEARCH Pitfall 8) */}
      <video
        ref={videoRef}
        playsInline
        muted
        autoPlay
        aria-label="Visualização da câmera traseira"
        className="absolute inset-0 w-full h-full object-cover"
      />

      {/* Estado idle/requesting overlay */}
      {(status === 'idle' || status === 'requesting') && (
        <div className="absolute inset-0 flex flex-col items-center justify-center gap-3 bg-black/80 backdrop-blur-sm">
          <Camera className="h-12 w-12 text-white/70 animate-pulse" />
          <p className="text-sm text-white/80">Solicitando acesso à câmera</p>
        </div>
      )}

      {/* Overlay circular guia — UI-SPEC: borda branca/80, sem fill */}
      <div
        aria-hidden="true"
        className="pointer-events-none absolute inset-0 flex items-center justify-center"
      >
        <div className="aspect-square w-[60vmin] max-w-[360px] rounded-full border-2 border-white/80 shadow-[0_0_0_9999px_rgba(0,0,0,0.35)]" />
      </div>
    </div>
  )
}
```

2. **Criar `apps/web/components/capture/CameraDeniedScreen.tsx`**:

```typescript
'use client'

import Link from 'next/link'
import { CameraOff } from 'lucide-react'
import { Button, buttonVariants } from '@/components/ui/button'
import { Alert, AlertDescription, AlertTitle } from '@/components/ui/alert'
import { cn } from '@/lib/utils'
import type { CameraErrorType } from '@/hooks/use-camera'

interface CameraDeniedScreenProps {
  errorType: CameraErrorType | null
  onRetry: () => void | Promise<void>
}

const HEADING: Record<string, string> = {
  NotAllowedError: 'Permissão da câmera negada',
  NotFoundError: 'Câmera não disponível',
  NotReadableError: 'Câmera em uso por outro app',
  OverconstrainedError: 'Câmera traseira não disponível',
  AbortError: 'Falha ao acessar a câmera',
  SecurityError: 'Conexão insegura',
  UnknownError: 'Câmera não disponível',
}

export function CameraDeniedScreen({ errorType, onRetry }: CameraDeniedScreenProps) {
  const heading = errorType ? (HEADING[errorType] ?? HEADING.UnknownError) : HEADING.UnknownError

  return (
    <div className="flex-1 flex flex-col items-center justify-center min-h-[100dvh] gap-6 px-6 py-12 bg-background text-foreground">
      <CameraOff className="h-16 w-16 text-destructive" aria-hidden="true" />
      <div className="text-center space-y-2 max-w-md">
        <h1 className="text-xl font-semibold">{heading}</h1>
        <p className="text-sm text-muted-foreground">
          Para registrar as imagens da íris, o app precisa de acesso à câmera traseira do seu celular.
        </p>
      </div>

      <Alert className="max-w-md">
        <AlertTitle>Como reabilitar a câmera</AlertTitle>
        <AlertDescription className="space-y-2 text-sm">
          <p><strong>iPhone (Safari):</strong> Ajustes &gt; Safari &gt; Câmera &gt; Permitir.</p>
          <p><strong>Android (Chrome):</strong> Toque no cadeado da barra &gt; Permissões do site &gt; Câmera.</p>
          <p><strong>Computador (Chrome):</strong> Cadeado &gt; Permissões do site &gt; Câmera &gt; Permitir.</p>
        </AlertDescription>
      </Alert>

      <div className="flex flex-col gap-3 w-full max-w-md">
        <Button onClick={() => void onRetry()} className="h-12">
          Tentar novamente
        </Button>
        <Link
          href="/leituras/nova/upload"
          className={cn(buttonVariants({ variant: 'ghost' }), 'h-12')}
        >
          Continuar via upload no computador
        </Link>
      </div>
    </div>
  )
}
```

3. **Adicionar shadcn `alert` (necessário pelo CameraDeniedScreen)**:
   ```bash
   cd apps/web && pnpm dlx shadcn@latest add alert
   ```

4. **Snapshot test simples para CameraDeniedScreen** (`apps/web/components/capture/CameraDeniedScreen.test.tsx`):

```typescript
import { describe, it, expect, vi } from 'vitest'
import { render, screen } from '@testing-library/react'
import { CameraDeniedScreen } from './CameraDeniedScreen'

describe('CameraDeniedScreen', () => {
  it('renders NotAllowedError heading', () => {
    render(<CameraDeniedScreen errorType="NotAllowedError" onRetry={vi.fn()} />)
    expect(screen.getByText('Permissão da câmera negada')).toBeInTheDocument()
  })

  it('renders NotFoundError heading', () => {
    render(<CameraDeniedScreen errorType="NotFoundError" onRetry={vi.fn()} />)
    expect(screen.getByText('Câmera não disponível')).toBeInTheDocument()
  })

  it('renders unknown error fallback', () => {
    render(<CameraDeniedScreen errorType={null} onRetry={vi.fn()} />)
    expect(screen.getByText('Câmera não disponível')).toBeInTheDocument()
  })

  it('renders link to /leituras/nova/upload', () => {
    render(<CameraDeniedScreen errorType="NotAllowedError" onRetry={vi.fn()} />)
    const link = screen.getByText('Continuar via upload no computador')
    expect(link.closest('a')).toHaveAttribute('href', '/leituras/nova/upload')
  })

  it('renders retry button', () => {
    render(<CameraDeniedScreen errorType="NotAllowedError" onRetry={vi.fn()} />)
    expect(screen.getByText('Tentar novamente')).toBeInTheDocument()
  })
})
```
  </action>
  <verify>
    <automated>cd apps/web && pnpm run test:run components/capture/CameraDeniedScreen.test.tsx && pnpm run lint && pnpm run build 2>&1 | tail -20 && pnpm run audit:vocabulary</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/components/capture/CameraView.tsx` existe e contém literais: `useCamera`, `playsInline`, `muted`, `autoPlay`, `border-white/80` (overlay circular), `<CameraDeniedScreen` (renderizado em estados denied/error)
    - `apps/web/components/capture/CameraDeniedScreen.tsx` existe e contém literais: `Permissão da câmera negada`, `Câmera não disponível`, `/leituras/nova/upload`, `Tentar novamente`, `Continuar via upload no computador`
    - `apps/web/components/ui/alert.tsx` existe (instalado via shadcn)
    - `apps/web/components/capture/CameraDeniedScreen.test.tsx` existe com >= 5 testes
    - `pnpm run test:run components/capture/CameraDeniedScreen.test.tsx` exit 0
    - Build de produção verde
    - audit:vocabulary clean
  </acceptance_criteria>
  <done>CameraView com overlay circular + estados visuais funcional; CameraDeniedScreen com instruções por SO + 2 CTAs; alert shadcn instalado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Route group (capture) com layout fullscreen, page server component que valida readingId via RLS, capture-client skeleton com header (cancelar) e CameraView, hook useCamera com cleanup correto e error mapping completo, CameraDeniedScreen com instruções D-15 e fallback para upload placeholder. Sem MediaPipe ainda — só palco da câmera.
  </what-built>
  <how-to-verify>
    **Smoke desktop (Chrome):**
    1. `cd apps/web && pnpm run dev`
    2. Logar e ir para um cliente em /clientes/[id]; clicar "Nova Leitura"
    3. Submeter form em /leituras/nova → URL vai para /leituras/nova/capturar?reading=<uuid>
    4. Permitir câmera (laptop usa câmera frontal pois facingMode é ideal: environment, fallback para frontal está OK em desktop)
    5. Verificar:
       - Sidebar do dashboard NÃO aparece (route group (capture) tem layout próprio)
       - Vídeo aparece, ocupa fullscreen
       - Overlay circular branco visível, centralizado
       - Header tem nome do cliente + ícone X (cancelar)
       - Reading ID aparece no rodapé (debug placeholder)
    6. Clicar X (Cancelar leitura) → volta para /leituras (rascunho preservado per D-13)

    **Smoke deny (Chrome):**
    7. DevTools → ⋮ → Settings → Privacy and security → Site settings → Camera → bloquear
    8. Reload em /leituras/nova/capturar?reading=<uuid>
    9. Verificar CameraDeniedScreen renderiza com:
       - Ícone CameraOff vermelho
       - Heading "Permissão da câmera negada"
       - Body explicativo
       - Alert com instruções iOS/Android/Desktop
       - Botão "Tentar novamente" (h-12)
       - Botão ghost "Continuar via upload no computador"
    10. Clicar "Continuar via upload no computador" → /leituras/nova/upload (placeholder funciona)
    11. Re-permitir câmera + clicar "Tentar novamente" → câmera reabre

    **Smoke iPhone real:**
    12. Acessar URL deployada via Safari iOS
    13. Logar; criar leitura via /clientes/[id]
    14. Verificar câmera traseira ativa (NÃO frontal)
    15. Verificar vídeo aparece (não fullscreen native player) — playsInline funcionou
    16. Trocar de aba e voltar — câmera deve retomar (visibilitychange listener)
    17. Sair da rota (clicar X) e ir para qualquer outra rota — verificar via DevTools que LED da câmera apaga (cleanup funciona)

    **Smoke Android real:**
    18. Acessar URL deployada via Chrome Android
    19. Mesmo fluxo, mesma verificação de câmera traseira + cleanup

    **Audit:**
    20. `pnpm run audit:vocabulary` exit 0
    21. `pnpm run test:run` — todos os testes vitest verdes
  </how-to-verify>
  <resume-signal>Reportar OK + screenshots iPhone/Android com câmera ativa, ou listar issues (ex: vídeo preto, LED não apaga, layout quebrado).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser query param `?reading=<id>` → server | ID arbitrário; RLS filtra |
| getUserMedia API → device hardware | Permissão do usuário; sem privilégio especial |
| Stream da câmera → fora do componente | Cleanup obrigatório para não vazar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering / IDOR | page.tsx readingId | mitigate | Server component faz select.eq('id', readingId).single() com RLS de readings; null/error → redirect /leituras/nova; também guard em status (não pending → redirect) |
| T-03-04-02 | Information Disclosure / Resource leak | useCamera stream | mitigate | useEffect cleanup chama stop() que faz getTracks().forEach(t => t.stop()) e zera srcObject; testes vitest validam |
| T-03-04-03 | Tampering | iOS fullscreen native player | mitigate | <video playsInline muted autoPlay> — gates conhecidos do Pitfall 8; UAT iPhone real obrigatória |
| T-03-04-04 | Information Disclosure | error.message exposto na UI | mitigate | useCamera mapeia error.name para enum (CameraErrorType); CameraDeniedScreen seleciona copy server-side conhecida; nenhum error.message cru exposto ao usuário |
</threat_model>

<verification>
- (capture) route group com layout fullscreen funcional (sem sidebar)
- Page server component valida reading ownership via RLS, redireciona em invalido/não-pending
- useCamera com 7 testes vitest verdes (start, denied, NotFound, Overconstrained, stop, unmount cleanup, autoStart=false)
- CameraDeniedScreen com 5 testes vitest verdes (4 errorType variants + link)
- Smoke em Chrome desktop: câmera abre + overlay circular + header funciona; deny mostra fallback
- Smoke em iPhone real + Android real: câmera traseira ativa, cleanup do stream confirmado, visibilitychange retoma após troca de aba
- Build verde, lint clean, vocabulário proibido ausente
</verification>

<success_criteria>
- [ ] Route group `(capture)` criado com layout fullscreen sem sidebar
- [ ] Page server component valida readingId via RLS + guard de status
- [ ] useCamera hook com 7+ testes vitest verdes
- [ ] CameraView renderiza video + overlay circular + estados visuais
- [ ] CameraDeniedScreen renderiza copy correta por errorType + 2 CTAs funcionais
- [ ] Manual UAT em iPhone real: câmera traseira ativa, cleanup verifica LED apaga
- [ ] Manual UAT em Android real: idem
- [ ] Build/lint/audit verdes
</success_criteria>

<output>
After completion, create `.planning/phases/03-captura-mobile-pwa/03-04-SUMMARY.md` documenting:
- Caminhos das rotas criadas
- Resultado dos testes vitest (X/Y verdes para use-camera, CameraDeniedScreen)
- Resultado UAT iPhone real (sucesso/issues)
- Resultado UAT Android real (sucesso/issues)
- Confirmação que LED da câmera apaga após sair da rota
</output>
