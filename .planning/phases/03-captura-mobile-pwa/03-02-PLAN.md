---
plan_id: 03-02
wave: 1
depends_on: [03-01]
phase: 3
phase_slug: captura-mobile-pwa
objective: "PWA shell: app/manifest.ts + Serwist SW + next.config.ts wrap + ícones placeholder + viewport metadata + hook usePWAInstall (sem renderizar banner ainda — banner final em 03-08)"
requirements: [CAPTURE-01]
files_modified:
  - apps/web/package.json
  - apps/web/next.config.ts
  - apps/web/app/manifest.ts
  - apps/web/app/sw.ts
  - apps/web/app/layout.tsx
  - apps/web/hooks/use-pwa-install.ts
  - apps/web/public/icons/icon-192.png
  - apps/web/public/icons/icon-512.png
  - apps/web/public/icons/icon-maskable.png
must_haves:
  truths:
    - "App declara manifest.webmanifest válido com name/short_name/start_url/display=standalone/lang=pt-BR/orientation=portrait"
    - "Service Worker é registrado em produção e responde em /sw.js (não 404, não HTML)"
    - "Build de produção (next build) gera public/sw.js sem erro"
    - "App é instalável: Chrome DevTools → Application → Manifest mostra manifesto válido com 3 ícones"
    - "Hook usePWAInstall expõe { isStandalone, isIOS, canPromptAndroid, promptInstall } com cleanup adequado"
    - "Em dev (NODE_ENV=development), Serwist está disabled (não polui dev cache)"
    - "Layout root (app/layout.tsx) declara appleWebApp + viewport com viewport-fit=cover"
  artifacts:
    - path: "apps/web/app/manifest.ts"
      provides: "Next.js metadata route que serve /manifest.webmanifest"
      contains: "display: 'standalone'"
    - path: "apps/web/app/sw.ts"
      provides: "Source do Service Worker via Serwist (mínimo: skipWaiting + clientsClaim)"
    - path: "apps/web/next.config.ts"
      provides: "next config wrapped com withSerwistInit; SW disabled em dev"
    - path: "apps/web/hooks/use-pwa-install.ts"
      provides: "Hook PWA install — beforeinstallprompt (Android) + standalone detection (iOS legacy + matchMedia)"
    - path: "apps/web/public/icons/icon-{192,512,maskable}.png"
      provides: "3 ícones PWA placeholder (texto AI sobre fundo preto, identidade visual final é Fase 9)"
  key_links:
    - from: "apps/web/next.config.ts"
      to: "@serwist/next"
      via: "withSerwistInit({ swSrc, swDest, disable })"
      pattern: "withSerwistInit"
    - from: "apps/web/app/sw.ts"
      to: "serwist"
      via: "new Serwist({ skipWaiting: true, clientsClaim: true })"
      pattern: "new Serwist"
    - from: "apps/web/hooks/use-pwa-install.ts"
      to: "window.matchMedia('(display-mode: standalone)')"
      via: "addEventListener('beforeinstallprompt')"
      pattern: "beforeinstallprompt"
autonomous: false
threat_model:
  - id: T-03-02-01
    threat: "SW caching páginas autenticadas — terapeuta troca de conta e SW serve dashboard cacheado do user anterior"
    mitigation: "SW Serwist é estritamente mínimo nesta fase: skipWaiting + clientsClaim sem runtimeCaching customizado de rotas autenticadas; defaultCache é navigation-preload only (RESEARCH §Pitfall 4); cache offline rico deferido para Fase 9"
  - id: T-03-02-02
    threat: "SW vazaria como rota Next.js (app/sw.ts interpretado como page)"
    mitigation: "Serwist withSerwistInit cuida do build via swSrc + swDest (gera public/sw.js); verificar manualmente que GET /sw.js retorna JS válido após build"
  - id: T-03-02-03
    threat: "Usuário instala PWA antes de logar e fica preso em /dashboard que redireciona para /login (start_url quebrado)"
    mitigation: "manifest start_url=/dashboard; middleware da Fase 2 já redireciona para /login se sem sessão — aceitar (UX padrão); usuário re-loga e PWA continua útil"
created: 2026-05-01
---

<objective>
Construir o shell PWA do Aurel Iris: manifest válido, Service Worker mínimo via Serwist, ícones placeholder, viewport configurado para mobile com safe-area, e hook `usePWAInstall` que expõe a API necessária para o banner de instalação (banner UI será criado em plan 03-08; aqui só o hook).

Purpose: dar ao app a capacidade de ser instalado em iOS Safari e Chrome Android (CAPTURE-01). A instalação é pré-requisito UX para o fluxo de captura — câmera funciona melhor sem barras do navegador. SW mínimo é proposital: cache offline rico é Fase 9 polish; aqui só preciso "ser PWA instalável" para passar o gate de installability.

Output: build de produção verde com Serwist gerando `public/sw.js`; manifest servido em `/manifest.webmanifest` válido; 3 ícones em `public/icons/`; hook reutilizável; layout root com metadata Apple/viewport.

**autonomous: false** — verificação final de instalabilidade requer device físico (iPhone Safari + Android Chrome). UAT manual obrigatória.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-captura-mobile-pwa/03-CONTEXT.md
@.planning/phases/03-captura-mobile-pwa/03-RESEARCH.md
@.planning/phases/03-captura-mobile-pwa/03-UI-SPEC.md
@.planning/phases/03-captura-mobile-pwa/03-01-SUMMARY.md
@apps/web/next.config.ts
@apps/web/app/layout.tsx
@apps/web/hooks/use-mobile.ts

<interfaces>
<!-- Hook pattern atual em use-mobile.ts (apps/web/hooks/use-mobile.ts) -->
```typescript
import * as React from "react"

const MOBILE_BREAKPOINT = 768

export function useIsMobile() {
  const [isMobile, setIsMobile] = React.useState<boolean | undefined>(undefined)

  React.useEffect(() => {
    const mql = window.matchMedia(`(max-width: ${MOBILE_BREAKPOINT - 1}px)`)
    const onChange = () => { setIsMobile(window.innerWidth < MOBILE_BREAKPOINT) }
    mql.addEventListener("change", onChange)
    setIsMobile(window.innerWidth < MOBILE_BREAKPOINT)
    return () => mql.removeEventListener("change", onChange)
  }, [])

  return !!isMobile
}
```

<!-- Layout root atual (apps/web/app/layout.tsx) — já contém Geist fonts e lang="pt-BR" -->
```typescript
export const metadata: Metadata = {
  title: "Aurel Iris",
  description: "Ferramenta de apoio à anamnese terapêutica integrativa.",
};
```

<!-- next.config.ts atual está vazio — wrap com withSerwistInit é a alteração -->
```typescript
import type { NextConfig } from "next";
const nextConfig: NextConfig = { /* config options here */ };
export default nextConfig;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar Serwist + criar manifest, sw.ts, ícones placeholder, atualizar layout/next.config</name>
  <read_first>
    - apps/web/package.json (deps atuais; checar que não há serwist ainda)
    - apps/web/next.config.ts (estado atual — vazio)
    - apps/web/app/layout.tsx (Metadata + viewport — ponto de extensão)
    - .planning/phases/03-captura-mobile-pwa/03-RESEARCH.md (linhas 826-980 — PWA & Service Worker, manifest.ts, sw.ts boilerplate, next.config.ts wrap)
    - .planning/phases/03-captura-mobile-pwa/03-UI-SPEC.md (§PWA Manifest — tokens exatos: name, short_name, start_url, display, theme_color)
  </read_first>
  <action>
1. **Instalar Serwist** (versão verificada na RESEARCH):
   ```bash
   cd apps/web && pnpm add @serwist/next@9.5.10
   pnpm add -D serwist@9.5.10
   ```

2. **Criar `apps/web/app/manifest.ts`** (Next.js metadata route — copy literal do UI-SPEC §PWA Manifest):

   ```typescript
   import type { MetadataRoute } from 'next'

   export default function manifest(): MetadataRoute.Manifest {
     return {
       name: 'Aurel Iris',
       short_name: 'Aurel Iris',
       description: 'Ferramenta de apoio à anamnese terapêutica integrativa.',
       start_url: '/dashboard',
       display: 'standalone',
       orientation: 'portrait',
       theme_color: '#000000',
       background_color: '#ffffff',
       lang: 'pt-BR',
       icons: [
         { src: '/icons/icon-192.png', sizes: '192x192', type: 'image/png' },
         { src: '/icons/icon-512.png', sizes: '512x512', type: 'image/png' },
         { src: '/icons/icon-maskable.png', sizes: '512x512', type: 'image/png', purpose: 'maskable' },
       ],
     }
   }
   ```

3. **Criar `apps/web/app/sw.ts`** (Serwist source — verbatim RESEARCH §sw.ts):

   ```typescript
   import { defaultCache } from '@serwist/next/worker'
   import type { PrecacheEntry, SerwistGlobalConfig } from 'serwist'
   import { Serwist } from 'serwist'

   declare global {
     interface WorkerGlobalScope extends SerwistGlobalConfig {
       __SW_MANIFEST: (PrecacheEntry | string)[] | undefined
     }
   }

   declare const self: ServiceWorkerGlobalScope

   const serwist = new Serwist({
     precacheEntries: self.__SW_MANIFEST,
     skipWaiting: true,
     clientsClaim: true,
     navigationPreload: true,
     runtimeCaching: defaultCache,
   })

   serwist.addEventListeners()
   ```

4. **Atualizar `apps/web/next.config.ts`** (wrap com withSerwistInit — disabled em dev):

   ```typescript
   import type { NextConfig } from "next"
   import withSerwistInit from "@serwist/next"

   const withSerwist = withSerwistInit({
     swSrc: "app/sw.ts",
     swDest: "public/sw.js",
     disable: process.env.NODE_ENV === "development",
   })

   const nextConfig: NextConfig = {
     // config options aqui (atualmente vazio — manter)
   }

   export default withSerwist(nextConfig)
   ```

5. **Atualizar `apps/web/app/layout.tsx`** — adicionar `appleWebApp`, `applicationName` ao metadata e exportar `viewport`:

   ```typescript
   import type { Metadata, Viewport } from "next";
   import { Geist, Geist_Mono } from "next/font/google";
   import "./globals.css";

   const geistSans = Geist({
     variable: "--font-geist-sans",
     subsets: ["latin"],
   });
   const geistMono = Geist_Mono({
     variable: "--font-geist-mono",
     subsets: ["latin"],
   });

   export const metadata: Metadata = {
     title: "Aurel Iris",
     description: "Ferramenta de apoio à anamnese terapêutica integrativa.",
     applicationName: "Aurel Iris",
     appleWebApp: {
       capable: true,
       statusBarStyle: "black-translucent",
       title: "Aurel Iris",
     },
   };

   export const viewport: Viewport = {
     width: "device-width",
     initialScale: 1,
     maximumScale: 1,
     userScalable: false,
     viewportFit: "cover",
     themeColor: "#000000",
   };

   export default function RootLayout({
     children,
   }: Readonly<{ children: React.ReactNode }>) {
     return (
       <html lang="pt-BR">
         <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
           {children}
         </body>
       </html>
     );
   }
   ```

6. **Criar 3 ícones placeholder** em `apps/web/public/icons/`:

   Estratégia mais simples e portátil: gerar via Node script one-shot usando `sharp` ou via canvas API + node-canvas. **Alternativa preferida: usar SVG → PNG via `pwa-asset-generator`** (uma dep one-shot devDep) OU criar com qualquer ferramenta de imagem que produza:

   - `icon-192.png`: 192×192, fundo `#000000`, texto "AI" centralizado em branco (Geist Sans Bold 96px ou similar)
   - `icon-512.png`: 512×512, mesma composição em escala
   - `icon-maskable.png`: 512×512, fundo `#000000` ocupando 100% do canvas, com "AI" centralizado dentro de safe-zone 80% (ou seja, texto ocupa apenas 80% central — 10% de padding em cada lado para máscaras circulares Android)

   **Comando recomendado** (usando `pwa-asset-generator` como devDep one-shot — adiciona temporariamente, gera, remove):
   ```bash
   cd apps/web && pnpm dlx pwa-asset-generator@latest --type=png --background "#000" --maskable --opaque false --padding "calc(50vh - 200px) calc(50vw - 200px)" --icon-only --path "/icons" --manifest "" --index "" '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512"><rect width="512" height="512" fill="#000"/><text x="256" y="320" text-anchor="middle" font-family="sans-serif" font-size="240" font-weight="700" fill="#fff">AI</text></svg>' public
   ```

   Se `pwa-asset-generator` falhar ou não estiver disponível offline, **alternativa**: criar um Node script `apps/web/scripts/generate-icons.mjs` usando `sharp`:
   ```javascript
   import sharp from 'sharp'
   import fs from 'node:fs/promises'
   import path from 'node:path'

   const SVG = (size) => `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 ${size} ${size}">
     <rect width="${size}" height="${size}" fill="#000"/>
     <text x="${size/2}" y="${size*0.625}" text-anchor="middle"
           font-family="sans-serif" font-size="${size*0.47}" font-weight="700" fill="#fff">AI</text>
   </svg>`

   const SVG_MASKABLE = (size) => `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 ${size} ${size}">
     <rect width="${size}" height="${size}" fill="#000"/>
     <text x="${size/2}" y="${size*0.6}" text-anchor="middle"
           font-family="sans-serif" font-size="${size*0.35}" font-weight="700" fill="#fff">AI</text>
   </svg>`

   async function gen(svgString, outName, size) {
     const out = path.resolve('public/icons', outName)
     await fs.mkdir(path.dirname(out), { recursive: true })
     await sharp(Buffer.from(svgString)).resize(size, size).png().toFile(out)
     console.log('wrote', out)
   }

   await gen(SVG(192), 'icon-192.png', 192)
   await gen(SVG(512), 'icon-512.png', 512)
   await gen(SVG_MASKABLE(512), 'icon-maskable.png', 512)
   ```
   Rodar:
   ```bash
   cd apps/web && pnpm add -D sharp && node scripts/generate-icons.mjs
   ```

   **Identidade visual final é Fase 9 (deferred)** — placeholder "AI sobre preto" é suficiente.
  </action>
  <verify>
    <automated>cd apps/web && pnpm install && pnpm run build 2>&1 | tail -50 && ls -la public/icons/icon-192.png public/icons/icon-512.png public/icons/icon-maskable.png && ls -la public/sw.js</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/package.json` contém `"@serwist/next"` e `"serwist"` em dependencies/devDependencies
    - `apps/web/app/manifest.ts` existe e contém literais: `'standalone'`, `'/dashboard'`, `'pt-BR'`, `'#000000'`, `icon-maskable.png`
    - `apps/web/app/sw.ts` existe e contém literais: `new Serwist`, `skipWaiting: true`, `clientsClaim: true`
    - `apps/web/next.config.ts` contém literal `withSerwistInit` e `disable: process.env.NODE_ENV === "development"`
    - `apps/web/app/layout.tsx` contém literal `appleWebApp:` e `viewportFit: "cover"`
    - `apps/web/public/icons/icon-192.png`, `icon-512.png`, `icon-maskable.png` existem (file size > 100 bytes cada)
    - `cd apps/web && pnpm run build` exit code 0; output não contém "Failed to compile" nem "Module not found"
    - Após build: `apps/web/public/sw.js` existe (gerado pelo Serwist)
    - `pnpm run audit:vocabulary` continua exit 0 (nenhum vocabulário proibido introduzido)
  </acceptance_criteria>
  <done>Manifest, SW, ícones, next.config, layout configurados; build de produção gera /sw.js; nenhum erro TS/lint.</done>
</task>

<task type="auto">
  <name>Task 2: Criar hook use-pwa-install com beforeinstallprompt + standalone detection</name>
  <read_first>
    - apps/web/hooks/use-mobile.ts (pattern de hook custom com matchMedia + cleanup)
    - .planning/phases/03-captura-mobile-pwa/03-RESEARCH.md (linhas 925-963 — usePWAInstall implementation pattern)
    - .planning/phases/03-captura-mobile-pwa/03-CONTEXT.md (D-14 — PWA install banner strategy)
    - .planning/phases/03-captura-mobile-pwa/03-UI-SPEC.md (§PWAInstallBanner — copy iOS vs Android)
  </read_first>
  <action>
Criar `apps/web/hooks/use-pwa-install.ts` (kebab-case por convenção Phase 2 use-mobile.ts):

```typescript
'use client'

import * as React from 'react'

// Tipo do evento beforeinstallprompt (não exportado por padrão pela TS DOM lib)
interface BeforeInstallPromptEvent extends Event {
  readonly platforms: string[]
  readonly userChoice: Promise<{ outcome: 'accepted' | 'dismissed'; platform: string }>
  prompt(): Promise<void>
}

export interface UsePWAInstall {
  /** App está rodando em modo standalone (instalado) */
  isStandalone: boolean
  /** Device é iOS (Safari não suporta beforeinstallprompt) */
  isIOS: boolean
  /** Evento beforeinstallprompt foi capturado (Android Chrome) */
  canPromptAndroid: boolean
  /** Dispara o prompt nativo Android Chrome. Retorna true se aceito. No-op em iOS. */
  promptInstall: () => Promise<boolean>
}

export function usePWAInstall(): UsePWAInstall {
  const [isStandalone, setIsStandalone] = React.useState(false)
  const [isIOS, setIsIOS] = React.useState(false)
  const [installEvent, setInstallEvent] = React.useState<BeforeInstallPromptEvent | null>(null)

  React.useEffect(() => {
    if (typeof window === 'undefined') return

    // Detectar standalone — matchMedia (cross-browser) + iOS legacy navigator.standalone
    const mql = window.matchMedia('(display-mode: standalone)')
    const updateStandalone = () => {
      const legacyIOS = (window.navigator as Navigator & { standalone?: boolean }).standalone === true
      setIsStandalone(mql.matches || legacyIOS)
    }
    updateStandalone()
    mql.addEventListener('change', updateStandalone)

    // Detectar iOS (sem suporte a beforeinstallprompt)
    const ua = window.navigator.userAgent
    const iOS = /iPad|iPhone|iPod/.test(ua) && !(window as Window & { MSStream?: unknown }).MSStream
    setIsIOS(iOS)

    // Capturar beforeinstallprompt (Chrome/Edge/Samsung Android)
    const onBIP = (e: Event) => {
      e.preventDefault()
      setInstallEvent(e as BeforeInstallPromptEvent)
    }
    window.addEventListener('beforeinstallprompt', onBIP)

    // Limpar evento quando o app é instalado
    const onInstalled = () => {
      setInstallEvent(null)
      updateStandalone()
    }
    window.addEventListener('appinstalled', onInstalled)

    return () => {
      mql.removeEventListener('change', updateStandalone)
      window.removeEventListener('beforeinstallprompt', onBIP)
      window.removeEventListener('appinstalled', onInstalled)
    }
  }, [])

  const promptInstall = React.useCallback(async () => {
    if (!installEvent) return false
    await installEvent.prompt()
    const { outcome } = await installEvent.userChoice
    setInstallEvent(null)
    return outcome === 'accepted'
  }, [installEvent])

  return {
    isStandalone,
    isIOS,
    canPromptAndroid: installEvent !== null,
    promptInstall,
  }
}
```

Criar teste unitário em `apps/web/hooks/use-pwa-install.test.ts`:

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { renderHook, act } from '@testing-library/react'
import { usePWAInstall } from './use-pwa-install'

describe('usePWAInstall', () => {
  beforeEach(() => {
    Object.defineProperty(window.navigator, 'userAgent', {
      writable: true,
      value: 'Mozilla/5.0 (iPhone; CPU iPhone OS 17_0 like Mac OS X)',
    })
  })

  it('detects iOS user agent', () => {
    const { result } = renderHook(() => usePWAInstall())
    expect(result.current.isIOS).toBe(true)
  })

  it('isStandalone false when matchMedia returns no match', () => {
    const { result } = renderHook(() => usePWAInstall())
    expect(result.current.isStandalone).toBe(false)
  })

  it('canPromptAndroid is false until beforeinstallprompt fires', () => {
    const { result } = renderHook(() => usePWAInstall())
    expect(result.current.canPromptAndroid).toBe(false)
  })

  it('captures beforeinstallprompt event and exposes canPromptAndroid', () => {
    const { result } = renderHook(() => usePWAInstall())
    act(() => {
      const evt = new Event('beforeinstallprompt') as Event & { prompt: () => Promise<void>; userChoice: Promise<{ outcome: string }> }
      // injetar métodos do tipo BeforeInstallPromptEvent
      Object.assign(evt, {
        prompt: vi.fn().mockResolvedValue(undefined),
        userChoice: Promise.resolve({ outcome: 'accepted' as const, platform: 'web' }),
        platforms: ['web'],
      })
      window.dispatchEvent(evt)
    })
    expect(result.current.canPromptAndroid).toBe(true)
  })

  it('promptInstall returns false when no installEvent captured', async () => {
    const { result } = renderHook(() => usePWAInstall())
    const accepted = await result.current.promptInstall()
    expect(accepted).toBe(false)
  })
})
```
  </action>
  <verify>
    <automated>cd apps/web && pnpm run test:run hooks/use-pwa-install.test.ts && pnpm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/hooks/use-pwa-install.ts` existe e é client component (`'use client'` no topo)
    - Hook exporta tipo `UsePWAInstall` e função `usePWAInstall`
    - Hook tem cleanup explícito no return do useEffect (verificar grep `removeEventListener` em pelo menos 3 ocorrências: matchMedia change, beforeinstallprompt, appinstalled)
    - `apps/web/hooks/use-pwa-install.test.ts` existe e tem >= 4 cenários de teste
    - `cd apps/web && pnpm run test:run` exit 0; output mostra >= 4 testes passando para use-pwa-install
    - `cd apps/web && pnpm run lint` exit 0
    - `pnpm run audit:vocabulary` continua exit 0
  </acceptance_criteria>
  <done>Hook `usePWAInstall` exposto, testado e lint-clean; pronto para ser consumido por `PWAInstallBanner` no plan 03-08.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    PWA shell completo: manifest válido em /manifest.webmanifest, SW gerado em /sw.js, 3 ícones em /icons/, layout com viewport-fit=cover e appleWebApp metadata. Hook usePWAInstall criado mas ainda não consumido por nenhum componente — banner final virá no plan 03-08.
  </what-built>
  <how-to-verify>
    **Teste em desktop primeiro (Chrome DevTools):**
    1. `cd apps/web && pnpm run build && pnpm run start` (production mode — SW só é registrado em prod)
    2. Abrir http://localhost:3000 no Chrome
    3. DevTools → Application → Manifest:
       - Verificar nome="Aurel Iris", short_name="Aurel Iris"
       - Verificar start_url="/dashboard", display="standalone", lang="pt-BR"
       - Verificar 3 ícones (192/512/maskable) carregam preview sem erro
    4. DevTools → Application → Service Workers:
       - Verificar status "activated and is running"
       - Source = sw.js
    5. DevTools → Lighthouse → PWA audit:
       - "Installable" deve ser PASS
       - Não precisamos PWA categoria 100% nesta fase (só "instalável")

    **Teste em iPhone real (Safari):**
    6. Abrir URL deployada (ou use ngrok para localhost) no Safari iOS
    7. Toque em ⎙ (compartilhar) → "Adicionar à Tela de Início"
    8. Verificar ícone aparece na home screen com nome "Aurel Iris"
    9. Toque no ícone — app abre em modo standalone (sem barra de Safari)
    10. Status bar deve respeitar safe-area (não ficar atrás do notch)

    **Teste em Android real (Chrome):**
    11. Abrir URL deployada no Chrome Android
    12. Esperar critérios de engagement (5-10s navegando)
    13. Verificar prompt automático "Instalar app" aparece OU
    14. Menu Chrome → "Instalar app" disponível
    15. Após instalar: ícone aparece na home, app abre em standalone

    **Validar copy LGPD-compliant:**
    16. Manifest → description = "Ferramenta de apoio à anamnese terapêutica integrativa."
        (Sem palavras "diagnóstico", "tratamento", "cura")
    17. `pnpm run audit:vocabulary` → exit 0
  </how-to-verify>
  <resume-signal>Reportar OK + screenshots de iOS e Android instalados, ou listar issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service Worker → cached resources | SW intercepta todo fetch; cache mal configurado pode servir conteúdo de outro user |
| Browser install prompt → user device | Instalação é nativa do browser; user gesture-driven; sem privilégio especial |
| Manifest start_url=/dashboard → middleware /login | start_url protegido por middleware da Fase 2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Information Disclosure | Service Worker cache | mitigate | SW Serwist é mínimo (só skipWaiting + clientsClaim + defaultCache); sem runtimeCaching customizado de rotas /dashboard, /clientes, /leituras (são server-rendered com Supabase queries que são por-user via RLS); cache offline rico deferido para Fase 9 onde precisará blacklist explícita de rotas autenticadas |
| T-03-02-02 | Tampering | Build artifact /sw.js | mitigate | Serwist withSerwistInit gera /sw.js no build; verificar manualmente após build que public/sw.js existe e contém runtime válido (não 404); .gitignore exclui (Wave 0 Task 1) |
| T-03-02-03 | Denial of Service | start_url=/dashboard | accept | Aceitar: middleware Fase 2 redireciona para /login se sem sessão; UX padrão; usuário re-loga uma vez e PWA continua útil |
</threat_model>

<verification>
- `pnpm run build` exit 0 com `public/sw.js` gerado
- DevTools → Application → Manifest mostra manifesto válido com 3 ícones
- DevTools → Application → Service Workers status "activated"
- Lighthouse PWA audit "Installable" = PASS
- iPhone real: instalável via Share → Add to Home Screen, abre em standalone
- Android real: instalável via prompt automático ou menu Chrome, abre em standalone
- Hook usePWAInstall: testes vitest passando (>= 4 cenários)
- Lint clean, vocabulário proibido ausente
</verification>

<success_criteria>
- [ ] Build de produção verde com Serwist gerando public/sw.js
- [ ] Manifest válido em /manifest.webmanifest (Chrome DevTools mostra 3 ícones)
- [ ] Lighthouse PWA "Installable" = PASS
- [ ] iPhone Safari: install via Share → Add to Home Screen funciona; app standalone respeita safe-area
- [ ] Android Chrome: prompt automático ou menu install funcional; app standalone
- [ ] Hook usePWAInstall com >= 4 testes verdes
- [ ] Lint + audit:vocabulary verdes
</success_criteria>

<output>
After completion, create `.planning/phases/03-captura-mobile-pwa/03-02-SUMMARY.md` documenting:
- Versões instaladas (@serwist/next, serwist)
- Tamanho do public/sw.js gerado
- Resultado do Lighthouse PWA audit (instalável: yes/no)
- Confirmação iOS Safari install (yes/no, com screenshot opcional)
- Confirmação Android Chrome install (yes/no, com screenshot opcional)
- Resultado dos testes do hook usePWAInstall
</output>
