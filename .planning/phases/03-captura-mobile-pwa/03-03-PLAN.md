---
plan_id: 03-03
wave: 2
depends_on: [03-01]
phase: 3
phase_slug: captura-mobile-pwa
objective: "Server actions de readings (create/finalize/discard/resume) + entry pages /leituras/nova (select cliente) + /leituras/nova/upload placeholder + ativar botão 'Nova Leitura' em /clientes/[id]"
requirements: [CAPTURE-05]
files_modified:
  - apps/web/app/actions/readings.ts
  - apps/web/app/actions/readings.test.ts
  - apps/web/app/(dashboard)/leituras/nova/page.tsx
  - apps/web/app/(dashboard)/leituras/nova/new-reading-form.tsx
  - apps/web/app/(dashboard)/leituras/nova/upload/page.tsx
  - apps/web/app/(dashboard)/clientes/[id]/page.tsx
must_haves:
  truths:
    - "createReadingAction insere row em readings com status='pending', capture_method='mobile_camera', therapist_id=auth.uid(), client_id validado por RLS"
    - "createReadingAction redireciona para /leituras/nova/capturar?reading={id} após sucesso"
    - "discardReadingAction remove storage objects (best-effort) e deleta reading row (cascade apaga reading_images)"
    - "finalizeReadingAction NÃO muda status nesta fase (status fica 'pending'; transição pending→processing é Fase 5)"
    - "/leituras/nova renderiza select de cliente com lista própria do terapeuta + CTA 'Iniciar leitura' (per D-01 entrada dual)"
    - "/leituras/nova com query param ?cliente=<uuid> pré-seleciona o cliente"
    - "/leituras/nova/upload é placeholder em pt-BR apontando para Fase 4 (D-15 fallback de câmera negada precisa do path existir)"
    - "Botão 'Nova Leitura' em /clientes/[id] é ativado: link para /leituras/nova?cliente=<id> (não mais disabled)"
    - "Sem clientes cadastrados: /leituras/nova mostra empty-state CTA 'Cadastrar cliente'"
  artifacts:
    - path: "apps/web/app/actions/readings.ts"
      provides: "Server actions: createReadingAction, finalizeReadingAction, discardReadingAction, getDraftReading (query helper para D-12)"
      exports: ["createReadingAction", "finalizeReadingAction", "discardReadingAction", "getDraftReading", "ReadingFormState"]
    - path: "apps/web/app/(dashboard)/leituras/nova/page.tsx"
      provides: "Server component que lista clientes próprios e renderiza form"
    - path: "apps/web/app/(dashboard)/leituras/nova/new-reading-form.tsx"
      provides: "Client component form com Select base-ui para cliente + submit via useActionState"
    - path: "apps/web/app/(dashboard)/leituras/nova/upload/page.tsx"
      provides: "Placeholder estático (CONTEXT D-15 precisa que rota exista)"
  key_links:
    - from: "apps/web/app/(dashboard)/clientes/[id]/page.tsx"
      to: "/leituras/nova?cliente=<id>"
      via: "Link href trocando Button disabled por buttonVariants"
      pattern: "/leituras/nova\\?cliente="
    - from: "apps/web/app/actions/readings.ts createReadingAction"
      to: "/leituras/nova/capturar?reading=<id>"
      via: "redirect após insert"
      pattern: "redirect\\(`?/leituras/nova/capturar"
autonomous: true
threat_model:
  - id: T-03-03-01
    threat: "Reading creation cross-tenant — terapeuta A consegue criar reading com client_id de B"
    mitigation: "Server action chama getUser() server-side e injeta therapist_id=user.id; RLS de clients (Fase 1 D-12) bloqueia INSERT em readings com client_id que não pertence ao terapeuta — confirmar via teste vitest que insert com client_id arbitrário retorna error.code='42501' ou linha vazia"
  - id: T-03-03-02
    threat: "Discard sem ownership — usuário envia readingId de outro terapeuta"
    mitigation: "discardReadingAction usa getUser() + delete eq('id', readingId); RLS de readings filtra automaticamente; comentário inline 'RLS garante ownership' (pattern Fase 2)"
  - id: T-03-03-03
    threat: "Storage paths leak in discard — listar storage_path antes de auth check"
    mitigation: "Listar reading_images via supabase select (RLS filtra) ANTES do delete; se RLS retornar 0 rows (não é dono), nada é removido; se for dono, removeStorageObjects no bucket via supabase.storage.from(...).remove() — RLS de Storage também valida"
  - id: T-03-03-04
    threat: "Vocabulário proibido em copy de empty-state ou mensagens de erro"
    mitigation: "Copy literal vinda de UI-SPEC §Copywriting (sem 'diagnóstico/tratamento/cura'); audit:vocabulary roda no verify"
created: 2026-05-01
---

<objective>
Materializar a entrada do fluxo de captura: o terapeuta tem 2 caminhos (D-01) — botão "Nova Leitura" em `/clientes/[id]` (cliente já no contexto, fluxo rápido) e rota global `/leituras/nova` com dropdown. Ambos convergem para `createReadingAction` que cria o `readings` row com `status='pending'` (D-08) e redireciona para `/leituras/nova/capturar?reading=<id>` (rota da câmera, criada em 03-04).

Plan também cria os outros server actions necessários ao longo da fase: `finalizeReadingAction` (chamado no 6º slot por 03-08), `discardReadingAction` (usado pelo RecoveryBanner em 03-08, com cleanup de Storage), `getDraftReading` (query helper para detectar rascunho — D-12). Esses são criados juntos para evitar fragmentação de `app/actions/readings.ts`.

Placeholder `/leituras/nova/upload` existe nesta fase porque a tela de erro de câmera (`CameraDeniedScreen` em 03-04) tem CTA "Continuar via upload no computador" que aponta para `/leituras/nova/upload` (D-15). Sem placeholder, o link daria 404.

Output: 2 entry pages funcionais; server actions completas para todo o lifecycle do reading (create→finalize→discard) prontas para serem invocadas pelos plans seguintes; botão "Nova Leitura" em /clientes/[id] ativo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-captura-mobile-pwa/03-CONTEXT.md
@.planning/phases/03-captura-mobile-pwa/03-RESEARCH.md
@.planning/phases/03-captura-mobile-pwa/03-PATTERNS.md
@.planning/phases/03-captura-mobile-pwa/03-UI-SPEC.md
@.planning/phases/03-captura-mobile-pwa/03-01-SUMMARY.md
@apps/web/app/actions/clients.ts
@apps/web/components/clientes/client-form.tsx
@apps/web/components/clientes/delete-client-dialog.tsx
@apps/web/app/(dashboard)/clientes/page.tsx
@apps/web/app/(dashboard)/clientes/[id]/page.tsx
@apps/web/lib/supabase/server.ts
@apps/web/lib/supabase/client.ts
@apps/web/types/database.ts

<interfaces>
<!-- Pattern de server action (apps/web/app/actions/clients.ts linhas 1-51) -->
```typescript
'use server'

import { createClient } from '@/lib/supabase/server'
import { revalidatePath } from 'next/cache'
import { redirect } from 'next/navigation'
import { z } from 'zod'

const xxxSchema = z.object({ /* ... */ })

export type XxxFormState = { error?: Record<string, string[]> | string | null }

export async function createXxxAction(_prev: XxxFormState, formData: FormData): Promise<XxxFormState> {
  const supabase = await createClient()
  const { data: { user }, error: authError } = await supabase.auth.getUser()
  if (!user || authError) redirect('/login')

  const parsed = xxxSchema.safeParse({ /* ... */ })
  if (!parsed.success) return { error: parsed.error.flatten().fieldErrors }

  const { error } = await supabase.from('xxx').insert({ ...parsed.data, therapist_id: user.id })
  if (error) return { error: error.message }

  revalidatePath('/xxx')
  redirect('/xxx')
}
```

<!-- Pattern de useActionState + Form (client-form.tsx linhas 50-77) -->
```typescript
const [state, formAction, isPending] = useActionState(action, {})
// ...
<Form {...form}>
  <form action={formAction} className="space-y-4">
    <FormField ... />
    <Button type="submit" disabled={isPending} aria-busy={isPending}>...</Button>
  </form>
</Form>
```

<!-- Pattern de Select base-ui no form (client-form.tsx linhas 104-131) — name prop é OBRIGATÓRIA -->
```typescript
<Select name="client_id" value={field.value ?? null} onValueChange={field.onChange}>
  <FormControl>
    <SelectTrigger>
      <SelectValue placeholder="Selecione o cliente" />
    </SelectTrigger>
  </FormControl>
  <SelectContent>
    {clients.map(c => (
      <SelectItem key={c.id} value={c.id}>{c.full_name}</SelectItem>
    ))}
  </SelectContent>
</Select>
```

<!-- Botão "Nova Leitura" atual em /clientes/[id]/page.tsx (linhas 70-72) — DESABILITADO -->
```typescript
<Button disabled title="Disponível em breve">
  Nova Leitura
</Button>
```

<!-- Tipos do Supabase (types/database.ts) — usar sempre via Database type -->
```typescript
import type { Database } from '@/types/database'
type Reading = Database['public']['Tables']['readings']['Row']
type ReadingInsert = Database['public']['Tables']['readings']['Insert']
type Client = Database['public']['Tables']['clients']['Row']
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar app/actions/readings.ts com 4 server actions + tipos + testes vitest</name>
  <read_first>
    - apps/web/app/actions/clients.ts (109 linhas — pattern verbatim de auth + Zod + redirect + RLS comment)
    - apps/web/types/database.ts (regenerado em 03-01 — Database['public']['Tables']['readings'])
    - apps/web/lib/supabase/server.ts (createClient pattern)
    - .planning/phases/03-captura-mobile-pwa/03-RESEARCH.md (linhas 408-449 — pattern createReadingAction; linhas 1236-1271 — discardReadingAction com Storage cleanup)
    - .planning/phases/03-captura-mobile-pwa/03-PATTERNS.md (Pattern §1 Server Action Boilerplate)
    - .planning/phases/03-captura-mobile-pwa/03-CONTEXT.md (D-08, D-13, D-storage)
  </read_first>
  <behavior>
    - createReadingAction(prevState, formData): valida client_id (uuid), insere readings { client_id, therapist_id=user.id, status='pending', capture_method='mobile_camera' }, redireciona para `/leituras/nova/capturar?reading=<id>`. Em erro de auth → redirect /login. Em erro Zod → retorna `{ error }`. Em erro Supabase → retorna `{ error }`.
    - finalizeReadingAction(readingId): valida UUID, chama getUser, NÃO muda status (apenas revalidatePath). Retorna `{}` em sucesso. Esta é placeholder pra Fase 5 — comentário inline "// Fase 5: muda status para 'processing' aqui".
    - discardReadingAction(readingId): valida UUID, chama getUser, lista storage_paths via supabase.from('reading_images').select('storage_path').eq('reading_id', readingId). Se rows existem, chama supabase.storage.from('iris-captures').remove(paths). Depois supabase.from('readings').delete().eq('id', readingId). RLS faz o ownership check; cascade apaga reading_images.
    - getDraftReading(): query server-side que retorna o rascunho mais recente do user (`status='pending' AND count(reading_images) < 6`). Usado por 03-08 para o RecoveryBanner.
    - Tipo `ReadingFormState = { error?: Record<string, string[]> | string | null; readingId?: string }`.
    - Vocabulário proibido NUNCA em mensagens de erro.

    Testes vitest:
    - `parses valid client_id UUID` → schema aceita
    - `rejects invalid UUID` → schema retorna erro
    - `rejects missing client_id` → schema retorna erro
    - `discardReading schema rejects non-uuid string` → schema rejeita
  </behavior>
  <action>
1. **Criar `apps/web/app/actions/readings.ts`**:

```typescript
'use server'

import { createClient } from '@/lib/supabase/server'
import { revalidatePath } from 'next/cache'
import { redirect } from 'next/navigation'
import { z } from 'zod'

// Zod v4: usar .min(1, msg) não { required_error: msg }. Schemas exportados para reutilização nos testes.
export const createReadingSchema = z.object({
  client_id: z.string().uuid('client_id inválido'),
})

export const readingIdSchema = z.object({
  reading_id: z.string().uuid('reading_id inválido'),
})

export type ReadingFormState = {
  error?: Record<string, string[]> | string | null
  readingId?: string
}

/**
 * Cria um reading com status='pending', capture_method='mobile_camera'.
 * CONTEXT D-08: criação acontece ANTES da 1ª foto, para que reading_id
 * vire parte do storage_path desde o início (D-storage).
 *
 * Ao concluir, redireciona para /leituras/nova/capturar?reading=<id>
 * (rota implementada no plan 03-04).
 */
export async function createReadingAction(
  _prevState: ReadingFormState,
  formData: FormData
): Promise<ReadingFormState> {
  const supabase = await createClient()
  // SEMPRE verificar autenticação no Server Action (não depender só do middleware — T-02-06)
  const { data: { user }, error: authError } = await supabase.auth.getUser()
  if (!user || authError) {
    redirect('/login')
  }

  const parsed = createReadingSchema.safeParse({
    client_id: formData.get('client_id'),
  })
  if (!parsed.success) {
    return { error: parsed.error.flatten().fieldErrors }
  }

  // RLS de clients (Fase 1 D-12) impede inserir reading apontando para client de outro terapeuta:
  // o INSERT abaixo só sucede se o client_id pertencer ao auth.uid() — caso contrário, FK constraint
  // ou RLS bloqueiam. therapist_id é definido server-side a partir de user.id (defesa em profundidade).
  const { data: reading, error } = await supabase
    .from('readings')
    .insert({
      client_id: parsed.data.client_id,
      therapist_id: user.id,
      status: 'pending',
      capture_method: 'mobile_camera',
    })
    .select('id')
    .single()

  if (error) {
    return { error: error.message }
  }

  revalidatePath('/leituras')
  // Redirect lança throw — não retorna; mas mantenho o tipo de retorno explícito.
  redirect(`/leituras/nova/capturar?reading=${reading.id}`)
}

/**
 * Marca finalização do fluxo de captura. Nesta fase, status permanece 'pending'.
 * A transição 'pending' → 'processing' é responsabilidade da Fase 5 (Modal pipeline).
 */
export async function finalizeReadingAction(
  readingId: string
): Promise<{ error?: string }> {
  const supabase = await createClient()
  const { data: { user }, error: authError } = await supabase.auth.getUser()
  if (!user || authError) {
    redirect('/login')
  }

  const parsed = readingIdSchema.safeParse({ reading_id: readingId })
  if (!parsed.success) {
    return { error: 'reading_id inválido' }
  }

  // Fase 5: muda status para 'processing' aqui e dispara triggerVisionPipeline(reading_id).
  // Nesta fase apenas confirmamos a sessão e revalidamos os caches relevantes.
  revalidatePath('/leituras')
  revalidatePath(`/leituras/${parsed.data.reading_id}`)
  return {}
}

/**
 * Descarta um reading rascunho:
 *   1) Lista storage_paths via reading_images (RLS filtra para apenas próprios)
 *   2) Remove os blobs do bucket iris-captures (RLS de storage também valida)
 *   3) Deleta reading row — cascade apaga reading_images do banco
 *
 * CRÍTICO: cascade do banco NÃO apaga arquivos do Storage. Sem o passo 2,
 * blobs órfãos consomem cota.
 */
export async function discardReadingAction(
  readingId: string
): Promise<{ error?: string }> {
  const supabase = await createClient()
  const { data: { user }, error: authError } = await supabase.auth.getUser()
  if (!user || authError) {
    redirect('/login')
  }

  const parsed = readingIdSchema.safeParse({ reading_id: readingId })
  if (!parsed.success) {
    return { error: 'reading_id inválido' }
  }

  // 1) Listar storage_paths antes do delete (RLS de reading_images filtra para apenas próprios)
  const { data: images } = await supabase
    .from('reading_images')
    .select('storage_path')
    .eq('reading_id', parsed.data.reading_id)

  // 2) Remover blobs (best-effort: erro aqui não bloqueia o delete do reading row)
  if (images && images.length > 0) {
    const paths = images.map(i => i.storage_path)
    await supabase.storage.from('iris-captures').remove(paths)
  }

  // 3) RLS garante que só o dono pode deletar; cascade apaga reading_images
  const { error } = await supabase
    .from('readings')
    .delete()
    .eq('id', parsed.data.reading_id)

  if (error) {
    return { error: error.message }
  }

  revalidatePath('/dashboard')
  revalidatePath('/leituras')
  return {}
}

export type DraftReading = {
  id: string
  created_at: string
  client_id: string
  client_name: string
  imagesCaptured: number
}

/**
 * Retorna o rascunho mais recente do terapeuta (CONTEXT D-12 — recovery banner).
 * Critério: status='pending' AND count(reading_images) < 6.
 * Usa nested resource expansion do PostgREST para count.
 */
export async function getDraftReading(): Promise<DraftReading | null> {
  const supabase = await createClient()
  const { data: { user }, error: authError } = await supabase.auth.getUser()
  if (!user || authError) return null

  const { data: pending } = await supabase
    .from('readings')
    .select(`
      id,
      created_at,
      client_id,
      client:clients(full_name),
      reading_images(count)
    `)
    .eq('therapist_id', user.id)
    .eq('status', 'pending')
    .order('created_at', { ascending: false })
    .limit(5)

  if (!pending) return null

  for (const r of pending) {
    const captured = (r.reading_images?.[0]?.count as number | undefined) ?? 0
    if (captured < 6) {
      // O nested client retorna { full_name: string } | { full_name: string }[] dependendo
      // da forma da relação; tratar ambos.
      const clientObj = Array.isArray(r.client) ? r.client[0] : r.client
      return {
        id: r.id,
        created_at: r.created_at,
        client_id: r.client_id,
        client_name: clientObj?.full_name ?? 'Cliente',
        imagesCaptured: captured,
      }
    }
  }
  return null
}
```

2. **Criar `apps/web/app/actions/readings.test.ts`**:

```typescript
import { describe, it, expect } from 'vitest'
import { createReadingSchema, readingIdSchema } from './readings'

describe('createReadingSchema', () => {
  it('accepts a valid UUID client_id', () => {
    const r = createReadingSchema.safeParse({ client_id: '11111111-1111-1111-1111-111111111111' })
    expect(r.success).toBe(true)
  })

  it('rejects a non-UUID string', () => {
    const r = createReadingSchema.safeParse({ client_id: 'not-a-uuid' })
    expect(r.success).toBe(false)
  })

  it('rejects missing client_id', () => {
    const r = createReadingSchema.safeParse({})
    expect(r.success).toBe(false)
  })
})

describe('readingIdSchema', () => {
  it('accepts a valid UUID reading_id', () => {
    const r = readingIdSchema.safeParse({ reading_id: '22222222-2222-2222-2222-222222222222' })
    expect(r.success).toBe(true)
  })

  it('rejects a non-UUID string', () => {
    const r = readingIdSchema.safeParse({ reading_id: 'foo' })
    expect(r.success).toBe(false)
  })
})
```

   Nota: testes de side-effects do Supabase (insert + RLS cross-tenant) ficam em integration tests futuros ou no UAT manual — não tentar mockar a chain do supabase-js (gera testes frágeis). O ownership cross-tenant é validado pelo teste SQL de RLS criado em 03-01 + Fase 1.
  </action>
  <verify>
    <automated>cd apps/web && pnpm run test:run app/actions/readings.test.ts && pnpm run lint && pnpm run audit:vocabulary</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/actions/readings.ts` existe e tem `'use server'` no topo (linha 1)
    - Arquivo exporta literais: `createReadingAction`, `finalizeReadingAction`, `discardReadingAction`, `getDraftReading`, `ReadingFormState`, `DraftReading`, `createReadingSchema`, `readingIdSchema`
    - Arquivo contém literal `redirect('/login')` (auth fail) em pelo menos 3 funções
    - Arquivo contém literal `therapist_id: user.id` no insert (defesa em profundidade)
    - Arquivo contém literal `redirect(\`/leituras/nova/capturar?reading=` ou similar (verificar grep `redirect.*leituras.*nova.*capturar`)
    - Arquivo contém literal `'iris-captures'` no discardReadingAction
    - `apps/web/app/actions/readings.test.ts` existe e tem >= 5 testes (3 createReadingSchema + 2 readingIdSchema)
    - `cd apps/web && pnpm run test:run app/actions/readings.test.ts` exit 0; >= 5 testes passando
    - `pnpm run lint` exit 0
    - `pnpm run audit:vocabulary` exit 0
  </acceptance_criteria>
  <done>4 server actions + 1 query helper criados, testados e lint-clean. RLS é a única defesa de ownership; testes empíricos de cross-tenant ficam no SQL test e UAT.</done>
</task>

<task type="auto">
  <name>Task 2: Criar /leituras/nova page + new-reading-form + /leituras/nova/upload placeholder + ativar botão em /clientes/[id]</name>
  <read_first>
    - apps/web/app/(dashboard)/clientes/page.tsx (pattern: server query + Link CTA)
    - apps/web/components/clientes/client-form.tsx (linhas 50-167 — pattern useActionState + Form + Select base-ui com `name` prop)
    - apps/web/components/clientes/clients-table.tsx (linhas 41-53 — empty-state pattern)
    - apps/web/app/(dashboard)/leituras/page.tsx (pattern do placeholder atual)
    - apps/web/app/(dashboard)/clientes/[id]/page.tsx (linhas 70-72 — Botão "Nova Leitura" disabled a ativar)
    - .planning/phases/03-captura-mobile-pwa/03-UI-SPEC.md (§Copywriting CTAs e títulos para /leituras/nova; §Empty/Loading/Error states)
    - .planning/phases/03-captura-mobile-pwa/03-CONTEXT.md (D-01 entrada dual, D-15 placeholder upload)
    - .planning/phases/03-captura-mobile-pwa/03-PATTERNS.md (§/leituras/nova page mapping)
  </read_first>
  <action>
1. **Criar `apps/web/app/(dashboard)/leituras/nova/page.tsx`** (server component):

```typescript
import Link from 'next/link'
import { createClient } from '@/lib/supabase/server'
import { buttonVariants } from '@/components/ui/button'
import { cn } from '@/lib/utils'
import { NewReadingForm } from './new-reading-form'

export default async function NovaLeituraPage({
  searchParams,
}: {
  searchParams: Promise<{ cliente?: string }>
}) {
  const { cliente: preselected } = await searchParams
  const supabase = await createClient()

  // RLS filtra automaticamente para clientes do terapeuta logado
  const { data: clients } = await supabase
    .from('clients')
    .select('id, full_name')
    .order('full_name', { ascending: true })

  const list = clients ?? []

  return (
    <div className="space-y-6 max-w-lg">
      <div>
        <h1 className="text-2xl font-semibold">Nova leitura</h1>
        <p className="text-sm text-muted-foreground mt-1">
          Selecione o cliente para iniciar a captura das imagens.
        </p>
      </div>

      {list.length === 0 ? (
        <div className="flex flex-col items-center justify-center py-12 gap-3 text-center">
          <p className="text-lg font-medium">Você ainda não tem clientes cadastrados.</p>
          <p className="text-sm text-muted-foreground">
            Cadastre antes de iniciar uma leitura.
          </p>
          <Link href="/clientes/novo" className={cn(buttonVariants({ size: 'sm' }))}>
            Cadastrar cliente
          </Link>
        </div>
      ) : (
        <NewReadingForm clients={list} preselectedClientId={preselected} />
      )}
    </div>
  )
}
```

2. **Criar `apps/web/app/(dashboard)/leituras/nova/new-reading-form.tsx`** (client component):

```typescript
'use client'

import { useActionState } from 'react'
import { useForm } from 'react-hook-form'
import { zodResolver } from '@hookform/resolvers/zod'
import { z } from 'zod'
import Link from 'next/link'
import { Loader2 } from 'lucide-react'
import { Button, buttonVariants } from '@/components/ui/button'
import {
  Form,
  FormControl,
  FormField,
  FormItem,
  FormLabel,
  FormMessage,
} from '@/components/ui/form'
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from '@/components/ui/select'
import { cn } from '@/lib/utils'
import { createReadingAction, type ReadingFormState } from '@/app/actions/readings'

const formSchema = z.object({
  client_id: z.string().uuid('Selecione um cliente'),
})

type FormValues = z.infer<typeof formSchema>

interface NewReadingFormProps {
  clients: { id: string; full_name: string }[]
  preselectedClientId?: string
}

export function NewReadingForm({ clients, preselectedClientId }: NewReadingFormProps) {
  const [state, formAction, isPending] = useActionState<ReadingFormState, FormData>(
    createReadingAction,
    {}
  )

  const form = useForm<FormValues>({
    resolver: zodResolver(formSchema),
    defaultValues: {
      client_id: preselectedClientId ?? '',
    },
  })

  return (
    <div className="space-y-4">
      {typeof state.error === 'string' && (
        <div className="bg-destructive/10 border border-destructive text-sm px-4 py-2 rounded text-destructive">
          {state.error}
        </div>
      )}

      <Form {...form}>
        <form action={formAction} className="space-y-4">
          <FormField
            control={form.control}
            name="client_id"
            render={({ field }) => (
              <FormItem>
                <FormLabel>Cliente</FormLabel>
                {/* base-ui Select: name prop injeta hidden input no FormData (igual ao pattern de client-form.tsx) */}
                <Select name="client_id" value={field.value || null} onValueChange={field.onChange}>
                  <FormControl>
                    <SelectTrigger>
                      <SelectValue placeholder="Selecione o cliente" />
                    </SelectTrigger>
                  </FormControl>
                  <SelectContent>
                    {clients.map(c => (
                      <SelectItem key={c.id} value={c.id}>
                        {c.full_name}
                      </SelectItem>
                    ))}
                  </SelectContent>
                </Select>
                <FormMessage />
              </FormItem>
            )}
          />

          <div className="flex gap-3 pt-2">
            <Button type="submit" disabled={isPending} aria-busy={isPending}>
              {isPending ? (
                <>
                  <Loader2 className="mr-2 h-4 w-4 animate-spin" />
                  Preparando leitura...
                </>
              ) : (
                'Iniciar leitura'
              )}
            </Button>
            <Link href="/leituras" className={cn(buttonVariants({ variant: 'outline' }))}>
              Cancelar
            </Link>
          </div>
        </form>
      </Form>
    </div>
  )
}
```

3. **Criar `apps/web/app/(dashboard)/leituras/nova/upload/page.tsx`** (placeholder D-15):

```typescript
import { Upload } from 'lucide-react'

export default function UploadPlaceholderPage() {
  return (
    <div className="flex flex-col items-center justify-center min-h-[400px] gap-4 text-center">
      <Upload className="h-16 w-16 text-muted-foreground" />
      <h1 className="text-xl font-semibold">Upload no computador em breve</h1>
      <p className="text-sm text-muted-foreground max-w-sm">
        Disponível na Fase 4. Use a captura mobile pelo celular para registrar imagens agora.
      </p>
    </div>
  )
}
```

4. **Atualizar `apps/web/app/(dashboard)/clientes/[id]/page.tsx`** — substituir o `<Button disabled>` por `<Link>` para `/leituras/nova?cliente=<id>`. Mudança específica nas linhas 70-72:

   **DE:**
   ```typescript
   <Button disabled title="Disponível em breve">
     Nova Leitura
   </Button>
   ```

   **PARA:**
   ```typescript
   <Link
     href={`/leituras/nova?cliente=${client.id}`}
     className={cn(buttonVariants())}
   >
     Nova Leitura
   </Link>
   ```

   Necessário também garantir que `Link` e `cn` estão importados no topo do arquivo (já estão — confirmar). Remover `Button` do import se não for mais usado em mais nenhum lugar do arquivo.
  </action>
  <verify>
    <automated>cd apps/web && pnpm run lint && pnpm run build 2>&1 | tail -30 && pnpm run test:run && pnpm run audit:vocabulary</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/(dashboard)/leituras/nova/page.tsx` existe e contém literal `Nova leitura` (heading) e `Selecione o cliente para iniciar a captura das imagens.`
    - `apps/web/app/(dashboard)/leituras/nova/new-reading-form.tsx` existe e contém literais: `'use client'`, `useActionState`, `createReadingAction`, `<Select name="client_id"`, `Iniciar leitura`
    - `apps/web/app/(dashboard)/leituras/nova/upload/page.tsx` existe e contém literais: `Upload`, `Disponível na Fase 4` ou similar (confirmar copy literal)
    - `apps/web/app/(dashboard)/clientes/[id]/page.tsx` NÃO contém mais `<Button disabled` antes de `Nova Leitura` — verificar grep `Button disabled.*Nova Leitura` retorna 0
    - Mesmo arquivo contém literal `/leituras/nova?cliente=` (verificar grep)
    - `cd apps/web && pnpm run build` exit 0 (sem TS error em new-reading-form.tsx)
    - `cd apps/web && pnpm run lint` exit 0
    - `pnpm run audit:vocabulary` exit 0 (nenhuma string proibida)
    - Manual smoke (registrar no SUMMARY): rodar `pnpm dev`, logar, ir para /clientes/[id] de um cliente teste, clicar "Nova Leitura" → URL vira `/leituras/nova?cliente=<id>` → cliente pré-selecionado → submeter → URL vira `/leituras/nova/capturar?reading=<uuid>` (404 esperado nesta fase — rota da câmera é 03-04)
  </acceptance_criteria>
  <done>Entry pages funcionam end-to-end até a redireção para /capturar (404 esperado, será implementado em 03-04). Placeholder de upload existe. Botão "Nova Leitura" em /clientes/[id] ativo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser FormData → server action | client_id chega via FormData; pode ser arbitrário |
| Server action → readings INSERT | RLS de clients (Fase 1 D-12) é o gate; therapist_id server-side é defesa em profundidade |
| Server action → Storage cleanup (discard) | Storage RLS folder-based (criado em 03-01) é o gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering / IDOR | createReadingAction client_id | mitigate | RLS de clients filtra inserts por FK; therapist_id=user.id (server-side); Zod valida UUID; defesa em profundidade |
| T-03-03-02 | Tampering / IDOR | discardReadingAction reading_id | mitigate | RLS de readings filtra delete; getUser() obrigatório; Zod valida UUID; comentário inline "RLS garante ownership" |
| T-03-03-03 | Information Disclosure | discard listing storage_paths | mitigate | Listagem via supabase.from('reading_images').select é filtrada por RLS (Fase 1 policy "Terapeutas só veem imagens de suas próprias leituras"); se RLS retorna 0 rows, nenhum blob é tocado |
| T-03-03-04 | Compliance (LGPD) | Copy de empty/error states | mitigate | Copy verbatim de UI-SPEC §Copywriting; audit:vocabulary roda no verify |
</threat_model>

<verification>
- 4 server actions + 1 query helper criados em readings.ts
- 5 testes vitest passando para schemas Zod
- Página /leituras/nova funcional com select de cliente + empty-state CTA
- Página /leituras/nova/upload placeholder funcional (link da CameraDeniedScreen no 03-04 não dará 404)
- Botão "Nova Leitura" em /clientes/[id] ativo, leva a /leituras/nova com cliente pré-selecionado
- Lint clean; vocabulário proibido ausente
</verification>

<success_criteria>
- [ ] `app/actions/readings.ts` exporta as 4 actions + getDraftReading + types
- [ ] Testes Zod schemas verdes (>= 5 cenários)
- [ ] /leituras/nova renderiza select e empty-state corretamente
- [ ] /leituras/nova?cliente=<id> pré-seleciona cliente
- [ ] Submit do form cria reading e redireciona para /leituras/nova/capturar?reading=<id>
- [ ] /leituras/nova/upload renderiza placeholder pt-BR
- [ ] Botão "Nova Leitura" em /clientes/[id] funcional (não disabled)
- [ ] Build de produção verde
</success_criteria>

<output>
After completion, create `.planning/phases/03-captura-mobile-pwa/03-03-SUMMARY.md` documenting:
- Funções exportadas em readings.ts (lista)
- Resultado dos testes vitest (X passing)
- Smoke manual: cliente teste → "Nova Leitura" → /leituras/nova?cliente=<id> → submit → URL final (404 esperado)
- Ajustes de copy se algum termo proibido foi rejeitado pelo audit
</output>
