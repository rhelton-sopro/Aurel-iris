---
plan_id: 03-06
wave: 5
depends_on: [03-05]
phase: 3
phase_slug: captura-mobile-pwa
objective: "Sequência guiada de 6 capturas: AngleIcon SVG inline + AngleOverlay (entre ângulos do mesmo olho) + AngleInterstitial (entre olhos) + CaptureProgress (6 dots) + state machine completa em capture-client (ainda sem captura real — stub no trigger)"
requirements: [CAPTURE-05]
files_modified:
  - apps/web/lib/capture/sequence.ts
  - apps/web/lib/capture/sequence.test.ts
  - apps/web/components/capture/AngleIcon.tsx
  - apps/web/components/capture/AngleIcon.test.tsx
  - apps/web/components/capture/AngleOverlay.tsx
  - apps/web/components/capture/AngleInterstitial.tsx
  - apps/web/components/capture/CaptureProgress.tsx
  - apps/web/app/(capture)/leituras/nova/capturar/capture-client.tsx
must_haves:
  truths:
    - "SEQUENCE em ordem exata: right/frontal → right/lateral → right/backlight → left/frontal → left/lateral → left/backlight (6 itens)"
    - "Função getResumeSlotIndex(capturedSlots) retorna o primeiro slot ainda não capturado (0 se nenhum, -1 se todos)"
    - "AngleIcon renderiza 6 variantes (3 ângulos × 2 olhos): cada variante tem viewBox 0 0 96 96 e usa stroke=currentColor"
    - "AngleOverlay aparece entering ~2.5s e depois minimiza para chip discreto (UI-SPEC)"
    - "AngleInterstitial é fullscreen com CTA 'Pronto, vou capturar' que avança para próximo slot"
    - "AngleInterstitial só dispara na transição de olho (índice 3: right/backlight → left/frontal); demais usam AngleOverlay"
    - "CaptureProgress renderiza 6 dots com estados pending/current/done"
    - "State machine cobre fases: idle → streaming → overlay (entre ângulos do mesmo olho) → interstitial (transição de olho) → finalizing → complete"
    - "Reduced motion: pulse e fade-cross desativados quando prefers-reduced-motion: reduce"
  artifacts:
    - path: "apps/web/lib/capture/sequence.ts"
      provides: "SEQUENCE: Slot[] (6 itens), getResumeSlotIndex, isOuterEyeTransition, ANGLE_LABEL"
      exports: ["SEQUENCE", "Slot", "getResumeSlotIndex", "isOuterEyeTransition", "ANGLE_LABEL", "EYE_LABEL"]
    - path: "apps/web/components/capture/AngleIcon.tsx"
      provides: "SVG inline 96x96 com 6 variantes (eye × angle)"
    - path: "apps/web/components/capture/AngleOverlay.tsx"
      provides: "Banner full-width topo do viewfinder com AngleIcon + texto, auto-minimize após 2.5s"
    - path: "apps/web/components/capture/AngleInterstitial.tsx"
      provides: "Tela fullscreen com AngleIcon grande + heading + CTA 'Pronto, vou capturar'"
    - path: "apps/web/components/capture/CaptureProgress.tsx"
      provides: "Pill horizontal com 6 dots + texto compacto 'N de 6'"
  key_links:
    - from: "apps/web/app/(capture)/leituras/nova/capturar/capture-client.tsx"
      to: "@/lib/capture/sequence"
      via: "import { SEQUENCE, getResumeSlotIndex }"
      pattern: "import.*SEQUENCE.*from.*sequence"
    - from: "apps/web/components/capture/AngleInterstitial.tsx"
      to: "state machine 'interstitial' phase"
      via: "renderizado quando phase === 'interstitial'"
      pattern: "phase === 'interstitial'"
autonomous: false
threat_model:
  - id: T-03-06-01
    threat: "Sequência incorreta — captura no slot errado fura o pipeline da Fase 5 (features espelhadas/ausentes)"
    mitigation: "SEQUENCE como const literal exportada de lib pura; teste vitest valida ordem exata e que cada slot é único"
  - id: T-03-06-02
    threat: "Resume bugado — usuário retorna e re-captura slot já feito, sobrescrevendo OU pula slot"
    mitigation: "getResumeSlotIndex testado com fixtures (vazio, 3 capturadas, 6 capturadas) — retorna primeiro slot ausente; testes cobrem cenário cross (capturadas fora de ordem ainda assim retornam o primeiro ausente)"
  - id: T-03-06-03
    threat: "Auto-trigger durante interstitial — overlay parado mas score sobe e dispara captura sem interação"
    mitigation: "useStableQualityGate é desligado durante phase==='interstitial' (gate=Infinity ou pular hook); reset() chamado ao avançar slot"
  - id: T-03-06-04
    threat: "Vocabulário proibido em copy de instrucções — 'diagnóstico de íris', 'tratamento ocular' etc."
    mitigation: "Copy verbatim de UI-SPEC §Copywriting AngleInterstitial; audit:vocabulary roda no verify"
created: 2026-05-01
---

<objective>
Materializar a sequência guiada CAPTURE-05: o terapeuta percorre os 6 slots na ordem exata, com instruções híbridas (D-10) — `AngleOverlay` (banner inline transitório) entre ângulos do mesmo olho; `AngleInterstitial` (tela fullscreen) na transição de olho. Adicionar `CaptureProgress` (pill com 6 dots) ao topo do viewfinder. State machine no capture-client com fases bem definidas.

Plan **não** captura JPEG ainda — auto-trigger continua stub. Captura real (Canvas.toBlob → upload → insert reading_images) é o plan 03-07. Aqui validamos: ordem da sequência, transições corretas, resume funcional, instrução visual aparece no momento certo.

Output: ao terminar, em iPhone real, o terapeuta vê — barra de qualidade verde + chip "Boa" → mensagem "Ótima — capturando..." (stub) → AngleOverlay aparece "Próximo: lateral direito" → minimiza → score sobe novamente → captura stub no 3º (right/backlight) → AngleInterstitial fullscreen "Vamos para o olho esquerdo" → CTA → fade para próximo viewfinder. Todos os 6 slots percorridos em ordem.

**autonomous: false** — UX de transição entre slots e instruções visuais só é validável em device físico com input contínuo (humano).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-captura-mobile-pwa/03-CONTEXT.md
@.planning/phases/03-captura-mobile-pwa/03-RESEARCH.md
@.planning/phases/03-captura-mobile-pwa/03-PATTERNS.md
@.planning/phases/03-captura-mobile-pwa/03-UI-SPEC.md
@.planning/phases/03-captura-mobile-pwa/03-05-SUMMARY.md
@apps/web/lib/capture/quality-scoring.ts
@apps/web/hooks/use-iris-detector.ts
@apps/web/hooks/use-quality-score.ts
@apps/web/components/capture/QualityIndicator.tsx
@apps/web/components/capture/CameraView.tsx
@apps/web/app/(capture)/leituras/nova/capturar/capture-client.tsx

<interfaces>
<!-- SEQUENCE canônica (RESEARCH §Pattern 5 + §Capture Flow State Machine linhas 460-480) -->
```typescript
type Eye = 'left' | 'right'
type Angle = 'frontal' | 'lateral' | 'backlight'
type Slot = { eye: Eye; angle: Angle }

const SEQUENCE: Slot[] = [
  { eye: 'right', angle: 'frontal' },
  { eye: 'right', angle: 'lateral' },
  { eye: 'right', angle: 'backlight' },
  { eye: 'left',  angle: 'frontal' },   // ← AngleInterstitial fullscreen ANTES (D-10)
  { eye: 'left',  angle: 'lateral' },
  { eye: 'left',  angle: 'backlight' },
]
```

<!-- Resume logic (RESEARCH §Resume linhas 1196-1207) -->
```typescript
async function getResumeSlotIndex(captured: Slot[]): number {
  const set = new Set(captured.map(s => `${s.eye}_${s.angle}`))
  return SEQUENCE.findIndex(s => !set.has(`${s.eye}_${s.angle}`))
  // -1 se todos os 6 já foram
}
```

<!-- Copy AngleInterstitial (UI-SPEC §Copywriting) -->
```
heading: "Vamos para o olho esquerdo"
subtitle: "Os próximos 3 registros serão do olho esquerdo. Posicione o celular e toque em capturar quando estiver pronto."
CTA: "Pronto, vou capturar"
```

<!-- AngleIcon variantes (UI-SPEC §AngleIcon) -->
```
Frontal: olho de frente + seta direta para baixo
Lateral: olho + seta diagonal (↗ para right, ↖ para left)
Backlight: olho + raios de sol estilizados ATRÁS do contorno
ViewBox 0 0 96 96; stroke currentColor
```

<!-- CaptureProgress 6 dots (UI-SPEC §CaptureProgress) -->
```
pending: bg-neutral-300
current: bg-primary + ring pulsante
done: bg-emerald-500 + ícone check
texto à direita: "{N} de 6"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar lib sequence.ts (SEQUENCE + getResumeSlotIndex + helpers) com testes</name>
  <read_first>
    - apps/web/lib/capture/quality-scoring.ts (Eye type já exportado)
    - .planning/phases/03-captura-mobile-pwa/03-RESEARCH.md (linhas 460-480 — SEQUENCE; linhas 1196-1207 — getResumeSlotIndex)
  </read_first>
  <behavior>
    Tipos:
    - `Slot = { eye: Eye; angle: Angle }`
    - `SlotPhase = 'idle' | 'streaming' | 'overlay' | 'interstitial' | 'finalizing' | 'complete'`

    Constantes:
    - `SEQUENCE: readonly Slot[]` com exatamente 6 itens na ordem prescrita
    - `EYE_LABEL: { left: 'esquerdo', right: 'direito' }`
    - `ANGLE_LABEL: { frontal: 'frontal', lateral: 'lateral', backlight: 'contraluz' }`

    Funções:
    - `getResumeSlotIndex(captured: { eye: string; angle: string }[]): number` — retorna primeiro slot ausente; -1 se todos
    - `isOuterEyeTransition(fromIndex: number, toIndex: number): boolean` — true quando vai de slot do right para slot do left (ou seja, fromIndex=2, toIndex=3) OU quando começa direto no índice 3+ vindo de -1/<3
    - `getSlotProgressLabel(slotIndex: number): string` — "1 de 6", "2 de 6", etc.
    - `getInterstitialCopy(toEye: Eye): { heading: string; subtitle: string; cta: string }` — copy literal UI-SPEC

    Testes vitest:
    - SEQUENCE tem exatamente 6 itens
    - SEQUENCE[0] = right/frontal, SEQUENCE[5] = left/backlight
    - SEQUENCE não tem duplicatas
    - SEQUENCE alterna em ordem: 3 right consecutivos, depois 3 left
    - getResumeSlotIndex([]) = 0
    - getResumeSlotIndex([right/frontal, right/lateral]) = 2
    - getResumeSlotIndex(todos os 6) = -1
    - getResumeSlotIndex(em ordem aleatória — ex: [left/frontal, right/frontal]) ainda retorna o primeiro AUSENTE da SEQUENCE = 1 (right/lateral)
    - isOuterEyeTransition(2, 3) === true
    - isOuterEyeTransition(0, 1) === false
    - isOuterEyeTransition(3, 4) === false
    - getInterstitialCopy('left').heading === 'Vamos para o olho esquerdo' (verbatim)
  </behavior>
  <action>
Criar `apps/web/lib/capture/sequence.ts`:

```typescript
import type { Eye } from './quality-scoring'

export type Angle = 'frontal' | 'lateral' | 'backlight'
export interface Slot {
  eye: Eye
  angle: Angle
}

/**
 * Sequência canônica de 6 capturas (CONTEXT D-10 + ROADMAP Fase 3 success criterion #4).
 * Ordem fixa: 3 ângulos do olho direito → 3 do olho esquerdo.
 * AngleInterstitial fullscreen aparece ANTES do índice 3 (transição de olho).
 */
export const SEQUENCE: readonly Slot[] = [
  { eye: 'right', angle: 'frontal' },
  { eye: 'right', angle: 'lateral' },
  { eye: 'right', angle: 'backlight' },
  { eye: 'left', angle: 'frontal' },
  { eye: 'left', angle: 'lateral' },
  { eye: 'left', angle: 'backlight' },
] as const

export type SlotPhase =
  | 'idle'
  | 'streaming'
  | 'overlay'
  | 'interstitial'
  | 'finalizing'
  | 'complete'

export const EYE_LABEL: Record<Eye, string> = {
  left: 'esquerdo',
  right: 'direito',
}

export const ANGLE_LABEL: Record<Angle, string> = {
  frontal: 'frontal',
  lateral: 'lateral',
  backlight: 'contraluz',
}

/**
 * Retorna o índice do primeiro slot da SEQUENCE que ainda não foi capturado.
 * -1 se todos os 6 já foram capturados.
 */
export function getResumeSlotIndex(captured: { eye: string; angle: string }[]): number {
  const set = new Set(captured.map(c => `${c.eye}_${c.angle}`))
  for (let i = 0; i < SEQUENCE.length; i++) {
    const s = SEQUENCE[i]
    if (!set.has(`${s.eye}_${s.angle}`)) return i
  }
  return -1
}

/**
 * True quando a transição de fromIndex para toIndex cruza de 'right' para 'left'.
 * AngleInterstitial fullscreen é mostrado apenas nesta transição (CONTEXT D-10).
 */
export function isOuterEyeTransition(fromIndex: number, toIndex: number): boolean {
  if (fromIndex < 0 || fromIndex >= SEQUENCE.length) return false
  if (toIndex < 0 || toIndex >= SEQUENCE.length) return false
  return SEQUENCE[fromIndex].eye === 'right' && SEQUENCE[toIndex].eye === 'left'
}

export function getSlotProgressLabel(slotIndex: number): string {
  return `${slotIndex + 1} de ${SEQUENCE.length}`
}

export function getInterstitialCopy(toEye: Eye): { heading: string; subtitle: string; cta: string } {
  if (toEye === 'left') {
    return {
      heading: 'Vamos para o olho esquerdo',
      subtitle:
        'Os próximos 3 registros serão do olho esquerdo. Posicione o celular e toque em capturar quando estiver pronto.',
      cta: 'Pronto, vou capturar',
    }
  }
  return {
    heading: 'Vamos para o olho direito',
    subtitle:
      'Os próximos 3 registros serão do olho direito. Posicione o celular e toque em capturar quando estiver pronto.',
    cta: 'Pronto, vou capturar',
  }
}
```

Criar `apps/web/lib/capture/sequence.test.ts`:

```typescript
import { describe, it, expect } from 'vitest'
import {
  SEQUENCE,
  getResumeSlotIndex,
  isOuterEyeTransition,
  getSlotProgressLabel,
  getInterstitialCopy,
  EYE_LABEL,
  ANGLE_LABEL,
} from './sequence'

describe('SEQUENCE', () => {
  it('has exactly 6 slots', () => {
    expect(SEQUENCE).toHaveLength(6)
  })

  it('starts with right/frontal', () => {
    expect(SEQUENCE[0]).toEqual({ eye: 'right', angle: 'frontal' })
  })

  it('ends with left/backlight', () => {
    expect(SEQUENCE[5]).toEqual({ eye: 'left', angle: 'backlight' })
  })

  it('has no duplicate slots', () => {
    const keys = SEQUENCE.map(s => `${s.eye}_${s.angle}`)
    expect(new Set(keys).size).toBe(SEQUENCE.length)
  })

  it('puts all right slots before left slots', () => {
    const firstLeft = SEQUENCE.findIndex(s => s.eye === 'left')
    const lastRight = SEQUENCE.map((s, i) => ({ s, i })).filter(x => x.s.eye === 'right').pop()?.i ?? -1
    expect(lastRight).toBeLessThan(firstLeft)
  })

  it('covers all 3 angles per eye', () => {
    const right = SEQUENCE.filter(s => s.eye === 'right').map(s => s.angle).sort()
    const left = SEQUENCE.filter(s => s.eye === 'left').map(s => s.angle).sort()
    expect(right).toEqual(['backlight', 'frontal', 'lateral'])
    expect(left).toEqual(['backlight', 'frontal', 'lateral'])
  })
})

describe('getResumeSlotIndex', () => {
  it('returns 0 for empty captured', () => {
    expect(getResumeSlotIndex([])).toBe(0)
  })

  it('returns 2 when right/frontal and right/lateral are captured', () => {
    expect(getResumeSlotIndex([
      { eye: 'right', angle: 'frontal' },
      { eye: 'right', angle: 'lateral' },
    ])).toBe(2)
  })

  it('returns -1 when all 6 are captured', () => {
    expect(getResumeSlotIndex([...SEQUENCE])).toBe(-1)
  })

  it('returns first MISSING slot even if captured contains later slots out of order', () => {
    // Pulou o right/lateral e capturou os outros — primeiro ausente é index 1 (right/lateral)
    const captured = [
      { eye: 'right', angle: 'frontal' },
      { eye: 'right', angle: 'backlight' },
      { eye: 'left', angle: 'frontal' },
    ]
    expect(getResumeSlotIndex(captured)).toBe(1)
  })

  it('ignores unknown slots in captured', () => {
    const captured = [
      { eye: 'right', angle: 'frontal' },
      { eye: 'right', angle: 'unknown_angle' },
    ]
    expect(getResumeSlotIndex(captured)).toBe(1)
  })
})

describe('isOuterEyeTransition', () => {
  it('true when crossing index 2 → 3 (right/backlight → left/frontal)', () => {
    expect(isOuterEyeTransition(2, 3)).toBe(true)
  })
  it('false within right (0 → 1)', () => {
    expect(isOuterEyeTransition(0, 1)).toBe(false)
  })
  it('false within left (3 → 4)', () => {
    expect(isOuterEyeTransition(3, 4)).toBe(false)
  })
  it('false on out-of-bounds', () => {
    expect(isOuterEyeTransition(-1, 0)).toBe(false)
    expect(isOuterEyeTransition(5, 6)).toBe(false)
  })
})

describe('getSlotProgressLabel', () => {
  it('formats as "N de 6"', () => {
    expect(getSlotProgressLabel(0)).toBe('1 de 6')
    expect(getSlotProgressLabel(5)).toBe('6 de 6')
  })
})

describe('getInterstitialCopy', () => {
  it('returns left copy verbatim', () => {
    expect(getInterstitialCopy('left').heading).toBe('Vamos para o olho esquerdo')
    expect(getInterstitialCopy('left').cta).toBe('Pronto, vou capturar')
  })
  it('returns right copy', () => {
    expect(getInterstitialCopy('right').heading).toBe('Vamos para o olho direito')
  })
})

describe('labels', () => {
  it('EYE_LABEL maps to pt-BR', () => {
    expect(EYE_LABEL.left).toBe('esquerdo')
    expect(EYE_LABEL.right).toBe('direito')
  })
  it('ANGLE_LABEL maps to pt-BR', () => {
    expect(ANGLE_LABEL.frontal).toBe('frontal')
    expect(ANGLE_LABEL.lateral).toBe('lateral')
    expect(ANGLE_LABEL.backlight).toBe('contraluz')
  })
})
```
  </action>
  <verify>
    <automated>cd apps/web && pnpm run test:run lib/capture/sequence.test.ts && pnpm run lint && pnpm run audit:vocabulary</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/lib/capture/sequence.ts` existe e exporta literais: `SEQUENCE`, `Slot`, `Angle`, `SlotPhase`, `EYE_LABEL`, `ANGLE_LABEL`, `getResumeSlotIndex`, `isOuterEyeTransition`, `getSlotProgressLabel`, `getInterstitialCopy`
    - SEQUENCE tem exatamente 6 itens na ordem right/frontal→right/lateral→right/backlight→left/frontal→left/lateral→left/backlight
    - `apps/web/lib/capture/sequence.test.ts` existe com >= 16 testes (6 SEQUENCE + 5 getResumeSlotIndex + 4 isOuterEyeTransition + ...)
    - `pnpm run test:run lib/capture/sequence.test.ts` exit 0; >= 16 testes passando
    - Lint + audit:vocabulary verdes
  </acceptance_criteria>
  <done>Sequence lib + testes; capture-client e componentes podem importar SEQUENCE/getResumeSlotIndex sem ambiguidade.</done>
</task>

<task type="auto">
  <name>Task 2: Criar componentes AngleIcon (6 SVG variantes), CaptureProgress, AngleOverlay, AngleInterstitial</name>
  <read_first>
    - apps/web/lib/capture/sequence.ts (Task 1)
    - apps/web/lib/capture/quality-scoring.ts (Eye type)
    - apps/web/components/dashboard/summary-cards.tsx (presentational pattern)
    - apps/web/app/(auth)/layout.tsx (fullscreen wrapper pattern para AngleInterstitial)
    - .planning/phases/03-captura-mobile-pwa/03-UI-SPEC.md (§AngleIcon variantes; §AngleOverlay states; §AngleInterstitial; §CaptureProgress)
    - .planning/phases/03-captura-mobile-pwa/03-CONTEXT.md (D-11 SVG inline)
  </read_first>
  <action>
1. **Criar `apps/web/components/capture/AngleIcon.tsx`** (SVG inline com 6 variantes — D-11):

```typescript
import * as React from 'react'
import type { Eye } from '@/lib/capture/quality-scoring'
import type { Angle } from '@/lib/capture/sequence'

interface AngleIconProps extends React.SVGAttributes<SVGSVGElement> {
  eye: Eye
  angle: Angle
}

/**
 * SVG inline 96×96 com 6 variantes (3 ângulos × 2 olhos).
 * stroke=currentColor; escalar via Tailwind (className w-12 h-12, w-24 h-24).
 * NUNCA usar width=/height= inline — usar className apenas (UI-SPEC §AngleIcon).
 */
export function AngleIcon({ eye, angle, className, ...rest }: AngleIconProps) {
  return (
    <svg
      viewBox="0 0 96 96"
      xmlns="http://www.w3.org/2000/svg"
      fill="none"
      role="img"
      aria-label={`Indicador: olho ${eye === 'left' ? 'esquerdo' : 'direito'} ângulo ${angle}`}
      className={className}
      {...rest}
    >
      {/* Backlight: raios de sol atrás do olho (z-index inferior) */}
      {angle === 'backlight' && (
        <g opacity="0.35" stroke="currentColor" strokeWidth="2">
          {/* 8 raios */}
          {Array.from({ length: 8 }).map((_, i) => {
            const rad = (i * Math.PI) / 4
            const cx = 48
            const cy = 40
            const r1 = 30
            const r2 = 42
            const x1 = cx + r1 * Math.cos(rad)
            const y1 = cy + r1 * Math.sin(rad)
            const x2 = cx + r2 * Math.cos(rad)
            const y2 = cy + r2 * Math.sin(rad)
            return <line key={i} x1={x1} y1={y1} x2={x2} y2={y2} strokeLinecap="round" />
          })}
          <circle cx="48" cy="40" r="22" fill="currentColor" opacity="0.15" />
        </g>
      )}

      {/* Olho de frente — oval com íris (sempre presente) */}
      <g stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
        {/* Forma de olho: oval horizontal */}
        <path d="M16 40 Q48 18 80 40 Q48 62 16 40 Z" />
        {/* Íris (círculo central) */}
        <circle cx="48" cy="40" r="10" />
        {/* Pupila */}
        <circle cx="48" cy="40" r="3" fill="currentColor" />
      </g>

      {/* Setas — variam por angle */}
      {angle === 'frontal' && (
        // Seta direta para baixo abaixo do olho
        <g stroke="currentColor" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" fill="none">
          <line x1="48" y1="68" x2="48" y2="84" />
          <polyline points="42,78 48,84 54,78" />
        </g>
      )}

      {angle === 'lateral' && (
        // Seta diagonal saindo da têmpora; direção depende do olho
        <g stroke="currentColor" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" fill="none">
          {eye === 'right' ? (
            // Seta para cima-direita (↗)
            <>
              <line x1="76" y1="32" x2="90" y2="18" />
              <polyline points="82,18 90,18 90,26" />
            </>
          ) : (
            // Seta para cima-esquerda (↖)
            <>
              <line x1="20" y1="32" x2="6" y2="18" />
              <polyline points="14,18 6,18 6,26" />
            </>
          )}
        </g>
      )}

      {/* backlight: setas não — raios de sol atrás já indicam */}
    </svg>
  )
}
```

   Test simples:

```typescript
// apps/web/components/capture/AngleIcon.test.tsx
import { describe, it, expect } from 'vitest'
import { render } from '@testing-library/react'
import { AngleIcon } from './AngleIcon'

describe('AngleIcon', () => {
  it.each([
    ['right', 'frontal'],
    ['right', 'lateral'],
    ['right', 'backlight'],
    ['left', 'frontal'],
    ['left', 'lateral'],
    ['left', 'backlight'],
  ] as const)('renders %s/%s without crashing', (eye, angle) => {
    const { container } = render(<AngleIcon eye={eye} angle={angle} className="w-12 h-12" />)
    const svg = container.querySelector('svg')
    expect(svg).toBeInTheDocument()
    expect(svg?.getAttribute('viewBox')).toBe('0 0 96 96')
  })

  it('has aria-label with translated eye name', () => {
    const { container } = render(<AngleIcon eye="left" angle="frontal" />)
    const svg = container.querySelector('svg')
    expect(svg?.getAttribute('aria-label')).toContain('esquerdo')
  })
})
```

2. **Criar `apps/web/components/capture/CaptureProgress.tsx`**:

```typescript
'use client'

import * as React from 'react'
import { Check } from 'lucide-react'
import { SEQUENCE, getSlotProgressLabel } from '@/lib/capture/sequence'

interface CaptureProgressProps {
  currentIndex: number
  capturedCount: number
}

export function CaptureProgress({ currentIndex, capturedCount }: CaptureProgressProps) {
  return (
    <div
      role="group"
      aria-label="Progresso da captura"
      className="flex items-center gap-2 px-4 py-2 rounded-full bg-black/50 backdrop-blur-sm"
    >
      <div className="flex items-center gap-1.5">
        {SEQUENCE.map((_, idx) => {
          const isDone = idx < capturedCount
          const isCurrent = idx === currentIndex
          let cls = 'h-2 w-2 rounded-full transition-colors'
          if (isDone) cls += ' bg-emerald-500'
          else if (isCurrent) cls += ' bg-white motion-safe:animate-pulse'
          else cls += ' bg-white/30'
          return (
            <div key={idx} className={cls} aria-hidden="true">
              {isDone && <Check className="h-2 w-2 text-white -translate-y-0" aria-hidden="true" />}
            </div>
          )
        })}
      </div>
      <span className="text-xs text-white/80 font-medium ml-1" aria-live="polite">
        {getSlotProgressLabel(currentIndex < 0 ? capturedCount : currentIndex)}
      </span>
    </div>
  )
}
```

3. **Criar `apps/web/components/capture/AngleOverlay.tsx`** (UI-SPEC §AngleOverlay — entering 2.5s + minimized chip):

```typescript
'use client'

import * as React from 'react'
import { AngleIcon } from './AngleIcon'
import type { Slot } from '@/lib/capture/sequence'
import { ANGLE_LABEL, EYE_LABEL } from '@/lib/capture/sequence'

interface AngleOverlayProps {
  slot: Slot
  /** Chave de re-entry para resetar o timer quando muda de slot */
  resetKey: string | number
}

const ENTERING_MS = 2500

export function AngleOverlay({ slot, resetKey }: AngleOverlayProps) {
  const [phase, setPhase] = React.useState<'entering' | 'minimized'>('entering')

  React.useEffect(() => {
    setPhase('entering')
    const id = window.setTimeout(() => setPhase('minimized'), ENTERING_MS)
    return () => window.clearTimeout(id)
  }, [resetKey])

  if (phase === 'entering') {
    return (
      <div
        role="status"
        aria-live="polite"
        className="flex items-center gap-3 px-4 py-3 rounded-2xl bg-black/70 backdrop-blur-sm motion-safe:animate-in motion-safe:fade-in motion-safe:slide-in-from-top-2"
      >
        <AngleIcon eye={slot.eye} angle={slot.angle} className="w-12 h-12 text-white shrink-0" />
        <div className="text-white">
          <p className="text-sm font-semibold">
            Próximo: olho {EYE_LABEL[slot.eye]} — {ANGLE_LABEL[slot.angle]}
          </p>
          <p className="text-xs text-white/70">
            Posicione o celular conforme o ícone.
          </p>
        </div>
      </div>
    )
  }

  // minimized: chip pequeno persistente
  return (
    <div
      role="img"
      aria-label={`Captura atual: olho ${EYE_LABEL[slot.eye]} ${ANGLE_LABEL[slot.angle]}`}
      className="rounded-full bg-black/40 backdrop-blur-sm p-1.5"
    >
      <AngleIcon eye={slot.eye} angle={slot.angle} className="w-5 h-5 text-white" />
    </div>
  )
}
```

4. **Criar `apps/web/components/capture/AngleInterstitial.tsx`** (UI-SPEC §AngleInterstitial — fullscreen):

```typescript
'use client'

import * as React from 'react'
import { Button } from '@/components/ui/button'
import { AngleIcon } from './AngleIcon'
import type { Slot } from '@/lib/capture/sequence'
import { getInterstitialCopy } from '@/lib/capture/sequence'

interface AngleInterstitialProps {
  /** Slot que vai começar após o CTA */
  nextSlot: Slot
  onProceed: () => void
}

export function AngleInterstitial({ nextSlot, onProceed }: AngleInterstitialProps) {
  const copy = getInterstitialCopy(nextSlot.eye)

  return (
    <div className="absolute inset-0 z-50 bg-background text-foreground flex flex-col items-center justify-center px-6 py-12 motion-safe:animate-in motion-safe:fade-in motion-safe:duration-300">
      <div className="flex flex-col items-center gap-8 max-w-sm w-full">
        <AngleIcon
          eye={nextSlot.eye}
          angle={nextSlot.angle}
          className="w-24 h-24 text-foreground"
        />
        <div className="text-center space-y-3">
          <h1 className="text-xl font-semibold">{copy.heading}</h1>
          <p className="text-base text-muted-foreground">{copy.subtitle}</p>
        </div>
      </div>
      <div className="mt-12 w-full max-w-sm pb-[env(safe-area-inset-bottom)]">
        <Button onClick={onProceed} className="w-full h-12 text-base">
          {copy.cta}
        </Button>
      </div>
    </div>
  )
}
```
  </action>
  <verify>
    <automated>cd apps/web && pnpm run test:run components/capture/AngleIcon.test.tsx && pnpm run lint && pnpm run build 2>&1 | tail -20 && pnpm run audit:vocabulary</automated>
  </verify>
  <acceptance_criteria>
    - 4 componentes criados em `apps/web/components/capture/`: AngleIcon.tsx, CaptureProgress.tsx, AngleOverlay.tsx, AngleInterstitial.tsx
    - AngleIcon renderiza viewBox="0 0 96 96" e tem branch para cada combinação eye×angle (verificar grep `'right'.*'frontal'`, `'right'.*'lateral'`, `'right'.*'backlight'`, `'left'`)
    - AngleIcon.test.tsx existe e tem >= 7 testes (6 combinações + 1 aria-label)
    - `pnpm run test:run components/capture/AngleIcon.test.tsx` exit 0
    - CaptureProgress mostra `motion-safe:animate-pulse` (reduced motion respect)
    - AngleOverlay tem `motion-safe:animate-in` (reduced motion respect)
    - AngleInterstitial usa `getInterstitialCopy` e renderiza CTA `Pronto, vou capturar`
    - Build verde, lint clean, audit:vocabulary clean
  </acceptance_criteria>
  <done>4 componentes da sequência guiada criados; AngleIcon com 6 variantes; reduced motion respeitado; copy verbatim de UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 3: Integrar state machine completa em capture-client.tsx (sequência + transições + suspende auto-trigger durante interstitial)</name>
  <read_first>
    - apps/web/app/(capture)/leituras/nova/capturar/capture-client.tsx (versão criada em 03-05)
    - apps/web/lib/capture/sequence.ts (Task 1)
    - apps/web/components/capture/{AngleOverlay,AngleInterstitial,CaptureProgress}.tsx (Task 2)
    - apps/web/hooks/use-quality-score.ts (useStableQualityGate com reset)
    - .planning/phases/03-captura-mobile-pwa/03-UI-SPEC.md (§Animação entre eye/angle — fade-cross 200ms)
  </read_first>
  <action>
Substituir `apps/web/app/(capture)/leituras/nova/capturar/capture-client.tsx` pela versão completa abaixo. A estrutura nova:

- State: `phase`, `slotIndex`, `capturedSlots`, `score`, `lastCheck`
- Inicialização do slotIndex: `getResumeSlotIndex(initialCapturedSlots)` ou 0 se vazio
- Auto-trigger: useStableQualityGate é chamado, mas só executa lógica de captura se `phase === 'streaming'`. Em phase 'interstitial' ou 'overlay' o gate ainda calcula mas o callback é no-op.
- Transição após captura stub: avança slotIndex; se `isOuterEyeTransition(prev, next)` → `phase='interstitial'`; senão `phase='overlay'`. Após overlay terminar (timer próprio do AngleOverlay) ou interstitial CTA, retorna a `phase='streaming'` e reset() do gate.

```typescript
'use client'

import * as React from 'react'
import dynamic from 'next/dynamic'
import Link from 'next/link'
import { X } from 'lucide-react'
import { CameraView } from '@/components/capture/CameraView'
import { QualityIndicator } from '@/components/capture/QualityIndicator'
import { LiveFeedbackMessage } from '@/components/capture/LiveFeedbackMessage'
import { CaptureProgress } from '@/components/capture/CaptureProgress'
import { AngleOverlay } from '@/components/capture/AngleOverlay'
import { AngleInterstitial } from '@/components/capture/AngleInterstitial'
import {
  computeQualityCheck,
  overallScore,
  dominantFailure,
  feedbackMessage,
  type QualityCheck,
} from '@/lib/capture/quality-scoring'
import {
  SEQUENCE,
  type Slot,
  type SlotPhase,
  getResumeSlotIndex,
  isOuterEyeTransition,
} from '@/lib/capture/sequence'
import type { UseIrisDetectorResult } from '@/hooks/use-iris-detector'
import { useStableQualityGate } from '@/hooks/use-quality-score'

const IrisDetector = dynamic(() => import('@/components/capture/IrisDetector'), {
  ssr: false,
})

interface CapturedSlot { eye: string; angle: string }

interface CaptureClientProps {
  readingId: string
  therapistId: string
  clientName: string
  capturedSlots: CapturedSlot[]
  resumeMode: boolean
}

const ANALYSIS_W = 256
const ANALYSIS_H = 256

export function CaptureClient({
  readingId,
  therapistId: _therapistId,
  clientName,
  capturedSlots: initialCaptured,
  resumeMode: _resumeMode,
}: CaptureClientProps) {
  const videoRef = React.useRef<HTMLVideoElement>(null)
  const analysisCanvasRef = React.useRef<HTMLCanvasElement | null>(null)
  const detectorRef = React.useRef<UseIrisDetectorResult | null>(null)

  // Inicializa slotIndex baseado no que já foi capturado (D-12 + resume)
  const initialIndex = React.useMemo(() => {
    const idx = getResumeSlotIndex(initialCaptured)
    return idx === -1 ? SEQUENCE.length - 1 : idx
  }, [initialCaptured])

  const [slotIndex, setSlotIndex] = React.useState(initialIndex)
  const [phase, setPhase] = React.useState<SlotPhase>(() => {
    // Se for resume cruzando para o olho esquerdo, começar com interstitial
    if (initialIndex >= 3 && initialCaptured.every(c => c.eye !== 'left')) {
      return 'interstitial'
    }
    return 'streaming'
  })
  const [capturedCount, setCapturedCount] = React.useState(initialCaptured.length)
  const [score, setScore] = React.useState(0)
  const [check, setCheck] = React.useState<QualityCheck | null>(null)

  const slot: Slot = SEQUENCE[Math.min(slotIndex, SEQUENCE.length - 1)]

  // Loop de inferência por frame — só roda quando phase é streaming ou overlay
  React.useEffect(() => {
    const video = videoRef.current
    if (!video) return
    if (phase !== 'streaming' && phase !== 'overlay') return

    let handle: number | null = null
    const proto = HTMLVideoElement.prototype as unknown as {
      requestVideoFrameCallback?: (cb: (now: number) => void) => number
      cancelVideoFrameCallback?: (id: number) => void
    }
    const supportsRVFC = typeof proto.requestVideoFrameCallback === 'function'

    const tick = (now: number) => {
      const det = detectorRef.current
      if (det?.ready && video.readyState >= 2 && video.videoWidth > 0 && video.videoHeight > 0) {
        const result = det.detect(video, now)
        const landmarks = result?.faceLandmarks?.[0] ?? null
        if (!analysisCanvasRef.current) {
          analysisCanvasRef.current = document.createElement('canvas')
          analysisCanvasRef.current.width = ANALYSIS_W
          analysisCanvasRef.current.height = ANALYSIS_H
        }
        const canvas = analysisCanvasRef.current
        const ctx = canvas.getContext('2d', { alpha: false })
        if (ctx) {
          const minDim = Math.min(video.videoWidth, video.videoHeight)
          const sx = (video.videoWidth - minDim) / 2
          const sy = (video.videoHeight - minDim) / 2
          ctx.drawImage(video, sx, sy, minDim, minDim, 0, 0, ANALYSIS_W, ANALYSIS_H)
          const imageData = ctx.getImageData(0, 0, ANALYSIS_W, ANALYSIS_H)
          const c = computeQualityCheck(landmarks, slot.eye, imageData, ANALYSIS_W, ANALYSIS_H)
          setCheck(c)
          setScore(overallScore(c))
        }
      }
      const videoEx = video as HTMLVideoElement & {
        requestVideoFrameCallback?: (cb: (now: number) => void) => number
      }
      if (supportsRVFC && videoEx.requestVideoFrameCallback) {
        handle = videoEx.requestVideoFrameCallback(tick)
      } else {
        handle = requestAnimationFrame(tick)
      }
    }
    const videoEx = video as HTMLVideoElement & {
      requestVideoFrameCallback?: (cb: (now: number) => void) => number
      cancelVideoFrameCallback?: (id: number) => void
    }
    if (supportsRVFC && videoEx.requestVideoFrameCallback) {
      handle = videoEx.requestVideoFrameCallback(tick)
    } else {
      handle = requestAnimationFrame(tick)
    }
    return () => {
      if (handle == null) return
      if (supportsRVFC && videoEx.cancelVideoFrameCallback) {
        videoEx.cancelVideoFrameCallback(handle)
      } else {
        cancelAnimationFrame(handle)
      }
    }
  }, [phase, slot.eye])

  // Avança para o próximo slot — chamado pelo trigger stub OU por debug
  const advanceToNextSlot = React.useCallback(() => {
    const next = slotIndex + 1
    if (next >= SEQUENCE.length) {
      setPhase('finalizing')
      // 03-08 cuida da finalização real (chama finalizeReadingAction + redirect)
      // Por ora apenas marca e exibe debug
      return
    }
    if (isOuterEyeTransition(slotIndex, next)) {
      setPhase('interstitial')
    } else {
      setPhase('overlay')
    }
    setSlotIndex(next)
    setCapturedCount(c => c + 1)
  }, [slotIndex])

  // Stub do auto-trigger — em 03-07 capturará Canvas.toBlob + upload + insert
  const captureGate = useStableQualityGate(score, () => {
    if (phase !== 'streaming') return
    console.log(`[capture-client] STUB: capturing slot ${slot.eye}/${slot.angle} (idx ${slotIndex}, score ${score.toFixed(2)})`)
    advanceToNextSlot()
  })

  // Quando entra em 'overlay', auto-volta para 'streaming' após 2.5s + reset gate
  React.useEffect(() => {
    if (phase !== 'overlay') return
    const id = window.setTimeout(() => {
      setPhase('streaming')
      captureGate.reset()
    }, 2500)
    return () => window.clearTimeout(id)
  }, [phase, captureGate])

  // Reset gate quando entra em interstitial (não dispara enquanto está parado)
  React.useEffect(() => {
    if (phase === 'interstitial' || phase === 'finalizing') {
      captureGate.reset()
    }
  }, [phase, captureGate])

  const message = check ? feedbackMessage(dominantFailure(check)) : 'Aguarde — preparando câmera...'

  return (
    <div className="relative flex-1 flex flex-col">
      {/* Header */}
      <header className="absolute top-0 left-0 right-0 z-30 flex items-center justify-between px-4 py-3 pt-[env(safe-area-inset-top)]">
        <span className="text-sm text-white/80 truncate max-w-[60%]">{clientName}</span>
        <Link
          href="/leituras"
          aria-label="Cancelar leitura"
          className="rounded-full bg-black/50 backdrop-blur-sm p-2 text-white"
        >
          <X className="h-5 w-5" />
        </Link>
      </header>

      <CameraView videoRef={videoRef} />

      {/* QualityIndicator + CaptureProgress no topo */}
      {phase !== 'interstitial' && phase !== 'finalizing' && (
        <div className="absolute left-0 right-0 z-20 pt-[calc(env(safe-area-inset-top)+44px)] flex flex-col items-center gap-3">
          <QualityIndicator score={score} />
          <CaptureProgress currentIndex={slotIndex} capturedCount={capturedCount} />
        </div>
      )}

      {/* AngleOverlay quando phase==='overlay' */}
      {phase === 'overlay' && (
        <div className="absolute top-[calc(env(safe-area-inset-top)+120px)] left-1/2 -translate-x-1/2 z-25 max-w-[90%]">
          <AngleOverlay slot={slot} resetKey={`${slot.eye}_${slot.angle}`} />
        </div>
      )}

      {/* LiveFeedbackMessage no centro-inferior */}
      {phase === 'streaming' && (
        <div className="absolute left-0 right-0 bottom-[calc(env(safe-area-inset-bottom)+96px)] z-20 flex justify-center px-4">
          <LiveFeedbackMessage message={message} />
        </div>
      )}

      {/* AngleInterstitial fullscreen */}
      {phase === 'interstitial' && (
        <AngleInterstitial
          nextSlot={slot}
          onProceed={() => {
            setPhase('streaming')
            captureGate.reset()
          }}
        />
      )}

      {/* Finalizing stub — 03-08 substitui */}
      {phase === 'finalizing' && (
        <div className="absolute inset-0 z-50 bg-background flex flex-col items-center justify-center gap-4 text-foreground">
          <h1 className="text-xl font-semibold">6 de 6 imagens registradas</h1>
          <p className="text-sm text-muted-foreground">Finalizando leitura...</p>
        </div>
      )}

      {/* Lazy MediaPipe */}
      <IrisDetector
        onReady={(api) => {
          detectorRef.current = api
        }}
      />

      {/* Debug — remove em 03-08 */}
      <div className="absolute bottom-1 right-1 z-10 px-1.5 py-0.5 rounded bg-black/40 text-[10px] text-white/50">
        {phase} • {slotIndex + 1}/6 • {(score * 100).toFixed(0)}%
      </div>
    </div>
  )
}
```
  </action>
  <verify>
    <automated>cd apps/web && pnpm run lint && pnpm run build 2>&1 | tail -20 && pnpm run test:run && pnpm run audit:vocabulary</automated>
  </verify>
  <acceptance_criteria>
    - capture-client.tsx contém literais: `getResumeSlotIndex`, `isOuterEyeTransition`, `SEQUENCE`, `<AngleOverlay`, `<AngleInterstitial`, `<CaptureProgress`, `phase === 'interstitial'`, `phase === 'overlay'`
    - State machine: phase tem casos para idle, streaming, overlay, interstitial, finalizing, complete (verificar grep de cada string)
    - Auto-trigger condicionado em phase==='streaming' (verificar grep `if \(phase !== 'streaming'\) return`)
    - reset() do gate é chamado em transições (>= 3 ocorrências de captureGate.reset())
    - Build verde
    - Todos os testes vitest seguem verdes (incluindo testes de Tasks 1-2)
    - Lint clean, audit:vocabulary clean
  </acceptance_criteria>
  <done>State machine completa percorre os 6 slots na ordem; AngleOverlay aparece entre ângulos do mesmo olho; AngleInterstitial fullscreen na transição de olho; auto-trigger só dispara em phase streaming; finalizing é placeholder para 03-08.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Sequência guiada completa de 6 capturas. SEQUENCE canônica + getResumeSlotIndex testados. AngleIcon com 6 variantes SVG inline. AngleOverlay (entre ângulos do mesmo olho) + AngleInterstitial (transição de olho) + CaptureProgress (6 dots). State machine no capture-client. Captura é stub (console.log + advance) — JPEG real é o plan 03-07.
  </what-built>
  <how-to-verify>
    **Smoke iPhone real (UAT principal):**
    1. Acessar URL deployada; criar reading; ir para captura
    2. Verificar tela inicial:
       - QualityIndicator no topo
       - CaptureProgress ("1 de 6") abaixo do indicator com 1º dot ativo
       - LiveFeedbackMessage no centro-inferior
    3. Apontar câmera + manter olho centrado por 400ms+:
       - Console mostra "[capture-client] STUB: capturing slot right/frontal..."
       - AngleOverlay aparece no topo: "Próximo: olho direito — lateral" com ícone SVG seta diagonal ↗
       - Após ~2.5s, overlay minimiza para chip pequeno
       - CaptureProgress: dot 1 verde com check, dot 2 ativo (pulsando)
    4. Repetir para slot 2 (right/lateral) → slot 3 (right/backlight)
       - No backlight: AngleIcon mostra raios de sol atrás do olho
    5. Após capturar slot 3 (right/backlight) — TRANSIÇÃO DE OLHO:
       - Tela inteira muda para AngleInterstitial fullscreen
       - Heading: "Vamos para o olho esquerdo"
       - Subtítulo correto
       - Ícone grande (96×96) do olho esquerdo frontal
       - CTA "Pronto, vou capturar" em botão h-12 full-width
       - Câmera não dispara captura mesmo com score alto (gate suspended)
    6. Tap no CTA → fade-out interstitial, volta para câmera, slot atual = left/frontal
    7. Continuar para slots 4-6 com AngleOverlays apropriados (esquerdo: setas ↖ no lateral, raios atrás no backlight)
    8. Após slot 6 capturado → tela "6 de 6 imagens registradas — Finalizando leitura..."

    **Smoke resume:**
    9. Iniciar nova leitura, capturar até slot 3 (right/backlight)
    10. Recarregar a página com `?resume=true` na URL (ou simular saindo e voltando)
    11. Verificar: capture-client começa NA AngleInterstitial fullscreen (transição de olho — porque resume mostrou que próximo slot é left/frontal)

    **Smoke reduced motion:**
    12. iOS Settings → Acessibilidade → Movimento → Reduzir Movimento ON
    13. Repetir captura — verificar que pulse no CaptureProgress + animação de slide do AngleOverlay são desativados (motion-safe não dispara)

    **Audit técnico:**
    14. `pnpm run test:run` — todos os testes verdes (>= 16 sequence + outros)
    15. `pnpm run audit:vocabulary` exit 0
    16. `pnpm run build` verde
  </how-to-verify>
  <resume-signal>Reportar OK + screenshots de AngleOverlay e AngleInterstitial em iPhone, ou listar issues (ex: ordem errada, interstitial não aparece, AngleIcon quebrado em alguma variante).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lib sequence (pure) → state machine | Constantes imutáveis; sem input externo |
| capturedSlots prop → resume | Vem do server component (RLS validou); array tipado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Tampering / data integrity | SEQUENCE order | mitigate | const literal exportada; testes vitest validam ordem exata e ausência de duplicatas |
| T-03-06-02 | Logic flaw | resume slot index | mitigate | getResumeSlotIndex testado com 5 cenários (vazio, parcial, completo, fora-de-ordem, slots desconhecidos) |
| T-03-06-03 | Spoofing / false trigger | auto-capture durante interstitial | mitigate | useStableQualityGate.reset() chamado em entrada de interstitial; callback do gate verifica `if (phase !== 'streaming') return` |
| T-03-06-04 | Compliance (LGPD) | copy interstitial/overlay | mitigate | Copy verbatim de UI-SPEC §Copywriting via getInterstitialCopy(); audit:vocabulary roda no verify |
</threat_model>

<verification>
- sequence.ts com SEQUENCE + helpers testados (>= 16 cenários)
- 4 componentes (AngleIcon × 6 variantes + AngleOverlay + AngleInterstitial + CaptureProgress) renderizando corretamente
- AngleIcon.test.tsx valida as 6 combinações eye×angle
- capture-client.tsx com state machine completa: streaming↔overlay → interstitial → streaming → ... → finalizing
- Auto-trigger desativado durante interstitial e finalizing (grep verifica)
- Reduced motion respeitado (motion-safe:* nas animações)
- iPhone real: sequência percorre 6 slots em ordem, interstitial aparece na transição de olho, resume volta no slot certo
- Lint + audit + build verdes
</verification>

<success_criteria>
- [ ] sequence.ts + 16+ testes verdes
- [ ] AngleIcon, AngleOverlay, AngleInterstitial, CaptureProgress criados
- [ ] State machine no capture-client com 6 fases
- [ ] iPhone real: percorre 6 slots em ordem; interstitial entre olhos
- [ ] Resume funciona (URL com ?resume=true a partir de slot 3 → começa em interstitial)
- [ ] Reduced motion respeitado
- [ ] Lint + audit:vocabulary + build verdes
</success_criteria>

<output>
After completion, create `.planning/phases/03-captura-mobile-pwa/03-06-SUMMARY.md` documenting:
- Resultados dos testes vitest (X/Y verdes para sequence + AngleIcon)
- Screenshot de AngleOverlay em iPhone
- Screenshot de AngleInterstitial em iPhone
- Confirmação resume (testado a partir de qual slot)
- Confirmação reduced motion
- Eventuais ajustes de copy se algum termo proibido foi pego pelo audit
</output>
