---
phase: 09-polish-dogfooding-beta
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/lib/admin/dogfooding.ts
  - apps/web/lib/admin/dogfooding.test.ts
  - apps/web/app/admin/relatorios/page.tsx
autonomous: true
requirements:
  - ONBOARD-04

must_haves:
  truths:
    - "Em /admin/relatorios existe um bloco 'Dogfooding gate' visível só pro founder mostrando: data de início (2026-05-15), semanas decorridas, leituras/semana count separando is_self=true vs is_self=false, e status 'gate aberto/fechado'"
    - "A função fetchDogfoodingProgress busca readings do founder (filtra therapist_id = founder uid + por semana ISO) agregando counts por semana"
    - "Gate fecha quando: 3+ semanas consecutivas com ≥3 leituras/semana de clientes reais (is_self=false). Status calculado puramente por query — sem variable manual."
    - "Memory file project_dogfooding_gate_status.md é o registro escrito do gate (não medível por código — founder declara). UI mostra o sinal mas decisão final é dele."
  artifacts:
    - path: "apps/web/lib/admin/dogfooding.ts"
      provides: "Função fetchDogfoodingProgress(): retorna { startDate, weeksElapsed, weeklyRows: [{ week_start, week_label, real_count, self_count, qualifies }], gateClosedAt: Date | null, consecutiveQualifyingWeeks: number }"
      contains: "fetchDogfoodingProgress"
    - path: "apps/web/app/admin/relatorios/page.tsx"
      provides: "Novo bloco 'Dogfooding gate (Fase 9 / ONBOARD-04)' renderizado abaixo dos blocos existentes, usando o padrão Block + SimpleTable já presente"
      contains: "Dogfooding gate"
  key_links:
    - from: "apps/web/app/admin/relatorios/page.tsx"
      to: "apps/web/lib/admin/dogfooding.ts"
      via: "import + fetchDogfoodingProgress() em Promise.all"
      pattern: "fetchDogfoodingProgress"
    - from: "apps/web/lib/admin/dogfooding.ts"
      to: "readings table"
      via: "supabase.from('readings').select(...).gte('created_at', startDate).eq('is_self', ...)"
      pattern: "is_self"
---

<objective>
Entregar ONBOARD-04 (D-05): instrumentação do dogfooding gate em /admin/relatorios. Bloco visível só pro founder mostrando progresso semanal das leituras em clientes reais vs autoexame, com status calculado puramente por query (sem variável manual no código), e link de copia pro memory file `project_dogfooding_gate_status.md` que o founder mantém como source-of-truth narrativo.

Purpose: D-05 ("Founder declara passa/não-passa via memory") + métrica concreta visível pra reduzir auto-engano. Sem mini-dashboard, founder pode achatar mentalmente "tô usando bastante" quando na verdade são autoexames consecutivos. Sinal numérico mantém honestidade.

Output: 1 módulo de query + 1 arquivo de teste + 1 bloco novo em admin/relatorios/page.tsx renderizado abaixo do bloco "Aproveitamento por dispositivo".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-polish-dogfooding-beta/09-CONTEXT.md

# Pattern references
@apps/web/app/admin/relatorios/page.tsx
@apps/web/lib/auth/founder.ts

<interfaces>
<!-- Estado canonico: readings.is_self é boolean (cliente.is_self → reading via JOIN) -->
<!-- Founder começou uso real 2026-05-15 (Sonnet 2x v2.3.0 LIVE) — fonte: STATE.md + 09-CONTEXT.md -->
<!-- Hoje 2026-05-26 — ~1.5 semanas decorridas -->

// Constantes a usar:
const DOGFOODING_START_DATE = '2026-05-15'  // founder começou uso diário
const WEEKLY_THRESHOLD = 3                  // ≥3 leituras/semana
const CONSECUTIVE_WEEKS_REQUIRED = 3        // 3 semanas consecutivas

// readings.is_self vem via JOIN com clients.is_self (clients table — boolean).
// NÃO há reading.is_self direto — confirmar via grep em route handlers.

// Founder identity: process.env.FOUNDER_EMAIL ✗→ id is auth.users where email matches.
// Mas pra agregar por semana, precisa do user.id do founder — buscar via
// supabase.auth.admin.listUsers + filtrar email === FOUNDER_EMAIL.
// Alternativa: já há um isFounderEmail() check em /admin/relatorios/page.tsx;
// reuse via supabase.auth.getUser() no próprio handler (já chamado linha 41).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Módulo lib/admin/dogfooding.ts — fetchDogfoodingProgress + teste</name>
  <files>apps/web/lib/admin/dogfooding.ts, apps/web/lib/admin/dogfooding.test.ts</files>
  <read_first>
    - apps/web/app/admin/relatorios/page.tsx (linhas 16-25 — imports de lib/admin/reports; padrão de fetch helpers)
    - apps/web/lib/auth/founder.ts (isFounderEmail check pattern)
    - Estrutura geral de lib/admin/* (provavelmente existe lib/admin/reports.ts — espelhar)
  </read_first>
  <behavior>
    - Test 1: Sem founderUserId (null) → retorna { startDate, weeksElapsed: 0, weeklyRows: [], gateClosedAt: null, consecutiveQualifyingWeeks: 0 }
    - Test 2: Com founderUserId mas zero readings no período → weeklyRows tem buckets vazios pra cada semana entre startDate e hoje + consecutiveQualifyingWeeks: 0 + gateClosedAt: null
    - Test 3: 3 semanas consecutivas com 3+ readings de clientes is_self=false cada → consecutiveQualifyingWeeks: 3 + gateClosedAt: <date no fim da 3ª semana>
    - Test 4: Mix de is_self=true (autoexame) e is_self=false (clientes reais) — só is_self=false conta pro qualifies
    - Test 5: 2 semanas qualifying + 1 semana NÃO qualifying + 2 semanas qualifying → consecutiveQualifyingWeeks reseta na quebra (retorna 2 do trecho mais recente, não 4 total)
    - Test 6: weekly_rows ordering — mais recente primeiro (DESC por week_start)
    - Test 7: week_label em format pt-BR "DD/MM" tipo "26/05–01/06"
    - Test 8: Edge case 2026-05-15 (start) — primeira semana inclui exatamente 2026-05-15 + 6 dias após
  </behavior>
  <action>
    Criar `apps/web/lib/admin/dogfooding.ts`:

    ```typescript
    import 'server-only'
    import { createClient } from '@/lib/supabase/server'

    // Fase 9 / ONBOARD-04 / D-05: dogfooding gate strict.
    // Founder começou uso diário 2026-05-15 (Sonnet 2x v2.3.0 LIVE, ref STATE.md).
    // Gate fecha = 3 semanas consecutivas com ≥3 leituras/semana de clientes reais.
    export const DOGFOODING_START_DATE = '2026-05-15'
    export const WEEKLY_THRESHOLD = 3
    export const CONSECUTIVE_WEEKS_REQUIRED = 3

    export interface DogfoodingWeekRow {
      week_start: string         // YYYY-MM-DD da segunda-feira ISO
      week_label: string         // "26/05–01/06" pt-BR
      real_count: number         // is_self=false readings
      self_count: number         // is_self=true readings
      qualifies: boolean         // real_count >= WEEKLY_THRESHOLD
    }

    export interface DogfoodingProgress {
      startDate: string
      weeksElapsed: number
      weeklyRows: DogfoodingWeekRow[]
      gateClosedAt: string | null
      consecutiveQualifyingWeeks: number
    }

    export async function fetchDogfoodingProgress(
      founderUserId: string | null,
    ): Promise<DogfoodingProgress> {
      const startDate = DOGFOODING_START_DATE
      const today = new Date()
      const startDateObj = new Date(startDate + 'T00:00:00Z')
      const weeksElapsed = Math.max(
        0,
        Math.ceil((today.getTime() - startDateObj.getTime()) / (7 * 24 * 60 * 60 * 1000))
      )

      if (!founderUserId) {
        return {
          startDate,
          weeksElapsed: 0,
          weeklyRows: [],
          gateClosedAt: null,
          consecutiveQualifyingWeeks: 0,
        }
      }

      const supabase = await createClient()
      const { data: readings } = await supabase
        .from('readings')
        .select('created_at, client:clients(is_self)')
        .eq('therapist_id', founderUserId)
        .gte('created_at', startDate + 'T00:00:00Z')
        .order('created_at', { ascending: false })

      // Bucket por semana ISO (segunda-feira como início).
      const bucketMap = new Map<string, { real: number; self: number }>()
      for (const r of readings ?? []) {
        const created = new Date(r.created_at as string)
        const monday = startOfIsoWeek(created)
        const key = monday.toISOString().slice(0, 10)
        const cur = bucketMap.get(key) ?? { real: 0, self: 0 }
        const isSelf = (r.client as { is_self?: boolean } | null)?.is_self === true
        if (isSelf) cur.self += 1
        else cur.real += 1
        bucketMap.set(key, cur)
      }

      // Gerar buckets de TODAS as semanas (mesmo as vazias) entre startDate e hoje.
      const allWeeks: DogfoodingWeekRow[] = []
      let cursor = startOfIsoWeek(startDateObj)
      const todayMonday = startOfIsoWeek(today)
      while (cursor <= todayMonday) {
        const key = cursor.toISOString().slice(0, 10)
        const counts = bucketMap.get(key) ?? { real: 0, self: 0 }
        allWeeks.push({
          week_start: key,
          week_label: formatWeekLabel(cursor),
          real_count: counts.real,
          self_count: counts.self,
          qualifies: counts.real >= WEEKLY_THRESHOLD,
        })
        cursor = new Date(cursor.getTime() + 7 * 24 * 60 * 60 * 1000)
      }

      // Ordering DESC (mais recente primeiro) pra display.
      allWeeks.sort((a, b) => (a.week_start < b.week_start ? 1 : -1))

      // consecutiveQualifyingWeeks: conta a streak MAIS RECENTE no ASC order.
      // Reverte allWeeks pra ASC, depois conta do final.
      const ascWeeks = [...allWeeks].reverse()
      let streak = 0
      for (let i = ascWeeks.length - 1; i >= 0; i--) {
        if (ascWeeks[i].qualifies) streak += 1
        else break
      }

      // gateClosedAt: se streak >= CONSECUTIVE_WEEKS_REQUIRED, retorna o week_start
      // da semana qualifying mais recente (proxy de "quando fechou").
      const gateClosedAt =
        streak >= CONSECUTIVE_WEEKS_REQUIRED
          ? ascWeeks[ascWeeks.length - 1].week_start
          : null

      return {
        startDate,
        weeksElapsed,
        weeklyRows: allWeeks,
        gateClosedAt,
        consecutiveQualifyingWeeks: streak,
      }
    }

    // ── helpers (não exportados — keep module surface mínima) ──

    function startOfIsoWeek(d: Date): Date {
      const utc = new Date(Date.UTC(d.getUTCFullYear(), d.getUTCMonth(), d.getUTCDate()))
      const day = utc.getUTCDay() || 7  // 0=Sunday → 7
      utc.setUTCDate(utc.getUTCDate() - (day - 1))
      return utc
    }

    function formatWeekLabel(monday: Date): string {
      const sunday = new Date(monday.getTime() + 6 * 24 * 60 * 60 * 1000)
      const dd = (n: number) => n.toString().padStart(2, '0')
      return `${dd(monday.getUTCDate())}/${dd(monday.getUTCMonth() + 1)}–${dd(sunday.getUTCDate())}/${dd(sunday.getUTCMonth() + 1)}`
    }
    ```

    Criar `apps/web/lib/admin/dogfooding.test.ts`:
    - Mock `@/lib/supabase/server` createClient retornando supabase mock com from().select().eq().gte().order() chain
    - 8 cases conforme `<behavior>`
    - Use synthetic readings com created_at em datas-chave: 2026-05-15, 2026-05-20, 2026-05-25
    - Mock Date global pra controlar "hoje" (vi.useFakeTimers + vi.setSystemTime('2026-05-26'))

    NÃO criar UI de declaração manual ("declarar fechado"). NÃO criar memory tracker em DB. Founder mantém memory file separado — UI mostra sinal apenas.
  </action>
  <acceptance_criteria>
    - Arquivo `apps/web/lib/admin/dogfooding.ts` existe e começa com `import 'server-only'`.
    - `grep -c "export async function fetchDogfoodingProgress" apps/web/lib/admin/dogfooding.ts` retorna 1.
    - `grep -c "DOGFOODING_START_DATE = '2026-05-15'" apps/web/lib/admin/dogfooding.ts` retorna 1.
    - `grep -c "WEEKLY_THRESHOLD = 3" apps/web/lib/admin/dogfooding.ts` retorna 1.
    - `grep -c "CONSECUTIVE_WEEKS_REQUIRED = 3" apps/web/lib/admin/dogfooding.ts` retorna 1.
    - `grep -c "consecutiveQualifyingWeeks" apps/web/lib/admin/dogfooding.ts` retorna ≥2 (return + sums).
    - `grep -c "gateClosedAt" apps/web/lib/admin/dogfooding.ts` retorna ≥2.
    - LGPD audit: `grep -v '^[[:space:]]*//' apps/web/lib/admin/dogfooding.ts | grep -cE 'diagnóstico|tratamento|cura'` retorna 0.
    - `pnpm --filter @aurel/web vitest run lib/admin/dogfooding.test.ts` → 8/8 GREEN.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && pnpm vitest run lib/admin/dogfooding.test.ts</automated>
  </verify>
  <done>
    Módulo fetchDogfoodingProgress criado com bucket-by-week + qualifies threshold + streak counter + gateClosedAt derivation. 8 testes GREEN cobrindo edge cases (zero, full streak, broken streak, autoexame mix, ordering, week labels).
  </done>
</task>

<task type="auto">
  <name>Task 2: Bloco "Dogfooding gate" em /admin/relatorios/page.tsx</name>
  <files>apps/web/app/admin/relatorios/page.tsx</files>
  <read_first>
    - apps/web/app/admin/relatorios/page.tsx (toda a estrutura — Block component, SimpleTable component, fetchXxx pattern, ordering dos blocks 1-5)
    - apps/web/lib/admin/dogfooding.ts (Task 1)
  </read_first>
  <action>
    Editar `apps/web/app/admin/relatorios/page.tsx`:

    1. Adicionar import no topo (junto aos outros lib/admin/* imports linha ~9):
       ```typescript
       import { fetchDogfoodingProgress, CONSECUTIVE_WEEKS_REQUIRED, WEEKLY_THRESHOLD } from '@/lib/admin/dogfooding'
       ```

    2. Adicionar fetch ao Promise.all (linha ~56). Founder user.id já está disponível (linha 41 user.id):
       ```typescript
       const therapistsMap = await fetchTherapistsMap()
       const [funnel, quality, cost, throughput, devices, dogfooding] = await Promise.all([
         fetchFunnel(range),
         fetchQuality(range),
         fetchCost(range),
         fetchThroughput(range, therapistsMap),
         fetchDeviceBreakdown(range),
         fetchDogfoodingProgress(user.id),
       ])
       ```

    3. Adicionar bloco novo ABAIXO de "Bloco 5: Aproveitamento por dispositivo" (linha ~448, depois do </Block> dele) e ANTES do `</div>` final do return:
       ```tsx
       {/* Bloco 6: Dogfooding gate (Fase 9 / ONBOARD-04) */}
       <Block title={`Dogfooding gate — ONBOARD-04 (Fase 9)`}>
         <div className="grid gap-4 sm:grid-cols-2">
           <SimpleTable
             headers={['Métrica', 'Valor']}
             rows={[
               ['Data de início', new Date(dogfooding.startDate).toLocaleDateString('pt-BR')],
               ['Semanas decorridas', dogfooding.weeksElapsed.toString()],
               [
                 `Semanas consecutivas qualifying (≥${WEEKLY_THRESHOLD} reais/sem)`,
                 `${dogfooding.consecutiveQualifyingWeeks} / ${CONSECUTIVE_WEEKS_REQUIRED}`,
               ],
               [
                 'Status do gate',
                 dogfooding.gateClosedAt
                   ? `🎯 FECHADO em ${new Date(dogfooding.gateClosedAt).toLocaleDateString('pt-BR')}`
                   : '⏳ ABERTO',
               ],
             ]}
           />
           <div className="space-y-2">
             <h4 className="text-xs font-medium uppercase tracking-wide text-muted-foreground">
               Histórico semanal
             </h4>
             <SimpleTable
               headers={['Semana', 'Reais', 'Autoexame', 'Total', 'Qualifica?']}
               rows={
                 dogfooding.weeklyRows.length === 0
                   ? [['—', '0', '0', '0', '—']]
                   : dogfooding.weeklyRows.map((w) => [
                       w.week_label,
                       w.real_count.toString(),
                       w.self_count.toString(),
                       (w.real_count + w.self_count).toString(),
                       w.qualifies ? '✓' : '—',
                     ])
               }
             />
           </div>
         </div>
         <p className="mt-4 text-xs text-muted-foreground">
           Critério (D-05): {CONSECUTIVE_WEEKS_REQUIRED} semanas consecutivas com ≥{WEEKLY_THRESHOLD} leituras
           em clientes reais (is_self=false), sem notas paralelas. Founder declara
           passa/não-passa via memory <span className="font-mono">project_dogfooding_gate_status</span>.
         </p>
       </Block>
       ```

    4. NÃO remover blocks existentes. NÃO renumerar. NÃO mudar imports existentes. NÃO mudar handler de search params.

    LGPD audit no novo JSX: zero "diagnóstico"/"tratamento"/"cura" — strings em pt-BR são "leituras", "clientes reais", "autoexame", "qualifying", "gate". Acceptance grep confirma.
  </action>
  <acceptance_criteria>
    - `grep -c "fetchDogfoodingProgress" apps/web/app/admin/relatorios/page.tsx` retorna ≥2 (import + chamada).
    - `grep -c "Dogfooding gate" apps/web/app/admin/relatorios/page.tsx` retorna ≥1.
    - `grep -c "consecutiveQualifyingWeeks" apps/web/app/admin/relatorios/page.tsx` retorna ≥1.
    - `grep -c "weeklyRows" apps/web/app/admin/relatorios/page.tsx` retorna ≥1.
    - `grep -c "ONBOARD-04" apps/web/app/admin/relatorios/page.tsx` retorna ≥1.
    - LGPD audit prose-grep: `grep -v '^[[:space:]]*\\(//\\|\\*\\)' apps/web/app/admin/relatorios/page.tsx | grep -cE '\\b(diagnóstico|tratamento|cura)\\b'` retorna 0 (baseline-preserved — already pre-existing 7 hits in unrelated lib/anthropic/* per MEMORY).
    - Ordering check: linha do `Dogfooding gate` aparece DEPOIS do bloco `Aproveitamento por dispositivo` (linha do título "Aproveitamento por dispositivo").
    - `pnpm --filter @aurel/web tsc --noEmit` baseline preservado.
    - `pnpm --filter @aurel/web lint --max-warnings 0` baseline preservado.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit 2>&1 | grep "app/admin/relatorios/page\\.tsx" | grep -v "^$" && echo "TSC ERROR" && exit 1 || echo "TSC OK"</automated>
  </verify>
  <done>
    Bloco 6 "Dogfooding gate" renderizado em /admin/relatorios mostrando 4 KPIs (start, weeks elapsed, streak X/3, status) + histórico semanal com real/self/total/qualifies. Founder gate (isFounderEmail) já enforça via layout.tsx parent — sem mudança de gate. tsc + lint baseline preservados.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /admin/relatorios → fetchDogfoodingProgress | Server-only. Founder gate via isFounderEmail no parent (page.tsx linha 41). |
| supabase query → readings + clients(is_self) | RLS filtra por therapist_id pertencente ao user. founder.id = user.id no contexto. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Spoofing | Non-founder acessa /admin/relatorios e vê dogfooding count | mitigate | isFounderEmail check linha 42-44 em page.tsx mantém notFound() — defense-in-depth + middleware /admin gate. |
| T-09-04-02 | Information disclosure | Counts de readings de outros terapeutas vazariam | mitigate | fetchDogfoodingProgress filtra .eq('therapist_id', founderUserId) — só founder próprio. |
| T-09-04-03 | Tampering | Threshold ou consecutive_weeks change inadvertido | accept | Constantes literais no módulo — change requer code edit + push. |
| T-09-04-04 | Repudiation | Founder pode auto-enganar declarando passa antes do gate | accept | Memory file é narrativo, fora do código. UI mostra streak X/3 — bypass requer ignorar o número. Sinal-only. |
| T-09-04-05 | Information disclosure | LGPD vocab no copy do bloco | mitigate | Acceptance grep confirma zero hits no novo JSX. |
</threat_model>

<verification>
- 8 testes vitest GREEN em dogfooding.test.ts
- Manual smoke (founder): /admin/relatorios renderiza o bloco mostrando "Semanas consecutivas qualifying: 1/3" + "Status: ABERTO" + histórico semanal com counts reais (depende do uso real desde 2026-05-15).
- Non-founder hitando /admin/relatorios continua dando notFound() (gate inalterado).
- LGPD audit prose-grep clean nos 2 arquivos modificados.
- tsc + lint baseline preservados.
</verification>

<success_criteria>
- ONBOARD-04 instrumentation entregue (sinal numérico do gate).
- D-05 honrado (3 semanas consecutivas + ≥3/sem + clientes reais; founder declara via memory).
- Commit atômico: `feat(09-04): dogfooding gate progress block in admin reports (ONBOARD-04)`
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish-dogfooding-beta/09-04-SUMMARY.md` documenting:
- 2 arquivos modificados (lib + admin/relatorios) + 1 arquivo de teste
- 8 testes GREEN
- Manual smoke OK (founder confirma bloco visível + counts coerentes com uso desde 2026-05-15)
- Memory file `project_dogfooding_gate_status.md` mencionado como source-of-truth narrativo (criação fora deste plan — founder mantém)
</output>
