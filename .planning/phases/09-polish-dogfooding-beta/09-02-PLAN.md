---
phase: 09-polish-dogfooding-beta
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - apps/web/components/dashboard/onboarding-wizard.tsx
  - apps/web/components/dashboard/onboarding-wizard.test.tsx
  - apps/web/app/actions/onboarding.ts
  - apps/web/app/actions/onboarding.test.ts
  - apps/web/app/(dashboard)/dashboard/page.tsx
autonomous: true
requirements:
  - ONBOARD-01

must_haves:
  truths:
    - "Após magic-link login, terapeuta novo (sem dismiss + sem clientes + sem readings) vê banner 'Vamos começar (X de 3)' no topo da dashboard"
    - "Terapeuta com perfil completo + ≥1 cliente + ≥1 reading NÃO vê banner (terapeutas existentes pré-fase 9 NÃO regridem)"
    - "Terapeuta clica 'Pular' → dismissOnboardingAction grava profiles.onboarding_dismissed_at=now() → banner some no próximo render server-side"
    - "Cada step tem CTA navegando pra rota existente: Step 1→/perfil/completar (caso ainda incompleto após middleware gate), Step 2→/clientes/novo, Step 3→/leituras/nova"
    - "Estado de cada step é derivado de DB no server (perfil completo? COUNT(clients) > 0? COUNT(readings) > 0?) — NÃO state client persistido"
  artifacts:
    - path: "apps/web/components/dashboard/onboarding-wizard.tsx"
      provides: "Componente RSC OnboardingWizard que recebe props {step1Complete, step2Complete, step3Complete, onDismiss} e renderiza card hero com checkmarks + CTA + 'Pular' button"
      contains: "OnboardingWizard"
    - path: "apps/web/app/actions/onboarding.ts"
      provides: "Server Action dismissOnboardingAction() → UPDATE profiles.onboarding_dismissed_at=now() WHERE id=auth.uid() + revalidatePath('/dashboard')"
      contains: "dismissOnboardingAction"
    - path: "apps/web/app/(dashboard)/dashboard/page.tsx"
      provides: "Renderiza <OnboardingWizard /> condicionalmente acima de SummaryCards quando shouldShowWizard==true"
      contains: "OnboardingWizard"
  key_links:
    - from: "apps/web/app/(dashboard)/dashboard/page.tsx"
      to: "apps/web/components/dashboard/onboarding-wizard.tsx"
      via: "import + render condicional"
      pattern: "OnboardingWizard"
    - from: "apps/web/components/dashboard/onboarding-wizard.tsx"
      to: "apps/web/app/actions/onboarding.ts"
      via: "form action / button onClick → dismissOnboardingAction"
      pattern: "dismissOnboardingAction"
    - from: "apps/web/app/actions/onboarding.ts"
      to: "profiles table"
      via: "supabase.from('profiles').update({ onboarding_dismissed_at: ... })"
      pattern: "onboarding_dismissed_at"
---

<objective>
Entregar ONBOARD-01 (D-02, D-03): wizard inline na dashboard com 3 steps (Perfil → 1º cliente → 1ª leitura), skipable via "Pular", state derivado de DB, sem rota dedicada `/onboarding`.

Purpose: Terapeuta nova convidada via Fase 11 (`/admin/terapeutas`) chega no /dashboard via magic-link e precisa de um nudge UX que mostre os 3 primeiros passos pra chegar à primeira leitura em ≤30min. Sem onboarding mandatory — terapeutas existentes (founder, Cristiane-pop) NÃO regridem porque já têm clientes + readings → banner some.

Output: 1 componente RSC + 1 server action + edit em dashboard/page.tsx + 2 arquivos de teste (TDD pra component + action).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-polish-dogfooding-beta/09-CONTEXT.md
@.planning/phases/09-polish-dogfooding-beta/09-01-SUMMARY.md

# Pattern references
@apps/web/components/dashboard/summary-cards.tsx
@apps/web/app/(dashboard)/dashboard/page.tsx
@apps/web/app/actions/profile.ts
@apps/web/lib/gates/therapist-profile.ts

<interfaces>
<!-- Tipos e contratos relevantes -->

// profiles.Row pós-Plan 09-01 (types/database.ts):
profiles.Row: {
  id: string
  full_name: string
  phone: string | null
  specialties: string[] | null
  tos_accepted_at: string | null
  onboarding_dismissed_at: string | null  // NEW from 09-01
  // ...other fields
}

// evaluateTherapistProfile (lib/gates/therapist-profile.ts):
export function evaluateTherapistProfile(p: {
  phone: string | null
  specialties: string[] | null
  tos_accepted_at: string | null
}): { status: 'ok' } | { status: 'incomplete'; missing: TherapistGap[] }

// IMPORTANTE: middleware já força /perfil/completar antes do dashboard.
// Step 1 do wizard SEMPRE chega como complete=true em produção normal — mas
// componente NÃO assume isso: trata os 3 booleans uniformemente pra ser
// resiliente a edge cases (cache stale, race condition no signup).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Server Action dismissOnboardingAction + teste</name>
  <files>apps/web/app/actions/onboarding.ts, apps/web/app/actions/onboarding.test.ts</files>
  <read_first>
    - apps/web/app/actions/profile.ts (pattern canônico de Server Action — 'use server' directive, auth gate via getUser, redirect on unauth, supabase.from update, retorna {error?} ou void)
    - apps/web/app/actions/admin-therapists.ts (pattern de retorno com {ok, error?} pra surfaces que precisam feedback toast)
    - MEMORY rule feedback_use_server_export_hygiene.md (files com 'use server' SÓ exportam async functions — tipos/consts ficam em sibling .schemas.ts)
  </read_first>
  <behavior>
    - Test 1: dismissOnboardingAction sem sessão → retorna { ok: false, error: 'Unauthenticated' } (NÃO redirect — server action chamado de client toast precisa do retorno)
    - Test 2: dismissOnboardingAction com sessão válida → chama supabase.from('profiles').update({ onboarding_dismissed_at: <ISO timestamp> }).eq('id', user.id) exatamente 1x
    - Test 3: dismissOnboardingAction com sessão válida e update OK → retorna { ok: true } + chama revalidatePath('/dashboard') 1x
    - Test 4: dismissOnboardingAction com sessão válida mas update DB falha → retorna { ok: false, error: <string> } sem revalidatePath
    - Test 5: timestamp passado pro update é new Date().toISOString() formato (Z suffix, válido ISO-8601)
  </behavior>
  <action>
    Criar `apps/web/app/actions/onboarding.ts`:

    ```typescript
    'use server'

    import { revalidatePath } from 'next/cache'
    import { createClient } from '@/lib/supabase/server'

    // 'use server' rule: SÓ async functions exportadas (memory feedback_use_server_export_hygiene).
    // Sem types/interfaces aqui — retorno inline.
    export async function dismissOnboardingAction(): Promise<{ ok: boolean; error?: string }> {
      const supabase = await createClient()
      const {
        data: { user },
        error: authError,
      } = await supabase.auth.getUser()
      if (!user || authError) {
        return { ok: false, error: 'Unauthenticated' }
      }

      const { error } = await supabase
        .from('profiles')
        .update({ onboarding_dismissed_at: new Date().toISOString() })
        .eq('id', user.id)

      if (error) {
        return { ok: false, error: error.message }
      }

      revalidatePath('/dashboard')
      return { ok: true }
    }
    ```

    Criar `apps/web/app/actions/onboarding.test.ts` com vitest:
    - Mockar `@/lib/supabase/server` createClient retornando supabase mock object com auth.getUser + from().update().eq()
    - Mockar `next/cache` revalidatePath
    - 5 cases conforme `<behavior>` acima
    - Usar pattern de vi.hoisted() pra mock factories (per MEMORY feedback fix em Plan 06-11)

    Per D-04: hook em 09-03 vai usar a coluna `notification_sent_at` separadamente — esta action SÓ mexe em onboarding_dismissed_at.
  </action>
  <acceptance_criteria>
    - Arquivo `apps/web/app/actions/onboarding.ts` existe e começa com `'use server'`.
    - `grep -c "export async function dismissOnboardingAction" apps/web/app/actions/onboarding.ts` retorna 1.
    - `grep -c "^export " apps/web/app/actions/onboarding.ts` retorna 1 (SÓ a função async — sem export const/type/interface).
    - `grep -c "onboarding_dismissed_at" apps/web/app/actions/onboarding.ts` retorna 1.
    - `grep -c "revalidatePath('/dashboard')" apps/web/app/actions/onboarding.ts` retorna 1.
    - `grep -c "getUser" apps/web/app/actions/onboarding.ts` retorna ≥1 (auth gate).
    - `pnpm --filter @aurel/web vitest run app/actions/onboarding.test.ts` → 5/5 GREEN.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && pnpm vitest run app/actions/onboarding.test.ts</automated>
  </verify>
  <done>
    Server Action criada com auth gate + UPDATE idempotente + revalidatePath. 5 testes vitest GREEN cobrindo unauth, happy path, DB error, ISO timestamp shape, revalidate invocation.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Componente OnboardingWizard (RSC) + teste</name>
  <files>apps/web/components/dashboard/onboarding-wizard.tsx, apps/web/components/dashboard/onboarding-wizard.test.tsx</files>
  <read_first>
    - apps/web/components/dashboard/summary-cards.tsx (Card/CardContent/CardHeader pattern, grid responsive, Link wrapper, Lucide icons)
    - apps/web/app/admin/terapeutas/InviteTherapistForm.tsx (client component pattern com useTransition + sonner toast pra server action call)
    - apps/web/app/actions/onboarding.ts (Server Action criada na Task 1)
  </read_first>
  <behavior>
    - Test 1: Quando step1/2/3 todos false → render mostra "Vamos começar (0 de 3)" + 3 steps com bullet vazio + 3 CTAs ativos + botão "Pular"
    - Test 2: Quando step1=true, step2=false, step3=false → render mostra "Continue (1 de 3)" + step 1 com checkmark + steps 2/3 com bullets + CTAs ativos só nos não-completos
    - Test 3: Quando step1/2/3 todos true → componente retorna null (não renderiza)
    - Test 4: Step 1 CTA aponta href="/perfil/completar"
    - Test 5: Step 2 CTA aponta href="/clientes/novo"
    - Test 6: Step 3 CTA aponta href="/leituras/nova"
    - Test 7: Botão "Pular" tem data-testid="onboarding-dismiss-btn" + é um <form action={dismissOnboardingAction}> OU <button onClick> que chama dismissOnboardingAction (client component híbrido)
    - Test 8: Card hero é wrapped em <section data-testid="onboarding-wizard"> pra dashboard/page.tsx test conseguir absence-assertion (Plan 07.4-07 precedent)
  </behavior>
  <action>
    Criar `apps/web/components/dashboard/onboarding-wizard.tsx` — RSC server component (sem 'use client') que recebe os 3 booleans + tem botão dismiss via form action:

    ```typescript
    import Link from 'next/link'
    import { Check, Circle, User, Users, FileText } from 'lucide-react'
    import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
    import { Button } from '@/components/ui/button'
    import { buttonVariants } from '@/components/ui/button'
    import { cn } from '@/lib/utils'
    import { dismissOnboardingAction } from '@/app/actions/onboarding'

    interface OnboardingWizardProps {
      step1Complete: boolean  // Perfil completo (evaluateTherapistProfile === 'ok')
      step2Complete: boolean  // ≥1 cliente cadastrado (incluindo is_self)
      step3Complete: boolean  // ≥1 reading criada (qualquer status)
    }

    const STEPS = [
      { id: 1, title: 'Complete seu perfil', desc: 'Telefone, especialidades e aceite dos termos.', href: '/perfil/completar', Icon: User },
      { id: 2, title: 'Cadastre o primeiro cliente', desc: 'Pode ser cliente real ou autoexame seu.', href: '/clientes/novo', Icon: Users },
      { id: 3, title: 'Inicie a primeira leitura', desc: 'Suba 6 fotos da íris ou envie convite ao cliente.', href: '/leituras/nova', Icon: FileText },
    ] as const

    export function OnboardingWizard({ step1Complete, step2Complete, step3Complete }: OnboardingWizardProps) {
      const states = [step1Complete, step2Complete, step3Complete]
      const completedCount = states.filter(Boolean).length

      // Short-circuit: já completou tudo, não renderiza.
      if (completedCount === 3) return null

      return (
        <section
          data-testid="onboarding-wizard"
          className="rounded-md border-2 border-teal-700/20 bg-card p-6 space-y-4"
        >
          <header className="flex flex-wrap items-start justify-between gap-3">
            <div>
              <h2 className="text-lg font-semibold tracking-tight">
                Vamos começar ({completedCount} de 3)
              </h2>
              <p className="text-sm text-muted-foreground">
                Três passos pra você gerar a primeira leitura iridológica.
              </p>
            </div>
            <form action={dismissOnboardingAction}>
              <Button
                type="submit"
                variant="ghost"
                size="sm"
                data-testid="onboarding-dismiss-btn"
              >
                Pular
              </Button>
            </form>
          </header>

          <ol className="grid gap-3 sm:grid-cols-3">
            {STEPS.map((step, idx) => {
              const done = states[idx]
              return (
                <li
                  key={step.id}
                  className={cn(
                    'rounded-md border bg-background p-4 space-y-2',
                    done && 'opacity-60'
                  )}
                  data-testid={`onboarding-step-${step.id}`}
                >
                  <div className="flex items-center gap-2">
                    {done ? (
                      <Check className="h-4 w-4 text-teal-700" aria-label="Concluído" />
                    ) : (
                      <Circle className="h-4 w-4 text-muted-foreground" aria-label="Pendente" />
                    )}
                    <step.Icon className="h-4 w-4 text-muted-foreground" />
                    <span className="text-sm font-medium">{step.title}</span>
                  </div>
                  <p className="text-xs text-muted-foreground">{step.desc}</p>
                  {!done && (
                    <Link
                      href={step.href}
                      className={cn(buttonVariants({ variant: 'outline', size: 'sm' }), 'w-full')}
                    >
                      Começar
                    </Link>
                  )}
                </li>
              )
            })}
          </ol>
        </section>
      )
    }
    ```

    Criar `apps/web/components/dashboard/onboarding-wizard.test.tsx` com vitest + @testing-library/react:
    - 8 cases conforme `<behavior>` acima
    - Mock `next/link`, mock `@/app/actions/onboarding` (dismissOnboardingAction)
    - Pra Test 7 (form action): assert que `<form>` element existe + `<button data-testid="onboarding-dismiss-btn">` está dentro do form
    - Pra Test 3 (null return): assert `container.firstChild === null` quando todos true
    - Usar pattern react-testing-library precedent dos plans 07.4-* (queryByTestId + getByRole + container.querySelector)

    LGPD audit: o componente NÃO usa "diagnóstico"/"tratamento"/"cura" — strings em pt-BR são "leitura iridológica" + "perfil" + "cliente" + "passos" + "Pular" + "Começar". Acceptance grep confirma.
  </action>
  <acceptance_criteria>
    - Arquivo `apps/web/components/dashboard/onboarding-wizard.tsx` existe e NÃO contém 'use client' directive.
    - `grep -c "export function OnboardingWizard" apps/web/components/dashboard/onboarding-wizard.tsx` retorna 1.
    - `grep -c "step1Complete\\|step2Complete\\|step3Complete" apps/web/components/dashboard/onboarding-wizard.tsx` retorna ≥3.
    - `grep -c "dismissOnboardingAction" apps/web/components/dashboard/onboarding-wizard.tsx` retorna ≥1.
    - `grep -cE "/perfil/completar|/clientes/novo|/leituras/nova" apps/web/components/dashboard/onboarding-wizard.tsx` retorna ≥3 (1 hit por step).
    - LGPD audit: `grep -v '^[[:space:]]*//' apps/web/components/dashboard/onboarding-wizard.tsx | grep -cE 'diagnóstico|tratamento|cura'` retorna 0.
    - `grep -c "data-testid=\"onboarding-wizard\"" apps/web/components/dashboard/onboarding-wizard.tsx` retorna 1.
    - `grep -c "data-testid=\"onboarding-dismiss-btn\"" apps/web/components/dashboard/onboarding-wizard.tsx` retorna 1.
    - `pnpm --filter @aurel/web vitest run components/dashboard/onboarding-wizard.test.tsx` → 8/8 GREEN.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && pnpm vitest run components/dashboard/onboarding-wizard.test.tsx</automated>
  </verify>
  <done>
    Componente RSC criado com 3 steps + checkmarks + CTAs condicionais + form-action dismiss. 8 testes GREEN. LGPD audit clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire-up em dashboard/page.tsx — fetch state derivado + render condicional</name>
  <files>apps/web/app/(dashboard)/dashboard/page.tsx</files>
  <read_first>
    - apps/web/app/(dashboard)/dashboard/page.tsx (estrutura atual — DashboardPage RSC com countClientes + profile select)
    - apps/web/lib/gates/therapist-profile.ts (evaluateTherapistProfile retorna { status: 'ok' | 'incomplete' })
    - apps/web/components/dashboard/onboarding-wizard.tsx (criado Task 2 — props {step1Complete, step2Complete, step3Complete})
  </read_first>
  <action>
    Editar `apps/web/app/(dashboard)/dashboard/page.tsx` pra:

    1. Importar `OnboardingWizard` de `@/components/dashboard/onboarding-wizard`
    2. Importar `evaluateTherapistProfile` de `@/lib/gates/therapist-profile`
    3. Expandir o profile select pra incluir os campos do wizard:
       ```typescript
       const { data: profile } = await supabase
         .from('profiles')
         .select('subscription_status, trial_ends_at, beta_readings_used, phone, specialties, tos_accepted_at, onboarding_dismissed_at')
         .eq('id', user?.id ?? '')
         .single()
       ```
    4. Adicionar um COUNT de readings em paralelo com clientsCount (Promise.all):
       ```typescript
       const [{ count: clientsCount }, { count: readingsCount }, { data: profile }] = await Promise.all([
         supabase.from('clients').select('*', { count: 'exact', head: true }),
         supabase.from('readings').select('*', { count: 'exact', head: true }),
         supabase.from('profiles').select('subscription_status, trial_ends_at, beta_readings_used, phone, specialties, tos_accepted_at, onboarding_dismissed_at').eq('id', user?.id ?? '').single(),
       ])
       ```
    5. Calcular os 3 booleans:
       ```typescript
       const therapistGate = evaluateTherapistProfile({
         phone: profile?.phone ?? null,
         specialties: profile?.specialties ?? null,
         tos_accepted_at: profile?.tos_accepted_at ?? null,
       })
       const step1Complete = therapistGate.status === 'ok'
       const step2Complete = (clientsCount ?? 0) > 0
       const step3Complete = (readingsCount ?? 0) > 0

       // D-02: banner some quando dismiss OR 3 steps completos (short-circuit no componente).
       // Backward-compat: terapeutas existentes (founder, Cristiane-pop) já têm clients+readings → step2+step3=true → componente retorna null. Sem dismiss explícito necessário.
       const dismissed = profile?.onboarding_dismissed_at != null
       const showWizard = !dismissed
       ```
    6. Renderizar `<OnboardingWizard />` condicionalmente ACIMA de InviteReadingsSection (primeira coisa abaixo do header):
       ```tsx
       {showWizard && (
         <OnboardingWizard
           step1Complete={step1Complete}
           step2Complete={step2Complete}
           step3Complete={step3Complete}
         />
       )}
       ```

    Ordem dos blocos no return JSX:
    1. AutoRefreshWhileProcessing (mantém)
    2. h1 + Link autoexame (mantém)
    3. **NEW: OnboardingWizard condicional**
    4. InviteReadingsSection (mantém)
    5. SummaryCards (mantém)

    NÃO mudar estilo do header existente. NÃO adicionar 'use client'. NÃO refatorar SummaryCards.

    Backward-compat verification: terapeutas existentes (Cristiane-pop com ≥1 reading) terão step2Complete=true + step3Complete=true. Mesmo com step1Complete=false e dismissed=false, o componente OnboardingWizard renderiza o card (porque completedCount=2 < 3). Pra eles SUMIR completamente sem dismiss, precisamos do short-circuit: se ALL 3 são true → retorna null (já implementado em Task 2). Se NÃO todos true mas terapeuta é "existente" (tem reading e cliente), o wizard ainda aparece mostrando "2 de 3" + CTA pro perfil. Isso É o comportamento desejado (D-02: "estado derivado de DB"). Founder pode dismiss se quiser.

    Logica especificamente per CONTEXT.md cross-cutting "Backward-compat":
    > onboarding wizard NÃO aparece pra terapeutas existentes que já têm dados. Logic: se já tem ≥1 leitura ou perfil completo + cliente, considera onboarding "implicitamente completo" mesmo sem dismiss explícito.

    Implementar com a regra adicional: `showWizard = !dismissed && !(step1Complete && step2Complete && step3Complete)` — short-circuit Task 2 já faz isso, então showWizard pode ser simplesmente `!dismissed`. Componente garante null-return quando todos true.
  </action>
  <acceptance_criteria>
    - `grep -c "import { OnboardingWizard } from '@/components/dashboard/onboarding-wizard'" apps/web/app/(dashboard)/dashboard/page.tsx` retorna 1.
    - `grep -c "evaluateTherapistProfile" apps/web/app/(dashboard)/dashboard/page.tsx` retorna ≥1.
    - `grep -c "from('readings').select('\\*', { count: 'exact'" apps/web/app/(dashboard)/dashboard/page.tsx` retorna 1.
    - `grep -c "onboarding_dismissed_at" apps/web/app/(dashboard)/dashboard/page.tsx` retorna ≥1.
    - `grep -c "<OnboardingWizard" apps/web/app/(dashboard)/dashboard/page.tsx` retorna 1.
    - Order check: OnboardingWizard aparece ANTES de `<SummaryCards` no source (head -n linha do OnboardingWizard < linha do SummaryCards).
    - `pnpm --filter @aurel/web tsc --noEmit` baseline preservado (sem novos erros em dashboard/page.tsx).
    - `pnpm --filter @aurel/web lint --max-warnings 0` baseline preservado.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit 2>&1 | grep -E "app/\\(dashboard\\)/dashboard/page\\.tsx" | grep -v "^$" && echo "TSC ERRORS FOUND" && exit 1 || echo "TSC OK"</automated>
  </verify>
  <done>
    Dashboard renderiza OnboardingWizard condicionalmente. State derivado via 3 booleans calculados de profile + clientsCount + readingsCount. Backward-compat preservado (componente retorna null quando 3 completos). tsc + lint baseline preservados.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → dismissOnboardingAction | Server Action invoked via form action. Auth gate inside action (getUser) — NÃO confia em client. |
| Dashboard render → profiles SELECT | RLS já filtra por auth.uid()=id. evaluateTherapistProfile é pure function (zero side-effects). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Spoofing | dismissOnboardingAction chamado sem sessão | mitigate | getUser() gate retorna {ok:false, error:'Unauthenticated'} antes do UPDATE. Sem RLS bypass via service client. |
| T-09-02-02 | Tampering | Outro user tentando dismiss profile alheio | mitigate | UPDATE inclui .eq('id', user.id) — RLS adicional bloqueia se user.id !== profile.id. |
| T-09-02-03 | Information disclosure | clientsCount/readingsCount leak via render | accept | Counts são do PRÓPRIO terapeuta (RLS filtra). Nada exposto além do que o terapeuta já vê. |
| T-09-02-04 | Repudiation | Founder regrede pra "Vamos começar (0 de 3)" inadvertidamente | mitigate | Backward-compat: founder tem ≥1 reading → step3=true → componente curto-circuita render se também step1+step2. Pior caso: founder vê wizard com "2 de 3" + clica Pular 1x. Aceitável. |
| T-09-02-05 | Denial of service | UPDATE em hot path do dashboard load | accept | Dashboard render é GET (não UPDATE). UPDATE só dispara em click explícito do "Pular". Trade-off ok. |
| T-09-02-06 | Elevation of privilege | XSS via specialties array | mitigate | OnboardingWizard NÃO renderiza specialties (apenas usa para gate). Sem path de injection. |
</threat_model>

<verification>
- Terapeuta novo (zero readings + zero clients + perfil completo após /perfil/completar gate): vê wizard com "1 de 3" + bullets nos steps 2/3 (manual via founder regressar com 2ª conta gmail).
- Terapeuta existente (founder com readings + clients + perfil completo): wizard NÃO aparece (short-circuit no componente quando 3 steps completos).
- Clique "Pular" → POST server action → UPDATE profiles.onboarding_dismissed_at → revalidatePath → wizard some no próximo render.
- 13 testes vitest GREEN (5 onboarding.test.ts + 8 onboarding-wizard.test.tsx).
- LGPD audit clean nos 3 arquivos novos.
</verification>

<success_criteria>
- ONBOARD-01 entregue (3-step wizard inline + skipable + state-derived + backward-compat)
- D-02 honrado (banner volta caso dismiss=null; wizard skipable via "Pular")
- D-03 honrado (3 steps: Perfil → 1º cliente → 1ª leitura via CTAs pra rotas existentes)
- Commit atômico: `feat(09-02): onboarding wizard 3-step inline (ONBOARD-01)`
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish-dogfooding-beta/09-02-SUMMARY.md` documenting:
- 3 arquivos modificados (wizard component, server action, dashboard page) + 2 arquivos de teste
- 13 testes GREEN com counts
- Manual smoke OK (founder vê NÃO-renderização do wizard porque ≥1 reading; teste com gmail alt account confirma wizard aparece)
- Backward-compat confirmado
</output>
