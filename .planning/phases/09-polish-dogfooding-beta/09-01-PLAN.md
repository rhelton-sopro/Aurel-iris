---
phase: 09-polish-dogfooding-beta
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0032_phase_9_onboarding_and_notification.sql
  - apps/web/types/database.ts
autonomous: false
requirements:
  - ONBOARD-01
  - ONBOARD-02

must_haves:
  truths:
    - "Coluna readings.notification_sent_at existe no schema remoto e aceita timestamptz NULL/non-NULL"
    - "Coluna profiles.onboarding_dismissed_at existe no schema remoto e aceita timestamptz NULL/non-NULL"
    - "apps/web/types/database.ts reflete as duas novas colunas (Row/Insert/Update) após gen:types"
  artifacts:
    - path: "supabase/migrations/0032_phase_9_onboarding_and_notification.sql"
      provides: "ALTER TABLE para readings (notification_sent_at) + profiles (onboarding_dismissed_at) com COMMENT ON COLUMN documentando D-04 e D-02"
      contains: "alter table public.readings"
    - path: "apps/web/types/database.ts"
      provides: "Tipos regenerados via supabase gen types --linked"
      contains: "notification_sent_at"
  key_links:
    - from: "supabase/migrations/0032_phase_9_onboarding_and_notification.sql"
      to: "remote Supabase Postgres"
      via: "supabase db push --linked (BLOCKING)"
      pattern: "applied"
    - from: "apps/web/types/database.ts"
      to: "schema remoto pós-push"
      via: "pnpm gen:types"
      pattern: "notification_sent_at"
---

<objective>
Adicionar as 2 colunas necessárias pra Fase 9 (notification idempotency + onboarding dismiss persistence) via migration 0032, aplicar contra Supabase remoto, e regenerar types/database.ts — destravando 09-02 (wizard) e 09-03 (e-mail hook).

Purpose: D-04 (e-mail "leitura pronta" idempotente via flag) e D-02 (banner dismiss persistido) dependem de duas colunas que ainda não existem. Migration única atômica (mesma fase, mesmo tema) reduz drift entre tipos/código.

Output: 1 arquivo de migration `.sql` + 1 push aplicado no Supabase remoto + `apps/web/types/database.ts` regenerado refletindo as novas colunas + 1 commit atômico.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-polish-dogfooding-beta/09-CONTEXT.md

# Reference: migration 0031 é o mais recente — espelhar formato (header docstring + COMMENT ON COLUMN)
@supabase/migrations/0031_report_generations_v2_3_alignment.sql

<interfaces>
<!-- Tipos atuais de profiles e readings (extraídos de types/database.ts) -->
<!-- Após migration + gen:types, devem ganhar as 2 colunas novas. -->

profiles.Row atual (types/database.ts:429-446):
```typescript
profiles: {
  Row: {
    beta_readings_used: number
    bio: string | null
    city: string | null
    created_at: string | null
    full_name: string
    id: string
    phone: string | null
    professional_id: string | null
    specialties: string[] | null
    state: string | null
    stripe_customer_id: string | null
    subscription_status: string | null
    tos_accepted_at: string | null
    tos_version: string | null
    trial_ends_at: string | null
    // ESPERADO PÓS-MIGRATION: onboarding_dismissed_at: string | null
  }
}
```

readings: a Row já tem report_generated_at + analysis_started_at + analysis_completed_at.
Após migration, deve ganhar: notification_sent_at: string | null
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar migration 0032 com ALTER TABLE pra readings + profiles</name>
  <files>supabase/migrations/0032_phase_9_onboarding_and_notification.sql</files>
  <read_first>
    - supabase/migrations/0031_report_generations_v2_3_alignment.sql (formato canônico de header docstring + COMMENT ON COLUMN)
    - .planning/phases/09-polish-dogfooding-beta/09-CONTEXT.md (D-04 + D-02 — contexto de cada coluna)
  </read_first>
  <action>
    Criar arquivo `supabase/migrations/0032_phase_9_onboarding_and_notification.sql` com:

    1. Header docstring (mesmo estilo de 0031) explicando:
       - Fase 9 (Polish + dogfooding + beta)
       - 2 mudanças additive/safe:
         a) `readings.notification_sent_at timestamptz NULL` — flag idempotente pra e-mail "leitura pronta" (D-04 — regen NÃO duplica)
         b) `profiles.onboarding_dismissed_at timestamptz NULL` — persistência do dismiss do wizard onboarding (D-02 — banner volta caso null, somem caso non-null)
       - Strictly additive (zero DROP, zero backfill, zero reordenação)
       - Pode ser aplicada antes ou depois do código sem quebrar prod (colunas novas, NULL default)

    2. ALTER TABLE statements EXATOS (idempotente via `IF NOT EXISTS`):
       ```sql
       alter table public.readings
         add column if not exists notification_sent_at timestamptz;

       alter table public.profiles
         add column if not exists onboarding_dismissed_at timestamptz;
       ```

    3. COMMENT ON COLUMN pra cada uma (mesma convenção de 0031):
       ```sql
       comment on column public.readings.notification_sent_at is
         'Fase 9 (2026-05-26): timestamp do envio do e-mail "leitura pronta" pro terapeuta. NULL = nunca enviado. Non-NULL = idempotência da notificação (D-04: regen NÃO re-dispara e-mail). Set pelo hook em /api/readings/[id]/analyze/route.ts após report_generated UPDATE bem-sucedido.';

       comment on column public.profiles.onboarding_dismissed_at is
         'Fase 9 (2026-05-26): timestamp do dismiss manual do wizard de onboarding inline (D-02). NULL = wizard ainda pode aparecer. Non-NULL = terapeuta clicou "Pular" e não quer ver mais. Wizard ALSO some quando os 3 steps de onboarding completam (estado derivado de DB), independente desta coluna — D-02.';
       ```

    4. SEM DROP/CREATE function/RPC. SEM INDEX (essas 2 colunas não são query-hot — apenas read em RSC dashboard load + write 1x por reading/profile).

    SQL final estimado em ~50 linhas (header docstring extenso + 2 ALTERs + 2 COMMENTs). Espelhar exatamente o style de 0031.
  </action>
  <acceptance_criteria>
    - Arquivo `supabase/migrations/0032_phase_9_onboarding_and_notification.sql` existe.
    - `grep -c "alter table public.readings" supabase/migrations/0032_phase_9_onboarding_and_notification.sql` retorna ≥1.
    - `grep -c "alter table public.profiles" supabase/migrations/0032_phase_9_onboarding_and_notification.sql` retorna ≥1.
    - `grep -c "notification_sent_at" supabase/migrations/0032_phase_9_onboarding_and_notification.sql` retorna ≥2 (ALTER + COMMENT).
    - `grep -c "onboarding_dismissed_at" supabase/migrations/0032_phase_9_onboarding_and_notification.sql` retorna ≥2.
    - `grep -c "if not exists" supabase/migrations/0032_phase_9_onboarding_and_notification.sql` retorna ≥2 (idempotência).
    - `grep -c "comment on column" supabase/migrations/0032_phase_9_onboarding_and_notification.sql` retorna ≥2.
    - Header docstring contém literal "Fase 9" e "D-04" e "D-02".
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs = require('fs'); const sql = fs.readFileSync('supabase/migrations/0032_phase_9_onboarding_and_notification.sql', 'utf8'); const checks = [['notification_sent_at', 2], ['onboarding_dismissed_at', 2], ['alter table public.readings', 1], ['alter table public.profiles', 1], ['if not exists', 2], ['comment on column', 2], ['Fase 9', 1]]; for (const [pat, min] of checks) { const re = new RegExp(pat.replace(/[.*+?^\${}()|[\\]\\\\]/g, '\\\\\\$&'), 'gi'); const matches = sql.match(re) || []; if (matches.length < min) { console.error('FAIL', pat, matches.length, '<', min); process.exit(1); } } console.log('OK')"</automated>
  </verify>
  <done>
    Migration file existe com 2 ALTER TABLE + 2 COMMENT ON COLUMN + docstring referenciando D-04 e D-02 + idempotência via IF NOT EXISTS.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Aplicar migration 0032 no Supabase remoto via supabase db push --linked</name>
  <what-built>
    Migration file `supabase/migrations/0032_phase_9_onboarding_and_notification.sql` criada na Task 1.

    Esta é uma operação que requer credenciais Supabase (SUPABASE_ACCESS_TOKEN) e CLI configurada — Claude pode tentar mas se faltar token vai falhar. Founder roda OR confirma que Claude rodou com sucesso.

    BLOCKING: Sem este push, types/database.ts (Task 3) não vai refletir as colunas novas, e Plans 09-02 / 09-03 que leem/escrevem essas colunas vão falhar tsc.
  </what-built>
  <how-to-verify>
    **Caminho A — Claude executa (se SUPABASE_ACCESS_TOKEN setado):**
    ```bash
    cd D:/Projetos/Iridologista
    supabase db push --linked
    ```
    Esperado: stdout mostra "Applying migration 0032_phase_9_onboarding_and_notification.sql..." + "Finished supabase db push." sem erro.

    **Caminho B — Founder executa (se Claude não tem token):**
    1. Founder roda `supabase db push --linked` no terminal local OU aplica via Supabase Dashboard SQL Editor.
    2. Founder confirma aplicado bem-sucedido.

    **Verificação pós-push (Claude pode rodar):**
    ```bash
    # Confirma que as 2 colunas existem no schema remoto
    supabase db remote commit --message "verify cols exist" --dry-run 2>&1 | grep -E "(notification_sent_at|onboarding_dismissed_at)" || echo "Use Supabase Dashboard table editor pra verificar"
    ```

    Critério de aceitação:
    - Output do push: "Applying migration 0032..." sem erro.
    - Migration aparece em `supabase migration list --linked` (status: applied).
  </how-to-verify>
  <resume-signal>
    Founder responde "aplicada" OR Claude verificou via `supabase migration list` que 0032 está em "Applied". Bloqueia Task 3.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Regenerar apps/web/types/database.ts via pnpm gen:types</name>
  <files>apps/web/types/database.ts</files>
  <read_first>
    - apps/web/package.json (script gen:types: "supabase gen types typescript --linked --schema public > types/database.ts")
  </read_first>
  <action>
    Rodar `pnpm --filter @aurel/web gen:types` (ou `cd apps/web && pnpm gen:types`) pra regenerar `apps/web/types/database.ts` contra o schema remoto pós-Task 2.

    Após regen, abrir o arquivo e CONFIRMAR (via grep) que:
    - Linha contendo `notification_sent_at: string | null` aparece dentro do bloco `readings: { Row: { ... } }`
    - Linha contendo `onboarding_dismissed_at: string | null` aparece dentro do bloco `profiles: { Row: { ... } }`
    - Ambas aparecem também nos blocos `Insert:` e `Update:` (com `?:` opcional)

    Se gen:types falhar com "SUPABASE_ACCESS_TOKEN unset" ou similar: parar e pedir founder pra rodar local + cole o conteúdo regenerado de types/database.ts. NÃO editar manualmente — apenas regenerar.

    Se gen:types rodar mas types/database.ts NÃO ganhar as colunas: significa que Task 2 não aplicou de fato (cache local stale). Verificar `supabase migration list --linked` e re-rodar se necessário.
  </action>
  <acceptance_criteria>
    - `grep -c "notification_sent_at: string | null" apps/web/types/database.ts` retorna ≥3 (Row + Insert + Update).
    - `grep -c "onboarding_dismissed_at: string | null" apps/web/types/database.ts` retorna ≥3 (Row + Insert + Update).
    - `pnpm --filter @aurel/web tsc --noEmit` em arquivos que importem `Database['public']['Tables']['readings']['Row']` não introduz NOVOS erros (baseline preservado per MEMORY feedback_quality_scoring_test_gate).
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs = require('fs'); const t = fs.readFileSync('apps/web/types/database.ts', 'utf8'); const a = (t.match(/notification_sent_at: string \\| null/g) || []).length; const b = (t.match(/onboarding_dismissed_at: string \\| null/g) || []).length; if (a < 3) { console.error('notification_sent_at count', a, '< 3'); process.exit(1); } if (b < 3) { console.error('onboarding_dismissed_at count', b, '< 3'); process.exit(1); } console.log('OK', a, b)"</automated>
  </verify>
  <done>
    types/database.ts contém as 2 colunas novas em Row + Insert + Update de readings + profiles. pnpm tsc não introduz novos erros baseline-relativos.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude → Supabase remote | Migration aplicada via CLI (token-auth). Caso falhe, founder aplica manualmente. |
| types/database.ts → app code | TS compilation enforça que colunas novas existem; sem regen, código quebra em tsc (mitigação: BLOCKING checkpoint). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering | Migration applied parcialmente | mitigate | IF NOT EXISTS em ambos ALTERs garante re-execução idempotente. Sem backfill, zero risco de partial state. |
| T-09-01-02 | Information disclosure | Comments em colunas vazam intenção de produto | accept | Comments contêm "D-04 / D-02 / Fase 9" — meta-info de planejamento, não dados sensíveis. Schema é interno (não exposto via PostgREST anon role — RLS bloqueia). |
| T-09-01-03 | Denial of service | gen:types falha por SUPABASE_ACCESS_TOKEN ausente | mitigate | Task 3 detecta falha e pede founder pra rodar local. Sem token = founder roda. |
| T-09-01-04 | Tampering | Migration aplicada mas types/database.ts não regenerada | mitigate | Acceptance criteria de Task 3 grep-verifica que as 2 colunas estão em Row + Insert + Update. Falha trava 09-02/09-03. |
</threat_model>

<verification>
- Migration 0032 aplicada no remoto (verificada via `supabase migration list --linked` mostrando "Applied")
- apps/web/types/database.ts contém `notification_sent_at` em readings (Row/Insert/Update) e `onboarding_dismissed_at` em profiles (Row/Insert/Update)
- `pnpm --filter @aurel/web tsc --noEmit` baseline preservado (sem NOVOS erros)
- Git status: 2 arquivos modificados (migration + types) prontos pra commit atômico
</verification>

<success_criteria>
- 1 migration file criada + aplicada
- types/database.ts regenerada e validada (grep+tsc)
- Wave 1 fechada — Wave 2 (09-02 wizard + 09-03 e-mail) desbloqueada
- Commit atômico: `feat(09-01): migration 0032 + types regen — readings.notification_sent_at + profiles.onboarding_dismissed_at`
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish-dogfooding-beta/09-01-SUMMARY.md` documenting:
- Migration content + commit hash
- types/database.ts regen output (grep counts)
- Confirmation that Wave 2 plans (09-02, 09-03) can now proceed
</output>
