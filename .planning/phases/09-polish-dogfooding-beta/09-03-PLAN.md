---
phase: 09-polish-dogfooding-beta
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - apps/web/lib/notifications/notify-report-ready.ts
  - apps/web/lib/notifications/notify-report-ready.test.ts
  - apps/web/app/api/readings/[id]/analyze/route.ts
autonomous: true
requirements:
  - ONBOARD-02

must_haves:
  truths:
    - "Quando report_generated transita NULL→non-NULL pela 1ª vez E notification_sent_at IS NULL, um e-mail é disparado pra terapeuta via Resend"
    - "Regenerar análise NÃO re-dispara o e-mail (notification_sent_at é set durante o 1º envio bem-sucedido — idempotência)"
    - "Falha no Resend (HTTP non-2xx, network error, sem RESEND_API_KEY) NÃO bloqueia o pós-stream — log warn + segue normalmente"
    - "Sender é noreply@iriscodex.com (override RESEND_FROM_EMAIL respeitado, conforme convention de notify-therapist-capture-complete.ts)"
    - "E-mail contém link pra /leituras/[id] (session-based, sem signed URL — terapeuta loga se necessário)"
    - "Vocabulário LGPD-safe: zero 'diagnóstico'/'tratamento'/'cura' nos templates html/text"
  artifacts:
    - path: "apps/web/lib/notifications/notify-report-ready.ts"
      provides: "Função notifyTherapistReportReady(readingId, therapistId) — fetch dados do reading/profile/auth, monta email HTML+text, POST pro Resend, retorna { sent: boolean, reason?: string }"
      contains: "notifyTherapistReportReady"
    - path: "apps/web/app/api/readings/[id]/analyze/route.ts"
      provides: "Hook pós-stream chamando notifyTherapistReportReady DENTRO do guard `if (notification_sent_at IS NULL && report_generated populated)`. Best-effort, log on error, NÃO joga exception."
      contains: "notifyTherapistReportReady"
  key_links:
    - from: "apps/web/app/api/readings/[id]/analyze/route.ts"
      to: "apps/web/lib/notifications/notify-report-ready.ts"
      via: "import + chamada pós-UPDATE de report_generated"
      pattern: "await notifyTherapistReportReady"
    - from: "apps/web/lib/notifications/notify-report-ready.ts"
      to: "https://api.resend.com/emails"
      via: "fetch POST com Authorization: Bearer {RESEND_API_KEY}"
      pattern: "api.resend.com/emails"
    - from: "apps/web/app/api/readings/[id]/analyze/route.ts"
      to: "readings.notification_sent_at"
      via: "UPDATE atômico junto com revalidatePath, only when sent=true"
      pattern: "notification_sent_at"
---

<objective>
Entregar ONBOARD-02 (parte faltante): e-mail transacional "leitura pronta" disparado automaticamente quando o pipeline Sonnet 2x termina e grava report_generated pela 1ª vez. Magic-link auth já entregue via Fase 11 11-01 — esta plan finaliza ONBOARD-02.

Purpose: D-04: notificação útil pro terapeuta saber que o relatório está pronto SEM precisar manter aba aberta. Idempotente via flag readings.notification_sent_at — regen NÃO duplica. Best-effort: falha não bloqueia pipeline.

Output: 1 módulo de notification + 1 arquivo de teste + 1 hook integrado em /api/readings/[id]/analyze/route.ts + 1 UPDATE de notification_sent_at na mesma transação do report_generated UPDATE.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-polish-dogfooding-beta/09-CONTEXT.md
@.planning/phases/09-polish-dogfooding-beta/09-01-SUMMARY.md

# Pattern reference — copy literalmente o estilo (header docstring, escapeHtml, fallback paths)
@apps/web/lib/notifications/notify-therapist-capture-complete.ts

# Hook point — read full route to understand pós-stream UPDATE flow
@apps/web/app/api/readings/[id]/analyze/route.ts

<interfaces>
<!-- readings.Row pós-Plan 09-01: -->
readings.Row: {
  id: string
  therapist_id: string
  client_id: string | null
  status: string
  report_generated: Json | null            // populado pelo analyze pós-stream
  report_generated_at: string | null       // populado mesmo UPDATE
  notification_sent_at: string | null      // NEW from 09-01 — flag idempotente
  // ...other fields
}

// Reference: notifyTherapistCaptureComplete signature (precedent)
// export async function notifyTherapistCaptureComplete(readingId: string): Promise<void>
// — sem retorno (fire-and-forget)
// — degrade silencioso (no RESEND_API_KEY = console.log + return)
// — service client interno (svc = createServiceClient())
// — fetch direto pra https://api.resend.com/emails (sem SDK)
//
// Esta plan adiciona notifyTherapistReportReady com signature semelhante mas
// RETORNA { sent: boolean, reason?: string } pra route.ts decidir se faz UPDATE
// de notification_sent_at (sent=true) ou pula (sent=false → tenta novamente
// na próxima regen).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Módulo notify-report-ready.ts + teste vitest</name>
  <files>apps/web/lib/notifications/notify-report-ready.ts, apps/web/lib/notifications/notify-report-ready.test.ts</files>
  <read_first>
    - apps/web/lib/notifications/notify-therapist-capture-complete.ts (LITERAL pattern — header docstring, escapeHtml, fetch shape, Resend API, DEFAULT_FROM, env var handling, error logging)
    - .planning/phases/09-polish-dogfooding-beta/09-CONTEXT.md (D-04 — tom do e-mail: "relatório pronto", link pra /leituras/[id], session-based sem signed URL)
  </read_first>
  <behavior>
    - Test 1: Sem RESEND_API_KEY → retorna { sent: false, reason: 'no_api_key' } + NÃO faz fetch
    - Test 2: Reading não existe (maybeSingle retorna null) → { sent: false, reason: 'reading_not_found' }
    - Test 3: Reading existe mas auth.admin.getUserById(therapistId).user.email é null → { sent: false, reason: 'no_therapist_email' }
    - Test 4: Happy path → fetch chamado com URL https://api.resend.com/emails + Authorization: Bearer + body JSON contendo from/to/subject/text/html → response.ok=true → retorna { sent: true }
    - Test 5: Resend retorna HTTP 4xx/5xx → { sent: false, reason: 'resend_http_<status>' } + console.error logged
    - Test 6: Resend fetch lança exceção (network error) → { sent: false, reason: 'fetch_error' } + console.error logged
    - Test 7: Sender default é 'Iris Codex <noreply@iriscodex.com>' (override via env RESEND_FROM_EMAIL)
    - Test 8: Subject contém o nome do cliente E "leitura" + corpo do e-mail contém URL /leituras/{id}
    - Test 9: LGPD audit no template text+html — strings vetadas ausentes (testar via regex `/\b(diagnóstico|tratamento|cura)\b/i.test(body) === false`)
    - Test 10: escapeHtml aplicado em clientName antes de inserir no HTML body (safe contra ${"<script>"})
  </behavior>
  <action>
    Criar `apps/web/lib/notifications/notify-report-ready.ts` ESPELHANDO `notify-therapist-capture-complete.ts` byte-por-byte na shape (imports, constants, header docstring, escapeHtml helper), com diferenças:

    1. Header docstring substitui o tema "captura completa" por "relatório pronto":
       ```
       /**
        * Notifica o terapeuta por email quando o pipeline Sonnet 2x termina de
        * gerar o relatório (report_generated populado pela 1ª vez).
        *
        * Disparado pelo /api/readings/[id]/analyze/route.ts pós-stream, DENTRO
        * do guard `notification_sent_at IS NULL`. Idempotência via flag (D-04):
        * regen NÃO re-dispara. Falha Resend é best-effort — não bloqueia pipeline,
        * retorna { sent: false } e route.ts NÃO seta notification_sent_at, então
        * tentativa futura (próxima regen ou retry manual) pode mandar.
        *
        * Tom: "Seu relatório está pronto — abrir leitura". Link pra /leituras/[id]
        * (session-based — terapeuta loga via magic-link se sessão expirou).
        *
        * Resend via fetch direto (sem SDK). Sem RESEND_API_KEY → degrade silencioso.
        * Sender: noreply@iriscodex.com (RESEND_FROM_EMAIL override respeitado).
        *
        * NÃO filtra por origem (capture-complete filtra por used_by_reading_id pra
        * só disparar em invite). Aqui qualquer reading que gere report — invite OU
        * autoexame OU upload desktop — dispara, porque o sinal é útil em todos.
        */
       ```

    2. Signature retorna { sent: boolean, reason?: string } (não void) pra route.ts decidir UPDATE:
       ```typescript
       export async function notifyTherapistReportReady(
         readingId: string,
         therapistId: string,
       ): Promise<{ sent: boolean; reason?: string }> {
         const apiKey = process.env.RESEND_API_KEY
         if (!apiKey) {
           console.log('[notify-report] RESEND_API_KEY ausente — pulando email')
           return { sent: false, reason: 'no_api_key' }
         }

         const svc = createServiceClient()

         // Fetch reading + therapist email + therapist name em paralelo.
         const [{ data: reading }, { data: profile }, authResult] = await Promise.all([
           svc.from('readings').select('client:clients(full_name)').eq('id', readingId).maybeSingle(),
           svc.from('profiles').select('full_name').eq('id', therapistId).maybeSingle(),
           svc.auth.admin.getUserById(therapistId),
         ])

         if (!reading) return { sent: false, reason: 'reading_not_found' }

         const therapistEmail = authResult.data.user?.email
         if (!therapistEmail) {
           console.warn(`[notify-report] sem email pro terapeuta ${therapistId}`)
           return { sent: false, reason: 'no_therapist_email' }
         }

         const clientName = (reading.client as { full_name?: string } | null)?.full_name ?? 'seu cliente'
         const therapistName = profile?.full_name ?? ''
         const greeting = therapistName ? `Olá, ${therapistName}` : 'Olá'

         const baseUrl = process.env.NEXT_PUBLIC_SITE_URL ?? 'https://iriscodex.com'
         const readingUrl = `${baseUrl}/leituras/${readingId}`
         const fromEmail = process.env.RESEND_FROM_EMAIL ?? DEFAULT_FROM
         const subject = `Leitura pronta — ${clientName}`

         const textBody = `${greeting},

       A leitura iridológica de ${clientName} está pronta para você revisar.

       Acessar leitura: ${readingUrl}

       Você pode editar o texto antes de entregar ao cliente.

       Boas reflexões,
       Equipe Iris Codex`

         const htmlBody = `<!DOCTYPE html>
       <html lang="pt-BR">
       <head><meta charset="utf-8"></head>
       <body style="font-family: Georgia, 'Times New Roman', serif; color: #1a1a1a; max-width: 560px; margin: 0 auto; padding: 32px 24px; line-height: 1.6;">
         <h1 style="font-size: 20px; font-weight: 300; letter-spacing: 0.08em; text-transform: uppercase; color: #1a1a1a; margin: 0 0 24px;">Iris Codex</h1>
         <p style="margin: 0 0 16px;">${greeting},</p>
         <p style="margin: 0 0 16px;">A leitura iridológica de <strong>${escapeHtml(clientName)}</strong> está pronta para você revisar.</p>
         <p style="margin: 24px 0;">
           <a href="${readingUrl}" style="display: inline-block; background-color: #1e6b65; color: #ffffff; text-decoration: none; padding: 12px 24px; font-size: 11px; letter-spacing: 0.12em; text-transform: uppercase;">Abrir leitura</a>
         </p>
         <p style="margin: 16px 0; color: #6b6b6b; font-size: 13px;">Você pode editar o texto antes de entregar ao cliente.</p>
         <p style="margin: 24px 0 0; color: #6b6b6b; font-size: 13px;">Boas reflexões,<br>Equipe Iris Codex</p>
       </body>
       </html>`

         try {
           const res = await fetch(RESEND_API_URL, {
             method: 'POST',
             headers: { Authorization: `Bearer ${apiKey}`, 'Content-Type': 'application/json' },
             body: JSON.stringify({ from: fromEmail, to: therapistEmail, subject, text: textBody, html: htmlBody }),
           })
           if (!res.ok) {
             const detail = await res.text().catch(() => '')
             console.error(`[notify-report] resend HTTP ${res.status} — ${detail.slice(0, 200)}`)
             return { sent: false, reason: `resend_http_${res.status}` }
           }
           console.log(`[notify-report] email enviado pra ${therapistEmail} (reading ${readingId})`)
           return { sent: true }
         } catch (err) {
           console.error('[notify-report] resend fetch falhou:', err instanceof Error ? err.message : err)
           return { sent: false, reason: 'fetch_error' }
         }
       }

       function escapeHtml(s: string): string {
         return s.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;').replace(/'/g, '&#39;')
       }
       ```

    3. Constantes EXATAS do precedent:
       ```typescript
       import 'server-only'
       import { createServiceClient } from '@/lib/supabase/service'
       const RESEND_API_URL = 'https://api.resend.com/emails'
       const DEFAULT_FROM = 'Iris Codex <noreply@iriscodex.com>'
       ```

    Criar `apps/web/lib/notifications/notify-report-ready.test.ts` com vitest:
    - Mock `@/lib/supabase/service` createServiceClient retornando svc com from()/auth.admin.getUserById() configuráveis
    - Mock global fetch
    - Mock process.env.RESEND_API_KEY (set/unset por teste)
    - 10 cases conforme `<behavior>` (Tests 1-10)
    - Pra Test 9 (LGPD): rodar regex `/\b(diagnóstico|tratamento|cura)\b/iu` contra textBody e htmlBody (extraído via fetch mock body capture); assert match === null

    NÃO criar SDK. NÃO criar template engine. NÃO retentar — best-effort 1 attempt.
  </action>
  <acceptance_criteria>
    - Arquivo `apps/web/lib/notifications/notify-report-ready.ts` existe e começa com `import 'server-only'`.
    - `grep -c "export async function notifyTherapistReportReady" apps/web/lib/notifications/notify-report-ready.ts` retorna 1.
    - `grep -c "api.resend.com/emails" apps/web/lib/notifications/notify-report-ready.ts` retorna ≥1.
    - `grep -c "noreply@iriscodex.com" apps/web/lib/notifications/notify-report-ready.ts` retorna ≥1.
    - `grep -c "{ sent: false, reason:" apps/web/lib/notifications/notify-report-ready.ts` retorna ≥3 (no_api_key, no_therapist_email, resend_http_*, fetch_error).
    - `grep -c "{ sent: true" apps/web/lib/notifications/notify-report-ready.ts` retorna ≥1.
    - LGPD prose-grep: `grep -v '^[[:space:]]*[/*]' apps/web/lib/notifications/notify-report-ready.ts | grep -cE 'diagnóstico|tratamento|cura'` retorna 0.
    - `grep -c "escapeHtml" apps/web/lib/notifications/notify-report-ready.ts` retorna ≥2 (helper + call).
    - `pnpm --filter @aurel/web vitest run lib/notifications/notify-report-ready.test.ts` → 10/10 GREEN.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && pnpm vitest run lib/notifications/notify-report-ready.test.ts</automated>
  </verify>
  <done>
    Módulo de notification criado espelhando precedent (header, escapeHtml, fetch shape, env handling), com signature retornando {sent, reason} pra route.ts decidir UPDATE. 10 testes GREEN. LGPD prose-grep clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire-up em /api/readings/[id]/analyze/route.ts — hook pós-UPDATE de report_generated</name>
  <files>apps/web/app/api/readings/[id]/analyze/route.ts</files>
  <read_first>
    - apps/web/app/api/readings/[id]/analyze/route.ts (LINHAS 380-477 — try block do stream, posição após o UPDATE atômico de report_generated, antes dos 3 revalidatePath)
    - apps/web/lib/notifications/notify-report-ready.ts (Task 1)
  </read_first>
  <action>
    Editar `apps/web/app/api/readings/[id]/analyze/route.ts` pra:

    1. Adicionar import no topo (junto com os outros notify/email imports — atualmente NÃO há nenhum, então adicionar novo bloco após import de `logReportGeneration`):
       ```typescript
       import { notifyTherapistReportReady } from '@/lib/notifications/notify-report-ready'
       ```

    2. Adicionar SELECT de notification_sent_at na query inicial do reading (linha ~85, junto com os outros fields):
       ```typescript
       .select(
         'id, therapist_id, status, report_delivered, regeneration_count, regeneration_log, client_id, canonical_metadata, notification_sent_at, client:clients(full_name, birth_date)',
       )
       ```

    3. APÓS o UPDATE atômico de report_generated (linha ~407, depois do `.eq('id', readingId)`) e ANTES dos 3 revalidatePath (linha ~474), adicionar bloco de notification:
       ```typescript
       // D-04: e-mail "leitura pronta" idempotente. Só dispara se essa é a 1ª
       // vez que report_generated foi populado (notification_sent_at IS NULL).
       // Regen NÃO re-envia. Falha best-effort: { sent: false } NÃO seta o flag,
       // então próximo run tenta de novo (se houver). Erro NÃO joga exception
       // — try/catch defensivo mantém o stream-handler vivo até finally.
       if (reading.notification_sent_at == null) {
         try {
           const notifyResult = await notifyTherapistReportReady(readingId, user.id)
           if (notifyResult.sent) {
             await service
               .from('readings')
               .update({ notification_sent_at: new Date().toISOString() })
               .eq('id', readingId)
           } else {
             console.warn(
               `[analyze] notify-report-ready skipped: ${notifyResult.reason ?? 'unknown'}`,
             )
           }
         } catch (err) {
           console.error(
             '[analyze] notify-report-ready threw (non-fatal):',
             err instanceof Error ? err.message : err,
           )
         }
       }
       ```

    4. NÃO alterar o catch block do stream-error (linhas ~490-520) — esse path persiste partial state mas NÃO deve disparar e-mail (relatório incompleto).

    5. NÃO alterar nada antes do UPDATE atômico de report_generated. NÃO mover o UPDATE pra usar service client (mantém supabase com user-RLS — same pattern atual). NÃO incluir notification_sent_at no UPDATE principal — separado pra atomicidade ser opcional (sent=true persiste flag; sent=false deixa pra próxima).

    Posição exata do bloco novo: ENTRE `.eq('id', readingId)` do UPDATE atômico (linha ~407) e o `// ===== v2.3.0 Extração de frases-chave =====` (linha ~409). Inserir IMEDIATAMENTE após o UPDATE de report_generated, ANTES da extração de frases.

    Comentário acima do bloco deve referenciar D-04 explicitamente pra rastreabilidade.

    Risco operacional: notifyTherapistReportReady faz 3 queries paralelas (reading+profile+auth.admin.getUserById) + 1 fetch externo (Resend). Latência adicional típica ~200-500ms. Aceitável porque já estamos no end-of-stream (cliente já recebeu todo o texto, é só finalize agora). NÃO bloquear o `controller.close()` — wrap em try/catch e segue.

    Idempotência defense-in-depth: a flag `notification_sent_at` é checada NO INÍCIO da função (linha 85+ select), o que significa que se 2 regens concorrentes rodarem (improvável dado gate (f) "already running"), AMBAS verão notification_sent_at IS NULL e ambas enviarão. Mitigação: o UPDATE de notification_sent_at é set-after-send, garante que a SEGUNDA chamada veja non-NULL. Risco residual: dupla notificação em janela de race ~1s (extremamente rara, aceito).
  </action>
  <acceptance_criteria>
    - `grep -c "notifyTherapistReportReady" apps/web/app/api/readings/[id]/analyze/route.ts` retorna ≥2 (1 import + 1 chamada).
    - `grep -c "notification_sent_at" apps/web/app/api/readings/[id]/analyze/route.ts` retorna ≥3 (1 SELECT + 1 check + 1 UPDATE).
    - `grep -c "reading.notification_sent_at == null" apps/web/app/api/readings/[id]/analyze/route.ts` retorna 1.
    - Ordering check: linha do `await notifyTherapistReportReady` aparece DEPOIS da linha do UPDATE atômico de report_generated E ANTES da linha do `revalidatePath('/leituras')`.
    - `grep -c "D-04" apps/web/app/api/readings/[id]/analyze/route.ts` retorna ≥1 (rastreabilidade do comentário).
    - `pnpm --filter @aurel/web tsc --noEmit` baseline preservado (zero novos erros em route.ts).
    - `pnpm --filter @aurel/web lint --max-warnings 0` baseline preservado.
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit 2>&1 | grep "app/api/readings/\\[id\\]/analyze/route\\.ts" | grep -v "^$" && echo "TSC NEW ERRORS" && exit 1 || echo "TSC OK"</automated>
  </verify>
  <done>
    Hook wireado no route.ts pós-UPDATE de report_generated. Idempotência via notification_sent_at IS NULL. Best-effort com try/catch defensivo + log warn em falha. UPDATE atômico do flag só em sent=true. tsc + lint baseline preservados.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route.ts → Resend API | fetch POST com RESEND_API_KEY. Failed = best-effort log + segue. |
| service client → readings.notification_sent_at UPDATE | service role bypass RLS — auth ownership já validada via user client antes (linha ~112 .eq therapist_id). |
| Resend → terapeuta external mailbox | Sender autenticado (noreply@iriscodex.com Verified em 11-01). DMARC pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Information disclosure | E-mail vaza nome do cliente em transit | accept | Resend HTTPS-only. Body inclui apenas nome do cliente (não PII médica). Risco baixo, aceito per beta-stage. |
| T-09-03-02 | Tampering | XSS injection via clientName.full_name | mitigate | escapeHtml aplicado em ${clientName} no htmlBody. Test 10 cobre `<script>` injection. |
| T-09-03-03 | Denial of service | Resend 429/5xx bloqueia stream completion | mitigate | try/catch defensivo. fetch_error retorna {sent:false} sem throw. controller.close() ainda roda no finally. |
| T-09-03-04 | Repudiation | Double-send em race condition de regens concorrentes | accept | Gate (f) "already running" no route.ts (~150) bloqueia retries em <5min. Janela residual ~1s aceita. |
| T-09-03-05 | Information disclosure | LGPD: "diagnóstico"/"tratamento"/"cura" no template | mitigate | Test 9 LGPD audit (regex word-boundary). Acceptance criteria grep também. |
| T-09-03-06 | Spoofing | E-mail enviado pra terapeuta errado | mitigate | therapistId vem de user.id (auth gate route.ts). Auth.admin.getUserById é service-role. RLS na query de profile via .eq('id', therapistId) garante match. |
| T-09-03-07 | Tampering | notification_sent_at marcado true mas e-mail não foi enviado | mitigate | UPDATE só roda em sent===true (post-fetch successful 2xx). Best-effort: sent=false deixa flag NULL pra retry. |
</threat_model>

<verification>
- 10 testes vitest GREEN em notify-report-ready.test.ts (cobrindo no_api_key, no_therapist_email, reading_not_found, happy path, HTTP error, fetch error, sender override, LGPD, escapeHtml, subject shape).
- Manual smoke (founder): regenerar reading com notification_sent_at=null → e-mail chega na inbox real do founder + flag set. Regen again → e-mail NÃO chega 2x + flag preservado.
- LGPD prose-grep clean nos 2 arquivos modificados.
- tsc + lint baseline preservados.
- pipeline NÃO regride se Resend down (founder pode validar deletando RESEND_API_KEY temp e regenerando — relatório ainda salva, log warn em vez de exception).
</verification>

<success_criteria>
- ONBOARD-02 entregue (e-mail "leitura pronta" automático + idempotente).
- D-04 honrado (flag column + best-effort + sem duplicação em regen).
- Commit atômico: `feat(09-03): notify therapist on report ready (ONBOARD-02)`
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish-dogfooding-beta/09-03-SUMMARY.md` documenting:
- 2 arquivos modificados (notify module + route.ts hook) + 1 arquivo de teste
- 10 testes GREEN
- Manual smoke OK (founder confirma e-mail recebido + flag idempotente)
</output>
