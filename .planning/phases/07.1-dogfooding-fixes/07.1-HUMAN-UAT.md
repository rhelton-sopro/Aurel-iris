---
status: partial
phase: 07.1-dogfooding-fixes
source: [07.1-VERIFICATION.md]
started: 2026-05-11T10:35:00Z
updated: 2026-05-11T10:35:00Z
---

## Current Test

number: 1
name: Wave C — expand calibration corpus to N≥3 readings per category
expected: |
  Founder annotates 2+ additional verde/verde-mosaico readings + at least 1 per remaining category (azul, castanho, mista_hematogenea) via `/admin/calibration`, then re-runs `recalibrate-centroids.mjs` to derive centroides with intra-cluster variance from real photometric distribution (not N=1 self-derived Nailli).
awaiting: founder annotation sprint + re-run of recalibrate-centroids + (optionally) Modal redeploy with tuned thresholds

## Tests

### 1. Wave C — expand calibration corpus to N≥3 readings per category
expected: Founder annotates 2+ additional verde/verde-mosaico readings + at least 1 per remaining category (azul, castanho, mista_hematogenea) via `/admin/calibration`, then re-runs `recalibrate-centroids.mjs` to derive centroides with intra-cluster variance from real photometric distribution (not N=1 self-derived Nailli).
result: pending
why_human: |
  P0.4 acceptance criterion 6 explicitly defers empirical Nailli reprocess convergence to Wave C — code-level P0.1/P0.2/P0.3/P0.4 are delivered + 27/27 pytest green + sectoral_pigments key present in Modal payload (T3 wire confirmed), but iris_color/constitution/sectoral_pigments empirical values on Nailli still return castanho/hematogenea/[]. Decision tree: (a) CLAHE flattening hypothesis (threshold tuning); (b) Polar normalization shifting hypothesis (sector alignment); (c) k-means cluster center proximity to castanho centroid (corpus expansion). Cannot resolve without (i) the founder annotating more readings and (ii) reprocessing to compare empirical deltas — requires human iridological judgment + Modal redeploy cycle.

### 2. Wave C — empirical threshold tuning of detect_sectoral_pigments on real CLAHE-processed photos
expected: Adjust `DELTA_B_LEVE_MIN` (currently 12), `DELTA_B_MODERADO_MIN` (16), `DELTA_B_DENSO_MIN` (24) in `vision-service/pipeline/sectoral_pigments.py` based on actual LAB-delta distributions observed in Nailli + 2-3 more real iris photos post-pipeline (not synthetic). Possibly narrow `CILIARY_RING` rows from `[0.25, 0.75]` to `[0.35, 0.85]` (closer to limbus where pigment concentrates).
result: pending
why_human: |
  Thresholds were calibrated against synthetic numpy arrays (5/5 tests green) but the synthetic LAB deltas may not match the post-CLAHE chromatic signal in real photos. Requires running the pipeline against ≥3 real fixtures and inspecting the delta distribution per-sector — semi-quantitative judgment call that needs founder visual confirmation.

### 3. Re-run recalibrate-centroids.mjs with expanded corpus
expected: |
  After founder annotates ≥3 fixtures per category, re-execute:
  `cd apps/web && node --env-file=.env.local scripts/recalibrate-centroids.mjs`
  Expect: verde-mosaico centroid stabilized over N=6+ (3 readings × 2 eyes), azul and castanho centroides derived from real fixtures (currently kept hardcoded — no fixtures). Apply via commit `fix(vision): recalibrate <category> from N=<n> fixtures (Wave C)` to `vision-service/pipeline/features.py:53-69`.
result: pending
why_human: Requires the data-collection step (founder annotation sprint) before script can be re-run. Script itself is verified working with current N=1 fixture.

## Summary

total: 3
passed: 0
issues: 0
pending: 3
skipped: 0
blocked: 0

## Gaps
