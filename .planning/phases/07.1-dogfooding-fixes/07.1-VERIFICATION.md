---
phase: 07.1-dogfooding-fixes
verified: 2026-05-11T10:35:00Z
status: human_needed
score: 24/25 must-haves verified (1 deferred to Wave C per acceptance criterion 6)
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
  gaps_closed: []
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Wave C — expand calibration corpus to N≥3 readings per category"
    expected: "Founder annotates 2+ additional verde/verde-mosaico readings + at least 1 per remaining category (azul, castanho, mista_hematogenea) via /admin/calibration, then re-runs recalibrate-centroids.mjs to derive centroides with intra-cluster variance from real photometric distribution (not N=1 self-derived Nailli)"
    why_human: "P0.4 acceptance criterion 6 explicitly defers empirical Nailli reprocess convergence to Wave C — code-level P0.1/P0.2/P0.3/P0.4 are entregues + 27/27 pytest green + sectoral_pigments key present in Modal payload (T3 wire confirmed), but iris_color/constitution/sectoral_pigments empirical values on Nailli still return castanho/hematogenea/[]. Decision tree: (a) CLAHE flattening hypothesis (threshold tuning); (b) Polar normalization shifting hypothesis (sector alignment); (c) k-means cluster center proximity to castanho centroid (corpus expansion). Cannot resolve without (i) the founder annotating more readings and (ii) reprocessing to compare empirical deltas — requires human iridological judgment + Modal redeploy cycle."
  - test: "Wave C — empirical threshold tuning of detect_sectoral_pigments on real CLAHE-processed photos"
    expected: "Adjust DELTA_B_LEVE_MIN (currently 12), DELTA_B_MODERADO_MIN (16), DELTA_B_DENSO_MIN (24) in sectoral_pigments.py based on actual LAB-delta distributions observed in Nailli + 2-3 more real iris photos post-pipeline (not synthetic). Possibly narrow CILIARY_RING rows from [0.25, 0.75] to [0.35, 0.85] (closer to limbus where pigment concentrates)."
    why_human: "Thresholds were calibrated against synthetic numpy arrays (5/5 tests green) but the synthetic LAB deltas may not match the post-CLAHE chromatic signal in real photos. Requires running the pipeline against ≥3 real fixtures and inspecting the delta distribution per-sector — semi-quantitative judgment call that needs founder visual confirmation."
  - test: "Re-run recalibrate-centroids.mjs with expanded corpus"
    expected: "After founder annotates ≥3 fixtures per category, re-execute: cd apps/web && node --env-file=.env.local scripts/recalibrate-centroids.mjs. Expect: verde-mosaico centroid stabilized over N=6+ (3 readings × 2 eyes), azul and castanho centroides derived from real fixtures (currently kept hardcoded — no fixtures). Apply via commit fix(vision): recalibrate <category> from N=<n> fixtures (Wave C) to features.py:53-69."
    why_human: "Requires the data-collection step (founder annotation sprint) before script can be re-run. Script itself is verified working with current N=1 fixture."
---

# Phase 07.1: Dogfooding Fixes — Verification Report

**Phase Goal:** Closure of three dogfooding-blocker gaps surfaced in 2026-05-09 against the first real Nailli reading: (1) audit regex defects firing on well-anchored LGPD-correct reports, (2) vision pipeline cascade producing castanho/hematogenea/[] for a verde-acinzentado iris with sectoral amber pigment, (3) admin calibration tooling to capture ground truth corpus for Wave C.

**Verified:** 2026-05-11T10:35:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

#### Plan 07.1-01 — Audit Regex Polish

| #   | Truth                                                                  | Status     | Evidence                                                                                       |
| --- | ---------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------- |
| 1   | ANCHOR_RE aceita variações de capitalização ('ancorado'/'Ancorado'/'ANCORADO') | VERIFIED   | audit.ts:67 — alternation regex flags `giu` (`i` = case-insensitive)                           |
| 2   | ANCHOR_RE aceita conteúdo entre crases (`feature.path`)                | VERIFIED   | audit.ts:67 — `[^\]]+` accepts arbitrary content + compact branch with backtick-shaped path    |
| 3   | ANCHOR_RE aceita prefixo `features.` opcional                          | VERIFIED   | audit.ts:67 — `[^\]]+` accepts any inner content; tests 73-98 + 102-163 prove both shapes      |
| 4   | ANCHOR_RE NÃO casa colchetes não-relacionados (markdown links, outros) | VERIFIED   | audit.test.ts:86-98 (markdown link test) + audit.test.ts:152-163 (bracket-no-backtick test)   |
| 5   | extractForbiddenHits NÃO conta hits precedidos por construções negativas LGPD | VERIFIED   | audit.ts:121-131 — slice+lookback test using NEG_CONTEXT_RE                                    |
| 6   | Negativas reconhecidas: 'não um/uma/constitui/é/e/substitui/representa/significa' | VERIFIED   | audit.ts:95-96 — NEG_CONTEXT_RE alternation explicitly includes all 8 verbs                    |
| 7   | Skip da negativa é case-insensitive                                    | VERIFIED   | audit.ts:96 — flag `i` em NEG_CONTEXT_RE                                                       |
| 8   | Skip da negativa é word-boundary correto                               | VERIFIED   | audit.ts:96 — `\s+` força whitespace separator (não casa "naoum")                              |
| 9   | Hits POSITIVOS continuam sendo contados                                | VERIFIED   | audit.test.ts:304-316 (3 regression guards) — vitest GREEN                                     |
| 10  | FORBIDDEN_VOCAB_RE monta termos via concat indireto (`_F1/_F2/_F3`)    | VERIFIED   | audit.ts:72-81 — array.join() pattern; meta-invariant test em audit.test.ts:386-409 GREEN      |
| 11  | encerramento_disclaimer continua skipped no runAudit                   | VERIFIED   | audit.ts:184 — `if (key === 'encerramento_disclaimer') continue` (commit c559470 preservado)   |
| 12  | audit.test.ts ganha ≥6 novos cases                                     | VERIFIED   | 17 novos cases entregues (3 C1 + 5 C1.b + 9 C2) vs target 6                                    |
| 13  | Suite completa GREEN — zero regressão dos 19 cases existentes          | VERIFIED   | `pnpm test:run lib/anthropic/__tests__/audit.test.ts` → **36/36 passed** (executado nesta sessão) |

#### Plan 07.1-02 — Vision Pipeline Calibration

| #   | Truth                                                                          | Status     | Evidence                                                                                |
| --- | ------------------------------------------------------------------------------ | ---------- | --------------------------------------------------------------------------------------- |
| 14  | classify_iris_color filtra pixels mask-zerados E dentro do disco pupilar      | VERIFIED   | features.py:112-136 — B1a filter + P0.1 pupil disc exclusion via ogrid                  |
| 15  | Disco pupilar = pupil_circle × 1.10 margin                                     | VERIFIED   | features.py:131 — `r_excl = r_p * 1.10`                                                  |
| 16  | Fallback aceitável se pupil_circle is None (backward compat)                   | VERIFIED   | features.py:119 — guard `if pupil_circle is not None`; test_pupil_hint_optional GREEN  |
| 17  | Edge case: pupil_circle inválido (r ≤ 0 ou NaN) → skip sem crash               | VERIFIED   | features.py:120-130 — try/except + isfinite + r > 0 guards                              |
| 18  | ConstitutionType +4 valores (MISTA_BILIAR, MISTA_HEMATOGENEA, BILIAR, NEUROGENICA) | VERIFIED   | schemas.py:39-46 — 7 enum values total                                                  |
| 19  | Constitution.primary continua sendo str (não Enum-enforced)                    | VERIFIED   | schemas.py:50 — `primary: str` (backward compat preservada)                             |
| 20  | _classify_constitution 6-way heuristic com priority ordering                   | VERIFIED   | features.py:258-337 — Branches 1-6 com first-match-wins logic                           |
| 21  | Heurística usa sectoral_pigments como input adicional                          | VERIFIED   | features.py:261, 285, 290-299 — sectoral_pigments kwarg + amarelo_ambar_superior filter |
| 22  | Confidence calibrada: linfatica=0.8, mistas=0.65, neurogenica=0.7              | VERIFIED   | features.py:305, 312, 321-322, 328, 333 — explicit confidence per branch                |
| 23  | Fallback 'mista' só quando nenhum signal claro                                 | VERIFIED   | features.py:335-337 — Branch 6 default                                                  |
| 24  | sectoral_pigments.py exporta detect_sectoral_pigments com 12 setores horários  | VERIFIED   | sectoral_pigments.py:59-146 — SECTOR_COUNT=12, per-hour LAB delta computation           |
| 25  | LAB médio do estroma-base = mediana das 12 sector means                        | VERIFIED   | sectoral_pigments.py:111 — `base = np.median(np.stack(sector_means, axis=0), axis=0)`   |
| 26  | Pigmento amarelo-âmbar: delta_b > +12 AND \|delta_a\| < 8                      | VERIFIED   | sectoral_pigments.py:129 — `if dB > DELTA_B_LEVE_MIN and abs(dA) < 8`                   |
| 27  | Pigmento laranja: delta_a > +8 AND delta_b > +8 (priority over amarelo_ambar)  | VERIFIED   | sectoral_pigments.py:119 — branch ordem antes de amarelo_ambar                          |
| 28  | Pigmento marrom-difuso: delta_L < -10 AND delta_a > +5 AND delta_b > +5        | VERIFIED   | sectoral_pigments.py:138 — `if dL < -10 and dA > 5 and dB > 5`                          |
| 29  | Intensity buckets: <16=leve, 16-24=moderado, >24=denso                         | VERIFIED   | sectoral_pigments.py:43-56 — DELTA_B_*_MIN constants + _classify_intensity              |
| 30  | EyeFeatures.sectoral_pigments com default_factory=list (backward compat)       | VERIFIED   | schemas.py:177 — `sectoral_pigments: list[SectoralPigment] = Field(default_factory=list)` |
| 31  | extract_all chama detect_sectoral_pigments e popula sectoral_pigments          | VERIFIED   | features.py:451-457 + 486 — call + payload field                                        |
| 32  | _classify_constitution recebe sectoral_pigments via extract_all                | VERIFIED   | features.py:461-463 — `sectoral_pigments=sectoral_pigments` kwarg                       |
| 33  | recalibrate-centroids.mjs READ-ONLY proposal tool com graceful degradation N≥1 | VERIFIED   | scripts/recalibrate-centroids.mjs:1-258 — main() prints proposta, não escreve em features.py |
| 34  | verde-mosaico recalibrado de (140,110,145) para (150,141,145)                  | VERIFIED   | features.py:68 — value applied; audit-trail comment lines 56-67                         |
| 35  | Test gates pytest: 27/27 GREEN (test_features + test_sectoral_pigments)        | VERIFIED   | `python -m pytest tests/test_features.py tests/test_sectoral_pigments.py -v` → **27 passed in 0.99s** |
| 36  | Reprocess Nailli produz iris_color ∈ {verde-mosaico, misto}                    | DEFERRED   | SUMMARY T5 explicit: empírica volta castanho — acceptance criterion 6 partial aceito   |
| 37  | Reprocess Nailli produz constitution ∈ {mista_biliar, biliar, mista}           | DEFERRED   | SUMMARY T5 explicit: empírica volta hematogenea — Wave C threshold tuning required     |
| 38  | sectoral_pigments do OD contém ≥1 amarelo_ambar nos hours 11/12/1/2            | DEFERRED   | SUMMARY T5 explicit: empírica volta [] — Wave C corpus expansion required              |

#### Plan 07.1-03 — Admin Calibration Page

| #   | Truth                                                                      | Status     | Evidence                                                                                |
| --- | -------------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------- |
| 39  | /admin/calibration accessible APENAS para o founder                        | VERIFIED   | apps/web/lib/auth/founder.ts + middleware.ts + admin/layout.tsx (3-layer defense)       |
| 40  | Lista mostra readings com vision_features != NULL com filter pendente/anotado/revisado | VERIFIED   | apps/web/app/admin/calibration/page.tsx (261 linhas RSC)                                |
| 41  | Detalhe mostra 6 fotos + features formatados + form anotação + 2 buttons   | VERIFIED   | apps/web/app/admin/calibration/[id]/page.tsx + 5 calibration components present         |
| 42  | Form persiste em calibration_annotations (UPSERT por reading_id)           | VERIFIED   | actions/calibration.ts saveAnnotation + migration 0009 UNIQUE constraint                |
| 43  | Relatório técnico bruto NÃO chama LLM                                      | VERIFIED   | FeaturesDisplay.tsx é pure RSC render — sem chamadas ao /api/anthropic/*                |
| 44  | RLS na calibration_annotations garante defense-in-depth (auth.jwt ->> email) | VERIFIED   | Migration 0011 (bfbadf9) fix from broken auth.users subquery to auth.jwt() per memory rule 7 |

**Score:** 24/25 must-haves verified (truths 36-38 deferred to Wave C per acceptance criterion 6)

### Deferred Items (Wave C follow-ups)

Items not yet met but explicitly addressed by later work in this milestone phase.

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | Nailli iris_color empirically resolves to verde-mosaico/misto | Wave C (Phase 7.1 plan 04+) | Plan 07.1-02 acceptance criterion 6: "P0.1+P0.2+P0.3 OK, P0.4 first-pass requer corpus expansion → ciclo continua na Wave C" |
| 2 | Nailli constitution empirically resolves to mista_biliar/biliar/mista | Wave C | Same criterion 6 — depends on iris_color convergence |
| 3 | Nailli sectoral_pigments contains ≥1 amarelo_ambar nos hours 11/12/1/2 | Wave C | Plan 07.1-02 SUMMARY T5: "Hipóteses não-testadas (escopo Wave C): CLAHE flattening, polar normalization shifting, k-means cluster proximity, corpus N=1 insuficiente" |

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| apps/web/lib/anthropic/audit.ts | ANCHOR_RE relaxed + extractForbiddenHits LGPD-aware skip | VERIFIED | 197 linhas, audit-vocabulary:allowlist marker, 3 commits (1e58a88+36ad36c+4abe518) |
| apps/web/lib/anthropic/__tests__/audit.test.ts | ≥6 new cases (C1 + C2) | VERIFIED | 409 linhas, 17 new cases entregues, 36/36 vitest GREEN |
| vision-service/pipeline/features.py | classify_iris_color + 6-way constitution + extract_all wiring | VERIFIED | 554 linhas, all 4 P0 modifications applied + sanity-gate documented |
| vision-service/pipeline/schemas.py | ConstitutionType +4 valores + SectoralPigment model + EyeFeatures field | VERIFIED | 207 linhas, 7-value enum + SectoralPigment + default_factory=list |
| vision-service/pipeline/sectoral_pigments.py | detect_sectoral_pigments com LAB-delta threshold + per-image base | VERIFIED | 146 linhas, NEW module, 12-sector hour-based detection |
| vision-service/tests/test_features.py | 8 new cases (2 pupil + 6 constitution) | VERIFIED | 395 linhas, 8 new cases beyond baseline 14, 22/22 GREEN |
| vision-service/tests/test_sectoral_pigments.py | 5 new cases (amarelo_ambar + laranja + uniform + intensity) | VERIFIED | 101 linhas, NEW file, 5/5 GREEN |
| apps/web/scripts/recalibrate-centroids.mjs | Read-only proposal tool com graceful degradation | VERIFIED | 258 linhas, NEW file, sRGB→LAB OpenCV scale, eyJ-prefix sanity gate |
| supabase/migrations/0009_calibration_annotations.sql | calibration_annotations table + RLS founder-only | VERIFIED | Applied via supabase db push --linked; types regen confirmed (commit 785c05b) |
| supabase/migrations/0011_calibration_rls_jwt_email.sql | Fix broken auth.users subquery → auth.jwt | VERIFIED | bfbadf9 — per memory rule 7 |
| apps/web/app/admin/calibration/*.tsx + components/calibration/*.tsx | Founder UI for ground truth annotation | VERIFIED | All 5 components + 2 pages present (PhotoGrid, FeaturesDisplay, AnnotationForm, CalibrationDiagnosisForm, TechnicalReportCopyButton, PhotoDownloadButton) |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| audit.ts extractForbiddenHits | analise.ts:62/151 saveReportDelivered | LGPD-aware skip propaga transparentemente | WIRED | extractForbiddenHits exportada; consumer não muda (defense-in-depth preservada) |
| features.py classify_iris_color | composite_image['pupil_circle'] | P0.1 exclusion mask = disc(pupil_circle, r*1.10) | WIRED | features.py:445 (extract_all) passa pupil_circle ao classify_iris_color |
| features.py _classify_constitution | sectoral_pigments from extract_all | additional kwarg — heuristic checks ≥2 superior-sector yellow-amber | WIRED | features.py:461-463 — sectoral_pigments=sectoral_pigments kwarg |
| sectoral_pigments.py | enhanced_polar 64x504 RGB | Splits W into 12 sector columns + processes ciliary ring rows | WIRED | features.py:453-457 calls com enhanced_image (polar) |
| recalibrate-centroids.mjs | calibration_annotations + reading_images.storage_path | Service-role Supabase + storage download + LAB compute via Sharp | WIRED | script line 62-83 (annotations + images queries) + 107-151 (download + LAB compute) |
| AnnotationForm | calibration_annotations table | saveAnnotation server action UPSERT | WIRED | actions/calibration.ts saveAnnotation + UNIQUE(reading_id) constraint enforced by 0009 |
| middleware.ts /admin/* | isFounderEmail | NextResponse(null, status:404) if non-founder | WIRED | apps/web/middleware.ts checks request.nextUrl.pathname.startsWith('/admin/') |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| classify_iris_color | iris_color dict | k-means over LAB pixels with B1a + P0.1 pupil filter | YES (synthetic + Modal verified) | FLOWING (synthetic tests GREEN; empirical convergence on Nailli is Wave C — deferred per criterion 6) |
| _classify_constitution | constitution dict | iris_color + fiber_density + sectoral_pigments inputs | YES (6 GREEN unit tests) | FLOWING |
| detect_sectoral_pigments | list[SectoralPigment] | LAB delta computation against median-of-12-sector base | YES (synthetic 5/5 GREEN) | FLOWING (synthetic); empirical detection returns [] on Nailli — STATIC empirical (Wave C threshold tuning required, deferred per criterion 6) |
| extract_all sectoral_pigments | EyeFeatures.sectoral_pigments | Direct return from detect_sectoral_pigments | YES (Modal payload contains key) | FLOWING (T5 SUMMARY: "sectoral_pigments key PRESENT no payload — confirma T3 wire ativo no Modal") |
| AnnotationForm.real_iris_color | calibration_annotations row | saveAnnotation UPSERT | YES (1 row persisted by founder smoke per supplement) | FLOWING |
| CalibrationDiagnosisForm.diagnosis | calibration_diagnoses row | saveCalibrationDiagnosis UPSERT | YES (1 row persisted by founder smoke per supplement) | FLOWING |
| recalibrate-centroids.mjs proposal | stdout table | Sharp raw pixels → LAB centroid per category | YES (N=1 first-pass run documented in SUMMARY) | FLOWING (founder ran 2026-05-11; output: verde-mosaico (150,141,145) proposed + applied) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| audit.test.ts vitest suite | `pnpm --filter web test:run lib/anthropic/__tests__/audit.test.ts` | 36 passed | PASS |
| vision-service pytest (07.1-02 scope) | `python -m pytest tests/test_features.py tests/test_sectoral_pigments.py -v` | 27 passed in 0.99s | PASS |
| audit:vocabulary baseline preserved | `pnpm --filter web audit:vocabulary` | 8 baseline hits (login×2, signup×2, capture/validate×3, CapturePreview×1) — zero new from lib/anthropic | PASS |
| Meta-invariant audit.ts source clean | Embedded in audit.test.ts:386-409 | GREEN (strip-comments check passes) | PASS |
| Modal deploy + Nailli reprocess | per SUMMARY T5 (founder ran) | call_id fc-01KRBJHB8TAXBVXRNHAJ15KSX4 SUCCESS; sectoral_pigments field present | PASS (wire) / SKIP (empirical — criterion 6 partial) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| LLM-02 | 07.1-01 | (per SUMMARY frontmatter — anchor-rate audit gate) | SATISFIED | ANCHOR_RE relaxed → anchor_rate > 0 on Wave A v2 dogfooding |
| LLM-03 | 07.1-01 | (per SUMMARY frontmatter — LGPD vocab gate) | SATISFIED | NEG_CONTEXT_RE skip → false positives eliminated, regression guards GREEN |

Note: 07.1-02 and 07.1-03 have no formal requirement IDs (per user brief — backlog Phase 9 RESP-01..03 will be added when Wave C data confirms calibration approach).

### Anti-Patterns Found

Per `.planning/phases/07.1-dogfooding-fixes/07.1-REVIEW.md`: 0 critical / 4 warning / 6 info. None block this phase goal.

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| vision-service/pipeline/features.py | 539-542 | WR-01: asymmetry condition uses `not left_findings` instead of `not left_has_lacuna` (mirror on 541) | WARNING (PRE-EXISTING, not introduced by 07.1) | Lacuna asymmetry detection masked when contralateral sector has non-lacuna finding. Git blame confirms commit 1b9cbb6 (Phase 5.9, 2026-05-04) — out of 07.1 scope. Wave C follow-up. |
| vision-service/pipeline/features.py | 302-305, 320 | WR-02: _classify_constitution depends on string label "densa" not lifted as typed constant | WARNING | Brittleness if labels rename; no test catches regression. Wave C polish. |
| apps/web/scripts/recalibrate-centroids.mjs | 134-148 | WR-03: assumes Sharp returns 3 or 4 channels — grayscale causes NaN | WARNING | Defensive code missing; founder-local tool, low blast radius but should be guarded. Wave C follow-up. |
| apps/web/lib/anthropic/audit.ts | 98, 126-128 | WR-04: 30-char lookback can be insufficient for parenthetical-rich sentences ("Não, em hipótese alguma, esta análise constitui um diagnóstico") | WARNING | Plan failure_modes explicitly documents this trade-off — "usos elaborados raros caem para o gate humano via banner". ACCEPTED per plan. |
| vision-service/pipeline/features.py | 308-314 | IN-01: confidence asymmetry between linfatica branch and others | INFO | Documentation gap only. |
| vision-service/pipeline/features.py | 244 | IN-02: Sobel magic divisor 50.0 not named | INFO | Polish, no behavioral impact. |
| vision-service/pipeline/sectoral_pigments.py | 60-63 | IN-03: iris_circle and collarette_diameter_ratio accepted but unused | INFO | Forward-compat parameters, documented as reserved. |
| vision-service/pipeline/sectoral_pigments.py | 25 vs 120 | IN-04: docstring describes `|delta_a + delta_b|` but code computes `abs(dA) + abs(dB) - 8` | INFO | Doc/code drift; values coincide because both deltas are positive in laranja branch. |
| apps/web/scripts/recalibrate-centroids.mjs | 113-125 | IN-05: duplicate `sharp(buf)` init | INFO | Minor cleanup. |
| vision-service/pipeline/__init__.py | 6 | IN-06: stale docstring "SKELETON ONLY — Phase 1" | INFO | Drift; pipeline is now production. |

**Net assessment:** 0 BLOCKERS, 4 WARNINGS (1 pre-existing, 1 accepted-per-plan, 2 polish), 6 INFO. None gate the phase goal.

### Stub Detection

No new stubs introduced by Phase 07.1. All artifacts have real implementations:
- `EyeFeatures.sectoral_pigments` field defaults to `list[]` but is populated by `detect_sectoral_pigments()` in `extract_all` (features.py:486) — not a stub, it's data-driven empty.
- Modal payload returns `sectoral_pigments=[]` for Nailli — proves detector runs end-to-end but synthetic-thresholds fail to fire on real photos. This is a calibration gap, not a stub.

## Wave C follow-ups

Concrete deferred items for the next phase planning cycle:

(a) **Threshold tuning on N≥3 corpus per category** — re-run `detect_sectoral_pigments` against ≥3 real CLAHE-processed iris fixtures per pigment category. Adjust `DELTA_B_LEVE_MIN` (currently 12), `DELTA_B_MODERADO_MIN` (16), `DELTA_B_DENSO_MIN` (24) in `vision-service/pipeline/sectoral_pigments.py`. Possibly narrow `CILIARY_RING_TOP_FRAC=0.25` to 0.35 and `CILIARY_RING_BOTTOM_FRAC=0.75` to 0.85 (closer to limbus where pigment concentrates).

(b) **recalibrate-centroids.mjs re-run with expanded corpus** — after founder annotates N≥3 fixtures per category via `/admin/calibration`, re-execute `cd apps/web && node --env-file=.env.local scripts/recalibrate-centroids.mjs`. Apply derived centroides for `azul` and `castanho` categories (currently kept hardcoded — no fixtures). Re-derive `verde-mosaico` over N=6+ samples (3 readings × 2 eyes) to confirm or replace the N=1-derived (150,141,145).

(c) **WR-01 pre-existing asymmetry bug in features.py:539-542** — replace `if right_has_lacuna and not left_findings` with `if right_has_lacuna and not left_has_lacuna` (mirror on line 541). Add a regression test where `left_findings` contains a non-lacuna finding (e.g., `pigmentacao`) in the same sector as right's lacuna — currently the lacuna-unilateral note is silently suppressed. Origin: commit `1b9cbb6` (Phase 5.9, 2026-05-04). Not introduced by Phase 7.1 — pre-existing technical debt surfaced by the 07.1 code review.

(d) **WR-03 Sharp channel validation in recalibrate-centroids.mjs** — guard against `channels !== 3 && channels !== 4` in `apps/web/scripts/recalibrate-centroids.mjs:134-148` to prevent NaN propagation on accidental grayscale input. Suggested fix:
```javascript
if (channels !== 3 && channels !== 4) {
  throw new Error(`unsupported channel count ${channels} for ${storagePath} — convert to RGB first`)
}
```

### Human Verification Required

#### 1. Wave C — expand calibration corpus to N≥3 readings per category

**Test:** Founder annotates 2+ additional verde/verde-mosaico readings + at least 1 per remaining category (azul, castanho, mista_hematogenea) via `/admin/calibration`, then re-runs `recalibrate-centroids.mjs` to derive centroides from real photometric distribution.
**Expected:** verde-mosaico centroide stabilizes over N=6+ samples (3 readings × 2 eyes); azul and castanho centroides derived from real fixtures (currently hardcoded — no fixtures).
**Why human:** P0.4 acceptance criterion 6 explicitly defers empirical Nailli reprocess convergence to Wave C. Code-level P0.1/P0.2/P0.3/P0.4 are delivered with 27/27 pytest GREEN + `sectoral_pigments` key present in Modal payload (T3 wire confirmed). But iris_color/constitution/sectoral_pigments empirical values on Nailli still return `castanho/hematogenea/[]`. Cannot resolve without founder annotating more readings and reprocessing to compare empirical deltas — requires human iridological judgment + Modal redeploy cycle.

#### 2. Wave C — empirical threshold tuning of detect_sectoral_pigments

**Test:** Adjust `DELTA_B_LEVE_MIN` (12), `DELTA_B_MODERADO_MIN` (16), `DELTA_B_DENSO_MIN` (24) in `sectoral_pigments.py` based on actual LAB-delta distributions observed in Nailli + 2-3 more real iris photos post-pipeline. Possibly narrow `CILIARY_RING_*_FRAC` rows from [0.25, 0.75] to [0.35, 0.85].
**Expected:** detector fires on at least one of Nailli's superior sectors (11/12/1/2) with `type='amarelo_ambar'` and `intensity ∈ {moderado, denso}`.
**Why human:** Thresholds calibrated against synthetic numpy arrays (5/5 GREEN) but the synthetic LAB deltas may not match the post-CLAHE chromatic signal in real photos. Requires running pipeline against ≥3 real fixtures and inspecting the per-sector delta distribution — semi-quantitative judgment call that needs founder visual confirmation.

#### 3. Re-run recalibrate-centroids.mjs with expanded corpus

**Test:** After founder annotates ≥3 fixtures per category, re-execute:
```bash
cd apps/web && node --env-file=.env.local scripts/recalibrate-centroids.mjs
```
**Expected:** verde-mosaico centroide stabilized over N=6+ samples; azul and castanho centroides derived from real fixtures; intra-cluster variance reported per category.
**Why human:** Requires the data-collection step (founder annotation sprint) before script can be re-run. Script itself is verified working with current N=1 fixture (commit 3c1362d + a894edb empirical output documented in SUMMARY).

### Gaps Summary

**No gaps blocking phase 07.1 goal achievement.** All 24/25 must-haves are VERIFIED at code level + test level. The single deferred item (T5 empirical Nailli convergence) is the **explicit planned outcome** under acceptance criterion 6 of plan 07.1-02 ("Acceptance parcial: P0.1+P0.2+P0.3 OK, P0.4 first-pass requer corpus expansion → ciclo continua na Wave C").

Phase 07.1 delivered exactly the dogfooding-blocker fixes it scoped:
1. Audit banners no longer fire on well-anchored LGPD-correct reports (36/36 tests + 17 new cases + meta-invariant preserved).
2. Vision pipeline now has the architectural primitives (pupil mask, 6-way constitution, sectoral pigments detector, recalibration script + centroid update) to converge on Nailli once corpus expands (27/27 tests + Modal redeploy confirmed wire active).
3. Admin calibration tooling collected the first ground-truth row + an operational diagnosis loop, with 3 production bugs found and fixed during smoke (safeArray defensive, 'use server' export hygiene, RLS auth.jwt rewrite).

Convergence on the Nailli **specific empirical values** requires (a) corpus expansion + (b) threshold tuning + (c) possibly polar-coord alignment validation — all explicitly Wave C scope per the plan's own acceptance criterion 6.

---

_Verified: 2026-05-11T10:35:00Z_
_Verifier: Claude (gsd-verifier, Opus 4.7 1M context)_
