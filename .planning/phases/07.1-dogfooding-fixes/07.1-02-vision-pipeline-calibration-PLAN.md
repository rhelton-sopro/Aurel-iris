---
phase: 07.1-dogfooding-fixes
plan: 02
type: tdd
wave: B
depends_on: [07.1-01, 07.1-03]
files_modified:
  - vision-service/pipeline/features.py
  - vision-service/pipeline/schemas.py
  - vision-service/pipeline/sectoral_pigments.py        # NEW
  - vision-service/pipeline/__init__.py
  - vision-service/tests/test_features.py
  - vision-service/tests/test_sectoral_pigments.py      # NEW
  - apps/web/scripts/recalibrate-centroids.mjs          # NEW
autonomous: true
requirements: []
tags: [phase-7.1, dogfooding, vision-pipeline, iris-color, constitution, sectoral-pigments, wave-b, tdd]

must_haves:
  truths:
    # P0.1 — Pupil mask antes do k-means
    - "classify_iris_color filtra (a) pixels mask-zerados (R+G+B=0, já em vigor pelo B1a) E (b) pixels dentro do disco pupilar"
    - "Disco pupilar = (cx_pupil, cy_pupil, r_pupil) do composite_image['pupil_circle'] × 1.10 (10% margin para anti-aliasing)"
    - "Se pupil_circle is None: fallback aceitável é manter só o B1a filter (sem perda de regressão)"
    - "iris_circle do composite_image['iris_circle'] usado para narrow scope: pixels OUTSIDE iris circle E DENTRO pupil circle são excluídos"
    - "Edge case: pupil_circle inválido (r ≤ 0 ou centro fora do iris_circle) → log warn + skip pupil mask (sem crash)"

    # P0.2 — Constitution enum expandido
    - "ConstitutionType enum em schemas.py adiciona: MISTA_BILIAR, MISTA_HEMATOGENEA, BILIAR, NEUROGENICA (mantém LINFATICA, HEMATOGENEA, MISTA)"
    - "Constitution.primary continua sendo str (não enforced enum) — backward compat preservada"
    - "_classify_constitution heurística reescrita para mapear: azul+esparsa→linfatica, verde-mosaico+pigmento_amarelo→mista_biliar, verde-mosaico+sem_pigmento→biliar, castanho+denso→hematogenea, castanho+pigmento_difuso→mista_hematogenea, fibras_radiais_finas_marcadas→neurogenica"
    - "Heurística usa sectoral_pigments (P0.3) como input adicional — biliar/mista_biliar requer pigmento amarelo-âmbar detectado"
    - "Confidence calibrada: linfatica/hematogenea pura = 0.8; mistas = 0.65; neurogenica = 0.7"
    - "Fallback 'mista' (sem qualificador) só quando nenhum signal claro"

    # P0.3 — Detecção de pigmentos setoriais
    - "Novo módulo vision-service/pipeline/sectoral_pigments.py exporta detect_sectoral_pigments(enhanced_polar, iris_circle, collarette_diameter_ratio) -> list[dict]"
    - "Para cada um dos 12 setores horários: computa LAB médio do anel ciliar (entre collarette e bordo) restrito ao setor"
    - "Calcula LAB médio do estroma-base = mediana de TODOS os pixels do anel ciliar (exclui pupila + colarete + outliers via percentil 10-90)"
    - "delta_LAB[i] = LAB_setor[i] - LAB_base"
    - "Pigmento amarelo-âmbar: delta_b > +12 (mais amarelo) E |delta_a| < 8 (não vermelho/verde)"
    - "Pigmento laranja: delta_a > +8 E delta_b > +8 (vermelho + amarelo)"
    - "Pigmento marrom-difuso: delta_L < -10 (mais escuro) E delta_a > +5 E delta_b > +5"
    - "Retorna [{hour: int, type: 'amarelo_ambar'|'laranja'|'marrom_difuso', intensity: 'leve'|'moderado'|'denso', delta_lab: [dL,dA,dB]}]"
    - "Intensity buckets: |delta_b| < 16 = leve; 16-24 = moderado; > 24 = denso"
    - "Schema EyeFeatures (schemas.py) ganha campo sectoral_pigments: list[SectoralPigment] com default_factory=list (backward compat)"
    - "extract_all (features.py) chama detect_sectoral_pigments e popula vision_features.{eye}.sectoral_pigments"
    - "_classify_constitution (P0.2) RECEBE sectoral_pigments como input — pigmento amarelo-âmbar em ≥2 setores superiores (hours 11,12,1,2) com intensity ≥ moderado → flag biliar"

    # P0.4 — Recalibração de centroides LAB
    - "Novo script apps/web/scripts/recalibrate-centroids.mjs lê calibration_annotations (real_iris_color) + reading_images storage_path, baixa cada foto frontal, extrai LAB médio da região central, agrupa por real_iris_color"
    - "Output: tabela de centroides propostos por categoria + comparação com IRIS_COLOR_LAB_CENTROIDS hardcoded (delta + cluster count + variância intra-cluster)"
    - "Script é READ-ONLY — propõe valores, founder atualiza features.py:51-55 manualmente (preserva audit trail no diff)"
    - "Script funciona com N≥1 fixture (graceful degradation): com Nailli sozinha, propõe centroide 'verde-mosaico' baseado nela; outras categorias mantém hardcoded"
    - "Script imprime warning se N < 3 fixtures por categoria (low confidence)"
    - "Esta PLAN inclui first-pass run com Nailli como única fixture verde-mosaico/mista_biliar — output documentado no SUMMARY"
    - "features.py:51-55 é updated em commit separado (T4 último commit) DENTRO desta PLAN com os valores propostos pelo script"

    # Verificação cruzada — validação Nailli end-to-end
    - "Reprocess da Nailli (reading 71a7bf1d-747f-4de8-9129-13b69197c6a4) produz iris_color.primary IN {'verde-mosaico', 'misto'} (não 'castanho')"
    - "Constitution.primary IN {'mista_biliar', 'biliar', 'mista'} (não 'hematogenea')"
    - "sectoral_pigments do OD contém ≥1 entry tipo 'amarelo_ambar' nos hours 11/12/1/2 com intensity ≥ moderado"
    - "sectoral_pigments do OE contém ≥1 entry similar (pode ser menos denso conforme diagnóstico humano)"

    # Test gates (pytest)
    - "test_features.py::test_classify_iris_color_excludes_pupil_pixels adicionado — sintetiza imagem com pupila preta central + íris verde anel; sem fix castanho wins; com fix verde-mosaico wins"
    - "test_features.py::test_classify_constitution_mista_biliar_when_yellow_amber_pigment_present adicionado — input fixture verde-mosaico + sectoral_pigments=[{hour:12,type:'amarelo_ambar',intensity:'moderado'}] → primary='mista_biliar'"
    - "test_features.py::test_classify_constitution_hematogenea_pure_brown_no_pigment adicionado — regression: castanho denso sem pigmento difuso → hematogenea (não mista_hematogenea)"
    - "test_features.py::test_extract_all_round_trips_through_pydantic continua GREEN com schema novo (sectoral_pigments field presente, list pode ser vazia)"
    - "test_sectoral_pigments.py NOVO: 5 cases — sintetiza polar com setor 12 amarelo + base verde-acinzentada → detecta amarelo_ambar moderado em hour 12; setor 6 laranja → laranja; uniform polar → []; setor 12 leve → intensity='leve'; setor 12 denso → 'denso'"
    - "pytest --cov=vision-service/pipeline cobre os novos códigos ≥80%"
    - "Suite completa pytest verde — zero regressão em test_features.py existente (mantém todos os 17 cases)"

  artifacts:
    - path: "vision-service/pipeline/features.py"
      provides: "classify_iris_color com pupil mask + _classify_constitution rewrite (6-way) + extract_all wiring sectoral_pigments"
      contains: "pupil_circle disc exclusion; sectoral_pigments call"
    - path: "vision-service/pipeline/schemas.py"
      provides: "ConstitutionType enum expandido + SectoralPigment BaseModel + EyeFeatures.sectoral_pigments field"
      contains: "MISTA_BILIAR, MISTA_HEMATOGENEA, BILIAR, NEUROGENICA; class SectoralPigment"
    - path: "vision-service/pipeline/sectoral_pigments.py"
      provides: "detect_sectoral_pigments(enhanced_polar, iris_circle, collarette_diameter_ratio) -> list[dict]"
      contains: "delta_LAB threshold detector"
    - path: "vision-service/tests/test_features.py"
      provides: "3 new test cases (pupil mask, mista_biliar, hematogenea regression)"
      contains: "test_classify_iris_color_excludes_pupil_pixels"
    - path: "vision-service/tests/test_sectoral_pigments.py"
      provides: "5 new test cases for sectoral pigment detection"
      contains: "test_detect_amarelo_ambar_moderado_hour_12"
    - path: "apps/web/scripts/recalibrate-centroids.mjs"
      provides: "Read-only recalibration proposal script — graceful degradation with N≥1 fixtures"
      contains: "Compute LAB centroid per real_iris_color category from calibration_annotations + reading_images"

  key_links:
    - from: "vision-service/pipeline/features.py classify_iris_color"
      to: "composite_image['pupil_circle']"
      via: "P0.1 exclusion mask = disc(pupil_circle, r*1.10)"
      pattern: "Pixels filtered BEFORE k-means clustering"
    - from: "vision-service/pipeline/features.py _classify_constitution"
      to: "sectoral_pigments from extract_all pipeline"
      via: "additional input arg — heuristic checks ≥2 superior-sector yellow-amber hits → biliar branch"
      pattern: "Constitution depends on cross-feature signal, not just iris_color+fiber_density"
    - from: "vision-service/pipeline/sectoral_pigments.py"
      to: "enhanced_polar 64x512 RGB (output of pipeline.enhance.clahe + normalize.daugman)"
      via: "Direct consumption — splits W into 12 sector columns + processes ciliary ring rows"
      pattern: "Pure function, no Modal-specific dependencies — testable with synthetic numpy arrays"
    - from: "apps/web/scripts/recalibrate-centroids.mjs"
      to: "calibration_annotations.real_iris_color + reading_images.storage_path"
      via: "Service-role Supabase read + storage download + LAB compute (sharp/jimp or canvas)"
      pattern: "Founder-local tool, read-only output to stdout"
---

<objective>
Corrigir a cascata de erros do pipeline de visão diagnosticada na sessão de calibração
2026-05-09 (calibration_diagnoses row, Nailli reading 71a7bf1d): iris_color reportada
como `castanho` quando real é `verde-acinzentada com pigmento amarelo-âmbar setorial`,
constitution reportada como `hematogenea` quando real é `mista biliar`, e pigmentos
setoriais (achado clínico mais visível) ausentes do output do pipeline.

**Scope: 4 P0s bundlados em 1 PLAN (decisão do founder) porque (a) tocam mesmo arquivo
features.py em ramos relacionados, (b) validação unificada é o mesmo reprocess da Nailli,
(c) P0.4 funciona com Nailli como fixture inicial (graceful degradation com N=1) e cresce
conforme corpus de anotações expande.**

**Por que esta PLAN existe agora:** B1a (mask filter pixels pretos) já está em prod, mas
reprocess da Nailli 2026-05-11 confirmou que B1a sozinho não chega — classificação ainda
volta `castanho/hematogenea`. Diagnóstico humano salvo no DB aponta 4 root causes
adicionais. Sem corrigir, dogfooding interno fica bloqueado (relatórios LLM ancorados em
features erradas = output clinicamente inútil).

**Workflow alvo (executor agent):**
1. T1 — pupil mask no classify_iris_color
2. T2 — ConstitutionType enum + _classify_constitution rewrite
3. T3 — sectoral_pigments.py + wire em extract_all
4. T4 — recalibrate-centroids.mjs script + apply Nailli-derived centroides em features.py:51-55
5. Validação end-to-end: founder roda `modal deploy` + reprocess Nailli + verify expectativas
</objective>

<context>
**Diagnóstico salvo (calibration_diagnoses id `19ae43a7-bfb1-44ff-9036-014cb7970dc5`, reading
Nailli `71a7bf1d-747f-4de8-9129-13b69197c6a4`, salvo 2026-05-09T21:22 por founder):**

Erros classificatórios constatados visualmente:
- Cor real: verde-acinzentada com pigmento amarelo-âmbar SETORIAL (concentrado no setor
  superior-medial próximo ao colarete, bilateral com leve assimetria, mais denso no OD).
  Pipeline atual reporta `castanho` (LAB médio 66-86 puxado pela média global incluindo
  pupila preta + sombras de pálpebra + pigmento amarelo escuro minoritário).
- Constituição real: mista biliar (base linfática + pigmentação biliar sobreposta). Pipeline
  reporta `hematogenea` por cascata de P0.1 (cor errada).
- Pigmentos setoriais: amarelo-âmbar bilateral nos setores superiores 11/12/1/2 — NÃO
  detectado pelo pipeline atual (`sectoral_pigments` campo não existe; `findings` só
  contém lacunas, não pigmentos cromáticos).

Hipóteses de root cause (validadas por inspeção visual + log do features.py:51-55):
- IRIS_COLOR_LAB_CENTROIDS hardcoded calibrado "às cegas" (não derivado de fixtures reais).
- classify_iris_color k-means inclui pixels pupilares que puxam L para 0 → distância para
  centroide `castanho` (L=90) fica artificialmente menor que para `verde-mosaico` (L=140).
- _classify_constitution só conhece linfatica/hematogenea/mista — não acomoda biliar puro
  nem mistas com qualificador.
- Pigmentação cromática setorial não é feature do pipeline — pipeline atual só extrai
  achados geométricos (lacunas via connectedComponentsWithStats).

Estado pós-B1a (2026-05-11 reprocess, call_id KRBARC...): iris ainda classificada como
castanho/hematogenea — confirma que B1a é necessário mas insuficiente.

**P0.4 corpus prereq:** diagnóstico literal pede "10-15 fixtures" antes da recalibração.
Hoje temos 1 row em `calibration_annotations` (Nailli). Decisão do founder: incluir o
script de recalibração nesta PLAN com graceful degradation (funciona com N=1, propõe só
o centroide das categorias com fixtures disponíveis, mantém hardcoded as outras). Centroide
verde-mosaico derivado da Nailli é melhor que o hardcoded chutado; outras categorias só
melhoram quando founder anotar mais readings via /admin/calibration.

**Estrutura do código atual relevante:**
- `vision-service/pipeline/features.py:48-58` — IRIS_COLOR_LABELS + IRIS_COLOR_LAB_CENTROIDS
- `vision-service/pipeline/features.py:65-142` — classify_iris_color (B1a já aplicado em 89-91)
- `vision-service/pipeline/features.py:211-250` — _classify_constitution (3-way: linfatica/hematogenea/mista)
- `vision-service/pipeline/features.py:322-388` — extract_all (orquestrador)
- `vision-service/pipeline/schemas.py:30-43` — ConstitutionType enum + Constitution model
- `vision-service/pipeline/schemas.py:142-153` — EyeFeatures (model_config=extra='forbid', então
  adicionar `sectoral_pigments` requer schema bump)
- `apps/web/app/api/vision/webhook/route.ts:64-79` — Zod envelope usa `z.record(z.string(), z.unknown())`
  como passthrough → nenhuma mudança aqui (vision-service é authority do inner schema)
- `apps/web/types/database.ts` — `vision_features` é `Json` → nenhuma migration/regen necessária

**Backward compatibility:**
- `EyeFeatures.sectoral_pigments` ganha `default_factory=list` → readings legadas (sem campo)
  passam pelo `model_validate` sem erro de validação.
- `extra='forbid'` no EyeFeatures **DEVE** ser preservado — adicionar `sectoral_pigments`
  como campo declarado mantém o forbid funcional para outros campos não previstos.
- Constitution.primary continua sendo `str` (não enforced Enum) → adicionar valores novos
  não quebra payloads antigos.

**Não-objetivos desta PLAN:**
- P1.1 (conversão pixels→mm das lacunas) — fica para Wave C
- P1.2 (threshold de lacuna por constituição) — Wave C
- P2.1/P2.2 (collarette irregular, density saturation) — Wave C
- Detecção setorial pigmentar fina (anel ciliar vs zona colarética) — esta PLAN usa
  resolução por hour (12 buckets), refinements ficam para Wave C
- Validation com >1 fixture (Nailli only) — corpus expansion é parallel sprint
</context>

<tasks>

## T1 — P0.1: Pupil mask em classify_iris_color

**Wave 1** | Sequential | Estimated: ~1h

### T1.1 — RED: test sintético para pupila preta puxando media para castanho

Adicionar em `vision-service/tests/test_features.py`:

```python
def test_classify_iris_color_excludes_pupil_pixels():
    """Synthetic image: 200x200 RGB with central black pupil disc (r=30)
    + iris annulus painted verde-mosaico (L=140, a=110, b=145 in LAB).
    Without pupil mask: k-means picks black cluster as primary -> castanho.
    With pupil mask: clean verde-mosaico -> primary='verde-mosaico'.
    """
    import numpy as np
    from pipeline.features import classify_iris_color

    H, W = 200, 200
    cx, cy = W // 2, H // 2
    r_iris = 80
    r_pupil = 30

    # Verde-mosaico LAB -> RGB approx (L=140 a=110 b=145 -> ~RGB (102, 130, 80))
    img = np.zeros((H, W, 3), dtype=np.uint8)
    yy, xx = np.ogrid[:H, :W]
    iris_mask = (xx - cx) ** 2 + (yy - cy) ** 2 <= r_iris ** 2
    pupil_mask = (xx - cx) ** 2 + (yy - cy) ** 2 <= r_pupil ** 2

    img[iris_mask] = [102, 130, 80]    # verde-mosaico fill
    img[pupil_mask] = [0, 0, 0]        # pupila preta (>0 pixels após bitwise_and)

    # WITHOUT pupil_circle hint: today B1a only filters R+G+B==0; pupila preta
    # tem pixels com baixíssimo valor mas não exatamente zero após enhance — vamos
    # forçar a presença pintando 1 pra simular o caso real onde pupila chega como
    # cinza muito escuro (não puro zero).
    pupil_dark_ring = ((xx - cx) ** 2 + (yy - cy) ** 2 > r_pupil ** 2) & \
                      ((xx - cx) ** 2 + (yy - cy) ** 2 <= (r_pupil + 5) ** 2)
    img[pupil_dark_ring] = [3, 3, 3]   # quase preto mas não zero

    # Sem hint: pode falhar (acceptable — comportamento documentado, não asserted)
    # Com hint: deve acertar
    result_with_hint = classify_iris_color(
        img,
        iris_circle=(cx, cy, r_iris),
        pupil_circle=(cx, cy, r_pupil),
    )
    assert result_with_hint["primary"] in {"verde-mosaico", "misto"}, (
        f"With pupil hint, primary should be verde-mosaico or misto, got "
        f"{result_with_hint['primary']}"
    )


def test_classify_iris_color_pupil_hint_optional_no_crash():
    """Backward compat: pupil_circle=None must not crash (fallback to B1a only)."""
    import numpy as np
    from pipeline.features import classify_iris_color

    img = np.zeros((50, 50, 3), dtype=np.uint8)
    img[10:40, 10:40] = [102, 130, 80]
    # No pupil_circle, no iris_circle — should not raise
    result = classify_iris_color(img)
    assert result["primary"] in {"azul", "castanho", "verde-mosaico", "misto"}
```

**Gate:** `pytest -k test_classify_iris_color_excludes_pupil_pixels` RED (FAIL on `pupil_circle` keyword arg not accepted by current signature).

### T1.2 — GREEN: implementar pupil exclusion em classify_iris_color

Modificar `vision-service/pipeline/features.py` `classify_iris_color`:

```python
def classify_iris_color(
    masked_image: np.ndarray,
    iris_circle: Optional[tuple[float, float, float]] = None,
    pupil_circle: Optional[tuple[float, float, float]] = None,   # NEW
) -> dict:
    """...
    Args:
        ...
        pupil_circle: Optional (cx, cy, r) — exclui disco pupilar (r*1.10
            margem para anti-aliasing) do k-means. Sem ele, B1a sozinho
            (filter R+G+B==0) deixa passar pixels pupilares cinza-quase-preto
            e o cluster center médio cai em range marrom escuro (bug B1a-followup).
    """
    lab = cv2.cvtColor(masked_image, cv2.COLOR_RGB2LAB)
    rgb_pixels = masked_image.reshape(-1, 3)

    # B1a filter (existing): non-iris mask pixels are pure black.
    iris_pixels_mask = rgb_pixels.sum(axis=1) > 0

    # P0.1: also exclude pupil disc if hint provided.
    if pupil_circle is not None:
        cx_p, cy_p, r_p = pupil_circle
        if r_p > 0:
            r_excl = r_p * 1.10
            H, W = masked_image.shape[:2]
            yy, xx = np.ogrid[:H, :W]
            pupil_disc = (xx - cx_p) ** 2 + (yy - cy_p) ** 2 <= r_excl ** 2
            # Flatten + AND with iris_pixels_mask (skip pupil pixels)
            iris_pixels_mask = iris_pixels_mask & (~pupil_disc.reshape(-1))

    pixels = lab.reshape(-1, 3).astype(np.float32)[iris_pixels_mask]

    if pixels.shape[0] < KMEANS_K:
        return {"primary": "misto", "secondary": None, "central_heterochromia": False}

    # ... rest unchanged (k-means + _nearest_color)
```

Atualizar `extract_all` (linha ~355) para passar pupil_circle:

```python
iris_color = classify_iris_color(
    composite_image["segmented_image"],
    iris_circle=composite_image.get("iris_circle"),
    pupil_circle=composite_image.get("pupil_circle"),    # NEW
)
```

**Gate:** `pytest vision-service/tests/test_features.py -v` — todos os 17 cases existentes + 2 novos GREEN.

### T1.3 — Commit atômico

```
feat(vision/B1a-followup): exclude pupil disc from iris-color k-means (P0.1)

B1a (mask filter) alone insufficient — pupil pixels arrive as ~RGB (3,3,3)
after enhance.clahe (not pure zero) and still pull cluster center toward
LAB-black, which lands on 'castanho' centroid (L=90). P0.1 adds optional
pupil_circle hint to classify_iris_color: when provided, pixels inside
disc(cx, cy, r*1.10) are filtered before k-means.

Wired through extract_all.composite_image['pupil_circle'] which is already
populated by pipeline.detect.locate_pupil.
```

---

## T2 — P0.2: Constitution enum expandido + heuristic rewrite

**Wave 1** | Sequential after T1 | Estimated: ~2-3h

**Dependência:** T2.2 (`_classify_constitution`) recebe sectoral_pigments como input, mas
sectoral_pigments só existe após T3. **Estratégia:** T2 implementa o enum + estrutura nova
de `_classify_constitution` aceitando `sectoral_pigments: list[dict] | None = None`
parameter (default None preserva backward compat). T3 wires real value. Tests intermédios
de T2 passam sectoral_pigments=None ou hardcoded fixture.

### T2.1 — RED: tests para mista_biliar e hematogenea-pure regressão

Adicionar em `vision-service/tests/test_features.py`:

```python
def test_classify_constitution_mista_biliar_when_yellow_amber_pigment_present():
    """Verde-mosaico + ≥2 amarelo_ambar moderado pigments in hours 11/12/1/2 → mista_biliar."""
    from pipeline.features import _classify_constitution

    iris_color = {"primary": "verde-mosaico", "secondary": None, "central_heterochromia": False}
    fiber_density = {"score": 0.55, "interpretation": "media"}
    sectoral_pigments = [
        {"hour": 12, "type": "amarelo_ambar", "intensity": "moderado", "delta_lab": [0, 0, 18]},
        {"hour": 1, "type": "amarelo_ambar", "intensity": "denso", "delta_lab": [0, 0, 26]},
    ]
    result = _classify_constitution(iris_color, fiber_density, sectoral_pigments=sectoral_pigments)
    assert result["primary"] == "mista_biliar"
    assert "pigmento_amarelo_ambar_setorial" in result["indicators"]
    assert result["confidence"] >= 0.65


def test_classify_constitution_biliar_pure_no_lymphatic_base():
    """Verde-mosaico denso + ≥2 amarelo-âmbar superior + alta densidade → biliar (não mista_biliar)."""
    from pipeline.features import _classify_constitution

    iris_color = {"primary": "verde-mosaico", "secondary": None, "central_heterochromia": False}
    fiber_density = {"score": 0.90, "interpretation": "densa"}
    sectoral_pigments = [
        {"hour": 12, "type": "amarelo_ambar", "intensity": "denso", "delta_lab": [0, 0, 30]},
        {"hour": 1, "type": "amarelo_ambar", "intensity": "moderado", "delta_lab": [0, 0, 20]},
    ]
    result = _classify_constitution(iris_color, fiber_density, sectoral_pigments=sectoral_pigments)
    assert result["primary"] == "biliar"


def test_classify_constitution_hematogenea_pure_unchanged_regression():
    """Castanho denso + sem pigmento difuso continua hematogenea (não promove para mista_hematogenea)."""
    from pipeline.features import _classify_constitution

    iris_color = {"primary": "castanho", "secondary": None, "central_heterochromia": False}
    fiber_density = {"score": 0.85, "interpretation": "densa"}
    result = _classify_constitution(iris_color, fiber_density, sectoral_pigments=[])
    assert result["primary"] == "hematogenea"


def test_classify_constitution_mista_hematogenea_when_brown_diffuse_pigment():
    """Castanho + marrom_difuso pigment → mista_hematogenea."""
    from pipeline.features import _classify_constitution

    iris_color = {"primary": "castanho", "secondary": None, "central_heterochromia": False}
    fiber_density = {"score": 0.6, "interpretation": "media"}
    sectoral_pigments = [
        {"hour": 9, "type": "marrom_difuso", "intensity": "moderado", "delta_lab": [-12, 6, 8]},
        {"hour": 10, "type": "marrom_difuso", "intensity": "moderado", "delta_lab": [-13, 7, 9]},
    ]
    result = _classify_constitution(iris_color, fiber_density, sectoral_pigments=sectoral_pigments)
    assert result["primary"] == "mista_hematogenea"


def test_classify_constitution_neurogenica_when_fine_radial_dense():
    """Linfatica indicators + fibras_finas_radiais marcadas → neurogenica."""
    from pipeline.features import _classify_constitution

    iris_color = {"primary": "azul", "secondary": None, "central_heterochromia": False}
    fiber_density = {"score": 0.88, "interpretation": "densa"}
    # heurística: azul + densa = neurogenica (fibras radiais finas marcadas)
    result = _classify_constitution(iris_color, fiber_density, sectoral_pigments=[])
    assert result["primary"] == "neurogenica"


def test_classify_constitution_sectoral_pigments_optional_default_none():
    """Backward compat: chamadas sem sectoral_pigments funcionam (default None)."""
    from pipeline.features import _classify_constitution

    result = _classify_constitution(
        {"primary": "azul", "secondary": None, "central_heterochromia": False},
        {"score": 0.5, "interpretation": "esparsa"},
    )
    assert result["primary"] == "linfatica"
```

**Gate:** `pytest -k test_classify_constitution` — 6 cases RED.

### T2.2 — GREEN: enum + heuristic rewrite

Modificar `vision-service/pipeline/schemas.py:30-35` (ConstitutionType enum):

```python
class ConstitutionType(str, Enum):
    """Canonical Jensen constitution values (informational; not enforced).

    Expanded 2026-05-11 (PLAN 07.1-02) to accommodate Brazilian iridology
    school + mixed types diagnosed by founder in calibration sprint.
    """

    LINFATICA = "linfatica"
    HEMATOGENEA = "hematogenea"
    MISTA = "mista"
    # Added by PLAN 07.1-02:
    MISTA_BILIAR = "mista_biliar"
    MISTA_HEMATOGENEA = "mista_hematogenea"
    BILIAR = "biliar"
    NEUROGENICA = "neurogenica"
```

Reescrever `_classify_constitution` em `vision-service/pipeline/features.py:211-250`:

```python
def _classify_constitution(
    iris_color: dict,
    fiber_density: dict,
    sectoral_pigments: Optional[list[dict]] = None,
) -> dict:
    """6-way Jensen constitution heuristic (PLAN 07.1-02).

    Heuristic priority (first match wins):
      1. azul + densa             -> neurogenica
      2. azul + (esparsa|media)   -> linfatica
      3. verde-mosaico + amarelo-âmbar superior x≥2 -> mista_biliar (low fiber) | biliar (densa)
      4. castanho + marrom_difuso x≥2               -> mista_hematogenea
      5. castanho                 -> hematogenea
      6. fallback                 -> mista

    Args:
        iris_color: output of classify_iris_color.
        fiber_density: output of _compute_fiber_density.
        sectoral_pigments: output of detect_sectoral_pigments (P0.3); None when
            called without sectoral_pigments support (backward compat).

    Returns:
        dict with primary (str), confidence (float), indicators (list[str]).
    """
    primary_color = iris_color.get("primary", "misto")
    interpretation = fiber_density.get("interpretation", "media")
    pigments = sectoral_pigments or []
    indicators: list[str] = []

    # Pigment signals
    superior_hours = {11, 12, 1, 2}
    amarelo_ambar_superior = [
        p for p in pigments
        if p.get("type") == "amarelo_ambar"
        and p.get("hour") in superior_hours
        and p.get("intensity") in ("moderado", "denso")
    ]
    marrom_difuso_present = [
        p for p in pigments
        if p.get("type") == "marrom_difuso" and p.get("intensity") in ("moderado", "denso")
    ]

    # Branch 1: neurogenica (azul + densa)
    if primary_color == "azul" and interpretation == "densa":
        indicators.append("coloracao_azul_clara")
        indicators.append("fibras_radiais_finas_marcadas")
        return {"primary": "neurogenica", "confidence": 0.7, "indicators": indicators}

    # Branch 2: linfatica
    if primary_color == "azul":
        indicators.append("coloracao_azul_clara")
        return {
            "primary": "linfatica",
            "confidence": 0.8 if iris_color.get("secondary") is None else 0.7,
            "indicators": indicators,
        }

    # Branch 3: biliar / mista_biliar
    if primary_color in ("verde-mosaico", "misto") and len(amarelo_ambar_superior) >= 2:
        indicators.append("coloracao_verde_amarelada")
        indicators.append("pigmento_amarelo_ambar_setorial")
        if interpretation == "densa":
            return {"primary": "biliar", "confidence": 0.7, "indicators": indicators}
        return {"primary": "mista_biliar", "confidence": 0.65, "indicators": indicators}

    # Branch 4: mista_hematogenea
    if primary_color == "castanho" and len(marrom_difuso_present) >= 2:
        indicators.append("coloracao_castanha")
        indicators.append("pigmento_marrom_difuso")
        return {"primary": "mista_hematogenea", "confidence": 0.65, "indicators": indicators}

    # Branch 5: hematogenea pura
    if primary_color == "castanho":
        indicators.append("coloracao_castanha")
        return {"primary": "hematogenea", "confidence": 0.8, "indicators": indicators}

    # Branch 6: fallback
    indicators.append("coloracao_mista_ou_verde")
    return {"primary": "mista", "confidence": 0.6, "indicators": indicators}
```

Atualizar `extract_all` (após T3 wire de sectoral_pigments fica):

```python
constitution = _classify_constitution(iris_color, fiber_density, sectoral_pigments=sectoral_pigments_eye)
```

Em T2 (antes de T3): chamar `_classify_constitution(iris_color, fiber_density)` sem 3º arg (default None) — keeps green.

**Gate:** `pytest vision-service/tests/test_features.py -v` — todos os 17 originais + 8 novos (T1 + T2) GREEN.

### T2.3 — Commit atômico

```
feat(vision/schemas): expand ConstitutionType + 6-way heuristic (P0.2)

Original 3-way (linfatica/hematogenea/mista) too coarse for Brazilian
iridology school where mixed biliary is the most common adult diagnosis.
Adds: MISTA_BILIAR, MISTA_HEMATOGENEA, BILIAR, NEUROGENICA.

Constitution.primary remains `str` (not Enum-enforced) — old payloads
validate unchanged. New heuristic accepts optional sectoral_pigments input
(default None preserves backward compat); P0.3 will wire real value.
```

---

## T3 — P0.3: Sectoral pigment detection

**Wave 2** | Sequential after T2 | Estimated: ~3-4h

### T3.1 — RED: test_sectoral_pigments.py NEW

Criar `vision-service/tests/test_sectoral_pigments.py`:

```python
"""Tests for pipeline.sectoral_pigments — chromatic per-sector pigment detector.

PLAN 07.1-02 P0.3. Synthesizes polar-normalized 64x512 RGB images where
specific sector columns are painted with a target LAB delta over a base,
asserts the detector returns the expected hour/type/intensity entries.
"""
import numpy as np
import pytest


BASE_LAB = (120, 128, 128)  # neutral gray, neither warm nor cool


def _lab_to_rgb_uint8(lab_tuple):
    """Crude LAB(uint8 0-255) -> RGB approx via cv2."""
    import cv2
    arr = np.array([[list(lab_tuple)]], dtype=np.uint8)
    rgb = cv2.cvtColor(arr, cv2.COLOR_LAB2RGB)
    return tuple(int(v) for v in rgb[0, 0])


def _make_polar(base_rgb, sector_overrides):
    """64x512 polar image: column[i*42:(i+1)*42] gets sector i+1 override RGB.

    sector_overrides = {hour_int: rgb_tuple}
    """
    H, W = 64, 504  # 12 * 42 — clean divisions
    img = np.zeros((H, W, 3), dtype=np.uint8)
    img[:, :] = base_rgb
    SECTOR_W = W // 12
    for hour, rgb in sector_overrides.items():
        col_start = (hour - 1) * SECTOR_W
        col_end = hour * SECTOR_W
        # Ciliary ring = middle rows (rows 16..48 of 64)
        img[16:48, col_start:col_end] = rgb
    return img


def test_detect_amarelo_ambar_moderado_hour_12():
    from pipeline.sectoral_pigments import detect_sectoral_pigments

    base = _lab_to_rgb_uint8(BASE_LAB)
    yellow = _lab_to_rgb_uint8((120, 128, 128 + 20))  # delta_b = +20 -> moderado

    polar = _make_polar(base, {12: yellow})
    pigments = detect_sectoral_pigments(polar, iris_circle=None, collarette_diameter_ratio=0.32)

    yellows = [p for p in pigments if p["type"] == "amarelo_ambar"]
    assert any(p["hour"] == 12 for p in yellows), f"hour 12 missing: {pigments}"
    p12 = next(p for p in yellows if p["hour"] == 12)
    assert p12["intensity"] in ("moderado", "denso"), p12
    assert p12["delta_lab"][2] > 12, p12  # b channel positive


def test_detect_laranja_when_a_and_b_both_positive():
    from pipeline.sectoral_pigments import detect_sectoral_pigments

    base = _lab_to_rgb_uint8(BASE_LAB)
    orange = _lab_to_rgb_uint8((120, 128 + 12, 128 + 16))
    polar = _make_polar(base, {6: orange})
    pigments = detect_sectoral_pigments(polar, iris_circle=None, collarette_diameter_ratio=0.32)

    oranges = [p for p in pigments if p["type"] == "laranja" and p["hour"] == 6]
    assert oranges, f"hour 6 laranja missing: {pigments}"


def test_detect_empty_when_uniform_base():
    from pipeline.sectoral_pigments import detect_sectoral_pigments

    base = _lab_to_rgb_uint8(BASE_LAB)
    polar = _make_polar(base, {})
    pigments = detect_sectoral_pigments(polar, iris_circle=None, collarette_diameter_ratio=0.32)
    assert pigments == [], pigments


def test_detect_intensity_leve_when_delta_b_small():
    from pipeline.sectoral_pigments import detect_sectoral_pigments

    base = _lab_to_rgb_uint8(BASE_LAB)
    light_yellow = _lab_to_rgb_uint8((120, 128, 128 + 14))  # delta_b = +14 -> leve
    polar = _make_polar(base, {1: light_yellow})
    pigments = detect_sectoral_pigments(polar, iris_circle=None, collarette_diameter_ratio=0.32)

    yellows = [p for p in pigments if p["type"] == "amarelo_ambar" and p["hour"] == 1]
    if yellows:  # may be filtered as too weak — accept either way
        assert yellows[0]["intensity"] == "leve", yellows[0]


def test_detect_intensity_denso_when_delta_b_large():
    from pipeline.sectoral_pigments import detect_sectoral_pigments

    base = _lab_to_rgb_uint8(BASE_LAB)
    dense_yellow = _lab_to_rgb_uint8((120, 128, 128 + 30))  # delta_b = +30 -> denso
    polar = _make_polar(base, {12: dense_yellow})
    pigments = detect_sectoral_pigments(polar, iris_circle=None, collarette_diameter_ratio=0.32)

    yellows = [p for p in pigments if p["type"] == "amarelo_ambar" and p["hour"] == 12]
    assert yellows, "denso yellow hour 12 missing"
    assert yellows[0]["intensity"] == "denso", yellows[0]
```

**Gate:** `pytest vision-service/tests/test_sectoral_pigments.py -v` 5 cases RED (ModuleNotFoundError pipeline.sectoral_pigments).

### T3.2 — GREEN: implementar sectoral_pigments.py

Criar `vision-service/pipeline/sectoral_pigments.py`:

```python
"""Per-sector chromatic pigment detection (PLAN 07.1-02 P0.3).

Iridology context (founder calibration 2026-05-09): pigment markers visible
on the iris are clinically as important as geometric findings (lacunas).
Amarelo-âmbar in superior sectors signals hepato-biliary terrain;
marrom-difuso signals chronic hematogenea overlap; laranja signals
digestive/pancreatic involvement.

Algorithm:
    1. Take the polar-normalized iris image (64 x 504 RGB, 12 hour-sectors).
    2. For each hour i in 1..12:
         - Slice column range [(i-1)*SECTOR_W : i*SECTOR_W]
         - Restrict rows to the ciliary ring (middle 50% of H — excludes
           pupil edge artifacts at top rows and limbus at bottom)
         - Compute LAB mean across that slice.
    3. Compute base LAB = median across ALL 12 sector means
       (robust to outliers — if 2 sectors are dyed, base still reflects stroma).
    4. delta_LAB[i] = sector_LAB[i] - base_LAB
    5. Classify:
         amarelo_ambar:   delta_b > +12 AND |delta_a| < 8
         laranja:         delta_a > +8 AND delta_b > +8
         marrom_difuso:   delta_L < -10 AND delta_a > +5 AND delta_b > +5
       (other deltas may exist but only these 3 are clinically actionable in v1).
    6. Intensity from |delta_b| (or |delta_a + delta_b| for laranja):
         < 16  -> leve
         16-24 -> moderado
         > 24  -> denso

The `collarette_diameter_ratio` arg is currently UNUSED but accepted for forward
compat — Wave C P2.1 will narrow rows to "between collarette and limbus" using it.
The `iris_circle` arg is also accepted but not used at polar resolution (the polar
unwrap already mapped iris geometry; pixels are addressable directly via column index).
"""
from __future__ import annotations

from typing import Optional

import cv2
import numpy as np

SECTOR_COUNT = 12
DELTA_B_LEVE_MIN = 12
DELTA_B_MODERADO_MIN = 16
DELTA_B_DENSO_MIN = 24

CILIARY_RING_TOP_FRAC = 0.25  # rows 25%..75% of H
CILIARY_RING_BOTTOM_FRAC = 0.75


def _classify_intensity(delta_b_abs: float) -> str:
    if delta_b_abs >= DELTA_B_DENSO_MIN:
        return "denso"
    if delta_b_abs >= DELTA_B_MODERADO_MIN:
        return "moderado"
    return "leve"


def detect_sectoral_pigments(
    enhanced_polar: np.ndarray,
    iris_circle: Optional[tuple[float, float, float]] = None,
    collarette_diameter_ratio: float = 0.32,
) -> list[dict]:
    """Detect chromatic pigment markers per hour sector.

    Args:
        enhanced_polar: RGB uint8 polar image, shape (H, W, 3). W must be
            divisible by SECTOR_COUNT (default polar pipeline emits 64x504
            which gives 42 cols/sector).
        iris_circle: Reserved for future (unused at polar resolution).
        collarette_diameter_ratio: Reserved for future row-narrowing.

    Returns:
        List of pigment dicts (may be empty):
            [{
                'hour': int (1..12),
                'type': 'amarelo_ambar'|'laranja'|'marrom_difuso',
                'intensity': 'leve'|'moderado'|'denso',
                'delta_lab': [dL, dA, dB]
            }, ...]
    """
    H, W = enhanced_polar.shape[:2]
    sector_w = W // SECTOR_COUNT
    if sector_w == 0:
        return []

    row_top = int(H * CILIARY_RING_TOP_FRAC)
    row_bottom = int(H * CILIARY_RING_BOTTOM_FRAC)
    if row_bottom <= row_top:
        return []

    lab = cv2.cvtColor(enhanced_polar, cv2.COLOR_RGB2LAB).astype(np.float32)
    ciliary = lab[row_top:row_bottom]

    sector_means: list[np.ndarray] = []
    for i in range(SECTOR_COUNT):
        col_start = i * sector_w
        col_end = (i + 1) * sector_w
        slab = ciliary[:, col_start:col_end].reshape(-1, 3)
        if slab.size == 0:
            sector_means.append(np.array([0, 128, 128], dtype=np.float32))
        else:
            sector_means.append(slab.mean(axis=0))

    base = np.median(np.stack(sector_means, axis=0), axis=0)

    results: list[dict] = []
    for i, sm in enumerate(sector_means):
        dL, dA, dB = (sm - base).tolist()
        hour = i + 1

        # amarelo_ambar
        if dB > DELTA_B_LEVE_MIN and abs(dA) < 8 and not (dA > 8 and dB > 8):
            results.append({
                "hour": hour,
                "type": "amarelo_ambar",
                "intensity": _classify_intensity(abs(dB)),
                "delta_lab": [round(dL, 2), round(dA, 2), round(dB, 2)],
            })
            continue
        # laranja (a+ AND b+)
        if dA > 8 and dB > 8:
            results.append({
                "hour": hour,
                "type": "laranja",
                "intensity": _classify_intensity(abs(dA) + abs(dB) - 8),
                "delta_lab": [round(dL, 2), round(dA, 2), round(dB, 2)],
            })
            continue
        # marrom_difuso
        if dL < -10 and dA > 5 and dB > 5:
            results.append({
                "hour": hour,
                "type": "marrom_difuso",
                "intensity": _classify_intensity(abs(dL)),
                "delta_lab": [round(dL, 2), round(dA, 2), round(dB, 2)],
            })

    return results
```

Adicionar em `vision-service/pipeline/schemas.py` após `class Pupil`:

```python
class SectoralPigment(BaseModel):
    """Per-sector chromatic pigment marker (PLAN 07.1-02 P0.3)."""

    hour: int = Field(ge=1, le=12)
    type: str  # 'amarelo_ambar' | 'laranja' | 'marrom_difuso'
    intensity: str  # 'leve' | 'moderado' | 'denso'
    delta_lab: list[float] = Field(min_length=3, max_length=3)

    model_config = ConfigDict(extra="forbid")
```

E adicionar campo em `EyeFeatures`:

```python
class EyeFeatures(BaseModel):
    constitution: Constitution
    iris_color: IrisColor
    fiber_density: FiberDensity
    collarette: Collarette
    pupil: Pupil
    sectors: list[Sector] = Field(default_factory=list)
    sectoral_pigments: list[SectoralPigment] = Field(default_factory=list)  # NEW (P0.3)
    rings: Rings
    global_signs: GlobalSigns
    image_quality: ImageQuality

    model_config = ConfigDict(extra="forbid")
```

Wire em `extract_all` (features.py):

```python
from pipeline.sectoral_pigments import detect_sectoral_pigments  # top of file

# inside extract_all, after sectors loop:
sectoral_pigments_eye = detect_sectoral_pigments(
    enhanced_image,
    iris_circle=composite_image.get("iris_circle"),
    collarette_diameter_ratio=collarette.get("diameter_ratio", 0.32),
)

# update constitution call to use sectoral pigments:
constitution = _classify_constitution(
    iris_color, fiber_density, sectoral_pigments=sectoral_pigments_eye
)

return {
    "constitution": constitution,
    "iris_color": iris_color,
    "fiber_density": fiber_density,
    "collarette": collarette,
    "pupil": pupil,
    "sectors": sectors,
    "sectoral_pigments": sectoral_pigments_eye,    # NEW
    "rings": rings,
    "global_signs": global_signs,
    "image_quality": image_quality,
}
```

Export do módulo em `vision-service/pipeline/__init__.py` (se houver `__all__`, adicionar `'sectoral_pigments'`).

**Gate:** `pytest vision-service/tests/test_sectoral_pigments.py vision-service/tests/test_features.py -v` — todos GREEN. `test_extract_all_round_trips_through_pydantic` confirma EyeFeatures aceita o novo campo.

### T3.3 — Commit atômico

```
feat(vision): sectoral pigment detector (P0.3)

New module pipeline/sectoral_pigments.py + EyeFeatures.sectoral_pigments
field. Detects per-hour chromatic markers (amarelo_ambar / laranja /
marrom_difuso) via LAB delta against per-image stroma base.

Wires into extract_all + _classify_constitution (P0.2) — biliar/mista_biliar
branch now triggers on amarelo-âmbar superior x≥2 confirmed by detector,
not heuristic guess.

EyeFeatures backward compat: sectoral_pigments default_factory=list, old
payloads model_validate clean.
```

---

## T4 — P0.4: Centroid recalibration script + first-pass apply

**Wave 3** | Sequential after T3 | Estimated: ~1-2h (with N=1 fixture)

### T4.1 — Implementar recalibrate-centroids.mjs

Criar `apps/web/scripts/recalibrate-centroids.mjs`:

```javascript
#!/usr/bin/env node
// Read-only proposal script: extracts LAB centroids per real_iris_color
// from calibration_annotations + reading_images, prints proposal table.
// Founder applies values manually to vision-service/pipeline/features.py:51-55.
//
// Graceful degradation: works with N≥1 fixture per category. Categories
// without any fixture keep hardcoded centroids (script reports gap).
//
// Usage (from apps/web/):
//   MODAL_ANALYZE_ENDPOINT_URL=... node --env-file=./.env.local scripts/recalibrate-centroids.mjs
//
// PLAN 07.1-02 P0.4 — 2026-05-11.

import { createClient } from '@supabase/supabase-js'
import sharp from 'sharp'  // already in apps/web deps via shadcn/next-image — verify

const url = process.env.SUPABASE_URL || process.env.NEXT_PUBLIC_SUPABASE_URL
const key = process.env.SUPABASE_SERVICE_ROLE_KEY
if (!url || !key) { console.error('missing supabase env'); process.exit(2) }

const c = createClient(url, key, { auth: { persistSession: false } })

const HARDCODED = {
  azul: [220, 130, 110],
  castanho: [90, 145, 160],
  'verde-mosaico': [140, 110, 145],
}

const COLOR_TO_CATEGORY = {
  // Map calibration_annotations.real_iris_color values to feature categories
  'azul': 'azul',
  'azul-claro': 'azul',
  'azul-acinzentado': 'azul',
  'castanho': 'castanho',
  'castanho-claro': 'castanho',
  'castanho-escuro': 'castanho',
  'verde': 'verde-mosaico',
  'verde-acinzentado': 'verde-mosaico',
  'verde-amarelado': 'verde-mosaico',
  'mista': 'misto',
  // founder's calibration page may add other values — gracefully grouped to 'unknown'
}

async function fetchAnnotations() {
  const { data, error } = await c
    .from('calibration_annotations')
    .select('reading_id, real_iris_color')
  if (error) throw error
  return data ?? []
}

async function fetchImagesForReadings(readingIds) {
  if (!readingIds.length) return {}
  const { data, error } = await c
    .from('reading_images')
    .select('reading_id, eye, angle, storage_path')
    .in('reading_id', readingIds)
    .eq('angle', 'frontal')   // frontal only for centroid (best lighting)
  if (error) throw error
  const byReading = {}
  for (const r of data ?? []) {
    if (!byReading[r.reading_id]) byReading[r.reading_id] = []
    byReading[r.reading_id].push(r)
  }
  return byReading
}

async function downloadAndCentroid(storagePath) {
  const { data, error } = await c.storage.from('iris-captures').download(storagePath)
  if (error) throw new Error(`download ${storagePath}: ${error.message}`)
  const buf = Buffer.from(await data.arrayBuffer())

  const img = sharp(buf)
  const meta = await img.metadata()
  // Crop central 60% to avoid eyelids/lashes
  const W = meta.width, H = meta.height
  const cropW = Math.floor(W * 0.6)
  const cropH = Math.floor(H * 0.6)
  const left = Math.floor((W - cropW) / 2)
  const top = Math.floor((H - cropH) / 2)

  const { data: pixels, info } = await sharp(buf)
    .extract({ left, top, width: cropW, height: cropH })
    .raw()
    .toBuffer({ resolveWithObject: true })

  // RGB -> LAB approx (sRGB -> LAB via D65 reference)
  // Use a lightweight conversion; sharp doesn't have native LAB conversion,
  // so we use the standard sRGB -> XYZ -> LAB formula.
  let sumL = 0, sumA = 0, sumB = 0, n = 0
  for (let i = 0; i < pixels.length; i += info.channels) {
    const r = pixels[i] / 255, g = pixels[i + 1] / 255, b = pixels[i + 2] / 255
    if (r + g + b < 0.05) continue  // skip near-black (pupil/mask)
    const [L, A, B] = rgbToLab(r, g, b)
    sumL += L; sumA += A; sumB += B; n++
  }
  if (n === 0) return null
  return [sumL / n, sumA / n, sumB / n].map(v => Math.round(v))
}

function rgbToLab(r, g, b) {
  // sRGB -> linear
  const lin = v => v <= 0.04045 ? v / 12.92 : Math.pow((v + 0.055) / 1.055, 2.4)
  const lr = lin(r), lg = lin(g), lb = lin(b)
  // D65
  const x = (lr * 0.4124 + lg * 0.3576 + lb * 0.1805) / 0.95047
  const y = (lr * 0.2126 + lg * 0.7152 + lb * 0.0722) / 1.00000
  const z = (lr * 0.0193 + lg * 0.1192 + lb * 0.9505) / 1.08883
  const f = t => t > 0.008856 ? Math.cbrt(t) : (7.787 * t + 16 / 116)
  const L = 116 * f(y) - 16
  const A = 500 * (f(x) - f(y))
  const B = 200 * (f(y) - f(z))
  // Scale to uint8-equivalent (vision-service uses cv2.LAB which is L*255/100, A+128, B+128)
  return [Math.round(L * 255 / 100), Math.round(A + 128), Math.round(B + 128)]
}

async function main() {
  const annotations = await fetchAnnotations()
  console.log(`Found ${annotations.length} annotated reading(s).`)
  if (!annotations.length) {
    console.log('No fixtures available — no recalibration possible.')
    console.log('Keep IRIS_COLOR_LAB_CENTROIDS hardcoded.')
    return
  }

  const ids = annotations.map(a => a.reading_id).filter(Boolean)
  const imagesByReading = await fetchImagesForReadings(ids)

  // Per-category aggregation
  const buckets = {}
  for (const a of annotations) {
    const cat = COLOR_TO_CATEGORY[a.real_iris_color] ?? 'unknown'
    if (!buckets[cat]) buckets[cat] = []
    const imgs = imagesByReading[a.reading_id] ?? []
    for (const img of imgs) {
      try {
        const lab = await downloadAndCentroid(img.storage_path)
        if (lab) {
          buckets[cat].push({ lab, source: `${a.reading_id} ${img.eye}` })
        }
      } catch (err) {
        console.error(`  skip ${img.storage_path}: ${err.message}`)
      }
    }
  }

  console.log('\nProposal table:\n')
  console.log('category         | n | proposed centroid | hardcoded         | delta')
  console.log('-----------------|---|-------------------|-------------------|-----------------')
  for (const cat of Object.keys(buckets)) {
    if (cat === 'unknown') continue
    const arr = buckets[cat]
    const n = arr.length
    if (n === 0) continue
    const mean = arr[0].lab.map((_, i) => Math.round(arr.reduce((s, x) => s + x.lab[i], 0) / n))
    const hard = HARDCODED[cat] ?? [null, null, null]
    const delta = hard[0] != null ? mean.map((v, i) => v - hard[i]) : [null, null, null]
    console.log(
      `${cat.padEnd(16)} | ${String(n).padEnd(1)} | (${mean.join(', ')}) | (${hard.join(', ')}) | (${delta.join(', ')})`,
    )
    if (n < 3) console.log(`    WARN: low confidence (n=${n} < 3). More fixtures recommended.`)
  }
  console.log('\nCategories without fixtures (keep hardcoded):')
  for (const cat of Object.keys(HARDCODED)) {
    if (!buckets[cat] || buckets[cat].length === 0) {
      console.log(`  ${cat}: KEEP hardcoded (${HARDCODED[cat].join(', ')})`)
    }
  }

  console.log('\nTo apply: update vision-service/pipeline/features.py:51-55 with the proposed values.')
}

main().catch(err => { console.error('FAIL:', err.message); process.exit(1) })
```

Verificar `sharp` está em `apps/web/package.json`:
- Se não, executor instala via `pnpm --filter web add sharp`. (Sharp já é dep transitiva do Next.js para image optimization, então provavelmente já está disponível.)

### T4.2 — First-pass run + apply Nailli-derived centroides

Executor roda o script:

```bash
cd D:/Projetos/Iridologista/apps/web
node --env-file=./.env.local scripts/recalibrate-centroids.mjs > /tmp/recalibrate-output.txt 2>&1
cat /tmp/recalibrate-output.txt
```

Anota o LAB proposto para categoria 'verde-mosaico' (vem da Nailli, que está anotada como verde-acinzentada).

Atualiza `vision-service/pipeline/features.py:51-55` SOMENTE o centroide com fixture:

```python
IRIS_COLOR_LAB_CENTROIDS: dict[str, tuple[int, int, int]] = {
    "azul": (220, 130, 110),        # KEEP — no fixtures yet
    "castanho": (90, 145, 160),     # KEEP — no fixtures yet
    "verde-mosaico": (XXX, YYY, ZZZ),  # P0.4 — from Nailli reading (apps/web/scripts/recalibrate-centroids.mjs 2026-05-11)
}
```

Onde `XXX, YYY, ZZZ` = valores propostos pelo script. **Critical:** se o script propor centroide com L ≥ 200 (muito claro) ou L ≤ 50 (muito escuro), DESCARTAR e manter hardcoded — anota no SUMMARY que first-pass falhou e P0.4 precisa de outras fixtures.

### T4.3 — Commit atômicos (separados — script vs. apply)

Commit 1:
```
feat(scripts): recalibrate-centroids.mjs read-only proposal tool (P0.4)

Reads calibration_annotations + reading_images, downloads frontal photos
via service-role, computes LAB centroid per real_iris_color category.
Outputs proposal table with delta vs hardcoded IRIS_COLOR_LAB_CENTROIDS.

Graceful degradation: works with N≥1 fixture per category; categories
without fixtures keep hardcoded values. Warns when n<3.

Output is read-only — founder applies proposed values manually to
vision-service/pipeline/features.py to preserve audit trail in diff.
```

Commit 2 (only if first-pass produced sane verde-mosaico):
```
fix(vision): recalibrate verde-mosaico LAB centroid from Nailli fixture (P0.4)

First-pass run of scripts/recalibrate-centroids.mjs against 1 fixture
(Nailli reading 71a7bf1d, annotated verde-acinzentado/mista_biliar) proposed
LAB centroid (X, Y, Z) for verde-mosaico category. Replaces hardcoded chute
(140, 110, 145).

Other categories (azul, castanho) keep hardcoded — no fixtures yet.
Centroid will be re-derived as calibration corpus grows past N=3 per category.
```

---

## T5 — End-to-end validation (founder gate)

**Wave 4** | After T1-T4 | Estimated: ~10 min

### T5.1 — Modal redeploy

```bash
cd vision-service && PYTHONIOENCODING=utf-8 modal deploy modal_app.py
```

Expect: `App deployed in <Xs> 🎉`.

### T5.2 — Flip Nailli reading to failed (if currently ready)

Reuse the pattern from 2026-05-11 resume session:

```bash
cd apps/web
# Use existing reprocess-nailli.mjs (uses service-role + MODAL tokens)
# OR simpler: just toggle the row via SQL or the flip-script if recreated
```

If `reprocess-nailli.mjs` can be invoked from local dev: founder needs
MODAL_TOKEN_ID/SECRET in .env.local. Alternative: founder flips status via
admin UI or one-shot service-role script then clicks Reprocessar in /leituras.

### T5.3 — Wait + verify

After Modal completes (~30-60s), query the reading and assert:

```javascript
// Quick verification (one-shot read script)
const { data } = await c.from('readings').select('vision_features').eq('id', '71a7bf1d...').single()
const vf = data.vision_features
console.assert(vf.left_eye.iris_color.primary !== 'castanho', 'iris_color still castanho!')
console.assert(vf.left_eye.constitution.primary !== 'hematogenea', 'constitution still hematogenea!')
console.assert((vf.left_eye.sectoral_pigments ?? []).length > 0, 'no sectoral pigments!')
```

Expected:
- `iris_color.primary` IN {`verde-mosaico`, `misto`} (NOT `castanho`)
- `constitution.primary` IN {`mista_biliar`, `biliar`, `mista`} (NOT `hematogenea`)
- `sectoral_pigments` array non-empty, at least 1 entry with `type='amarelo_ambar'` in hours {11,12,1,2}

### T5.4 — Documentar resultado no SUMMARY

SUMMARY.md inclui:
- LAB centroide proposto pelo script (Nailli first-pass)
- vision_features.left_eye.* e right_eye.* da Nailli pós-reprocess
- Diff antes/depois (castanho → ???, hematogenea → ???, sectoral_pigments_count: 0 → N)
- Pendências (P1.1, P1.2 Wave C; outras anotações para refinement)

---

</tasks>

<verification_gates>

**Por wave:**
1. T1.2 GREEN → `pytest vision-service/tests/test_features.py -k "test_classify_iris_color"` PASS (incluindo 2 novos)
2. T2.2 GREEN → `pytest vision-service/tests/test_features.py -k "test_classify_constitution"` PASS (6 novos)
3. T3.2 GREEN → `pytest vision-service/tests/test_sectoral_pigments.py` PASS (5 novos)
4. T3.2 GREEN → `pytest vision-service/tests/test_features.py::test_extract_all_round_trips_through_pydantic` PASS (schema validates new field)
5. T4.1 → script runs sem erro, output legível, tabela formatada
6. T4.2 → IRIS_COLOR_LAB_CENTROIDS atualizado APENAS se proposed value sane (L 100-180 para verde-mosaico)
7. T5.3 → Nailli reprocess produz expected output OU founder declara o ciclo "B1a-followup insuficiente, abrir Wave C"

**Suite completa pós-PLAN:**
- `cd vision-service && pytest` — todos os tests + 13 novos GREEN, zero regressões
- `cd apps/web && pnpm tsc --noEmit` — sem novos erros (pre-existentes Phase 3/5 documentados em STATE.md "Itens diferidos" permanecem)
- `cd apps/web && pnpm test:run` — sem novas falhas (3 pre-existing Phase 3 quality-scoring failures known per project memory feedback_quality_scoring_test_gate.md)
- `cd apps/web && pnpm audit:vocabulary` — 8 hits pre-existing inalterados

**Não-blockers explícitos (não-gate):**
- P0.4 first-pass pode falhar (Nailli foto resolution baixa, eyelids muito presentes, etc.) — se sim, mantém hardcoded e documenta em SUMMARY como "Wave C-or-later dependency on calibration sprint"
- 3 pre-existing failures em `quality-scoring.test.ts` (WEIGHTS.reflex Phase 3 debt) NÃO gate esta PLAN (per project memory)

</verification_gates>

<deviations_policy>

- **Rule 1 — Code-line fix:** se ConstitutionType enum string value não matches o naming (e.g. underscore vs hyphen drift), corrigir inline + nota no SUMMARY. Idem para `delta_lab` rounding (se 2 casas decimais quebra Pydantic float min_length, ajusta).
- **Rule 2 — Defensive hardening:** se test sintético de pupil_mask passa mas reprocess real falha em edge case (e.g. pupil_circle from Hough é (NaN, NaN, 0)), adicionar guard. Documentar.
- **Rule 3 — Schema TS drift:** apps/web não tem Zod schema do inner — não há TS drift esperado. Se algum consumer (FeaturesDisplay.tsx, anthropic/analyze.ts, rag/build-queries.ts) explicitly access `vision_features.left_eye.constitution.primary` com narrowing para o velho 3-way string union, defer fix to PLAN seguinte (não bloquear desta).
- **Rule 4 — Architectural reverse:** se durante T3 ficar claro que sectoral_pigments depende de feature ainda não computada (e.g. exact collarette boundary radius), PARAR e flagar — não improvisar workaround. Diagnóstico do founder é fonte da verdade.
- **Founder gate explícito:** T4.2 (apply proposed centroides) é DECISÃO subjetiva. Se executor agent não tem confiança no LAB proposto (e.g. n=1 + L fora de range esperado), commitar T4.1 script SEM commit T4.2 e flagar no SUMMARY para founder revisar manualmente.

</deviations_policy>

<acceptance_criteria>

PLAN é considerada COMPLETE quando:

1. ✓ T1-T4 commits no main branch.
2. ✓ Suite pytest vision-service verde (todos os tests + 13 novos).
3. ✓ Suite vitest apps/web sem novas falhas.
4. ✓ Modal deploy succeeded (vision-service refletindo todas as mudanças).
5. ✓ Reading Nailli reprocessada — vision_features atualizado.
6. **OU** Founder declarado: "Acceptance criteria parcial: P0.1+P0.2+P0.3 OK, P0.4 first-pass requer corpus expansion → ciclo continua na Wave C".

Critério 5 + acceptance (mista_biliar / verde-mosaico / sectoral_pigments populated) é o **goal**;
critério 6 é o **fallback explícito** caso first-pass centroid recalibration não converja.
SUMMARY documenta qual aconteceu.

</acceptance_criteria>
