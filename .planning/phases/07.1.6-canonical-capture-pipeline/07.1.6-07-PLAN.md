---
phase: 07.1.6
plan: 07
type: execute
wave: 3
depends_on: [03]
files_modified:
  - .gitignore
  - apps/web/scripts/probe-haiku-iris-landmarks.mjs
  - apps/web/scripts/output/landmarks-probe-sonnet-4-6.json
  - apps/web/lib/canonicalize/__tests__/probe-canonical.test.ts
autonomous: true
requirements: []
tags: [canonical-capture, regression-suite, golden-fixture, gated-integration-test]

must_haves:
  truths:
    - "D-06: Regression suite = golden JSON fixture + gated integration test (skip-by-default; opt-in via CANONICAL_PROBE=1 env var). Same pattern as Phase 7 ANTHROPIC_INTEGRATION=1."
    - "D-06: Golden fixture = Nailli e85ea7de Sonnet output (5 ok + 1 fallback right_frontal). Corpus expansion é Phase 07.2 Wave C."
    - "D-06: tolerance ±5% center_x_pct/center_y_pct, ±10% radius_pct contra golden fixture"
    - "D-06: founder roda manualmente antes de PR em mudanças ao canonicalize endpoint ou Sonnet prompt; CI não roda (custo Sonnet + flakiness VLM)"
    - "Probe script committable: header runbook + remove hardcoded Nailli default (aceita readingId positional arg; default mantido)"
    - "Existing scripts/output/* permanecem .gitignored exceto o golden fixture específico (negação por path)"
  artifacts:
    - path: ".gitignore"
      provides: "Add negation `!apps/web/scripts/output/landmarks-probe-sonnet-4-6.json` para permitir commit do golden fixture específico"
      contains: "!apps/web/scripts/output/landmarks-probe-sonnet-4-6.json"
    - path: "apps/web/scripts/probe-haiku-iris-landmarks.mjs"
      provides: "Probe script atualizado: header runbook expandido (env vars, output paths, cost expectation, golden-fixture role), NAILLI_DEFAULT renomeado/documentado como NAILLI_E85EA7DE_DEFAULT (golden reading)"
      contains: "Golden fixture role: Phase 07.1.6 regression baseline"
    - path: "apps/web/scripts/output/landmarks-probe-sonnet-4-6.json"
      provides: "Frozen Sonnet output para Nailli e85ea7de (committed por --force ou via .gitignore negation)"
      contains: "e85ea7de"
    - path: "apps/web/lib/canonicalize/__tests__/probe-canonical.test.ts"
      provides: "Gated integration test (CANONICAL_PROBE=1) re-rodar Sonnet contra Nailli e85ea7de e comparar bbox coords com tolerância contra golden JSON"
      min_lines: 60
  key_links:
    - from: "apps/web/lib/canonicalize/__tests__/probe-canonical.test.ts"
      to: "apps/web/scripts/output/landmarks-probe-sonnet-4-6.json (golden fixture)"
      via: "fs.readFileSync golden file as baseline"
      pattern: "landmarks-probe-sonnet-4-6\\.json"
    - from: "apps/web/lib/canonicalize/__tests__/probe-canonical.test.ts"
      to: "apps/web/lib/canonicalize/sonnet-bbox.ts fetchIrisBbox (Plan 03)"
      via: "live Sonnet call gated by CANONICAL_PROBE=1"
      pattern: "fetchIrisBbox"
---

<objective>
Commit a regression suite per D-06: golden fixture JSON (Nailli e85ea7de Sonnet output frozen) + probe script committable + gated integration test que re-roda Sonnet contra a Nailli e85ea7de e compara com tolerance.

Founder roda manualmente:
```
CANONICAL_PROBE=1 pnpm --filter web test -- probe-canonical
```
ANTES de mergear PRs que tocam canonicalize endpoint, Sonnet prompt, ou crop logic. CI NÃO roda (custo $0.05/run + flakiness VLM).

Output: 1 .gitignore mod + 1 script committed (currently gitignored mas não no .gitignore raiz — script file path está fora dos diretórios negados) + 1 golden fixture commit (via .gitignore negation) + 1 test file novo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1.6-canonical-capture-pipeline/07.1.6-CONTEXT.md
@.planning/phases/07.1.6-canonical-capture-pipeline/07.1.6-PATTERNS.md
@apps/web/scripts/probe-haiku-iris-landmarks.mjs
@apps/web/lib/anthropic/__tests__/integration.test.ts
@apps/web/lib/canonicalize/sonnet-bbox.ts

<interfaces>
<!-- Existing files (state) -->

`apps/web/scripts/probe-haiku-iris-landmarks.mjs` (currently committed in branch — see `git status` shows it under untracked? Verify):
- File EXISTS no working dir (lido em context)
- Status no git: untracked OR committed? Check via `git ls-files apps/web/scripts/probe-haiku-iris-landmarks.mjs`
- Current NAILLI_DEFAULT (L35): `'71a7bf1d-747f-4de8-9129-13b69197c6a4'` — Nailli ANTIGA (pre-protocol). Para D-06 golden = e85ea7de (NOVA, post-protocol). Update to `e85ea7de-0e5f-4f49-a889-f886e4a05073`.

`apps/web/scripts/output/landmarks-probe-sonnet-4-6.json`:
- File EXISTS no working dir (~/apps/web/scripts/output/ é gitignored linha 22 .gitignore)
- Contains Nailli e85ea7de Sonnet output (5/6 visually accepted by founder)

`.gitignore` line 22: `apps/web/scripts/output/`
- Wholesale ignore — need negation specific para o golden fixture.

`apps/web/lib/anthropic/__tests__/integration.test.ts` (analog):
```typescript
import { describe, it } from 'vitest'
const RUN_INTEGRATION = process.env.ANTHROPIC_INTEGRATION === '1'
const maybeDescribe = RUN_INTEGRATION ? describe : describe.skip

maybeDescribe('lib/anthropic — Anthropic API integration (manual opt-in)', () => {
  it.todo('...')
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update .gitignore to allow committing the golden fixture JSON</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (full file — verify line 22 wholesale ignore)
  </read_first>
  <action>
    Em `.gitignore`, IMEDIATAMENTE APÓS a linha `apps/web/scripts/output/` (linha 22 atual), adicione uma negation explícita para o golden fixture específico (e nada mais):

    ```
    # vision-service ML models (downloaded on-demand by probe scripts)
    vision-service/models/*.task
    vision-service/scripts/output/
    apps/web/scripts/output/
    # Phase 07.1.6 D-06: commit only the golden fixture; outros outputs continuam ignored.
    !apps/web/scripts/output/landmarks-probe-sonnet-4-6.json
    ```

    NÃO negue o diretório inteiro (continuamos ignorando crops + outros JSON outputs). NÃO adicione negation pra outros golden fixtures (Wave C corpus expansion vai handle múltiplos).
  </action>
  <verify>
    <automated>grep -q "^!apps/web/scripts/output/landmarks-probe-sonnet-4-6.json$" .gitignore && grep -q "^apps/web/scripts/output/$" .gitignore && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - .gitignore contains literal line `apps/web/scripts/output/` (wholesale ignore preserved)
    - .gitignore contains literal line `!apps/web/scripts/output/landmarks-probe-sonnet-4-6.json` (negation)
    - The negation line APPEARS AFTER the wholesale ignore (order matters for .gitignore)
    - Other paths in scripts/output/ continue ignored: `git check-ignore apps/web/scripts/output/landmarks-probe-sonnet-4-6-pre-protocol.json` returns exit 0 (still ignored)
    - Golden fixture is now trackable: `git check-ignore apps/web/scripts/output/landmarks-probe-sonnet-4-6.json` returns exit 1 (not ignored)
  </acceptance_criteria>
  <done>
    Founder pode `git add apps/web/scripts/output/landmarks-probe-sonnet-4-6.json` sem `--force`. Outros JSON outputs (`-pre-protocol.json`, `haiku-landmarks-probe.json`) e crops continuam corretamente ignorados.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update probe-haiku-iris-landmarks.mjs header + golden default</name>
  <files>apps/web/scripts/probe-haiku-iris-landmarks.mjs</files>
  <read_first>
    - apps/web/scripts/probe-haiku-iris-landmarks.mjs (full file — current state)
    - .planning/phases/07.1.6-canonical-capture-pipeline/07.1.6-CONTEXT.md §D-06 + "Claude's Discretion" → "Probe script — make committable"
  </read_first>
  <action>
    Em `apps/web/scripts/probe-haiku-iris-landmarks.mjs`, atualize APENAS:

    **Mudança 1** — substitua o header comment block (linhas 2-28) com runbook expandido:

    ```javascript
    // scripts/probe-haiku-iris-landmarks.mjs
    //
    // Phase 07.1.6 D-06 — golden fixture probe + regression baseline.
    //
    // Role:
    //   - Re-runs Sonnet 4.6 (default) ou Haiku 4.5 contra um reading_id real
    //     e gera (a) o JSON output em scripts/output/landmarks-probe-<model>.json
    //     e (b) crops visuais em scripts/output/crops-<model>/.
    //   - Golden fixture commitado: scripts/output/landmarks-probe-sonnet-4-6.json
    //     (Nailli e85ea7de, post-protocol commit f885462). Outros outputs continuam
    //     .gitignored (apps/web/scripts/output/ wholesale + negation só pro golden).
    //
    // Usage (from apps/web/):
    //   node --env-file=.env.local scripts/probe-haiku-iris-landmarks.mjs
    //                                       # default model + default reading (NAILLI_E85EA7DE_GOLDEN)
    //
    //   node --env-file=.env.local scripts/probe-haiku-iris-landmarks.mjs <readingId>
    //                                       # explicit readingId override
    //
    //   node --env-file=.env.local scripts/probe-haiku-iris-landmarks.mjs --model=claude-haiku-4-5
    //                                       # different model (cheaper, validates C-04 templating finding)
    //
    // Required env vars (.env.local):
    //   SUPABASE_URL (or NEXT_PUBLIC_SUPABASE_URL)
    //   SUPABASE_SERVICE_ROLE_KEY (eyJ prefix guarded at startup)
    //   ANTHROPIC_API_KEY
    //
    // Output paths (suffixed with model slug to avoid clobber):
    //   scripts/output/landmarks-probe-<modelSlug>.json
    //   scripts/output/crops-<modelSlug>/{eye}_{angle}_crop.jpg
    //
    // Cost expectations (per 6-photo reading):
    //   Sonnet 4.6: ~$0.05  (~1.8k input + ~170 output tokens × 6 fotos)
    //   Haiku 4.5:  ~$0.006 (same shape, lower per-Mtok)
    //
    // Founder workflow (pre-PR validation):
    //   1. Run probe with default Sonnet against Nailli golden reading
    //   2. Open crops side-by-side; eyeball test (íris centrada? pálpebras horizontais?
    //      nada importante cortado?)
    //   3. Compare JSON output to scripts/output/landmarks-probe-sonnet-4-6.json (golden)
    //      using gated integration test: CANONICAL_PROBE=1 pnpm test -- probe-canonical
    //   4. CI NÃO roda este script — custo Sonnet + flakiness VLM tornam unsuitable for CI.
    ```

    **Mudança 2** — renomeie e atualize a constante NAILLI_DEFAULT (linha 35 atual):

    Antes:
    ```javascript
    const NAILLI_DEFAULT = '71a7bf1d-747f-4de8-9129-13b69197c6a4'
    ```

    Depois:
    ```javascript
    // Golden fixture role: Phase 07.1.6 regression baseline.
    // Nailli e85ea7de = reading capturada com protocol revisto (commit f885462).
    // Antiga (71a7bf1d) pre-protocol é mantida como referência historiográfica
    // mas NÃO é o golden — corpus expansion é Phase 07.2 Wave C.
    const NAILLI_E85EA7DE_GOLDEN = 'e85ea7de-0e5f-4f49-a889-f886e4a05073'
    ```

    **Mudança 3** — atualize a inicialização `let readingId = NAILLI_DEFAULT` (linha 86 atual) para usar o novo nome:

    Antes:
    ```javascript
    let readingId = NAILLI_DEFAULT
    ```

    Depois:
    ```javascript
    let readingId = NAILLI_E85EA7DE_GOLDEN
    ```

    NÃO altere `MODEL_DEFAULT` — fica em `claude-haiku-4-5-20251001` para preservar compat com runs históricos (founder explicita `--model=claude-sonnet-4-6` quando quer Sonnet). NÃO mude SYSTEM_PROMPT (byte-exact com sonnet-bbox.ts é o que mantém golden fixture comparable). NÃO remova rotação code (probe script é OK ter rotation logic — só lib/canonicalize/crop.ts é que não pode chamar; probe é tooling-only).

    Para o **Plan 07 acceptance**: rodar `pnpm --filter web exec node --env-file=.env.local scripts/probe-haiku-iris-landmarks.mjs --model=claude-sonnet-4-6` deve continuar funcionando sem erro de variável (NAILLI_E85EA7DE_GOLDEN substitui NAILLI_DEFAULT em todas as 2 referências). Não rode no CI executor — deixar para founder pré-deploy.
  </action>
  <verify>
    <automated>grep -q "NAILLI_E85EA7DE_GOLDEN = 'e85ea7de-0e5f-4f49-a889-f886e4a05073'" apps/web/scripts/probe-haiku-iris-landmarks.mjs && grep -q "Phase 07.1.6 D-06 — golden fixture probe" apps/web/scripts/probe-haiku-iris-landmarks.mjs && grep -q "let readingId = NAILLI_E85EA7DE_GOLDEN" apps/web/scripts/probe-haiku-iris-landmarks.mjs && ! grep -q "NAILLI_DEFAULT" apps/web/scripts/probe-haiku-iris-landmarks.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/scripts/probe-haiku-iris-landmarks.mjs` header contains literal `Phase 07.1.6 D-06 — golden fixture probe`
    - Header documents required env vars: SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, ANTHROPIC_API_KEY
    - Header documents cost expectations: `~$0.05` (Sonnet) and `~$0.006` (Haiku)
    - Constant renamed to `NAILLI_E85EA7DE_GOLDEN = 'e85ea7de-0e5f-4f49-a889-f886e4a05073'`
    - Default `readingId` initialization uses `NAILLI_E85EA7DE_GOLDEN`
    - Old `NAILLI_DEFAULT` string no longer present
    - Script remains syntactically valid: `node --check apps/web/scripts/probe-haiku-iris-landmarks.mjs` exit 0
  </acceptance_criteria>
  <done>
    Script committable; default reading agora é golden fixture; header runbook completo pra founder + new contributors saberem como rodar.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Stage golden fixture for git commit</name>
  <files>apps/web/scripts/output/landmarks-probe-sonnet-4-6.json</files>
  <read_first>
    - .gitignore (just updated Task 1 — verify negation present)
    - apps/web/scripts/output/landmarks-probe-sonnet-4-6.json (verify file exists e contém reading e85ea7de)
  </read_first>
  <action>
    Verifique que `apps/web/scripts/output/landmarks-probe-sonnet-4-6.json` existe (já presente no working dir per discovery) e contém o reading id `e85ea7de`. NÃO modifique o conteúdo — fixture é frozen output (committing as-is).

    Comando shell para confirmar a file será committable após Task 1:
    ```bash
    git check-ignore apps/web/scripts/output/landmarks-probe-sonnet-4-6.json
    # Exit code 1 = not ignored ✓
    ```

    Se file não existe ou não contém `e85ea7de`, abort: founder precisa re-rodar o probe primeiro. Logue erro e retorne sem mudar nada. Esta task é "validation + stage" — NÃO escreve novo JSON; o JSON foi produced manualmente pelo founder em 2026-05-11.

    Para o commit phase (final step de phase execution), o executor add explicitamente:
    ```bash
    git add apps/web/scripts/output/landmarks-probe-sonnet-4-6.json
    ```

    NÃO use `git add -A` ou `git add .` aqui — esse padrão acidentalmente captura outros outputs que ainda devem ficar ignorados (mig 0011 + .pre-protocol JSON, crops, etc.).
  </action>
  <verify>
    <automated>test -f apps/web/scripts/output/landmarks-probe-sonnet-4-6.json && grep -q "e85ea7de" apps/web/scripts/output/landmarks-probe-sonnet-4-6.json && ! git check-ignore apps/web/scripts/output/landmarks-probe-sonnet-4-6.json 2>/dev/null && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/web/scripts/output/landmarks-probe-sonnet-4-6.json` exists
    - Contains literal string `e85ea7de` (the golden reading id)
    - `git check-ignore` returns exit code 1 (not ignored) — confirms Task 1 negation works
    - File NOT modified (frozen golden — content preserved as founder produced 2026-05-11)
  </acceptance_criteria>
  <done>
    Golden fixture is now trackable. Final commit phase (executor's standard end-of-plan commit) inclui this file in the staged set.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Create gated integration test probe-canonical.test.ts</name>
  <files>apps/web/lib/canonicalize/__tests__/probe-canonical.test.ts</files>
  <read_first>
    - apps/web/lib/anthropic/__tests__/integration.test.ts (closest analog: gated describe pattern)
    - apps/web/lib/canonicalize/sonnet-bbox.ts (Plan 03 — fetchIrisBbox is the function under test)
    - apps/web/scripts/output/landmarks-probe-sonnet-4-6.json (golden fixture, just staged)
    - .planning/phases/07.1.6-canonical-capture-pipeline/07.1.6-PATTERNS.md §"lib/canonicalize/__tests__/probe-canonical.test.ts"
    - .planning/phases/07.1.6-canonical-capture-pipeline/07.1.6-CONTEXT.md §D-06 (tolerance ±5% center, ±10% radius)
  </read_first>
  <action>
    Crie `apps/web/lib/canonicalize/__tests__/probe-canonical.test.ts`:

    ```typescript
    /**
     * Phase 07.1.6 D-06 — Gated integration test.
     *
     * Skip-by-default. Opt-in: CANONICAL_PROBE=1 pnpm test -- probe-canonical
     *
     * Mirrors Phase 7 ANTHROPIC_INTEGRATION=1 pattern. Compares live Sonnet 4.6
     * bbox output contra o golden fixture committed em scripts/output/landmarks-probe-sonnet-4-6.json
     * (Nailli e85ea7de, capture protocol commit f885462).
     *
     * Tolerance (CONTEXT.md D-06):
     *   ±5% absolute em center_x_pct/center_y_pct
     *   ±10% absolute em radius_pct
     *
     * Founder roda manualmente antes de PR em mudanças ao Sonnet prompt ou
     * canonicalize endpoint. CI NÃO roda (custo Sonnet $0.05/run + flakiness VLM).
     */
    import { describe, it, expect } from 'vitest'
    import { readFileSync } from 'node:fs'
    import { join } from 'node:path'
    import { createClient } from '@supabase/supabase-js'

    const RUN_PROBE = process.env.CANONICAL_PROBE === '1'
    const maybeDescribe = RUN_PROBE ? describe : describe.skip

    const GOLDEN_READING_ID = 'e85ea7de-0e5f-4f49-a889-f886e4a05073'
    const GOLDEN_FIXTURE_PATH = join(process.cwd(), 'scripts/output/landmarks-probe-sonnet-4-6.json')
    const TOLERANCE_CENTER = 0.05  // ±5% absolute em center_x/y_pct
    const TOLERANCE_RADIUS = 0.10  // ±10% absolute em radius_pct
    const STORAGE_BUCKET = 'iris-captures'

    interface GoldenEntry {
      eye: string
      angle: string
      haiku_response?: {
        iris_bbox?: { center_x_pct: number; center_y_pct: number; radius_pct: number }
        valid?: boolean
      }
    }

    interface GoldenFixture {
      reading_id: string
      results: GoldenEntry[]
    }

    function loadGolden(): GoldenFixture {
      const raw = readFileSync(GOLDEN_FIXTURE_PATH, 'utf8')
      return JSON.parse(raw) as GoldenFixture
    }

    maybeDescribe('lib/canonicalize — Sonnet bbox probe (CANONICAL_PROBE=1)', () => {
      it('golden fixture exists and matches Nailli e85ea7de', () => {
        const golden = loadGolden()
        expect(golden.reading_id).toBe(GOLDEN_READING_ID)
        expect(golden.results.length).toBe(6)
      })

      it('live Sonnet bbox matches golden within tolerance per imagem', async () => {
        // Dynamically import — avoids loading server-only module ao não rodar o gate
        const { fetchIrisBbox } = await import('@/lib/canonicalize/sonnet-bbox')
        const sharp = (await import('sharp')).default

        const supabaseUrl = process.env.SUPABASE_URL ?? process.env.NEXT_PUBLIC_SUPABASE_URL
        const serviceKey = process.env.SUPABASE_SERVICE_ROLE_KEY
        if (!supabaseUrl || !serviceKey) {
          throw new Error('CANONICAL_PROBE requires SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY env vars')
        }
        const supabase = createClient(supabaseUrl, serviceKey, {
          auth: { autoRefreshToken: false, persistSession: false },
        })

        const golden = loadGolden()
        const { data: images, error } = await supabase
          .from('reading_images')
          .select('eye, angle, storage_path')
          .eq('reading_id', GOLDEN_READING_ID)
        if (error || !images || images.length === 0) {
          throw new Error(`Failed to load Nailli e85ea7de images: ${error?.message ?? 'no rows'}`)
        }

        for (const img of images) {
          const goldenEntry = golden.results.find(r => r.eye === img.eye && r.angle === img.angle)
          if (!goldenEntry?.haiku_response?.iris_bbox || goldenEntry.haiku_response.valid === false) {
            continue  // skip fixtures that golden also flagged as invalid (right_frontal pode estar aqui)
          }
          const goldenBbox = goldenEntry.haiku_response.iris_bbox

          const { data: blob, error: dlError } = await supabase.storage.from(STORAGE_BUCKET).download(img.storage_path)
          if (dlError || !blob) throw new Error(`Download failed for ${img.eye}_${img.angle}: ${dlError?.message}`)
          const ab = await blob.arrayBuffer()
          const rawBuf = Buffer.from(ab)
          const baked = await sharp(rawBuf).rotate().toBuffer()

          const { bbox } = await fetchIrisBbox(baked)

          expect(bbox.valid, `${img.eye}_${img.angle} should remain valid vs golden`).toBe(true)
          expect(Math.abs(bbox.center_x_pct - goldenBbox.center_x_pct), `${img.eye}_${img.angle} center_x drift`).toBeLessThanOrEqual(TOLERANCE_CENTER)
          expect(Math.abs(bbox.center_y_pct - goldenBbox.center_y_pct), `${img.eye}_${img.angle} center_y drift`).toBeLessThanOrEqual(TOLERANCE_CENTER)
          expect(Math.abs(bbox.radius_pct - goldenBbox.radius_pct), `${img.eye}_${img.angle} radius drift`).toBeLessThanOrEqual(TOLERANCE_RADIUS)
        }
      }, 90_000)  // 90s timeout — 6 Sonnet calls + Storage downloads
    })
    ```

    NÃO use `it.todo()` — testes devem ser concrete (founder runs e espera GREEN). NÃO faça mock — este é o INTEGRATION test point. NÃO assert sobre right_frontal específico (CONTEXT.md §Specifics aceita que gate D-02 não catches it; if golden flagged invalid, skip). 90s timeout para 6 Sonnet calls sequenciais + downloads (não é Promise.all aqui — sequencial para reduzir rate-limit risk e dar logs mais limpos no probe).
  </action>
  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit && grep -q "CANONICAL_PROBE === '1'" apps/web/lib/canonicalize/__tests__/probe-canonical.test.ts && grep -q "TOLERANCE_CENTER = 0.05" apps/web/lib/canonicalize/__tests__/probe-canonical.test.ts && grep -q "TOLERANCE_RADIUS = 0.10" apps/web/lib/canonicalize/__tests__/probe-canonical.test.ts && grep -q "e85ea7de-0e5f-4f49-a889-f886e4a05073" apps/web/lib/canonicalize/__tests__/probe-canonical.test.ts && pnpm test -- canonicalize/__tests__/probe-canonical.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/web/lib/canonicalize/__tests__/probe-canonical.test.ts` exists
    - Contains `process.env.CANONICAL_PROBE === '1'` env gate
    - Contains `const maybeDescribe = RUN_PROBE ? describe : describe.skip`
    - Contains literal tolerances `TOLERANCE_CENTER = 0.05`, `TOLERANCE_RADIUS = 0.10`
    - Contains literal `GOLDEN_READING_ID = 'e85ea7de-0e5f-4f49-a889-f886e4a05073'`
    - Reads golden fixture via `readFileSync` from `scripts/output/landmarks-probe-sonnet-4-6.json`
    - Dynamically imports `@/lib/canonicalize/sonnet-bbox` (avoids server-only load when skipped)
    - Tests have timeout >= 60s for live Sonnet calls
    - Default `pnpm test -- probe-canonical` (without CANONICAL_PROBE=1) exits 0 (tests SKIPPED, not failed)
    - `pnpm tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    Founder pode rodar `CANONICAL_PROBE=1 pnpm --filter web test -- probe-canonical` pré-PR. Sem env var, vitest skipa silenciosamente. Golden fixture é frozen — drift > tolerance causa test fail.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| founder local dev → live Anthropic API (CANONICAL_PROBE=1 path) | Test consome API key real + chama Sonnet. Same trust boundary que probe script. |
| founder local dev → live Supabase Storage (service-role) | Download Nailli e85ea7de originais; service-role bypass RLS. Same trust boundary que probe script. |
| .gitignore mod | Public-by-default → commit json. Risk: leak PII via golden fixture content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1.6-32 | Information Disclosure | golden fixture JSON committed | mitigate | Conteúdo do golden é Sonnet output (bbox coords + tokens + model name) — NÃO contém pixel data, NÃO contém PII de paciente. Reading id `e85ea7de-0e5f-4f49-a889-f886e4a05073` é UUID anonimizado; therapist_id (founder próprio) e patient name (Nailli) NÃO aparecem no JSON Sonnet response (apenas storage_path strings, que já são gitignored em outros outputs). Pre-commit: founder confirma cat do JSON para verificar. |
| T-07.1.6-33 | Tampering | golden fixture sobrescrito por re-run | mitigate | Probe script outputs com `--model=` suffix; default Sonnet output overrides scripts/output/landmarks-probe-sonnet-4-6.json on re-run. Mitigação: founder commit-time review (`git diff`); test would fail se golden inadvertidamente regrediu. CI não roda probe (cost + flakiness). |
| T-07.1.6-34 | Repudiation | CANONICAL_PROBE=1 manual run trail | accept | Founder runs locally; trail é git history of golden updates + Anthropic billing dashboard. Sem CI = sem automated trail. |
| T-07.1.6-35 | Denial of Service | probe-canonical test infinite loop | mitigate | Vitest timeout configurado (90s); per-image sequential com 6 Sonnet calls = ~30-60s wall-clock; nunca exceeds 90s no caminho feliz. Tier 1 Anthropic 5 RPM Sonnet handles 6 sequenciais (~12s entre cada). |
| T-07.1.6-36 | Spoofing | dynamic import of '@/lib/canonicalize/sonnet-bbox' (server-only) | accept | Dynamic import só rodado quando CANONICAL_PROBE=1 (test runs in node, not edge). server-only module loads fine no test runner env. |
</threat_model>

<verification>
- 4 changes: .gitignore mod + script header mod + golden fixture trackable + test file novo.
- `pnpm tsc --noEmit` passes (apps/web).
- `pnpm test -- canonicalize` passes (sanity.test.ts GREEN; probe-canonical.test.ts SKIPPED por default).
- `pnpm lint` passes.
- Founder local: `CANONICAL_PROBE=1 pnpm --filter web test -- probe-canonical` runs in ~60s, costs ~$0.05, passes all tolerances. Esta ÚLTIMA validação é responsabilidade do founder pré-merge (Plan executor não roda — sem env vars + custo).
</verification>

<success_criteria>
- [ ] .gitignore: linha `apps/web/scripts/output/` mantida + linha `!apps/web/scripts/output/landmarks-probe-sonnet-4-6.json` adicionada DEPOIS
- [ ] apps/web/scripts/probe-haiku-iris-landmarks.mjs: header runbook expandido, NAILLI_E85EA7DE_GOLDEN replaces NAILLI_DEFAULT
- [ ] apps/web/scripts/output/landmarks-probe-sonnet-4-6.json: not gitignored (git check-ignore exit 1)
- [ ] apps/web/lib/canonicalize/__tests__/probe-canonical.test.ts: created, gated, default-skipped
- [ ] pnpm test default exit 0 (probe-canonical skipped, no fail)
- [ ] pnpm tsc --noEmit + pnpm lint exit 0
</success_criteria>

<output>
After completion, create `.planning/phases/07.1.6-canonical-capture-pipeline/07.1.6-07-SUMMARY.md` covering:
- 4 file ops (1 gitignore mod + 1 script mod + 1 stage + 1 new test)
- Decisions implemented: D-06 (golden + gated test pattern), corpus expansion deferred to 07.2 Wave C
- Founder runbook: `CANONICAL_PROBE=1 pnpm --filter web test -- probe-canonical` antes de qualquer PR que toca lib/canonicalize/ ou apps/web/scripts/probe-haiku-iris-landmarks.mjs
- Note: regenerar o golden requer (a) re-run probe contra Nailli e85ea7de, (b) git diff scripts/output/landmarks-probe-sonnet-4-6.json, (c) commit explícito com mensagem `chore(07.1.6): refresh golden fixture (justification: ...)`. NUNCA regenerar sem investigation porque drift > tolerância.
</output>
