---
phase: 07.1.6
plan: 05
type: execute
wave: 2
depends_on: [01, 04]
files_modified:
  - apps/web/app/actions/readings.ts
  - apps/web/app/api/readings/[id]/process/route.ts
autonomous: true
requirements: []
tags: [canonical-capture, finalize-wiring, modal-pipeline-url-resolution]

must_haves:
  truths:
    - "D-01: canonicalize falhar NUNCA propaga ao terapeuta — finalize trata try/catch e segue para Modal trigger; original passes pra Modal pra fotos em fallback (Modal lida mal mas lida)"
    - "D-04: env var CANONICAL_CAPTURE_ENABLED — set 'false' faz finalize não disparar canonicalize, e process/route.ts resolve sempre storage_path (path original)"
    - "Process route resolve canonical_storage_path ?? storage_path por imagem — backward-compat com readings pre-07.1.6 (canonical_storage_path IS NULL)"
    - "Modal contract inalterado — apenas as URLs em ImageUrlEntry mudam (canonical bucket path vs originais)"
    - "Modal redeploy NOT needed (canonical é só URLs diferentes)"
    - "Signed URL TTL 600s (D-T6) para canonical signed URLs (mesmo que originais)"
  artifacts:
    - path: "apps/web/app/actions/readings.ts"
      provides: "finalizeReadingAction extension: chama POST /api/capture/canonicalize via internal fetch ANTES do trigger Modal (try/catch fire-and-forget; D-01)"
      contains: "fetch.*api/capture/canonicalize"
    - path: "apps/web/app/api/readings/[id]/process/route.ts"
      provides: "process/route resolve canonical_storage_path por imagem (canonical_storage_path ?? storage_path); selectquery adicionada para canonical_storage_path"
      contains: "canonical_storage_path"
  key_links:
    - from: "apps/web/app/actions/readings.ts (finalizeReadingAction)"
      to: "apps/web/app/api/capture/canonicalize/route.ts (Plan 04)"
      via: "internal fetch POST com Cookie forwarding"
      pattern: "/api/capture/canonicalize"
    - from: "apps/web/app/api/readings/[id]/process/route.ts"
      to: "reading_images.canonical_storage_path (Plan 01 migration)"
      via: "select canonical_storage_path + resolve ?? storage_path per image"
      pattern: "canonical_storage_path \\?\\? storage_path"
---

<objective>
Wire o canonicalize endpoint (Plan 04) no fluxo de finalize, e estender process/route.ts para resolver `canonical_storage_path ?? storage_path` ao gerar signed URLs para o Modal.

Mudanças mínimas em 2 arquivos existentes:
1. `apps/web/app/actions/readings.ts` finalizeReadingAction — adicionar fetch internal a `/api/capture/canonicalize` ANTES do trigger Modal, dentro de try/catch (D-01: NEVER block finalize for canonicalize failure).
2. `apps/web/app/api/readings/[id]/process/route.ts` — adicionar `canonical_storage_path` ao select de reading_images; ao montar `paths` para `createSignedUrls`, usar `canonical_storage_path ?? storage_path` por imagem.

Purpose: connecting tissue entre Plan 04 endpoint e o Modal pipeline. Backward-compat: readings pre-07.1.6 (canonical_storage_path IS NULL) continuam funcionando porque resolve cai em storage_path.

Output: 2 arquivos modificados, ZERO arquivos novos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1.6-canonical-capture-pipeline/07.1.6-CONTEXT.md
@.planning/phases/07.1.6-canonical-capture-pipeline/07.1.6-PATTERNS.md
@apps/web/app/actions/readings.ts
@apps/web/app/api/readings/[id]/process/route.ts

<interfaces>
<!-- Contract from Plan 04 endpoint (Wave 1) -->

POST /api/capture/canonicalize:
- Request: `{ readingId: string }` JSON body
- Response 200: `{ results: CanonicalizeResult[], metadata: CanonicalMetadata, status_summary: { ok, fallback, disabled } }`
- Response 4xx/5xx: `{ error: string }`

Internal fetch pattern (current finalize code at apps/web/app/actions/readings.ts L127-153):
```typescript
const cookieStore = await cookies()
const cookieHeader = cookieStore.toString()
const baseUrl = process.env.NEXT_PUBLIC_SITE_URL ?? 'http://localhost:3000'

try {
  const res = await fetch(`${baseUrl}/api/...`, {
    method: 'POST',
    headers: { Cookie: cookieHeader, 'Content-Type': 'application/json' },
    body: JSON.stringify({...}),
    cache: 'no-store',
  })
  // ... handle status
} catch (err) {
  // ... log; never propagate to caller
}
```

Process route current select (L70-77):
```typescript
const { data: images, error: imagesError } = await supabase
  .from('reading_images')
  .select('eye, angle, storage_path')
  .eq('reading_id', readingId)
// ...
const paths = images.map((img) => img.storage_path)
```

Hygiene constraint: `apps/web/app/actions/readings.ts` é `'use server'` (line 1). MEMORY feedback_use_server_export_hygiene: `'use server'` files only export async functions. Não adicione novo `export const` ou `export type` — qualquer helper deve ficar inline ou em `readings.schemas.ts`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend finalizeReadingAction with canonical fire-and-forget fetch</name>
  <files>apps/web/app/actions/readings.ts</files>
  <read_first>
    - apps/web/app/actions/readings.ts (full file; specifically lines 113-170 finalizeReadingAction)
    - apps/web/app/api/capture/canonicalize/route.ts (Plan 04 — the endpoint being called)
    - .planning/phases/07.1.6-canonical-capture-pipeline/07.1.6-PATTERNS.md §"app/actions/readings.ts — finalizeReadingAction extension"
    - .planning/phases/07.1.6-canonical-capture-pipeline/07.1.6-CONTEXT.md §D-01 (fallback comportamento)
    - C:\Users\rhelt\.claude\projects\D--Projetos-Iridologista\memory\feedback_use_server_export_hygiene.md (constraint context)
  </read_first>
  <action>
    Em `apps/web/app/actions/readings.ts`, INSIRA um bloco de canonicalize call DENTRO de `finalizeReadingAction`, logo APÓS o `readingIdSchema.safeParse` validation (after current L121-125) e ANTES do bloco `D-T1: dispara trigger via internal fetch` (current L127).

    Snippet exato a inserir (após L125 — entre o safeParse error return e a primeira leitura de cookieStore):

    ```typescript
      // D-T1+07.1.6: canonical capture ANTES do trigger Modal.
      // Fire-and-forget per D-01: canonicalize falhar NUNCA bloqueia finalize.
      // Se a env var CANONICAL_CAPTURE_ENABLED=false, o endpoint canonicalizeReading
      // retorna early all-disabled — chamada barata, sem custo Anthropic.
      const cookieStore = await cookies()
      const cookieHeader = cookieStore.toString()
      const baseUrl = process.env.NEXT_PUBLIC_SITE_URL ?? 'http://localhost:3000'

      try {
        const canonicalRes = await fetch(`${baseUrl}/api/capture/canonicalize`, {
          method: 'POST',
          headers: { Cookie: cookieHeader, 'Content-Type': 'application/json' },
          body: JSON.stringify({ readingId: parsed.data.reading_id }),
          cache: 'no-store',
        })
        if (!canonicalRes.ok) {
          const detail = await canonicalRes.text().catch(() => '')
          // Non-blocking — D-01: original ao Modal por imagem ainda funciona via process/route fallback.
          console.error(
            `[finalize] canonicalize non-200 reading=${parsed.data.reading_id} status=${canonicalRes.status} body=${detail.slice(0, 200)}`,
          )
        }
      } catch (err) {
        // D-01: never block finalize for canonicalize failure. Process route caira em storage_path por imagem.
        const errMessage = err instanceof Error ? err.message : 'unknown'
        console.error(
          `[finalize] canonicalize fetch threw reading=${parsed.data.reading_id}: ${errMessage}`,
        )
      }

    ```

    DEPOIS modifique o bloco existente "D-T1: dispara trigger via internal fetch" (L127-130) — REMOVA a redeclaração de `cookieStore`, `cookieHeader`, `baseUrl` (já foram declarados acima). Cada um deve aparecer EXATAMENTE 1× na função. O bloco existente do Modal trigger fica:

    ```typescript
      // D-T1: dispara trigger via internal fetch, forwarding session cookie
      // para que o auth gate de /api/readings/[id]/process aceite a request.
      const triggerUrl = `${baseUrl}/api/readings/${parsed.data.reading_id}/process`

      let triggerStatus: number | null = null
      try {
        const res = await fetch(triggerUrl, {
          // ... (resto existente inalterado)
    ```

    NÃO adicione novos `export const` ou `export type` ao arquivo (use_server_export_hygiene constraint). NÃO mude o type de retorno de `finalizeReadingAction` (ainda é `Promise<ReadingFormState>`). NÃO faça revalidatePath após canonicalize call (revalidate já existe no final do método; canonical metadata vai aparecer no /admin/calibration via Plan 06 server fetch fresh).
  </action>
  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit && node -e "const s=require('fs').readFileSync('apps/web/app/actions/readings.ts','utf8'); for (const t of ['/api/capture/canonicalize','[finalize] canonicalize','D-01: never block finalize']) { if(!s.includes(t)) { console.error('missing token:', t); process.exit(1); } } const cookieCount=(s.match(/await cookies\\(\\)/g)||[]).length; const baseUrlCount=(s.match(/process\\.env\\.NEXT_PUBLIC_SITE_URL/g)||[]).length; if (cookieCount !== 1) { console.error('cookies() should be called exactly once in finalizeReadingAction (now:'+cookieCount+')'); process.exit(1); } if (baseUrlCount !== 1) { console.error('NEXT_PUBLIC_SITE_URL should be read exactly once (now:'+baseUrlCount+')'); process.exit(1); } console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/actions/readings.ts` contains literal `/api/capture/canonicalize`
    - Contains literal `[finalize] canonicalize` log prefix
    - Contains literal `D-01: never block finalize` comment marker
    - `await cookies()` appears exactly ONCE in finalizeReadingAction (no duplicate declaration)
    - `process.env.NEXT_PUBLIC_SITE_URL` appears exactly ONCE in finalizeReadingAction
    - Canonicalize call wraps in try/catch that NEVER throws to caller
    - Order preserved: canonicalize fetch happens BEFORE the Modal trigger fetch (canonical fetch's `try { fetch('/api/capture/canonicalize'...)` comes textually before `triggerUrl = ${baseUrl}/api/readings/...`)
    - Function still returns `Promise<ReadingFormState>` (no new export types)
    - `pnpm tsc --noEmit` exit 0
    - `pnpm lint` exit 0
  </acceptance_criteria>
  <done>
    finalizeReadingAction agora chama `/api/capture/canonicalize` antes do `/api/readings/[id]/process`. Em fallback (canonicalize 500 ou network throw), apenas console.error é emitido — terapeuta continua redirecionado pra /leituras, e Modal recebe URL original via process/route.ts Task 2 resolution.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: process/route.ts — resolve canonical_storage_path per image</name>
  <files>apps/web/app/api/readings/[id]/process/route.ts</files>
  <read_first>
    - apps/web/app/api/readings/[id]/process/route.ts (full file; specifically L70-102 — select + signed URL generation)
    - .planning/phases/07.1.6-canonical-capture-pipeline/07.1.6-PATTERNS.md §"app/api/readings/[id]/process/route.ts — canonical_storage_path resolution"
    - .planning/phases/07.1.6-canonical-capture-pipeline/07.1.6-CONTEXT.md §"Code Insights" → "Modal contract"
  </read_first>
  <action>
    Em `apps/web/app/api/readings/[id]/process/route.ts`:

    **Mudança 1** (L70-77 — extend select):

    Antes:
    ```typescript
      const { data: images, error: imagesError } = await supabase
        .from('reading_images')
        .select('eye, angle, storage_path')
        .eq('reading_id', readingId)
    ```

    Depois:
    ```typescript
      // Phase 07.1.6: select canonical_storage_path; resolve per-image abaixo (D-01 fallback).
      const { data: images, error: imagesError } = await supabase
        .from('reading_images')
        .select('eye, angle, storage_path, canonical_storage_path')
        .eq('reading_id', readingId)
    ```

    **Mudança 2** (L81-82 — resolve canonical preferentially):

    Antes:
    ```typescript
      const serviceClient = createServiceClient()
      const paths = images.map((img) => img.storage_path)
    ```

    Depois:
    ```typescript
      const serviceClient = createServiceClient()
      // Phase 07.1.6 D-01: canonical_storage_path se non-NULL, senão storage_path original.
      // Backward-compat: readings pre-07.1.6 têm canonical_storage_path IS NULL → storage_path.
      // D-04 disabled: same resolution; canonical_storage_path IS NULL → storage_path.
      const paths = images.map((img) => img.canonical_storage_path ?? img.storage_path)
    ```

    NÃO mude o shape de `ImageUrlEntry` (eye/angle/url) — Modal pipeline contract permanece. NÃO mude ordering das images. NÃO modifique pre-spawn UPDATE, callId handling, ou error rollback. Apenas adicione o `canonical_storage_path` ao select + nullish-coalesce na construção de `paths`.
  </action>
  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit && node -e "const s=require('fs').readFileSync('apps/web/app/api/readings/[id]/process/route.ts','utf8'); for (const t of ['canonical_storage_path','img.canonical_storage_path ?? img.storage_path','Phase 07.1.6']) { if(!s.includes(t)) { console.error('missing token:', t); process.exit(1); } } const selectMatch = s.match(/\\.select\\([^)]*canonical_storage_path[^)]*\\)/); if (!selectMatch) { console.error('select must include canonical_storage_path'); process.exit(1); } console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/api/readings/[id]/process/route.ts` contains literal `canonical_storage_path` in the `.select(...)` call
    - Contains literal expression `img.canonical_storage_path ?? img.storage_path` (nullish coalesce)
    - Contains comment marker `Phase 07.1.6` near the changed lines
    - Modal contract unchanged: `ImageUrlEntry` shape (eye/angle/url) preserved
    - Pre-spawn UPDATE, callId handling, error rollback untouched
    - `pnpm tsc --noEmit` exit 0
    - `pnpm lint` exit 0
    - Existing route tests (if any) still pass: `pnpm test -- process/route` exit 0 (file may not have tests; OK if N/A)
  </acceptance_criteria>
  <done>
    Modal pipeline recebe canonical URLs para imagens onde `canonical_storage_path` está populado, e original URLs onde está NULL (fallback ou pre-07.1.6). Modal Python pipeline inalterado.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| finalizeReadingAction → POST /api/capture/canonicalize | Internal fetch com Cookie forwarding — same trust boundary que o Modal trigger existente. |
| process/route.ts → reading_images.canonical_storage_path read | Internal Supabase query (user client, RLS-enforced). |
| process/route.ts → Storage signed URL para canonical path | Service-role gera signed URL TTL 600s. canonical path follows RLS folder convention (therapist_id em segment 0). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1.6-23 | Tampering | canonical_storage_path injection via Supabase row | mitigate | canonical_storage_path é write-only via service-role em canonicalizeReading (Plan 03); user-client UPDATE bloqueado por RLS implicit (reading_images RLS herdada). Nullish coalesce produz path com `{user.id}` prefix — RLS folder gate (mig 0004) garante user só lê próprias canonical URLs. |
| T-07.1.6-24 | Information Disclosure | signed URL para canonical containing other therapist's data | mitigate | canonical path always begins with `{therapistId}/...`; service-role createSignedUrls não bypasses path validation (Supabase Storage returns 404 se path malformed). Ownership-gated upstream em process/route (L54-62). |
| T-07.1.6-25 | Denial of Service | canonicalize endpoint loop (finalize triggers, canonicalize 500s, retry?) | mitigate | finalize chama canonicalize **exactly once** (sem retry loop). Failure → console.error, segue para Modal trigger. Não há recursive fetch. |
| T-07.1.6-26 | Repudiation | finalize fire-and-forget = silent canonical failure | accept | console.error log captura status + body slice; canonical_metadata.canonicalized_at registra success em DB. Founder pode auditar via /admin/calibration (Plan 06). |
</threat_model>

<verification>
- `pnpm tsc --noEmit` passes (workspace apps/web).
- `pnpm lint` passes (MEMORY feedback_run_lint_before_push — eslint pre-push gate).
- Existing test suite passes: `pnpm test` (no new tests added; modificações em routes não breakam upload.test.ts, etc.).
- Smoke test (founder pré-deploy): nova reading com 6 fotos finalizar redireciona pra /leituras sem erro; logs server-side mostram tanto `[finalize] canonicalize` (success ou non-200 fallback) E `[process/route]` Modal trigger.
</verification>

<success_criteria>
- [ ] apps/web/app/actions/readings.ts contains canonicalize fire-and-forget block before Modal trigger
- [ ] cookies() + NEXT_PUBLIC_SITE_URL declared exactly 1× cada em finalizeReadingAction
- [ ] apps/web/app/api/readings/[id]/process/route.ts select inclui canonical_storage_path
- [ ] paths array usa nullish coalesce img.canonical_storage_path ?? img.storage_path
- [ ] pnpm tsc --noEmit exit 0
- [ ] pnpm lint exit 0
- [ ] readings.ts retains 'use server' as line 1; no new const/type exports added
</success_criteria>

<output>
After completion, create `.planning/phases/07.1.6-canonical-capture-pipeline/07.1.6-05-SUMMARY.md` covering:
- 2 arquivos modificados (LOC delta)
- Decisions implemented: D-01 (fire-and-forget never blocks finalize), D-04 (env-flag honored by orchestrator → endpoint), backward-compat (canonical_storage_path NULL → storage_path)
- Sequence diagram: finalize → POST /canonicalize (Plan 04) → orchestrator (Plan 03) → DB upsert → finalize continues → POST /process → resolve per-image → signed URLs to Modal
- Founder smoke-test note: capturar nova reading, verificar logs `[finalize] canonicalize` apparece + canonical URL aparece em Modal request (Modal dashboard logs).
</output>
