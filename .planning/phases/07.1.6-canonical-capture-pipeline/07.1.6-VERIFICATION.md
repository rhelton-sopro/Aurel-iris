---
phase: 07.1.6-canonical-capture-pipeline
verified: 2026-05-12
status: human_needed
must_haves_total: 8
must_haves_passed: 5
must_haves_human: 3
must_haves_gap: 0
decisions_honored: 14
decisions_violated: 0
human_verification_items: 4
verifier: inline-orchestrator (gsd-verifier hit usage limit at 11:17 BRT)
---

# Phase 07.1.6 Canonical Capture Pipeline — Verification

**Verdict:** `human_needed` — 5/8 success criteria mechanically verified ✓; 3/8 require live founder dogfooding (criteria 5, 6 behavioral, 8 ≥3 reading sample). Zero gaps, zero violated decisions, zero new test regressions.

## Goal Coverage (8 success criteria from ROADMAP)

| # | Criterion | Status | Evidence |
|---|-----------|--------|----------|
| 1 | Schema additivo aplicado (reading_images.canonical_storage_path + readings.canonical_metadata) | ✅ PASS | Migration `supabase/migrations/0012_canonical_capture.sql` committed `cb03ced`. `supabase db push` applied to live DB by founder 2026-05-12 (verified: 2 rows em information_schema, ambas `is_nullable=YES`). |
| 2 | Pipeline canonical funcional: Sonnet bbox → trust gate → sharp crop 800×800 SEM rotation → upload → upsert | ✅ PASS | 4 files commit `198978b`/`b28fe83`/`d1f7385`/`2a172e3`: `storage-path.ts` 47L, `sonnet-bbox.ts` 182L (`fetchIrisBbox` calls `anthropicClient.messages.create` with `SONNET_BBOX_MODEL`), `crop.ts` 97L (sharp lanczos3, SEM rotate angle), `index.ts` 336L (orchestrator: `Promise.all` × 6 → `isCanonicalAccepted` → upsert `onConflict: reading_id,eye,angle`). 43/43 unit tests pass. |
| 3 | Trust gate (cross-angle + geometric) pure function em sanity.ts com Wave 0 RED unit tests verde | ✅ PASS | `sanity.ts` exports `isGeometricallySane` (L47), `isCrossAngleOutlier` (L68), `isCanonicalAccepted` (L89, combines both — L94-95). RED→GREEN cycle `f1e183b`→`3479d11`. 17/17 vitest GREEN. Thresholds match D-02: center [0.20, 0.80], radius [0.05, 0.30], cross-angle \|c-median\| > 0.08. |
| 4 | Modal redeploy NÃO necessário (canonical é só URLs diferentes) | ✅ PASS | `apps/web/app/api/readings/[id]/process/route.ts:74,85` — select adicionado `canonical_storage_path`, paths.map resolve `img.canonical_storage_path ?? img.storage_path`. Modal contract inalterado: payload shape preserved, apenas string URLs flipam. Commit `d417d8a`. |
| 5 | Nailli e85ea7de reprocessada produz processing_metadata.detect_diagnostics com segment centers ≤50 px | ⏸ HUMAN | **Requires live dogfooding.** Founder roda reading via UI (or `RecanonicalizeButton` em /admin/calibration), depois inspeciona vision_features.processing_metadata.detect_diagnostics no DB e confirma cross-angle center spread ≤ 50 px LEFT + RIGHT. Verifier não pode automatizar (custo Sonnet + Modal trigger). |
| 6 | /admin/calibration mostra canonical_status badge + Re-canonicalizar idempotente | ✅ CODE / ⏸ HUMAN | Code shipped: `RecanonicalizeButton.tsx` 129L (`a3b2e58`), `page.tsx` badge derivada de `canonical_metadata.status_summary` (`e1528b4`), photos API `canonical_url` field (`4e69413`). Visual rendering correctness, badge accuracy, idempotent re-click behavior — require live founder smoke-test em /admin/calibration. |
| 7 | Regression suite: probe script + golden fixture + CANONICAL_PROBE=1 gated test | ✅ PASS | `.gitignore` negação 3-line pattern (Rule 3 fix vs plan literal), `probe-haiku-iris-landmarks.mjs` agora tracked + header runbook D-06 expanded (`01fcb90`), golden fixture `landmarks-probe-sonnet-4-6.json` 430L committed (`1836743`), `probe-canonical.test.ts` 173L gated por `CANONICAL_PROBE === '1'` (`93b6d38`). Default suite vê 2/2 SKIPPED — não dispara Sonnet em CI. |
| 8 | Founder dogfooding ≥3 leituras pós-deploy com canonical ON; ≥80% canonical_status='ok' em readings novas | ⏸ HUMAN | **Requires production dogfooding sample.** Founder run ≥3 readings com `CANONICAL_CAPTURE_ENABLED` unset (default ON), abre cada em /admin/calibration, verifica badge mostra ≥80% das 18 fotos (3×6) com status `ok`. |

**Mechanical PASS:** 5/8 (criteria 1, 2, 3, 4, 7).
**HUMAN-NEEDED:** 3/8 (criteria 5, 6, 8 — all behavioral, require live readings).

## Locked Decisions Honored (14/14 ✓)

| Decision | Status | Evidence |
|----------|--------|----------|
| C-01 canonical via Sonnet bbox (NÃO U-Net) | ✅ | Zero references to U-Net, MediaPipe, segmentation, mask in canonicalize/. Sonnet bbox via `fetchIrisBbox`. |
| C-02 SEM rotation correction | ✅ | `crop.ts` zero `.rotate(<angle>)` calls. `.rotate()` no-arg para baked-EXIF em `index.ts:172,174` é orientation-only (EXIF read), NÃO rotation correction — semantically `sharp.rotate()` sem args lê EXIF e roda canvas pra orientação correta antes do crop. C-02 honra prohibition on angle-based rotation. |
| C-03 crop floor `max(2.5×r, 0.4×min(W,H))` | ✅ | `crop.ts:36-37` `CROP_MULTIPLIER=2.5` + `CROP_FLOOR_FRACTION=0.4`. L77 literal formula encoded. |
| C-04 SONNET_BBOX_MODEL hardcoded literal type | ✅ | `client.ts:43` `export const SONNET_BBOX_MODEL = 'claude-sonnet-4-6' as const`. NÃO env-overridable. `sonnet-bbox.ts:121` usa SONNET_BBOX_MODEL (não hardcoded string, não env-overridable `MODEL`). |
| C-05 Sonnet bbox call independente da Haiku validate | ✅ | `sonnet-bbox.ts:27,121` `anthropicClient.messages.create` direto — não delega para validate route. 2 calls Anthropic (validate + bbox) mantidas. |
| C-06/C-07 pre-shipped | ✅ | Phase 7.1.5 lineage; out of 07.1.6 scope per CONTEXT.md. |
| C-08 originais nunca sobrescritos | ✅ | `index.ts` upsert é em `reading_images.canonical_storage_path` (additive column); `storage_path` original nunca touched. Bucket path `iris-captures` é additive em `{therapist}/{reading}/canonical/...`. |
| D-01 gate fail → fallback (no 500) | ✅ | `sanity.ts:94-95` retorna `'fallback'` (não throw); `index.ts:216` pre-gate failures marcam fallback sem retry; `route.ts` 500 só pra catastrophic infrastructure (DB/Anthropic auth down), gate failures retornam 200 com results indicando per-image fallback. |
| D-02 trust gate combina geometric + cross-angle | ✅ | `isCanonicalAccepted` (sanity.ts:89-95) calls `isGeometricallySane` AND `isCrossAngleOutlier`. Plan 02 GREEN suite tests both. |
| D-03 sem retry | ✅ | Grep "retry" em canonicalize/ retorna apenas comentários documentando D-03 (`sanity.ts:11,82`, `index.ts:216`). Zero retry logic. |
| D-04 CANONICAL_CAPTURE_ENABLED kill-switch | ✅ | `index.ts:104` `process.env.CANONICAL_CAPTURE_ENABLED !== 'false'` default ON. `'false'` literal → early return all-disabled. |
| D-05 endpoint idempotente | ✅ | `index.ts:265` `onConflict 'reading_id,eye,angle'` em upsert — re-canonicalize regenera canonical crop, mantém originais inalterados. |
| D-06 golden fixture + CANONICAL_PROBE=1 gated test | ✅ | Both committed (`1836743` + `93b6d38`). `probe-canonical.test.ts:33` `RUN_PROBE = process.env.CANONICAL_PROBE === '1'`, `describe.skipIf(!RUN_PROBE)`. Default 2/2 SKIPPED. |

Plus the implicit decisions from CONTEXT.md:
- D-T6 signed URL TTL 600s — preserved em photos API e finalize wiring.
- Backward compat pre-07.1.6 readings (canonical_storage_path NULL) — `??` operator no process/route.ts:85 honra.

## Cross-File Integration (5/5 ✓)

| From → To | Pattern | Status |
|-----------|---------|--------|
| Plan 03 → Plan 04 | `route.ts` imports `canonicalizeReading` from `@/lib/canonicalize` | ✅ |
| Plan 04 → Plan 05 | `actions/readings.ts:136` fetches `/api/capture/canonicalize` | ✅ |
| Plan 04 → Plan 06 | `RecanonicalizeButton.tsx:60` fetches `/api/capture/canonicalize` | ✅ |
| Plan 01 → Plan 02+03 | `SONNET_BBOX_MODEL` imported em sonnet-bbox.ts:27; `IrisBbox` imported em sanity.ts e canonicalize/index.ts | ✅ |
| Plan 02 → Plan 03 | `isCanonicalAccepted` called em canonicalize/index.ts orchestrator | ✅ |

## Test Posture

**Pre-existing failures (NOT new regressions):**
- `lib/capture/quality-scoring.test.ts` — 3 failures (`feedback_quality_scoring_test_gate.md` MEMORY entry). Phase 3 debt, documented across all 7 SUMMARYs as out-of-scope.

**07.1.6 test additions:**
- `lib/canonicalize/__tests__/sanity.test.ts` — 17 tests (Plan 02)
- `lib/canonicalize/__tests__/storage-path.test.ts` — 13 tests (Plan 03)
- `lib/canonicalize/__tests__/sonnet-bbox.test.ts` — 8 tests, mocked anthropicClient (Plan 03)
- `lib/canonicalize/__tests__/crop.test.ts` — 5 tests (Plan 03)
- `app/api/admin/calibration/photos/[reading_id]/route.test.ts` — atualizado com new canonical_url test (Plan 06)
- `lib/canonicalize/__tests__/probe-canonical.test.ts` — 2 gated tests (skipped por default, Plan 07)

**Final baseline (full suite):**
- 499 passed / 3 failed (pre-existing Phase 3) / 24 todo / 2 skipped + 2 from probe-canonical default-skipped
- Zero new failures vs starting Phase 07.1.6 baseline
- `tsc --noEmit` apps/web: zero new errors em arquivos do phase
- `pnpm lint apps/web`: 0 errors / 9 baseline warnings (zero new)

## Architectural Spot-Checks (informational, non-blocking)

**`apps/web/lib/canonicalize/index.ts` orchestrator:**
- Service-role Supabase client correto pra cross-RLS reads
- `Promise.all` × 6 imagens — sequential Sonnet calls eliminadas, paralelas
- Per-eye grouping (LEFT vs RIGHT) antes do gate — D-02 honra
- EXIF baking (`sharp(raw).rotate().toBuffer()`) antes do crop — coords pós-EXIF, evita misalignment
- aggregate canonical_metadata UPDATE atomic via service-role

**`apps/web/app/api/capture/canonicalize/route.ts` endpoint:**
- runtime='nodejs' explicit (sharp + Anthropic SDK precisam Node, não Edge) ✓
- Stage flow: body parse (400) → auth getUser (401) → ownership single (404) → delegate (200/500) ✓
- Ownership query usa `.eq('id', readingId).eq('therapist_id', user.id).single()` — RLS-safe, MEMORY rule honored (não queries `auth.users`) ✓
- 500 catch-all está OK pra D-01 porque routine gate failures retornam 200 com fallback results; só catastrophic infra dispara 500

**`apps/web/lib/canonicalize/sanity.ts`:**
- Pure functions, zero I/O, zero env, zero `import 'server-only'` — testable em Node sem mocks
- Threshold constants exportados para reuso em sonnet-bbox.ts (não duplicação)

## Human Verification Items

These items require the founder to physically run the application post-merge. They are saved to `07.1.6-HUMAN-UAT.md` after this VERIFICATION.md commits.

### 1. Live canonical pipeline smoke test
**Expected:** Run a NEW reading (or use existing Nailli e85ea7de via Re-canonicalizar). After capture/finalize, in DB inspect `readings.canonical_metadata` — should have `sonnet_input_tokens > 0`, `sonnet_output_tokens > 0`, `cost_usd ≈ 0.05`, `status_summary.ok` + `status_summary.fallback` summing to 6, `canonicalized_at` set.

### 2. Modal pipeline uses canonical URLs
**Expected:** For the reading from item 1, inspect `vision_features.processing_metadata.detect_diagnostics`. Cross-angle center spread (max distance between centers across the 3 angles of same eye) should be ≤ 50 px for LEFT and ≤ 50 px for RIGHT. This validates that canonical crop standardized the iris position.

### 3. /admin/calibration UI smoke
**Expected:** Navigate to /admin/calibration/{readingId}. Confirm:
- Canonical badge renders next to existing reading metadata
- Badge text matches the actual canonical_metadata.status_summary
- "Re-canonicalizar" button is present
- Clicking it triggers POST /api/capture/canonicalize, shows loading state, then toast "X imagens canonicalizadas (Y fallback)"
- Page refresh after toast shows updated badge

### 4. Dogfooding sample ≥3 readings
**Expected:** Founder captures ≥3 new readings over the next session. Aggregate canonical_status across all 18 photos (3 readings × 6 photos) — at least 80% should be `ok` (≤4 fallbacks across 18). If >4 fallbacks, the gate is too tight OR the capture protocol needs more refinement — file as a follow-up item.

## Verdict

**Status: `human_needed`** — phase code-complete, mechanically verified, awaiting founder dogfooding (3 behavioral criteria).

All 7 plans landed across 26 atomic commits. Zero locked decisions violated. Zero new test regressions. Schema applied to live DB. Pipeline plumbing wired end-to-end (finalize → endpoint → orchestrator → Sonnet → gate → crop → upload → upsert; admin badge + button; gated regression test). Approval path: founder responds "approved" after dogfooding items 1–4 above, or describes specific issues → gap closure plan.
