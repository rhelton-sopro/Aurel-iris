---
phase: 07.1.6
plan: 02
type: tdd
wave: 0
depends_on: []
files_modified:
  - apps/web/lib/canonicalize/sanity.ts
  - apps/web/lib/canonicalize/__tests__/sanity.test.ts
autonomous: true
requirements: []
tags: [canonical-capture, sanity-gate, tdd, pure-function]

must_haves:
  truths:
    - "D-02: trust gate combina geometric sanity + cross-angle outlier detection — pure function, pre-Modal, server-side"
    - "D-02: geometric sanity = center coord in [0.20, 0.80] E radius_pct in [0.05, 0.30]"
    - "D-02: cross-angle outlier = |center_x_pct - median_x| > 0.08 OR |center_y_pct - median_y| > 0.08"
    - "D-03: sem retry — gate falhou → fallback direto (sem rephrasing, sem cascade)"
    - "Thresholds são empíricos: derivados de Nailli e85ea7de probe + founder visual review (CONTEXT.md §Specifics)"
  artifacts:
    - path: "apps/web/lib/canonicalize/sanity.ts"
      provides: "Pure functions: isGeometricallySane, isCrossAngleOutlier, isCanonicalAccepted, median (private). Threshold constants exported as CROSS_ANGLE_OUTLIER_THRESHOLD etc para reuse em sonnet-bbox.ts."
      exports: ["isGeometricallySane", "isCrossAngleOutlier", "isCanonicalAccepted"]
      min_lines: 60
    - path: "apps/web/lib/canonicalize/__tests__/sanity.test.ts"
      provides: "RED→GREEN unit tests covering 3 functions + Nailli e85ea7de empirical fixture"
      min_lines: 80
  key_links:
    - from: "apps/web/lib/canonicalize/sanity.ts"
      to: "apps/web/lib/anthropic/types.ts IrisBbox"
      via: "import type { IrisBbox, CanonicalStatus } from '@/lib/anthropic/types'"
      pattern: "import type \\{ IrisBbox"
---

<objective>
TDD RED→GREEN scaffolding pra o trust gate puro da Phase 07.1.6: 3 funções pure (sem side effects, sem 'server-only' — math only) em `apps/web/lib/canonicalize/sanity.ts` + unit tests inline com fixtures Nailli `e85ea7de` (CONTEXT.md §Specifics). Estes são os blocks foundacionais que `index.ts` (Wave 1) chama por imagem após receber bbox do Sonnet.

Purpose: travar o gate empírico D-02 como pure function ANTES de qualquer Sonnet call. Thresholds (0.20/0.80 center, 0.05/0.30 radius, 0.08 cross-angle) são hardcoded constants no módulo — não env-overridable (CONTEXT.md §Specifics empirical derivation).

Output: 2 arquivos novos sob `apps/web/lib/canonicalize/`. Zero deps externas (sem Anthropic, sem Supabase). Tests rodam em <1s.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07.1.6-canonical-capture-pipeline/07.1.6-CONTEXT.md
@.planning/phases/07.1.6-canonical-capture-pipeline/07.1.6-PATTERNS.md

<interfaces>
<!-- Types this module consumes — defined Wave 0 Plan 01 (no runtime dep, just type imports). -->

From apps/web/lib/anthropic/types.ts (created Plan 01):
```typescript
export interface IrisBbox {
  center_x_pct: number      // 0.0-1.0 fraction of image width
  center_y_pct: number      // 0.0-1.0 fraction of image height
  radius_pct: number        // fraction of min(W,H)
  confidence: number        // 0.0-1.0 Sonnet self-reported
  valid: boolean
}

export type CanonicalStatus = 'ok' | 'fallback' | 'disabled'
```

Test framework: `vitest` (existing — see apps/web/lib/capture/upload.test.ts pattern).

Empirical fixture values (from CONTEXT.md §Specifics — Nailli e85ea7de probe):
- LEFT eye median: center_x_pct ≈ 0.47, center_y_pct ≈ 0.41, radius_pct ≈ 0.13
- RIGHT eye median: center_x_pct ≈ 0.46, center_y_pct ≈ 0.44, radius_pct ≈ 0.14
- Expected right_frontal fail mode: bbox numerically dentro do range geométrico mas visualmente errada — gate D-02 talvez NÃO catch (founder usa /admin Re-canonicalizar como escape hatch — accepted limitation per CONTEXT.md §Specifics).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write failing unit tests for sanity gate</name>
  <files>apps/web/lib/canonicalize/__tests__/sanity.test.ts</files>
  <read_first>
    - apps/web/lib/capture/upload.test.ts (closest analog: vitest pure unit test structure, describe/it/expect with inline fixtures)
    - .planning/phases/07.1.6-canonical-capture-pipeline/07.1.6-PATTERNS.md §"lib/canonicalize/__tests__/sanity.test.ts"
    - .planning/phases/07.1.6-canonical-capture-pipeline/07.1.6-CONTEXT.md §Specifics → "Trust gate — threshold values empíricos" + "Trust gate — failure mode atual (right_frontal)"
  </read_first>
  <behavior>
    - isGeometricallySane: accepts bbox dentro de [0.20,0.80]×[0.20,0.80] + radius [0.05,0.30]
    - isGeometricallySane: rejects center_x=0.19 (out of [0.20,0.80])
    - isGeometricallySane: rejects center_y=0.81 (out of [0.20,0.80])
    - isGeometricallySane: rejects radius_pct=0.04 (below 0.05)
    - isGeometricallySane: rejects radius_pct=0.31 (above 0.30)
    - isGeometricallySane: accepts boundary values 0.20, 0.80, 0.05, 0.30 (inclusive)
    - isCrossAngleOutlier: returns false when peers.length < 2 (cannot compute median reliably)
    - isCrossAngleOutlier: returns true when |center_x_pct - median(peers.center_x)| > 0.08
    - isCrossAngleOutlier: returns true when |center_y_pct - median(peers.center_y)| > 0.08
    - isCrossAngleOutlier: returns false at boundary delta = 0.08 (strict >)
    - isCanonicalAccepted: returns 'ok' when sane AND not outlier
    - isCanonicalAccepted: returns 'fallback' when geometrically insane
    - isCanonicalAccepted: returns 'fallback' when cross-angle outlier (even if sane)
    - isCanonicalAccepted: returns 'fallback' when bbox.valid === false (Sonnet self-flagged unreadable)
  </behavior>
  <action>
    Crie `apps/web/lib/canonicalize/__tests__/sanity.test.ts` com toda a structure de testes. Use fixtures inline (sem vi.mock — funções são pure). Imports:

    ```typescript
    import { describe, it, expect } from 'vitest'
    import {
      isGeometricallySane,
      isCrossAngleOutlier,
      isCanonicalAccepted,
    } from '../sanity'
    import type { IrisBbox } from '@/lib/anthropic/types'
    ```

    Fixtures literais a usar (não invente outros valores — estes são empíricos):
    ```typescript
    const validNailliLeftFrontal: IrisBbox = { center_x_pct: 0.47, center_y_pct: 0.41, radius_pct: 0.13, confidence: 0.85, valid: true }
    const validNailliLeftLateral: IrisBbox = { center_x_pct: 0.46, center_y_pct: 0.42, radius_pct: 0.13, confidence: 0.82, valid: true }
    const validNailliLeftBacklight: IrisBbox = { center_x_pct: 0.48, center_y_pct: 0.40, radius_pct: 0.14, confidence: 0.80, valid: true }
    const edgeCenterXLow: IrisBbox = { center_x_pct: 0.19, center_y_pct: 0.50, radius_pct: 0.13, confidence: 0.75, valid: true }
    const edgeCenterYHigh: IrisBbox = { center_x_pct: 0.50, center_y_pct: 0.81, radius_pct: 0.13, confidence: 0.75, valid: true }
    const edgeRadiusLow: IrisBbox = { center_x_pct: 0.50, center_y_pct: 0.50, radius_pct: 0.04, confidence: 0.75, valid: true }
    const edgeRadiusHigh: IrisBbox = { center_x_pct: 0.50, center_y_pct: 0.50, radius_pct: 0.31, confidence: 0.75, valid: true }
    const boundaryCenter: IrisBbox = { center_x_pct: 0.20, center_y_pct: 0.80, radius_pct: 0.05, confidence: 0.75, valid: true }
    const invalidBbox: IrisBbox = { center_x_pct: 0.5, center_y_pct: 0.5, radius_pct: 0, confidence: 0, valid: false }
    // Outlier x test: median peers.x = 0.46, bbox.center_x = 0.46+0.09 = 0.55 → delta 0.09 > 0.08
    const crossAngleOutlierX: IrisBbox = { center_x_pct: 0.55, center_y_pct: 0.41, radius_pct: 0.13, confidence: 0.78, valid: true }
    // Boundary delta exactly 0.08 — NOT outlier (strict >)
    const boundaryOutlierX: IrisBbox = { center_x_pct: 0.54, center_y_pct: 0.41, radius_pct: 0.13, confidence: 0.78, valid: true }
    ```

    Os 3 `describe()` blocks com TODOS os 14 `it()` cases listados em `<behavior>` acima — escreva cada um com `expect(...).toBe(...)` ou `expect(...).toEqual(...)` concreto, NÃO use `it.todo()`. Pelo menos um cross-angle test deve usar `[validNailliLeftLateral, validNailliLeftBacklight]` como peers e verificar que `boundaryOutlierX` retorna `false` (boundary inclusive não é outlier).

    Estes testes vão FALHAR neste ponto (módulo sanity.ts ainda não existe). Isto é RED da fase TDD.
  </action>
  <verify>
    <automated>cd apps/web && pnpm test -- canonicalize/__tests__/sanity.test.ts 2>&1 | grep -E "(failed|FAIL|Error|Cannot find module)" | head -5 || echo "Expected RED phase: test file should exist and import should fail"</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/web/lib/canonicalize/__tests__/sanity.test.ts` exists
    - Contains 3 `describe()` blocks: `isGeometricallySane`, `isCrossAngleOutlier`, `isCanonicalAccepted`
    - Contains ≥14 `it(` test cases (NOT `it.todo`)
    - Imports `import { isGeometricallySane, isCrossAngleOutlier, isCanonicalAccepted } from '../sanity'`
    - Imports `import type { IrisBbox } from '@/lib/anthropic/types'`
    - Test run currently FAILS due to missing module `../sanity` (RED state expected)
  </acceptance_criteria>
  <done>
    Test file exists, encodes empirical Nailli fixtures + boundary cases. Vitest currently reports "Cannot find module '../sanity'" — this is RED before GREEN implementation.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement sanity.ts to make tests pass</name>
  <files>apps/web/lib/canonicalize/sanity.ts</files>
  <read_first>
    - apps/web/lib/canonicalize/__tests__/sanity.test.ts (just-written tests — they specify the exact contract)
    - .planning/phases/07.1.6-canonical-capture-pipeline/07.1.6-PATTERNS.md §"lib/canonicalize/sanity.ts" (lines 70-129)
    - apps/web/lib/capture/storage-path.ts (analog: pure function module, no 'server-only' needed)
    - .planning/phases/07.1.6-canonical-capture-pipeline/07.1.6-CONTEXT.md §Specifics (threshold rationale)
  </read_first>
  <behavior>
    Module must export exactly 3 functions matching the test contract. NO side effects, NO async, NO Anthropic, NO Supabase. Pure math.
  </behavior>
  <action>
    Crie `apps/web/lib/canonicalize/sanity.ts`:

    ```typescript
    /**
     * Phase 07.1.6 canonical capture — trust gate (pure functions).
     *
     * D-02: combo geometric sanity + cross-angle outlier detection.
     * D-03: sem retry — gate fail → fallback direto.
     *
     * Pure module: no 'server-only', no async, no I/O. Math only.
     * Thresholds empíricos derivados de Nailli e85ea7de probe (CONTEXT.md §Specifics).
     */
    import type { IrisBbox, CanonicalStatus } from '@/lib/anthropic/types'

    // D-02 geometric sanity bounds — empirical (founder visual review):
    // center coord ∈ [0.20, 0.80] (centrais 60% do frame)
    // radius_pct ∈ [0.05, 0.30] (catches "bbox em canto" + "radius 0.40 cobrindo meio rosto")
    const GEOM_CENTER_MIN = 0.20
    const GEOM_CENTER_MAX = 0.80
    const GEOM_RADIUS_MIN = 0.05
    const GEOM_RADIUS_MAX = 0.30

    // D-02 cross-angle outlier threshold — empirical right_frontal delta was 0.10 from median;
    // 0.08 catches it com folga, sem flagear ruído normal entre 3 ângulos do mesmo olho.
    const CROSS_ANGLE_OUTLIER_THRESHOLD = 0.08

    /**
     * Geometric sanity: bbox center deve estar nos centrais 60% do frame
     * E radius_pct dentro de [0.05, 0.30]. Bounds são INCLUSIVE.
     */
    export function isGeometricallySane(bbox: IrisBbox): boolean {
      return (
        bbox.center_x_pct >= GEOM_CENTER_MIN &&
        bbox.center_x_pct <= GEOM_CENTER_MAX &&
        bbox.center_y_pct >= GEOM_CENTER_MIN &&
        bbox.center_y_pct <= GEOM_CENTER_MAX &&
        bbox.radius_pct >= GEOM_RADIUS_MIN &&
        bbox.radius_pct <= GEOM_RADIUS_MAX
      )
    }

    /**
     * Cross-angle outlier: para os 3 ângulos do mesmo olho, calcular median de
     * peers.center_x e peers.center_y; bbox é outlier se delta > 0.08 em qualquer eixo.
     *
     * Strict > (boundary delta = 0.08 NÃO é outlier).
     * Returns false when peers.length < 2 (não dá pra calcular median confiável com 0-1 peer).
     */
    export function isCrossAngleOutlier(bbox: IrisBbox, peers: IrisBbox[]): boolean {
      if (peers.length < 2) return false
      const medianX = median(peers.map(p => p.center_x_pct))
      const medianY = median(peers.map(p => p.center_y_pct))
      return (
        Math.abs(bbox.center_x_pct - medianX) > CROSS_ANGLE_OUTLIER_THRESHOLD ||
        Math.abs(bbox.center_y_pct - medianY) > CROSS_ANGLE_OUTLIER_THRESHOLD
      )
    }

    /**
     * D-02 combined gate: 'ok' iff bbox.valid AND sane AND não-outlier.
     * Qualquer falha → 'fallback' (D-01 + D-03: sem retry, original ao Modal).
     */
    export function isCanonicalAccepted(bbox: IrisBbox, peers: IrisBbox[]): CanonicalStatus {
      if (!bbox.valid) return 'fallback'
      if (!isGeometricallySane(bbox)) return 'fallback'
      if (isCrossAngleOutlier(bbox, peers)) return 'fallback'
      return 'ok'
    }

    /** Private median helper (even-length array = mean of 2 middle values). */
    function median(values: number[]): number {
      const sorted = [...values].sort((a, b) => a - b)
      const mid = Math.floor(sorted.length / 2)
      return sorted.length % 2 === 0
        ? (sorted[mid - 1] + sorted[mid]) / 2
        : sorted[mid]
    }
    ```

    NÃO adicione `import 'server-only'` (este módulo é pure, deve ser usável em testes node + edge se desejado). NÃO exporte `median` (private helper). NÃO faça threshold env-overridable (CONTEXT.md §Specifics: empírico, locked).
  </action>
  <verify>
    <automated>cd apps/web && pnpm test -- canonicalize/__tests__/sanity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/web/lib/canonicalize/sanity.ts` exists
    - Exports `isGeometricallySane`, `isCrossAngleOutlier`, `isCanonicalAccepted` (verifiable with grep: `export function isGeometricallySane`, `export function isCrossAngleOutlier`, `export function isCanonicalAccepted`)
    - Does NOT contain `import 'server-only'` (pure module)
    - Does NOT contain `process.env` (thresholds are hardcoded per CONTEXT.md)
    - `cd apps/web && pnpm test -- canonicalize/__tests__/sanity.test.ts` exits 0 (all 14+ tests GREEN)
    - `cd apps/web && pnpm tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    14+ vitest cases passing GREEN. `import { isCanonicalAccepted } from '@/lib/canonicalize/sanity'` resolvable; Wave 1 `index.ts` can call it per-image without further work.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (no external trust boundary in this plan — pure functions) | Sanity gate é called server-side com bboxes que já foram parsed/validated em Wave 1 sonnet-bbox.ts. Inputs nunca vêm direto de user request. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1.6-06 | Tampering | sanity.ts threshold constants | mitigate | Thresholds são literal const sem `process.env` lookup (CONTEXT.md §Specifics empirical lock); tampering em env não afeta gate. |
| T-07.1.6-07 | Denial of Service | isCrossAngleOutlier(bbox, peers) com peers.length grande | accept | peers.length nunca >2 em uso normal (3 ângulos por olho menos o bbox sendo testado). Median sort é O(n log n) em ≤2 elementos = trivial. |
| T-07.1.6-08 | Spoofing | bbox.valid=true mas valores degenerados (NaN, Infinity) | mitigate | Wave 1 sonnet-bbox.ts valida JSON parsed + clamps antes de chamar isCanonicalAccepted. Defesa em profundidade: comparações `>=`/`<=`/`>` retornam false em NaN, automaticamente caindo em 'fallback' branch. |
</threat_model>

<verification>
- 2 files novos sob apps/web/lib/canonicalize/ (sanity.ts + __tests__/sanity.test.ts).
- `pnpm test -- canonicalize/sanity` passes com ≥14 GREEN cases.
- `pnpm tsc --noEmit` passes (workspace apps/web).
- Sem imports a Anthropic, Supabase, sharp, fs — confirmado por grep ausência de strings: `import 'server-only'`, `from '@/lib/supabase'`, `from '@anthropic-ai/sdk'`.
</verification>

<success_criteria>
- [ ] apps/web/lib/canonicalize/sanity.ts criado com 3 exports
- [ ] apps/web/lib/canonicalize/__tests__/sanity.test.ts criado com ≥14 it() cases
- [ ] pnpm test target sanity.test.ts exit 0
- [ ] pnpm tsc --noEmit exit 0
- [ ] Hardcoded thresholds 0.20/0.80, 0.05/0.30, 0.08 grep-verifiable em sanity.ts
</success_criteria>

<output>
After completion, create `.planning/phases/07.1.6-canonical-capture-pipeline/07.1.6-02-SUMMARY.md` covering:
- TDD cycle: RED commit hash + GREEN commit hash (separate commits per gsd convention)
- Test count + assertion count
- Decisions implemented: D-02 (sanity + outlier combo), D-03 (no retry — gate fail returns 'fallback' direct)
- Downstream contract: Wave 1 index.ts importa `{ isCanonicalAccepted }` por imagem; assina `(bbox: IrisBbox, peers: IrisBbox[]) => 'ok' | 'fallback' | 'disabled'`
- Acceptance note: right_frontal Nailli específico talvez NÃO seja flagged pelo gate (números dentro do range — accepted limitation per CONTEXT.md §Specifics; founder usa /admin Re-canonicalizar como escape hatch — Plan 06).
</output>
