---
status: partial
phase: 07.1.6-canonical-capture-pipeline
source: [07.1.6-VERIFICATION.md]
started: 2026-05-12T14:45:00-03:00
updated: 2026-05-12T14:45:00-03:00
---

## Current Test

[awaiting founder dogfooding sample ≥3 readings]

## Tests

### 1. Live canonical pipeline smoke test
expected: Founder captura uma nova reading (ou usa Re-canonicalizar em existing). Pós-finalize, inspecionar `readings.canonical_metadata` via Supabase Studio — espera-se: `sonnet_input_tokens > 0`, `sonnet_output_tokens > 0`, `cost_usd ≈ 0.05`, `status_summary.ok + status_summary.fallback = 6`, `canonicalized_at` ISO timestamp set.
result: [pending]

### 2. Modal pipeline uses canonical URLs (≤50 px center spread)
expected: Pra reading do item 1, inspecionar `vision_features.processing_metadata.detect_diagnostics` no DB. Cross-angle center spread (max distance entre centers dos 3 ângulos do mesmo olho) ≤ 50 px pra LEFT e ≤ 50 px pra RIGHT. Esse é o gate empírico que 07.1.5 perdeu.
result: [pending]

### 3. /admin/calibration UI smoke
expected: Navegar pra /admin/calibration/{readingId}. Confirmar:
- canonical badge renderizado (status_summary com "X/6 ok" ou "fallback"/"disabled" conforme caso)
- "Re-canonicalizar" botão presente next aos existing buttons
- click no botão → POST /api/capture/canonicalize → loading state → toast "X imagens canonicalizadas (Y fallback)"
- page refresh pós-toast mostra badge atualizado
result: [pending]

### 4. Dogfooding sample ≥3 readings — ≥80% canonical_status='ok'
expected: Founder captura ≥3 novas readings nas próximas sessões. Aggregate canonical_status nas 18 fotos (3 readings × 6 fotos) — pelo menos 80% (≥15/18) com status `ok`. Se >4 fallbacks total, gate pode estar muito apertado OU capture protocol precisa refinement — file como follow-up Phase 07.2.
result: [pending]

## Summary

total: 4
passed: 0
issues: 0
pending: 4
skipped: 0
blocked: 0

## Gaps
