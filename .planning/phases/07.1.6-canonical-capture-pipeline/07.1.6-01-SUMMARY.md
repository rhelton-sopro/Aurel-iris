---
phase: 07.1.6-canonical-capture-pipeline
plan: 01
subsystem: database
tags: [canonical-capture, schema-migration, supabase, anthropic-types, sonnet-4-6]

requires:
  - phase: 07
    provides: anthropicClient + estimateCostUsd singleton em apps/web/lib/anthropic/client.ts
  - phase: 07
    provides: types.ts com audit-vocabulary:allowlist marker (Phase 7 Anthropic SDK install)
provides:
  - reading_images.canonical_storage_path nullable column (NULL = pre-canonicalização, backward-compat)
  - readings.canonical_metadata jsonb column (Sonnet cost/usage tracking per reading)
  - SONNET_BBOX_MODEL = 'claude-sonnet-4-6' as const (hardcoded literal type — NOT env-overridable, enforce C-04)
  - IrisBbox interface (center_x_pct, center_y_pct, radius_pct, confidence, valid)
  - CanonicalStatus union ('ok' | 'fallback' | 'disabled')
  - CanonicalMetadata interface (sonnet_input_tokens, sonnet_output_tokens, cost_usd, status_summary, canonicalized_at)
affects: [07.1.6-02-sanity, 07.1.6-03-canonicalize-core, 07.1.6-04-endpoint, 07.1.6-05-finalize-wiring, 07.1.6-06-admin-ui]

tech-stack:
  added: []
  patterns:
    - "Hardcoded model constant (literal-typed `as const`) — prevents env-override drift for high-stakes models"
    - "Additive-only schema migration (begin/commit transaction, `add column if not exists`, zero alter/drop/not null)"

key-files:
  created:
    - supabase/migrations/0012_canonical_capture.sql
  modified:
    - apps/web/lib/anthropic/client.ts
    - apps/web/lib/anthropic/types.ts

key-decisions:
  - "C-01: canonical via Sonnet bbox, NÃO U-Net — locked CHECKPOINT-2026-05-11; ESCALATE_C ABANDONED"
  - "C-04: claude-sonnet-4-6 hardcoded como literal type — NOT env-overridable; ESCALATE_C2 (Haiku fallback) ABANDONED"
  - "Schema additivo, backward-compat: pre-07.1.6 readings têm canonical_storage_path IS NULL; process route resolves canonical ?? storage_path"
  - "Originais nunca sobrescritos: canonical_storage_path é coluna independente de storage_path"

patterns-established:
  - "Migration shape: `begin; alter table T add column if not exists C type; comment on column ...; commit;` — re-runnable, additive, no breaking changes"
  - "Sonnet/Haiku model constants colocated em apps/web/lib/anthropic/client.ts com PRICING_<MODEL> sibling para cost telemetry"

requirements-completed: []

duration: ~2min (file edits) + manual `supabase db push` (founder)
completed: 2026-05-12
---

# Phase 07.1.6 Plan 01: Schema Migration + Anthropic Types Summary

**Foundation laid: canonical capture schema columns live no Supabase + Sonnet model constant hardcoded + IrisBbox/CanonicalMetadata types disponíveis para Waves 1–3 importarem.**

## Performance

- **Duration:** ~14 min (file edits ~2 min + founder `supabase db push` + verification SQL ~10 min)
- **Started:** 2026-05-12T07:30:05-03:00 (first commit)
- **Completed:** 2026-05-12T07:43:57-03:00 (post-applied confirmation)
- **Tasks:** 4 (3 code commits + 1 [BLOCKING] human-action `supabase db push`)
- **Files modified:** 3

## Accomplishments

- Schema migration `0012_canonical_capture.sql` committed e aplicada à live database via `supabase db push` (founder confirmed: 2 rows em information_schema, ambas `is_nullable = YES`).
- `SONNET_BBOX_MODEL` exportado como literal type `'claude-sonnet-4-6' as const` — Wave 1 (Plan 03) e qualquer caller futuro são impedidos pelo TypeScript de hot-swap para Haiku/Opus via env var (C-04 enforce).
- `IrisBbox` + `CanonicalStatus` + `CanonicalMetadata` types exportados; `audit-vocabulary:allowlist` marker preservado no header de `types.ts`.

## Task Commits

Cada task committed atomicamente:

1. **Task 1: Migration 0012_canonical_capture.sql** — `cb03ced` (feat)
2. **Task 2: [BLOCKING] supabase db push** — manual, no commit (founder applied à live DB, confirmed via verification SQL)
3. **Task 3: SONNET_BBOX_MODEL constant em client.ts** — `4c0203c` (feat)
4. **Task 4: IrisBbox + CanonicalStatus + CanonicalMetadata types em types.ts** — `8161591` (feat)

## Files Created/Modified

- `supabase/migrations/0012_canonical_capture.sql` — Additive schema: `reading_images.canonical_storage_path text` (nullable) + `readings.canonical_metadata jsonb` (nullable). Wrapped em `begin;`/`commit;`. Zero `drop`/`alter column`/`not null`/`create policy`. Re-runnable via `if not exists`.
- `apps/web/lib/anthropic/client.ts` — Added `export const SONNET_BBOX_MODEL = 'claude-sonnet-4-6' as const`. `MODEL`, `anthropicClient`, `PRICING_SONNET_4_6`, `estimateCostUsd`, `import 'server-only'` inalterados.
- `apps/web/lib/anthropic/types.ts` — Added 3 exports (IrisBbox interface, CanonicalStatus union, CanonicalMetadata interface). Linha 1 `audit-vocabulary:allowlist` marker e existing exports (ENCERRAMENTO_LITERAL, REPORT_SECTIONS, AuditMetadata, AnthropicError) intactos.

## Verification Gates

- Migration content (literal-string check): OK
- Forbidden tokens (drop/alter column/not null/create policy/default): none found
- `pnpm tsc --noEmit` em `apps/web/`: zero erros nos arquivos do plano; pre-existing failures (quality-scoring.test.ts — Phase 3 debt, feedback memory) intocados, fora de escopo
- `pnpm test:run lib/anthropic`: **81 passed / 5 todo / 0 failed** — zero regressões na Phase 7 suite
- `supabase db push`: founder reportou "applied" pós-execution; verification SQL retorna 2 rows com `is_nullable = YES`

## Key Links Honored (from plan must_haves)

- ✓ `supabase/migrations/0012_canonical_capture.sql → live Supabase database` via `supabase db push` (Wave 0 BLOCKING task — done)
- ✓ `apps/web/lib/anthropic/client.ts → apps/web/lib/canonicalize/sonnet-bbox.ts (Wave 1)` via `import { anthropicClient, SONNET_BBOX_MODEL, PRICING_SONNET_4_6 } from '@/lib/anthropic/client'` — ready for Plan 03 consumption

## Self-Check: PASSED

All artifacts produced, all key_links established, schema applied to live DB, types compile, zero regressions. Plan 01 is complete and Wave 0 Plan 02 (sanity.ts TDD) is unblocked.
