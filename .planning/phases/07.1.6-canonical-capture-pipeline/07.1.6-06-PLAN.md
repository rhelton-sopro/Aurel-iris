---
phase: 07.1.6
plan: 06
type: execute
wave: 2
depends_on: [01, 04]
files_modified:
  - apps/web/app/admin/calibration/[id]/page.tsx
  - apps/web/app/api/admin/calibration/photos/[reading_id]/route.ts
  - apps/web/components/calibration/RecanonicalizeButton.tsx
autonomous: true
requirements: []
tags: [canonical-capture, admin-ui, recanonicalize-button]

must_haves:
  truths:
    - "D-05: Backfill estratégia é on-demand via /admin/calibration Re-canonicalizar button — sem batch script. Idempotente: regenera canonical crop, mantém originais inalterados."
    - "D-05: founder escolhe quais leituras existentes valem o re-trabalho — antiga (pre-protocol) vs nova (post-protocol) — manual escape hatch para o failure mode D-02 não cobre"
    - "Endpoint reusado é /api/capture/canonicalize (Plan 04) — mesmo POST que finalize chama"
    - "Originais nunca sobrescritos: canonical_storage_path overwrite via service-role; storage_path inalterado"
    - "canonical_status badge na /admin/calibration mostra distribution ok/fallback/disabled — auditing para founder dogfooding"
  artifacts:
    - path: "apps/web/components/calibration/RecanonicalizeButton.tsx"
      provides: "Client component — POST /api/capture/canonicalize com { readingId }, loading state, toast feedback. Pattern mirror de PhotoDownloadButton.tsx."
      exports: ["RecanonicalizeButton"]
      min_lines: 50
    - path: "apps/web/app/admin/calibration/[id]/page.tsx"
      provides: "Page renderiza canonical_status badge (ok N/6 ou fallback indicator) + RecanonicalizeButton ao lado dos botões existentes"
      contains: "RecanonicalizeButton"
    - path: "apps/web/app/api/admin/calibration/photos/[reading_id]/route.ts"
      provides: "GET extended para incluir canonical_url per imagem (signed URL para canonical_storage_path se non-NULL)"
      contains: "canonical_url"
  key_links:
    - from: "apps/web/components/calibration/RecanonicalizeButton.tsx"
      to: "apps/web/app/api/capture/canonicalize/route.ts (Plan 04)"
      via: "fetch POST /api/capture/canonicalize body { readingId }"
      pattern: "/api/capture/canonicalize"
    - from: "apps/web/app/admin/calibration/[id]/page.tsx"
      to: "readings.canonical_metadata (Plan 01 schema)"
      via: "service-role select; pass to badge"
      pattern: "canonical_metadata"
---

<objective>
Surface admin UI para canonical capture audit + manual re-trigger:
1. `RecanonicalizeButton` client component (mirror padrão `PhotoDownloadButton.tsx`) — botão que POSTa para `/api/capture/canonicalize` com loading state + toast.
2. `/admin/calibration/[id]/page.tsx` — adicionar badge `canonical N/6` lendo de `readings.canonical_metadata.status_summary` + renderizar `<RecanonicalizeButton>` ao lado dos botões existentes.
3. `/api/admin/calibration/photos/[reading_id]/route.ts` — extend para devolver `canonical_url` per imagem (signed URL p/ canonical_storage_path se non-NULL).

Purpose: founder pode (a) ver quantas fotos canonicalizaram OK por reading e (b) re-disparar canonical em readings antigas (D-05 backfill on-demand). Item (c) — visual diff de canonical vs original — deixado pra phase de polish futura (não bloqueia 07.1.6 ship).

Output: 1 arquivo novo + 2 arquivos modificados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1.6-canonical-capture-pipeline/07.1.6-CONTEXT.md
@.planning/phases/07.1.6-canonical-capture-pipeline/07.1.6-PATTERNS.md
@apps/web/app/admin/calibration/[id]/page.tsx
@apps/web/app/api/admin/calibration/photos/[reading_id]/route.ts
@apps/web/components/calibration/PhotoDownloadButton.tsx

<interfaces>
<!-- Endpoint contract from Plan 04 -->
POST /api/capture/canonicalize:
- Request body: `{ readingId: string }`
- Response 200: `{ results, metadata, status_summary: { ok: number, fallback: number, disabled: number } }`

<!-- Existing PhotoDownloadButton pattern (mirror this) -->
File: apps/web/components/calibration/PhotoDownloadButton.tsx
- `'use client'` + `useState(isLoading)` + `fetch(...)` + `toast.error`/`toast.success`/`toast.warning` from `'sonner'`
- Uses `<Button>` from `@/components/ui/button` + `<Loader2>`/`<Download>` from `lucide-react`
- Stable button label + spinner on loading; aria-busy on button

<!-- Existing page.tsx (apps/web/app/admin/calibration/[id]/page.tsx) -->
- Server component, default async export
- Uses `createServiceClient()` and selects `reading.id, created_at, status, vision_features` + nested client
- For badges, uses `<Badge variant=...>` from `@/components/ui/badge`
- Header has existing flex-wrap badge area (lines 151-162) where we add canonical badge

<!-- Existing JSON endpoint for photos (apps/web/app/api/admin/calibration/photos/[reading_id]/route.ts) -->
- GET only; founder-gated via isFounderEmail
- Selects reading_images.eye, angle, storage_path; createSignedUrls(paths, TTL=24h)
- Response shape: `{ signedUrls: [{ eye, angle, url, filename }] }`

For canonical extension: add `canonical_url: string | null` field per entry (TTL=24h).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create RecanonicalizeButton client component</name>
  <files>apps/web/components/calibration/RecanonicalizeButton.tsx</files>
  <read_first>
    - apps/web/components/calibration/PhotoDownloadButton.tsx (closest analog — copy pattern verbatim, adapt for POST)
    - apps/web/app/api/capture/canonicalize/route.ts (Plan 04 — verify exact request shape)
    - .planning/phases/07.1.6-canonical-capture-pipeline/07.1.6-PATTERNS.md §"app/admin/calibration/[id]/page.tsx" → "Re-canonicalizar button"
  </read_first>
  <action>
    Crie `apps/web/components/calibration/RecanonicalizeButton.tsx`:

    ```tsx
    'use client'

    import { useState } from 'react'
    import { RefreshCw, Loader2 } from 'lucide-react'
    import { toast } from 'sonner'
    import { Button } from '@/components/ui/button'
    import { useRouter } from 'next/navigation'

    interface CanonicalizeResponseBody {
      status_summary?: { ok: number; fallback: number; disabled: number }
      error?: string
    }

    /**
     * Phase 07.1.6 D-05: backfill on-demand.
     * POST /api/capture/canonicalize re-roda Sonnet bbox + crop + upload em todas
     * as 6 fotos. Idempotente — overwrite canonical_storage_path; originais
     * inalterados. Custo ~$0.05/reading.
     *
     * Pós-success: router.refresh() para o page.tsx (server component) re-fetchar
     * canonical_metadata e atualizar o badge.
     */
    export function RecanonicalizeButton({ readingId }: { readingId: string }) {
      const [isLoading, setIsLoading] = useState(false)
      const router = useRouter()

      async function handleClick() {
        if (!confirm('Re-canonicalizar todas as 6 fotos? Custo Sonnet ~$0.05.')) return
        setIsLoading(true)
        try {
          const res = await fetch('/api/capture/canonicalize', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ readingId }),
            cache: 'no-store',
          })
          if (!res.ok) {
            const detail = await res.json().catch(() => ({})) as CanonicalizeResponseBody
            toast.error(`Falha (${res.status}): ${detail.error ?? 'erro desconhecido'}`)
            return
          }
          const body = (await res.json()) as CanonicalizeResponseBody
          const sum = body.status_summary
          if (sum) {
            const msg = `canonical: ${sum.ok} ok · ${sum.fallback} fallback · ${sum.disabled} disabled`
            sum.ok >= 5 ? toast.success(msg) : toast.warning(msg)
          } else {
            toast.success('Re-canonicalizado')
          }
          router.refresh()
        } catch (err) {
          const msg = err instanceof Error ? err.message : 'falha de rede'
          toast.error(`Falha: ${msg}`)
        } finally {
          setIsLoading(false)
        }
      }

      return (
        <Button
          type="button"
          variant="outline"
          size="sm"
          onClick={handleClick}
          disabled={isLoading}
          aria-busy={isLoading}
        >
          {isLoading ? (
            <>
              <Loader2 className="mr-2 h-4 w-4 animate-spin" />
              Canonicalizando...
            </>
          ) : (
            <>
              <RefreshCw className="mr-2 h-4 w-4" />
              Re-canonicalizar
            </>
          )}
        </Button>
      )
    }
    ```

    NÃO faça revalidatePath aqui (client component). Use `router.refresh()` (next/navigation) para re-render do parent server component após success. NÃO esconda o confirm() — founder action é costly ($0.05), feedback explícito.
  </action>
  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit && grep -q "export function RecanonicalizeButton" apps/web/components/calibration/RecanonicalizeButton.tsx && grep -q "/api/capture/canonicalize" apps/web/components/calibration/RecanonicalizeButton.tsx && grep -q "router.refresh" apps/web/components/calibration/RecanonicalizeButton.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/web/components/calibration/RecanonicalizeButton.tsx` exists
    - Line 1 contains `'use client'`
    - Exports `function RecanonicalizeButton({ readingId }: { readingId: string })`
    - Calls `fetch('/api/capture/canonicalize', { method: 'POST', ... body: JSON.stringify({ readingId }) })`
    - Uses `toast.success`/`toast.error`/`toast.warning` from `'sonner'`
    - Calls `router.refresh()` after success (from `next/navigation`)
    - Has loading state with `<Loader2 className="...animate-spin" />`
    - Button variant `outline`, size `sm`
    - `pnpm tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>
    Component pronto pra ser importado. Page.tsx pode renderizar `<RecanonicalizeButton readingId={reading.id} />`. Backend Plan 04 handles auth + ownership; success refresca o badge automaticamente.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend /admin/calibration/[id]/page.tsx with canonical badge + button</name>
  <files>apps/web/app/admin/calibration/[id]/page.tsx</files>
  <read_first>
    - apps/web/app/admin/calibration/[id]/page.tsx (full file)
    - apps/web/components/calibration/RecanonicalizeButton.tsx (Task 1 just-built)
    - .planning/phases/07.1.6-canonical-capture-pipeline/07.1.6-PATTERNS.md §"app/admin/calibration/[id]/page.tsx — canonical_status badge + Re-canonicalizar"
  </read_first>
  <action>
    Em `apps/web/app/admin/calibration/[id]/page.tsx`:

    **Mudança 1** — adicionar import (após linha 11):
    ```typescript
    import { RecanonicalizeButton } from '@/components/calibration/RecanonicalizeButton'
    ```

    **Mudança 2** — estender `ReadingDetail` interface (L17-23) para incluir `canonical_metadata`:
    ```typescript
    interface ReadingDetail {
      id: string
      created_at: string
      status: string | null
      vision_features: unknown
      canonical_metadata: unknown   // Phase 07.1.6 — jsonb CanonicalMetadata shape
      client: { full_name: string | null; birth_date: string | null } | { full_name: string | null; birth_date: string | null }[] | null
    }
    ```

    **Mudança 3** — estender o select query (L67-72) para incluir `canonical_metadata`:
    ```typescript
      const { data: reading, error: readingError } = await supabase
        .from('readings')
        .select(
          'id, created_at, status, vision_features, canonical_metadata, client:clients(full_name, birth_date)'
        )
        .eq('id', readingId)
        .maybeSingle()
    ```

    **Mudança 4** — antes do `return` (após linha onde `const client = getClient(...)`), parse canonical_metadata para badge:
    ```typescript
      // Phase 07.1.6 — canonical_metadata jsonb (CanonicalMetadata shape).
      const canonicalMeta = reading.canonical_metadata as {
        status_summary?: { ok?: number; fallback?: number; disabled?: number }
        canonicalized_at?: string
      } | null
      const okCount = canonicalMeta?.status_summary?.ok ?? 0
      const fallbackCount = canonicalMeta?.status_summary?.fallback ?? 0
      const disabledCount = canonicalMeta?.status_summary?.disabled ?? 0
      const totalCanonical = okCount + fallbackCount + disabledCount
    ```

    **Mudança 5** — adicionar badge no header flex-wrap (L151-162, junto aos badges existentes), ANTES dos badges `existingDiagnosis`/`existingAnnotation`:
    ```tsx
          {totalCanonical > 0 && (
            <Badge
              variant={fallbackCount > 0 ? 'secondary' : 'default'}
              className={fallbackCount === 0 ? 'bg-violet-600' : ''}
            >
              canonical {okCount}/{totalCanonical}
            </Badge>
          )}
          {totalCanonical === 0 && (
            <Badge variant="outline" className="text-muted-foreground">
              canonical pendente
            </Badge>
          )}
    ```

    **Mudança 6** — adicionar `<RecanonicalizeButton>` ao lado dos botões existentes (linha onde `<PhotoDownloadButton>` e `<TechnicalReportCopyButton>` aparecem dentro do flex-wrap):
    ```tsx
          <TechnicalReportCopyButton ... />
          <PhotoDownloadButton readingId={reading.id} />
          <RecanonicalizeButton readingId={reading.id} />
    ```

    NÃO mude AnnotationForm, CalibrationDiagnosisForm, PhotoGrid, ou FeaturesDisplay. NÃO altere ordering das seções existentes. NÃO faça canonical_url image rendering aqui (deferred — Task 3 só adiciona o JSON field; visual diff é futura phase).
  </action>
  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit && grep -q "RecanonicalizeButton" apps/web/app/admin/calibration/\[id\]/page.tsx && grep -q "canonical_metadata" apps/web/app/admin/calibration/\[id\]/page.tsx && grep -q "canonical pendente" apps/web/app/admin/calibration/\[id\]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/admin/calibration/[id]/page.tsx` imports `RecanonicalizeButton` from `@/components/calibration/RecanonicalizeButton`
    - Select query inclui `canonical_metadata` (grep verifies select contains canonical_metadata)
    - Page renders Badge `canonical {okCount}/{totalCanonical}` quando `totalCanonical > 0`
    - Page renders Badge `canonical pendente` quando `totalCanonical === 0`
    - Page renders `<RecanonicalizeButton readingId={reading.id} />` no flex-wrap dos botões
    - AnnotationForm, CalibrationDiagnosisForm, PhotoGrid, FeaturesDisplay unchanged
    - `pnpm tsc --noEmit` exit 0
    - `pnpm lint` exit 0
  </acceptance_criteria>
  <done>
    Founder abre `/admin/calibration/{readingId}` e vê:
    1. Badge `canonical 5/6` (ou pendente)
    2. Botão "Re-canonicalizar" ao lado dos botões existentes
    3. Click → POST /api/capture/canonicalize → toast feedback → page refreshes com badge atualizado
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend photos API to include canonical_url</name>
  <files>apps/web/app/api/admin/calibration/photos/[reading_id]/route.ts</files>
  <read_first>
    - apps/web/app/api/admin/calibration/photos/[reading_id]/route.ts (full file — closest analog: signed URL pattern)
    - .planning/phases/07.1.6-canonical-capture-pipeline/07.1.6-PATTERNS.md §"app/api/admin/calibration/photos/[reading_id]/route.ts"
  </read_first>
  <action>
    Em `apps/web/app/api/admin/calibration/photos/[reading_id]/route.ts`:

    **Mudança 1** — estender `SignedPhotoEntry` interface (L27-32):
    ```typescript
    interface SignedPhotoEntry {
      eye: string
      angle: string
      url: string
      filename: string
      canonical_url: string | null   // Phase 07.1.6 — null se canonical_storage_path IS NULL
    }
    ```

    **Mudança 2** — estender o select (L49-52):
    ```typescript
      const { data: images, error: imagesError } = await service
        .from('reading_images')
        .select('eye, angle, storage_path, canonical_storage_path')
        .eq('reading_id', readingId)
    ```

    **Mudança 3** — adicionar canonical signed URL batch ANTES do return (após o existing originals signed batch, around L66-74):
    ```typescript
      // Phase 07.1.6: separate signed URL batch for canonical paths.
      // canonical_storage_path can be NULL per row; build a parallel array with
      // index-matching to images[]. Empty string para NULL paths (Supabase
      // createSignedUrls reclama de empty strings — filter + remap by index).
      const canonicalPathPairs = images.map((img, idx) => ({
        idx,
        path: img.canonical_storage_path,
      })).filter(p => typeof p.path === 'string' && p.path.length > 0)

      const canonicalUrlByIndex = new Map<number, string>()
      if (canonicalPathPairs.length > 0) {
        const { data: signedCanonical, error: canSignError } = await service.storage
          .from('iris-captures')
          .createSignedUrls(canonicalPathPairs.map(p => p.path as string), SIGNED_URL_TTL_SECONDS)
        if (canSignError) {
          console.error(`[photos] canonical sign error: ${canSignError.message}`)
        } else if (signedCanonical) {
          canonicalPathPairs.forEach((pair, k) => {
            const signedUrl = signedCanonical[k]?.signedUrl
            if (signedUrl) canonicalUrlByIndex.set(pair.idx, signedUrl)
          })
        }
      }
    ```

    **Mudança 4** — estender o map final que constrói `signedUrls` (L77-89): incluir `canonical_url` derivado do `canonicalUrlByIndex.get(idx) ?? null`:
    ```typescript
      const signedUrls: SignedPhotoEntry[] = images
        .map((img, idx) => {
          const url = signed[idx]?.signedUrl
          if (!url) return null
          return {
            eye: img.eye,
            angle: img.angle,
            url,
            filename: `${readingId}_${img.eye}_${img.angle}.jpg`,
            canonical_url: canonicalUrlByIndex.get(idx) ?? null,
          }
        })
        .filter((entry): entry is SignedPhotoEntry => entry !== null)
    ```

    NÃO remova `isFounderEmail` defense-in-depth gate (L42-45). NÃO mude TTL (24h é existing convention). NÃO crie um endpoint paralelo para canonical — extender o existente é mais simples + 1 round-trip.
  </action>
  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit && grep -q "canonical_url" apps/web/app/api/admin/calibration/photos/\[reading_id\]/route.ts && grep -q "canonical_storage_path" apps/web/app/api/admin/calibration/photos/\[reading_id\]/route.ts && grep -q "canonicalUrlByIndex" apps/web/app/api/admin/calibration/photos/\[reading_id\]/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/api/admin/calibration/photos/[reading_id]/route.ts` `SignedPhotoEntry` interface includes `canonical_url: string | null`
    - Select query includes `canonical_storage_path`
    - File contains `canonicalUrlByIndex` Map for index-matched canonical URLs
    - canonical URLs use same TTL constant `SIGNED_URL_TTL_SECONDS`
    - isFounderEmail gate present at top (L42-45) — unchanged
    - `pnpm tsc --noEmit` exit 0
    - `pnpm lint` exit 0
  </acceptance_criteria>
  <done>
    GET `/api/admin/calibration/photos/{readingId}` returns enriched JSON with `canonical_url` per photo. Future polish phase pode usar esses URLs para visual diff. Page.tsx Task 2 não consome o canonical_url ainda (deferred — apenas the badge usa canonical_metadata).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (admin user) → POST /api/capture/canonicalize | RecanonicalizeButton client → Plan 04 endpoint. Auth-gated; ownership check em Plan 04. |
| browser (admin user) → GET /api/admin/calibration/photos/[reading_id] | Founder-gated via isFounderEmail (existing); extension only adds canonical_url payload. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1.6-27 | Spoofing | RecanonicalizeButton readingId tampering | mitigate | Plan 04 endpoint faz auth+ownership check (`.eq('therapist_id', user.id)`). User-provided readingId que não pertence ao auth.uid retorna 404. |
| T-07.1.6-28 | Tampering | canonical_url in JSON response | accept | Signed URLs são gerados server-side via service-role; client-side tampering inválida assinatura. Defense-in-depth: founder-only endpoint (isFounderEmail). |
| T-07.1.6-29 | Information Disclosure | canonical_url exposed in admin photos response | accept | Endpoint já era founder-gated (existing isFounderEmail check); canonical URLs herdam o mesmo controle. Sem expansão de attack surface. |
| T-07.1.6-30 | Denial of Service | repeated RecanonicalizeButton clicks | mitigate | confirm() prompt + isLoading state desabilita botão durante request. Server-side Plan 04 rate-limit consideration is accepted (founder action, ≤3/dia peak). |
| T-07.1.6-31 | Repudiation | re-canonicalize over-writes prior canonical_metadata silently | mitigate | canonicalized_at timestamp em canonical_metadata é updated cada re-canonicalize call. Founder vê o badge atualizado pós-refresh; toast confirms summary. Hard audit via canonical_metadata.canonicalized_at no DB. |
</threat_model>

<verification>
- 1 file novo + 2 modificados.
- `pnpm tsc --noEmit` passes (apps/web).
- `pnpm lint` passes.
- Smoke test (founder): abrir `/admin/calibration/{readingId}` antes do dogfooding deploy → badge `canonical pendente` aparece (canonical_metadata IS NULL pra reading antiga). Click Re-canonicalizar → toast `canonical: 5 ok · 1 fallback` (Nailli e85ea7de espera) → page refreshes → badge muda para `canonical 5/6`.
</verification>

<success_criteria>
- [ ] apps/web/components/calibration/RecanonicalizeButton.tsx criado
- [ ] page.tsx imports RecanonicalizeButton + renderiza badge canonical + botão
- [ ] photos/[reading_id]/route.ts retorna canonical_url per imagem
- [ ] pnpm tsc --noEmit exit 0
- [ ] pnpm lint exit 0
- [ ] AnnotationForm, CalibrationDiagnosisForm, PhotoGrid, FeaturesDisplay unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/07.1.6-canonical-capture-pipeline/07.1.6-06-SUMMARY.md` covering:
- 3 arquivos (1 new + 2 modified)
- Decisions implemented: D-05 (on-demand backfill via admin button), founder audit-ability via badge
- Smoke test note (founder): re-canonicalizar Nailli antiga (`71a7bf1d`) pré-deploy; verify badge updates + storage shows canonical/ paths under `1e02831f-.../71a7bf1d-.../canonical/`
- Deferred: visual diff canonical-vs-original side-by-side (Phase 07.1.7 polish ou Wave C corpus expansion)
</output>
