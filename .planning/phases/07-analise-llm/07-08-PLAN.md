---
phase: 07-analise-llm
plan: 08
type: execute
wave: 5
depends_on: [07-01, 07-04, 07-05, 07-07]
files_modified:
  - apps/web/app/api/readings/[id]/analyze/route.ts
  - apps/web/app/api/readings/[id]/analyze/__tests__/route.test.ts
autonomous: true
requirements: [LLM-01, LLM-03]
tags: [phase-7, route-handler, streaming, auth, web-streams]

must_haves:
  truths:
    - "Requisição POST anônima (sem session) retorna 401 com mensagem clara — terapeuta não logado bloqueado (T-7-AUTH gate-a)"
    - "Terapeuta tentando analisar reading de outro terapeuta recebe 403 — ownership respeitado (T-7-AUTH gate-b)"
    - "Terapeuta tentando regenerar uma 4ª vez recebe 409 com mensagem 'Limite de 3 regenerações atingido' (T-7-COST gate-e)"
    - "Terapeuta dispara análise e o stream completa em <90s típico para Sonnet 4.6 com prompt cache hit (LLM-01 SC1)"
    - "Disclaimer literal SPEC §6 aparece ao final de todo report_generated, mesmo se LLM tentar parafrasear (D-P3 server-appended)"
    - "Se o stream falhar no meio, as seções já recebidas continuam salvas e o contador de regenerações NÃO é incrementado (D-S2 + D-S3 — terapeuta pode tentar novamente sem perder slot)"
    - "Após análise completar, terapeuta vê audit_metadata persistido (anchor_rate_pct, forbidden_vocab, audited_at) pronto para o banner D-A1/D-A2 da Surface 2"
    - "Logs server-side NÃO contêm nome do cliente, notas do terapeuta, ou texto do relatório (D-T1 — apenas UUIDs, latencies, token buckets)"
    - "Wave-0 file `apps/web/app/api/readings/[id]/analyze/__tests__/route.test.ts` criado com tests it.todo cobrindo os 5 auth gates (a-e) — implementação de mock-stream fica para founder UAT"
  artifacts:
    - path: "apps/web/app/api/readings/[id]/analyze/route.ts"
      provides: "Streaming POST handler with 5 auth gates + section-boundary persistence + audit + telemetry"
      min_lines: 200
      contains: "ReadableStream"
    - path: "apps/web/app/api/readings/[id]/analyze/__tests__/route.test.ts"
      provides: "Tests for 5 auth gates + Response shape"
      min_lines: 130
  key_links:
    - from: "apps/web/app/api/readings/[id]/analyze/route.ts"
      to: "lib/anthropic/analyze.ts analyzeReading"
      via: "consumed for stream + finalize"
      pattern: "analyzeReading\\("
    - from: "apps/web/app/api/readings/[id]/analyze/route.ts"
      to: "lib/anthropic/parser.ts findAllBoundaries"
      via: "section-boundary detection on accumulated buffer"
      pattern: "findAllBoundaries"
    - from: "apps/web/app/api/readings/[id]/analyze/route.ts"
      to: "lib/anthropic/audit.ts runAudit"
      via: "post-stream audit + UPDATE audit_metadata"
      pattern: "runAudit\\("
    - from: "apps/web/app/api/readings/[id]/analyze/route.ts"
      to: "ENCERRAMENTO_LITERAL from types.ts"
      via: "server-appended after stream completes (D-P3)"
      pattern: "ENCERRAMENTO_LITERAL"
---

<objective>
Construir o Route Handler streaming `app/api/readings/[id]/analyze/route.ts` — POST com 5 auth gates (T-7-AUTH a-e), Web Streams API consumindo o `analyzeReading` orchestrator, section-boundary parsing incremental sobre buffer acumulado, persistência server-side via `jsonb_set` em cada seção FECHADA, append do `ENCERRAMENTO_LITERAL` literal pós-stream, audit + telemetry pós-stream, treatment de erros que NÃO incrementa cap. Wave-0 stub do test file também criado.

Purpose: Esta é a casca HTTP do pipeline LLM. Defesas críticas: (a) auth gates impedem cross-therapist analysis (T-7-AUTH); (b) regen cap impede cost drain (T-7-COST); (c) section-boundary parser impede jsonb corruption (T-7-DRIFT); (d) server-appended encerramento garante SC4 (T-7-DRIFT); (e) AbortSignal plumbing impede LLM continuar rodando após user fechar tab (T-7-COST). Sem este handler, nada do que foi construído nas Waves 1-3 chega ao cliente.
Output: Route Handler funcional + test file Wave-0 criado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-analise-llm/07-CONTEXT.md
@.planning/phases/07-analise-llm/07-RESEARCH.md
@.planning/phases/07-analise-llm/07-PATTERNS.md
@apps/web/lib/anthropic/analyze.ts
@apps/web/lib/anthropic/parser.ts
@apps/web/lib/anthropic/audit.ts
@apps/web/lib/anthropic/types.ts
@apps/web/lib/anthropic/client.ts
@apps/web/app/api/readings/[id]/process/route.ts
@apps/web/app/api/vision/webhook/route.ts
@apps/web/lib/supabase/server.ts
@apps/web/types/database.ts

<interfaces>
<!-- Route Handler shape — extracted from RESEARCH §Pattern 2 lines 426-547 -->
<!-- Existing analog: app/api/readings/[id]/process/route.ts (Fase 5) -->

import { NextResponse, type NextRequest } from 'next/server'
import { createClient } from '@/lib/supabase/server'
import { analyzeReading } from '@/lib/anthropic/analyze'
import { findAllBoundaries, closeSections } from '@/lib/anthropic/parser'
import { runAudit } from '@/lib/anthropic/audit'
import { ENCERRAMENTO_LITERAL, type ReportJsonb, type RegenerationLogEntry } from '@/lib/anthropic/types'

export const runtime = 'nodejs'
export const maxDuration = 300

export async function POST(
  request: NextRequest,
  { params }: { params: Promise<{ id: string }> },
): Promise<Response> {
  const { id: readingId } = await params
  // ... auth gates → ReadableStream → finalize
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Wave-0 stub test file for Route Handler auth gates</name>
  <files>apps/web/app/api/readings/[id]/analyze/__tests__/route.test.ts</files>
  <read_first>
    - `.planning/phases/07-analise-llm/07-VALIDATION.md` line 60 (route.test.ts cases)
    - `.planning/phases/07-analise-llm/07-RESEARCH.md` lines 1054-1060 (D-S1, D-S4 test gates)
    - `apps/web/lib/rag/__tests__/search.test.ts` (analog: hoisted mocks pattern, vi.mock structure)
  </read_first>
  <behavior>
    Wave-0 stub com 8 it.todo cobrindo os 5 auth gates + Response shape + abort. Implementação completa fica como future work (integration testing complexity é alta para Anthropic stream mocking; esses cases são best validados via founder UAT). Cada it.todo descreve o caso para deixar trilha futura.
  </behavior>
  <action>
Criar `apps/web/app/api/readings/[id]/analyze/__tests__/route.test.ts`:

```typescript
/**
 * Wave-0 stubs for the analyze Route Handler.
 *
 * NOTE: Full integration tests with mocked Anthropic.messages.stream are
 * deferred to a follow-up. The 5 auth gates are validated by founder UAT
 * (07-UAT.md scenarios). These stubs document the contract.
 *
 * Source: 07-VALIDATION.md line 60, 07-RESEARCH.md lines 1054-1060.
 */
import { describe, it } from 'vitest'

describe('app/api/readings/[id]/analyze — auth gates (T-7-AUTH a-e)', () => {
  it.todo('401 quando session ausente (gate a)')
  it.todo('403 quando reading.therapist_id !== user.id (gate b)')
  it.todo('404 quando reading não existe')
  it.todo("409 quando reading.status !== 'ready' (gate c)")
  it.todo('409 quando reading.report_delivered IS NOT NULL (gate d)')
  it.todo('409 quando reading.regeneration_count >= 3 (gate e)')
})

describe('app/api/readings/[id]/analyze — Response shape (D-S1)', () => {
  it.todo('headers Content-Type: text/plain; charset=utf-8')
  it.todo('headers Cache-Control: no-cache, no-transform')
  it.todo('headers X-Content-Type-Options: nosniff')
  it.todo('body é ReadableStream of Uint8Array text deltas')
})

describe('app/api/readings/[id]/analyze — finalization (D-S2 + D-P3)', () => {
  it.todo('append ENCERRAMENTO_LITERAL em report_generated.encerramento_disclaimer')
  it.todo('UPDATE audit_metadata via runAudit pós-stream')
  it.todo('regeneration_count incrementado em sucesso (não em erro)')
  it.todo('regeneration_log append entry com {timestamp, model, latency, tokens, cost}')
})

describe('app/api/readings/[id]/analyze — params Promise (Pitfall 5)', () => {
  it.todo('await params before usando id (Next.js 15 contract)')
})

describe('app/api/readings/[id]/analyze — abort plumbing', () => {
  it.todo('request.signal.aborted dispara llmStream.controller.abort')
})
```

Esse arquivo serve como contrato de teste e Wave-0 stub; implementação real é founder UAT scenarios em 07-UAT.md (criado na próxima Fase de verify-work).
  </action>
  <verify>
    <automated>cd apps/web && pnpm test:run app/api/readings/\\[id\\]/analyze/__tests__/route.test.ts 2>&1 | tee /tmp/route-stub.txt; ! grep -qE "FAIL|×" /tmp/route-stub.txt && echo "OK: Wave-0 stub passes (all it.todo skip-pass)"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/api/readings/[id]/analyze/__tests__/route.test.ts` existe
    - Contém ≥ 8 chamadas `it.todo(`
    - Cobre todos os 5 auth gates (a, b, c, d, e) + Response shape + finalization
    - `pnpm test:run` exit 0; output mostra todos `it.todo` (skipped, 0 failed)
  </acceptance_criteria>
  <done>Stub criado; founder UAT vai validar os gates em scenarios reais.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author Route Handler streaming POST</name>
  <files>apps/web/app/api/readings/[id]/analyze/route.ts</files>
  <read_first>
    - `apps/web/app/api/readings/[id]/process/route.ts` (analog completo: imports, runtime, params Promise unwrap, auth gate, ownership check, status guard)
    - `apps/web/app/api/vision/webhook/route.ts` (analog para revalidatePath + structured logging)
    - `.planning/phases/07-analise-llm/07-RESEARCH.md` Pattern 2 lines 459-547 (Route Handler streaming complete code)
    - `.planning/phases/07-analise-llm/07-PATTERNS.md` lines 242-307 (analog process/route.ts; aplicar 5 gates a-e; NÃO usar createServiceClient — D-S1)
    - `apps/web/lib/anthropic/analyze.ts` (07-07 — `analyzeReading` interface)
    - `apps/web/lib/anthropic/parser.ts` (07-04 — `findAllBoundaries`, `closeSections`)
    - `apps/web/lib/anthropic/audit.ts` (07-05 — `runAudit`)
    - `apps/web/lib/anthropic/types.ts` (07-03 — `ENCERRAMENTO_LITERAL`, `ReportJsonb`, `RegenerationLogEntry`)
  </read_first>
  <action>
Criar `apps/web/app/api/readings/[id]/analyze/route.ts` com conteúdo verbatim:

```typescript
/**
 * POST /api/readings/[id]/analyze
 *
 * Streaming endpoint for Phase 7 LLM analysis. Owned by the Reading detail
 * page (`/leituras/[id]`); user fires "Gerar análise" button → fetch POST
 * → consume ReadableStream of text deltas → re-fetch on close.
 *
 * Auth gates (T-7-AUTH a-e):
 *   a) Session present (`auth.getUser()`)
 *   b) reading.therapist_id === user.id
 *   c) reading.status === 'ready'
 *   d) reading.report_delivered IS NULL (not yet delivered to client)
 *   e) reading.regeneration_count < 3 (D-S4 cap)
 *
 * Streaming (D-S1, D-S2):
 *   - Web Streams API: `Response(new ReadableStream({...}))`
 *   - Plain text/plain; charset=utf-8 + Transfer-Encoding: chunked
 *   - Section-boundary parser (07-04) detects each `^### N. ` and persists
 *     completed sections via UPDATE jsonb_set (D-S2 — 14 writes total)
 *   - ENCERRAMENTO_LITERAL appended server-side AFTER stream end (D-P3)
 *   - On error: partial save preserved; regeneration_count NOT incremented
 *     (D-S3 — infra failures don't punish the user)
 *
 * Phase 7 | Plan 07-08 | Decisions: D-S1, D-S2, D-S3, D-S4, D-P3, D-T1
 */
import { NextResponse, type NextRequest } from 'next/server'
import { revalidatePath } from 'next/cache'

import { createClient } from '@/lib/supabase/server'
import { analyzeReading } from '@/lib/anthropic/analyze'
import { findAllBoundaries, closeSections } from '@/lib/anthropic/parser'
import { runAudit } from '@/lib/anthropic/audit'
import { MODEL } from '@/lib/anthropic/client'
import {
  ENCERRAMENTO_LITERAL,
  type ReportJsonb,
  type RegenerationLogEntry,
} from '@/lib/anthropic/types'

export const runtime = 'nodejs'
export const maxDuration = 300

export async function POST(
  request: NextRequest,
  { params }: { params: Promise<{ id: string }> },
): Promise<Response> {
  const { id: readingId } = await params

  // Gate (a): session present
  const supabase = await createClient()
  const {
    data: { user },
    error: authError,
  } = await supabase.auth.getUser()
  if (!user || authError) {
    return NextResponse.json({ error: 'Unauthenticated' }, { status: 401 })
  }

  // Load reading + features (RLS + explicit therapist_id check)
  const { data: reading, error: readingError } = await supabase
    .from('readings')
    .select(
      'id, therapist_id, status, vision_features, report_delivered, regeneration_count, regeneration_log, client:clients(full_name, birth_date)',
    )
    .eq('id', readingId)
    .maybeSingle()

  if (readingError) {
    return NextResponse.json({ error: 'Database error' }, { status: 500 })
  }
  if (!reading) {
    return NextResponse.json({ error: 'Reading not found' }, { status: 404 })
  }
  // Gate (b): ownership
  if (reading.therapist_id !== user.id) {
    return NextResponse.json({ error: 'Forbidden' }, { status: 403 })
  }
  // Gate (c): status
  if (reading.status !== 'ready') {
    return NextResponse.json(
      { error: `Reading status '${reading.status}' is not ready for analysis` },
      { status: 409 },
    )
  }
  // Gate (d): not yet delivered
  if (reading.report_delivered != null) {
    return NextResponse.json(
      { error: 'Reading already delivered. Cannot regenerate.' },
      { status: 409 },
    )
  }
  // Gate (e): regen cap
  const currentCount = reading.regeneration_count ?? 0
  if (currentCount >= 3) {
    return NextResponse.json(
      { error: 'Regeneration limit reached (3/3). Edit manually instead.' },
      { status: 409 },
    )
  }

  // Compute client age for prompt injection
  const clientName = (reading.client as { full_name?: string } | null)?.full_name ?? 'Cliente'
  const clientBirth = (reading.client as { birth_date?: string } | null)?.birth_date ?? null
  const clientAge = clientBirth
    ? Math.floor((Date.now() - new Date(clientBirth).getTime()) / 31_557_600_000)
    : null

  // Open the analysis stream
  const analysis = await analyzeReading({
    readingId,
    therapistId: user.id,
    visionFeatures: reading.vision_features as Record<string, unknown> & {
      constitution: { primary: string; secondary?: string }
    },
    clientName,
    clientAge,
    therapistNotes: null, // Phase 7 doesn't surface notes; Fase 9 polish may add
    irisMap: 'jensen',
    signal: request.signal,
  })

  // Web Streams shell — drives Anthropic deltas to client + DB
  const encoder = new TextEncoder()
  const completedSections: ReportJsonb = {}
  let buffer = ''
  let lastBoundaryCount = 0

  const responseStream = new ReadableStream({
    async start(controller) {
      try {
        for await (const text of analysis.stream) {
          buffer += text
          controller.enqueue(encoder.encode(text))

          // Detect new boundaries — only trigger persistence when section CLOSES
          const boundaries = findAllBoundaries(buffer)
          if (boundaries.length > lastBoundaryCount) {
            // Close all sections except the latest (still open)
            const closed = closeSections(boundaries.slice(0, -1), buffer)
            for (const section of closed) {
              if (completedSections[section.key] !== section.content) {
                completedSections[section.key] = section.content
                // Atomic write of just this key via jsonb_set
                await supabase
                  .from('readings')
                  .update({
                    report_generated: { ...completedSections },
                  })
                  .eq('id', readingId)
              }
            }
            lastBoundaryCount = boundaries.length
          }
        }

        // Stream ended — close last section
        const finalBoundaries = findAllBoundaries(buffer)
        const allClosed = closeSections(finalBoundaries, buffer)
        for (const section of allClosed) {
          completedSections[section.key] = section.content
        }

        // D-P3 server-appended encerramento
        completedSections.encerramento_disclaimer = ENCERRAMENTO_LITERAL

        // Finalize: usage + audit + regeneration_log
        const finalization = await analysis.finalize()
        const audit = runAudit(completedSections)

        const logEntry: RegenerationLogEntry = {
          timestamp: new Date().toISOString(),
          therapist_id: user.id,
          reading_id: readingId,
          model_version: MODEL,
          latency_ms: finalization.latency_ms,
          tokens_in: finalization.usage.input_tokens,
          tokens_out: finalization.usage.output_tokens,
          cache_creation_input_tokens: finalization.usage.cache_creation_input_tokens,
          cache_read_input_tokens: finalization.usage.cache_read_input_tokens,
          cost_estimate_usd: finalization.cost_estimate_usd,
        }

        const existingLog = Array.isArray(reading.regeneration_log)
          ? (reading.regeneration_log as unknown as RegenerationLogEntry[])
          : []

        await supabase
          .from('readings')
          .update({
            report_generated: completedSections,
            report_generated_at: new Date().toISOString(),
            regeneration_count: currentCount + 1,
            regeneration_log: [...existingLog, logEntry] as unknown as never,
            audit_metadata: audit as unknown as never,
          })
          .eq('id', readingId)

        revalidatePath(`/leituras/${readingId}`)
        revalidatePath(`/leituras/${readingId}/editar`)
        revalidatePath('/leituras')

        controller.close()
      } catch (err) {
        // D-S3: do NOT increment regeneration_count on error
        console.error(
          '[analyze] stream error reading=' + readingId + ' err=',
          err instanceof Error ? err.message : 'unknown',
        )
        try {
          // Persist partial sections that were already closed mid-stream
          if (Object.keys(completedSections).length > 0) {
            await supabase
              .from('readings')
              .update({ report_generated: completedSections })
              .eq('id', readingId)
          }
          controller.enqueue(
            encoder.encode(
              '\n\n[erro]: ' + (err instanceof Error ? err.message : 'desconhecido'),
            ),
          )
        } finally {
          controller.close()
        }
      }
    },

    async cancel() {
      // Caller (browser) aborted the fetch → analyze.ts AbortSignal cascades
      // to llmStream.controller.abort() (already wired in analyze.ts)
      console.info('[analyze] stream cancelled by caller reading=' + readingId)
    },
  })

  return new Response(responseStream, {
    headers: {
      'Content-Type': 'text/plain; charset=utf-8',
      'Cache-Control': 'no-cache, no-transform',
      'X-Content-Type-Options': 'nosniff',
    },
  })
}
```

**Sub-task 2b — Comment-only edit em `apps/web/app/api/vision/webhook/route.ts`:**

Ler o arquivo atual e localizar as linhas 14-17 (comment block que menciona `ai_report_edited`). Atualizar para:

```typescript
 *     Late retries from Modal find `status='ready'` or `'failed'` and no-op.
 *     Phase 7 edits move status to `'edited'` and write to `report_delivered`
 *     jsonb (the legacy `ai_report_edited` text view is now a GENERATED column
 *     reconstructed from report_delivered — see migration 0007).
 *   - DEFENSE-IN-DEPTH: ...
```

Apenas comment update — zero code change. Verifica via `git diff` que apenas as linhas de comment foram tocadas.
  </action>
  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit 2>&1 | grep -E "analyze/route\\.ts.*error" | head -5; pnpm audit:vocabulary 2>&1 | tail -3; node -e "const f=require('fs').readFileSync('apps/web/app/api/readings/[id]/analyze/route.ts','utf8'); const checks={'runtime nodejs':/export const runtime = 'nodejs'/.test(f),'maxDuration 300':/export const maxDuration = 300/.test(f),'5 gates':/Unauthenticated/.test(f) && /Forbidden/.test(f) && /not ready for analysis/.test(f) && /Reading already delivered/.test(f) && /Regeneration limit reached/.test(f),'NOT createServiceClient':!/createServiceClient/.test(f),'ReadableStream':/new ReadableStream/.test(f),'analyzeReading':/analyzeReading\\(/.test(f),'findAllBoundaries':/findAllBoundaries/.test(f),'ENCERRAMENTO_LITERAL':/ENCERRAMENTO_LITERAL/.test(f),'runAudit':/runAudit/.test(f),'request.signal':/request\\.signal/.test(f),'revalidatePath':/revalidatePath/.test(f) && (f.match(/revalidatePath/g)||[]).length>=2,'NO regen on error':/D-S3.*do NOT/i.test(f),'no PII (basic — full scan in chained command below)':true}; for(const[k,v]of Object.entries(checks))console.log(v?'OK':'FAIL',k); process.exit(Object.values(checks).every(Boolean)?0:1);" && node -e "const f=require('fs').readFileSync('apps/web/app/api/readings/[id]/analyze/route.ts','utf8'); const m=f.match(/console\\.(info|error|warn|log)\\([^;]*\\)/gs)||[]; const bad=m.find(c => /clientName|therapistNotes|reportContent|client_name|therapist_notes/.test(c)); if(bad){console.error('PII leak in console call:', bad); process.exit(1);} console.log('OK PII scan: '+m.length+' console calls clean');"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/api/readings/[id]/analyze/route.ts` existe, ≥ 200 linhas
    - Declara `export const runtime = 'nodejs'` E `export const maxDuration = 300`
    - Implementa 5 auth gates: 401 (no session), 403 (wrong therapist), 404 (not found), 409×3 (status, delivered, regen-cap)
    - NÃO usa `createServiceClient` (D-S1 — session client only)
    - Usa `Response(new ReadableStream({ async start(controller) {...} }))` com headers `text/plain; charset=utf-8` + `no-cache, no-transform` + `nosniff`
    - Importa e chama `analyzeReading`, `findAllBoundaries`, `closeSections`, `runAudit`, `ENCERRAMENTO_LITERAL`
    - Section-boundary persistence: chama `findAllBoundaries(buffer)` no for-await, escreve via `.update({ report_generated: {...} })` quando seção FECHA (boundary count cresceu)
    - `ENCERRAMENTO_LITERAL` é appendado APÓS stream end (D-P3 server-controlled, não LLM-generated)
    - Em erro mid-stream: NÃO incrementa `regeneration_count`; preserva `report_generated` parcial; enqueue `\n\n[erro]: ...` inline; controller.close()
    - `revalidatePath` chamado ≥ 2× pós-stream (`/leituras/[id]`, `/leituras/[id]/editar`, `/leituras`)
    - `console.error` NÃO contém `clientName` ou `therapistNotes` (sem PII)
    - Comment-only edit em `app/api/vision/webhook/route.ts` linhas 14-17: `ai_report_edited` substituído por menção a `report_delivered` jsonb
    - `pnpm tsc --noEmit` apps/web exit 0 (sem novos erros para analyze/route.ts)
    - `pnpm audit:vocabulary` exit 0
  </acceptance_criteria>
  <done>Route Handler funcional com 5 gates + Web Streams + section-boundary persistence + audit + telemetry; webhook comment refresh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser fetch → Route Handler POST | HTTPS via Vercel; cookie session auth |
| Route Handler → Anthropic SDK | Server-side; API key em env |
| Route Handler → Supabase RLS | Session client; therapist_id explicit check + RLS auto |
| `request.signal` → AbortSignal | User fechando tab dispara abort cascade |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-AUTH | Elevation of Privilege | Cross-therapist analysis | mitigate | 5 gates explicit (a-e); RLS auto-enforced; line-by-line `reading.therapist_id !== user.id` check returns 403; analyze.ts NUNCA chamado sem todas gates passando |
| T-7-COST | Denial-of-Service / Repudiation | Regeneration abuse drains credits | mitigate | Hard cap regeneration_count >= 3 → 409; CHECK constraint no schema (07-01) é defesa em profundidade; D-S3 erro não incrementa para impedir bug-induced infinite-retry burn |
| T-7-DRIFT | Tampering | LLM omits or paraphrases disclaimer | mitigate | `ENCERRAMENTO_LITERAL` appendado server-side AFTER stream completes (D-P3); SC4 garantido independente de prompt drift |
| T-7-RACE | Tampering | Concurrent regenerations on same reading | accept | Cap=3 + delivered-IS-NULL guard + jsonb append regeneration_log atômico via supabase.update; race window é small (founder não dispara 2× simultaneamente normalmente); last-writer-wins é tolerável dado os outros guards |
| T-7-INJECTION | Tampering | User-controlled vision_features in prompt | mitigate | vision_features é server-generated (Fase 5 pipeline); JSON.stringify code-fenced; Sonnet system+user role separation; Princípio 2 system prompt forbids inventing signs |
| T-7-LGPD | Information Disclosure | console.error leaks PII | mitigate | console.error contém apenas `reading=${readingId}` + `err.message` (Anthropic error doesn't include PII); NUNCA loga client name/notes/report text |
| T-7-XSS | Tampering | Browser receives unsanitized text | mitigate | `Content-Type: text/plain` + `X-Content-Type-Options: nosniff`; client renderização markdown via `react-markdown` (default disable HTML); LLM output não vai para `<div dangerouslySetInnerHTML>` em lugar nenhum |
| T-7-PARTIAL | Tampering | Stream interrupted, partial save corrupts | accept | D-S2 explicitly aceita partial save como recoverable state; UI detecta `report_generated.encerramento_disclaimer == null` → mostra "Geração incompleta — N/13 seções" + "Continuar geração" CTA (UI-SPEC §State C error fallback) |
</threat_model>

<verification>
- Route Handler com runtime=nodejs + maxDuration=300
- 5 auth gates implementados (401/403/404/409/409/409)
- Web Streams API com headers corretos
- Section-boundary persistence via parser.ts
- ENCERRAMENTO_LITERAL server-appended D-P3
- Audit + telemetry pós-stream
- AbortSignal plumbing (cancel callback + analyze.ts addEventListener)
- Erro NÃO incrementa regen count (D-S3)
- revalidatePath chamado para 3 paths
- Webhook comment refresh apply
- tsc + audit clean
</verification>

<success_criteria>
- Route Handler é o único entry point HTTP para Anthropic streaming na Fase 7
- Todos os 5 auth gates testáveis em founder UAT
- Section-boundary persistence atomic (não corrompe jsonb com partial writes)
- Disclaimer literal SC4 garantido server-side
- Cost cap server-enforced (T-7-COST)
- Comment-only update em webhook route preserva D-T4 contract da Fase 5 com nova column name
</success_criteria>

<output>
After completion, create `.planning/phases/07-analise-llm/07-08-SUMMARY.md` documenting:
1. Lista exata dos 5 gates com status code de cada
2. Sequence diagram resumido: client → POST → analyze.ts → Anthropic stream → buffer/parser → DB UPDATE → finalize → audit → final UPDATE → close
3. Performance estimativa: # de DB writes por análise = 13 (mid-stream UPDATEs por seção fechando) + 1 (final UPDATE com encerramento + audit + regeneration_log) = 14 writes total ≈ ~10ms each = ~140ms DB overhead em 30-60s de stream
4. Decisão: comment update em webhook foi single-line edit (linhas 14-17 mostradas no diff)
</output>
