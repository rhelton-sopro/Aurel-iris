---
phase: 07-analise-llm
plan: 07
type: execute
wave: 4
depends_on: [07-03, 07-04, 07-05, 07-06]
files_modified:
  - apps/web/lib/anthropic/analyze.ts
  - apps/web/lib/rag/__tests__/section-queries.test.ts
autonomous: true
requirements: [LLM-01, LLM-02, LLM-03]
tags: [phase-7, analyze, orchestrator, streaming, server-only]

must_haves:
  truths:
    - "`apps/web/lib/anthropic/analyze.ts` declara `import 'server-only'`"
    - "Exporta `analyzeReading({ readingId, visionFeatures, clientName, clientAge, therapistNotes })` que retorna `{ stream: ReadableStream<Uint8Array>; finalize: () => Promise<{ completedSections: ReportJsonb; usage: AnthropicUsage; latencyMs: number }> }`"
    - "Chama `retrieveRelevantKnowledge({ features, reportSections: REPORT_SECTIONS })` em paralelo com `loadSystemPrompt()` + `loadInjectionTemplate()` via `Promise.all`"
    - "Renderiza prompt user via `renderInjection(template, vars)` com `client_name`, `age`, `therapist_notes`, `iris_map`, `vision_features_json`, `rag_chunks_concatenated_with_citations`"
    - "Chama `anthropicClient.messages.stream({ model: MODEL, max_tokens: MAX_OUTPUT_TOKENS, system: [{ type: 'text', text, cache_control: DEFAULT_SYSTEM_CACHE_CONTROL }], messages: [{ role: 'user', content }] })` (Pattern 1 RESEARCH)"
    - "Concatena chunks RAG no formato `[${c.source_book}, p.${c.page ?? '?'}] ${c.text}` separados por `\\n\\n`"
    - "Telemetria `console.info({ event: 'llm_generate', reading_id, therapist_id, model_version, n_chunks_rag, latency_ms, tokens_in, tokens_out, cache_creation_input_tokens, cache_read_input_tokens, cost_estimate_usd, anchor_rate_pct, forbidden_vocab_count, sections_completed })` SEM PII (D-T1)"
    - "Test em `lib/rag/__tests__/section-queries.test.ts` flipado: assert que `REPORT_SECTIONS` (de analyze.ts) ⊆ keys de `SECTION_QUERY_TEMPLATES` — D-PR2 frozen contract CI gate"
  artifacts:
    - path: "apps/web/lib/anthropic/analyze.ts"
      provides: "Orchestration: load prompts + RAG + Anthropic stream + buffer parser"
      min_lines: 130
      contains: "retrieveRelevantKnowledge"
    - path: "apps/web/lib/rag/__tests__/section-queries.test.ts"
      provides: "D-PR2 CI gate test: every analyze slug exists in SECTION_QUERY_TEMPLATES"
      min_lines: 25
  key_links:
    - from: "apps/web/lib/anthropic/analyze.ts"
      to: "lib/rag/search.ts retrieveRelevantKnowledge"
      via: "frozen Fase 6 contract D-R1"
      pattern: "retrieveRelevantKnowledge\\(\\{"
    - from: "apps/web/lib/anthropic/analyze.ts"
      to: "REPORT_SECTIONS from types.ts"
      via: "D-PR2 frozen 7-slug array"
      pattern: "REPORT_SECTIONS"
    - from: "apps/web/lib/rag/__tests__/section-queries.test.ts"
      to: "SECTION_QUERY_TEMPLATES record"
      via: "import + assert subset"
      pattern: "SECTION_QUERY_TEMPLATES"
---

<objective>
Construir o orchestrator `analyze.ts` — função pura server-side que dado um `vision_features` jsonb + client context, executa em paralelo (load prompts + RAG retrieval), monta o prompt user via mustache, abre o stream Anthropic com `cache_control: ephemeral`, e devolve um `{ stream, finalize }` para o Route Handler consumir. Preencher o D-PR2 CI gate em `lib/rag/__tests__/section-queries.test.ts`.

Purpose: Sem `analyze.ts`, o Route Handler vira um Frankenstein de 300 linhas misturando stream parsing + RAG + audit + DB writes. `analyze.ts` é a casca limpa: consome contratos da Fase 6 (frozen), produz contrato simples para a casca HTTP. Test D-PR2 garante que slugs nunca driftam silenciosamente.
Output: `analyze.ts` server-only + 1 test D-PR2 GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-analise-llm/07-CONTEXT.md
@.planning/phases/07-analise-llm/07-RESEARCH.md
@.planning/phases/07-analise-llm/07-PATTERNS.md
@apps/web/lib/anthropic/types.ts
@apps/web/lib/anthropic/client.ts
@apps/web/lib/anthropic/prompts.ts
@apps/web/lib/rag/search.ts
@apps/web/lib/rag/section-queries.ts
@apps/web/lib/rag/types.ts
@SPEC.md

<interfaces>
<!-- analyze.ts shape — extracted from RESEARCH §Pattern 1 + §Pattern 2 -->

import 'server-only'
import { anthropicClient, MODEL, DEFAULT_SYSTEM_CACHE_CONTROL, MAX_OUTPUT_TOKENS, estimateCostUsd } from './client'
import { loadSystemPrompt, loadInjectionTemplate, renderInjection } from './prompts'
import { REPORT_SECTIONS, ENCERRAMENTO_LITERAL, type ReportJsonb } from './types'
import { retrieveRelevantKnowledge } from '@/lib/rag/search'
import type { IrisFeaturesForRag } from '@/lib/rag/build-queries'

export interface AnalyzeArgs {
  readingId: string  // for telemetry only — no PII
  therapistId: string  // for telemetry only — no PII
  visionFeatures: IrisFeaturesForRag & Record<string, unknown>
  clientName: string  // for prompt injection only — never logged
  clientAge: number | null
  therapistNotes: string | null
  irisMap?: 'jensen' | 'jausas' | 'hidalgo'
}

export interface AnalyzeResult {
  /** Async iterable that yields LLM text deltas. */
  stream: AsyncIterable<string>
  /** Resolves to final usage + latency once stream ends. */
  finalize: () => Promise<AnalyzeFinalization>
}

export interface AnalyzeFinalization {
  usage: {
    input_tokens: number
    output_tokens: number
    cache_creation_input_tokens: number
    cache_read_input_tokens: number
  }
  latency_ms: number
  cost_estimate_usd: number
  n_chunks_rag: number
}

export async function analyzeReading(args: AnalyzeArgs): Promise<AnalyzeResult> { ... }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author lib/anthropic/analyze.ts orchestrator</name>
  <files>apps/web/lib/anthropic/analyze.ts</files>
  <read_first>
    - `apps/web/lib/rag/search.ts` lines 1-60 (banner pattern, imports, server-only, telemetria pattern lines 209-216)
    - `apps/web/lib/rag/build-queries.ts` (interface `IrisFeaturesForRag` — required input shape para RAG)
    - `.planning/phases/07-analise-llm/07-RESEARCH.md` Pattern 1 lines 366-411 (Anthropic stream code), Pattern 2 lines 459-547 (Route Handler code consumindo isso)
    - `.planning/phases/07-analise-llm/07-CONTEXT.md` lines 144-146 (D-PR1, D-PR2)
    - `.planning/phases/07-analise-llm/07-PATTERNS.md` lines 73-122 (analog `lib/rag/search.ts`; auth-gate ELE NÃO é responsabilidade do analyze.ts — fica no Route Handler — porque analyze.ts pode ser chamado também de scripts admin)
  </read_first>
  <action>
Criar `apps/web/lib/anthropic/analyze.ts` com conteúdo verbatim:

```typescript
/**
 * `analyzeReading` — orchestrates a Phase 7 LLM analysis call.
 *
 * Composition (CONTEXT D-S1, D-S2, D-PR1, D-PR2, D-T1, RESEARCH §Pattern 1, §Pattern 2):
 *   1. Parallel: load system.md + feature-injection.md from FS (cached)
 *      + retrieveRelevantKnowledge(features, REPORT_SECTIONS) (frozen Fase 6)
 *   2. Render user content via mustache substitution
 *   3. Open Anthropic stream with cache_control: ephemeral on system block
 *   4. Return { stream: AsyncIterable<string>, finalize: () => Promise<...> }
 *
 * Auth gate is NOT here — it's the caller's (Route Handler) responsibility.
 * This module is invoked by `app/api/readings/[id]/analyze/route.ts` AFTER
 * the route validates session + ownership + status + regen-cap.
 *
 * LGPD: telemetry log explicitly excludes client_name + therapist_notes.
 * Logs only UUIDs + counts + latencies + token counts (D-T1).
 *
 * Phase 7 | Plan 07-07 | Decisions: D-PR1, D-PR2, D-S1, D-S2, D-T1
 */
import 'server-only'

import {
  anthropicClient,
  MODEL,
  DEFAULT_SYSTEM_CACHE_CONTROL,
  MAX_OUTPUT_TOKENS,
  estimateCostUsd,
} from './client'
import {
  loadSystemPrompt,
  loadInjectionTemplate,
  renderInjection,
} from './prompts'
import { REPORT_SECTIONS } from './types'
import { retrieveRelevantKnowledge } from '@/lib/rag/search'
import type { IrisFeaturesForRag } from '@/lib/rag/build-queries'
import type { KnowledgeChunkRow } from '@/lib/rag/types'

export interface AnalyzeArgs {
  readingId: string
  therapistId: string
  visionFeatures: IrisFeaturesForRag & Record<string, unknown>
  clientName: string
  clientAge: number | null
  therapistNotes: string | null
  irisMap?: 'jensen' | 'jausas' | 'hidalgo'
  /** Optional AbortSignal — Route Handler passes request.signal here. */
  signal?: AbortSignal
}

export interface AnalyzeFinalization {
  usage: {
    input_tokens: number
    output_tokens: number
    cache_creation_input_tokens: number
    cache_read_input_tokens: number
  }
  latency_ms: number
  cost_estimate_usd: number
  n_chunks_rag: number
}

export interface AnalyzeResult {
  stream: AsyncIterable<string>
  finalize: () => Promise<AnalyzeFinalization>
}

function concatChunksForKnowledge(chunks: KnowledgeChunkRow[]): string {
  return chunks
    .map((c) => `[${c.source_book}, p.${c.page ?? '?'}] ${c.text}`)
    .join('\n\n')
}

/**
 * Main entry point. Returns immediately with a stream + finalize promise.
 * Caller (Route Handler) drives the stream into a Web ReadableStream and
 * awaits finalize() AFTER the stream loop completes.
 */
export async function analyzeReading(args: AnalyzeArgs): Promise<AnalyzeResult> {
  const startedAt = Date.now()

  // 1. Parallel: prompts + RAG (D-PR2 frozen contract — passes REPORT_SECTIONS)
  const [systemPrompt, injectionTemplate, ragChunks] = await Promise.all([
    Promise.resolve(loadSystemPrompt()),
    Promise.resolve(loadInjectionTemplate()),
    retrieveRelevantKnowledge({
      features: args.visionFeatures,
      reportSections: REPORT_SECTIONS,
    }),
  ])

  // 2. Render user content
  const userContent = renderInjection(injectionTemplate, {
    client_name: args.clientName,
    age: args.clientAge != null ? String(args.clientAge) : '',
    therapist_notes: args.therapistNotes ?? '',
    iris_map: args.irisMap ?? 'jensen',
    vision_features_json: JSON.stringify(args.visionFeatures, null, 2),
    rag_chunks_concatenated_with_citations: concatChunksForKnowledge(ragChunks),
  })

  // 3. Open Anthropic stream
  const llmStream = anthropicClient.messages.stream({
    model: MODEL,
    max_tokens: MAX_OUTPUT_TOKENS,
    system: [
      {
        type: 'text',
        text: systemPrompt,
        cache_control: DEFAULT_SYSTEM_CACHE_CONTROL,
      },
    ],
    messages: [{ role: 'user', content: userContent }],
  })

  // 4. Wire AbortSignal if provided
  if (args.signal) {
    args.signal.addEventListener('abort', () => {
      try {
        llmStream.controller.abort()
      } catch {
        // already ended — no-op
      }
    })
  }

  // Forward text deltas as plain strings — caller does buffer + parsing.
  async function* toTextStream(): AsyncIterable<string> {
    for await (const event of llmStream) {
      if (event.type === 'content_block_delta' && event.delta.type === 'text_delta') {
        yield event.delta.text
      }
    }
  }

  async function finalize(): Promise<AnalyzeFinalization> {
    const final = await llmStream.finalMessage()
    const usage = {
      input_tokens: final.usage.input_tokens ?? 0,
      output_tokens: final.usage.output_tokens ?? 0,
      cache_creation_input_tokens: final.usage.cache_creation_input_tokens ?? 0,
      cache_read_input_tokens: final.usage.cache_read_input_tokens ?? 0,
    }
    const latencyMs = Date.now() - startedAt
    const cost = estimateCostUsd(usage)

    // D-T1 telemetry — NO PII (no clientName, no therapistNotes, no report text)
    console.info({
      event: 'llm_generate',
      reading_id: args.readingId,
      therapist_id: args.therapistId,
      model_version: MODEL,
      n_chunks_rag: ragChunks.length,
      latency_ms: latencyMs,
      tokens_in: usage.input_tokens,
      tokens_out: usage.output_tokens,
      cache_creation_input_tokens: usage.cache_creation_input_tokens,
      cache_read_input_tokens: usage.cache_read_input_tokens,
      cost_estimate_usd: Number(cost.toFixed(4)),
    })

    return {
      usage,
      latency_ms: latencyMs,
      cost_estimate_usd: cost,
      n_chunks_rag: ragChunks.length,
    }
  }

  return {
    stream: toTextStream(),
    finalize,
  }
}
```

**Verificação imediata:**
```bash
cd apps/web
pnpm tsc --noEmit
# Esperado: zero novos erros para analyze.ts
pnpm audit:vocabulary
# Esperado: exit 0 (analyze.ts não cita vocabulário proibido)
```
  </action>
  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit 2>&1 | grep -E "analyze\\.ts.*error" | head -5; echo "---"; pnpm audit:vocabulary 2>&1 | tail -3; echo "---"; node -e "const f=require('fs').readFileSync('apps/web/lib/anthropic/analyze.ts','utf8'); const checks={'server-only':f.startsWith(\"/**\")&&/import 'server-only'/.test(f),'retrieveRelevantKnowledge':/retrieveRelevantKnowledge\\(\\{/.test(f),'REPORT_SECTIONS':/REPORT_SECTIONS/.test(f),'cache_control':/DEFAULT_SYSTEM_CACHE_CONTROL/.test(f),'console.info no PII':!/clientName/.test(f.split('console.info')[1]?.split('})')[0]||'')&&!/therapistNotes/.test(f.split('console.info')[1]?.split('})')[0]||''),'AbortSignal':/llmStream\\.controller\\.abort/.test(f),'mustache vars':/vision_features_json/.test(f) && /rag_chunks_concatenated_with_citations/.test(f)}; for(const[k,v]of Object.entries(checks))console.log(v?'OK':'FAIL',k); process.exit(Object.values(checks).every(Boolean)?0:1);"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/lib/anthropic/analyze.ts` existe, ≥ 130 linhas
    - Primeira import statement: `import 'server-only'`
    - Importa `retrieveRelevantKnowledge` de `@/lib/rag/search` (Fase 6 frozen contract)
    - Importa `REPORT_SECTIONS` de `./types` (D-PR2 7-slug array)
    - Chama `retrieveRelevantKnowledge({ features, reportSections: REPORT_SECTIONS })` em `Promise.all`
    - Usa `anthropicClient.messages.stream({...})` com `cache_control: DEFAULT_SYSTEM_CACHE_CONTROL` no system block
    - Mustache substitution com 6 vars: `client_name`, `age`, `therapist_notes`, `iris_map`, `vision_features_json`, `rag_chunks_concatenated_with_citations`
    - `console.info({ event: 'llm_generate', … })` NÃO contém `clientName`, `therapistNotes`, ou texto do relatório (apenas UUIDs + counts + latencies + token buckets + cost)
    - `signal?: AbortSignal` plumbing chama `llmStream.controller.abort()` quando signal aborted
    - `pnpm tsc --noEmit` apps/web exit 0 (sem novos erros para analyze.ts)
    - `pnpm audit:vocabulary` exit 0 (sem regressão)
  </acceptance_criteria>
  <done>Orchestrator commitado, ready para Route Handler consumir.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Flip lib/rag/__tests__/section-queries.test.ts (D-PR2 CI gate)</name>
  <files>apps/web/lib/rag/__tests__/section-queries.test.ts</files>
  <read_first>
    - `apps/web/lib/rag/section-queries.ts` (export `SECTION_QUERY_TEMPLATES` Record com 7 keys)
    - `apps/web/lib/anthropic/types.ts` (export `REPORT_SECTIONS` array com 7 slugs)
    - `.planning/phases/07-analise-llm/07-CONTEXT.md` D-PR2 line 146 ("CI gate como teste unitário em `lib/rag/section-queries.test.ts` que importa `lib/anthropic/analyze.ts` e valida que todo slug passado existe no Record do template")
    - `apps/web/lib/rag/__tests__/section-queries.test.ts` (Wave-0 stub criado em 07-02)
  </read_first>
  <behavior>
    - Test único: `REPORT_SECTIONS` array (de `lib/anthropic/types.ts` ou de `lib/anthropic/analyze.ts` — ambos exportam) é subset (ou igual) das keys de `SECTION_QUERY_TEMPLATES` (de `lib/rag/section-queries.ts`)
    - Falha alta e clara se algum slug em `REPORT_SECTIONS` não tem template em `SECTION_QUERY_TEMPLATES`
  </behavior>
  <action>
Substituir o conteúdo de `apps/web/lib/rag/__tests__/section-queries.test.ts`:

```typescript
/**
 * D-PR2 frozen contract CI gate (Phase 7 + Phase 6 cross-reference).
 *
 * `analyze.ts` chama `retrieveRelevantKnowledge({ reportSections: REPORT_SECTIONS })`.
 * `SECTION_QUERY_TEMPLATES` em `lib/rag/section-queries.ts` é um Record com chaves
 * = todos os slugs aceitos. Se REPORT_SECTIONS adicionar um slug sem template,
 * o retrieval retorna queries Família B vazias para essa seção (silently broken).
 *
 * Esse teste falha LOUD quando o set REPORT_SECTIONS desvia do Record.
 *
 * Source: 07-CONTEXT.md D-PR2.
 */
import { describe, it, expect } from 'vitest'
import { SECTION_QUERY_TEMPLATES } from '../section-queries'
import { REPORT_SECTIONS } from '@/lib/anthropic/types'

describe('D-PR2 frozen contract: REPORT_SECTIONS ⊆ SECTION_QUERY_TEMPLATES', () => {
  it('every slug REPORT_SECTIONS passes is a key in SECTION_QUERY_TEMPLATES', () => {
    const recordKeys = new Set(Object.keys(SECTION_QUERY_TEMPLATES))
    const missing: string[] = []
    for (const slug of REPORT_SECTIONS) {
      if (!recordKeys.has(slug)) missing.push(slug)
    }
    expect(missing).toEqual([])
  })

  it('REPORT_SECTIONS tem exatamente 7 slugs (Fase 6 D-R2B contract)', () => {
    expect(REPORT_SECTIONS.length).toBe(7)
  })

  it('SECTION_QUERY_TEMPLATES tem >= 7 keys (pode adicionar mais sem quebrar)', () => {
    expect(Object.keys(SECTION_QUERY_TEMPLATES).length).toBeGreaterThanOrEqual(7)
  })

  it('cada template em SECTION_QUERY_TEMPLATES retorna array com >= 1 query string', () => {
    const dummyFeatures = { constitution: { primary: 'linfática' } }
    for (const slug of REPORT_SECTIONS) {
      const template = SECTION_QUERY_TEMPLATES[slug]
      expect(template).toBeDefined()
      const queries = template!(dummyFeatures)
      expect(Array.isArray(queries)).toBe(true)
      expect(queries.length).toBeGreaterThanOrEqual(1)
      expect(queries.every((q) => typeof q === 'string' && q.length > 0)).toBe(true)
    }
  })
})
```

**Verificação:**
```bash
cd apps/web
pnpm test:run lib/rag/__tests__/section-queries.test.ts
# Esperado: 4 passed, 0 failed.
```
  </action>
  <verify>
    <automated>cd apps/web && pnpm test:run lib/rag/__tests__/section-queries.test.ts 2>&1 | tee /tmp/sq.txt; ! grep -qE "FAIL|×" /tmp/sq.txt && echo "OK: section-queries D-PR2 gate green"; grep -E "passed" /tmp/sq.txt</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/lib/rag/__tests__/section-queries.test.ts` contém ≥ 4 chamadas `it(`
    - Importa `SECTION_QUERY_TEMPLATES` de `'../section-queries'` E `REPORT_SECTIONS` de `'@/lib/anthropic/types'`
    - Test 1 valida `REPORT_SECTIONS ⊆ Object.keys(SECTION_QUERY_TEMPLATES)` (set difference == [])
    - Test 4 valida que cada template é uma function que retorna `string[]` não-vazio
    - `pnpm test:run lib/rag/__tests__/section-queries.test.ts` exit 0; ≥ 4 passed
  </acceptance_criteria>
  <done>D-PR2 CI gate funcional; qualquer drift futuro entre lib/anthropic/types.ts REPORT_SECTIONS e lib/rag/section-queries.ts SECTION_QUERY_TEMPLATES é detectado loud.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `vision_features` jsonb → mustache prompt injection | User-controlled JSON injetado em prompt LLM (T-7-INJECTION); risco de prompt injection mitigado por: (a) vision_features é gerado por server (Fase 5 pipeline), não user input direto; (b) injetado como JSON.stringify code-fenced, não free text; (c) Sonnet system+user role separation bounds blast radius |
| `client_name` / `therapist_notes` → telemetria | NUNCA loggados (D-T1) — apenas UUIDs |
| RAG chunks → prompt | Chunks vêm da base curada Fase 6 — auditados via `audit:vocabulary:db` (D-N6) |
| Anthropic stream controller → AbortSignal | Plumbing permite Route Handler abortar quando user fecha tab |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-INJECTION | Tampering | `vision_features` JSON injected into LLM prompt | mitigate | Server-generated JSON (Fase 5 pipeline output, not user free text); JSON.stringify produces code-fenced structure; system prompt SPEC §6 Princípio 2 explicitly forbids LLM from inventing signs not in features; mustache regex `[\w_]+` only — no regex injection from user vars |
| T-7-LGPD | Information Disclosure | client_name leaks to telemetry | mitigate | Telemetry block explicitly omits `clientName` and `therapistNotes`; only UUIDs (reading_id, therapist_id) logged; D-T1 contract documented |
| T-7-COST | Denial-of-Service | Stream runs to max_tokens after user closes tab | mitigate | `args.signal?.addEventListener('abort', () => llmStream.controller.abort())` plumbing; Route Handler wires `request.signal`; cost bounded by max_tokens=16000 even sem abort |
| T-7-DRIFT | Tampering | REPORT_SECTIONS adds slug not in templates | mitigate | D-PR2 CI gate test in section-queries.test.ts fails loud on drift; cross-reference comments in both files |
| T-7-CACHE | Information Disclosure | Cache silently disabled | mitigate | `cache_control: DEFAULT_SYSTEM_CACHE_CONTROL` set; `loadSystemPrompt` warns if < 2200 tokens (07-03); telemetry tracks cache_creation_input_tokens + cache_read_input_tokens (07-03 client.ts) |
</threat_model>

<verification>
- `analyze.ts` declara `import 'server-only'`
- Chama `retrieveRelevantKnowledge` com `REPORT_SECTIONS` (D-PR2 frozen)
- Stream Anthropic com `cache_control: ephemeral` + `max_tokens: 16000`
- Telemetria sem PII (apenas UUIDs + counts)
- AbortSignal plumbing presente
- `tsc --noEmit` + `audit:vocabulary` clean
- D-PR2 CI gate test 4+ passed
</verification>

<success_criteria>
- `analyzeReading` é o único entry point para Anthropic streaming
- Route Handler (07-08) consome `{ stream, finalize }` sem reimplementar lógica de prompt/RAG
- D-PR2 CI gate funcional
- Telemetria estruturada D-T1 sem PII
- Cost estimator integra com regeneration_log (07-08)
</success_criteria>

<output>
After completion, create `.planning/phases/07-analise-llm/07-07-SUMMARY.md` documenting:
1. Lista exata das vars do `renderInjection` e como cada uma é populada
2. Decisão sobre AbortSignal: testar com mock vs aceitar como integration smoke (atualmente acepta integration smoke)
3. Comportamento se RAG retorna 0 chunks: log warn? throw? — atual: continua com `<knowledge>` vazio (compat com Sonnet pivot para conhecimento generalista per Princípio 3)
</output>
