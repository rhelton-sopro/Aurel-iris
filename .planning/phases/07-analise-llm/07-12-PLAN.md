---
phase: 07-analise-llm
plan: 12
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/actions/analise.ts
  - apps/web/app/(dashboard)/leituras/[id]/editar/__tests__/save-action.test.ts
autonomous: true
gap_closure: true
requirements: [LLM-01, LLM-02, LLM-03, LLM-04]
tags: [server-actions, lgpd, audit, terminal-state]

must_haves:
  truths:
    - "markReadingDelivered retorna { error } quando audit_metadata.low_anchor_rate === true, antes de qualquer UPDATE em is_delivered (gate server-side SC2)."
    - "saveReportDelivered SEMPRE escreve ENCERRAMENTO_LITERAL em report_delivered.encerramento_disclaimer, sobrescrevendo qualquer payload do cliente (D-P3 contract enforcement SC4)."
    - "markReadingDelivered retorna { error } quando reading.report_delivered é null OU objeto vazio ({} sem chaves) — bloqueia entrega de relatório vazio (CR-04)."
    - "saveReportDelivered retorna { error } quando reading.is_delivered === true — leituras já entregues são read-only no Server Action (WR-08)."
    - "save-action.test.ts tem ≥ 4 testes GREEN cobrindo as 4 guardas acima (it.todo stubs Wave-0 substituídos por testes reais)."
  artifacts:
    - path: "apps/web/app/actions/analise.ts"
      provides: "Server Actions com 4 guardas server-side novos (low_anchor_rate, ENCERRAMENTO_LITERAL overwrite, empty-content gate, terminal-state gate)"
      contains: "ENCERRAMENTO_LITERAL"
    - path: "apps/web/app/(dashboard)/leituras/[id]/editar/__tests__/save-action.test.ts"
      provides: "Testes vitest GREEN para as 4 guardas (suite com mock supabase no padrão vi.hoisted estabelecido)"
      contains: "low_anchor_rate"
  key_links:
    - from: "apps/web/app/actions/analise.ts"
      to: "apps/web/lib/anthropic/types.ts"
      via: "import { ENCERRAMENTO_LITERAL } from '@/lib/anthropic/types'"
      pattern: "import.*ENCERRAMENTO_LITERAL.*from.*['\"]@/lib/anthropic/types['\"]"
    - from: "apps/web/app/(dashboard)/leituras/[id]/editar/__tests__/save-action.test.ts"
      to: "apps/web/app/actions/analise.ts"
      via: "import { saveReportDelivered, markReadingDelivered } from '@/app/actions/analise' (or relative path)"
      pattern: "saveReportDelivered|markReadingDelivered"
---

<objective>
Fechar 3 gaps server-side identificados em `07-VERIFICATION.md` (status: gaps_found, score 3/5). Os 3 gaps estão todos em `apps/web/app/actions/analise.ts` e bloqueiam SC2, SC4 e SC5 do ROADMAP da Fase 7. Após este plan rodar e a verificação re-rodar, Phase 7 deve transitar para `status: passed` (5/5 truths verified, exceto SC3 que continua UNCERTAIN human-needed).

**Purpose:** Os 3 SCs falhos têm uma raiz comum: `markReadingDelivered` e `saveReportDelivered` aceitam ações que violam contratos da fase (entregar com âncora baixa, sobrescrever disclaimer literal, entregar relatório vazio, escrever em leitura terminal). A infra existe e está correta — falta apenas a ação de **rejeitar** server-side. CR-05 do REVIEW (disclaimer bypass) e CR-04 (empty content) são BLOCKERS; WR-08 (terminal-state bypass) é warning mas trivial de fechar no mesmo touch.

**Output:** 1 patch em `analise.ts` (~15 linhas líquidas: 1 import + 4 guardas) + 1 suite de testes nova substituindo 4+ `it.todo` por testes GREEN com mock supabase. Zero arquivos novos, zero mudanças de schema, zero migrations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-analise-llm/07-CONTEXT.md
@.planning/phases/07-analise-llm/07-VERIFICATION.md
@.planning/phases/07-analise-llm/07-REVIEW.md
@apps/web/app/actions/analise.ts
@apps/web/app/actions/analise.schemas.ts
@apps/web/lib/anthropic/types.ts
@apps/web/app/(dashboard)/leituras/[id]/editar/__tests__/save-action.test.ts

<interfaces>
<!-- Contratos extraídos da fase 7 já implementada — executor NÃO precisa explorar codebase. -->
<!-- Source: types.ts (07-03), analise.schemas.ts (07-10), audit.ts (07-05), readings.test.ts (mock pattern). -->

From `apps/web/lib/anthropic/types.ts`:
```typescript
// Server-appended literal — SPEC §6 linhas 624-627. Byte-exact, NÃO editar.
export const ENCERRAMENTO_LITERAL: string  // 4-line blockquote starting with "> Esta leitura iridológica…"

export type ReportSectionKey =
  | '1_constituicao' | '2_estrutural_fisica' | '3_indicacoes_sistemicas'
  | '4_toxemia' | '5_psicoemocional' | '6_cargas_temporais'
  | '7_carencias_nutricionais' | '8_simbolico_espiritual' | '9_cuidados_integrativos'
  | '10_potenciais_forcas' | '11_afirmacoes_integracao' | '12_sintese_integrativa'
  | '13_mensagem_final' | 'encerramento_disclaimer'

export type ReportJsonb = Partial<Record<ReportSectionKey, string>>

export interface AuditMetadata {
  low_anchor_rate: boolean
  anchor_rate_pct: number
  anchor_rate_per_section: Record<string, number>
  forbidden_vocab: Array<{ section: string; term: string; occurrences: number }>
  audited_at: string
  auditor_version: 'v1'
}
```

From `apps/web/app/actions/analise.schemas.ts`:
```typescript
// passthrough() — qualquer campo extra passa pela validação. Schema NÃO protege
// encerramento_disclaimer; defesa precisa ser server-side post-parse.
export const reportDeliveredSchema: z.ZodObject<...>.passthrough()
export const readingIdSchema: z.ZodObject<{ reading_id: z.ZodString /* uuid */ }>
```

From `apps/web/app/actions/readings.test.ts` (mock pattern estabelecido — copiar verbatim):
```typescript
vi.mock('next/cache', () => ({ revalidatePath: vi.fn() }))
vi.mock('next/navigation', () => ({
  redirect: vi.fn((path: string) => { throw new Error(`NEXT_REDIRECT:${path}`) }),
}))
vi.mock('@/lib/supabase/server', () => ({ createClient: vi.fn(async () => ({ /* chain */ })) }))
```

Supabase chain shape usado por `analise.ts`:
- `supabase.auth.getUser()` → `{ data: { user: { id } }, error }`
- `supabase.from('readings').select('...').eq('id', id).eq('therapist_id', uid).single()` → `{ data: { id, therapist_id, report_delivered, is_delivered, audit_metadata, report_generated }, error }`
- `supabase.from('readings').update({...}).eq('id', id)` → `{ error }`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Patch analise.ts — 4 guardas server-side</name>
  <files>apps/web/app/actions/analise.ts</files>

  <read_first>
    - apps/web/app/actions/analise.ts (arquivo a modificar — 156 linhas)
    - apps/web/lib/anthropic/types.ts (origem de ENCERRAMENTO_LITERAL — confirmar export e shape)
    - apps/web/app/actions/analise.schemas.ts (entender que reportDeliveredSchema é .passthrough() e por que a defesa não pode estar no zod)
    - .planning/phases/07-analise-llm/07-VERIFICATION.md (gaps A/B/C definidos com linha exata)
    - .planning/phases/07-analise-llm/07-REVIEW.md (CR-04 + CR-05 + WR-08 — ler apenas as 3 entradas; serve para garantir que a mensagem em pt-BR esteja alinhada com o vocabulário usado pelo REVIEW)
  </read_first>

  <behavior>
    Após este task, `apps/web/app/actions/analise.ts` terá 4 guardas novos:

    **Guarda A — saveReportDelivered: terminal-state gate (WR-08).**
    Após o select inicial de `reading` (atualmente linhas 71-76 selecionando apenas `'id, therapist_id, report_generated'`), o select também traz `is_delivered`. Imediatamente após o check `readingError || !reading`, adicionar: `if (reading.is_delivered) return { error: 'Leitura já entregue ao cliente — somente leitura.' }`. Bloqueia escrita em leituras já entregues.

    **Guarda B — saveReportDelivered: ENCERRAMENTO_LITERAL overwrite (CR-05 / SC4).**
    Imediatamente após `const delivered = bodyParsed.data as ReportJsonb` (atualmente linha 80), adicionar uma linha: `delivered.encerramento_disclaimer = ENCERRAMENTO_LITERAL`. Sobrescreve qualquer valor que o cliente tenha enviado nessa chave. Pseudo-clientes maliciosos NÃO conseguem alterar o disclaimer literal D-P3.

    **Guarda C — markReadingDelivered: low_anchor_rate gate (SC2).**
    Estender o select de `reading` (atualmente linhas 122-127, hoje seleciona `'id, therapist_id, report_delivered, is_delivered'`) para incluir `audit_metadata`. Após o check `readingError || !reading` E após o existing `if (reading.is_delivered)`, adicionar: `const audit = reading.audit_metadata as AuditMetadata | null`; depois `if (audit?.low_anchor_rate === true) return { error: 'Âncora insuficiente — taxa de ancoragem abaixo de 95% nas seções clínicas. Edite e re-salve antes de entregar.' }`. **Importante:** se `audit` for null (relatório nunca foi auditado por bug), aplicar fail-closed: `if (!audit || audit.low_anchor_rate !== false) return { error: 'Auditoria de ancoragem ausente ou pendente. Re-salve o relatório para re-rodar a auditoria.' }`. Justificativa fail-closed: SC2 fala em "rejeita"; permitir entrega com `audit=null` é equivalente a aceitar âncora baixa silenciosamente.

    **Guarda D — markReadingDelivered: empty-content gate (CR-04).**
    Após o gate `is_delivered` E antes do gate `low_anchor_rate`, adicionar: `const delivered = (reading.report_delivered as ReportJsonb | null)`; `if (!delivered || Object.keys(delivered).length === 0) return { error: 'Salve a edição antes de entregar ao cliente.' }`. Bloqueia entrega de objeto vazio (terapeuta nunca clicou em Salvar antes de Entregar). Substitui a coercão `?? {}` que hoje (linha 131) silenciava o vazio. NB: a iteração existente sobre `Object.entries(delivered)` (linhas 132-142, vocab audit) deve permanecer; ela agora consome o `delivered` já validado como não-vazio.

    **Imports necessários (no topo do arquivo):**
    - Adicionar `ENCERRAMENTO_LITERAL` à importação existente de `'@/lib/anthropic/types'` (atualmente importa apenas `ReportJsonb` na linha 30). Resultado: `import type { ReportJsonb, AuditMetadata } from '@/lib/anthropic/types'` para os tipos + `import { ENCERRAMENTO_LITERAL } from '@/lib/anthropic/types'` para o valor (NB: `AuditMetadata` é interface, vai junto com `ReportJsonb` no `import type`; `ENCERRAMENTO_LITERAL` é const value, precisa de import sem `type`).

    **Ordem dos guards em markReadingDelivered (importa para mensagens corretas):**
    1. Auth gate (existente)
    2. zod parse (existente)
    3. select (modificar para incluir audit_metadata)
    4. `readingError || !reading` (existente)
    5. `is_delivered` gate (existente — manter)
    6. **NOVO:** empty-content gate (Guarda D)
    7. **NOVO:** low_anchor_rate gate (Guarda C, fail-closed)
    8. Forbidden vocab gate (existente — manter)
    9. UPDATE (existente — manter)

    **Ordem dos guards em saveReportDelivered:**
    1. Auth (existente)
    2. zod (existente)
    3. forbidden vocab pre-save (existente — manter)
    4. select (modificar para incluir is_delivered)
    5. `readingError || !reading` (existente)
    6. **NOVO:** terminal-state gate (Guarda A)
    7. `delivered = bodyParsed.data` (existente)
    8. **NOVO:** disclaimer overwrite (Guarda B)
    9. classifyAllSections + runAudit + UPDATE (existente)
  </behavior>

  <action>
    Aplicar 4 patches localizados em `apps/web/app/actions/analise.ts`. Não reescrever o arquivo; usar Edit com ranges precisos. Seguir EXATAMENTE as posições e textos descritos em `<behavior>`. Não adicionar logs novos, não mexer em revalidatePath, não tocar em `runAudit` / `classifyAllSections` / `extractForbiddenHits` (estes já funcionam — só queremos compor os guards corretos antes deles).

    **Mensagens de erro exatas (literal — terapeutas brasileiros vão ver):**
    - Guarda A: `'Leitura já entregue ao cliente — somente leitura.'`
    - Guarda C (anchor low): `'Âncora insuficiente — taxa de ancoragem abaixo de 95% nas seções clínicas. Edite e re-salve antes de entregar.'`
    - Guarda C (audit null/missing — fail-closed): `'Auditoria de ancoragem ausente ou pendente. Re-salve o relatório para re-rodar a auditoria.'`
    - Guarda D: `'Salve a edição antes de entregar ao cliente.'`
    - Disclaimer NÃO tem mensagem — é silent overwrite (cliente não percebe; é defesa em profundidade contra prompt-drift e payload tampering).

    **Vocabulário LGPD:** as 3 mensagens novas NÃO contêm "diagnóstico", "tratamento", "cura". Verificar via grep ao final.

    **Casts:** o select retorna `audit_metadata: Json | null` e `report_delivered: Json | null` (de `types/database.ts`). O cast para `AuditMetadata | null` é OK porque o auditor v1 grava esse shape exato; o cast para `ReportJsonb | null` segue o pattern já usado em `saveReportDelivered:79` e `markReadingDelivered:131`. Manter consistência.

    Decisões de implementação per D-A2 (vocab existente), D-A3 (audit_metadata shape), D-P3 (encerramento literal D-P3), CR-04, CR-05, WR-08 do REVIEW.
  </action>

  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit -p . 2>&1 | grep "actions/analise" | head -20</automated>
  </verify>

  <acceptance_criteria>
    1. **Imports corretos:** `grep -E "ENCERRAMENTO_LITERAL|AuditMetadata" apps/web/app/actions/analise.ts | grep -v '^#' | wc -l` retorna ≥ 3 (1 import line + 1 use no guard B + 1 type cast no guard C).
    2. **Guarda B presente:** `grep -n "delivered.encerramento_disclaimer = ENCERRAMENTO_LITERAL" apps/web/app/actions/analise.ts` retorna 1 match.
    3. **Guarda A presente:** `grep -n "Leitura já entregue ao cliente — somente leitura" apps/web/app/actions/analise.ts` retorna 1 match.
    4. **Guarda C presente:** `grep -n "low_anchor_rate" apps/web/app/actions/analise.ts` retorna ≥ 1 match (no guard); `grep -n "Âncora insuficiente" apps/web/app/actions/analise.ts` retorna 1 match.
    5. **Guarda D presente:** `grep -n "Salve a edição antes de entregar" apps/web/app/actions/analise.ts` retorna 1 match; `grep -n "Object.keys(delivered).length === 0" apps/web/app/actions/analise.ts` retorna 1 match.
    6. **Select de audit_metadata:** `grep -n "audit_metadata" apps/web/app/actions/analise.ts` retorna ≥ 2 matches (select + use).
    7. **Select de is_delivered em saveReportDelivered:** `grep -n -A1 "from('readings')" apps/web/app/actions/analise.ts | grep -c "is_delivered"` retorna ≥ 2 (1 em saveReportDelivered, 1 já existia em markReadingDelivered).
    8. **Vocab LGPD limpo:** `cd apps/web && pnpm audit:vocabulary 2>&1 | grep "actions/analise.ts"` retorna VAZIO (sem violações).
    9. **TypeScript clean:** `cd apps/web && pnpm tsc --noEmit -p . 2>&1 | grep "actions/analise"` retorna VAZIO (zero erros novos no arquivo modificado).
    10. **Não quebrar outros consumidores:** `cd apps/web && pnpm tsc --noEmit -p . 2>&1 | grep -v "out-of-scope-files-from-STATE.md" | tail -20` mostra zero erros novos vs baseline (Phase 5/3 errors documentados em STATE.md como out-of-scope).
  </acceptance_criteria>

  <done>
    - 4 guardas adicionados em `analise.ts` na ordem especificada.
    - `ENCERRAMENTO_LITERAL` importado e usado em saveReportDelivered.
    - `AuditMetadata` importado e usado em markReadingDelivered.
    - 3 mensagens de erro novas em pt-BR alinhadas com tom existente.
    - tsc clean para o arquivo; audit:vocabulary clean para o arquivo.
    - `pnpm --filter web test:run app/actions/__tests__/analise.test.ts` ainda passa OU não existe (smoke test rodará em Task 2).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flip save-action.test.ts it.todos para testes GREEN cobrindo as 4 guardas</name>
  <files>apps/web/app/(dashboard)/leituras/[id]/editar/__tests__/save-action.test.ts</files>

  <read_first>
    - apps/web/app/(dashboard)/leituras/[id]/editar/__tests__/save-action.test.ts (arquivo a substituir — 24 linhas, 11 it.todo)
    - apps/web/app/actions/analise.ts (versão JÁ PATCHEADA pela Task 1 — não re-pesquisar, ler para confirmar shape final dos guards e nomes exatos das mensagens)
    - apps/web/app/actions/readings.test.ts (mock pattern estabelecido; **copiar a estrutura de `vi.mock` verbatim** — esse pattern já passa CI hoje)
    - apps/web/lib/anthropic/types.ts (re-confirmar ENCERRAMENTO_LITERAL shape — testes precisam comparar bytes do disclaimer)
    - apps/web/app/actions/analise.schemas.ts (re-confirmar shape do payload aceito pelo Server Action)
  </read_first>

  <behavior>
    Substituir o conteúdo de `save-action.test.ts` por uma suite real com **≥ 8 testes GREEN** (4 cobrindo as 4 guardas novas + 4 cobrindo guardas já existentes que estão em `it.todo`). Manter `it.todo` apenas para testes que exigem mocks elaborados de RPC do Supabase fora do escopo (ex: o test de `revalidatePath` chamado 3 vezes — manter como `it.todo` com nota explícita).

    **Pattern de mock — copiar verbatim de `apps/web/app/actions/readings.test.ts:11-32`:**

    ```typescript
    vi.mock('next/cache', () => ({ revalidatePath: vi.fn() }))
    vi.mock('next/navigation', () => ({
      redirect: vi.fn((path: string) => { throw new Error(`NEXT_REDIRECT:${path}`) }),
    }))
    vi.mock('next/headers', () => ({
      cookies: vi.fn(async () => ({ toString: () => 'sb-access-token=test' })),
    }))
    ```

    **Mock supabase factory parametrizável (helper local no test file):**

    ```typescript
    function createMockSupabase(opts: {
      user?: { id: string } | null
      reading?: Record<string, unknown> | null
      readingError?: { message: string } | null
      updateError?: { message: string } | null
    }) {
      const update = vi.fn(() => ({ eq: vi.fn().mockResolvedValue({ error: opts.updateError ?? null }) }))
      const single = vi.fn().mockResolvedValue({ data: opts.reading ?? null, error: opts.readingError ?? null })
      const fromChain = {
        select: vi.fn(() => ({ eq: vi.fn(() => ({ eq: vi.fn(() => ({ single })) })) })),
        update,
      }
      return {
        auth: {
          getUser: vi.fn().mockResolvedValue({
            data: { user: opts.user ?? { id: 'user-1' } },
            error: opts.user === null ? { message: 'no session' } : null,
          }),
        },
        from: vi.fn(() => fromChain),
        // Expose update spy for assertions
        __update: update,
      }
    }

    vi.mock('@/lib/supabase/server', () => ({
      createClient: vi.fn(),
    }))
    ```

    Nos testes, importar `createClient` do mock e fazer `vi.mocked(createClient).mockResolvedValueOnce(createMockSupabase({...}) as never)` antes de cada chamada ao Server Action.

    **Mocks adicionais de libs que `analise.ts` chama:**
    - `vi.mock('@/lib/anthropic/audit', () => ({ runAudit: vi.fn(() => ({ low_anchor_rate: false, anchor_rate_pct: 100, anchor_rate_per_section: {}, forbidden_vocab: [], audited_at: '2026-05-08T15:00:00Z', auditor_version: 'v1' })), extractForbiddenHits: vi.fn(() => []) }))` — default returns vacío para o caminho feliz; testes que precisam de hits/low-anchor podem `vi.mocked(extractForbiddenHits).mockReturnValueOnce([{...}])`.
    - `vi.mock('@/lib/anthropic/diff', () => ({ classifyAllSections: vi.fn(() => ({ edit_diff: {}, zonas_editadas: [], tipo_edicao: [] })) }))` — placeholder.

    **8 testes GREEN obrigatórios:**

    Em `describe('saveReportDelivered (D-A2 + WR-08 + CR-05)')`:
    1. `'BLOCK quando is_delivered=true (WR-08 terminal-state gate)'` — mock reading com is_delivered=true; chama saveReportDelivered; assert `result.error === 'Leitura já entregue ao cliente — somente leitura.'` E que `from('readings').update` NÃO foi chamado (`expect(supabaseMock.__update).not.toHaveBeenCalled()`).
    2. `'overrides encerramento_disclaimer com ENCERRAMENTO_LITERAL (CR-05 SC4 D-P3)'` — mock reading válido (is_delivered=false, report_generated={...}); chama saveReportDelivered passando `{ '1_constituicao': 'texto', encerramento_disclaimer: 'TENTATIVA DE BYPASS' }`; assert `result.success === true` E que o segundo argumento de `update()` contém `report_delivered.encerramento_disclaimer === ENCERRAMENTO_LITERAL` (importar `ENCERRAMENTO_LITERAL` no test e comparar). Pode capturar o argumento via `update.mock.calls[0][0]`.
    3. `'BLOCK save com vocab proibido (D-A2 — re-asserting baseline ainda funciona após patch)'` — mock `extractForbiddenHits` retornando `[{ term: 'diagnóstico', section: '1_constituicao', occurrences: 1 }]`; chama saveReportDelivered; assert `result.error` contém `'diagnóstico'` E que update NÃO foi chamado.
    4. `'redirects to /login quando user é null'` — mock user=null; chama saveReportDelivered; assert que redirect lança `Error('NEXT_REDIRECT:/login')` (testar via `await expect(saveReportDelivered(...)).rejects.toThrow('/login')`).

    Em `describe('markReadingDelivered (CR-04 + SC2 + WR-08-existing)')`:
    5. `'BLOCK quando report_delivered é null (CR-04 empty-content gate)'` — mock reading com `report_delivered: null, is_delivered: false, audit_metadata: {...low_anchor_rate=false}`; chama markReadingDelivered; assert `result.error === 'Salve a edição antes de entregar ao cliente.'` E update NÃO chamado.
    6. `'BLOCK quando report_delivered é objeto vazio {}  (CR-04)'` — mock reading com `report_delivered: {}, is_delivered: false, audit_metadata: {...low_anchor_rate=false}`; assert mesma mensagem que (5) E update NÃO chamado.
    7. `'BLOCK quando audit_metadata.low_anchor_rate=true (SC2 gate)'` — mock reading com `report_delivered: { '1_constituicao': 'texto válido' }, is_delivered: false, audit_metadata: { low_anchor_rate: true, anchor_rate_pct: 87, anchor_rate_per_section: {...}, forbidden_vocab: [], audited_at: '...', auditor_version: 'v1' }`; assert `result.error === 'Âncora insuficiente — taxa de ancoragem abaixo de 95% nas seções clínicas. Edite e re-salve antes de entregar.'` E update NÃO chamado.
    8. `'BLOCK quando audit_metadata é null — fail-closed (SC2 missing audit)'` — mock reading com `report_delivered: { '1_constituicao': 'texto' }, is_delivered: false, audit_metadata: null`; assert `result.error === 'Auditoria de ancoragem ausente ou pendente. Re-salve o relatório para re-rodar a auditoria.'` E update NÃO chamado.

    **Bonus — happy path (1 teste extra):**
    9. `'happy path — flip is_delivered=true quando todos os gates passam'` — mock reading com `report_delivered: {...non-empty}, is_delivered: false, audit_metadata: { low_anchor_rate: false, ... }`, mock extractForbiddenHits returning `[]`; chama markReadingDelivered; assert `result.success === true` E update foi chamado com `{ is_delivered: true, delivered_at: <ISO string> }`.

    **it.todo MANTIDOS** (com comment explicativo):
    - `it.todo('classifyAllSections é chamado com (report_generated, delivered) — produz edit_diff per-key — TODO: integration test em 07-UAT')`
    - `it.todo('audit_metadata atualizado com runAudit(delivered) pós-save — TODO: integration test em 07-UAT')`
    - `it.todo('revalidatePath chamado para 3 paths — TODO: requires elaborate mock surface')`

    **Total esperado:** ≥ 9 testes GREEN, ≥ 3 it.todo (justificadas).
  </behavior>

  <action>
    Sobrescrever inteiramente `apps/web/app/(dashboard)/leituras/[id]/editar/__tests__/save-action.test.ts` com a suite descrita em `<behavior>`. NÃO usar `cat << EOF` — usar a tool Write.

    **Imports do arquivo de teste:**
    ```typescript
    import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest'
    import { ENCERRAMENTO_LITERAL } from '@/lib/anthropic/types'
    ```

    Fazer todos os `vi.mock(...)` ANTES do import dinâmico/estático do action. Padrão:
    ```typescript
    vi.mock('next/cache', () => ({ revalidatePath: vi.fn() }))
    // ... outros mocks ...
    vi.mock('@/lib/supabase/server', () => ({ createClient: vi.fn() }))

    // Lazy import para garantir ordem dos mocks:
    import { saveReportDelivered, markReadingDelivered } from '@/app/actions/analise'
    import { createClient } from '@/lib/supabase/server'
    ```

    **`afterEach(() => vi.clearAllMocks())`** entre testes para isolar contadores.

    **Comparação byte-exact do disclaimer:** importar `ENCERRAMENTO_LITERAL` direto de `@/lib/anthropic/types` no test file e usar `expect(updatedRow.report_delivered.encerramento_disclaimer).toBe(ENCERRAMENTO_LITERAL)`. Esse import garante que se o disclaimer for alterado em `types.ts` no futuro, o teste evolui em lock-step (sem hardcoded string drift).

    **VALID_READING_UUID:** reusar o pattern de `readings.test.ts:39` — `'401288f4-0f02-43aa-bdee-16d501089dc9'`. zod v4 rejeita UUIDs sintéticos `1111...`.

    **Path import:** preferir `@/app/actions/analise` se o tsconfig path alias suporta (verificar `apps/web/tsconfig.json`); fallback para path relativo `../../../../../actions/analise` (5 níveis acima de `__tests__/`).
  </action>

  <verify>
    <automated>cd apps/web && pnpm test:run -- "app/(dashboard)/leituras/\[id\]/editar/__tests__/save-action.test.ts" 2>&1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    1. **Suite roda e passa:** `cd apps/web && pnpm test:run -- "save-action.test.ts" 2>&1 | grep -E "Tests.*passed"` retorna line com `≥ 9 passed` (e zero failed).
    2. **Cobertura das 4 guardas novas:** `grep -c "^\s*it(" apps/web/app/(dashboard)/leituras/[id]/editar/__tests__/save-action.test.ts` retorna ≥ 9 (≥ 9 testes ativos, não it.todo).
    3. **Mensagens exatas testadas:** `grep -E "Leitura já entregue|Âncora insuficiente|Salve a edição|Auditoria de ancoragem ausente" apps/web/app/(dashboard)/leituras/[id]/editar/__tests__/save-action.test.ts | grep -v '^#' | wc -l` retorna ≥ 4 (uma asserção por mensagem).
    4. **ENCERRAMENTO_LITERAL importado e comparado byte-exact:** `grep -E "import.*ENCERRAMENTO_LITERAL" apps/web/app/(dashboard)/leituras/[id]/editar/__tests__/save-action.test.ts` retorna 1 match; `grep -E "toBe\(ENCERRAMENTO_LITERAL\)" apps/web/app/(dashboard)/leituras/[id]/editar/__tests__/save-action.test.ts` retorna 1 match.
    5. **vi.mock pattern reusado verbatim:** `grep -E "vi\.mock\('@/lib/supabase/server'" apps/web/app/(dashboard)/leituras/[id]/editar/__tests__/save-action.test.ts` retorna 1 match.
    6. **TypeScript clean para o test file:** `cd apps/web && pnpm tsc --noEmit -p . 2>&1 | grep "save-action.test.ts"` retorna VAZIO.
    7. **Vocab LGPD clean para o test file:** `cd apps/web && pnpm audit:vocabulary 2>&1 | grep "save-action.test.ts"` retorna VAZIO. (Observação: o test file VAI conter a string `'diagnóstico'` no test (3) — deve estar em comment-marker `// audit-vocabulary:allowlist` no topo, ou vir de uma string variable que dispara LGPD-allowlisted via marker. Justificativa idêntica ao `lib/anthropic/types.ts:1-5`.)
    8. **`it.todo` restantes documentados:** `grep "it.todo" apps/web/app/(dashboard)/leituras/[id]/editar/__tests__/save-action.test.ts` ≤ 3 e cada um tem comment "TODO:" explicando por que ficou.
  </acceptance_criteria>

  <done>
    - Suite com ≥ 9 testes GREEN cobrindo: terminal-state, disclaimer overwrite, vocab block, auth redirect, empty null, empty {}, low_anchor_rate, audit null fail-closed, happy path.
    - 3 it.todo restantes com justificativa "requires elaborate mock surface" (alinhado com nota original do file).
    - Mock pattern espelha `readings.test.ts` verbatim.
    - `pnpm test:run -- save-action.test.ts` exit 0 com ≥ 9 passed.
    - `pnpm tsc --noEmit` sem erros novos no test file.
    - `pnpm audit:vocabulary` clean (com allowlist marker se necessário).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server Action | Cliente envia `reading_id` + `reportDelivered` payload via RPC Server Action; passa por zod `.passthrough()`. Cliente NÃO é confiável. |
| Server Action → Postgres | RLS aplicado via session user; Postgres é trust-internal mas reading.therapist_id !== user.id é validado explicitamente em select. |
| Server Action → ENCERRAMENTO_LITERAL constant | Constante carregada de `lib/anthropic/types.ts` (server-only via `import 'server-only'` indireto via consumidores); imune a tampering em runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-12-01 | Tampering | `saveReportDelivered` payload | mitigate | Guarda B sobrescreve `delivered.encerramento_disclaimer` com `ENCERRAMENTO_LITERAL` server-side **após** zod parse. Cliente malicioso enviando `encerramento_disclaimer: '<malicious>'` é silenciosamente neutralizado. Test #2 da Task 2 valida byte-exact equality. |
| T-7-12-02 | Tampering | `markReadingDelivered` `audit_metadata` bypass | mitigate | Guarda C aplica fail-closed quando `audit_metadata` é null/missing. Atacante (ou bug) que limpa `audit_metadata` para 'enganar' o gate é bloqueado pela mensagem "Auditoria de ancoragem ausente ou pendente". Test #8 da Task 2 valida. |
| T-7-12-03 | Elevation of Privilege | `saveReportDelivered` em leitura terminal | mitigate | Guarda A bloqueia escrita quando `is_delivered=true`. Mesmo se atacante chamasse o Server Action diretamente (bypass UI que oculta o botão), Postgres não recebe UPDATE. Test #1 da Task 2 valida. |
| T-7-12-04 | Repudiation | Entrega de relatório vazio | mitigate | Guarda D bloqueia entrega quando `report_delivered` é null/`{}`. Garante que toda transição `is_delivered=true` está acompanhada de conteúdo persistido. Audit trail (`delivered_at` timestamp + RLS-protected log) permanece consistente. Tests #5 e #6 validam. |
| T-7-12-05 | Information Disclosure | Test fixture com 'diagnóstico' | accept | Test #3 da Task 2 inclui a palavra `'diagnóstico'` como mock-input para validar o BLOCK do vocab gate. Mitigado por `// audit-vocabulary:allowlist` marker no topo do test file (mesmo pattern de `lib/anthropic/types.ts:1-5`). Justificativa: vocab-test precisa do termo para ser semântico-meaningful. |
| T-7-12-06 | Denial of Service | Auditor gera audit_metadata null em loop | accept | Risk baixo — auditor v1 é determinístico (não tem caminho que retorne null). Se ele falhar, retorna AuditMetadata com flags=true e o gate dispara mensagem clara. Não é DoS, é fail-closed UX. Sem mitigação adicional necessária. |
</threat_model>

<verification>
## Phase-level checks (a serem rodados após Tasks 1+2)

1. **Server-side guards smoke test (manual run, não bloqueia plan execution):**
   - Caminho A (low anchor): inserir manualmente em DB `UPDATE readings SET audit_metadata = '{"low_anchor_rate": true, "anchor_rate_pct": 87, "anchor_rate_per_section": {}, "forbidden_vocab": [], "audited_at": "2026-05-08T15:00:00Z", "auditor_version": "v1"}'::jsonb WHERE id = '<test-reading>'`. Em `/leituras/<id>/editar`, clicar Entregar. Esperado: error toast "Âncora insuficiente — taxa de ancoragem abaixo de 95% nas seções clínicas." (Smoke test deferido para 07-VERIFICATION run, NÃO requirement deste plan.)
   - Caminho B (disclaimer): em `/leituras/<id>/editar`, abrir DevTools → Network. Editar a seção 1. Submeter. Inspecionar payload do POST: `encerramento_disclaimer` é o que o usuário viu (read-only no UI). Inspecionar row no DB: `report_delivered.encerramento_disclaimer === ENCERRAMENTO_LITERAL` byte-exact. (Manual; documentar em 07-VERIFICATION re-run.)

2. **Re-run verification:** após este plan executar com sucesso, rodar `/gsd-verify-work 7`. Esperado: gaps_found → passed (5/5 truths verified — exceto SC3 UNCERTAIN human-needed que permanece UNCERTAIN; isso é por design, não é gap).

3. **TypeScript + LGPD audit globais:**
   - `cd apps/web && pnpm tsc --noEmit -p .` exit 0 (excluindo erros pré-existentes Phase 5/3 em STATE.md "Itens diferidos").
   - `cd apps/web && pnpm audit:vocabulary` exit 0.
</verification>

<success_criteria>
## Plan-level (todos verificáveis via grep / pnpm scripts)

1. ✅ `apps/web/app/actions/analise.ts` contém `ENCERRAMENTO_LITERAL` import e use.
2. ✅ `apps/web/app/actions/analise.ts` contém 4 guardas novos (verificáveis via 5 grep matches descritos em Task 1 acceptance #1-#5).
3. ✅ `apps/web/app/(dashboard)/leituras/[id]/editar/__tests__/save-action.test.ts` tem ≥ 9 testes GREEN (verificável via `pnpm test:run -- save-action.test.ts | grep "passed"`).
4. ✅ Tests cobrem: terminal-state (Guarda A), disclaimer overwrite (Guarda B), low_anchor_rate (Guarda C low + Guarda C null), empty-content (Guarda D null + Guarda D {}), happy path, vocab BLOCK (regression), auth redirect (regression).
5. ✅ tsc clean + audit:vocabulary clean para os 2 arquivos modificados.
6. ✅ Zero migrations, zero novos arquivos, zero novas dependências.

## Phase-level (post-execution, não bloqueia este plan)

- 07-VERIFICATION re-run muda Status: gaps_found → passed.
- Roadmap Fase 7 marca 07-12-PLAN.md como [x] e Fase 7 fica `passed`.
</success_criteria>

<output>
Após Tasks 1+2 completarem com tudo GREEN, criar `.planning/phases/07-analise-llm/07-12-SUMMARY.md` documentando:
- 4 guardas adicionados (com line ranges em analise.ts antes/depois do patch)
- Test counts (≥ 9 GREEN, 3 it.todo restantes com justificativas)
- Verification status preview (gaps fechados em VERIFICATION.md gap A/B/C)
- Próximo passo: `/gsd-verify-work 7` para re-verificar e fechar a fase
</output>
