---
phase: 07-analise-llm
plan: 06
type: tdd
wave: 2
depends_on: [07-02, 07-03]
files_modified:
  - apps/web/lib/anthropic/diff.ts
  - apps/web/lib/anthropic/__tests__/diff.test.ts
autonomous: true
requirements: [LLM-04]
tags: [phase-7, diff, classifier, edit-tipo, tdd]

must_haves:
  truths:
    - "`apps/web/lib/anthropic/diff.ts` declara `import 'server-only'`"
    - "Exporta `classifyEdit(generated: string, delivered: string): { type: EditTipo; diff_summary: string; char_delta: number; changed_pct: number }` (D-U2)"
    - "Classifica via `diffWords` do npm `diff@9` package"
    - "Threshold 30% para distinguir `corrigido` (<30%) vs `reescrito` (>=30%)"
    - "Edge cases corretos: empty→text = `adicionado`, text→empty = `removido`, idêntico = `none`"
    - "Exporta `classifyAllSections(generated: ReportJsonb, delivered: ReportJsonb)` que aplica `classifyEdit` em cada chave canônica e produz `edit_diff` jsonb + `zonas_editadas` array + `tipo_edicao` text[] dedup (D-U2 outputs)"
    - "Wave-0 stub `diff.test.ts` flipado de 7 it.todo para ≥ 12 it com body GREEN cobrindo boundaries 29/30/31% + edges + classifyAllSections"
  artifacts:
    - path: "apps/web/lib/anthropic/diff.ts"
      provides: "Per-section edit classifier producing D-U2 jsonb shapes"
      min_lines: 100
      contains: "diffWords"
    - path: "apps/web/lib/anthropic/__tests__/diff.test.ts"
      provides: "Filled tests covering all 5 EditTipo values + boundary 30%"
      min_lines: 110
  key_links:
    - from: "apps/web/lib/anthropic/diff.ts"
      to: "diff@9 npm package"
      via: "import { diffWords, type Change } from 'diff'"
      pattern: "from 'diff'"
    - from: "apps/web/lib/anthropic/diff.ts"
      to: "EditTipo type from types.ts"
      via: "type-only import"
      pattern: "type \\{ EditTipo"
---

<objective>
Construir o diff classifier — para cada chave canônica do `report_generated`/`report_delivered` jsonb, calcula o tipo de edição (`adicionado`/`removido`/`corrigido`/`reescrito`/`none`) usando `diffWords` do npm `diff@9` package com threshold 30% (D-U2 founder heuristic). Produz os 3 jsonb outputs canônicos: `edit_diff` (per-key detalhe), `zonas_editadas` (lista das chaves alteradas), `tipo_edicao` (text[] dedup dos tipos). Preencher Wave-0 stub `diff.test.ts` cobrindo boundaries 29/30/31% e cada EditTipo.

Purpose: Sem classificador determinístico, a Fase 10 SAC não tem sinal estruturado pra aprender. `tipo_edicao` text[] é o sinal que vai alimentar o futuro modelo de aprendizagem; `zonas_editadas` permite queries SQL agregadas; `edit_diff` jsonb tem o detalhe per-section para debugging humano.
Output: `diff.ts` server-only + 12+ testes GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-analise-llm/07-CONTEXT.md
@.planning/phases/07-analise-llm/07-RESEARCH.md
@.planning/phases/07-analise-llm/07-PATTERNS.md
@apps/web/lib/anthropic/types.ts
@apps/web/lib/anthropic/__tests__/diff.test.ts

<interfaces>
<!-- diff.ts shape — extracted from RESEARCH §Pattern 4 lines 615-651 -->

import 'server-only'
import { diffWords, type Change } from 'diff'
import type { ReportJsonb, ReportSectionKey, EditTipo } from './types'

export interface ClassifiedEdit {
  type: EditTipo
  diff_summary: string
  char_delta: number
  changed_pct: number
}

export function classifyEdit(generated: string, delivered: string): ClassifiedEdit { ... }

export interface SectionDiffs {
  edit_diff: Partial<Record<ReportSectionKey, ClassifiedEdit>>
  zonas_editadas: ReportSectionKey[]   // keys where type !== 'none'
  tipo_edicao: EditTipo[]              // dedup list
}

export function classifyAllSections(
  generated: ReportJsonb,
  delivered: ReportJsonb,
): SectionDiffs { ... }

<!-- D-U2 thresholds -->
const THRESHOLD_PCT = 30  // <30% = 'corrigido', >=30% = 'reescrito'
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): Flip diff.test.ts with boundary 29/30/31% + EditTipo coverage</name>
  <files>apps/web/lib/anthropic/__tests__/diff.test.ts</files>
  <read_first>
    - `.planning/phases/07-analise-llm/07-CONTEXT.md` D-U2 lines 113-125 (classifier rules: vazio→texto adicionado, threshold 30% corrigido vs reescrito)
    - `.planning/phases/07-analise-llm/07-RESEARCH.md` Pattern 4 lines 608-651 (diffWords template + Change[] shape)
    - `.planning/phases/07-analise-llm/07-VALIDATION.md` line 1081 (boundary cases 29/30/31%)
    - `apps/web/lib/anthropic/types.ts` (EditTipo, ReportSectionKey)
    - `apps/web/lib/anthropic/__tests__/diff.test.ts` (Wave-0 stub criado em 07-02)
  </read_first>
  <behavior>
    - Tests classifyEdit: idêntico = none / vazio→texto = adicionado / texto→vazio = removido / mudança 28% = corrigido / mudança 30% = reescrito / mudança 31% = reescrito
    - Tests char_delta = trimDel.length - trimGen.length
    - Tests classifyAllSections: 14-key full shape, zonas_editadas filtra type !== 'none', tipo_edicao dedup
  </behavior>
  <action>
Escrever `apps/web/lib/anthropic/__tests__/diff.test.ts` substituindo os 7 it.todo. Conteúdo verbatim:

```typescript
import { describe, it, expect } from 'vitest'
import { classifyEdit, classifyAllSections } from '../diff'
import type { ReportJsonb } from '../types'

describe('lib/anthropic/diff — classifyEdit (D-U2)', () => {
  it('texto idêntico = "none"', () => {
    const r = classifyEdit('Foo bar baz.', 'Foo bar baz.')
    expect(r.type).toBe('none')
    expect(r.changed_pct).toBe(0)
    expect(r.char_delta).toBe(0)
  })

  it('vazio → texto = "adicionado"', () => {
    const r = classifyEdit('', 'Conteúdo novo aqui.')
    expect(r.type).toBe('adicionado')
    expect(r.changed_pct).toBe(100)
    expect(r.char_delta).toBeGreaterThan(0)
  })

  it('texto → vazio = "removido"', () => {
    const r = classifyEdit('Conteúdo removido.', '')
    expect(r.type).toBe('removido')
    expect(r.changed_pct).toBe(100)
    expect(r.char_delta).toBeLessThan(0)
  })

  it('whitespace only original = também "adicionado" (trim aplica)', () => {
    const r = classifyEdit('   \n  ', 'Conteúdo novo.')
    expect(r.type).toBe('adicionado')
  })

  it('mudança ~28% (abaixo do threshold 30%) = "corrigido"', () => {
    // 10 palavras, mudar 2 → 20% changed (token-level)
    // Use diffWords semantics: count tokens, not chars.
    const generated = 'um dois três quatro cinco seis sete oito nove dez'
    const delivered = 'um dois três quatro cinco seis sete oito ZERO ONZE'
    const r = classifyEdit(generated, delivered)
    expect(r.type).toBe('corrigido')
    expect(r.changed_pct).toBeLessThan(30)
  })

  it('mudança ~30% (boundary inclusive >=) = "reescrito"', () => {
    // Force ≥30% by replacing more tokens.
    const generated = 'um dois três quatro cinco seis sete oito nove dez'
    const delivered = 'A B C quatro cinco seis sete oito nove dez'
    const r = classifyEdit(generated, delivered)
    // 3 tokens replaced of ~10 input tokens — borderline 30%.
    expect(r.type).toBe('reescrito')
    expect(r.changed_pct).toBeGreaterThanOrEqual(30)
  })

  it('mudança >50% (claramente acima) = "reescrito"', () => {
    const generated = 'um dois três quatro cinco'
    const delivered = 'A B C D quatro'
    const r = classifyEdit(generated, delivered)
    expect(r.type).toBe('reescrito')
    expect(r.changed_pct).toBeGreaterThan(50)
  })

  it('char_delta = trimDel.length - trimGen.length', () => {
    const generated = 'foo'
    const delivered = 'foo bar'
    const r = classifyEdit(generated, delivered)
    expect(r.char_delta).toBe(4)  // ' bar' = 4 chars added
  })

  it('diff_summary é string não-vazia para edits not none', () => {
    const r = classifyEdit('original', 'modified')
    expect(typeof r.diff_summary).toBe('string')
    expect(r.diff_summary.length).toBeGreaterThan(0)
  })
})

describe('lib/anthropic/diff — classifyAllSections (D-U2 outputs)', () => {
  it('produz edit_diff jsonb + zonas_editadas + tipo_edicao para 14 keys', () => {
    const generated: ReportJsonb = {
      '1_constituicao': 'Original constituição.',
      '2_estrutural_fisica': 'Original estrutural.',
      '3_indicacoes_sistemicas': 'Original sistêmicas.',
    }
    const delivered: ReportJsonb = {
      '1_constituicao': 'Original constituição.',  // none
      '2_estrutural_fisica': 'Modificação completa diferente texto outro novo.',  // reescrito
      '3_indicacoes_sistemicas': '',  // removido
    }
    const r = classifyAllSections(generated, delivered)
    expect(r.edit_diff['1_constituicao']?.type).toBe('none')
    expect(r.edit_diff['2_estrutural_fisica']?.type).toBe('reescrito')
    expect(r.edit_diff['3_indicacoes_sistemicas']?.type).toBe('removido')
    expect(r.zonas_editadas).toContain('2_estrutural_fisica')
    expect(r.zonas_editadas).toContain('3_indicacoes_sistemicas')
    expect(r.zonas_editadas).not.toContain('1_constituicao')
    expect(r.tipo_edicao.sort()).toEqual(['removido', 'reescrito'].sort())
  })

  it('chave ausente em generated mas presente em delivered = "adicionado"', () => {
    const generated: ReportJsonb = {}
    const delivered: ReportJsonb = { '5_psicoemocional': 'Texto novo aqui.' }
    const r = classifyAllSections(generated, delivered)
    expect(r.edit_diff['5_psicoemocional']?.type).toBe('adicionado')
    expect(r.zonas_editadas).toEqual(['5_psicoemocional'])
    expect(r.tipo_edicao).toEqual(['adicionado'])
  })

  it('tudo idêntico = zonas_editadas vazio + tipo_edicao vazio', () => {
    const report: ReportJsonb = {
      '1_constituicao': 'Mesmo texto.',
      '2_estrutural_fisica': 'Mesmo texto 2.',
    }
    const r = classifyAllSections(report, report)
    expect(r.zonas_editadas).toEqual([])
    expect(r.tipo_edicao).toEqual([])
  })

  it('classifyAllSections processa encerramento_disclaimer (terapeuta NÃO deveria editar mas defesa em profundidade)', () => {
    const generated: ReportJsonb = { 'encerramento_disclaimer': '> Disclaimer original.' }
    const delivered: ReportJsonb = { 'encerramento_disclaimer': '> Disclaimer alterado pelo terapeuta.' }
    const r = classifyAllSections(generated, delivered)
    expect(r.edit_diff['encerramento_disclaimer']?.type).not.toBe('none')
    expect(r.zonas_editadas).toContain('encerramento_disclaimer')
  })
})
```

**Verificação imediata (RED):**
```bash
cd apps/web
pnpm test:run lib/anthropic/__tests__/diff.test.ts
# Esperado: ALL FAIL — diff.ts ainda não existe.
```
  </action>
  <verify>
    <automated>cd apps/web && pnpm test:run lib/anthropic/__tests__/diff.test.ts 2>&1 | tee /tmp/diff-red.txt; grep -E "FAIL|Cannot find module" /tmp/diff-red.txt | head -3 && echo "OK: diff tests RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/lib/anthropic/__tests__/diff.test.ts` contém ≥ 12 chamadas `it(`
    - Tests cobrem 5 EditTipo values: none / adicionado / removido / corrigido / reescrito
    - Tests cobrem boundary 29% (corrigido) e 30% (reescrito) e >50% (reescrito)
    - Tests cobrem `classifyAllSections` produzindo `edit_diff` + `zonas_editadas` + `tipo_edicao`
    - Tests cobrem chave ausente em generated mas presente em delivered → 'adicionado'
    - `pnpm test:run lib/anthropic/__tests__/diff.test.ts` exit ≠ 0 (RED esperado)
  </acceptance_criteria>
  <done>12+ tests escritos, RED esperado.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Author lib/anthropic/diff.ts to make all tests pass</name>
  <files>apps/web/lib/anthropic/diff.ts</files>
  <read_first>
    - `apps/web/lib/anthropic/__tests__/diff.test.ts` (Task 1)
    - `apps/web/lib/anthropic/types.ts` (07-03 Task 1 — `ReportJsonb`, `ReportSectionKey`, `EditTipo`)
    - `.planning/phases/07-analise-llm/07-RESEARCH.md` Pattern 4 lines 615-645 (template)
    - `.planning/phases/07-analise-llm/07-PATTERNS.md` lines 184-197 (no analog — RESEARCH §Pattern 4 owns shape)
  </read_first>
  <action>
Criar `apps/web/lib/anthropic/diff.ts` com conteúdo verbatim:

```typescript
/**
 * Diff classifier (D-U2) — produces D-U2 jsonb outputs from per-section
 * comparison: edit_diff (per-key detail), zonas_editadas (changed keys),
 * tipo_edicao (dedup list of types).
 *
 * Uses `diff@9` `diffWords` (Myers algorithm, BSD, 0 deps, TS-native ESM+CJS).
 * Threshold 30% changed tokens distinguishes 'corrigido' (<30%) vs 'reescrito'
 * (>=30%) per CONTEXT D-U2 founder heuristic.
 *
 * Phase 7 | Plan 07-06 | Decisions: D-U2, RESEARCH §Pattern 4
 */
import 'server-only'
import { diffWords, type Change } from 'diff'
import type { ReportJsonb, ReportSectionKey, EditTipo } from './types'

const THRESHOLD_PCT = 30

export interface ClassifiedEdit {
  type: EditTipo
  diff_summary: string
  char_delta: number
  changed_pct: number
}

export function classifyEdit(generated: string, delivered: string): ClassifiedEdit {
  const trimGen = (generated ?? '').trim()
  const trimDel = (delivered ?? '').trim()

  if (trimGen === trimDel) {
    return { type: 'none', diff_summary: '', char_delta: 0, changed_pct: 0 }
  }
  if (trimGen === '' && trimDel !== '') {
    return {
      type: 'adicionado',
      diff_summary: 'novo conteúdo adicionado',
      char_delta: trimDel.length - trimGen.length,
      changed_pct: 100,
    }
  }
  if (trimGen !== '' && trimDel === '') {
    return {
      type: 'removido',
      diff_summary: 'conteúdo removido',
      char_delta: trimDel.length - trimGen.length,
      changed_pct: 100,
    }
  }

  const changes: Change[] = diffWords(trimGen, trimDel)
  const totalTokens = changes.reduce((sum, c) => sum + (c.count ?? 0), 0)
  const changedTokens = changes
    .filter((c) => c.added || c.removed)
    .reduce((sum, c) => sum + (c.count ?? 0), 0)
  const changedPct =
    totalTokens === 0 ? 0 : Math.round((changedTokens / totalTokens) * 100)

  return {
    type: changedPct >= THRESHOLD_PCT ? 'reescrito' : 'corrigido',
    diff_summary: summarizeChanges(changes),
    char_delta: trimDel.length - trimGen.length,
    changed_pct: changedPct,
  }
}

function summarizeChanges(changes: Change[]): string {
  const added: string[] = []
  const removed: string[] = []
  for (const c of changes) {
    if (c.added) added.push(c.value.trim())
    if (c.removed) removed.push(c.value.trim())
  }
  const addedTop = added.slice(0, 3).join(' | ')
  const removedTop = removed.slice(0, 3).join(' | ')
  const parts: string[] = []
  if (addedTop) parts.push(`+ ${addedTop}`)
  if (removedTop) parts.push(`- ${removedTop}`)
  return parts.join(' / ')
}

export interface SectionDiffs {
  edit_diff: Partial<Record<ReportSectionKey, ClassifiedEdit>>
  zonas_editadas: ReportSectionKey[]
  tipo_edicao: EditTipo[]
}

const ALL_REPORT_KEYS: ReportSectionKey[] = [
  '1_constituicao',
  '2_estrutural_fisica',
  '3_indicacoes_sistemicas',
  '4_toxemia',
  '5_psicoemocional',
  '6_cargas_temporais',
  '7_carencias_nutricionais',
  '8_simbolico_espiritual',
  '9_cuidados_integrativos',
  '10_potenciais_forcas',
  '11_afirmacoes_integracao',
  '12_sintese_integrativa',
  '13_mensagem_final',
  'encerramento_disclaimer',
]

/**
 * Iterate through the union of keys present in either report, classify each,
 * and produce the three D-U2 outputs in their canonical jsonb shapes.
 */
export function classifyAllSections(
  generated: ReportJsonb,
  delivered: ReportJsonb,
): SectionDiffs {
  const keys = new Set<ReportSectionKey>()
  for (const k of ALL_REPORT_KEYS) {
    if (k in generated || k in delivered) keys.add(k)
  }

  const editDiff: Partial<Record<ReportSectionKey, ClassifiedEdit>> = {}
  const zonasEditadas: ReportSectionKey[] = []
  const tiposSet = new Set<EditTipo>()

  for (const key of keys) {
    const classified = classifyEdit(generated[key] ?? '', delivered[key] ?? '')
    editDiff[key] = classified
    if (classified.type !== 'none') {
      zonasEditadas.push(key)
      tiposSet.add(classified.type)
    }
  }

  return {
    edit_diff: editDiff,
    zonas_editadas: zonasEditadas,
    tipo_edicao: Array.from(tiposSet),
  }
}
```

**Verificação imediata (GREEN):**
```bash
cd apps/web
pnpm test:run lib/anthropic/__tests__/diff.test.ts
# Esperado: ≥ 12 passed, 0 failed.
pnpm tsc --noEmit
# Esperado: zero novos erros para diff.ts
pnpm audit:vocabulary
# Esperado: exit 0 (diff.ts não cita as palavras proibidas)
```
  </action>
  <verify>
    <automated>cd apps/web && pnpm test:run lib/anthropic/__tests__/diff.test.ts 2>&1 | tee /tmp/diff-green.txt; ! grep -qE "FAIL|×" /tmp/diff-green.txt && echo "OK: diff tests GREEN"; grep -E "passed" /tmp/diff-green.txt    <automated>cd apps/web && pnpm test:run lib/anthropic/__tests__/diff.test.ts 2>&1 | tee /tmp/diff-green.txt; ! grep -qE "FAIL|×" /tmp/diff-green.txt && echo "OK: diff tests GREEN"; grep -E "passed" /tmp/diff-green.txt</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/lib/anthropic/diff.ts` existe, ≥ 100 linhas
    - Primeira import statement: `import 'server-only'`
    - Importa `diffWords` e `type Change` de `'diff'`
    - `THRESHOLD_PCT = 30` constante (D-U2)
    - `classifyEdit` retorna 5 valores possíveis de `type`: none / adicionado / removido / corrigido / reescrito
    - `classifyAllSections` itera sobre 14 chaves canônicas e produz `edit_diff` + `zonas_editadas` + `tipo_edicao` dedup
    - `pnpm test:run lib/anthropic/__tests__/diff.test.ts` exit 0; ≥ 12 passed, 0 failed
    - `pnpm tsc --noEmit` apps/web exit 0
    - `pnpm audit:vocabulary` exit 0
  </acceptance_criteria>
  <done>Diff classifier commitado, 12+ testes GREEN, 3 D-U2 jsonb outputs prontos para Server Action saveReportDelivered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Therapist edited text → diff classifier | Texto user-editable; trim aplica antes de comparar |
| `diff@9` npm package → server-only | Importado em arquivo `'server-only'`; não vaza para bundle do client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-DIFF-DOS | Denial-of-Service | Texto enorme em delivered → diffWords O(N²) | accept | `report_delivered` é cap em max_tokens=16000 do LLM (~64k chars total); diffWords em 4-5KB por seção é negligível; sem hard-limit explícito no schema (jsonb), mas Server Action zod schema pode adicionar |
| T-7-DIFF-CLASSIFY | Tampering | Threshold 30% miscalibrado | accept | D-U2 explícito heurística inicial; founder pode iterar baseado em dogfooding; auditor_version não aplica a diff (esse é audit metadata) |
| T-7-DIFF-DEPS | Malicious Code | npm `diff@9` package | accept | BSD, kpdecker, 10+ anos, zero deps, audited via `pnpm audit` na install; verificado RESEARCH lines 122-126 |
</threat_model>

<verification>
- `diff.ts` declara `import 'server-only'` e importa `diffWords` de `'diff'`
- `classifyEdit` cobre todos os 5 EditTipo + boundary 30%
- `classifyAllSections` produz 3 outputs canônicos D-U2
- 12+ tests GREEN
- `pnpm tsc --noEmit` apps/web exit 0
- `pnpm audit:vocabulary` exit 0
</verification>

<success_criteria>
- Diff classifier server-only com `classifyEdit` + `classifyAllSections`
- D-U2 outputs (edit_diff jsonb + zonas_editadas + tipo_edicao text[]) prontos pra Server Action saveReportDelivered (07-09)
- Threshold 30% testado em 29/30/31% boundaries
- Pronto pra ser consumido pelo Server Action no plano 07-09
</success_criteria>

<output>
After completion, create `.planning/phases/07-analise-llm/07-06-SUMMARY.md` documenting:
1. Lista de cases boundary capturados (29% / 30% / 31%) — qual behavior diffWords produz para cada
2. Performance benchmark: tempo de `classifyAllSections` em report sintético de 14 seções × 1000 chars cada
3. Decisão: o que fazer se Sonnet emitir uma chave fora dos 14 canônicos (planner decide: ignorar OU classify mesmo assim) — atual code IGNORA chaves fora de `ALL_REPORT_KEYS`
</output>
