---
phase: 07-analise-llm
plan: 11
type: execute
wave: 7
depends_on: [07-09, 07-10]
files_modified:
  - apps/web/app/(dashboard)/leituras/page.tsx
autonomous: true
requirements: [LLM-01, LLM-04]
tags: [phase-7, listing, cta-extension]

must_haves:
  truths:
    - "`apps/web/app/(dashboard)/leituras/page.tsx` extends existing Table com nova coluna 'Análise' (5ª coluna)"
    - "CTA dinâmico baseado em (status, report_generated, is_delivered):"
    - "  - status='ready' AND report_generated IS NULL → link 'Gerar análise' → `/leituras/${id}`"
    - "  - status='ready' AND report_generated IS NOT NULL → link 'Ver análise' → `/leituras/${id}`"
    - "  - status='edited' → link 'Continuar editando' → `/leituras/${id}/editar`"
    - "  - is_delivered=true → link 'Ver entregue' → `/leituras/${id}`"
    - "Outros statuses (pending, processing, failed) → renderizar `—` (sem CTA)"
    - "Reuso do pattern `<Link className={cn(buttonVariants({ size: 'sm', variant: 'outline' }))}>` (linha existente ~110)"
    - "Query select extends para incluir `report_generated`, `is_delivered`"
  artifacts:
    - path: "apps/web/app/(dashboard)/leituras/page.tsx"
      provides: "Listing extension with conditional analysis CTA column"
      contains: "Ver análise"
  key_links:
    - from: "apps/web/app/(dashboard)/leituras/page.tsx"
      to: "/leituras/[id] e /leituras/[id]/editar"
      via: "conditional Link rendering por status"
      pattern: "/leituras/\\$\\{.*\\}/editar"
---

<objective>
Estender o listing `/leituras/page.tsx` (já existente da Fase 5) com nova coluna "Análise" — link condicional baseado em (status, report_generated, is_delivered) por UI-SPEC §Surface 3 lines 150-157. Este é o último link para fechar a navegação cíclica: lista → detail → editar → lista.

Purpose: Sem este CTA na listing, terapeuta tem que digitar manualmente `/leituras/[id]` ou clicar no row inteiro. UI-SPEC §Surface 3 já definiu o copy + os 4 estados. Single-file edit, baixo risco.
Output: Page existente com 5ª coluna funcional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-analise-llm/07-UI-SPEC.md
@apps/web/app/(dashboard)/leituras/page.tsx
@apps/web/components/readings/StatusBadge.tsx
@apps/web/components/readings/ReprocessButton.tsx

<interfaces>
<!-- UI-SPEC §Surface 3 lines 150-157 -->
| Element | Copy |
| New action column link (status='ready', report_generated populated) | `Ver análise` |
| New action column link (status='ready', report_generated NULL) | `Gerar análise` |
| New action column link (status='edited') | `Continuar editando` |
| New action column link (is_delivered=true) | `Ver entregue` |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend leituras/page.tsx with Análise column</name>
  <files>apps/web/app/(dashboard)/leituras/page.tsx</files>
  <read_first>
    - `apps/web/app/(dashboard)/leituras/page.tsx` (estado atual COMPLETO antes de editar — preserve all existing structure)
    - `.planning/phases/07-analise-llm/07-UI-SPEC.md` lines 150-157 (Surface 3 copy), lines 270-272 (listing extension layout)
    - `.planning/phases/07-analise-llm/07-PATTERNS.md` lines 752-754 (analog: same file self-edit; reuse `<Link className={cn(buttonVariants({ size: 'sm', variant: 'outline' }))}>` pattern from existing line 110)
  </read_first>
  <action>
**Pré-requisito:** Ler `apps/web/app/(dashboard)/leituras/page.tsx` por completo PRIMEIRO. Localizar:
1. A query select existente — adicionar `report_generated` e `is_delivered` aos campos selecionados
2. A estrutura da Table — descobrir onde renderizar os rows e quais TableHead/TableCell já existem

**Edit:** Adicionar uma nova `TableHead` ao header (depois de qualquer coluna existente) com label `Análise` e uma nova `TableCell` por row com link condicional baseado em:

```typescript
function renderAnalysisLink(reading: {
  id: string
  status: string | null
  report_generated: unknown
  is_delivered: boolean | null
}): React.ReactNode {
  const hasReport = reading.report_generated != null && Object.keys(reading.report_generated as Record<string, unknown>).length > 0
  const isDelivered = reading.is_delivered ?? false
  const status = reading.status

  if (isDelivered) {
    return (
      <Link
        href={`/leituras/${reading.id}`}
        className={cn(buttonVariants({ size: 'sm', variant: 'outline' }))}
      >
        Ver entregue
      </Link>
    )
  }
  if (status === 'edited') {
    return (
      <Link
        href={`/leituras/${reading.id}/editar`}
        className={cn(buttonVariants({ size: 'sm', variant: 'outline' }))}
      >
        Continuar editando
      </Link>
    )
  }
  if (status === 'ready' && hasReport) {
    return (
      <Link
        href={`/leituras/${reading.id}`}
        className={cn(buttonVariants({ size: 'sm', variant: 'outline' }))}
      >
        Ver análise
      </Link>
    )
  }
  if (status === 'ready' && !hasReport) {
    return (
      <Link
        href={`/leituras/${reading.id}`}
        className={cn(buttonVariants({ size: 'sm', variant: 'default' }))}
      >
        Gerar análise
      </Link>
    )
  }
  return <span className="text-sm text-muted-foreground">—</span>
}
```

**Inserir** essa função no escopo do componente da página (server component, OK), e usá-la para preencher a nova `TableCell` de cada row.

**Conventions a manter:**
- Não tocar em outras colunas / ReprocessButton / StatusBadge — são out of scope
- Não modificar a query existente além de ADICIONAR `report_generated, is_delivered` ao select
- Mantém o estilo Tailwind existente (não introduzir novos tokens)
- O resto do arquivo (imports, layout, empty state) permanece IDÊNTICO

**Verificação imediata:**
```bash
cd apps/web
pnpm tsc --noEmit
# Esperado: 0 novos erros
pnpm audit:vocabulary
# Esperado: exit 0
pnpm build
# Smoke build pra garantir RSC compila
```
  </action>
  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit 2>&1 | grep -E "leituras/page\\.tsx.*error" | head -5; pnpm audit:vocabulary 2>&1 | tail -3; node -e "const f=require('fs').readFileSync('apps/web/app/(dashboard)/leituras/page.tsx','utf8'); const checks={'select report_generated':/report_generated/.test(f),'select is_delivered':/is_delivered/.test(f),'Ver análise':/Ver análise/.test(f),'Gerar análise':/Gerar análise/.test(f),'Continuar editando':/Continuar editando/.test(f),'Ver entregue':/Ver entregue/.test(f),'Análise column header':/Análise/.test(f)}; for(const[k,v]of Object.entries(checks))console.log(v?'OK':'FAIL',k); process.exit(Object.values(checks).every(Boolean)?0:1);"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/(dashboard)/leituras/page.tsx` query select inclui `report_generated` e `is_delivered`
    - Page contém literais "Ver análise", "Gerar análise", "Continuar editando", "Ver entregue" (UI-SPEC line 154-157)
    - Page tem nova TableHead "Análise"
    - Função renderAnalysisLink (ou inline equivalent) cobre 4 estados condicionais + fallback `—`
    - Outras colunas / ReprocessButton / StatusBadge inalteradas (verify via `git diff` minimal)
    - `pnpm tsc --noEmit` exit 0
    - `pnpm audit:vocabulary` exit 0
    - `pnpm build` apps/web exit 0 (smoke build OK)
  </acceptance_criteria>
  <done>Listing extension functional; navigational loop fechada (lista ↔ detalhe ↔ editar).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Existing list page → new conditional CTA | Self-edit; preserves RLS via existing query; new fields are reads only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-NAV | Information Disclosure | Link to wrong reading id | mitigate | `reading.id` vem da própria query RLS-protegida; link points to `/leituras/${reading.id}` que tem auth gates próprios (RSC + Route Handler) |
| T-7-LISTING | Tampering | New column breaks existing rows | mitigate | Self-edit preserva structure existing; tsc + build smoke catch regressions |
</threat_model>

<verification>
- Single-file edit em `leituras/page.tsx`
- 4 conditional CTAs implementados com copy literal UI-SPEC
- tsc + audit + build clean
</verification>

<success_criteria>
- Listing tem 5ª coluna "Análise" com 4 estados condicionais
- Navegação completa: `/leituras` → click "Gerar análise" → `/leituras/[id]` (page criada em 07-09) → click "Editar análise" → `/leituras/[id]/editar` (page criada em 07-10) → save → click "Voltar para leituras" → de volta no listing
- Reading entregue → CTA muda para "Ver entregue" (read-only mode no editor 07-10)
</success_criteria>

<output>
After completion, create `.planning/phases/07-analise-llm/07-11-SUMMARY.md` documenting:
1. Diff exato (linhas adicionadas, sem alteração em colunas existentes)
2. 4 estados condicionais cobertos por UAT scenarios em 07-UAT.md (a ser criado em verify-phase)
3. Decisão: fallback `—` para statuses pending/processing/failed (não há CTA de análise se vision pipeline ainda não rodou)
</output>
