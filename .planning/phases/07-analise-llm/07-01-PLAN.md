---
phase: 07-analise-llm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0007_phase_7_analise_llm.sql
  - supabase/tests/0007_jsonb_concat_order.sql
  - apps/web/types/database.ts
autonomous: false
requirements: [LLM-04]
tags: [phase-7, migration, schema, supabase, jsonb, generated-column]
user_setup:
  - service: supabase-cli
    why: "Aplicar migration 0007 no projeto remoto sa-east-1; sem isso o pipeline da Fase 7 não tem onde gravar `report_generated`/`report_delivered` jsonb."
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "supabase login --token <PAT> (Personal Access Token de https://supabase.com/dashboard/account/tokens)"
    dashboard_config:
      - task: "Confirmar que `supabase link --project-ref <ref>` aponta para o projeto correto antes do push"
        location: "supabase/.temp/project-ref"

must_haves:
  truths:
    - "Migration 0007 aplicada com sucesso ao projeto remoto sa-east-1"
    - "`apps/web/types/database.ts` regenerado expõe colunas `report_generated`, `report_delivered`, `audit_metadata`, `regeneration_count`, `regeneration_log`, `report_generated_at`, `report_delivered_at`, `edit_diff`, `zonas_editadas`, `tipo_edicao`, `clinical_feedback`, `exam_notes`, `feedback_collected_at`"
    - "Função `jsonb_concat_sections_pt_br(jsonb)` declarada `IMMUTABLE PARALLEL SAFE` e usada nas colunas `ai_report_raw` / `ai_report_edited` GENERATED ALWAYS AS … STORED"
    - "Smoke SQL `supabase/tests/0007_jsonb_concat_order.sql` prova ordem numérica `1, 2, …, 13, encerramento` (não lex `1, 10, 11, 12, 13, 2, 3, …`)"
    - "Status enum `pending|processing|ready|failed|edited` permanece inalterado (D-P4)"
  artifacts:
    - path: "supabase/migrations/0007_phase_7_analise_llm.sql"
      provides: "DROP+ADD canonical jsonb columns, IMMUTABLE function, GENERATED text columns, 11 forward-compat columns (D-P1, D-P2, D-P3)"
      min_lines: 80
      contains: "IMMUTABLE PARALLEL SAFE"
    - path: "supabase/tests/0007_jsonb_concat_order.sql"
      provides: "Smoke SQL test asserting numeric ordering of sections (Pitfall 1 defense)"
      min_lines: 15
    - path: "apps/web/types/database.ts"
      provides: "Regenerated TypeScript Database type with 11 new columns on `readings`"
      contains: "report_generated"
  key_links:
    - from: "supabase/migrations/0007_phase_7_analise_llm.sql"
      to: "function jsonb_concat_sections_pt_br"
      via: "GENERATED ALWAYS AS (jsonb_concat_sections_pt_br(report_generated)) STORED"
      pattern: "GENERATED ALWAYS AS \\(jsonb_concat_sections_pt_br"
    - from: "apps/web/types/database.ts"
      to: "readings table Row/Insert/Update"
      via: "supabase gen types typescript --linked"
      pattern: "report_generated.*Json"
---

<objective>
Criar e aplicar a migration `0007_phase_7_analise_llm.sql`, que substitui as colunas `ai_report_raw`/`ai_report_edited` (text) por jsonb canônico `report_generated`/`report_delivered` mantendo retrocompat sintática via colunas `GENERATED ALWAYS AS … STORED` reconstruídas a partir do jsonb por uma função `IMMUTABLE PARALLEL SAFE`. Adicionar todos os 11 campos forward-compat da Fase 10 (SAC) na mesma migration. Validar ordenação numérica de seções via smoke SQL. Aplicar a migration ao projeto remoto e regenerar `apps/web/types/database.ts`.

Purpose: Sem esse schema, nenhum código downstream da Fase 7 tem onde gravar dados — Route Handler, Server Actions, RSC do editor e telemetria de regeneração todos dependem dessas colunas. Ordenação numérica errada é um silent corruption que vira `ai_report_raw` com seção 2 depois da 13 (Pitfall 1).
Output: Migration aplicada, types regenerados, smoke SQL verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-analise-llm/07-CONTEXT.md
@.planning/phases/07-analise-llm/07-RESEARCH.md
@.planning/phases/07-analise-llm/07-PATTERNS.md
@.planning/REQUIREMENTS.md
@supabase/migrations/0005_knowledge_chunks_content_hash_and_source_type.sql
@supabase/migrations/0006_match_knowledge_chunks_drop_set_local.sql
@apps/web/types/database.ts

<interfaces>
<!-- Migration style template (idempotent, IMMUTABLE function, header banner) extracted from 0005. -->
From supabase/migrations/0005_knowledge_chunks_content_hash_and_source_type.sql:
```sql
-- Header style: phase + plan + decisions + idempotency clause
alter table <table> add column if not exists <name> <type>;

-- Constraint via DO block (PG version compat):
do $$
begin
  if not exists (select 1 from pg_constraint where conname = '<name>') then
    alter table <table> add constraint <name> ...;
  end if;
end $$;

-- IMMUTABLE function for use in GENERATED column:
create or replace function <name>(<args>)
  returns <type>
  language sql
  immutable
  parallel safe
as $$
  select ...;
$$;

grant execute on function <name>(<args>) to authenticated;
```

From migration 0006 (D-N1 fix learning):
```sql
-- DO NOT use SET LOCAL inside a STABLE/IMMUTABLE function.
-- Postgres rejects volatile features in non-VOLATILE functions.
-- Function bodies must be PURE SELECT (no SET, no procedural side effects).
```

Status enum (existing, do NOT change per D-P4):
'pending' | 'processing' | 'ready' | 'failed' | 'edited'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Authoring migration 0007_phase_7_analise_llm.sql</name>
  <files>supabase/migrations/0007_phase_7_analise_llm.sql</files>
  <read_first>
    - `supabase/migrations/0005_knowledge_chunks_content_hash_and_source_type.sql` (style template for header banner, idempotent ALTER, DO blocks for constraints, IMMUTABLE function with `language sql immutable parallel safe`, `grant execute … to authenticated`)
    - `supabase/migrations/0006_match_knowledge_chunks_drop_set_local.sql` (D-N1 fix: STABLE/IMMUTABLE functions cannot use `SET LOCAL` — function body must be pure SELECT)
    - `.planning/phases/07-analise-llm/07-CONTEXT.md` lines 56-110 (D-P1 schema rebuild, D-P2 11 forward-compat columns, D-P3 jsonb shape, D-P4 status enum unchanged)
    - `.planning/phases/07-analise-llm/07-RESEARCH.md` Pattern 3 lines 555-606 (full migration template), Pitfall 1 lines 689-697 (key ordering bug + numeric cast fix)
    - `.planning/phases/07-analise-llm/07-PATTERNS.md` lines 683-731 (analog 0005, style rules from 0005+0006, idempotency clauses)
  </read_first>
  <action>
Crie `supabase/migrations/0007_phase_7_analise_llm.sql` com EXATAMENTE o conteúdo abaixo (copiar literal — todas as decisões CONTEXT D-P1..P4 já estão refletidas; toda a defesa contra Pitfalls 1 e 11 do RESEARCH está embutida; idempotência seguindo template 0005). Este é o arquivo completo, ASCII apenas onde possível, mas as palavras pt-BR precisam manter acentuação:

```sql
-- supabase/migrations/0007_phase_7_analise_llm.sql
-- Phase 7 — Análise LLM: substitui ai_report_raw/ai_report_edited (text) por
-- jsonb canônico report_generated/report_delivered, mantendo retrocompat
-- sintática via GENERATED ALWAYS AS … STORED columns reconstruídas a partir
-- do jsonb por uma função IMMUTABLE PARALLEL SAFE.
--
-- Adiciona TODOS os 11 campos forward-compat da Fase 10 (SAC) — 07-CONTEXT.md
-- D-P2: report_generated_at, report_delivered_at, edit_diff, zonas_editadas,
-- tipo_edicao, clinical_feedback, exam_notes, feedback_collected_at,
-- audit_metadata, regeneration_count, regeneration_log.
--
-- Idempotência: DROP COLUMN IF EXISTS + ADD COLUMN IF NOT EXISTS + CREATE OR
-- REPLACE FUNCTION. CONTEXT D-P1 confirma: zero linhas existentes têm
-- ai_report_raw/edited populadas (Fase 7 nunca executou).
--
-- Pitfall 1 (RESEARCH): ORDER BY com cast numérico para evitar ordenação
-- lexicográfica '1, 10, 11, 12, 13, 2, 3, …'.
--
-- Migration 0006 learning: funções usadas em GENERATED ALWAYS AS (...) STORED
-- DEVEM ser IMMUTABLE PARALLEL SAFE e NÃO podem usar SET LOCAL.
--
-- 07-PLAN-01 desta fase. Decisões: D-P1, D-P2, D-P3, D-P4.

begin;

-- ============================================================================
-- 1) Função IMMUTABLE jsonb_concat_sections_pt_br
-- Concatena valores do jsonb separados por dupla quebra de linha, ordenados
-- numericamente pelo prefixo '1_', '2_', …, '13_'. NULLS LAST garante que
-- 'encerramento_disclaimer' (sem prefixo numérico) ordene por último.
-- ============================================================================
create or replace function jsonb_concat_sections_pt_br(input jsonb)
  returns text
  language sql
  immutable
  parallel safe
as $$
  select string_agg(value, E'\n\n' order by
    coalesce((regexp_match(key, '^(\d+)_'))[1]::int, 99),
    key
  )
  from jsonb_each_text(input);
$$;

grant execute on function jsonb_concat_sections_pt_br(jsonb) to authenticated;

-- ============================================================================
-- 2) DROP+ADD das colunas ai_report_raw / ai_report_edited
-- One-shot atômico (zero rows affected — D-P1 confirmed). Em transação para
-- evitar estado intermediário com tabela inconsistente.
-- ============================================================================
alter table readings
  drop column if exists ai_report_raw,
  drop column if exists ai_report_edited;

-- ============================================================================
-- 3) Adicionar colunas canônicas jsonb + GENERATED text columns
-- ============================================================================
alter table readings
  add column if not exists report_generated jsonb,
  add column if not exists report_delivered jsonb,
  add column if not exists ai_report_raw text generated always as (
    jsonb_concat_sections_pt_br(report_generated)
  ) stored,
  add column if not exists ai_report_edited text generated always as (
    jsonb_concat_sections_pt_br(report_delivered)
  ) stored;

-- ============================================================================
-- 4) Adicionar 11 campos forward-compat (D-P2 — SAC Fase 10)
-- ============================================================================
alter table readings
  add column if not exists report_generated_at timestamptz,
  add column if not exists report_delivered_at timestamptz,
  add column if not exists edit_diff jsonb,
  add column if not exists zonas_editadas jsonb,
  add column if not exists tipo_edicao text[],
  add column if not exists clinical_feedback jsonb,
  add column if not exists exam_notes text,
  add column if not exists feedback_collected_at timestamptz,
  add column if not exists audit_metadata jsonb default '{}'::jsonb,
  add column if not exists regeneration_count int default 0,
  add column if not exists regeneration_log jsonb default '[]'::jsonb;

-- ============================================================================
-- 5) CHECK em regeneration_count (D-S4 — cap em 3)
-- ============================================================================
do $$
begin
  if not exists (
    select 1 from pg_constraint where conname = 'readings_regeneration_count_cap_check'
  ) then
    alter table readings
      add constraint readings_regeneration_count_cap_check
      check (regeneration_count >= 0 and regeneration_count <= 3);
  end if;
end $$;

commit;
```

**Convenções não-negociáveis**:
- NÃO usar `SET LOCAL` na função (Pitfall 11 do RESEARCH; aprendizado da migration 0006).
- ORDER BY com cast numérico explícito + `NULLS LAST` (Pitfall 1).
- `coalesce(..., 99)` é o sentinel para `encerramento_disclaimer` (sem prefixo numérico) ordenar por último.
- Função declarada `immutable parallel safe` (mandatório para uso em GENERATED ALWAYS AS … STORED, PG15+).
- Transação `begin; … commit;` envolve DROP+ADD para evitar estado inconsistente.
- Idempotência: `if not exists` em CHECK constraint, `if exists`/`if not exists` nas colunas, `create or replace` na função.
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('supabase/migrations/0007_phase_7_analise_llm.sql','utf8'); const checks={'IMMUTABLE PARALLEL SAFE':/immutable\s+parallel safe/i.test(f),'GENERATED jsonb_concat':/generated always as \(jsonb_concat_sections_pt_br/i.test(f),'NULLS LAST or coalesce 99':/coalesce\(\(regexp_match\(key,\s*'\^\(\\d\+\)_'\)\)\[1\]::int,\s*99\)/i.test(f),'12 ADD COLUMN':(f.match(/add column if not exists/gi)||[]).length>=14,'no SET LOCAL':!/set local/i.test(f),'transaction':/begin;[\s\S]*commit;/i.test(f),'cap CHECK':/regeneration_count\s*>=\s*0\s+and\s+regeneration_count\s*<=\s*3/i.test(f)}; for (const [k,v] of Object.entries(checks)) console.log(v?'OK':'FAIL', k); process.exit(Object.values(checks).every(Boolean)?0:1);"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `supabase/migrations/0007_phase_7_analise_llm.sql` existe e tem ≥ 80 linhas
    - `grep -c "IMMUTABLE PARALLEL SAFE\\|immutable.*parallel safe" supabase/migrations/0007_phase_7_analise_llm.sql` retorna ≥ 1 (case-insensitive)
    - `grep -c "GENERATED ALWAYS AS (jsonb_concat_sections_pt_br" supabase/migrations/0007_phase_7_analise_llm.sql` retorna ≥ 2 (uma para ai_report_raw, uma para ai_report_edited)
    - `grep -c "add column if not exists" supabase/migrations/0007_phase_7_analise_llm.sql` retorna ≥ 14 (2 jsonb canônicos + 2 GENERATED + 11 forward-compat - 1 que já é a função, na verdade 4+11=15 mínimo; aceitar ≥14)
    - `grep -c "set local" supabase/migrations/0007_phase_7_analise_llm.sql` retorna 0 (case-insensitive)
    - `grep -E "begin;.*commit;" supabase/migrations/0007_phase_7_analise_llm.sql` retorna match (a migration está em transação)
    - O arquivo contém literal `coalesce((regexp_match(key, '^(\d+)_'))[1]::int, 99)` (Pitfall 1 defense)
    - O arquivo contém CHECK `regeneration_count >= 0 and regeneration_count <= 3` (D-S4 cap)
    - Migration NÃO altera o enum status (verificar via `grep -i "create type" supabase/migrations/0007_phase_7_analise_llm.sql` retorna 0)
  </acceptance_criteria>
  <done>Migration file commitada com header banner, função IMMUTABLE, DROP+ADD em transação, 4 colunas canônicas + 11 forward-compat, CHECK cap em regeneration_count, todos os checks de acceptance verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Smoke SQL test for jsonb_concat_sections_pt_br ordering (Pitfall 1 defense)</name>
  <files>supabase/tests/0007_jsonb_concat_order.sql</files>
  <read_first>
    - `supabase/migrations/0007_phase_7_analise_llm.sql` (criado na Task 1 — função sob teste)
    - `.planning/phases/07-analise-llm/07-RESEARCH.md` Pitfall 1 lines 689-697 (lex order bug: `'13_'` < `'2_'` porque `'1' < '2'` char-wise)
    - `.planning/phases/07-analise-llm/07-VALIDATION.md` line 65 (`supabase/migrations/__tests__/0007_smoke.sql` — Wave 0 requirement)
  </read_first>
  <behavior>
    - Test 1: jsonb com chaves `13_x`, `2_y`, `1_z`, `encerramento_disclaimer` produz string concatenada na ORDEM `1_z`, `2_y`, `13_x`, `encerramento_disclaimer` (NÃO lex `1_z, 13_x, 2_y, encerramento`).
    - Test 2: jsonb com 14 chaves canônicas (`1_constituicao`…`13_mensagem_final` + `encerramento_disclaimer`) produz exatamente 14 valores separados por `\n\n`, na ordem 1..13 + encerramento.
    - Test 3: Função aceita jsonb vazio `'{}'::jsonb` e retorna NULL (sem erro).
  </behavior>
  <action>
Crie `supabase/tests/0007_jsonb_concat_order.sql` com smoke tests usando `psql` `\echo` para visibilidade humana e DO blocks com `assert`. Conteúdo verbatim:

```sql
-- supabase/tests/0007_jsonb_concat_order.sql
-- Smoke SQL test for jsonb_concat_sections_pt_br ordering invariant.
-- Pitfall 1 (07-RESEARCH.md): lexicographic ordering puts '13_' BEFORE '2_'
-- because '1' < '2' char-wise. The function must use numeric cast on the
-- prefix to produce the expected order 1, 2, …, 13, encerramento.
--
-- Run: psql "$DATABASE_URL" -f supabase/tests/0007_jsonb_concat_order.sql
-- Expected: all DO blocks complete without RAISE EXCEPTION.

\echo 'Test 1: numeric ordering 1_z, 2_y, 13_x, encerramento'
do $$
declare
  result text;
  expected text := 'a' || E'\n\n' || 'b' || E'\n\n' || 'c' || E'\n\n' || 'd';
begin
  select jsonb_concat_sections_pt_br(jsonb_build_object(
    '13_x', 'c',
    '2_y', 'b',
    '1_z', 'a',
    'encerramento_disclaimer', 'd'
  )) into result;
  if result is distinct from expected then
    raise exception 'Test 1 FAIL: expected % got %', expected, result;
  end if;
  raise notice 'Test 1 PASS';
end $$;

\echo 'Test 2: full 14-key canonical shape'
do $$
declare
  input jsonb;
  result text;
  expected_first text := 'um';
  expected_last text := 'fim';
  parts text[];
begin
  input := jsonb_build_object(
    '1_constituicao', 'um',
    '2_estrutural_fisica', 'dois',
    '3_indicacoes_sistemicas', 'tres',
    '4_toxemia', 'quatro',
    '5_psicoemocional', 'cinco',
    '6_cargas_temporais', 'seis',
    '7_carencias_nutricionais', 'sete',
    '8_simbolico_espiritual', 'oito',
    '9_cuidados_integrativos', 'nove',
    '10_potenciais_forcas', 'dez',
    '11_afirmacoes_integracao', 'onze',
    '12_sintese_integrativa', 'doze',
    '13_mensagem_final', 'treze',
    'encerramento_disclaimer', 'fim'
  );
  select jsonb_concat_sections_pt_br(input) into result;
  parts := string_to_array(result, E'\n\n');
  if array_length(parts, 1) <> 14 then
    raise exception 'Test 2 FAIL: expected 14 parts, got %', array_length(parts, 1);
  end if;
  if parts[1] is distinct from expected_first then
    raise exception 'Test 2 FAIL: first element should be % got %', expected_first, parts[1];
  end if;
  if parts[14] is distinct from expected_last then
    raise exception 'Test 2 FAIL: last element should be % got %', expected_last, parts[14];
  end if;
  raise notice 'Test 2 PASS (14 parts, first=%, last=%)', parts[1], parts[14];
end $$;

\echo 'Test 3: empty jsonb returns NULL (string_agg over zero rows)'
do $$
declare
  result text;
begin
  select jsonb_concat_sections_pt_br('{}'::jsonb) into result;
  if result is not null then
    raise exception 'Test 3 FAIL: expected NULL, got %', result;
  end if;
  raise notice 'Test 3 PASS';
end $$;

\echo 'All smoke tests passed.'
```

Esse arquivo é executado APÓS o supabase db push da Task 3, contra o banco remoto, via psql. Não use Vitest aqui — é SQL puro contra Postgres real.
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('supabase/tests/0007_jsonb_concat_order.sql','utf8'); const checks={'3 DO blocks':(f.match(/^do \$\$/gm)||[]).length===3,'expected first=um':/expected_first text\s*:=\s*'um'/.test(f),'expected last=fim':/expected_last text\s*:=\s*'fim'/.test(f),'numeric order test':/'13_x'.*'c'/.test(f) && /'2_y'.*'b'/.test(f) && /'1_z'.*'a'/.test(f),'14-key full shape':/'13_mensagem_final'/.test(f) && /'encerramento_disclaimer'/.test(f),'empty jsonb test':/'\{\}'::jsonb/.test(f)}; for(const[k,v]of Object.entries(checks))console.log(v?'OK':'FAIL',k); process.exit(Object.values(checks).every(Boolean)?0:1);"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `supabase/tests/0007_jsonb_concat_order.sql` existe e tem ≥ 60 linhas
    - Contém 3 blocos `DO $$ … $$` (test 1 numeric ordering, test 2 full 14-key shape, test 3 empty input)
    - Test 1 espera ordem `a\n\nb\n\nc\n\nd` (não a-d em ordem lexicográfica errada)
    - Test 2 enumera todas as 14 chaves canônicas (`1_constituicao`…`13_mensagem_final` + `encerramento_disclaimer`)
    - Test 3 verifica que jsonb vazio retorna NULL sem erro
    - `grep -c "raise exception" supabase/tests/0007_jsonb_concat_order.sql` retorna ≥ 3 (cada DO block tem exception em caso de fail)
  </acceptance_criteria>
  <done>Smoke SQL committed; pode ser executado via `psql -f supabase/tests/0007_jsonb_concat_order.sql` após o db push da Task 3.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Apply migration 0007 to remote Supabase + regenerate types + run smoke SQL</name>
  <files>apps/web/types/database.ts</files>
  <read_first>
    - `apps/web/package.json` (verifica que `gen:types` script existe: `supabase gen types typescript --linked --schema public > types/database.ts`)
    - `supabase/migrations/0007_phase_7_analise_llm.sql` (Task 1)
    - `supabase/tests/0007_jsonb_concat_order.sql` (Task 2)
  </read_first>
  <what-built>
    Migration file `0007_phase_7_analise_llm.sql` committed (Task 1) e smoke SQL `0007_jsonb_concat_order.sql` committed (Task 2). O agente NÃO aplicou ainda no banco remoto — esse é o gate humano (D-X push schema requirement do orchestrator).
  </what-built>
  <how-to-verify>
**O fundador executa estes comandos exatamente nesta ordem (a partir de `D:\Projetos\Iridologista`):**

1. **Aplicar migration ao remoto:**
   ```powershell
   supabase db push --linked
   ```
   Esperado: output `Applied migration 20260508_phase_7_analise_llm.sql` (ou nome similar; CLI pode normalizar). Se `supabase db push` perguntar confirmação, responder `Y`.

2. **Regenerar types TypeScript:**
   ```powershell
   pnpm --filter @iridologista/web gen:types
   ```
   Esperado: arquivo `apps/web/types/database.ts` modificado. `git diff apps/web/types/database.ts | head -100` deve mostrar adições contendo `report_generated: Json | null`, `report_delivered: Json | null`, `audit_metadata: Json | null`, `regeneration_count: number | null`, `regeneration_log: Json | null`, `ai_report_raw: string | null` (GENERATED), `ai_report_edited: string | null` (GENERATED), e os 7 campos forward-compat restantes.

3. **Executar smoke SQL contra o banco remoto:**
   ```powershell
   psql "$env:DATABASE_URL" -f supabase/tests/0007_jsonb_concat_order.sql
   ```
   Esperado: três `NOTICE: Test N PASS` + final `All smoke tests passed.`. Se algum DO block emitir `EXCEPTION`, a migration tem bug de ordenação — ABORT.

4. **Verificar TypeScript compila com os novos types:**
   ```powershell
   cd apps/web; pnpm tsc --noEmit
   ```
   Esperado: zero erros novos relacionados a `readings`. Erros pré-existentes da Fase 5/3 (documentados em STATE.md "Itens diferidos") podem permanecer.

5. **Commit das types regeneradas:**
   ```powershell
   git add apps/web/types/database.ts
   git commit -m "chore(07-01): regenerate types after migration 0007"
   ```

**Se algo falhar:**
- `supabase db push` falhar com erro de sintaxe SQL → fix Task 1 + recomitir + re-run.
- Smoke SQL `Test 1 FAIL` → função `jsonb_concat_sections_pt_br` não usa numeric cast — fix Task 1 (verificar que `coalesce((regexp_match(key, '^(\d+)_'))[1]::int, 99)` está presente).
- `gen:types` produzir output vazio → CLI não conseguiu autenticar; rodar `supabase login` primeiro.
  </how-to-verify>
  <resume-signal>
Type "applied" se migration aplicada + types regenerados + smoke SQL todo PASS + TS compila + commit das types feito. Caso contrário, descrever o erro exato (output do comando + nome do passo que falhou).
  </resume-signal>
  <acceptance_criteria>
    - `apps/web/types/database.ts` (versão pós-regen) contém substring literal `report_generated: Json` (case-sensitive)
    - `apps/web/types/database.ts` contém substring literal `report_delivered: Json`
    - `apps/web/types/database.ts` contém substring literal `audit_metadata: Json`
    - `apps/web/types/database.ts` contém substring literal `regeneration_count: number`
    - `apps/web/types/database.ts` contém substring literal `regeneration_log: Json`
    - `apps/web/types/database.ts` contém substring literal `ai_report_raw: string` (coluna GENERATED ainda existe sintaticamente)
    - `apps/web/types/database.ts` contém substring literal `ai_report_edited: string`
    - `pnpm --filter @iridologista/web tsc --noEmit` exit 0 (ou apenas erros pré-existentes em Phase 5/3 documentados)
    - Founder confirms `supabase db push --linked` exit 0 e `psql -f supabase/tests/0007_jsonb_concat_order.sql` mostra 3× `Test N PASS` + `All smoke tests passed`
  </acceptance_criteria>
  <done>Migration aplicada ao remoto sa-east-1, `apps/web/types/database.ts` regenerado e commitado, smoke SQL verde. A partir desse ponto, código downstream pode importar `Database['public']['Tables']['readings']['Row']` com os novos campos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration → live database | DDL irreversível (DROP COLUMN não pode ser desfeita sem perder dados) |
| `gen:types` CLI → repo file | CLI lê schema do remoto e sobrescreve `apps/web/types/database.ts` |
| GENERATED column → IMMUTABLE function | Postgres recalcula coluna automaticamente; função PURA é mandatório |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-SCHEMA | Tampering | `jsonb_concat_sections_pt_br` IMMUTABLE function | mitigate | Função é `immutable parallel safe` (Postgres rejeita expressão volátil em GENERATED column desde PG15+); body é puro SELECT sem `SET LOCAL` (aprendizado migration 0006); smoke SQL prova ordenação numérica antes de qualquer code path consumir |
| T-7-SCHEMA-LEX | Tampering | jsonb key ordering | mitigate | Pitfall 1 RESEARCH: `coalesce((regexp_match(key, '^(\d+)_'))[1]::int, 99)` força ordem numérica; smoke SQL Test 1 prova `'13_x'` ordena APÓS `'2_y'` (não antes como em ordem lex); Test 2 valida full 14-key shape |
| T-7-COST | Information Disclosure | `regeneration_count` cap | mitigate | CHECK constraint `regeneration_count >= 0 AND regeneration_count <= 3` no schema; defesa em profundidade contra Route Handler com bug que permita >3; sem essa CHECK, malicious bypass do guard server pode drenar créditos Anthropic |
| T-7-DATA-LOSS | Tampering | DROP COLUMN ai_report_raw/edited | accept | D-P1 confirma zero rows existentes; transação `begin; … commit;` evita estado intermediário; tabela retorna ao estado consistente em qualquer falha (atomicidade Postgres); risco mitigado pela natureza one-shot da migration |
| T-7-IDEMPOTENCY | Tampering | Re-run accidental | mitigate | `add column if not exists`, `drop column if exists`, `create or replace function`, DO block com `if not exists` em CHECK constraint; migration pode ser re-aplicada sem erro (mesmo padrão das migrations 0004-0006) |
</threat_model>

<verification>
- Migration file existe, ≥ 80 linhas, todos os 6 acceptance gates da Task 1 passam
- Smoke SQL existe, ≥ 60 linhas, todos os 6 acceptance gates da Task 2 passam
- Founder aplicou migration ao remoto e regenerou types: 7 substrings literais presentes em `apps/web/types/database.ts`
- `pnpm tsc --noEmit` retorna 0 erros novos
- Smoke SQL output `All smoke tests passed.` (Pitfall 1 defesa real-world verde)
</verification>

<success_criteria>
- Migration 0007 aplicada ao projeto Supabase remoto
- `apps/web/types/database.ts` regenerado contém todos os novos campos do `readings` table
- Função `jsonb_concat_sections_pt_br` ordena chaves numericamente (1..13 + encerramento), não lexicograficamente
- Status enum (`pending|processing|ready|failed|edited`) inalterado (D-P4)
- CHECK constraint `regeneration_count >= 0 AND <= 3` existe no schema
- Todos os planos downstream (07-03 em diante) podem importar tipos `Database['public']['Tables']['readings']['Row']` com os novos campos como source of truth
</success_criteria>

<output>
After completion, create `.planning/phases/07-analise-llm/07-01-SUMMARY.md` documenting:
1. Comando exato usado pelo founder em `supabase db push --linked` (com timestamp)
2. Diff resumo de `apps/web/types/database.ts` (qual versão antes / depois — listar 11+2+2 campos novos confirmados)
3. Output literal das 3 NOTICE lines do smoke SQL
4. Confirmação que `regeneration_count` CHECK constraint está ativa (`SELECT conname, consrc FROM pg_constraint WHERE conrelid = 'readings'::regclass`)
</output>
