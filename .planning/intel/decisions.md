# Decisions

No ADR documents were classified in this ingest. This file is intentionally empty pending future ADR ingestion.

The SPEC document (`SPEC.md`) contains technology selections that are stack-shaping decisions (e.g., Next.js 15, Supabase Postgres + pgvector, Modal.com for vision, Claude Sonnet 4.6, Voyage AI embeddings). These are recorded as constraints in `constraints.md` rather than as locked ADR decisions, because the source classification is `SPEC` with `locked: false` and no per-doc precedence override.

If these technology selections need to become locked architectural decisions, capture them in dedicated ADRs and re-run ingest.
