# Ingest Conflicts Report

Generated by gsd-doc-synthesizer.
Mode: new (net-new bootstrap; no existing `.planning/` context to reconcile against).
Source set: 1 document (1 SPEC).
Precedence applied: ADR > SPEC > PRD > DOC (default).

## Conflict Detection Report

### BLOCKERS (0)

None. Single-document ingest cannot produce LOCKED-vs-LOCKED contradictions, and no `UNKNOWN` low-confidence classifications were present. The cross-reference graph is acyclic (the SPEC references implementation paths that do not classify back to other planning docs).

### WARNINGS (0)

None. Single-document ingest cannot produce competing acceptance variants because there is no second PRD to disagree with. No PRDs were classified at all in this ingest, so requirement-level conflict detection is not exercised.

### INFO (0)

None. With a single SPEC and no ADR/PRD set above or below it in precedence, no auto-resolution was triggered.

---

## Notes for downstream consumers

- All synthesized content traces to a single source: `D:/GDrive/iridologista/SPEC.md` (classification `SPEC-md-a3f2b1c8.json`, confidence high, manifest_override true, locked false).
- `decisions.md` is intentionally near-empty; promote SPEC technology selections to ADRs if they need lock semantics.
- `requirements.md` is intentionally near-empty; the candidate `REQ-*` IDs listed there are inferred from the SPEC's roadmap and require formal PRD ratification before becoming binding.
- Because there is no PRD layer and no ADR layer, the precedence chain in this ingest collapses to "SPEC is authoritative", and the constraints in `constraints.md` should be treated as the primary planning input for `gsd-roadmapper`.
